openapi: 3.0.0
info:
  version: "2021-10-26"
  title: AWS Migration Hub Refactor Spaces
  description: AWS Migration Hub Refactor Spaces
  x-serviceName: refactor-spaces
  x-serviceAlias: migration_hub_refactor_spaces
  x-protocol: rest-json
  x-apiVersion: "2021-10-26"
  x-endpointPrefix: refactor-spaces
  x-jsonVersion: "1.1"
servers:
  - url: https://refactor-spaces.{region}.amazonaws.com
    variables:
      region:
        default: us-east-1
        description: AWS region
    description: Regional endpoint for migration-hub-refactor-spaces
paths:
  /environments/{environment_identifier}/applications:
    post:
      operationId: CreateApplication
      description: Creates an Amazon Web Services Migration Hub Refactor Spaces application. The account that owns the environment also owns the applications created inside the environment, regardless of the account that creates the application. Refactor Spaces provisions an Amazon API Gateway, API Gateway VPC link, and Network Load Balancer for the application proxy inside your account. In environments created with a CreateEnvironment:NetworkFabricType of NONE you need to configure VPC to VPC connectivity between your service VPC and the application proxy VPC to route traffic through the application proxy to a service with a private URL endpoint. For more information, see Create an application in the Refactor Spaces User Guide.
      parameters:
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ApiGatewayProxy:
                  $ref: "#/components/schemas/ApiGatewayProxyInput"
                ClientToken:
                  $ref: "#/components/schemas/ClientToken"
                Name:
                  $ref: "#/components/schemas/ApplicationName"
                ProxyType:
                  $ref: "#/components/schemas/ProxyType"
                Tags:
                  $ref: "#/components/schemas/TagMap"
                VpcId:
                  $ref: "#/components/schemas/VpcId"
              required:
                - ProxyType
                - VpcId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateApplicationResponse"
    get:
      operationId: ListApplications
      description: Lists all the Amazon Web Services Migration Hub Refactor Spaces applications within an environment.
      parameters:
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListApplicationsResponse"
  /environments:
    post:
      operationId: CreateEnvironment
      description: Creates an Amazon Web Services Migration Hub Refactor Spaces environment. The caller owns the environment resource, and all Refactor Spaces applications, services, and routes created within the environment. They are referred to as the environment owner. The environment owner has cross-account visibility and control of Refactor Spaces resources that are added to the environment by other accounts that the environment is shared with. When creating an environment with a CreateEnvironment:NetworkFabricType of TRANSIT_GATEWAY, Refactor Spaces provisions a transit gateway to enable services in VPCs to communicate directly across accounts. If CreateEnvironment:NetworkFabricType is NONE, Refactor Spaces does not create a transit gateway and you must use your network infrastructure to route traffic to services with private URL endpoints.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ClientToken:
                  $ref: "#/components/schemas/ClientToken"
                Description:
                  $ref: "#/components/schemas/Description"
                Name:
                  $ref: "#/components/schemas/EnvironmentName"
                NetworkFabricType:
                  $ref: "#/components/schemas/NetworkFabricType"
                Tags:
                  $ref: "#/components/schemas/TagMap"
              required:
                - NetworkFabricType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateEnvironmentResponse"
    get:
      operationId: ListEnvironments
      description: Lists Amazon Web Services Migration Hub Refactor Spaces environments owned by a caller account or shared with the caller account.
      parameters:
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListEnvironmentsResponse"
  /environments/{environment_identifier}/applications/{application_identifier}/routes:
    post:
      operationId: CreateRoute
      description: "Creates an Amazon Web Services Migration Hub Refactor Spaces route. The account owner of the service resource is always the environment owner, regardless of which account creates the route. Routes target a service in the application. If an application does not have any routes, then the first route must be created as a DEFAULT RouteType. When created, the default route defaults to an active state so state is not a required input. However, like all other state values the state of the default route can be updated after creation, but only when all other routes are also inactive. Conversely, no route can be active without the default route also being active. When you create a route, Refactor Spaces configures the Amazon API Gateway to send traffic to the target service as follows: URL Endpoints If the service has a URL endpoint, and the endpoint resolves to a private IP address, Refactor Spaces routes traffic using the API Gateway VPC link. If a service endpoint resolves to a public IP address, Refactor Spaces routes traffic over the public internet. Services can have HTTP or HTTPS URL endpoints. For HTTPS URLs, publicly-signed certificates are supported. Private Certificate Authorities (CAs) are permitted only if the CA's domain is also publicly resolvable. Refactor Spaces automatically resolves the public Domain Name System (DNS) names that are set in CreateService:UrlEndpoint when you create a service. The DNS names resolve when the DNS time-to-live (TTL) expires, or every 60 seconds for TTLs less than 60 seconds. This periodic DNS resolution ensures that the route configuration remains up-to-date. One-time health check A one-time health check is performed on the service when either the route is updated from inactive to active, or when it is created with an active state. If the health check fails, the route transitions the route state to FAILED, an error code of SERVICE_ENDPOINT_HEALTH_CHECK_FAILURE is provided, and no traffic is sent to the service. For private URLs, a target group is created on the Network Load Balancer and the load balancer target group runs default target health checks. By default, the health check is run against the service endpoint URL. Optionally, the health check can be performed against a different protocol, port, and/or path using the CreateService:UrlEndpoint parameter. All other health check settings for the load balancer use the default values described in the Health checks for your target groups in the Elastic Load Balancing guide. The health check is considered successful if at least one target within the target group transitions to a healthy state. Lambda function endpoints If the service has an Lambda function endpoint, then Refactor Spaces configures the Lambda function's resource policy to allow the application's API Gateway to invoke the function. The Lambda function state is checked. If the function is not active, the function configuration is updated so that Lambda resources are provisioned. If the Lambda state is Failed, then the route creation fails. For more information, see the GetFunctionConfiguration's State response parameter in the Lambda Developer Guide. A check is performed to determine that a Lambda function with the specified ARN exists. If it does not exist, the health check fails. For public URLs, a connection is opened to the public endpoint. If the URL is not reachable, the health check fails. Environments without a network bridge When you create environments without a network bridge (CreateEnvironment:NetworkFabricType is NONE) and you use your own networking infrastructure, you need to configure VPC to VPC connectivity between your network and the application proxy VPC. Route creation from the application proxy to service endpoints will fail if your network is not configured to connect to the application proxy VPC. For more information, see Create a route in the Refactor Spaces User Guide."
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application within which the route is being created.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment in which the route is created.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ClientToken:
                  $ref: "#/components/schemas/ClientToken"
                DefaultRoute:
                  $ref: "#/components/schemas/DefaultRouteInput"
                RouteType:
                  $ref: "#/components/schemas/RouteType"
                ServiceIdentifier:
                  $ref: "#/components/schemas/ServiceId"
                Tags:
                  $ref: "#/components/schemas/TagMap"
                UriPathRoute:
                  $ref: "#/components/schemas/UriPathRouteInput"
              required:
                - RouteType
                - ServiceIdentifier
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateRouteResponse"
    get:
      operationId: ListRoutes
      description: Lists all the Amazon Web Services Migration Hub Refactor Spaces routes within an application.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRoutesResponse"
  /environments/{environment_identifier}/applications/{application_identifier}/services:
    post:
      operationId: CreateService
      description: Creates an Amazon Web Services Migration Hub Refactor Spaces service. The account owner of the service is always the environment owner, regardless of which account in the environment creates the service. Services have either a URL endpoint in a virtual private cloud (VPC), or a Lambda function endpoint. If an Amazon Web Services resource is launched in a service VPC, and you want it to be accessible to all of an environment’s services with VPCs and routes, apply the RefactorSpacesSecurityGroup to the resource. Alternatively, to add more cross-account constraints, apply your own security group.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application which the service is created.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment in which the service is created.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ClientToken:
                  $ref: "#/components/schemas/ClientToken"
                Description:
                  $ref: "#/components/schemas/Description"
                EndpointType:
                  $ref: "#/components/schemas/ServiceEndpointType"
                LambdaEndpoint:
                  $ref: "#/components/schemas/LambdaEndpointInput"
                Name:
                  $ref: "#/components/schemas/ServiceName"
                Tags:
                  $ref: "#/components/schemas/TagMap"
                UrlEndpoint:
                  $ref: "#/components/schemas/UrlEndpointInput"
                VpcId:
                  $ref: "#/components/schemas/VpcId"
              required:
                - EndpointType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateServiceResponse"
    get:
      operationId: ListServices
      description: Lists all the Amazon Web Services Migration Hub Refactor Spaces services within an application.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListServicesResponse"
  /environments/{environment_identifier}/applications/{application_identifier}:
    delete:
      operationId: DeleteApplication
      description: Deletes an Amazon Web Services Migration Hub Refactor Spaces application. Before you can delete an application, you must first delete any services or routes within the application.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteApplicationResponse"
    get:
      operationId: GetApplication
      description: Gets an Amazon Web Services Migration Hub Refactor Spaces application.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetApplicationResponse"
  /environments/{environment_identifier}:
    delete:
      operationId: DeleteEnvironment
      description: Deletes an Amazon Web Services Migration Hub Refactor Spaces environment. Before you can delete an environment, you must first delete any applications and services within the environment.
      parameters:
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteEnvironmentResponse"
    get:
      operationId: GetEnvironment
      description: Gets an Amazon Web Services Migration Hub Refactor Spaces environment.
      parameters:
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetEnvironmentResponse"
  /resourcepolicy/{identifier}:
    delete:
      operationId: DeleteResourcePolicy
      description: Deletes the resource policy set for the environment.
      parameters:
        - name: identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ResourcePolicyIdentifier"
          description: Amazon Resource Name (ARN) of the resource associated with the policy.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResourcePolicyResponse"
    get:
      operationId: GetResourcePolicy
      description: Gets the resource-based permission policy that is set for the given environment.
      parameters:
        - name: identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ResourcePolicyIdentifier"
          description: The Amazon Resource Name (ARN) of the resource associated with the policy.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetResourcePolicyResponse"
  /environments/{environment_identifier}/applications/{application_identifier}/routes/{route_identifier}:
    delete:
      operationId: DeleteRoute
      description: Deletes an Amazon Web Services Migration Hub Refactor Spaces route.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application to delete the route from.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment to delete the route from.
        - name: route_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/RouteId"
          description: The ID of the route to delete.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteRouteResponse"
    get:
      operationId: GetRoute
      description: Gets an Amazon Web Services Migration Hub Refactor Spaces route.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
        - name: route_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/RouteId"
          description: The ID of the route.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetRouteResponse"
    patch:
      operationId: UpdateRoute
      description: Updates an Amazon Web Services Migration Hub Refactor Spaces route.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application within which the route is being updated.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment in which the route is being updated.
        - name: route_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/RouteId"
          description: The unique identifier of the route to update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ActivationState:
                  $ref: "#/components/schemas/RouteActivationState"
              required:
                - ActivationState
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateRouteResponse"
  /environments/{environment_identifier}/applications/{application_identifier}/services/{service_identifier}:
    delete:
      operationId: DeleteService
      description: Deletes an Amazon Web Services Migration Hub Refactor Spaces service.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: Deletes a Refactor Spaces service. The RefactorSpacesSecurityGroup security group must be removed from all Amazon Web Services resources in the virtual private cloud (VPC) prior to deleting a service with a URL endpoint in a VPC.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment that the service is in.
        - name: service_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ServiceId"
          description: The ID of the service to delete.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteServiceResponse"
    get:
      operationId: GetService
      description: Gets an Amazon Web Services Migration Hub Refactor Spaces service.
      parameters:
        - name: application_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
        - name: service_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ServiceId"
          description: The ID of the service.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetServiceResponse"
  /environments/{environment_identifier}/vpcs:
    get:
      operationId: ListEnvironmentVpcs
      description: Lists all Amazon Web Services Migration Hub Refactor Spaces service virtual private clouds (VPCs) that are part of the environment.
      parameters:
        - name: environment_identifier
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment.
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListEnvironmentVpcsResponse"
  /tags/{resource_arn}:
    get:
      operationId: ListTagsForResource
      description: Lists the tags of a resource. The caller account must be the same as the resource’s OwnerAccountId. Listing tags in other accounts is not supported.
      parameters:
        - name: resource_arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListTagsForResourceResponse"
    post:
      operationId: TagResource
      description: Removes the tags of a given resource. Tags are metadata which can be used to manage a resource. To tag a resource, the caller account must be the same as the resource’s OwnerAccountId. Tagging resources in other accounts is not supported. Amazon Web Services Migration Hub Refactor Spaces does not propagate tags to orchestrated resources, such as an environment’s transit gateway.
      parameters:
        - name: resource_arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the resource.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Tags:
                  $ref: "#/components/schemas/TagMap"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagResourceResponse"
    delete:
      operationId: UntagResource
      description: Adds to or modifies the tags of the given resource. Tags are metadata which can be used to manage a resource. To untag a resource, the caller account must be the same as the resource’s OwnerAccountId. Untagging resources across accounts is not supported.
      parameters:
        - name: resource_arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the resource.
        - name: tagKeys
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/TagKeys"
          description: The list of keys of the tags to be removed from the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UntagResourceResponse"
  /resourcepolicy:
    put:
      operationId: PutResourcePolicy
      description: Attaches a resource-based permission policy to the Amazon Web Services Migration Hub Refactor Spaces environment. The policy must contain the same actions and condition statements as the arn:aws:ram::aws:permission/AWSRAMDefaultPermissionRefactorSpacesEnvironment permission in Resource Access Manager. The policy must not contain new lines or blank lines.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Policy:
                  $ref: "#/components/schemas/PolicyString"
                ResourceArn:
                  $ref: "#/components/schemas/ResourceArn"
              required:
                - ResourceArn
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PutResourcePolicyResponse"
components:
  parameters:
    X-Amz-Content-Sha256:
      in: header
      name: X-Amz-Content-Sha256
      required: false
      schema:
        type: string
    X-Amz-Date:
      in: header
      name: X-Amz-Date
      required: false
      schema:
        type: string
    X-Amz-Algorithm:
      in: header
      name: X-Amz-Algorithm
      required: false
      schema:
        type: string
    X-Amz-Credential:
      in: header
      name: X-Amz-Credential
      required: false
      schema:
        type: string
    X-Amz-Security-Token:
      in: header
      name: X-Amz-Security-Token
      required: false
      schema:
        type: string
    X-Amz-Signature:
      in: header
      name: X-Amz-Signature
      required: false
      schema:
        type: string
    X-Amz-SignedHeaders:
      in: header
      name: X-Amz-SignedHeaders
      required: false
      schema:
        type: string
  schemas:
    EnvironmentId:
      type: string
      minLength: 14
      maxLength: 14
      pattern: ^env-[0-9A-Za-z]{10}$
    ApiGatewayProxyInput:
      description: A wrapper object holding the Amazon API Gateway endpoint input.
      type: object
      properties:
        EndpointType:
          $ref: "#/components/schemas/ApiGatewayEndpointType"
          description: The type of endpoint to use for the API Gateway proxy. If no value is specified in the request, the value is set to REGIONAL by default. If the value is set to PRIVATE in the request, this creates a private API endpoint that is isolated from the public internet. The private endpoint can only be accessed by using Amazon Virtual Private Cloud (Amazon VPC) interface endpoints for the Amazon API Gateway that has been granted access. For more information about creating a private connection with Refactor Spaces and interface endpoint (Amazon Web Services PrivateLink) availability, see Access Refactor Spaces using an interface endpoint (Amazon Web Services PrivateLink).
        StageName:
          $ref: "#/components/schemas/StageName"
          description: The name of the API Gateway stage. The name defaults to prod.
    ApiGatewayEndpointType:
      type: string
      enum:
        - REGIONAL
        - PRIVATE
    StageName:
      type: string
      minLength: 1
      maxLength: 128
      pattern: ^[-a-zA-Z0-9_]*$
    ClientToken:
      type: string
      minLength: 1
      maxLength: 64
      pattern: ^[\x20-\x7E]{1,64}$
    ApplicationName:
      type: string
      minLength: 3
      maxLength: 63
      pattern: ^(?!app-)[a-zA-Z0-9]+[a-zA-Z0-9-_ ]+$
    ProxyType:
      type: string
      enum:
        - API_GATEWAY
    TagMap:
      description: A collection of up to 50 unique tags
      type: object
      properties: {}
    VpcId:
      type: string
      minLength: 12
      maxLength: 21
      pattern: ^vpc-[-a-f0-9]{8}([-a-f0-9]{9})?$
    CreateApplicationResponse:
      type: object
      properties:
        ApiGatewayProxy:
          $ref: "#/components/schemas/ApiGatewayProxyInput"
          description: A wrapper object holding the API Gateway endpoint type and stage name for the proxy.
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The unique identifier of the application.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the application. The format for this ARN is arn:aws:refactor-spaces:region:account-id:resource-type/resource-id . For more information about ARNs, see Amazon Resource Names (ARNs) in the Amazon Web Services General Reference.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of application creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the application is created.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The ID of the environment in which the application is created.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the application was last updated.
        Name:
          $ref: "#/components/schemas/ApplicationName"
          description: The name of the application.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the application owner (which is always the same as the environment owner account ID).
        ProxyType:
          $ref: "#/components/schemas/ProxyType"
          description: The proxy type of the proxy created within the application.
        State:
          $ref: "#/components/schemas/ApplicationState"
          description: The current state of the application.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the application. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair.
        VpcId:
          $ref: "#/components/schemas/VpcId"
          description: The ID of the Amazon VPC.
    ApplicationId:
      type: string
      minLength: 14
      maxLength: 14
      pattern: ^app-[0-9A-Za-z]{10}$
    ResourceArn:
      type: string
      minLength: 20
      maxLength: 2048
      pattern: ^arn:aws:refactor-spaces:[a-zA-Z0-9\-]+:\w{12}:[a-zA-Z_0-9+=,.@\-_/]+$
    AccountId:
      type: string
      minLength: 12
      maxLength: 12
      pattern: ^\d{12}$
    Timestamp:
      type: string
      format: date-time
    ApplicationState:
      type: string
      enum:
        - CREATING
        - ACTIVE
        - DELETING
        - FAILED
        - UPDATING
    Description:
      type: string
      minLength: 1
      maxLength: 256
      pattern: ^[a-zA-Z0-9-_\s\.\!\*\#\@\']+$
    EnvironmentName:
      type: string
      minLength: 3
      maxLength: 63
      pattern: ^(?!env-)[a-zA-Z0-9]+[a-zA-Z0-9-_ ]+$
    NetworkFabricType:
      type: string
      enum:
        - TRANSIT_GATEWAY
        - NONE
    CreateEnvironmentResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the environment.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment is created.
        Description:
          $ref: "#/components/schemas/Description"
          description: A description of the environment.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment was last updated.
        Name:
          $ref: "#/components/schemas/EnvironmentName"
          description: The name of the environment.
        NetworkFabricType:
          $ref: "#/components/schemas/NetworkFabricType"
          description: The network fabric type of the environment.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of environment owner.
        State:
          $ref: "#/components/schemas/EnvironmentState"
          description: The current state of the environment.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the created environment. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair..
    EnvironmentState:
      type: string
      enum:
        - CREATING
        - ACTIVE
        - DELETING
        - FAILED
    DefaultRouteInput:
      description: The configuration for the default route type.
      type: object
      properties:
        ActivationState:
          $ref: "#/components/schemas/RouteActivationState"
          description: If set to ACTIVE, traffic is forwarded to this route’s service after the route is created.
    RouteActivationState:
      type: string
      enum:
        - ACTIVE
        - INACTIVE
    RouteType:
      type: string
      enum:
        - DEFAULT
        - URI_PATH
    ServiceId:
      type: string
      minLength: 14
      maxLength: 14
      pattern: ^svc-[0-9A-Za-z]{10}$
    UriPathRouteInput:
      description: The configuration for the URI path route type.
      type: object
      properties:
        ActivationState:
          $ref: "#/components/schemas/RouteActivationState"
          description: If set to ACTIVE, traffic is forwarded to this route’s service after the route is created.
        AppendSourcePath:
          $ref: "#/components/schemas/Boolean"
          description: If set to true, this option appends the source path to the service URL endpoint.
        IncludeChildPaths:
          $ref: "#/components/schemas/Boolean"
          description: Indicates whether to match all subpaths of the given source path. If this value is false, requests must match the source path exactly before they are forwarded to this route's service.
        Methods:
          $ref: "#/components/schemas/HttpMethods"
          description: A list of HTTP methods to match. An empty list matches all values. If a method is present, only HTTP requests using that method are forwarded to this route’s service.
        SourcePath:
          $ref: "#/components/schemas/UriPath"
          description: This is the path that Refactor Spaces uses to match traffic. Paths must start with / and are relative to the base of the application. To use path parameters in the source path, add a variable in curly braces. For example, the resource path {user} represents a path parameter called 'user'.
      required:
        - ActivationState
        - SourcePath
    Boolean:
      type: boolean
    HttpMethods:
      type: array
      items:
        $ref: "#/components/schemas/HttpMethod"
    HttpMethod:
      type: string
      enum:
        - DELETE
        - GET
        - HEAD
        - OPTIONS
        - PATCH
        - POST
        - PUT
    UriPath:
      type: string
      minLength: 1
      maxLength: 2048
      pattern: ^(/([a-zA-Z0-9._:-]+|\{[a-zA-Z0-9._:-]+\}))+$
    CreateRouteResponse:
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application in which the route is created.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the route. The format for this ARN is arn:aws:refactor-spaces:region:account-id:resource-type/resource-id . For more information about ARNs, see Amazon Resource Names (ARNs) in the Amazon Web Services General Reference.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the route creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the route is created.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the route was last updated.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the route owner.
        RouteId:
          $ref: "#/components/schemas/RouteId"
          description: The unique identifier of the route.
        RouteType:
          $ref: "#/components/schemas/RouteType"
          description: The route type of the route.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The ID of service in which the route is created. Traffic that matches this route is forwarded to this service.
        State:
          $ref: "#/components/schemas/RouteState"
          description: The current state of the route. Activation state only allows ACTIVE or INACTIVE as user inputs. FAILED is a route state that is system generated.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the created route. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair.
        UriPathRoute:
          $ref: "#/components/schemas/UriPathRouteInput"
          description: Configuration for the URI path route type.
    RouteId:
      type: string
      minLength: 14
      maxLength: 14
      pattern: ^rte-[0-9A-Za-z]{10}$
    RouteState:
      type: string
      enum:
        - CREATING
        - ACTIVE
        - DELETING
        - FAILED
        - UPDATING
        - INACTIVE
    ServiceEndpointType:
      type: string
      enum:
        - LAMBDA
        - URL
    LambdaEndpointInput:
      description: The input for the Lambda endpoint type.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/LambdaArn"
          description: The Amazon Resource Name (ARN) of the Lambda function or alias.
      required:
        - Arn
    LambdaArn:
      type: string
      minLength: 1
      maxLength: 2048
      pattern: ^arn:(aws[a-zA-Z-]*)?:lambda:[a-z]{2}((-gov)|(-iso(b?)))?-[a-z]+-\d{1}:\d{12}:function:[a-zA-Z0-9-_]+(:(\$LATEST|[a-zA-Z0-9-_]+))?$
    ServiceName:
      type: string
      minLength: 3
      maxLength: 63
      pattern: ^(?!svc-)[a-zA-Z0-9]+[a-zA-Z0-9-_ ]+$
    UrlEndpointInput:
      description: The configuration for the URL endpoint type.
      type: object
      properties:
        HealthUrl:
          $ref: "#/components/schemas/Uri"
          description: The health check URL of the URL endpoint type. If the URL is a public endpoint, the HealthUrl must also be a public endpoint. If the URL is a private endpoint inside a virtual private cloud (VPC), the health URL must also be a private endpoint, and the host must be the same as the URL.
        Url:
          $ref: "#/components/schemas/Uri"
          description: The URL to route traffic to. The URL must be an rfc3986-formatted URL. If the host is a domain name, the name must be resolvable over the public internet. If the scheme is https, the top level domain of the host must be listed in the IANA root zone database.
      required:
        - Url
    Uri:
      type: string
      minLength: 1
      maxLength: 2048
      pattern: ^https?://[-a-zA-Z0-9+\x38@#/%?=~_|!:,.;]*[-a-zA-Z0-9+\x38@#/%=~_|]$
    CreateServiceResponse:
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application that the created service belongs to.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the service.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the service creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the service is created.
        Description:
          $ref: "#/components/schemas/Description"
          description: The description of the created service.
        EndpointType:
          $ref: "#/components/schemas/ServiceEndpointType"
          description: The endpoint type of the service.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        LambdaEndpoint:
          $ref: "#/components/schemas/LambdaEndpointInput"
          description: The configuration for the Lambda endpoint type.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the service was last updated.
        Name:
          $ref: "#/components/schemas/ServiceName"
          description: The name of the service.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the service owner.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The unique identifier of the service.
        State:
          $ref: "#/components/schemas/ServiceState"
          description: The current state of the service.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the created service. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair..
        UrlEndpoint:
          $ref: "#/components/schemas/UrlEndpointInput"
          description: The configuration for the URL endpoint type.
        VpcId:
          $ref: "#/components/schemas/VpcId"
          description: The ID of the VPC.
    ServiceState:
      type: string
      enum:
        - CREATING
        - ACTIVE
        - DELETING
        - FAILED
    DeleteApplicationResponse:
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the application.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the application’s environment.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment was last updated.
        Name:
          $ref: "#/components/schemas/ApplicationName"
          description: The name of the application.
        State:
          $ref: "#/components/schemas/ApplicationState"
          description: The current state of the application.
    DeleteEnvironmentResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the environment.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment was last updated.
        Name:
          $ref: "#/components/schemas/EnvironmentName"
          description: The name of the environment.
        State:
          $ref: "#/components/schemas/EnvironmentState"
          description: The current state of the environment.
    ResourcePolicyIdentifier:
      type: string
      minLength: 20
      maxLength: 2048
      pattern: ^arn:aws:refactor-spaces:[a-zA-Z0-9\-]+:\w{12}:[a-zA-Z_0-9+=,.@\-_/]+$
    DeleteResourcePolicyResponse:
      type: object
    DeleteRouteResponse:
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application that the route belongs to.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the route.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the route was last updated.
        RouteId:
          $ref: "#/components/schemas/RouteId"
          description: The ID of the route to delete.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The ID of the service that the route belongs to.
        State:
          $ref: "#/components/schemas/RouteState"
          description: The current state of the route.
    DeleteServiceResponse:
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application that the service is in.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the service.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the service was last updated.
        Name:
          $ref: "#/components/schemas/ServiceName"
          description: The name of the service.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The unique identifier of the service.
        State:
          $ref: "#/components/schemas/ServiceState"
          description: The current state of the service.
    GetApplicationResponse:
      type: object
      properties:
        ApiGatewayProxy:
          $ref: "#/components/schemas/ApiGatewayProxyConfig"
          description: The endpoint URL of the API Gateway proxy.
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The unique identifier of the application.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the application.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the application creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the application is created.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the application resource.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the application was last updated.
        Name:
          $ref: "#/components/schemas/ApplicationName"
          description: The name of the application.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the application owner (which is always the same as the environment owner account ID).
        ProxyType:
          $ref: "#/components/schemas/ProxyType"
          description: The proxy type of the proxy created within the application.
        State:
          $ref: "#/components/schemas/ApplicationState"
          description: The current state of the application.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the application. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair.
        VpcId:
          $ref: "#/components/schemas/VpcId"
          description: The ID of the virtual private cloud (VPC).
    ApiGatewayProxyConfig:
      description: A wrapper object holding the Amazon API Gateway proxy configuration.
      type: object
      properties:
        ApiGatewayId:
          $ref: "#/components/schemas/ApiGatewayId"
          description: The resource ID of the API Gateway for the proxy.
        EndpointType:
          $ref: "#/components/schemas/ApiGatewayEndpointType"
          description: The type of API Gateway endpoint created.
        NlbArn:
          $ref: "#/components/schemas/NlbArn"
          description: The Amazon Resource Name (ARN) of the Network Load Balancer configured by the API Gateway proxy.
        NlbName:
          $ref: "#/components/schemas/NlbName"
          description: The name of the Network Load Balancer that is configured by the API Gateway proxy.
        ProxyUrl:
          $ref: "#/components/schemas/Uri"
          description: The endpoint URL of the API Gateway proxy.
        StageName:
          $ref: "#/components/schemas/StageName"
          description: The name of the API Gateway stage. The name defaults to prod.
        VpcLinkId:
          $ref: "#/components/schemas/VpcLinkId"
          description: The VpcLink ID of the API Gateway proxy.
    ApiGatewayId:
      type: string
      minLength: 10
      maxLength: 10
      pattern: ^[a-z0-9]{10}$
    NlbArn:
      type: string
      minLength: 20
      maxLength: 2048
      pattern: ^arn:aws:elasticloadbalancing:[a-zA-Z0-9\-]+:\w{12}:[a-zA-Z_0-9+=,.@\-_/]+$
    NlbName:
      type: string
      minLength: 1
      maxLength: 32
      pattern: ^(?!internal-)[a-zA-Z0-9]+[a-zA-Z0-9-_ ]+.*[^-]$
    VpcLinkId:
      type: string
      minLength: 10
      maxLength: 10
      pattern: ^[a-z0-9]{10}$
    ErrorResponse:
      description: Error associated with a resource returned for a Get or List resource response.
      type: object
      properties:
        AccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the resource owner.
        AdditionalDetails:
          type: object
          properties: {}
          description: Additional details about the error.
        Code:
          $ref: "#/components/schemas/ErrorCode"
          description: The error code associated with the error.
        Message:
          $ref: "#/components/schemas/ErrorMessage"
          description: The message associated with the error.
        ResourceIdentifier:
          $ref: "#/components/schemas/ResourceIdentifier"
          description: The ID of the resource.
        ResourceType:
          $ref: "#/components/schemas/ErrorResourceType"
          description: The type of resource.
    ErrorCode:
      type: string
      enum:
        - INVALID_RESOURCE_STATE
        - RESOURCE_LIMIT_EXCEEDED
        - RESOURCE_CREATION_FAILURE
        - RESOURCE_UPDATE_FAILURE
        - SERVICE_ENDPOINT_HEALTH_CHECK_FAILURE
        - RESOURCE_DELETION_FAILURE
        - RESOURCE_RETRIEVAL_FAILURE
        - RESOURCE_IN_USE
        - RESOURCE_NOT_FOUND
        - STATE_TRANSITION_FAILURE
        - REQUEST_LIMIT_EXCEEDED
        - NOT_AUTHORIZED
    ErrorMessage:
      type: string
      minLength: 0
      maxLength: 255
      pattern: ^[\p{Alnum}\p{Punct}\p{Blank}]*$
    ResourceIdentifier:
      type: string
      minLength: 3
      maxLength: 63
      pattern: (^(env|svc|pxy|rte|app)-([0-9A-Za-z]{10}$))
    ErrorResourceType:
      type: string
      enum:
        - ENVIRONMENT
        - APPLICATION
        - ROUTE
        - SERVICE
        - TRANSIT_GATEWAY
        - TRANSIT_GATEWAY_ATTACHMENT
        - API_GATEWAY
        - NLB
        - TARGET_GROUP
        - LOAD_BALANCER_LISTENER
        - VPC_LINK
        - LAMBDA
        - VPC
        - SUBNET
        - ROUTE_TABLE
        - SECURITY_GROUP
        - VPC_ENDPOINT_SERVICE_CONFIGURATION
        - RESOURCE_SHARE
        - IAM_ROLE
    GetEnvironmentResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the environment.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment is created.
        Description:
          $ref: "#/components/schemas/Description"
          description: The description of the environment.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the environment resource.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment was last updated.
        Name:
          $ref: "#/components/schemas/EnvironmentName"
          description: The name of the environment.
        NetworkFabricType:
          $ref: "#/components/schemas/NetworkFabricType"
          description: The network fabric type of the environment.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the environment owner.
        State:
          $ref: "#/components/schemas/EnvironmentState"
          description: The current state of the environment.
        Tags:
          type: object
          properties: {}
          description: The tags to assign to the environment. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair.
        TransitGatewayId:
          $ref: "#/components/schemas/TransitGatewayId"
          description: The ID of the Transit Gateway set up by the environment, if applicable.
    TransitGatewayId:
      type: string
      minLength: 21
      maxLength: 21
      pattern: ^tgw-[-a-f0-9]{17}$
    GetResourcePolicyResponse:
      type: object
      properties:
        Policy:
          $ref: "#/components/schemas/PolicyString"
          description: A JSON-formatted string for an Amazon Web Services resource-based policy.
    PolicyString:
      type: string
      minLength: 1
      maxLength: 300000
      pattern: ^.*\S.*$
    GetRouteResponse:
      type: object
      properties:
        AppendSourcePath:
          $ref: "#/components/schemas/Boolean"
          description: If set to true, this option appends the source path to the service URL endpoint.
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application that the route belongs to.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the route.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the route creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the route is created.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: Unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the route resource.
        IncludeChildPaths:
          $ref: "#/components/schemas/Boolean"
          description: Indicates whether to match all subpaths of the given source path. If this value is false, requests must match the source path exactly before they are forwarded to this route's service.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the route was last updated.
        Methods:
          $ref: "#/components/schemas/HttpMethods"
          description: A list of HTTP methods to match. An empty list matches all values. If a method is present, only HTTP requests using that method are forwarded to this route’s service.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the route owner.
        PathResourceToId:
          type: object
          properties: {}
          description: A mapping of Amazon API Gateway path resources to resource IDs.
        RouteId:
          $ref: "#/components/schemas/RouteId"
          description: "The unique identifier of the route. DEFAULT: All traffic that does not match another route is forwarded to the default route. Applications must have a default route before any other routes can be created. URI_PATH: A route that is based on a URI path."
        RouteType:
          $ref: "#/components/schemas/RouteType"
          description: The type of route.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The unique identifier of the service.
        SourcePath:
          $ref: "#/components/schemas/UriPath"
          description: This is the path that Refactor Spaces uses to match traffic. Paths must start with / and are relative to the base of the application. To use path parameters in the source path, add a variable in curly braces. For example, the resource path {user} represents a path parameter called 'user'.
        State:
          $ref: "#/components/schemas/RouteState"
          description: The current state of the route.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the route. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair.
    GetServiceResponse:
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the service.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the service creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the service is created.
        Description:
          $ref: "#/components/schemas/Description"
          description: The description of the service.
        EndpointType:
          $ref: "#/components/schemas/ServiceEndpointType"
          description: The endpoint type of the service.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the service resource.
        LambdaEndpoint:
          $ref: "#/components/schemas/LambdaEndpointConfig"
          description: The configuration for the Lambda endpoint type. The Arn is the Amazon Resource Name (ARN) of the Lambda function associated with this service.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the service was last updated.
        Name:
          $ref: "#/components/schemas/ServiceName"
          description: The name of the service.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the service owner.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The unique identifier of the service.
        State:
          $ref: "#/components/schemas/ServiceState"
          description: The current state of the service.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the service. A tag is a label that you assign to an Amazon Web Services resource. Each tag consists of a key-value pair.
        UrlEndpoint:
          $ref: "#/components/schemas/UrlEndpointConfig"
          description: The configuration for the URL endpoint type. The Url isthe URL of the endpoint type. The HealthUrl is the health check URL of the endpoint type.
        VpcId:
          $ref: "#/components/schemas/VpcId"
          description: The ID of the virtual private cloud (VPC).
    LambdaEndpointConfig:
      description: The configuration for the Lambda endpoint type.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/LambdaArn"
          description: The Amazon Resource Name (ARN) of the Lambda endpoint.
    UrlEndpointConfig:
      description: The configuration for the URL endpoint type.
      type: object
      properties:
        HealthUrl:
          $ref: "#/components/schemas/Uri"
          description: The health check URL of the URL endpoint type.
        Url:
          $ref: "#/components/schemas/Uri"
          description: The HTTP URL endpoint.
    MaxResults:
      type: integer
      minimum: 1
      maximum: 100
    NextToken:
      type: string
      minLength: 1
      maxLength: 2048
      pattern: ^[a-zA-Z0-9/\+\=]{0,2048}$
    ListApplicationsResponse:
      type: object
      properties:
        ApplicationSummaryList:
          $ref: "#/components/schemas/ApplicationSummaries"
          description: The list of ApplicationSummary objects.
        NextToken:
          $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
    ApplicationSummaries:
      type: array
      items:
        $ref: "#/components/schemas/ApplicationSummary"
    ApplicationSummary:
      description: The list of ApplicationSummary objects.
      type: object
      properties:
        ApiGatewayProxy:
          $ref: "#/components/schemas/ApiGatewayProxySummary"
          description: The endpoint URL of the Amazon API Gateway proxy.
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The unique identifier of the application.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the application.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the application creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the application is created.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the application resource.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the application was last updated.
        Name:
          $ref: "#/components/schemas/ApplicationName"
          description: The name of the application.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the application owner (which is always the same as the environment owner account ID).
        ProxyType:
          $ref: "#/components/schemas/ProxyType"
          description: The proxy type of the proxy created within the application.
        State:
          $ref: "#/components/schemas/ApplicationState"
          description: The current state of the application.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the application.
        VpcId:
          $ref: "#/components/schemas/VpcId"
          description: The ID of the virtual private cloud (VPC).
    ApiGatewayProxySummary:
      description: A wrapper object holding the Amazon API Gateway proxy summary.
      type: object
      properties:
        ApiGatewayId:
          $ref: "#/components/schemas/ApiGatewayId"
          description: The resource ID of the API Gateway for the proxy.
        EndpointType:
          $ref: "#/components/schemas/ApiGatewayEndpointType"
          description: The type of API Gateway endpoint created.
        NlbArn:
          $ref: "#/components/schemas/NlbArn"
          description: The Amazon Resource Name (ARN) of the Network Load Balancer configured by the API Gateway proxy.
        NlbName:
          $ref: "#/components/schemas/NlbName"
          description: The name of the Network Load Balancer that is configured by the API Gateway proxy.
        ProxyUrl:
          $ref: "#/components/schemas/Uri"
          description: The endpoint URL of the API Gateway proxy.
        StageName:
          $ref: "#/components/schemas/StageName"
          description: The name of the API Gateway stage. The name defaults to prod.
        VpcLinkId:
          $ref: "#/components/schemas/VpcLinkId"
          description: The VpcLink ID of the API Gateway proxy.
    ListEnvironmentVpcsResponse:
      type: object
      properties:
        EnvironmentVpcList:
          $ref: "#/components/schemas/EnvironmentVpcs"
          description: The list of EnvironmentVpc objects.
        NextToken:
          $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
    EnvironmentVpcs:
      type: array
      items:
        $ref: "#/components/schemas/EnvironmentVpc"
    EnvironmentVpc:
      description: Provides summary information for the EnvironmentVpc resource as a response to ListEnvironmentVpc.
      type: object
      properties:
        AccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the virtual private cloud (VPC) owner.
        CidrBlocks:
          $ref: "#/components/schemas/CidrBlocks"
          description: The list of Amazon Virtual Private Cloud (Amazon VPC) CIDR blocks.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the VPC is first added to the environment.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the VPC was last updated by the environment.
        VpcId:
          $ref: "#/components/schemas/VpcId"
          description: The ID of the VPC.
        VpcName:
          $ref: "#/components/schemas/Ec2TagValue"
          description: The name of the VPC at the time it is added to the environment.
    CidrBlocks:
      type: array
      items:
        $ref: "#/components/schemas/CidrBlock"
    CidrBlock:
      type: string
    Ec2TagValue:
      type: string
      minLength: 0
      maxLength: 255
      pattern: ^.*$
    ListEnvironmentsResponse:
      type: object
      properties:
        EnvironmentSummaryList:
          $ref: "#/components/schemas/EnvironmentSummaries"
          description: The list of EnvironmentSummary objects.
        NextToken:
          $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
    EnvironmentSummaries:
      type: array
      items:
        $ref: "#/components/schemas/EnvironmentSummary"
    EnvironmentSummary:
      description: The summary information for environments as a response to ListEnvironments.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the environment.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment is created.
        Description:
          $ref: "#/components/schemas/Description"
          description: A description of the environment.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the environment resource.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the environment was last updated.
        Name:
          $ref: "#/components/schemas/EnvironmentName"
          description: The name of the environment.
        NetworkFabricType:
          $ref: "#/components/schemas/NetworkFabricType"
          description: The network fabric type of the environment.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the environment owner.
        State:
          $ref: "#/components/schemas/EnvironmentState"
          description: The current state of the environment.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the environment.
        TransitGatewayId:
          $ref: "#/components/schemas/TransitGatewayId"
          description: The ID of the Transit Gateway set up by the environment.
    ListRoutesResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
        RouteSummaryList:
          $ref: "#/components/schemas/RouteSummaries"
          description: The list of RouteSummary objects.
    RouteSummaries:
      type: array
      items:
        $ref: "#/components/schemas/RouteSummary"
    RouteSummary:
      description: The summary information for the routes as a response to ListRoutes.
      type: object
      properties:
        AppendSourcePath:
          $ref: "#/components/schemas/Boolean"
          description: If set to true, this option appends the source path to the service URL endpoint.
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The unique identifier of the application.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the route.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the route creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the route is created.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the route resource.
        IncludeChildPaths:
          $ref: "#/components/schemas/Boolean"
          description: Indicates whether to match all subpaths of the given source path. If this value is false, requests must match the source path exactly before they are forwarded to this route's service.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the route was last updated.
        Methods:
          $ref: "#/components/schemas/HttpMethods"
          description: A list of HTTP methods to match. An empty list matches all values. If a method is present, only HTTP requests using that method are forwarded to this route’s service.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the route owner.
        PathResourceToId:
          type: object
          properties: {}
          description: A mapping of Amazon API Gateway path resources to resource IDs.
        RouteId:
          $ref: "#/components/schemas/RouteId"
          description: The unique identifier of the route.
        RouteType:
          $ref: "#/components/schemas/RouteType"
          description: The route type of the route.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The unique identifier of the service.
        SourcePath:
          $ref: "#/components/schemas/UriPath"
          description: This is the path that Refactor Spaces uses to match traffic. Paths must start with / and are relative to the base of the application. To use path parameters in the source path, add a variable in curly braces. For example, the resource path {user} represents a path parameter called 'user'.
        State:
          $ref: "#/components/schemas/RouteState"
          description: The current state of the route.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the route.
    ListServicesResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/NextToken"
          description: The token for the next page of results.
        ServiceSummaryList:
          $ref: "#/components/schemas/ServiceSummaries"
          description: The list of ServiceSummary objects.
    ServiceSummaries:
      type: array
      items:
        $ref: "#/components/schemas/ServiceSummary"
    ServiceSummary:
      description: A summary for the service as a response to ListServices.
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The unique identifier of the application.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the service.
        CreatedByAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the service creator.
        CreatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the service is created.
        Description:
          $ref: "#/components/schemas/Description"
          description: A description of the service.
        EndpointType:
          $ref: "#/components/schemas/ServiceEndpointType"
          description: The endpoint type of the service.
        EnvironmentId:
          $ref: "#/components/schemas/EnvironmentId"
          description: The unique identifier of the environment.
        Error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Any error associated with the service resource.
        LambdaEndpoint:
          $ref: "#/components/schemas/LambdaEndpointSummary"
          description: A summary of the configuration for the Lambda endpoint type.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the service was last updated.
        Name:
          $ref: "#/components/schemas/ServiceName"
          description: The name of the service.
        OwnerAccountId:
          $ref: "#/components/schemas/AccountId"
          description: The Amazon Web Services account ID of the service owner.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The unique identifier of the service.
        State:
          $ref: "#/components/schemas/ServiceState"
          description: The current state of the service.
        Tags:
          type: object
          properties: {}
          description: The tags assigned to the service.
        UrlEndpoint:
          $ref: "#/components/schemas/UrlEndpointSummary"
          description: The summary of the configuration for the URL endpoint type.
        VpcId:
          $ref: "#/components/schemas/VpcId"
          description: The ID of the virtual private cloud (VPC).
    LambdaEndpointSummary:
      description: The summary for the Lambda endpoint type.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/LambdaArn"
          description: The Amazon Resource Name (ARN) of the Lambda endpoint.
    UrlEndpointSummary:
      description: The summary of the configuration for the URL endpoint type.
      type: object
      properties:
        HealthUrl:
          $ref: "#/components/schemas/Uri"
          description: The health check URL of the URL endpoint type. If the URL is a public endpoint, the HealthUrl must also be a public endpoint. If the URL is a private endpoint inside a virtual private cloud (VPC), the health URL must also be a private endpoint, and the host must be the same as the URL.
        Url:
          $ref: "#/components/schemas/Uri"
          description: The URL to route traffic to. The URL must be an rfc3986-formatted URL. If the host is a domain name, the name must be resolvable over the public internet. If the scheme is https, the top level domain of the host must be listed in the IANA root zone database.
    String:
      type: string
    ListTagsForResourceResponse:
      type: object
      properties:
        Tags:
          type: object
          properties: {}
          description: The list of tags assigned to the resource.
    PutResourcePolicyResponse:
      type: object
    TagResourceResponse:
      type: object
    TagKeys:
      type: array
      items:
        $ref: "#/components/schemas/String"
    UntagResourceResponse:
      type: object
    UpdateRouteResponse:
      type: object
      properties:
        ApplicationId:
          $ref: "#/components/schemas/ApplicationId"
          description: The ID of the application in which the route is being updated.
        Arn:
          $ref: "#/components/schemas/ResourceArn"
          description: The Amazon Resource Name (ARN) of the route. The format for this ARN is arn:aws:refactor-spaces:region:account-id:resource-type/resource-id . For more information about ARNs, see Amazon Resource Names (ARNs) in the Amazon Web Services General Reference.
        LastUpdatedTime:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp that indicates when the route was last updated.
        RouteId:
          $ref: "#/components/schemas/RouteId"
          description: The unique identifier of the route.
        ServiceId:
          $ref: "#/components/schemas/ServiceId"
          description: The ID of service in which the route was created. Traffic that matches this route is forwarded to this service.
        State:
          $ref: "#/components/schemas/RouteState"
          description: The current state of the route.
  x-stackQL-resources:
    applications:
      id: aws.migration_hub_refactor_spaces.applications
      name: applications
      title: applications
      methods:
        create_application:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_applications:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.ApplicationSummaryList
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_application:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_application:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/applications/methods/get_application"
          - $ref: "#/components/x-stackQL-resources/applications/methods/list_applications"
        insert:
          - $ref: "#/components/x-stackQL-resources/applications/methods/create_application"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/applications/methods/delete_application"
    environments:
      id: aws.migration_hub_refactor_spaces.environments
      name: environments
      title: environments
      methods:
        create_environment:
          operation:
            $ref: "#/paths/~1environments/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_environments:
          operation:
            $ref: "#/paths/~1environments/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.EnvironmentSummaryList
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_environment:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_environment:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/environments/methods/get_environment"
          - $ref: "#/components/x-stackQL-resources/environments/methods/list_environments"
        insert:
          - $ref: "#/components/x-stackQL-resources/environments/methods/create_environment"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/environments/methods/delete_environment"
    routes:
      id: aws.migration_hub_refactor_spaces.routes
      name: routes
      title: routes
      methods:
        create_route:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1routes/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_routes:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1routes/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.RouteSummaryList
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_route:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1routes~1{route_identifier}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_route:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1routes~1{route_identifier}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_route:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1routes~1{route_identifier}/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/routes/methods/get_route"
          - $ref: "#/components/x-stackQL-resources/routes/methods/list_routes"
        insert:
          - $ref: "#/components/x-stackQL-resources/routes/methods/create_route"
        update:
          - $ref: "#/components/x-stackQL-resources/routes/methods/update_route"
        delete:
          - $ref: "#/components/x-stackQL-resources/routes/methods/delete_route"
    services:
      id: aws.migration_hub_refactor_spaces.services
      name: services
      title: services
      methods:
        create_service:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1services/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_services:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1services/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.ServiceSummaryList
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_service:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1services~1{service_identifier}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_service:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1applications~1{application_identifier}~1services~1{service_identifier}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/services/methods/get_service"
          - $ref: "#/components/x-stackQL-resources/services/methods/list_services"
        insert:
          - $ref: "#/components/x-stackQL-resources/services/methods/create_service"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/services/methods/delete_service"
    resource_policies:
      id: aws.migration_hub_refactor_spaces.resource_policies
      name: resource_policies
      title: resource_policies
      methods:
        delete_resource_policy:
          operation:
            $ref: "#/paths/~1resourcepolicy~1{identifier}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_resource_policy:
          operation:
            $ref: "#/paths/~1resourcepolicy~1{identifier}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        put_resource_policy:
          operation:
            $ref: "#/paths/~1resourcepolicy/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_policies/methods/get_resource_policy"
        insert: []
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/resource_policies/methods/put_resource_policy"
        delete:
          - $ref: "#/components/x-stackQL-resources/resource_policies/methods/delete_resource_policy"
    environment_vpcs:
      id: aws.migration_hub_refactor_spaces.environment_vpcs
      name: environment_vpcs
      title: environment_vpcs
      methods:
        list_environment_vpcs:
          operation:
            $ref: "#/paths/~1environments~1{environment_identifier}~1vpcs/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.EnvironmentVpcList
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/environment_vpcs/methods/list_environment_vpcs"
        insert: []
        update: []
        delete: []
    tags:
      id: aws.migration_hub_refactor_spaces.tags
      name: tags
      title: tags
      methods:
        list_tags_for_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource_arn}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        tag_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource_arn}/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        untag_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource_arn}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tags/methods/list_tags_for_resource"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/tags/methods/untag_resource"
          - $ref: "#/components/x-stackQL-resources/tags/methods/tag_resource"
        delete: []
