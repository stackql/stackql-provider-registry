openapi: 3.0.0
info:
  version: "2017-06-07"
  title: AWS Greengrass
  description: AWS Greengrass
  x-serviceName: greengrass
  x-serviceAlias: greengrass
  x-protocol: rest-json
  x-apiVersion: "2017-06-07"
  x-endpointPrefix: greengrass
  x-jsonVersion: "1.1"
servers:
  - url: https://greengrass.{region}.amazonaws.com
    variables:
      region:
        default: us-east-1
        description: AWS region
    description: Regional endpoint for greengrass
paths:
  /greengrass/groups/{group_id}/role:
    put:
      operationId: AssociateRoleToGroup
      description: Associates a role with a group. Your Greengrass core will use the role to access AWS cloud services. The role's permissions should allow Greengrass core Lambda functions to perform actions against the cloud.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                RoleArn:
                  $ref: "#/components/schemas/__string"
              required:
                - RoleArn
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssociateRoleToGroupResponse"
    delete:
      operationId: DisassociateRoleFromGroup
      description: Disassociates the role from a group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisassociateRoleFromGroupResponse"
    get:
      operationId: GetAssociatedRole
      description: Retrieves the role associated with a particular group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetAssociatedRoleResponse"
  /greengrass/servicerole:
    put:
      operationId: AssociateServiceRoleToAccount
      description: Associates a role with your account. AWS IoT Greengrass will use the role to access your Lambda functions and AWS IoT resources. This is necessary for deployments to succeed. The role must have at least minimum permissions in the policy ''AWSGreengrassResourceAccessRolePolicy''.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                RoleArn:
                  $ref: "#/components/schemas/__string"
              required:
                - RoleArn
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssociateServiceRoleToAccountResponse"
    delete:
      operationId: DisassociateServiceRoleFromAccount
      description: Disassociates the service role from your account. Without a service role, deployments will not work.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisassociateServiceRoleFromAccountResponse"
    get:
      operationId: GetServiceRoleForAccount
      description: Retrieves the service role that is attached to your account.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetServiceRoleForAccountResponse"
  /greengrass/definition/connectors:
    post:
      operationId: CreateConnectorDefinition
      description: Creates a connector definition. You may provide the initial version of the connector definition now or use ''CreateConnectorDefinitionVersion'' at a later time.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/ConnectorDefinitionVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateConnectorDefinitionResponse"
    get:
      operationId: ListConnectorDefinitions
      description: Retrieves a list of connector definitions.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListConnectorDefinitionsResponse"
  /greengrass/definition/connectors/{connector_definition_id}/versions:
    post:
      operationId: CreateConnectorDefinitionVersion
      description: Creates a version of a connector definition which has already been defined.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: connector_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the connector definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Connectors:
                  $ref: "#/components/schemas/__listOfConnector"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateConnectorDefinitionVersionResponse"
    get:
      operationId: ListConnectorDefinitionVersions
      description: Lists the versions of a connector definition, which are containers for connectors. Connectors run on the Greengrass core and contain built-in integration with local infrastructure, device protocols, AWS, and other cloud services.
      parameters:
        - name: connector_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the connector definition.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListConnectorDefinitionVersionsResponse"
  /greengrass/definition/cores:
    post:
      operationId: CreateCoreDefinition
      description: Creates a core definition. You may provide the initial version of the core definition now or use ''CreateCoreDefinitionVersion'' at a later time. Greengrass groups must each contain exactly one Greengrass core.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/CoreDefinitionVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateCoreDefinitionResponse"
    get:
      operationId: ListCoreDefinitions
      description: Retrieves a list of core definitions.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListCoreDefinitionsResponse"
  /greengrass/definition/cores/{core_definition_id}/versions:
    post:
      operationId: CreateCoreDefinitionVersion
      description: Creates a version of a core definition that has already been defined. Greengrass groups must each contain exactly one Greengrass core.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: core_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the core definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Cores:
                  $ref: "#/components/schemas/__listOfCore"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateCoreDefinitionVersionResponse"
    get:
      operationId: ListCoreDefinitionVersions
      description: Lists the versions of a core definition.
      parameters:
        - name: core_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the core definition.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListCoreDefinitionVersionsResponse"
  /greengrass/groups/{group_id}/deployments:
    post:
      operationId: CreateDeployment
      description: Creates a deployment. ''CreateDeployment'' requests are idempotent with respect to the ''X-Amzn-Client-Token'' token and the request parameters.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                DeploymentId:
                  $ref: "#/components/schemas/__string"
                DeploymentType:
                  $ref: "#/components/schemas/DeploymentType"
                GroupVersionId:
                  $ref: "#/components/schemas/__string"
              required:
                - DeploymentType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateDeploymentResponse"
    get:
      operationId: ListDeployments
      description: Returns a history of deployments for the group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDeploymentsResponse"
  /greengrass/definition/devices:
    post:
      operationId: CreateDeviceDefinition
      description: Creates a device definition. You may provide the initial version of the device definition now or use ''CreateDeviceDefinitionVersion'' at a later time.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/DeviceDefinitionVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateDeviceDefinitionResponse"
    get:
      operationId: ListDeviceDefinitions
      description: Retrieves a list of device definitions.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDeviceDefinitionsResponse"
  /greengrass/definition/devices/{device_definition_id}/versions:
    post:
      operationId: CreateDeviceDefinitionVersion
      description: Creates a version of a device definition that has already been defined.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: device_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the device definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Devices:
                  $ref: "#/components/schemas/__listOfDevice"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateDeviceDefinitionVersionResponse"
    get:
      operationId: ListDeviceDefinitionVersions
      description: Lists the versions of a device definition.
      parameters:
        - name: device_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the device definition.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDeviceDefinitionVersionsResponse"
  /greengrass/definition/functions:
    post:
      operationId: CreateFunctionDefinition
      description: Creates a Lambda function definition which contains a list of Lambda functions and their configurations to be used in a group. You can create an initial version of the definition by providing a list of Lambda functions and their configurations now, or use ''CreateFunctionDefinitionVersion'' later.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/FunctionDefinitionVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateFunctionDefinitionResponse"
    get:
      operationId: ListFunctionDefinitions
      description: Retrieves a list of Lambda function definitions.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFunctionDefinitionsResponse"
  /greengrass/definition/functions/{function_definition_id}/versions:
    post:
      operationId: CreateFunctionDefinitionVersion
      description: Creates a version of a Lambda function definition that has already been defined.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: function_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Lambda function definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                DefaultConfig:
                  $ref: "#/components/schemas/FunctionDefaultConfig"
                Functions:
                  $ref: "#/components/schemas/__listOfFunction"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateFunctionDefinitionVersionResponse"
    get:
      operationId: ListFunctionDefinitionVersions
      description: Lists the versions of a Lambda function definition.
      parameters:
        - name: function_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Lambda function definition.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFunctionDefinitionVersionsResponse"
  /greengrass/groups:
    post:
      operationId: CreateGroup
      description: "Creates a group. You may provide the initial version of the group or use ''CreateGroupVersion'' at a later time. Tip: You can use the ''gg_group_setup'' package (https://github.com/awslabs/aws-greengrass-group-setup) as a library or command-line application to create and deploy Greengrass groups."
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/GroupVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateGroupResponse"
    get:
      operationId: ListGroups
      description: Retrieves a list of groups.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListGroupsResponse"
  /greengrass/groups/{group_id}/certificateauthorities:
    post:
      operationId: CreateGroupCertificateAuthority
      description: Creates a CA for the group. If a CA already exists, it will rotate the existing CA.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateGroupCertificateAuthorityResponse"
    get:
      operationId: ListGroupCertificateAuthorities
      description: Retrieves the current CAs for a group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListGroupCertificateAuthoritiesResponse"
  /greengrass/groups/{group_id}/versions:
    post:
      operationId: CreateGroupVersion
      description: Creates a version of a group which has already been defined.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                ConnectorDefinitionVersionArn:
                  $ref: "#/components/schemas/__string"
                CoreDefinitionVersionArn:
                  $ref: "#/components/schemas/__string"
                DeviceDefinitionVersionArn:
                  $ref: "#/components/schemas/__string"
                FunctionDefinitionVersionArn:
                  $ref: "#/components/schemas/__string"
                LoggerDefinitionVersionArn:
                  $ref: "#/components/schemas/__string"
                ResourceDefinitionVersionArn:
                  $ref: "#/components/schemas/__string"
                SubscriptionDefinitionVersionArn:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateGroupVersionResponse"
    get:
      operationId: ListGroupVersions
      description: Lists the versions of a group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListGroupVersionsResponse"
  /greengrass/definition/loggers:
    post:
      operationId: CreateLoggerDefinition
      description: Creates a logger definition. You may provide the initial version of the logger definition now or use ''CreateLoggerDefinitionVersion'' at a later time.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/LoggerDefinitionVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateLoggerDefinitionResponse"
    get:
      operationId: ListLoggerDefinitions
      description: Retrieves a list of logger definitions.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListLoggerDefinitionsResponse"
  /greengrass/definition/loggers/{logger_definition_id}/versions:
    post:
      operationId: CreateLoggerDefinitionVersion
      description: Creates a version of a logger definition that has already been defined.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: logger_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the logger definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Loggers:
                  $ref: "#/components/schemas/__listOfLogger"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateLoggerDefinitionVersionResponse"
    get:
      operationId: ListLoggerDefinitionVersions
      description: Lists the versions of a logger definition.
      parameters:
        - name: logger_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the logger definition.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListLoggerDefinitionVersionsResponse"
  /greengrass/definition/resources:
    post:
      operationId: CreateResourceDefinition
      description: Creates a resource definition which contains a list of resources to be used in a group. You can create an initial version of the definition by providing a list of resources now, or use ''CreateResourceDefinitionVersion'' later.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/ResourceDefinitionVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateResourceDefinitionResponse"
    get:
      operationId: ListResourceDefinitions
      description: Retrieves a list of resource definitions.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResourceDefinitionsResponse"
  /greengrass/definition/resources/{resource_definition_id}/versions:
    post:
      operationId: CreateResourceDefinitionVersion
      description: Creates a version of a resource definition that has already been defined.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: resource_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the resource definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Resources:
                  $ref: "#/components/schemas/__listOfResource"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateResourceDefinitionVersionResponse"
    get:
      operationId: ListResourceDefinitionVersions
      description: Lists the versions of a resource definition.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        - name: resource_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the resource definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResourceDefinitionVersionsResponse"
  /greengrass/updates:
    post:
      operationId: CreateSoftwareUpdateJob
      description: Creates a software update for a core or group of cores (specified as an IoT thing group.) Use this to update the OTA Agent as well as the Greengrass core software. It makes use of the IoT Jobs feature which provides additional commands to manage a Greengrass core software update job.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                S3UrlSignerRole:
                  $ref: "#/components/schemas/S3UrlSignerRole"
                SoftwareToUpdate:
                  $ref: "#/components/schemas/SoftwareToUpdate"
                UpdateAgentLogLevel:
                  $ref: "#/components/schemas/UpdateAgentLogLevel"
                UpdateTargets:
                  $ref: "#/components/schemas/UpdateTargets"
                UpdateTargetsArchitecture:
                  $ref: "#/components/schemas/UpdateTargetsArchitecture"
                UpdateTargetsOperatingSystem:
                  $ref: "#/components/schemas/UpdateTargetsOperatingSystem"
              required:
                - S3UrlSignerRole
                - SoftwareToUpdate
                - UpdateTargets
                - UpdateTargetsArchitecture
                - UpdateTargetsOperatingSystem
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSoftwareUpdateJobResponse"
  /greengrass/definition/subscriptions:
    post:
      operationId: CreateSubscriptionDefinition
      description: Creates a subscription definition. You may provide the initial version of the subscription definition now or use ''CreateSubscriptionDefinitionVersion'' at a later time.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                InitialVersion:
                  $ref: "#/components/schemas/SubscriptionDefinitionVersion"
                Name:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSubscriptionDefinitionResponse"
    get:
      operationId: ListSubscriptionDefinitions
      description: Retrieves a list of subscription definitions.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSubscriptionDefinitionsResponse"
  /greengrass/definition/subscriptions/{subscription_definition_id}/versions:
    post:
      operationId: CreateSubscriptionDefinitionVersion
      description: Creates a version of a subscription definition which has already been defined.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: subscription_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Subscriptions:
                  $ref: "#/components/schemas/__listOfSubscription"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSubscriptionDefinitionVersionResponse"
    get:
      operationId: ListSubscriptionDefinitionVersions
      description: Lists the versions of a subscription definition.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        - name: subscription_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSubscriptionDefinitionVersionsResponse"
  /greengrass/definition/connectors/{connector_definition_id}:
    delete:
      operationId: DeleteConnectorDefinition
      description: Deletes a connector definition.
      parameters:
        - name: connector_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the connector definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteConnectorDefinitionResponse"
    get:
      operationId: GetConnectorDefinition
      description: Retrieves information about a connector definition.
      parameters:
        - name: connector_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the connector definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetConnectorDefinitionResponse"
    put:
      operationId: UpdateConnectorDefinition
      description: Updates a connector definition.
      parameters:
        - name: connector_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the connector definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateConnectorDefinitionResponse"
  /greengrass/definition/cores/{core_definition_id}:
    delete:
      operationId: DeleteCoreDefinition
      description: Deletes a core definition.
      parameters:
        - name: core_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the core definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteCoreDefinitionResponse"
    get:
      operationId: GetCoreDefinition
      description: Retrieves information about a core definition version.
      parameters:
        - name: core_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the core definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetCoreDefinitionResponse"
    put:
      operationId: UpdateCoreDefinition
      description: Updates a core definition.
      parameters:
        - name: core_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the core definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateCoreDefinitionResponse"
  /greengrass/definition/devices/{device_definition_id}:
    delete:
      operationId: DeleteDeviceDefinition
      description: Deletes a device definition.
      parameters:
        - name: device_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the device definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteDeviceDefinitionResponse"
    get:
      operationId: GetDeviceDefinition
      description: Retrieves information about a device definition.
      parameters:
        - name: device_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the device definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetDeviceDefinitionResponse"
    put:
      operationId: UpdateDeviceDefinition
      description: Updates a device definition.
      parameters:
        - name: device_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the device definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateDeviceDefinitionResponse"
  /greengrass/definition/functions/{function_definition_id}:
    delete:
      operationId: DeleteFunctionDefinition
      description: Deletes a Lambda function definition.
      parameters:
        - name: function_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Lambda function definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteFunctionDefinitionResponse"
    get:
      operationId: GetFunctionDefinition
      description: Retrieves information about a Lambda function definition, including its creation time and latest version.
      parameters:
        - name: function_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Lambda function definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetFunctionDefinitionResponse"
    put:
      operationId: UpdateFunctionDefinition
      description: Updates a Lambda function definition.
      parameters:
        - name: function_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Lambda function definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateFunctionDefinitionResponse"
  /greengrass/groups/{group_id}:
    delete:
      operationId: DeleteGroup
      description: Deletes a group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteGroupResponse"
    get:
      operationId: GetGroup
      description: Retrieves information about a group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetGroupResponse"
    put:
      operationId: UpdateGroup
      description: Updates a group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateGroupResponse"
  /greengrass/definition/loggers/{logger_definition_id}:
    delete:
      operationId: DeleteLoggerDefinition
      description: Deletes a logger definition.
      parameters:
        - name: logger_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the logger definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteLoggerDefinitionResponse"
    get:
      operationId: GetLoggerDefinition
      description: Retrieves information about a logger definition.
      parameters:
        - name: logger_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the logger definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetLoggerDefinitionResponse"
    put:
      operationId: UpdateLoggerDefinition
      description: Updates a logger definition.
      parameters:
        - name: logger_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the logger definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateLoggerDefinitionResponse"
  /greengrass/definition/resources/{resource_definition_id}:
    delete:
      operationId: DeleteResourceDefinition
      description: Deletes a resource definition.
      parameters:
        - name: resource_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the resource definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResourceDefinitionResponse"
    get:
      operationId: GetResourceDefinition
      description: Retrieves information about a resource definition, including its creation time and latest version.
      parameters:
        - name: resource_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the resource definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetResourceDefinitionResponse"
    put:
      operationId: UpdateResourceDefinition
      description: Updates a resource definition.
      parameters:
        - name: resource_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the resource definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateResourceDefinitionResponse"
  /greengrass/definition/subscriptions/{subscription_definition_id}:
    delete:
      operationId: DeleteSubscriptionDefinition
      description: Deletes a subscription definition.
      parameters:
        - name: subscription_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteSubscriptionDefinitionResponse"
    get:
      operationId: GetSubscriptionDefinition
      description: Retrieves information about a subscription definition.
      parameters:
        - name: subscription_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetSubscriptionDefinitionResponse"
    put:
      operationId: UpdateSubscriptionDefinition
      description: Updates a subscription definition.
      parameters:
        - name: subscription_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Name:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateSubscriptionDefinitionResponse"
  /greengrass/bulk/deployments/{bulk_deployment_id}/status:
    get:
      operationId: GetBulkDeploymentStatus
      description: Returns the status of a bulk deployment.
      parameters:
        - name: bulk_deployment_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the bulk deployment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetBulkDeploymentStatusResponse"
  /greengrass/things/{thing_name}/connectivityInfo:
    get:
      operationId: GetConnectivityInfo
      description: Retrieves the connectivity information for a core.
      parameters:
        - name: thing_name
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The thing name.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetConnectivityInfoResponse"
    put:
      operationId: UpdateConnectivityInfo
      description: Updates the connectivity information for the core. Any devices that belong to the group which has this core will receive this information in order to find the location of the core and connect to it.
      parameters:
        - name: thing_name
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The thing name.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                ConnectivityInfo:
                  $ref: "#/components/schemas/__listOfConnectivityInfo"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateConnectivityInfoResponse"
  /greengrass/definition/connectors/{connector_definition_id}/versions/{connector_definition_version_id}:
    get:
      operationId: GetConnectorDefinitionVersion
      description: Retrieves information about a connector definition version, including the connectors that the version contains. Connectors are prebuilt modules that interact with local infrastructure, device protocols, AWS, and other cloud services.
      parameters:
        - name: connector_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the connector definition.
        - name: connector_definition_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the connector definition version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListConnectorDefinitionVersions'' requests. If the version is the last one that was associated with a connector definition, the value also maps to the ''LatestVersion'' property of the corresponding ''DefinitionInformation'' object.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetConnectorDefinitionVersionResponse"
  /greengrass/definition/cores/{core_definition_id}/versions/{core_definition_version_id}:
    get:
      operationId: GetCoreDefinitionVersion
      description: Retrieves information about a core definition version.
      parameters:
        - name: core_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the core definition.
        - name: core_definition_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the core definition version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListCoreDefinitionVersions'' requests. If the version is the last one that was associated with a core definition, the value also maps to the ''LatestVersion'' property of the corresponding ''DefinitionInformation'' object.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetCoreDefinitionVersionResponse"
  /greengrass/groups/{group_id}/deployments/{deployment_id}/status:
    get:
      operationId: GetDeploymentStatus
      description: Returns the status of a deployment.
      parameters:
        - name: deployment_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the deployment.
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetDeploymentStatusResponse"
  /greengrass/definition/devices/{device_definition_id}/versions/{device_definition_version_id}:
    get:
      operationId: GetDeviceDefinitionVersion
      description: Retrieves information about a device definition version.
      parameters:
        - name: device_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the device definition.
        - name: device_definition_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the device definition version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListDeviceDefinitionVersions'' requests. If the version is the last one that was associated with a device definition, the value also maps to the ''LatestVersion'' property of the corresponding ''DefinitionInformation'' object.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetDeviceDefinitionVersionResponse"
  /greengrass/definition/functions/{function_definition_id}/versions/{function_definition_version_id}:
    get:
      operationId: GetFunctionDefinitionVersion
      description: Retrieves information about a Lambda function definition version, including which Lambda functions are included in the version and their configurations.
      parameters:
        - name: function_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Lambda function definition.
        - name: function_definition_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the function definition version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListFunctionDefinitionVersions'' requests. If the version is the last one that was associated with a function definition, the value also maps to the ''LatestVersion'' property of the corresponding ''DefinitionInformation'' object.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetFunctionDefinitionVersionResponse"
  /greengrass/groups/{group_id}/certificateauthorities/{certificate_authority_id}:
    get:
      operationId: GetGroupCertificateAuthority
      description: Retreives the CA associated with a group. Returns the public key of the CA.
      parameters:
        - name: certificate_authority_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the certificate authority.
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetGroupCertificateAuthorityResponse"
  /greengrass/groups/{group_id}/certificateauthorities/configuration/expiry:
    get:
      operationId: GetGroupCertificateConfiguration
      description: Retrieves the current configuration for the CA used by the group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetGroupCertificateConfigurationResponse"
    put:
      operationId: UpdateGroupCertificateConfiguration
      description: Updates the Certificate expiry time for a group.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                CertificateExpiryInMilliseconds:
                  $ref: "#/components/schemas/__string"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateGroupCertificateConfigurationResponse"
  /greengrass/groups/{group_id}/versions/{group_version_id}:
    get:
      operationId: GetGroupVersion
      description: Retrieves information about a group version.
      parameters:
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
        - name: group_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the group version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListGroupVersions'' requests. If the version is the last one that was associated with a group, the value also maps to the ''LatestVersion'' property of the corresponding ''GroupInformation'' object.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetGroupVersionResponse"
  /greengrass/definition/loggers/{logger_definition_id}/versions/{logger_definition_version_id}:
    get:
      operationId: GetLoggerDefinitionVersion
      description: Retrieves information about a logger definition version.
      parameters:
        - name: logger_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the logger definition.
        - name: logger_definition_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the logger definition version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListLoggerDefinitionVersions'' requests. If the version is the last one that was associated with a logger definition, the value also maps to the ''LatestVersion'' property of the corresponding ''DefinitionInformation'' object.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetLoggerDefinitionVersionResponse"
  /greengrass/definition/resources/{resource_definition_id}/versions/{resource_definition_version_id}:
    get:
      operationId: GetResourceDefinitionVersion
      description: Retrieves information about a resource definition version, including which resources are included in the version.
      parameters:
        - name: resource_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the resource definition.
        - name: resource_definition_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the resource definition version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListResourceDefinitionVersions'' requests. If the version is the last one that was associated with a resource definition, the value also maps to the ''LatestVersion'' property of the corresponding ''DefinitionInformation'' object.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetResourceDefinitionVersionResponse"
  /greengrass/definition/subscriptions/{subscription_definition_id}/versions/{subscription_definition_version_id}:
    get:
      operationId: GetSubscriptionDefinitionVersion
      description: Retrieves information about a subscription definition version.
      parameters:
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        - name: subscription_definition_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition.
        - name: subscription_definition_version_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition version. This value maps to the ''Version'' property of the corresponding ''VersionInformation'' object, which is returned by ''ListSubscriptionDefinitionVersions'' requests. If the version is the last one that was associated with a subscription definition, the value also maps to the ''LatestVersion'' property of the corresponding ''DefinitionInformation'' object.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetSubscriptionDefinitionVersionResponse"
  /greengrass/things/{thing_name}/runtimeconfig:
    get:
      operationId: GetThingRuntimeConfiguration
      description: Get the runtime configuration of a thing.
      parameters:
        - name: thing_name
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The thing name.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetThingRuntimeConfigurationResponse"
    put:
      operationId: UpdateThingRuntimeConfiguration
      description: Updates the runtime configuration of a thing.
      parameters:
        - name: thing_name
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The thing name.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                TelemetryConfiguration:
                  $ref: "#/components/schemas/TelemetryConfigurationUpdate"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateThingRuntimeConfigurationResponse"
  /greengrass/bulk/deployments/{bulk_deployment_id}/detailed-reports:
    get:
      operationId: ListBulkDeploymentDetailedReports
      description: Gets a paginated list of the deployments that have been started in a bulk deployment operation, and their current deployment status.
      parameters:
        - name: bulk_deployment_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the bulk deployment.
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBulkDeploymentDetailedReportsResponse"
  /greengrass/bulk/deployments:
    get:
      operationId: ListBulkDeployments
      description: Returns a list of bulk deployments.
      parameters:
        - name: MaxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The maximum number of results to be returned per request.
        - name: NextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBulkDeploymentsResponse"
    post:
      operationId: StartBulkDeployment
      description: Deploys multiple groups in one operation. This action starts the bulk deployment of a specified set of group versions. Each group version deployment will be triggered with an adaptive rate that has a fixed upper limit. We recommend that you include an ''X-Amzn-Client-Token'' token in every ''StartBulkDeployment'' request. These requests are idempotent with respect to the token and the request parameters.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ExecutionRoleArn:
                  $ref: "#/components/schemas/__string"
                InputFileUri:
                  $ref: "#/components/schemas/__string"
                tags:
                  $ref: "#/components/schemas/Tags"
              required:
                - ExecutionRoleArn
                - InputFileUri
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartBulkDeploymentResponse"
  /tags/{resource-arn}:
    get:
      operationId: ListTagsForResource
      description: Retrieves a list of resource tags for a resource arn.
      parameters:
        - name: resource-arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The Amazon Resource Name (ARN) of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListTagsForResourceResponse"
    post:
      operationId: TagResource
      description: Adds tags to a Greengrass resource. Valid resources are 'Group', 'ConnectorDefinition', 'CoreDefinition', 'DeviceDefinition', 'FunctionDefinition', 'LoggerDefinition', 'SubscriptionDefinition', 'ResourceDefinition', and 'BulkDeployment'.
      parameters:
        - name: resource-arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The Amazon Resource Name (ARN) of the resource.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  $ref: "#/components/schemas/Tags"
      responses:
        "204":
          description: Success
    delete:
      operationId: UntagResource
      description: Remove resource tags from a Greengrass Resource.
      parameters:
        - name: resource-arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The Amazon Resource Name (ARN) of the resource.
        - name: tagKeys
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/__listOf__string"
          description: An array of tag keys to delete
      responses:
        "204":
          description: Success
  /greengrass/groups/{group_id}/deployments/$reset:
    post:
      operationId: ResetDeployments
      description: Resets a group's deployments.
      parameters:
        - name: X-Amzn-Client-Token
          in: header
          required: false
          schema:
            $ref: "#/components/schemas/__string"
          description: A client token used to correlate requests and responses.
        - name: group_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the Greengrass group.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                Force:
                  $ref: "#/components/schemas/__boolean"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResetDeploymentsResponse"
  /greengrass/bulk/deployments/{bulk_deployment_id}/$stop:
    put:
      operationId: StopBulkDeployment
      description: Stops the execution of a bulk deployment. This action returns a status of ''Stopping'' until the deployment is stopped. You cannot start a new bulk deployment while a previous deployment is in the ''Stopping'' state. This action doesn't rollback completed deployments or cancel pending deployments.
      parameters:
        - name: bulk_deployment_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/__string"
          description: The ID of the bulk deployment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StopBulkDeploymentResponse"
components:
  parameters:
    X-Amz-Content-Sha256:
      in: header
      name: X-Amz-Content-Sha256
      required: false
      schema:
        type: string
    X-Amz-Date:
      in: header
      name: X-Amz-Date
      required: false
      schema:
        type: string
    X-Amz-Algorithm:
      in: header
      name: X-Amz-Algorithm
      required: false
      schema:
        type: string
    X-Amz-Credential:
      in: header
      name: X-Amz-Credential
      required: false
      schema:
        type: string
    X-Amz-Security-Token:
      in: header
      name: X-Amz-Security-Token
      required: false
      schema:
        type: string
    X-Amz-Signature:
      in: header
      name: X-Amz-Signature
      required: false
      schema:
        type: string
    X-Amz-SignedHeaders:
      in: header
      name: X-Amz-SignedHeaders
      required: false
      schema:
        type: string
  schemas:
    __string:
      type: string
    AssociateRoleToGroupResponse:
      type: object
      properties:
        AssociatedAt:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the role ARN was associated with the group.
    AssociateServiceRoleToAccountResponse:
      type: object
      properties:
        AssociatedAt:
          $ref: "#/components/schemas/__string"
          description: The time when the service role was associated with the account.
    ConnectorDefinitionVersion:
      description: Information about the connector definition version, which is a container for connectors.
      type: object
      properties:
        Connectors:
          $ref: "#/components/schemas/__listOfConnector"
          description: A list of references to connectors in this version, with their corresponding configuration settings.
    __listOfConnector:
      type: array
      items:
        $ref: "#/components/schemas/Connector"
    Connector:
      description: Information about a connector. Connectors run on the Greengrass core and contain built-in integration with local infrastructure, device protocols, AWS, and other cloud services.
      type: object
      properties:
        ConnectorArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the connector.
        Id:
          $ref: "#/components/schemas/__string"
          description: A descriptive or arbitrary ID for the connector. This value must be unique within the connector definition version. Max length is 128 characters with pattern [a-zA-Z0-9:_-]+.
        Parameters:
          type: object
          properties: {}
          description: The parameters or configuration that the connector uses.
      required:
        - ConnectorArn
        - Id
    Tags:
      description: The key-value pair for the resource tag.
      type: object
      properties: {}
    CreateConnectorDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateConnectorDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    CoreDefinitionVersion:
      description: Information about a core definition version.
      type: object
      properties:
        Cores:
          $ref: "#/components/schemas/__listOfCore"
          description: A list of cores in the core definition version.
    __listOfCore:
      type: array
      items:
        $ref: "#/components/schemas/Core"
    Core:
      description: Information about a core.
      type: object
      properties:
        CertificateArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the certificate associated with the core.
        Id:
          $ref: "#/components/schemas/__string"
          description: A descriptive or arbitrary ID for the core. This value must be unique within the core definition version. Max length is 128 characters with pattern ''[a-zA-Z0-9:_-]+''.
        SyncShadow:
          $ref: "#/components/schemas/__boolean"
          description: If true, the core's local shadow is automatically synced with the cloud.
        ThingArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the thing which is the core.
      required:
        - ThingArn
        - Id
        - CertificateArn
    __boolean:
      type: boolean
    CreateCoreDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateCoreDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    DeploymentType:
      description: The type of deployment. When used for ''CreateDeployment'', only ''NewDeployment'' and ''Redeployment'' are valid.
      type: string
      enum:
        - NewDeployment
        - Redeployment
        - ResetDeployment
        - ForceResetDeployment
    CreateDeploymentResponse:
      type: object
      properties:
        DeploymentArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the deployment.
        DeploymentId:
          $ref: "#/components/schemas/__string"
          description: The ID of the deployment.
    DeviceDefinitionVersion:
      description: Information about a device definition version.
      type: object
      properties:
        Devices:
          $ref: "#/components/schemas/__listOfDevice"
          description: A list of devices in the definition version.
    __listOfDevice:
      type: array
      items:
        $ref: "#/components/schemas/Device"
    Device:
      description: Information about a device.
      type: object
      properties:
        CertificateArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the certificate associated with the device.
        Id:
          $ref: "#/components/schemas/__string"
          description: A descriptive or arbitrary ID for the device. This value must be unique within the device definition version. Max length is 128 characters with pattern ''[a-zA-Z0-9:_-]+''.
        SyncShadow:
          $ref: "#/components/schemas/__boolean"
          description: If true, the device's local shadow will be automatically synced with the cloud.
        ThingArn:
          $ref: "#/components/schemas/__string"
          description: The thing ARN of the device.
      required:
        - ThingArn
        - Id
        - CertificateArn
    CreateDeviceDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateDeviceDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    FunctionDefinitionVersion:
      description: Information about a function definition version.
      type: object
      properties:
        DefaultConfig:
          $ref: "#/components/schemas/FunctionDefaultConfig"
          description: The default configuration that applies to all Lambda functions in this function definition version. Individual Lambda functions can override these settings.
        Functions:
          $ref: "#/components/schemas/__listOfFunction"
          description: A list of Lambda functions in this function definition version.
    FunctionDefaultConfig:
      description: The default configuration that applies to all Lambda functions in the group. Individual Lambda functions can override these settings.
      type: object
      properties:
        Execution:
          $ref: "#/components/schemas/FunctionDefaultExecutionConfig"
    FunctionDefaultExecutionConfig:
      description: Configuration information that specifies how a Lambda function runs.
      type: object
      properties:
        IsolationMode:
          $ref: "#/components/schemas/FunctionIsolationMode"
        RunAs:
          $ref: "#/components/schemas/FunctionRunAsConfig"
    FunctionIsolationMode:
      description: Specifies whether the Lambda function runs in a Greengrass container (default) or without containerization. Unless your scenario requires that you run without containerization, we recommend that you run in a Greengrass container. Omit this value to run the Lambda function with the default containerization for the group.
      type: string
      enum:
        - GreengrassContainer
        - NoContainer
    FunctionRunAsConfig:
      description: Specifies the user and group whose permissions are used when running the Lambda function. You can specify one or both values to override the default values. We recommend that you avoid running as root unless absolutely necessary to minimize the risk of unintended changes or malicious attacks. To run as root, you must set ''IsolationMode'' to ''NoContainer'' and update config.json in ''greengrass-root/config'' to set ''allowFunctionsToRunAsRoot'' to ''yes''.
      type: object
      properties:
        Gid:
          $ref: "#/components/schemas/__integer"
          description: The group ID whose permissions are used to run a Lambda function.
        Uid:
          $ref: "#/components/schemas/__integer"
          description: The user ID whose permissions are used to run a Lambda function.
    __integer:
      type: integer
    __listOfFunction:
      type: array
      items:
        $ref: "#/components/schemas/Function"
    Function:
      description: Information about a Lambda function.
      type: object
      properties:
        FunctionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the Lambda function.
        FunctionConfiguration:
          $ref: "#/components/schemas/FunctionConfiguration"
          description: The configuration of the Lambda function.
        Id:
          $ref: "#/components/schemas/__string"
          description: A descriptive or arbitrary ID for the function. This value must be unique within the function definition version. Max length is 128 characters with pattern ''[a-zA-Z0-9:_-]+''.
      required:
        - Id
    FunctionConfiguration:
      description: The configuration of the Lambda function.
      type: object
      properties:
        EncodingType:
          $ref: "#/components/schemas/EncodingType"
          description: The expected encoding type of the input payload for the function. The default is ''json''.
        Environment:
          $ref: "#/components/schemas/FunctionConfigurationEnvironment"
          description: The environment configuration of the function.
        ExecArgs:
          $ref: "#/components/schemas/__string"
          description: The execution arguments.
        Executable:
          $ref: "#/components/schemas/__string"
          description: The name of the function executable.
        MemorySize:
          $ref: "#/components/schemas/__integer"
          description: The memory size, in KB, which the function requires. This setting is not applicable and should be cleared when you run the Lambda function without containerization.
        Pinned:
          $ref: "#/components/schemas/__boolean"
          description: True if the function is pinned. Pinned means the function is long-lived and starts when the core starts.
        Timeout:
          $ref: "#/components/schemas/__integer"
          description: The allowed function execution time, after which Lambda should terminate the function. This timeout still applies to pinned Lambda functions for each request.
        FunctionRuntimeOverride:
          $ref: "#/components/schemas/__string"
          description: The Lambda runtime supported by Greengrass which is to be used instead of the one specified in the Lambda function.
    EncodingType:
      type: string
      enum:
        - binary
        - json
    FunctionConfigurationEnvironment:
      description: The environment configuration of the function.
      type: object
      properties:
        AccessSysfs:
          $ref: "#/components/schemas/__boolean"
          description: If true, the Lambda function is allowed to access the host's /sys folder. Use this when the Lambda function needs to read device information from /sys. This setting applies only when you run the Lambda function in a Greengrass container.
        Execution:
          $ref: "#/components/schemas/FunctionExecutionConfig"
          description: Configuration related to executing the Lambda function
        ResourceAccessPolicies:
          $ref: "#/components/schemas/__listOfResourceAccessPolicy"
          description: A list of the resources, with their permissions, to which the Lambda function will be granted access. A Lambda function can have at most 10 resources. ResourceAccessPolicies apply only when you run the Lambda function in a Greengrass container.
        Variables:
          type: object
          properties: {}
          description: Environment variables for the Lambda function's configuration.
    FunctionExecutionConfig:
      description: Configuration information that specifies how a Lambda function runs.
      type: object
      properties:
        IsolationMode:
          $ref: "#/components/schemas/FunctionIsolationMode"
        RunAs:
          $ref: "#/components/schemas/FunctionRunAsConfig"
    __listOfResourceAccessPolicy:
      type: array
      items:
        $ref: "#/components/schemas/ResourceAccessPolicy"
    ResourceAccessPolicy:
      description: A policy used by the function to access a resource.
      type: object
      properties:
        Permission:
          $ref: "#/components/schemas/Permission"
          description: The permissions that the Lambda function has to the resource. Can be one of ''rw'' (read/write) or ''ro'' (read-only).
        ResourceId:
          $ref: "#/components/schemas/__string"
          description: The ID of the resource. (This ID is assigned to the resource when you create the resource definiton.)
      required:
        - ResourceId
    Permission:
      description: The type of permission a function has to access a resource.
      type: string
      enum:
        - ro
        - rw
    CreateFunctionDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateFunctionDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    GroupVersion:
      description: Information about a group version.
      type: object
      properties:
        ConnectorDefinitionVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the connector definition version for this group.
        CoreDefinitionVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the core definition version for this group.
        DeviceDefinitionVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the device definition version for this group.
        FunctionDefinitionVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the function definition version for this group.
        LoggerDefinitionVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the logger definition version for this group.
        ResourceDefinitionVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the resource definition version for this group.
        SubscriptionDefinitionVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the subscription definition version for this group.
    CreateGroupResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateGroupCertificateAuthorityResponse:
      type: object
      properties:
        GroupCertificateAuthorityArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the group certificate authority.
    CreateGroupVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    LoggerDefinitionVersion:
      description: Information about a logger definition version.
      type: object
      properties:
        Loggers:
          $ref: "#/components/schemas/__listOfLogger"
          description: A list of loggers.
    __listOfLogger:
      type: array
      items:
        $ref: "#/components/schemas/Logger"
    Logger:
      description: Information about a logger
      type: object
      properties:
        Component:
          $ref: "#/components/schemas/LoggerComponent"
          description: The component that will be subject to logging.
        Id:
          $ref: "#/components/schemas/__string"
          description: A descriptive or arbitrary ID for the logger. This value must be unique within the logger definition version. Max length is 128 characters with pattern ''[a-zA-Z0-9:_-]+''.
        Level:
          $ref: "#/components/schemas/LoggerLevel"
          description: The level of the logs.
        Space:
          $ref: "#/components/schemas/__integer"
          description: The amount of file space, in KB, to use if the local file system is used for logging purposes.
        Type:
          $ref: "#/components/schemas/LoggerType"
          description: The type of log output which will be used.
      required:
        - Type
        - Level
        - Id
        - Component
    LoggerComponent:
      type: string
      enum:
        - GreengrassSystem
        - Lambda
    LoggerLevel:
      type: string
      enum:
        - DEBUG
        - INFO
        - WARN
        - ERROR
        - FATAL
    LoggerType:
      type: string
      enum:
        - FileSystem
        - AWSCloudWatch
    CreateLoggerDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateLoggerDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    ResourceDefinitionVersion:
      description: Information about a resource definition version.
      type: object
      properties:
        Resources:
          $ref: "#/components/schemas/__listOfResource"
          description: A list of resources.
    __listOfResource:
      type: array
      items:
        $ref: "#/components/schemas/Resource"
    Resource:
      description: Information about a resource.
      type: object
      properties:
        Id:
          $ref: "#/components/schemas/__string"
          description: The resource ID, used to refer to a resource in the Lambda function configuration. Max length is 128 characters with pattern ''[a-zA-Z0-9:_-]+''. This must be unique within a Greengrass group.
        Name:
          $ref: "#/components/schemas/__string"
          description: The descriptive resource name, which is displayed on the AWS IoT Greengrass console. Max length 128 characters with pattern ''[a-zA-Z0-9:_-]+''. This must be unique within a Greengrass group.
        ResourceDataContainer:
          $ref: "#/components/schemas/ResourceDataContainer"
          description: A container of data for all resource types.
      required:
        - ResourceDataContainer
        - Id
        - Name
    ResourceDataContainer:
      description: "A container for resource data. The container takes only one of the following supported resource data types: ''LocalDeviceResourceData'', ''LocalVolumeResourceData'', ''SageMakerMachineLearningModelResourceData'', ''S3MachineLearningModelResourceData'', ''SecretsManagerSecretResourceData''."
      type: object
      properties:
        LocalDeviceResourceData:
          $ref: "#/components/schemas/LocalDeviceResourceData"
          description: Attributes that define the local device resource.
        LocalVolumeResourceData:
          $ref: "#/components/schemas/LocalVolumeResourceData"
          description: Attributes that define the local volume resource.
        S3MachineLearningModelResourceData:
          $ref: "#/components/schemas/S3MachineLearningModelResourceData"
          description: Attributes that define an Amazon S3 machine learning resource.
        SageMakerMachineLearningModelResourceData:
          $ref: "#/components/schemas/SageMakerMachineLearningModelResourceData"
          description: Attributes that define an Amazon SageMaker machine learning resource.
        SecretsManagerSecretResourceData:
          $ref: "#/components/schemas/SecretsManagerSecretResourceData"
          description: Attributes that define a secret resource, which references a secret from AWS Secrets Manager.
    LocalDeviceResourceData:
      description: Attributes that define a local device resource.
      type: object
      properties:
        GroupOwnerSetting:
          $ref: "#/components/schemas/GroupOwnerSetting"
          description: Group/owner related settings for local resources.
        SourcePath:
          $ref: "#/components/schemas/__string"
          description: The local absolute path of the device resource. The source path for a device resource can refer only to a character device or block device under ''/dev''.
    GroupOwnerSetting:
      description: Group owner related settings for local resources.
      type: object
      properties:
        AutoAddGroupOwner:
          $ref: "#/components/schemas/__boolean"
          description: If true, AWS IoT Greengrass automatically adds the specified Linux OS group owner of the resource to the Lambda process privileges. Thus the Lambda process will have the file access permissions of the added Linux group.
        GroupOwner:
          $ref: "#/components/schemas/__string"
          description: The name of the Linux OS group whose privileges will be added to the Lambda process. This field is optional.
    LocalVolumeResourceData:
      description: Attributes that define a local volume resource.
      type: object
      properties:
        DestinationPath:
          $ref: "#/components/schemas/__string"
          description: The absolute local path of the resource inside the Lambda environment.
        GroupOwnerSetting:
          $ref: "#/components/schemas/GroupOwnerSetting"
          description: Allows you to configure additional group privileges for the Lambda process. This field is optional.
        SourcePath:
          $ref: "#/components/schemas/__string"
          description: The local absolute path of the volume resource on the host. The source path for a volume resource type cannot start with ''/sys''.
    S3MachineLearningModelResourceData:
      description: Attributes that define an Amazon S3 machine learning resource.
      type: object
      properties:
        DestinationPath:
          $ref: "#/components/schemas/__string"
          description: The absolute local path of the resource inside the Lambda environment.
        OwnerSetting:
          $ref: "#/components/schemas/ResourceDownloadOwnerSetting"
        S3Uri:
          $ref: "#/components/schemas/__string"
          description: The URI of the source model in an S3 bucket. The model package must be in tar.gz or .zip format.
    ResourceDownloadOwnerSetting:
      description: The owner setting for downloaded machine learning resources.
      type: object
      properties:
        GroupOwner:
          $ref: "#/components/schemas/__string"
          description: The group owner of the resource. This is the name of an existing Linux OS group on the system or a GID. The group's permissions are added to the Lambda process.
        GroupPermission:
          $ref: "#/components/schemas/Permission"
          description: The permissions that the group owner has to the resource. Valid values are ''rw'' (read/write) or ''ro'' (read-only).
      required:
        - GroupOwner
        - GroupPermission
    SageMakerMachineLearningModelResourceData:
      description: Attributes that define an Amazon SageMaker machine learning resource.
      type: object
      properties:
        DestinationPath:
          $ref: "#/components/schemas/__string"
          description: The absolute local path of the resource inside the Lambda environment.
        OwnerSetting:
          $ref: "#/components/schemas/ResourceDownloadOwnerSetting"
        SageMakerJobArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the Amazon SageMaker training job that represents the source model.
    SecretsManagerSecretResourceData:
      description: Attributes that define a secret resource, which references a secret from AWS Secrets Manager. AWS IoT Greengrass stores a local, encrypted copy of the secret on the Greengrass core, where it can be securely accessed by connectors and Lambda functions.
      type: object
      properties:
        ARN:
          $ref: "#/components/schemas/__string"
          description: The ARN of the Secrets Manager secret to make available on the core. The value of the secret's latest version (represented by the ''AWSCURRENT'' staging label) is included by default.
        AdditionalStagingLabelsToDownload:
          $ref: "#/components/schemas/__listOf__string"
          description: Optional. The staging labels whose values you want to make available on the core, in addition to ''AWSCURRENT''.
    __listOf__string:
      type: array
      items:
        $ref: "#/components/schemas/__string"
    CreateResourceDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateResourceDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    S3UrlSignerRole:
      description: The IAM Role that Greengrass will use to create pre-signed URLs pointing towards the update artifact.
      type: string
    SoftwareToUpdate:
      description: The piece of software on the Greengrass core that will be updated.
      type: string
      enum:
        - core
        - ota_agent
    UpdateAgentLogLevel:
      description: The minimum level of log statements that should be logged by the OTA Agent during an update.
      type: string
      enum:
        - NONE
        - TRACE
        - DEBUG
        - VERBOSE
        - INFO
        - WARN
        - ERROR
        - FATAL
    UpdateTargets:
      description: The ARNs of the targets (IoT things or IoT thing groups) that this update will be applied to.
      type: array
      items:
        $ref: "#/components/schemas/__string"
    UpdateTargetsArchitecture:
      description: The architecture of the cores which are the targets of an update.
      type: string
      enum:
        - armv6l
        - armv7l
        - x86_64
        - aarch64
    UpdateTargetsOperatingSystem:
      description: The operating system of the cores which are the targets of an update.
      type: string
      enum:
        - ubuntu
        - raspbian
        - amazon_linux
        - openwrt
    CreateSoftwareUpdateJobResponse:
      type: object
      properties:
        IotJobArn:
          $ref: "#/components/schemas/__string"
          description: The IoT Job ARN corresponding to this update.
        IotJobId:
          $ref: "#/components/schemas/__string"
          description: The IoT Job Id corresponding to this update.
        PlatformSoftwareVersion:
          $ref: "#/components/schemas/__string"
          description: The software version installed on the device or devices after the update.
    SubscriptionDefinitionVersion:
      description: Information about a subscription definition version.
      type: object
      properties:
        Subscriptions:
          $ref: "#/components/schemas/__listOfSubscription"
          description: A list of subscriptions.
    __listOfSubscription:
      type: array
      items:
        $ref: "#/components/schemas/Subscription"
    Subscription:
      description: Information about a subscription.
      type: object
      properties:
        Id:
          $ref: "#/components/schemas/__string"
          description: A descriptive or arbitrary ID for the subscription. This value must be unique within the subscription definition version. Max length is 128 characters with pattern ''[a-zA-Z0-9:_-]+''.
        Source:
          $ref: "#/components/schemas/__string"
          description: The source of the subscription. Can be a thing ARN, a Lambda function ARN, a connector ARN, 'cloud' (which represents the AWS IoT cloud), or 'GGShadowService'.
        Subject:
          $ref: "#/components/schemas/__string"
          description: The MQTT topic used to route the message.
        Target:
          $ref: "#/components/schemas/__string"
          description: Where the message is sent to. Can be a thing ARN, a Lambda function ARN, a connector ARN, 'cloud' (which represents the AWS IoT cloud), or 'GGShadowService'.
      required:
        - Target
        - Id
        - Subject
        - Source
    CreateSubscriptionDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
    CreateSubscriptionDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    DeleteConnectorDefinitionResponse:
      type: object
    DeleteCoreDefinitionResponse:
      type: object
    DeleteDeviceDefinitionResponse:
      type: object
    DeleteFunctionDefinitionResponse:
      type: object
    DeleteGroupResponse:
      type: object
    DeleteLoggerDefinitionResponse:
      type: object
    DeleteResourceDefinitionResponse:
      type: object
    DeleteSubscriptionDefinitionResponse:
      type: object
    DisassociateRoleFromGroupResponse:
      type: object
      properties:
        DisassociatedAt:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the role was disassociated from the group.
    DisassociateServiceRoleFromAccountResponse:
      type: object
      properties:
        DisassociatedAt:
          $ref: "#/components/schemas/__string"
          description: The time when the service role was disassociated from the account.
    GetAssociatedRoleResponse:
      type: object
      properties:
        AssociatedAt:
          $ref: "#/components/schemas/__string"
          description: The time when the role was associated with the group.
        RoleArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the role that is associated with the group.
    GetBulkDeploymentStatusResponse:
      type: object
      properties:
        BulkDeploymentMetrics:
          $ref: "#/components/schemas/BulkDeploymentMetrics"
          description: Relevant metrics on input records processed during bulk deployment.
        BulkDeploymentStatus:
          $ref: "#/components/schemas/BulkDeploymentStatus"
          description: The status of the bulk deployment.
        CreatedAt:
          $ref: "#/components/schemas/__string"
          description: The time, in ISO format, when the deployment was created.
        ErrorDetails:
          $ref: "#/components/schemas/ErrorDetails"
          description: Error details
        ErrorMessage:
          $ref: "#/components/schemas/__string"
          description: Error message
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    BulkDeploymentMetrics:
      description: Relevant metrics on input records processed during bulk deployment.
      type: object
      properties:
        InvalidInputRecords:
          $ref: "#/components/schemas/__integer"
          description: The total number of records that returned a non-retryable error. For example, this can occur if a group record from the input file uses an invalid format or specifies a nonexistent group version, or if the execution role doesn't grant permission to deploy a group or group version.
        RecordsProcessed:
          $ref: "#/components/schemas/__integer"
          description: The total number of group records from the input file that have been processed so far, or attempted.
        RetryAttempts:
          $ref: "#/components/schemas/__integer"
          description: The total number of deployment attempts that returned a retryable error. For example, a retry is triggered if the attempt to deploy a group returns a throttling error. ''StartBulkDeployment'' retries a group deployment up to five times.
    BulkDeploymentStatus:
      description: The current status of the bulk deployment.
      type: string
      enum:
        - Initializing
        - Running
        - Completed
        - Stopping
        - Stopped
        - Failed
    ErrorDetails:
      description: A list of error details.
      type: array
      items:
        $ref: "#/components/schemas/ErrorDetail"
    ErrorDetail:
      description: Details about the error.
      type: object
      properties:
        DetailedErrorCode:
          $ref: "#/components/schemas/__string"
          description: A detailed error code.
        DetailedErrorMessage:
          $ref: "#/components/schemas/__string"
          description: A detailed error message.
    GetConnectivityInfoResponse:
      type: object
      properties:
        ConnectivityInfo:
          $ref: "#/components/schemas/__listOfConnectivityInfo"
          description: Connectivity info list.
        Message:
          $ref: "#/components/schemas/__string"
          description: A message about the connectivity info request.
    __listOfConnectivityInfo:
      type: array
      items:
        $ref: "#/components/schemas/ConnectivityInfo"
    ConnectivityInfo:
      description: Information about a Greengrass core's connectivity.
      type: object
      properties:
        HostAddress:
          $ref: "#/components/schemas/__string"
          description: The endpoint for the Greengrass core. Can be an IP address or DNS.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the connectivity information.
        Metadata:
          $ref: "#/components/schemas/__string"
          description: Metadata for this endpoint.
        PortNumber:
          $ref: "#/components/schemas/__integer"
          description: The port of the Greengrass core. Usually 8883.
    GetConnectorDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetConnectorDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the connector definition version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the connector definition version was created.
        Definition:
          $ref: "#/components/schemas/ConnectorDefinitionVersion"
          description: Information about the connector definition version.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the connector definition version.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Version:
          $ref: "#/components/schemas/__string"
          description: The version of the connector definition version.
    GetCoreDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetCoreDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the core definition version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the core definition version was created.
        Definition:
          $ref: "#/components/schemas/CoreDefinitionVersion"
          description: Information about the core definition version.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the core definition version.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Version:
          $ref: "#/components/schemas/__string"
          description: The version of the core definition version.
    GetDeploymentStatusResponse:
      type: object
      properties:
        DeploymentStatus:
          $ref: "#/components/schemas/__string"
          description: "The status of the deployment: ''InProgress'', ''Building'', ''Success'', or ''Failure''."
        DeploymentType:
          $ref: "#/components/schemas/DeploymentType"
          description: The type of the deployment.
        ErrorDetails:
          $ref: "#/components/schemas/ErrorDetails"
          description: Error details
        ErrorMessage:
          $ref: "#/components/schemas/__string"
          description: Error message
        UpdatedAt:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the deployment status was updated.
    GetDeviceDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetDeviceDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the device definition version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the device definition version was created.
        Definition:
          $ref: "#/components/schemas/DeviceDefinitionVersion"
          description: Information about the device definition version.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the device definition version.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Version:
          $ref: "#/components/schemas/__string"
          description: The version of the device definition version.
    GetFunctionDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetFunctionDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the function definition version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the function definition version was created.
        Definition:
          $ref: "#/components/schemas/FunctionDefinitionVersion"
          description: Information on the definition.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the function definition version.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Version:
          $ref: "#/components/schemas/__string"
          description: The version of the function definition version.
    GetGroupResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetGroupCertificateAuthorityResponse:
      type: object
      properties:
        GroupCertificateAuthorityArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the certificate authority for the group.
        GroupCertificateAuthorityId:
          $ref: "#/components/schemas/__string"
          description: The ID of the certificate authority for the group.
        PemEncodedCertificate:
          $ref: "#/components/schemas/__string"
          description: The PEM encoded certificate for the group.
    GetGroupCertificateConfigurationResponse:
      type: object
      properties:
        CertificateAuthorityExpiryInMilliseconds:
          $ref: "#/components/schemas/__string"
          description: The amount of time remaining before the certificate authority expires, in milliseconds.
        CertificateExpiryInMilliseconds:
          $ref: "#/components/schemas/__string"
          description: The amount of time remaining before the certificate expires, in milliseconds.
        GroupId:
          $ref: "#/components/schemas/__string"
          description: The ID of the group certificate configuration.
    GetGroupVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the group version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the group version was created.
        Definition:
          $ref: "#/components/schemas/GroupVersion"
          description: Information about the group version definition.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the group that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the group version.
    GetLoggerDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetLoggerDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the logger definition version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the logger definition version was created.
        Definition:
          $ref: "#/components/schemas/LoggerDefinitionVersion"
          description: Information about the logger definition version.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the logger definition version.
        Version:
          $ref: "#/components/schemas/__string"
          description: The version of the logger definition version.
    GetResourceDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetResourceDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: Arn of the resource definition version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the resource definition version was created.
        Definition:
          $ref: "#/components/schemas/ResourceDefinitionVersion"
          description: Information about the definition.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the resource definition version.
        Version:
          $ref: "#/components/schemas/__string"
          description: The version of the resource definition version.
    GetServiceRoleForAccountResponse:
      type: object
      properties:
        AssociatedAt:
          $ref: "#/components/schemas/__string"
          description: The time when the service role was associated with the account.
        RoleArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the role which is associated with the account.
    GetSubscriptionDefinitionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    GetSubscriptionDefinitionVersionResponse:
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the subscription definition version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the subscription definition version was created.
        Definition:
          $ref: "#/components/schemas/SubscriptionDefinitionVersion"
          description: Information about the subscription definition version.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the subscription definition version.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Version:
          $ref: "#/components/schemas/__string"
          description: The version of the subscription definition version.
    GetThingRuntimeConfigurationResponse:
      type: object
      properties:
        RuntimeConfiguration:
          $ref: "#/components/schemas/RuntimeConfiguration"
          description: Runtime configuration for a thing.
    RuntimeConfiguration:
      description: Runtime configuration for a thing.
      type: object
      properties:
        TelemetryConfiguration:
          $ref: "#/components/schemas/TelemetryConfiguration"
          description: Configuration for telemetry service.
    TelemetryConfiguration:
      description: Configuration settings for running telemetry.
      type: object
      properties:
        ConfigurationSyncStatus:
          $ref: "#/components/schemas/ConfigurationSyncStatus"
          description: Synchronization status of the device reported configuration with the desired configuration.
        Telemetry:
          $ref: "#/components/schemas/Telemetry"
          description: Configure telemetry to be on or off.
      required:
        - Telemetry
    ConfigurationSyncStatus:
      type: string
      enum:
        - InSync
        - OutOfSync
    Telemetry:
      type: string
      enum:
        - "On"
        - "Off"
    ListBulkDeploymentDetailedReportsResponse:
      type: object
      properties:
        Deployments:
          $ref: "#/components/schemas/BulkDeploymentResults"
          description: A list of the individual group deployments in the bulk deployment operation.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    BulkDeploymentResults:
      type: array
      items:
        $ref: "#/components/schemas/BulkDeploymentResult"
    BulkDeploymentResult:
      description: Information about an individual group deployment in a bulk deployment operation.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/__string"
          description: The time, in ISO format, when the deployment was created.
        DeploymentArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the group deployment.
        DeploymentId:
          $ref: "#/components/schemas/__string"
          description: The ID of the group deployment.
        DeploymentStatus:
          $ref: "#/components/schemas/__string"
          description: "The current status of the group deployment: ''InProgress'', ''Building'', ''Success'', or ''Failure''."
        DeploymentType:
          $ref: "#/components/schemas/DeploymentType"
          description: The type of the deployment.
        ErrorDetails:
          $ref: "#/components/schemas/ErrorDetails"
          description: Details about the error.
        ErrorMessage:
          $ref: "#/components/schemas/__string"
          description: The error message for a failed deployment
        GroupArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the Greengrass group.
    ListBulkDeploymentsResponse:
      type: object
      properties:
        BulkDeployments:
          $ref: "#/components/schemas/BulkDeployments"
          description: A list of bulk deployments.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    BulkDeployments:
      type: array
      items:
        $ref: "#/components/schemas/BulkDeployment"
    BulkDeployment:
      description: Information about a bulk deployment. You cannot start a new bulk deployment while another one is still running or in a non-terminal state.
      type: object
      properties:
        BulkDeploymentArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the bulk deployment.
        BulkDeploymentId:
          $ref: "#/components/schemas/__string"
          description: The ID of the bulk deployment.
        CreatedAt:
          $ref: "#/components/schemas/__string"
          description: The time, in ISO format, when the deployment was created.
    ListConnectorDefinitionVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    __listOfVersionInformation:
      type: array
      items:
        $ref: "#/components/schemas/VersionInformation"
    VersionInformation:
      description: Information about a version.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the version.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the version was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the parent definition that the version is associated with.
        Version:
          $ref: "#/components/schemas/__string"
          description: The ID of the version.
    ListConnectorDefinitionsResponse:
      type: object
      properties:
        Definitions:
          $ref: "#/components/schemas/__listOfDefinitionInformation"
          description: Information about a definition.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    __listOfDefinitionInformation:
      type: array
      items:
        $ref: "#/components/schemas/DefinitionInformation"
    DefinitionInformation:
      description: Information about a definition.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the definition.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the definition.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the definition was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the definition.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the definition.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the definition.
        Tags:
          type: object
          properties: {}
          description: Tag(s) attached to the resource arn.
    ListCoreDefinitionVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    ListCoreDefinitionsResponse:
      type: object
      properties:
        Definitions:
          $ref: "#/components/schemas/__listOfDefinitionInformation"
          description: Information about a definition.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    ListDeploymentsResponse:
      type: object
      properties:
        Deployments:
          $ref: "#/components/schemas/Deployments"
          description: A list of deployments for the requested groups.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    Deployments:
      type: array
      items:
        $ref: "#/components/schemas/Deployment"
    Deployment:
      description: Information about a deployment.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the deployment was created.
        DeploymentArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the deployment.
        DeploymentId:
          $ref: "#/components/schemas/__string"
          description: The ID of the deployment.
        DeploymentType:
          $ref: "#/components/schemas/DeploymentType"
          description: The type of the deployment.
        GroupArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the group for this deployment.
    ListDeviceDefinitionVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    ListDeviceDefinitionsResponse:
      type: object
      properties:
        Definitions:
          $ref: "#/components/schemas/__listOfDefinitionInformation"
          description: Information about a definition.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    ListFunctionDefinitionVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    ListFunctionDefinitionsResponse:
      type: object
      properties:
        Definitions:
          $ref: "#/components/schemas/__listOfDefinitionInformation"
          description: Information about a definition.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    ListGroupCertificateAuthoritiesResponse:
      type: object
      properties:
        GroupCertificateAuthorities:
          $ref: "#/components/schemas/__listOfGroupCertificateAuthorityProperties"
          description: A list of certificate authorities associated with the group.
    __listOfGroupCertificateAuthorityProperties:
      type: array
      items:
        $ref: "#/components/schemas/GroupCertificateAuthorityProperties"
    GroupCertificateAuthorityProperties:
      description: Information about a certificate authority for a group.
      type: object
      properties:
        GroupCertificateAuthorityArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the certificate authority for the group.
        GroupCertificateAuthorityId:
          $ref: "#/components/schemas/__string"
          description: The ID of the certificate authority for the group.
    ListGroupVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    ListGroupsResponse:
      type: object
      properties:
        Groups:
          $ref: "#/components/schemas/__listOfGroupInformation"
          description: Information about a group.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    __listOfGroupInformation:
      type: array
      items:
        $ref: "#/components/schemas/GroupInformation"
    GroupInformation:
      description: Information about a group.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the group.
        CreationTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the group was created.
        Id:
          $ref: "#/components/schemas/__string"
          description: The ID of the group.
        LastUpdatedTimestamp:
          $ref: "#/components/schemas/__string"
          description: The time, in milliseconds since the epoch, when the group was last updated.
        LatestVersion:
          $ref: "#/components/schemas/__string"
          description: The ID of the latest version associated with the group.
        LatestVersionArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the latest version associated with the group.
        Name:
          $ref: "#/components/schemas/__string"
          description: The name of the group.
    ListLoggerDefinitionVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    ListLoggerDefinitionsResponse:
      type: object
      properties:
        Definitions:
          $ref: "#/components/schemas/__listOfDefinitionInformation"
          description: Information about a definition.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    ListResourceDefinitionVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    ListResourceDefinitionsResponse:
      type: object
      properties:
        Definitions:
          $ref: "#/components/schemas/__listOfDefinitionInformation"
          description: Information about a definition.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    ListSubscriptionDefinitionVersionsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
        Versions:
          $ref: "#/components/schemas/__listOfVersionInformation"
          description: Information about a version.
    ListSubscriptionDefinitionsResponse:
      type: object
      properties:
        Definitions:
          $ref: "#/components/schemas/__listOfDefinitionInformation"
          description: Information about a definition.
        NextToken:
          $ref: "#/components/schemas/__string"
          description: The token for the next set of results, or ''null'' if there are no additional results.
    ListTagsForResourceResponse:
      type: object
      properties:
        tags:
          type: object
          properties: {}
    ResetDeploymentsResponse:
      type: object
      properties:
        DeploymentArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the deployment.
        DeploymentId:
          $ref: "#/components/schemas/__string"
          description: The ID of the deployment.
    StartBulkDeploymentResponse:
      type: object
      properties:
        BulkDeploymentArn:
          $ref: "#/components/schemas/__string"
          description: The ARN of the bulk deployment.
        BulkDeploymentId:
          $ref: "#/components/schemas/__string"
          description: The ID of the bulk deployment.
    StopBulkDeploymentResponse:
      type: object
    UpdateConnectivityInfoResponse:
      type: object
      properties:
        Message:
          $ref: "#/components/schemas/__string"
          description: A message about the connectivity info update request.
        Version:
          $ref: "#/components/schemas/__string"
          description: The new version of the connectivity info.
    UpdateConnectorDefinitionResponse:
      type: object
    UpdateCoreDefinitionResponse:
      type: object
    UpdateDeviceDefinitionResponse:
      type: object
    UpdateFunctionDefinitionResponse:
      type: object
    UpdateGroupResponse:
      type: object
    UpdateGroupCertificateConfigurationResponse:
      type: object
      properties:
        CertificateAuthorityExpiryInMilliseconds:
          $ref: "#/components/schemas/__string"
          description: The amount of time remaining before the certificate authority expires, in milliseconds.
        CertificateExpiryInMilliseconds:
          $ref: "#/components/schemas/__string"
          description: The amount of time remaining before the certificate expires, in milliseconds.
        GroupId:
          $ref: "#/components/schemas/__string"
          description: The ID of the group certificate configuration.
    UpdateLoggerDefinitionResponse:
      type: object
    UpdateResourceDefinitionResponse:
      type: object
    UpdateSubscriptionDefinitionResponse:
      type: object
    TelemetryConfigurationUpdate:
      description: Configuration settings for running telemetry.
      type: object
      properties:
        Telemetry:
          $ref: "#/components/schemas/Telemetry"
          description: Configure telemetry to be on or off.
      required:
        - Telemetry
    UpdateThingRuntimeConfigurationResponse:
      type: object
  x-stackQL-resources:
    groups:
      id: aws.greengrass.groups
      name: groups
      title: groups
      methods:
        associate_role_to_group:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1role/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        disassociate_role_from_group:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1role/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        create_group:
          operation:
            $ref: "#/paths/~1greengrass~1groups/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_groups:
          operation:
            $ref: "#/paths/~1greengrass~1groups/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Groups
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_group:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_group:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_group:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/groups/methods/get_group"
          - $ref: "#/components/x-stackQL-resources/groups/methods/list_groups"
        insert:
          - $ref: "#/components/x-stackQL-resources/groups/methods/create_group"
        update:
          - $ref: "#/components/x-stackQL-resources/groups/methods/associate_role_to_group"
          - $ref: "#/components/x-stackQL-resources/groups/methods/update_group"
        delete:
          - $ref: "#/components/x-stackQL-resources/groups/methods/delete_group"
    associated_roles:
      id: aws.greengrass.associated_roles
      name: associated_roles
      title: associated_roles
      methods:
        get_associated_role:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1role/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/associated_roles/methods/get_associated_role"
        insert: []
        update: []
        delete: []
    service_role_to_accounts:
      id: aws.greengrass.service_role_to_accounts
      name: service_role_to_accounts
      title: service_role_to_accounts
      methods:
        associate_service_role_to_account:
          operation:
            $ref: "#/paths/~1greengrass~1servicerole/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select: []
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/service_role_to_accounts/methods/associate_service_role_to_account"
        delete: []
    service_role_from_accounts:
      id: aws.greengrass.service_role_from_accounts
      name: service_role_from_accounts
      title: service_role_from_accounts
      methods:
        disassociate_service_role_from_account:
          operation:
            $ref: "#/paths/~1greengrass~1servicerole/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select: []
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/service_role_from_accounts/methods/disassociate_service_role_from_account"
        delete: []
    service_role_for_accounts:
      id: aws.greengrass.service_role_for_accounts
      name: service_role_for_accounts
      title: service_role_for_accounts
      methods:
        get_service_role_for_account:
          operation:
            $ref: "#/paths/~1greengrass~1servicerole/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/service_role_for_accounts/methods/get_service_role_for_account"
        insert: []
        update: []
        delete: []
    connector_definitions:
      id: aws.greengrass.connector_definitions
      name: connector_definitions
      title: connector_definitions
      methods:
        create_connector_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_connector_definitions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Definitions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_connector_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors~1{connector_definition_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_connector_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors~1{connector_definition_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_connector_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors~1{connector_definition_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/connector_definitions/methods/get_connector_definition"
          - $ref: "#/components/x-stackQL-resources/connector_definitions/methods/list_connector_definitions"
        insert:
          - $ref: "#/components/x-stackQL-resources/connector_definitions/methods/create_connector_definition"
        update:
          - $ref: "#/components/x-stackQL-resources/connector_definitions/methods/update_connector_definition"
        delete:
          - $ref: "#/components/x-stackQL-resources/connector_definitions/methods/delete_connector_definition"
    connector_definition_versions:
      id: aws.greengrass.connector_definition_versions
      name: connector_definition_versions
      title: connector_definition_versions
      methods:
        create_connector_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors~1{connector_definition_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_connector_definition_versions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors~1{connector_definition_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_connector_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1connectors~1{connector_definition_id}~1versions~1{connector_definition_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/connector_definition_versions/methods/get_connector_definition_version"
          - $ref: "#/components/x-stackQL-resources/connector_definition_versions/methods/list_connector_definition_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/connector_definition_versions/methods/create_connector_definition_version"
        update: []
        delete: []
    core_definitions:
      id: aws.greengrass.core_definitions
      name: core_definitions
      title: core_definitions
      methods:
        create_core_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_core_definitions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Definitions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_core_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores~1{core_definition_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_core_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores~1{core_definition_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_core_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores~1{core_definition_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/core_definitions/methods/get_core_definition"
          - $ref: "#/components/x-stackQL-resources/core_definitions/methods/list_core_definitions"
        insert:
          - $ref: "#/components/x-stackQL-resources/core_definitions/methods/create_core_definition"
        update:
          - $ref: "#/components/x-stackQL-resources/core_definitions/methods/update_core_definition"
        delete:
          - $ref: "#/components/x-stackQL-resources/core_definitions/methods/delete_core_definition"
    core_definition_versions:
      id: aws.greengrass.core_definition_versions
      name: core_definition_versions
      title: core_definition_versions
      methods:
        create_core_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores~1{core_definition_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_core_definition_versions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores~1{core_definition_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_core_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1cores~1{core_definition_id}~1versions~1{core_definition_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/core_definition_versions/methods/get_core_definition_version"
          - $ref: "#/components/x-stackQL-resources/core_definition_versions/methods/list_core_definition_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/core_definition_versions/methods/create_core_definition_version"
        update: []
        delete: []
    deployments:
      id: aws.greengrass.deployments
      name: deployments
      title: deployments
      methods:
        create_deployment:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1deployments/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_deployments:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1deployments/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Deployments
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        reset_deployments:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1deployments~1$reset/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/deployments/methods/list_deployments"
        insert:
          - $ref: "#/components/x-stackQL-resources/deployments/methods/create_deployment"
        update: []
        delete: []
    device_definitions:
      id: aws.greengrass.device_definitions
      name: device_definitions
      title: device_definitions
      methods:
        create_device_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_device_definitions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Definitions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_device_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices~1{device_definition_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_device_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices~1{device_definition_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_device_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices~1{device_definition_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/device_definitions/methods/get_device_definition"
          - $ref: "#/components/x-stackQL-resources/device_definitions/methods/list_device_definitions"
        insert:
          - $ref: "#/components/x-stackQL-resources/device_definitions/methods/create_device_definition"
        update:
          - $ref: "#/components/x-stackQL-resources/device_definitions/methods/update_device_definition"
        delete:
          - $ref: "#/components/x-stackQL-resources/device_definitions/methods/delete_device_definition"
    device_definition_versions:
      id: aws.greengrass.device_definition_versions
      name: device_definition_versions
      title: device_definition_versions
      methods:
        create_device_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices~1{device_definition_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_device_definition_versions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices~1{device_definition_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_device_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1devices~1{device_definition_id}~1versions~1{device_definition_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/device_definition_versions/methods/get_device_definition_version"
          - $ref: "#/components/x-stackQL-resources/device_definition_versions/methods/list_device_definition_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/device_definition_versions/methods/create_device_definition_version"
        update: []
        delete: []
    function_definitions:
      id: aws.greengrass.function_definitions
      name: function_definitions
      title: function_definitions
      methods:
        create_function_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_function_definitions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Definitions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_function_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions~1{function_definition_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_function_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions~1{function_definition_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_function_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions~1{function_definition_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/function_definitions/methods/get_function_definition"
          - $ref: "#/components/x-stackQL-resources/function_definitions/methods/list_function_definitions"
        insert:
          - $ref: "#/components/x-stackQL-resources/function_definitions/methods/create_function_definition"
        update:
          - $ref: "#/components/x-stackQL-resources/function_definitions/methods/update_function_definition"
        delete:
          - $ref: "#/components/x-stackQL-resources/function_definitions/methods/delete_function_definition"
    function_definition_versions:
      id: aws.greengrass.function_definition_versions
      name: function_definition_versions
      title: function_definition_versions
      methods:
        create_function_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions~1{function_definition_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_function_definition_versions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions~1{function_definition_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_function_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1functions~1{function_definition_id}~1versions~1{function_definition_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/function_definition_versions/methods/get_function_definition_version"
          - $ref: "#/components/x-stackQL-resources/function_definition_versions/methods/list_function_definition_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/function_definition_versions/methods/create_function_definition_version"
        update: []
        delete: []
    group_certificate_authorities:
      id: aws.greengrass.group_certificate_authorities
      name: group_certificate_authorities
      title: group_certificate_authorities
      methods:
        create_group_certificate_authority:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1certificateauthorities/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_group_certificate_authorities:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1certificateauthorities/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_group_certificate_authority:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1certificateauthorities~1{certificate_authority_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/group_certificate_authorities/methods/get_group_certificate_authority"
          - $ref: "#/components/x-stackQL-resources/group_certificate_authorities/methods/list_group_certificate_authorities"
        insert:
          - $ref: "#/components/x-stackQL-resources/group_certificate_authorities/methods/create_group_certificate_authority"
        update: []
        delete: []
    group_versions:
      id: aws.greengrass.group_versions
      name: group_versions
      title: group_versions
      methods:
        create_group_version:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_group_versions:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_group_version:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1versions~1{group_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/group_versions/methods/get_group_version"
          - $ref: "#/components/x-stackQL-resources/group_versions/methods/list_group_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/group_versions/methods/create_group_version"
        update: []
        delete: []
    logger_definitions:
      id: aws.greengrass.logger_definitions
      name: logger_definitions
      title: logger_definitions
      methods:
        create_logger_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_logger_definitions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Definitions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_logger_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers~1{logger_definition_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_logger_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers~1{logger_definition_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_logger_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers~1{logger_definition_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/logger_definitions/methods/get_logger_definition"
          - $ref: "#/components/x-stackQL-resources/logger_definitions/methods/list_logger_definitions"
        insert:
          - $ref: "#/components/x-stackQL-resources/logger_definitions/methods/create_logger_definition"
        update:
          - $ref: "#/components/x-stackQL-resources/logger_definitions/methods/update_logger_definition"
        delete:
          - $ref: "#/components/x-stackQL-resources/logger_definitions/methods/delete_logger_definition"
    logger_definition_versions:
      id: aws.greengrass.logger_definition_versions
      name: logger_definition_versions
      title: logger_definition_versions
      methods:
        create_logger_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers~1{logger_definition_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_logger_definition_versions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers~1{logger_definition_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_logger_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1loggers~1{logger_definition_id}~1versions~1{logger_definition_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/logger_definition_versions/methods/get_logger_definition_version"
          - $ref: "#/components/x-stackQL-resources/logger_definition_versions/methods/list_logger_definition_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/logger_definition_versions/methods/create_logger_definition_version"
        update: []
        delete: []
    resource_definitions:
      id: aws.greengrass.resource_definitions
      name: resource_definitions
      title: resource_definitions
      methods:
        create_resource_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_resource_definitions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Definitions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_resource_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources~1{resource_definition_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_resource_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources~1{resource_definition_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_resource_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources~1{resource_definition_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_definitions/methods/get_resource_definition"
          - $ref: "#/components/x-stackQL-resources/resource_definitions/methods/list_resource_definitions"
        insert:
          - $ref: "#/components/x-stackQL-resources/resource_definitions/methods/create_resource_definition"
        update:
          - $ref: "#/components/x-stackQL-resources/resource_definitions/methods/update_resource_definition"
        delete:
          - $ref: "#/components/x-stackQL-resources/resource_definitions/methods/delete_resource_definition"
    resource_definition_versions:
      id: aws.greengrass.resource_definition_versions
      name: resource_definition_versions
      title: resource_definition_versions
      methods:
        create_resource_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources~1{resource_definition_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_resource_definition_versions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources~1{resource_definition_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_resource_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1resources~1{resource_definition_id}~1versions~1{resource_definition_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_definition_versions/methods/get_resource_definition_version"
          - $ref: "#/components/x-stackQL-resources/resource_definition_versions/methods/list_resource_definition_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/resource_definition_versions/methods/create_resource_definition_version"
        update: []
        delete: []
    software_update_jobs:
      id: aws.greengrass.software_update_jobs
      name: software_update_jobs
      title: software_update_jobs
      methods:
        create_software_update_job:
          operation:
            $ref: "#/paths/~1greengrass~1updates/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select: []
        insert:
          - $ref: "#/components/x-stackQL-resources/software_update_jobs/methods/create_software_update_job"
        update: []
        delete: []
    subscription_definitions:
      id: aws.greengrass.subscription_definitions
      name: subscription_definitions
      title: subscription_definitions
      methods:
        create_subscription_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_subscription_definitions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Definitions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        delete_subscription_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions~1{subscription_definition_id}/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_subscription_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions~1{subscription_definition_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_subscription_definition:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions~1{subscription_definition_id}/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/subscription_definitions/methods/get_subscription_definition"
          - $ref: "#/components/x-stackQL-resources/subscription_definitions/methods/list_subscription_definitions"
        insert:
          - $ref: "#/components/x-stackQL-resources/subscription_definitions/methods/create_subscription_definition"
        update:
          - $ref: "#/components/x-stackQL-resources/subscription_definitions/methods/update_subscription_definition"
        delete:
          - $ref: "#/components/x-stackQL-resources/subscription_definitions/methods/delete_subscription_definition"
    subscription_definition_versions:
      id: aws.greengrass.subscription_definition_versions
      name: subscription_definition_versions
      title: subscription_definition_versions
      methods:
        create_subscription_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions~1{subscription_definition_id}~1versions/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        list_subscription_definition_versions:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions~1{subscription_definition_id}~1versions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Versions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
        get_subscription_definition_version:
          operation:
            $ref: "#/paths/~1greengrass~1definition~1subscriptions~1{subscription_definition_id}~1versions~1{subscription_definition_version_id}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/subscription_definition_versions/methods/get_subscription_definition_version"
          - $ref: "#/components/x-stackQL-resources/subscription_definition_versions/methods/list_subscription_definition_versions"
        insert:
          - $ref: "#/components/x-stackQL-resources/subscription_definition_versions/methods/create_subscription_definition_version"
        update: []
        delete: []
    bulk_deployment_status:
      id: aws.greengrass.bulk_deployment_status
      name: bulk_deployment_status
      title: bulk_deployment_status
      methods:
        get_bulk_deployment_status:
          operation:
            $ref: "#/paths/~1greengrass~1bulk~1deployments~1{bulk_deployment_id}~1status/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/bulk_deployment_status/methods/get_bulk_deployment_status"
        insert: []
        update: []
        delete: []
    connectivity_infos:
      id: aws.greengrass.connectivity_infos
      name: connectivity_infos
      title: connectivity_infos
      methods:
        get_connectivity_info:
          operation:
            $ref: "#/paths/~1greengrass~1things~1{thing_name}~1connectivityInfo/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_connectivity_info:
          operation:
            $ref: "#/paths/~1greengrass~1things~1{thing_name}~1connectivityInfo/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/connectivity_infos/methods/get_connectivity_info"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/connectivity_infos/methods/update_connectivity_info"
        delete: []
    deployment_status:
      id: aws.greengrass.deployment_status
      name: deployment_status
      title: deployment_status
      methods:
        get_deployment_status:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1deployments~1{deployment_id}~1status/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/deployment_status/methods/get_deployment_status"
        insert: []
        update: []
        delete: []
    group_certificate_configurations:
      id: aws.greengrass.group_certificate_configurations
      name: group_certificate_configurations
      title: group_certificate_configurations
      methods:
        get_group_certificate_configuration:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1certificateauthorities~1configuration~1expiry/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_group_certificate_configuration:
          operation:
            $ref: "#/paths/~1greengrass~1groups~1{group_id}~1certificateauthorities~1configuration~1expiry/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/group_certificate_configurations/methods/get_group_certificate_configuration"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/group_certificate_configurations/methods/update_group_certificate_configuration"
        delete: []
    thing_runtime_configurations:
      id: aws.greengrass.thing_runtime_configurations
      name: thing_runtime_configurations
      title: thing_runtime_configurations
      methods:
        get_thing_runtime_configuration:
          operation:
            $ref: "#/paths/~1greengrass~1things~1{thing_name}~1runtimeconfig/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.RuntimeConfiguration
        update_thing_runtime_configuration:
          operation:
            $ref: "#/paths/~1greengrass~1things~1{thing_name}~1runtimeconfig/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/thing_runtime_configurations/methods/get_thing_runtime_configuration"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/thing_runtime_configurations/methods/update_thing_runtime_configuration"
        delete: []
    bulk_deployment_detailed_reports:
      id: aws.greengrass.bulk_deployment_detailed_reports
      name: bulk_deployment_detailed_reports
      title: bulk_deployment_detailed_reports
      methods:
        list_bulk_deployment_detailed_reports:
          operation:
            $ref: "#/paths/~1greengrass~1bulk~1deployments~1{bulk_deployment_id}~1detailed-reports/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Deployments
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/bulk_deployment_detailed_reports/methods/list_bulk_deployment_detailed_reports"
        insert: []
        update: []
        delete: []
    bulk_deployments:
      id: aws.greengrass.bulk_deployments
      name: bulk_deployments
      title: bulk_deployments
      methods:
        list_bulk_deployments:
          operation:
            $ref: "#/paths/~1greengrass~1bulk~1deployments/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.BulkDeployments
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: NextToken
                location: query
              responseToken:
                key: $.NextToken
                location: body
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/bulk_deployments/methods/list_bulk_deployments"
        insert: []
        update: []
        delete: []
    tags:
      id: aws.greengrass.tags
      name: tags
      title: tags
      methods:
        list_tags_for_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource-arn}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        tag_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource-arn}/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        untag_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource-arn}/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tags/methods/list_tags_for_resource"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/tags/methods/untag_resource"
          - $ref: "#/components/x-stackQL-resources/tags/methods/tag_resource"
        delete: []
