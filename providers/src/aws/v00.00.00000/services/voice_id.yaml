openapi: 3.0.0
info:
  version: "2021-09-27"
  title: Amazon Voice ID
  description: Amazon Voice ID
  x-serviceName: voiceid
  x-serviceAlias: voice_id
  x-protocol: json
  x-apiVersion: "2021-09-27"
  x-endpointPrefix: voiceid
  x-targetPrefix: VoiceID
  x-jsonVersion: "1.0"
servers:
  - url: https://voiceid.{region}.amazonaws.com
    variables:
      region:
        default: us-east-1
        description: AWS region
    description: Regional endpoint for voice-id
paths:
  /AssociateFraudster:
    post:
      operationId: AssociateFraudster
      description: Associates the fraudsters with the watchlist specified in the same domain.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.AssociateFraudster
            default: VoiceID.AssociateFraudster
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the fraudster.
                FraudsterId:
                  $ref: "#/components/schemas/FraudsterId"
                  description: The identifier of the fraudster to be associated with the watchlist.
                WatchlistId:
                  $ref: "#/components/schemas/WatchlistId"
                  description: The identifier of the watchlist you want to associate with the fraudster.
              required:
                - DomainId
                - FraudsterId
                - WatchlistId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssociateFraudsterResponse"
  /CreateDomain:
    post:
      operationId: CreateDomain
      description: Creates a domain that contains all Amazon Connect Voice ID data, such as speakers, fraudsters, customer audio, and voiceprints. Every domain is created with a default watchlist that fraudsters can be a part of.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.CreateDomain
            default: VoiceID.CreateDomain
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                ClientToken:
                  $ref: "#/components/schemas/ClientTokenString"
                  description: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
                Description:
                  $ref: "#/components/schemas/Description"
                  description: A brief description of this domain.
                Name:
                  $ref: "#/components/schemas/DomainName"
                  description: The name of the domain.
                ServerSideEncryptionConfiguration:
                  $ref: "#/components/schemas/ServerSideEncryptionConfiguration"
                  description: The configuration, containing the KMS key identifier, to be used by Voice ID for the server-side encryption of your data. Refer to Amazon Connect Voice ID encryption at rest for more details on how the KMS key is used.
                Tags:
                  $ref: "#/components/schemas/TagList"
                  description: A list of tags you want added to the domain.
              required:
                - ServerSideEncryptionConfiguration
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateDomainResponse"
  /CreateWatchlist:
    post:
      operationId: CreateWatchlist
      description: Creates a watchlist that fraudsters can be a part of.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.CreateWatchlist
            default: VoiceID.CreateWatchlist
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                ClientToken:
                  $ref: "#/components/schemas/ClientTokenString"
                  description: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
                Description:
                  $ref: "#/components/schemas/WatchlistDescription"
                  description: A brief description of this watchlist.
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the watchlist.
                Name:
                  $ref: "#/components/schemas/WatchlistName"
                  description: The name of the watchlist.
              required:
                - DomainId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateWatchlistResponse"
  /DeleteDomain:
    post:
      operationId: DeleteDomain
      description: Deletes the specified domain from Voice ID.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DeleteDomain
            default: VoiceID.DeleteDomain
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain you want to delete.
              required:
                - DomainId
      responses:
        "200":
          description: Success
  /DeleteFraudster:
    post:
      operationId: DeleteFraudster
      description: Deletes the specified fraudster from Voice ID. This action disassociates the fraudster from any watchlists it is a part of.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DeleteFraudster
            default: VoiceID.DeleteFraudster
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the fraudster.
                FraudsterId:
                  $ref: "#/components/schemas/FraudsterId"
                  description: The identifier of the fraudster you want to delete.
              required:
                - DomainId
                - FraudsterId
      responses:
        "200":
          description: Success
  /DeleteSpeaker:
    post:
      operationId: DeleteSpeaker
      description: Deletes the specified speaker from Voice ID.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DeleteSpeaker
            default: VoiceID.DeleteSpeaker
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the speaker.
                SpeakerId:
                  $ref: "#/components/schemas/SpeakerId"
                  description: The identifier of the speaker you want to delete.
              required:
                - DomainId
                - SpeakerId
      responses:
        "200":
          description: Success
  /DeleteWatchlist:
    post:
      operationId: DeleteWatchlist
      description: Deletes the specified watchlist from Voice ID. This API throws an exception when there are fraudsters in the watchlist that you are trying to delete. You must delete the fraudsters, and then delete the watchlist. Every domain has a default watchlist which cannot be deleted.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DeleteWatchlist
            default: VoiceID.DeleteWatchlist
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the watchlist.
                WatchlistId:
                  $ref: "#/components/schemas/WatchlistId"
                  description: The identifier of the watchlist to be deleted.
              required:
                - DomainId
                - WatchlistId
      responses:
        "200":
          description: Success
  /DescribeDomain:
    post:
      operationId: DescribeDomain
      description: Describes the specified domain.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DescribeDomain
            default: VoiceID.DescribeDomain
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that you are describing.
              required:
                - DomainId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeDomainResponse"
  /DescribeFraudster:
    post:
      operationId: DescribeFraudster
      description: Describes the specified fraudster.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DescribeFraudster
            default: VoiceID.DescribeFraudster
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the fraudster.
                FraudsterId:
                  $ref: "#/components/schemas/FraudsterId"
                  description: The identifier of the fraudster you are describing.
              required:
                - DomainId
                - FraudsterId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeFraudsterResponse"
  /DescribeFraudsterRegistrationJob:
    post:
      operationId: DescribeFraudsterRegistrationJob
      description: Describes the specified fraudster registration job.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DescribeFraudsterRegistrationJob
            default: VoiceID.DescribeFraudsterRegistrationJob
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the fraudster registration job.
                JobId:
                  $ref: "#/components/schemas/JobId"
                  description: The identifier of the fraudster registration job you are describing.
              required:
                - DomainId
                - JobId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeFraudsterRegistrationJobResponse"
  /DescribeSpeaker:
    post:
      operationId: DescribeSpeaker
      description: Describes the specified speaker.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DescribeSpeaker
            default: VoiceID.DescribeSpeaker
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the speaker.
                SpeakerId:
                  $ref: "#/components/schemas/SpeakerId"
                  description: The identifier of the speaker you are describing.
              required:
                - DomainId
                - SpeakerId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeSpeakerResponse"
  /DescribeSpeakerEnrollmentJob:
    post:
      operationId: DescribeSpeakerEnrollmentJob
      description: Describes the specified speaker enrollment job.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DescribeSpeakerEnrollmentJob
            default: VoiceID.DescribeSpeakerEnrollmentJob
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the speaker enrollment job.
                JobId:
                  $ref: "#/components/schemas/JobId"
                  description: The identifier of the speaker enrollment job you are describing.
              required:
                - DomainId
                - JobId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeSpeakerEnrollmentJobResponse"
  /DescribeWatchlist:
    post:
      operationId: DescribeWatchlist
      description: Describes the specified watchlist.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DescribeWatchlist
            default: VoiceID.DescribeWatchlist
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the watchlist.
                WatchlistId:
                  $ref: "#/components/schemas/WatchlistId"
                  description: The identifier of the watchlist that you are describing.
              required:
                - DomainId
                - WatchlistId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeWatchlistResponse"
  /DisassociateFraudster:
    post:
      operationId: DisassociateFraudster
      description: Disassociates the fraudsters from the watchlist specified. Voice ID always expects a fraudster to be a part of at least one watchlist. If you try to disassociate a fraudster from its only watchlist, a ValidationException is thrown.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.DisassociateFraudster
            default: VoiceID.DisassociateFraudster
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the fraudster.
                FraudsterId:
                  $ref: "#/components/schemas/FraudsterId"
                  description: The identifier of the fraudster to be disassociated from the watchlist.
                WatchlistId:
                  $ref: "#/components/schemas/WatchlistId"
                  description: The identifier of the watchlist that you want to disassociate from the fraudster.
              required:
                - DomainId
                - FraudsterId
                - WatchlistId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisassociateFraudsterResponse"
  /EvaluateSession:
    post:
      operationId: EvaluateSession
      description: Evaluates a specified session based on audio data accumulated during a streaming Amazon Connect Voice ID call.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.EvaluateSession
            default: VoiceID.EvaluateSession
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain where the session started.
                SessionNameOrId:
                  $ref: "#/components/schemas/SessionNameOrId"
                  description: The session identifier, or name of the session, that you want to evaluate. In Voice ID integration, this is the Contact-Id.
              required:
                - DomainId
                - SessionNameOrId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvaluateSessionResponse"
  /ListDomains:
    post:
      operationId: ListDomains
      description: Lists all the domains in the Amazon Web Services account.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.ListDomains
            default: VoiceID.ListDomains
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                MaxResults:
                  $ref: "#/components/schemas/MaxResultsForListDomainFe"
                  description: The maximum number of results that are returned per call. You can use NextToken to obtain more pages of results. The default is 100; the maximum allowed page size is also 100.
                NextToken:
                  $ref: "#/components/schemas/NextToken"
                  description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDomainsResponse"
  /ListFraudsterRegistrationJobs:
    post:
      operationId: ListFraudsterRegistrationJobs
      description: Lists all the fraudster registration jobs in the domain with the given JobStatus. If JobStatus is not provided, this lists all fraudster registration jobs in the given domain.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.ListFraudsterRegistrationJobs
            default: VoiceID.ListFraudsterRegistrationJobs
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the fraudster registration Jobs.
                JobStatus:
                  $ref: "#/components/schemas/FraudsterRegistrationJobStatus"
                  description: Provides the status of your fraudster registration job.
                MaxResults:
                  $ref: "#/components/schemas/MaxResultsForList"
                  description: The maximum number of results that are returned per call. You can use NextToken to obtain more pages of results. The default is 100; the maximum allowed page size is also 100.
                NextToken:
                  $ref: "#/components/schemas/NextToken"
                  description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
              required:
                - DomainId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFraudsterRegistrationJobsResponse"
  /ListFraudsters:
    post:
      operationId: ListFraudsters
      description: Lists all fraudsters in a specified watchlist or domain.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.ListFraudsters
            default: VoiceID.ListFraudsters
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain.
                MaxResults:
                  $ref: "#/components/schemas/MaxResultsForList"
                  description: The maximum number of results that are returned per call. You can use NextToken to obtain more pages of results. The default is 100; the maximum allowed page size is also 100.
                NextToken:
                  $ref: "#/components/schemas/NextToken"
                  description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
                WatchlistId:
                  $ref: "#/components/schemas/WatchlistId"
                  description: The identifier of the watchlist. If provided, all fraudsters in the watchlist are listed. If not provided, all fraudsters in the domain are listed.
              required:
                - DomainId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFraudstersResponse"
  /ListSpeakerEnrollmentJobs:
    post:
      operationId: ListSpeakerEnrollmentJobs
      description: Lists all the speaker enrollment jobs in the domain with the specified JobStatus. If JobStatus is not provided, this lists all jobs with all possible speaker enrollment job statuses.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.ListSpeakerEnrollmentJobs
            default: VoiceID.ListSpeakerEnrollmentJobs
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the speaker enrollment jobs.
                JobStatus:
                  $ref: "#/components/schemas/SpeakerEnrollmentJobStatus"
                  description: Provides the status of your speaker enrollment Job.
                MaxResults:
                  $ref: "#/components/schemas/MaxResultsForList"
                  description: The maximum number of results that are returned per call. You can use NextToken to obtain more pages of results. The default is 100; the maximum allowed page size is also 100.
                NextToken:
                  $ref: "#/components/schemas/NextToken"
                  description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
              required:
                - DomainId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSpeakerEnrollmentJobsResponse"
  /ListSpeakers:
    post:
      operationId: ListSpeakers
      description: Lists all speakers in a specified domain.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.ListSpeakers
            default: VoiceID.ListSpeakers
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain.
                MaxResults:
                  $ref: "#/components/schemas/MaxResultsForList"
                  description: The maximum number of results that are returned per call. You can use NextToken to obtain more pages of results. The default is 100; the maximum allowed page size is also 100.
                NextToken:
                  $ref: "#/components/schemas/NextToken"
                  description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
              required:
                - DomainId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSpeakersResponse"
  /ListTagsForResource:
    post:
      operationId: ListTagsForResource
      description: Lists all tags associated with a specified Voice ID resource.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.ListTagsForResource
            default: VoiceID.ListTagsForResource
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                ResourceArn:
                  $ref: "#/components/schemas/AmazonResourceName"
                  description: The Amazon Resource Name (ARN) of the Voice ID resource for which you want to list the tags.
              required:
                - ResourceArn
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListTagsForResourceResponse"
  /ListWatchlists:
    post:
      operationId: ListWatchlists
      description: Lists all watchlists in a specified domain.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.ListWatchlists
            default: VoiceID.ListWatchlists
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain.
                MaxResults:
                  $ref: "#/components/schemas/MaxResultsForList"
                  description: The maximum number of results that are returned per call. You can use NextToken to obtain more pages of results. The default is 100; the maximum allowed page size is also 100.
                NextToken:
                  $ref: "#/components/schemas/NextToken"
                  description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
              required:
                - DomainId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListWatchlistsResponse"
  /OptOutSpeaker:
    post:
      operationId: OptOutSpeaker
      description: Opts out a speaker from Voice ID. A speaker can be opted out regardless of whether or not they already exist in Voice ID. If they don't yet exist, a new speaker is created in an opted out state. If they already exist, their existing status is overridden and they are opted out. Enrollment and evaluation authentication requests are rejected for opted out speakers, and opted out speakers have no voice embeddings stored in Voice ID.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.OptOutSpeaker
            default: VoiceID.OptOutSpeaker
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the speaker.
                SpeakerId:
                  $ref: "#/components/schemas/SpeakerId"
                  description: The identifier of the speaker you want opted-out.
              required:
                - DomainId
                - SpeakerId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptOutSpeakerResponse"
  /StartFraudsterRegistrationJob:
    post:
      operationId: StartFraudsterRegistrationJob
      description: Starts a new batch fraudster registration job using provided details.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.StartFraudsterRegistrationJob
            default: VoiceID.StartFraudsterRegistrationJob
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                ClientToken:
                  $ref: "#/components/schemas/ClientTokenString"
                  description: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
                DataAccessRoleArn:
                  $ref: "#/components/schemas/IamRoleArn"
                  description: The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the Job output file. Refer to the Create and edit a fraudster watchlist documentation for the permissions needed in this role.
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the fraudster registration job and in which the fraudsters are registered.
                InputDataConfig:
                  $ref: "#/components/schemas/InputDataConfig"
                  description: The input data config containing an S3 URI for the input manifest file that contains the list of fraudster registration requests.
                JobName:
                  $ref: "#/components/schemas/JobName"
                  description: The name of the new fraudster registration job.
                OutputDataConfig:
                  $ref: "#/components/schemas/OutputDataConfig"
                  description: The output data config containing the S3 location where Voice ID writes the job output file; you must also include a KMS key ID to encrypt the file.
                RegistrationConfig:
                  $ref: "#/components/schemas/RegistrationConfig"
                  description: The registration config containing details such as the action to take when a duplicate fraudster is detected, and the similarity threshold to use for detecting a duplicate fraudster.
              required:
                - DataAccessRoleArn
                - DomainId
                - InputDataConfig
                - OutputDataConfig
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartFraudsterRegistrationJobResponse"
  /StartSpeakerEnrollmentJob:
    post:
      operationId: StartSpeakerEnrollmentJob
      description: Starts a new batch speaker enrollment job using specified details.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.StartSpeakerEnrollmentJob
            default: VoiceID.StartSpeakerEnrollmentJob
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                ClientToken:
                  $ref: "#/components/schemas/ClientTokenString"
                  description: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
                DataAccessRoleArn:
                  $ref: "#/components/schemas/IamRoleArn"
                  description: The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the job output file. Refer to Batch enrollment using audio data from prior calls for the permissions needed in this role.
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the speaker enrollment job and in which the speakers are enrolled.
                EnrollmentConfig:
                  $ref: "#/components/schemas/EnrollmentConfig"
                  description: The enrollment config that contains details such as the action to take when a speaker is already enrolled in Voice ID or when a speaker is identified as a fraudster.
                InputDataConfig:
                  $ref: "#/components/schemas/InputDataConfig"
                  description: The input data config containing the S3 location for the input manifest file that contains the list of speaker enrollment requests.
                JobName:
                  $ref: "#/components/schemas/JobName"
                  description: A name for your speaker enrollment job.
                OutputDataConfig:
                  $ref: "#/components/schemas/OutputDataConfig"
                  description: The output data config containing the S3 location where Voice ID writes the job output file; you must also include a KMS key ID to encrypt the file.
              required:
                - DataAccessRoleArn
                - DomainId
                - InputDataConfig
                - OutputDataConfig
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartSpeakerEnrollmentJobResponse"
  /TagResource:
    post:
      operationId: TagResource
      description: Tags a Voice ID resource with the provided list of tags.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.TagResource
            default: VoiceID.TagResource
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                ResourceArn:
                  $ref: "#/components/schemas/AmazonResourceName"
                  description: The Amazon Resource Name (ARN) of the Voice ID resource you want to tag.
                Tags:
                  $ref: "#/components/schemas/TagList"
                  description: The list of tags to assign to the specified resource.
              required:
                - ResourceArn
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagResourceResponse"
  /UntagResource:
    post:
      operationId: UntagResource
      description: Removes specified tags from a specified Amazon Connect Voice ID resource.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.UntagResource
            default: VoiceID.UntagResource
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                ResourceArn:
                  $ref: "#/components/schemas/AmazonResourceName"
                  description: The Amazon Resource Name (ARN) of the Voice ID resource you want to remove tags from.
                TagKeys:
                  $ref: "#/components/schemas/TagKeyList"
                  description: The list of tag keys you want to remove from the specified resource.
              required:
                - ResourceArn
                - TagKeys
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UntagResourceResponse"
  /UpdateDomain:
    post:
      operationId: UpdateDomain
      description: Updates the specified domain. This API has clobber behavior, and clears and replaces all attributes. If an optional field, such as 'Description' is not provided, it is removed from the domain.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.UpdateDomain
            default: VoiceID.UpdateDomain
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                Description:
                  $ref: "#/components/schemas/Description"
                  description: A brief description about this domain.
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain to be updated.
                Name:
                  $ref: "#/components/schemas/DomainName"
                  description: The name of the domain.
                ServerSideEncryptionConfiguration:
                  $ref: "#/components/schemas/ServerSideEncryptionConfiguration"
                  description: The configuration, containing the KMS key identifier, to be used by Voice ID for the server-side encryption of your data. Changing the domain's associated KMS key immediately triggers an asynchronous process to remove dependency on the old KMS key, such that the domain's data can only be accessed using the new KMS key. The domain's ServerSideEncryptionUpdateDetails contains the details for this process.
              required:
                - DomainId
                - ServerSideEncryptionConfiguration
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateDomainResponse"
  /UpdateWatchlist:
    post:
      operationId: UpdateWatchlist
      description: Updates the specified watchlist. Every domain has a default watchlist which cannot be updated.
      parameters:
        - name: X-Amz-Target
          in: header
          description: Action Header
          required: false
          schema:
            type: string
            enum:
              - VoiceID.UpdateWatchlist
            default: VoiceID.UpdateWatchlist
      requestBody:
        required: true
        content:
          application/x-amz-json-1.0:
            schema:
              type: object
              properties:
                Description:
                  $ref: "#/components/schemas/WatchlistDescription"
                  description: A brief description about this watchlist.
                DomainId:
                  $ref: "#/components/schemas/DomainId"
                  description: The identifier of the domain that contains the watchlist.
                Name:
                  $ref: "#/components/schemas/WatchlistName"
                  description: The name of the watchlist.
                WatchlistId:
                  $ref: "#/components/schemas/WatchlistId"
                  description: The identifier of the watchlist to be updated.
              required:
                - DomainId
                - WatchlistId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateWatchlistResponse"
components:
  parameters:
    X-Amz-Content-Sha256:
      in: header
      name: X-Amz-Content-Sha256
      required: false
      schema:
        type: string
    X-Amz-Date:
      in: header
      name: X-Amz-Date
      required: false
      schema:
        type: string
    X-Amz-Algorithm:
      in: header
      name: X-Amz-Algorithm
      required: false
      schema:
        type: string
    X-Amz-Credential:
      in: header
      name: X-Amz-Credential
      required: false
      schema:
        type: string
    X-Amz-Security-Token:
      in: header
      name: X-Amz-Security-Token
      required: false
      schema:
        type: string
    X-Amz-Signature:
      in: header
      name: X-Amz-Signature
      required: false
      schema:
        type: string
    X-Amz-SignedHeaders:
      in: header
      name: X-Amz-SignedHeaders
      required: false
      schema:
        type: string
  schemas:
    DomainId:
      type: string
      minLength: 22
      maxLength: 22
      pattern: ^[a-zA-Z0-9]{22}$
    FraudsterId:
      type: string
      minLength: 25
      maxLength: 25
      pattern: ^id#[a-zA-Z0-9]{22}$
    WatchlistId:
      type: string
      minLength: 22
      maxLength: 22
      pattern: ^[a-zA-Z0-9]{22}$
    AssociateFraudsterResponse:
      type: object
      properties:
        Fraudster:
          $ref: "#/components/schemas/Fraudster"
    Fraudster:
      description: Contains all the information about a fraudster.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when Voice ID identified the fraudster.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the fraudster.
        GeneratedFraudsterId:
          $ref: "#/components/schemas/GeneratedFraudsterId"
          description: The service-generated identifier for the fraudster.
        WatchlistIds:
          $ref: "#/components/schemas/ResponseWatchlistIds"
          description: The identifier of the watchlists the fraudster is a part of.
    Timestamp:
      type: string
      format: date-time
    GeneratedFraudsterId:
      type: string
      minLength: 25
      maxLength: 25
      pattern: ^id#[a-zA-Z0-9]{22}$
    ResponseWatchlistIds:
      type: array
      items:
        $ref: "#/components/schemas/WatchlistId"
    ClientTokenString:
      type: string
      minLength: 1
      maxLength: 64
      pattern: ^[a-zA-Z0-9-_]+$
    Description:
      type: string
      minLength: 1
      maxLength: 1024
      pattern: ^([\p{L}\p{Z}\p{N}_.:/=+\-%@]*)$
    DomainName:
      type: string
      minLength: 1
      maxLength: 256
      pattern: ^[a-zA-Z0-9][a-zA-Z0-9_-]*$
    ServerSideEncryptionConfiguration:
      description: The configuration containing information about the customer managed key used for encrypting customer data.
      type: object
      properties:
        KmsKeyId:
          $ref: "#/components/schemas/KmsKeyId"
          description: The identifier of the KMS key to use to encrypt data stored by Voice ID. Voice ID doesn't support asymmetric customer managed keys.
      required:
        - KmsKeyId
    KmsKeyId:
      type: string
      minLength: 1
      maxLength: 2048
    TagList:
      type: array
      items:
        $ref: "#/components/schemas/Tag"
    Tag:
      description: 'The tags used to organize, track, or control access for this resource. For example, { "tags": {"key1":"value1", "key2":"value2"} }.'
      type: object
      properties:
        Key:
          $ref: "#/components/schemas/TagKey"
          description: The first part of a key:value pair that forms a tag associated with a given resource. For example, in the tag 'Department':'Sales', the key is 'Department'.
        Value:
          $ref: "#/components/schemas/TagValue"
          description: The second part of a key:value pair that forms a tag associated with a given resource. For example, in the tag 'Department':'Sales', the value is 'Sales'.
      required:
        - Key
        - Value
    TagKey:
      type: string
      minLength: 1
      maxLength: 128
      pattern: ^([\p{L}\p{Z}\p{N}_.:/=+\-@]*)$
    TagValue:
      type: string
      minLength: 0
      maxLength: 256
      pattern: ^([\p{L}\p{Z}\p{N}_.:/=+\-@]*)$
    CreateDomainResponse:
      type: object
      properties:
        Domain:
          $ref: "#/components/schemas/Domain"
          description: Information about the newly created domain.
    Domain:
      description: Contains all the information about a domain.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/Arn"
          description: The Amazon Resource Name (ARN) for the domain.
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the domain was created.
        Description:
          $ref: "#/components/schemas/Description"
          description: The description of the domain.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain.
        DomainStatus:
          $ref: "#/components/schemas/DomainStatus"
          description: The current status of the domain.
        Name:
          $ref: "#/components/schemas/DomainName"
          description: The name for the domain.
        ServerSideEncryptionConfiguration:
          $ref: "#/components/schemas/ServerSideEncryptionConfiguration"
          description: The server-side encryption configuration containing the KMS key identifier you want Voice ID to use to encrypt your data.
        ServerSideEncryptionUpdateDetails:
          $ref: "#/components/schemas/ServerSideEncryptionUpdateDetails"
          description: Details about the most recent server-side encryption configuration update. When the server-side encryption configuration is changed, dependency on the old KMS key is removed through an asynchronous process. When this update is complete, the domain's data can only be accessed using the new KMS key.
        UpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the domain was last update.
        WatchlistDetails:
          $ref: "#/components/schemas/WatchlistDetails"
          description: The watchlist details of a domain. Contains the default watchlist ID of the domain.
    Arn:
      type: string
      pattern: ^arn:aws(-[^:]+)?:voiceid.+:[0-9]{12}:domain/[a-zA-Z0-9]{22}$
    DomainStatus:
      type: string
      enum:
        - ACTIVE
        - PENDING
        - SUSPENDED
    ServerSideEncryptionUpdateDetails:
      description: Details about the most recent server-side encryption configuration update. When the server-side encryption configuration is changed, dependency on the old KMS key is removed through an asynchronous process. When this update is complete, the domain’s data can only be accessed using the new KMS key.
      type: object
      properties:
        Message:
          $ref: "#/components/schemas/String"
          description: Message explaining the current UpdateStatus. When the UpdateStatus is FAILED, this message explains the cause of the failure.
        OldKmsKeyId:
          $ref: "#/components/schemas/KmsKeyId"
          description: The previous KMS key ID the domain was encrypted with, before ServerSideEncryptionConfiguration was updated to a new KMS key ID.
        UpdateStatus:
          $ref: "#/components/schemas/ServerSideEncryptionUpdateStatus"
          description: Status of the server-side encryption update. During an update, if there is an issue with the domain's current or old KMS key ID, such as an inaccessible or disabled key, then the status is FAILED. In order to resolve this, the key needs to be made accessible, and then an UpdateDomain call with the existing server-side encryption configuration will re-attempt this update process.
    String:
      type: string
      minLength: 1
    ServerSideEncryptionUpdateStatus:
      type: string
      enum:
        - IN_PROGRESS
        - COMPLETED
        - FAILED
    WatchlistDetails:
      description: Details of the watchlists in a domain.
      type: object
      properties:
        DefaultWatchlistId:
          $ref: "#/components/schemas/WatchlistId"
          description: The identifier of the default watchlist.
      required:
        - DefaultWatchlistId
    WatchlistDescription:
      type: string
      minLength: 1
      maxLength: 1024
      pattern: ^([\p{L}\p{Z}\p{N}_.:/=+\-%@]*)$
    WatchlistName:
      type: string
      minLength: 1
      maxLength: 256
      pattern: ^[a-zA-Z0-9][a-zA-Z0-9_-]*$
    CreateWatchlistResponse:
      type: object
      properties:
        Watchlist:
          $ref: "#/components/schemas/Watchlist"
          description: Information about the newly created watchlist.
    Watchlist:
      description: Contains all the information about a watchlist.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the watchlist was created.
        DefaultWatchlist:
          $ref: "#/components/schemas/Boolean"
          description: Whether the specified watchlist is the default watchlist of a domain.
        Description:
          $ref: "#/components/schemas/WatchlistDescription"
          description: The description of the watchlist.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the watchlist.
        Name:
          $ref: "#/components/schemas/WatchlistName"
          description: The name for the watchlist.
        UpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the watchlist was updated.
        WatchlistId:
          $ref: "#/components/schemas/WatchlistId"
          description: The identifier of the watchlist.
    Boolean:
      type: boolean
    SpeakerId:
      type: string
      minLength: 1
      maxLength: 256
      pattern: ^(id#[a-zA-Z0-9]{22}|[a-zA-Z0-9][a-zA-Z0-9_-]*)$
    DescribeDomainResponse:
      type: object
      properties:
        Domain:
          $ref: "#/components/schemas/Domain"
          description: Information about the specified domain.
    DescribeFraudsterResponse:
      type: object
      properties:
        Fraudster:
          $ref: "#/components/schemas/Fraudster"
          description: Information about the specified fraudster.
    JobId:
      type: string
      minLength: 22
      maxLength: 22
      pattern: ^[a-zA-Z0-9]{22}$
    DescribeFraudsterRegistrationJobResponse:
      type: object
      properties:
        Job:
          $ref: "#/components/schemas/FraudsterRegistrationJob"
          description: Contains details about the specified fraudster registration job.
    FraudsterRegistrationJob:
      description: Contains all the information about a fraudster registration job.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the fraudster registration job was created.
        DataAccessRoleArn:
          $ref: "#/components/schemas/IamRoleArn"
          description: The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the job output file.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the fraudster registration job.
        EndedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the fraudster registration job ended.
        FailureDetails:
          $ref: "#/components/schemas/FailureDetails"
          description: Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed.
        InputDataConfig:
          $ref: "#/components/schemas/InputDataConfig"
          description: The input data config containing an S3 URI for the input manifest file that contains the list of fraudster registration job requests.
        JobId:
          $ref: "#/components/schemas/JobId"
          description: The service-generated identifier for the fraudster registration job.
        JobName:
          $ref: "#/components/schemas/JobName"
          description: The client-provided name for the fraudster registration job.
        JobProgress:
          $ref: "#/components/schemas/JobProgress"
          description: Shows the completed percentage of registration requests listed in the input file.
        JobStatus:
          $ref: "#/components/schemas/FraudsterRegistrationJobStatus"
          description: The current status of the fraudster registration job.
        OutputDataConfig:
          $ref: "#/components/schemas/OutputDataConfig"
          description: The output data config containing the S3 location where you want Voice ID to write your job output file; you must also include a KMS key ID in order to encrypt the file.
        RegistrationConfig:
          $ref: "#/components/schemas/RegistrationConfig"
          description: The registration config containing details such as the action to take when a duplicate fraudster is detected, and the similarity threshold to use for detecting a duplicate fraudster.
    IamRoleArn:
      type: string
      minLength: 20
      maxLength: 2048
      pattern: ^arn:aws(-[^:]+)?:iam::[0-9]{12}:role/.+$
    FailureDetails:
      description: Contains error details for a failed batch job.
      type: object
      properties:
        Message:
          $ref: "#/components/schemas/String"
          description: A description of the error that caused the batch job failure.
        StatusCode:
          $ref: "#/components/schemas/Integer"
          description: An HTTP status code representing the nature of the error.
    Integer:
      type: integer
    InputDataConfig:
      description: The configuration containing input file information for a batch job.
      type: object
      properties:
        S3Uri:
          $ref: "#/components/schemas/S3Uri"
          description: The S3 location for the input manifest file that contains the list of individual enrollment or registration job requests.
      required:
        - S3Uri
    S3Uri:
      type: string
      minLength: 0
      maxLength: 1024
      pattern: ^s3://[a-z0-9][\.\-a-z0-9]{1,61}[a-z0-9](/.*)?$
    JobName:
      type: string
      minLength: 1
      maxLength: 256
      pattern: ^[a-zA-Z0-9][a-zA-Z0-9_-]*$
    JobProgress:
      description: Indicates the completion progress for a batch job.
      type: object
      properties:
        PercentComplete:
          $ref: "#/components/schemas/Score"
          description: Shows the completed percentage of enrollment or registration requests listed in the input file.
    Score:
      type: integer
      minimum: 0
      maximum: 100
    FraudsterRegistrationJobStatus:
      type: string
      enum:
        - SUBMITTED
        - IN_PROGRESS
        - COMPLETED
        - COMPLETED_WITH_ERRORS
        - FAILED
    OutputDataConfig:
      description: The configuration containing output file information for a batch job.
      type: object
      properties:
        KmsKeyId:
          $ref: "#/components/schemas/KmsKeyId"
          description: The identifier of the KMS key you want Voice ID to use to encrypt the output file of a speaker enrollment job/fraudster registration job.
        S3Uri:
          $ref: "#/components/schemas/S3Uri"
          description: The S3 path of the folder where Voice ID writes the job output file. It has a *.out extension. For example, if the input file name is input-file.json and the output folder path is s3://output-bucket/output-folder, the full output file path is s3://output-bucket/output-folder/job-Id/input-file.json.out.
      required:
        - S3Uri
    RegistrationConfig:
      description: The registration configuration to be used during the batch fraudster registration job.
      type: object
      properties:
        DuplicateRegistrationAction:
          $ref: "#/components/schemas/DuplicateRegistrationAction"
          description: The action to take when a fraudster is identified as a duplicate. The default action is SKIP, which skips registering the duplicate fraudster. Setting the value to REGISTER_AS_NEW always registers a new fraudster into the specified domain.
        FraudsterSimilarityThreshold:
          $ref: "#/components/schemas/Score"
          description: The minimum similarity score between the new and old fraudsters in order to consider the new fraudster a duplicate.
        WatchlistIds:
          $ref: "#/components/schemas/RegistrationConfigWatchlistIds"
          description: The identifiers of watchlists that a fraudster is registered to. If a watchlist isn't provided, the fraudsters are registered to the default watchlist.
    DuplicateRegistrationAction:
      type: string
      enum:
        - SKIP
        - REGISTER_AS_NEW
    RegistrationConfigWatchlistIds:
      type: array
      items:
        $ref: "#/components/schemas/WatchlistId"
    DescribeSpeakerResponse:
      type: object
      properties:
        Speaker:
          $ref: "#/components/schemas/Speaker"
          description: Information about the specified speaker.
    Speaker:
      description: Contains all the information about a speaker.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the speaker was created.
        CustomerSpeakerId:
          $ref: "#/components/schemas/CustomerSpeakerId"
          description: The client-provided identifier for the speaker.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the speaker.
        GeneratedSpeakerId:
          $ref: "#/components/schemas/GeneratedSpeakerId"
          description: The service-generated identifier for the speaker.
        LastAccessedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the speaker was last accessed for enrollment, re-enrollment or a successful authentication. This timestamp is accurate to one hour.
        Status:
          $ref: "#/components/schemas/SpeakerStatus"
          description: The current status of the speaker.
        UpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of the speaker's last update.
    CustomerSpeakerId:
      type: string
      minLength: 1
      maxLength: 256
      pattern: ^[a-zA-Z0-9][a-zA-Z0-9_-]*$
    GeneratedSpeakerId:
      type: string
      minLength: 25
      maxLength: 25
      pattern: ^id#[a-zA-Z0-9]{22}$
    SpeakerStatus:
      type: string
      enum:
        - ENROLLED
        - EXPIRED
        - OPTED_OUT
        - PENDING
    DescribeSpeakerEnrollmentJobResponse:
      type: object
      properties:
        Job:
          $ref: "#/components/schemas/SpeakerEnrollmentJob"
          description: Contains details about the specified speaker enrollment job.
    SpeakerEnrollmentJob:
      description: Contains all the information about a speaker enrollment job.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the speaker enrollment job was created.
        DataAccessRoleArn:
          $ref: "#/components/schemas/IamRoleArn"
          description: The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the job output file.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the speaker enrollment job.
        EndedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the speaker enrollment job ended.
        EnrollmentConfig:
          $ref: "#/components/schemas/EnrollmentConfig"
          description: The configuration that defines the action to take when the speaker is already enrolled in Voice ID, and the FraudDetectionConfig to use.
        FailureDetails:
          $ref: "#/components/schemas/FailureDetails"
          description: Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed.
        InputDataConfig:
          $ref: "#/components/schemas/InputDataConfig"
          description: The input data config containing an S3 URI for the input manifest file that contains the list of speaker enrollment job requests.
        JobId:
          $ref: "#/components/schemas/JobId"
          description: The service-generated identifier for the speaker enrollment job.
        JobName:
          $ref: "#/components/schemas/JobName"
          description: The client-provided name for the speaker enrollment job.
        JobProgress:
          $ref: "#/components/schemas/JobProgress"
          description: Provides details on job progress. This field shows the completed percentage of registration requests listed in the input file.
        JobStatus:
          $ref: "#/components/schemas/SpeakerEnrollmentJobStatus"
          description: The current status of the speaker enrollment job.
        OutputDataConfig:
          $ref: "#/components/schemas/OutputDataConfig"
          description: The output data config containing the S3 location where Voice ID writes the job output file; you must also include a KMS key ID to encrypt the file.
    EnrollmentConfig:
      description: Contains configurations defining enrollment behavior for the batch job.
      type: object
      properties:
        ExistingEnrollmentAction:
          $ref: "#/components/schemas/ExistingEnrollmentAction"
          description: The action to take when the specified speaker is already enrolled in the specified domain. The default value is SKIP, which skips the enrollment for the existing speaker. Setting the value to OVERWRITE replaces the existing voice prints and enrollment audio stored for that speaker with new data generated from the latest audio.
        FraudDetectionConfig:
          $ref: "#/components/schemas/EnrollmentJobFraudDetectionConfig"
          description: The fraud detection configuration to use for the speaker enrollment job.
    ExistingEnrollmentAction:
      type: string
      enum:
        - SKIP
        - OVERWRITE
    EnrollmentJobFraudDetectionConfig:
      description: The fraud detection configuration to be used during the batch speaker enrollment job.
      type: object
      properties:
        FraudDetectionAction:
          $ref: "#/components/schemas/FraudDetectionAction"
          description: The action to take when the given speaker is flagged by the fraud detection system. The default value is FAIL, which fails the speaker enrollment. Changing this value to IGNORE results in the speaker being enrolled even if they are flagged by the fraud detection system.
        RiskThreshold:
          $ref: "#/components/schemas/Score"
          description: Threshold value for determining whether the speaker is a high risk to be fraudulent. If the detected risk score calculated by Voice ID is greater than or equal to the threshold, the speaker is considered a fraudster.
        WatchlistIds:
          $ref: "#/components/schemas/EnrollmentJobFraudDetectionConfigWatchlistIds"
          description: The identifier of watchlists against which fraud detection is performed.
    FraudDetectionAction:
      type: string
      enum:
        - IGNORE
        - FAIL
    EnrollmentJobFraudDetectionConfigWatchlistIds:
      type: array
      items:
        $ref: "#/components/schemas/WatchlistId"
    SpeakerEnrollmentJobStatus:
      type: string
      enum:
        - SUBMITTED
        - IN_PROGRESS
        - COMPLETED
        - COMPLETED_WITH_ERRORS
        - FAILED
    DescribeWatchlistResponse:
      type: object
      properties:
        Watchlist:
          $ref: "#/components/schemas/Watchlist"
          description: Information about the specified watchlist.
    DisassociateFraudsterResponse:
      type: object
      properties:
        Fraudster:
          $ref: "#/components/schemas/Fraudster"
    SessionNameOrId:
      type: string
      minLength: 1
      maxLength: 36
      pattern: ^(id#[a-zA-Z0-9]{22}|[a-zA-Z0-9][a-zA-Z0-9_-]*)$
    EvaluateSessionResponse:
      type: object
      properties:
        AuthenticationResult:
          $ref: "#/components/schemas/AuthenticationResult"
          description: Details resulting from the authentication process, such as authentication decision and authentication score.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the session.
        FraudDetectionResult:
          $ref: "#/components/schemas/FraudDetectionResult"
          description: Details resulting from the fraud detection process, such as fraud detection decision and risk score.
        SessionId:
          $ref: "#/components/schemas/SessionId"
          description: The service-generated identifier of the session.
        SessionName:
          $ref: "#/components/schemas/SessionName"
          description: The client-provided name of the session.
        StreamingStatus:
          $ref: "#/components/schemas/StreamingStatus"
          description: The current status of audio streaming for this session. This field is useful to infer next steps when the Authentication or Fraud Detection results are empty or the decision is NOT_ENOUGH_SPEECH. In this situation, if the StreamingStatus is ONGOING/PENDING_CONFIGURATION, it can mean that the client should call the API again later, after Voice ID has enough audio to produce a result. If the decision remains NOT_ENOUGH_SPEECH even after StreamingStatus is ENDED, it means that the previously streamed session did not have enough speech to perform evaluation, and a new streaming session is needed to try again.
    AuthenticationResult:
      description: The authentication result produced by Voice ID, processed against the current session state and streamed audio of the speaker.
      type: object
      properties:
        AudioAggregationEndedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when audio aggregation ended for this authentication result.
        AudioAggregationStartedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when audio aggregation started for this authentication result.
        AuthenticationResultId:
          $ref: "#/components/schemas/UniqueIdLarge"
          description: The unique identifier for this authentication result. Because there can be multiple authentications for a given session, this field helps to identify if the returned result is from a previous streaming activity or a new result. Note that in absence of any new streaming activity, AcceptanceThreshold changes, or SpeakerId changes, Voice ID always returns cached Authentication Result for this API.
        Configuration:
          $ref: "#/components/schemas/AuthenticationConfiguration"
          description: The AuthenticationConfiguration used to generate this authentication result.
        CustomerSpeakerId:
          $ref: "#/components/schemas/CustomerSpeakerId"
          description: The client-provided identifier for the speaker whose authentication result is produced. Only present if a SpeakerId is provided for the session.
        Decision:
          $ref: "#/components/schemas/AuthenticationDecision"
          description: The authentication decision produced by Voice ID, processed against the current session state and streamed audio of the speaker.
        GeneratedSpeakerId:
          $ref: "#/components/schemas/GeneratedSpeakerId"
          description: The service-generated identifier for the speaker whose authentication result is produced.
        Score:
          $ref: "#/components/schemas/Score"
          description: The authentication score for the speaker whose authentication result is produced. This value is only present if the authentication decision is either ACCEPT or REJECT.
    UniqueIdLarge:
      type: string
      minLength: 22
      maxLength: 22
      pattern: ^[a-zA-Z0-9]{22}$
    AuthenticationConfiguration:
      description: The configuration used to authenticate a speaker during a session.
      type: object
      properties:
        AcceptanceThreshold:
          $ref: "#/components/schemas/Score"
          description: The minimum threshold needed to successfully authenticate a speaker.
      required:
        - AcceptanceThreshold
    AuthenticationDecision:
      type: string
      enum:
        - ACCEPT
        - REJECT
        - NOT_ENOUGH_SPEECH
        - SPEAKER_NOT_ENROLLED
        - SPEAKER_OPTED_OUT
        - SPEAKER_ID_NOT_PROVIDED
        - SPEAKER_EXPIRED
    FraudDetectionResult:
      description: The fraud detection result produced by Voice ID, processed against the current session state and streamed audio of the speaker.
      type: object
      properties:
        AudioAggregationEndedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when audio aggregation ended for this fraud detection result.
        AudioAggregationStartedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when audio aggregation started for this fraud detection result.
        Configuration:
          $ref: "#/components/schemas/FraudDetectionConfiguration"
          description: The FraudDetectionConfiguration used to generate this fraud detection result.
        Decision:
          $ref: "#/components/schemas/FraudDetectionDecision"
          description: The fraud detection decision produced by Voice ID, processed against the current session state and streamed audio of the speaker.
        FraudDetectionResultId:
          $ref: "#/components/schemas/UniqueIdLarge"
          description: The unique identifier for this fraud detection result. Given there can be multiple fraud detections for a given session, this field helps in identifying if the returned result is from previous streaming activity or a new result. Note that in the absence of any new streaming activity or risk threshold changes, Voice ID always returns cached Fraud Detection result for this API.
        Reasons:
          $ref: "#/components/schemas/FraudDetectionReasons"
          description: "The reason speaker was flagged by the fraud detection system. This is only be populated if fraud detection Decision is HIGH_RISK, and the following possible values: KNOWN_FRAUDSTER and VOICE_SPOOFING."
        RiskDetails:
          $ref: "#/components/schemas/FraudRiskDetails"
          description: Details about each risk analyzed for this speaker. Currently, this contains KnownFraudsterRisk and VoiceSpoofingRisk details.
    FraudDetectionConfiguration:
      description: The configuration used for performing fraud detection over a speaker during a session.
      type: object
      properties:
        RiskThreshold:
          $ref: "#/components/schemas/Score"
          description: Threshold value for determining whether the speaker is a fraudster. If the detected risk score calculated by Voice ID is higher than the threshold, the speaker is considered a fraudster.
        WatchlistId:
          $ref: "#/components/schemas/WatchlistId"
          description: The identifier of the watchlist against which fraud detection is performed.
    FraudDetectionDecision:
      type: string
      enum:
        - HIGH_RISK
        - LOW_RISK
        - NOT_ENOUGH_SPEECH
    FraudDetectionReasons:
      type: array
      items:
        $ref: "#/components/schemas/FraudDetectionReason"
    FraudDetectionReason:
      type: string
      enum:
        - KNOWN_FRAUDSTER
        - VOICE_SPOOFING
    FraudRiskDetails:
      description: Details regarding various fraud risk analyses performed against the current session state and streamed audio of the speaker.
      type: object
      properties:
        KnownFraudsterRisk:
          $ref: "#/components/schemas/KnownFraudsterRisk"
          description: The details resulting from 'Known Fraudster Risk' analysis of the speaker.
        VoiceSpoofingRisk:
          $ref: "#/components/schemas/VoiceSpoofingRisk"
          description: The details resulting from 'Voice Spoofing Risk' analysis of the speaker.
      required:
        - KnownFraudsterRisk
        - VoiceSpoofingRisk
    KnownFraudsterRisk:
      description: Contains details produced as a result of performing known fraudster risk analysis on a speaker.
      type: object
      properties:
        GeneratedFraudsterId:
          $ref: "#/components/schemas/GeneratedFraudsterId"
          description: The identifier of the fraudster that is the closest match to the speaker. If there are no fraudsters registered in a given domain, or if there are no fraudsters with a non-zero RiskScore, this value is null.
        RiskScore:
          $ref: "#/components/schemas/Score"
          description: The score indicating the likelihood the speaker is a known fraudster.
      required:
        - RiskScore
    VoiceSpoofingRisk:
      description: The details resulting from 'Voice Spoofing Risk' analysis of the speaker.
      type: object
      properties:
        RiskScore:
          $ref: "#/components/schemas/Score"
          description: The score indicating the likelihood of speaker’s voice being spoofed.
      required:
        - RiskScore
    SessionId:
      type: string
      minLength: 25
      maxLength: 25
      pattern: ^id#[a-zA-Z0-9]{22}$
    SessionName:
      type: string
      minLength: 1
      maxLength: 36
      pattern: ^[a-zA-Z0-9][a-zA-Z0-9_-]*$
    StreamingStatus:
      type: string
      enum:
        - PENDING_CONFIGURATION
        - ONGOING
        - ENDED
    MaxResultsForListDomainFe:
      type: integer
      minimum: 1
      maximum: 10
    NextToken:
      type: string
      minLength: 0
      maxLength: 8192
      pattern: ^\p{ASCII}{0,8192}$
    ListDomainsResponse:
      type: object
      properties:
        DomainSummaries:
          $ref: "#/components/schemas/DomainSummaries"
          description: A list containing details about each domain in the Amazon Web Services account.
        NextToken:
          $ref: "#/components/schemas/String"
          description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
    DomainSummaries:
      type: array
      items:
        $ref: "#/components/schemas/DomainSummary"
    DomainSummary:
      description: Contains a summary of information about a domain.
      type: object
      properties:
        Arn:
          $ref: "#/components/schemas/Arn"
          description: The Amazon Resource Name (ARN) for the domain.
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the domain was created.
        Description:
          $ref: "#/components/schemas/Description"
          description: The description of the domain.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain.
        DomainStatus:
          $ref: "#/components/schemas/DomainStatus"
          description: The current status of the domain.
        Name:
          $ref: "#/components/schemas/DomainName"
          description: The client-provided name for the domain.
        ServerSideEncryptionConfiguration:
          $ref: "#/components/schemas/ServerSideEncryptionConfiguration"
          description: The server-side encryption configuration containing the KMS key identifier you want Voice ID to use to encrypt your data.
        ServerSideEncryptionUpdateDetails:
          $ref: "#/components/schemas/ServerSideEncryptionUpdateDetails"
          description: Details about the most recent server-side encryption configuration update. When the server-side encryption configuration is changed, dependency on the old KMS key is removed through an asynchronous process. When this update is complete, the domain's data can only be accessed using the new KMS key.
        UpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the domain was last updated.
        WatchlistDetails:
          $ref: "#/components/schemas/WatchlistDetails"
          description: Provides information about watchlistDetails and DefaultWatchlistID.
    MaxResultsForList:
      type: integer
      minimum: 1
      maximum: 100
    ListFraudsterRegistrationJobsResponse:
      type: object
      properties:
        JobSummaries:
          $ref: "#/components/schemas/FraudsterRegistrationJobSummaries"
          description: A list containing details about each specified fraudster registration job.
        NextToken:
          $ref: "#/components/schemas/String"
          description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
    FraudsterRegistrationJobSummaries:
      type: array
      items:
        $ref: "#/components/schemas/FraudsterRegistrationJobSummary"
    FraudsterRegistrationJobSummary:
      description: Contains a summary of information about a fraudster registration job.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the fraudster registration job was created.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the fraudster registration job.
        EndedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the fraudster registration job ended.
        FailureDetails:
          $ref: "#/components/schemas/FailureDetails"
          description: Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed.
        JobId:
          $ref: "#/components/schemas/JobId"
          description: The service-generated identifier for the fraudster registration job.
        JobName:
          $ref: "#/components/schemas/JobName"
          description: The client-provided name for the fraudster registration job.
        JobProgress:
          $ref: "#/components/schemas/JobProgress"
          description: Shows the completed percentage of registration requests listed in the input file.
        JobStatus:
          $ref: "#/components/schemas/FraudsterRegistrationJobStatus"
          description: The current status of the fraudster registration job.
    ListFraudstersResponse:
      type: object
      properties:
        FraudsterSummaries:
          $ref: "#/components/schemas/FraudsterSummaries"
          description: A list that contains details about each fraudster in the Amazon Web Services account.
        NextToken:
          $ref: "#/components/schemas/String"
          description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
    FraudsterSummaries:
      type: array
      items:
        $ref: "#/components/schemas/FraudsterSummary"
    FraudsterSummary:
      description: Contains a summary of information about a fraudster.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the fraudster summary was created.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the fraudster summary.
        GeneratedFraudsterId:
          $ref: "#/components/schemas/GeneratedFraudsterId"
          description: The service-generated identifier for the fraudster.
        WatchlistIds:
          $ref: "#/components/schemas/ResponseWatchlistIds"
          description: The identifier of the watchlists the fraudster is a part of.
    ListSpeakerEnrollmentJobsResponse:
      type: object
      properties:
        JobSummaries:
          $ref: "#/components/schemas/SpeakerEnrollmentJobSummaries"
          description: A list containing details about each specified speaker enrollment job.
        NextToken:
          $ref: "#/components/schemas/String"
          description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
    SpeakerEnrollmentJobSummaries:
      type: array
      items:
        $ref: "#/components/schemas/SpeakerEnrollmentJobSummary"
    SpeakerEnrollmentJobSummary:
      description: Contains a summary of information about a speaker enrollment job.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when of the speaker enrollment job was created.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the speaker enrollment job.
        EndedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp of when the speaker enrollment job ended.
        FailureDetails:
          $ref: "#/components/schemas/FailureDetails"
          description: Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed.
        JobId:
          $ref: "#/components/schemas/JobId"
          description: The service-generated identifier for the speaker enrollment job.
        JobName:
          $ref: "#/components/schemas/JobName"
          description: The client-provided name for the speaker enrollment job.
        JobProgress:
          $ref: "#/components/schemas/JobProgress"
          description: Provides details regarding job progress. This field shows the completed percentage of enrollment requests listed in the input file.
        JobStatus:
          $ref: "#/components/schemas/SpeakerEnrollmentJobStatus"
          description: The current status of the speaker enrollment job.
    ListSpeakersResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/String"
          description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
        SpeakerSummaries:
          $ref: "#/components/schemas/SpeakerSummaries"
          description: A list containing details about each speaker in the Amazon Web Services account.
    SpeakerSummaries:
      type: array
      items:
        $ref: "#/components/schemas/SpeakerSummary"
    SpeakerSummary:
      description: Contains a summary of information about a speaker.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp showing the speaker's creation time.
        CustomerSpeakerId:
          $ref: "#/components/schemas/CustomerSpeakerId"
          description: The client-provided identifier for the speaker.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the speaker.
        GeneratedSpeakerId:
          $ref: "#/components/schemas/GeneratedSpeakerId"
          description: The service-generated identifier for the speaker.
        LastAccessedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp when the speaker was last accessed for enrollment, re-enrollment or a successful authentication. This timestamp is accurate to one hour.
        Status:
          $ref: "#/components/schemas/SpeakerStatus"
          description: The current status of the speaker.
        UpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: A timestamp showing the speaker's last update.
    AmazonResourceName:
      type: string
      minLength: 1
      maxLength: 1011
      pattern: ^arn:aws(-[^:]+)?:voiceid.+:[0-9]{12}:domain/[a-zA-Z0-9]{22}$
    ListTagsForResourceResponse:
      type: object
      properties:
        Tags:
          $ref: "#/components/schemas/TagList"
          description: The list of tags associated with the specified resource.
    ListWatchlistsResponse:
      type: object
      properties:
        NextToken:
          $ref: "#/components/schemas/String"
          description: If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours.
        WatchlistSummaries:
          $ref: "#/components/schemas/WatchlistSummaries"
          description: A list that contains details about each watchlist in the Amazon Web Services account.
    WatchlistSummaries:
      type: array
      items:
        $ref: "#/components/schemas/WatchlistSummary"
    WatchlistSummary:
      description: Contains a summary of information about a watchlist.
      type: object
      properties:
        CreatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the watchlist was created.
        DefaultWatchlist:
          $ref: "#/components/schemas/Boolean"
          description: Whether the specified watchlist is the default watchlist of a domain.
        Description:
          $ref: "#/components/schemas/WatchlistDescription"
          description: The description of the watchlist.
        DomainId:
          $ref: "#/components/schemas/DomainId"
          description: The identifier of the domain that contains the watchlist.
        Name:
          $ref: "#/components/schemas/WatchlistName"
          description: The name for the watchlist.
        UpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: The timestamp of when the watchlist was last updated.
        WatchlistId:
          $ref: "#/components/schemas/WatchlistId"
          description: The identifier of the watchlist.
    OptOutSpeakerResponse:
      type: object
      properties:
        Speaker:
          $ref: "#/components/schemas/Speaker"
          description: Details about the opted-out speaker.
    StartFraudsterRegistrationJobResponse:
      type: object
      properties:
        Job:
          $ref: "#/components/schemas/FraudsterRegistrationJob"
          description: Details about the started fraudster registration job.
    StartSpeakerEnrollmentJobResponse:
      type: object
      properties:
        Job:
          $ref: "#/components/schemas/SpeakerEnrollmentJob"
          description: Details about the started speaker enrollment job.
    TagResourceResponse:
      type: object
    TagKeyList:
      type: array
      items:
        $ref: "#/components/schemas/TagKey"
    UntagResourceResponse:
      type: object
    UpdateDomainResponse:
      type: object
      properties:
        Domain:
          $ref: "#/components/schemas/Domain"
          description: Details about the updated domain
    UpdateWatchlistResponse:
      type: object
      properties:
        Watchlist:
          $ref: "#/components/schemas/Watchlist"
          description: Details about the updated watchlist.
  x-stackQL-resources:
    fraudsters:
      id: aws.voice_id.fraudsters
      name: fraudsters
      title: fraudsters
      methods:
        associate_fraudster:
          operation:
            $ref: "#/paths/~1AssociateFraudster/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_fraudster:
          operation:
            $ref: "#/paths/~1DeleteFraudster/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        describe_fraudster:
          operation:
            $ref: "#/paths/~1DescribeFraudster/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Fraudster
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        disassociate_fraudster:
          operation:
            $ref: "#/paths/~1DisassociateFraudster/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        list_fraudsters:
          operation:
            $ref: "#/paths/~1ListFraudsters/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.FraudsterSummaries
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            pagination:
              requestToken:
                key: NextToken
                location: body
              responseToken:
                key: $.NextToken
                location: body
            requestBodyTranslate:
              algorithm: naive
        start_fraudster_registration_job:
          operation:
            $ref: "#/paths/~1StartFraudsterRegistrationJob/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/fraudsters/methods/describe_fraudster"
          - $ref: "#/components/x-stackQL-resources/fraudsters/methods/list_fraudsters"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/fraudsters/methods/associate_fraudster"
        delete:
          - $ref: "#/components/x-stackQL-resources/fraudsters/methods/delete_fraudster"
    domains:
      id: aws.voice_id.domains
      name: domains
      title: domains
      methods:
        create_domain:
          operation:
            $ref: "#/paths/~1CreateDomain/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_domain:
          operation:
            $ref: "#/paths/~1DeleteDomain/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        describe_domain:
          operation:
            $ref: "#/paths/~1DescribeDomain/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Domain
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        evaluate_session:
          operation:
            $ref: "#/paths/~1EvaluateSession/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        list_domains:
          operation:
            $ref: "#/paths/~1ListDomains/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.DomainSummaries
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            pagination:
              requestToken:
                key: NextToken
                location: body
              responseToken:
                key: $.NextToken
                location: body
            requestBodyTranslate:
              algorithm: naive
        update_domain:
          operation:
            $ref: "#/paths/~1UpdateDomain/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/domains/methods/describe_domain"
          - $ref: "#/components/x-stackQL-resources/domains/methods/list_domains"
        insert:
          - $ref: "#/components/x-stackQL-resources/domains/methods/create_domain"
        update:
          - $ref: "#/components/x-stackQL-resources/domains/methods/update_domain"
        delete:
          - $ref: "#/components/x-stackQL-resources/domains/methods/delete_domain"
    watchlists:
      id: aws.voice_id.watchlists
      name: watchlists
      title: watchlists
      methods:
        create_watchlist:
          operation:
            $ref: "#/paths/~1CreateWatchlist/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_watchlist:
          operation:
            $ref: "#/paths/~1DeleteWatchlist/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        describe_watchlist:
          operation:
            $ref: "#/paths/~1DescribeWatchlist/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Watchlist
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        list_watchlists:
          operation:
            $ref: "#/paths/~1ListWatchlists/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.WatchlistSummaries
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            pagination:
              requestToken:
                key: NextToken
                location: body
              responseToken:
                key: $.NextToken
                location: body
            requestBodyTranslate:
              algorithm: naive
        update_watchlist:
          operation:
            $ref: "#/paths/~1UpdateWatchlist/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/watchlists/methods/describe_watchlist"
          - $ref: "#/components/x-stackQL-resources/watchlists/methods/list_watchlists"
        insert:
          - $ref: "#/components/x-stackQL-resources/watchlists/methods/create_watchlist"
        update:
          - $ref: "#/components/x-stackQL-resources/watchlists/methods/update_watchlist"
        delete:
          - $ref: "#/components/x-stackQL-resources/watchlists/methods/delete_watchlist"
    speakers:
      id: aws.voice_id.speakers
      name: speakers
      title: speakers
      methods:
        delete_speaker:
          operation:
            $ref: "#/paths/~1DeleteSpeaker/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        describe_speaker:
          operation:
            $ref: "#/paths/~1DescribeSpeaker/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Speaker
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        list_speakers:
          operation:
            $ref: "#/paths/~1ListSpeakers/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.SpeakerSummaries
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            pagination:
              requestToken:
                key: NextToken
                location: body
              responseToken:
                key: $.NextToken
                location: body
            requestBodyTranslate:
              algorithm: naive
        opt_out_speaker:
          operation:
            $ref: "#/paths/~1OptOutSpeaker/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        start_speaker_enrollment_job:
          operation:
            $ref: "#/paths/~1StartSpeakerEnrollmentJob/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/speakers/methods/describe_speaker"
          - $ref: "#/components/x-stackQL-resources/speakers/methods/list_speakers"
        insert: []
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/speakers/methods/delete_speaker"
    fraudster_registration_jobs:
      id: aws.voice_id.fraudster_registration_jobs
      name: fraudster_registration_jobs
      title: fraudster_registration_jobs
      methods:
        describe_fraudster_registration_job:
          operation:
            $ref: "#/paths/~1DescribeFraudsterRegistrationJob/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Job
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        list_fraudster_registration_jobs:
          operation:
            $ref: "#/paths/~1ListFraudsterRegistrationJobs/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.JobSummaries
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            pagination:
              requestToken:
                key: NextToken
                location: body
              responseToken:
                key: $.NextToken
                location: body
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/fraudster_registration_jobs/methods/describe_fraudster_registration_job"
          - $ref: "#/components/x-stackQL-resources/fraudster_registration_jobs/methods/list_fraudster_registration_jobs"
        insert: []
        update: []
        delete: []
    speaker_enrollment_jobs:
      id: aws.voice_id.speaker_enrollment_jobs
      name: speaker_enrollment_jobs
      title: speaker_enrollment_jobs
      methods:
        describe_speaker_enrollment_job:
          operation:
            $ref: "#/paths/~1DescribeSpeakerEnrollmentJob/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.Job
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        list_speaker_enrollment_jobs:
          operation:
            $ref: "#/paths/~1ListSpeakerEnrollmentJobs/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.JobSummaries
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            pagination:
              requestToken:
                key: NextToken
                location: body
              responseToken:
                key: $.NextToken
                location: body
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/speaker_enrollment_jobs/methods/describe_speaker_enrollment_job"
          - $ref: "#/components/x-stackQL-resources/speaker_enrollment_jobs/methods/list_speaker_enrollment_jobs"
        insert: []
        update: []
        delete: []
    tags:
      id: aws.voice_id.tags
      name: tags
      title: tags
      methods:
        list_tags_for_resource:
          operation:
            $ref: "#/paths/~1ListTagsForResource/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        tag_resource:
          operation:
            $ref: "#/paths/~1TagResource/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
        untag_resource:
          operation:
            $ref: "#/paths/~1UntagResource/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
            mediaType: application/x-amz-json-1.0
            base: "{}"
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tags/methods/list_tags_for_resource"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/tags/methods/untag_resource"
          - $ref: "#/components/x-stackQL-resources/tags/methods/tag_resource"
        delete: []
