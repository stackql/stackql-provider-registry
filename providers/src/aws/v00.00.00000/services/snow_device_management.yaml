openapi: 3.0.0
info:
  version: "2021-08-04"
  title: AWS Snow Device Management
  description: AWS Snow Device Management
  x-serviceName: snow-device-management
  x-serviceAlias: snow_device_management
  x-protocol: rest-json
  x-apiVersion: "2021-08-04"
  x-endpointPrefix: snow-device-management
  x-jsonVersion: "1.1"
servers:
  - url: https://snow-device-management.{region}.amazonaws.com
    variables:
      region:
        default: us-east-1
        description: AWS region
    description: Regional endpoint for snow-device-management
paths:
  /task/{task_id}/cancel:
    post:
      operationId: CancelTask
      description: Sends a cancel request for a specified task. You can cancel a task only if it's still in a QUEUED state. Tasks that are already running can't be cancelled. A task might still run if it's processed from the queue before the CancelTask operation changes the task's state.
      parameters:
        - name: task_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/TaskId"
          description: The ID of the task that you are attempting to cancel. You can retrieve a task ID by using the ListTasks operation.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelTaskOutput"
  /task:
    post:
      operationId: CreateTask
      description: Instructs one or more devices to start a task, such as unlocking or rebooting.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                clientToken:
                  $ref: "#/components/schemas/IdempotencyToken"
                command:
                  $ref: "#/components/schemas/Command"
                description:
                  $ref: "#/components/schemas/TaskDescriptionString"
                tags:
                  $ref: "#/components/schemas/TagMap"
                targets:
                  $ref: "#/components/schemas/TargetList"
              required:
                - command
                - targets
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateTaskOutput"
  /managed-device/{managed_device_id}/describe:
    post:
      operationId: DescribeDevice
      description: Checks device-specific information, such as the device type, software version, IP addresses, and lock status.
      parameters:
        - name: managed_device_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the device that you are checking the information of.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeDeviceOutput"
  /managed-device/{managed_device_id}/resources/ec2/describe:
    post:
      operationId: DescribeDeviceEc2Instances
      description: Checks the current state of the Amazon EC2 instances. The output is similar to describeDevice, but the results are sourced from the device cache in the Amazon Web Services Cloud and include a subset of the available fields.
      parameters:
        - name: managed_device_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the managed device.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                instanceIds:
                  $ref: "#/components/schemas/InstanceIdsList"
              required:
                - instanceIds
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeDeviceEc2Output"
  /task/{task_id}/execution/{managed_device_id}:
    post:
      operationId: DescribeExecution
      description: Checks the status of a remote task running on one or more target devices.
      parameters:
        - name: managed_device_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the managed device.
        - name: task_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/TaskId"
          description: The ID of the task that the action is describing.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeExecutionOutput"
  /task/{task_id}:
    post:
      operationId: DescribeTask
      description: Checks the metadata for a given task on a device.
      parameters:
        - name: task_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/TaskId"
          description: The ID of the task to be described.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DescribeTaskOutput"
  /managed-device/{managed_device_id}/resources:
    get:
      operationId: ListDeviceResources
      description: Returns a list of the Amazon Web Services resources available for a device. Currently, Amazon EC2 instances are the only supported resource type.
      parameters:
        - name: managed_device_id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the managed device that you are listing the resources of.
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of resources per page.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of results.
        - name: type
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ListDeviceResourcesInputTypeString"
          description: A structure used to filter the results by type of resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDeviceResourcesOutput"
  /managed-devices:
    get:
      operationId: ListDevices
      description: Returns a list of all devices on your Amazon Web Services account that have Amazon Web Services Snow Device Management enabled in the Amazon Web Services Region where the command is run.
      parameters:
        - name: jobId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/JobId"
          description: The ID of the job used to order the device.
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of devices to list per page.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of results.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDevicesOutput"
  /executions:
    get:
      operationId: ListExecutions
      description: Returns the status of tasks for one or more target devices.
      parameters:
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of tasks to list per page.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of tasks.
        - name: state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ExecutionState"
          description: A structure used to filter the tasks by their current state.
        - name: taskId
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/TaskId"
          description: The ID of the task.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListExecutionsOutput"
  /tags/{resource_arn}:
    get:
      operationId: ListTagsForResource
      description: Returns a list of tags for a managed device or task.
      parameters:
        - name: resource_arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the device or task.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListTagsForResourceOutput"
    post:
      operationId: TagResource
      description: Adds or replaces tags on a device or task.
      parameters:
        - name: resource_arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the device or task.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  $ref: "#/components/schemas/TagMap"
              required:
                - tags
      responses:
        "200":
          description: Success
    delete:
      operationId: UntagResource
      description: Removes a tag from a device or task.
      parameters:
        - name: resource_arn
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the device or task.
        - name: tagKeys
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/TagKeys"
          description: Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment.
      responses:
        "200":
          description: Success
  /tasks:
    get:
      operationId: ListTasks
      description: Returns a list of tasks that can be filtered by state.
      parameters:
        - name: maxResults
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MaxResults"
          description: The maximum number of tasks per page.
        - name: nextToken
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of tasks.
        - name: state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/TaskState"
          description: A structure used to filter the list of tasks.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListTasksOutput"
components:
  parameters:
    X-Amz-Content-Sha256:
      in: header
      name: X-Amz-Content-Sha256
      required: false
      schema:
        type: string
    X-Amz-Date:
      in: header
      name: X-Amz-Date
      required: false
      schema:
        type: string
    X-Amz-Algorithm:
      in: header
      name: X-Amz-Algorithm
      required: false
      schema:
        type: string
    X-Amz-Credential:
      in: header
      name: X-Amz-Credential
      required: false
      schema:
        type: string
    X-Amz-Security-Token:
      in: header
      name: X-Amz-Security-Token
      required: false
      schema:
        type: string
    X-Amz-Signature:
      in: header
      name: X-Amz-Signature
      required: false
      schema:
        type: string
    X-Amz-SignedHeaders:
      in: header
      name: X-Amz-SignedHeaders
      required: false
      schema:
        type: string
  schemas:
    TaskId:
      type: string
      minLength: 1
      maxLength: 64
    CancelTaskOutput:
      type: object
      properties:
        taskId:
          $ref: "#/components/schemas/String"
          description: The ID of the task that you are attempting to cancel.
    String:
      type: string
    IdempotencyToken:
      type: string
      minLength: 1
      maxLength: 64
      pattern: "[!-~]+"
    Command:
      description: The command given to the device to execute.
      type: object
      properties:
        reboot:
          $ref: "#/components/schemas/Reboot"
          description: Reboots the device.
        unlock:
          $ref: "#/components/schemas/Unlock"
          description: Unlocks the device.
    Reboot:
      description: A structure used to reboot the device.
      type: object
    Unlock:
      description: A structure used to unlock a device.
      type: object
    TaskDescriptionString:
      type: string
      minLength: 1
      maxLength: 128
      pattern: "[A-Za-z0-9 _.,!#]*"
    TagMap:
      type: object
      properties: {}
    TargetList:
      type: array
      items:
        $ref: "#/components/schemas/String"
    CreateTaskOutput:
      type: object
      properties:
        taskArn:
          $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the task that you created.
        taskId:
          $ref: "#/components/schemas/String"
          description: The ID of the task that you created.
    ManagedDeviceId:
      type: string
      minLength: 1
      maxLength: 64
    DescribeDeviceOutput:
      type: object
      properties:
        associatedWithJob:
          $ref: "#/components/schemas/String"
          description: The ID of the job used when ordering the device.
        deviceCapacities:
          $ref: "#/components/schemas/CapacityList"
          description: The hardware specifications of the device.
        deviceState:
          $ref: "#/components/schemas/UnlockState"
          description: The current state of the device.
        deviceType:
          $ref: "#/components/schemas/String"
          description: The type of Amazon Web Services Snow Family device.
        lastReachedOutAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the device last contacted the Amazon Web Services Cloud. Indicates that the device is online.
        lastUpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the device last pushed an update to the Amazon Web Services Cloud. Indicates when the device cache was refreshed.
        managedDeviceArn:
          $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the device.
        managedDeviceId:
          $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the device that you checked the information for.
        physicalNetworkInterfaces:
          $ref: "#/components/schemas/PhysicalNetworkInterfaceList"
          description: The network interfaces available on the device.
        software:
          $ref: "#/components/schemas/SoftwareInformation"
          description: The software installed on the device.
        tags:
          type: object
          properties: {}
          description: Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment.
    CapacityList:
      type: array
      items:
        $ref: "#/components/schemas/Capacity"
    Capacity:
      description: The physical capacity of the Amazon Web Services Snow Family device.
      type: object
      properties:
        available:
          $ref: "#/components/schemas/Long"
          description: The amount of capacity available for use on the device.
        name:
          $ref: "#/components/schemas/CapacityNameString"
          description: The name of the type of capacity, such as memory.
        total:
          $ref: "#/components/schemas/Long"
          description: The total capacity on the device.
        unit:
          $ref: "#/components/schemas/CapacityUnitString"
          description: The unit of measure for the type of capacity.
        used:
          $ref: "#/components/schemas/Long"
          description: The amount of capacity used on the device.
    Long:
      type: integer
      format: int64
    CapacityNameString:
      type: string
      minLength: 0
      maxLength: 100
    CapacityUnitString:
      type: string
      minLength: 0
      maxLength: 20
    UnlockState:
      type: string
      enum:
        - UNLOCKED
        - LOCKED
        - UNLOCKING
    Timestamp:
      type: string
      format: date-time
    PhysicalNetworkInterfaceList:
      type: array
      items:
        $ref: "#/components/schemas/PhysicalNetworkInterface"
    PhysicalNetworkInterface:
      description: The details about the physical network interface for the device.
      type: object
      properties:
        defaultGateway:
          $ref: "#/components/schemas/String"
          description: The default gateway of the device.
        ipAddress:
          $ref: "#/components/schemas/String"
          description: The IP address of the device.
        ipAddressAssignment:
          $ref: "#/components/schemas/IpAddressAssignment"
          description: A value that describes whether the IP address is dynamic or persistent.
        macAddress:
          $ref: "#/components/schemas/String"
          description: The MAC address of the device.
        netmask:
          $ref: "#/components/schemas/String"
          description: The netmask used to divide the IP address into subnets.
        physicalConnectorType:
          $ref: "#/components/schemas/PhysicalConnectorType"
          description: The physical connector type.
        physicalNetworkInterfaceId:
          $ref: "#/components/schemas/String"
          description: The physical network interface ID.
    IpAddressAssignment:
      type: string
      enum:
        - DHCP
        - STATIC
    PhysicalConnectorType:
      type: string
      enum:
        - RJ45
        - SFP_PLUS
        - QSFP
        - RJ45_2
        - WIFI
    SoftwareInformation:
      description: Information about the software on the device.
      type: object
      properties:
        installState:
          $ref: "#/components/schemas/String"
          description: The state of the software that is installed or that is being installed on the device.
        installedVersion:
          $ref: "#/components/schemas/String"
          description: The version of the software currently installed on the device.
        installingVersion:
          $ref: "#/components/schemas/String"
          description: The version of the software being installed on the device.
    InstanceIdsList:
      type: array
      items:
        $ref: "#/components/schemas/String"
    DescribeDeviceEc2Output:
      type: object
      properties:
        instances:
          $ref: "#/components/schemas/InstanceSummaryList"
          description: A list of structures containing information about each instance.
    InstanceSummaryList:
      type: array
      items:
        $ref: "#/components/schemas/InstanceSummary"
    InstanceSummary:
      description: The details about the instance.
      type: object
      properties:
        instance:
          $ref: "#/components/schemas/Instance"
          description: A structure containing details about the instance.
        lastUpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the instance summary was last updated.
    Instance:
      description: The description of an instance. Currently, Amazon EC2 instances are the only supported instance type.
      type: object
      properties:
        amiLaunchIndex:
          $ref: "#/components/schemas/Integer"
          description: The Amazon Machine Image (AMI) launch index, which you can use to find this instance in the launch group.
        blockDeviceMappings:
          $ref: "#/components/schemas/InstanceBlockDeviceMappingList"
          description: Any block device mapping entries for the instance.
        cpuOptions:
          $ref: "#/components/schemas/CpuOptions"
          description: The CPU options for the instance.
        createdAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the instance was created.
        imageId:
          $ref: "#/components/schemas/String"
          description: The ID of the AMI used to launch the instance.
        instanceId:
          $ref: "#/components/schemas/String"
          description: The ID of the instance.
        instanceType:
          $ref: "#/components/schemas/String"
          description: The instance type.
        privateIpAddress:
          $ref: "#/components/schemas/String"
          description: The private IPv4 address assigned to the instance.
        publicIpAddress:
          $ref: "#/components/schemas/String"
          description: The public IPv4 address assigned to the instance.
        rootDeviceName:
          $ref: "#/components/schemas/String"
          description: The device name of the root device volume (for example, /dev/sda1).
        securityGroups:
          $ref: "#/components/schemas/SecurityGroupIdentifierList"
          description: The security groups for the instance.
        state:
          $ref: "#/components/schemas/InstanceState"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the instance was last updated.
    Integer:
      type: integer
    InstanceBlockDeviceMappingList:
      type: array
      items:
        $ref: "#/components/schemas/InstanceBlockDeviceMapping"
    InstanceBlockDeviceMapping:
      description: The description of a block device mapping.
      type: object
      properties:
        deviceName:
          $ref: "#/components/schemas/String"
          description: The block device name.
        ebs:
          $ref: "#/components/schemas/EbsInstanceBlockDevice"
          description: The parameters used to automatically set up Amazon Elastic Block Store (Amazon EBS) volumes when the instance is launched.
    EbsInstanceBlockDevice:
      description: Describes a parameter used to set up an Amazon Elastic Block Store (Amazon EBS) volume in a block device mapping.
      type: object
      properties:
        attachTime:
          $ref: "#/components/schemas/Timestamp"
          description: When the attachment was initiated.
        deleteOnTermination:
          $ref: "#/components/schemas/Boolean"
          description: A value that indicates whether the volume is deleted on instance termination.
        status:
          $ref: "#/components/schemas/AttachmentStatus"
          description: The attachment state.
        volumeId:
          $ref: "#/components/schemas/String"
          description: The ID of the Amazon EBS volume.
    Boolean:
      type: boolean
    AttachmentStatus:
      type: string
      enum:
        - ATTACHING
        - ATTACHED
        - DETACHING
        - DETACHED
    CpuOptions:
      description: The options for how a device's CPU is configured.
      type: object
      properties:
        coreCount:
          $ref: "#/components/schemas/Integer"
          description: The number of cores that the CPU can use.
        threadsPerCore:
          $ref: "#/components/schemas/Integer"
          description: The number of threads per core in the CPU.
    SecurityGroupIdentifierList:
      type: array
      items:
        $ref: "#/components/schemas/SecurityGroupIdentifier"
    SecurityGroupIdentifier:
      description: Information about the device's security group.
      type: object
      properties:
        groupId:
          $ref: "#/components/schemas/String"
          description: The security group ID.
        groupName:
          $ref: "#/components/schemas/String"
          description: The security group name.
    InstanceState:
      description: The description of the current state of an instance.
      type: object
      properties:
        code:
          $ref: "#/components/schemas/Integer"
          description: "The state of the instance as a 16-bit unsigned integer. The high byte is all of the bits between 2^8 and (2^16)-1, which equals decimal values between 256 and 65,535. These numerical values are used for internal purposes and should be ignored. The low byte is all of the bits between 2^0 and (2^8)-1, which equals decimal values between 0 and 255. The valid values for the instance state code are all in the range of the low byte. These values are: 0 : pending 16 : running 32 : shutting-down 48 : terminated 64 : stopping 80 : stopped You can ignore the high byte value by zeroing out all of the bits above 2^8 or 256 in decimal."
        name:
          $ref: "#/components/schemas/InstanceStateName"
          description: The current state of the instance.
    InstanceStateName:
      type: string
      enum:
        - PENDING
        - RUNNING
        - SHUTTING_DOWN
        - TERMINATED
        - STOPPING
        - STOPPED
    DescribeExecutionOutput:
      type: object
      properties:
        executionId:
          $ref: "#/components/schemas/ExecutionId"
          description: The ID of the execution.
        lastUpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the status of the execution was last updated.
        managedDeviceId:
          $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the managed device that the task is being executed on.
        startedAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the execution began.
        state:
          $ref: "#/components/schemas/ExecutionState"
          description: The current state of the execution.
        taskId:
          $ref: "#/components/schemas/TaskId"
          description: The ID of the task being executed on the device.
    ExecutionId:
      type: string
      minLength: 1
      maxLength: 64
    ExecutionState:
      type: string
      enum:
        - QUEUED
        - IN_PROGRESS
        - CANCELED
        - FAILED
        - SUCCEEDED
        - REJECTED
        - TIMED_OUT
    DescribeTaskOutput:
      type: object
      properties:
        completedAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the task was completed.
        createdAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the CreateTask operation was called.
        description:
          $ref: "#/components/schemas/TaskDescriptionString"
          description: The description provided of the task and managed devices.
        lastUpdatedAt:
          $ref: "#/components/schemas/Timestamp"
          description: When the state of the task was last updated.
        state:
          $ref: "#/components/schemas/TaskState"
          description: The current state of the task.
        tags:
          type: object
          properties: {}
          description: Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment.
        targets:
          $ref: "#/components/schemas/TargetList"
          description: The managed devices that the task was sent to.
        taskArn:
          $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the task.
        taskId:
          $ref: "#/components/schemas/String"
          description: The ID of the task.
    TaskState:
      type: string
      enum:
        - IN_PROGRESS
        - CANCELED
        - COMPLETED
    MaxResults:
      type: integer
      minimum: 1
      maximum: 100
    NextToken:
      type: string
      minLength: 1
      maxLength: 1024
      pattern: "[a-zA-Z0-9+/=]*"
    ListDeviceResourcesInputTypeString:
      type: string
      minLength: 1
      maxLength: 50
    ListDeviceResourcesOutput:
      type: object
      properties:
        nextToken:
          $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of results.
        resources:
          $ref: "#/components/schemas/ResourceSummaryList"
          description: A structure defining the resource's type, Amazon Resource Name (ARN), and ID.
    ResourceSummaryList:
      type: array
      items:
        $ref: "#/components/schemas/ResourceSummary"
    ResourceSummary:
      description: A summary of a resource available on the device.
      type: object
      properties:
        arn:
          $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the resource.
        id:
          $ref: "#/components/schemas/String"
          description: The ID of the resource.
        resourceType:
          $ref: "#/components/schemas/String"
          description: The resource type.
      required:
        - resourceType
    JobId:
      type: string
      minLength: 1
      maxLength: 64
    ListDevicesOutput:
      type: object
      properties:
        devices:
          $ref: "#/components/schemas/DeviceSummaryList"
          description: A list of device structures that contain information about the device.
        nextToken:
          $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of devices.
    DeviceSummaryList:
      type: array
      items:
        $ref: "#/components/schemas/DeviceSummary"
    DeviceSummary:
      description: Identifying information about the device.
      type: object
      properties:
        associatedWithJob:
          $ref: "#/components/schemas/String"
          description: The ID of the job used to order the device.
        managedDeviceArn:
          $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the device.
        managedDeviceId:
          $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the device.
        tags:
          type: object
          properties: {}
          description: Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment.
    ListExecutionsOutput:
      type: object
      properties:
        executions:
          $ref: "#/components/schemas/ExecutionSummaryList"
          description: A list of executions. Each execution contains the task ID, the device that the task is executing on, the execution ID, and the status of the execution.
        nextToken:
          $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of executions.
    ExecutionSummaryList:
      type: array
      items:
        $ref: "#/components/schemas/ExecutionSummary"
    ExecutionSummary:
      description: The summary of a task execution on a specified device.
      type: object
      properties:
        executionId:
          $ref: "#/components/schemas/ExecutionId"
          description: The ID of the execution.
        managedDeviceId:
          $ref: "#/components/schemas/ManagedDeviceId"
          description: The ID of the managed device that the task is being executed on.
        state:
          $ref: "#/components/schemas/ExecutionState"
          description: The state of the execution.
        taskId:
          $ref: "#/components/schemas/TaskId"
          description: The ID of the task.
    ListTagsForResourceOutput:
      type: object
      properties:
        tags:
          type: object
          properties: {}
          description: The list of tags for the device or task.
    ListTasksOutput:
      type: object
      properties:
        nextToken:
          $ref: "#/components/schemas/NextToken"
          description: A pagination token to continue to the next page of tasks.
        tasks:
          $ref: "#/components/schemas/TaskSummaryList"
          description: A list of task structures containing details about each task.
    TaskSummaryList:
      type: array
      items:
        $ref: "#/components/schemas/TaskSummary"
    TaskSummary:
      description: Information about the task assigned to one or many devices.
      type: object
      properties:
        state:
          $ref: "#/components/schemas/TaskState"
          description: The state of the task assigned to one or many devices.
        tags:
          type: object
          properties: {}
          description: Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment.
        taskArn:
          $ref: "#/components/schemas/String"
          description: The Amazon Resource Name (ARN) of the task.
        taskId:
          $ref: "#/components/schemas/TaskId"
          description: The task ID.
      required:
        - taskId
    TagKeys:
      type: array
      items:
        $ref: "#/components/schemas/String"
  x-stackQL-resources:
    tasks:
      id: aws.snow_device_management.tasks
      name: tasks
      title: tasks
      methods:
        cancel_task:
          operation:
            $ref: "#/paths/~1task~1{task_id}~1cancel/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        create_task:
          operation:
            $ref: "#/paths/~1task/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        describe_task:
          operation:
            $ref: "#/paths/~1task~1{task_id}/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_tasks:
          operation:
            $ref: "#/paths/~1tasks/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.tasks
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.nextToken
                location: body
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/describe_task"
          - $ref: "#/components/x-stackQL-resources/tasks/methods/list_tasks"
        insert:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/create_task"
        update: []
        delete: []
    devices:
      id: aws.snow_device_management.devices
      name: devices
      title: devices
      methods:
        describe_device:
          operation:
            $ref: "#/paths/~1managed-device~1{managed_device_id}~1describe/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_devices:
          operation:
            $ref: "#/paths/~1managed-devices/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.devices
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.nextToken
                location: body
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/devices/methods/describe_device"
          - $ref: "#/components/x-stackQL-resources/devices/methods/list_devices"
        insert: []
        update: []
        delete: []
    device_ec2_instances:
      id: aws.snow_device_management.device_ec2_instances
      name: device_ec2_instances
      title: device_ec2_instances
      methods:
        describe_device_ec2_instances:
          operation:
            $ref: "#/paths/~1managed-device~1{managed_device_id}~1resources~1ec2~1describe/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/device_ec2_instances/methods/describe_device_ec2_instances"
        insert: []
        update: []
        delete: []
    executions:
      id: aws.snow_device_management.executions
      name: executions
      title: executions
      methods:
        describe_execution:
          operation:
            $ref: "#/paths/~1task~1{task_id}~1execution~1{managed_device_id}/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_executions:
          operation:
            $ref: "#/paths/~1executions/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.executions
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.nextToken
                location: body
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/executions/methods/describe_execution"
          - $ref: "#/components/x-stackQL-resources/executions/methods/list_executions"
        insert: []
        update: []
        delete: []
    device_resources:
      id: aws.snow_device_management.device_resources
      name: device_resources
      title: device_resources
      methods:
        list_device_resources:
          operation:
            $ref: "#/paths/~1managed-device~1{managed_device_id}~1resources/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.resources
          request:
            nativeCasing: pascal
          config:
            pagination:
              requestToken:
                key: nextToken
                location: query
              responseToken:
                key: $.nextToken
                location: body
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/device_resources/methods/list_device_resources"
        insert: []
        update: []
        delete: []
    tags:
      id: aws.snow_device_management.tags
      name: tags
      title: tags
      methods:
        list_tags_for_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource_arn}/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        tag_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource_arn}/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: pascal
          config:
            requestBodyTranslate:
              algorithm: naive
        untag_resource:
          operation:
            $ref: "#/paths/~1tags~1{resource_arn}/delete"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: pascal
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tags/methods/list_tags_for_resource"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/tags/methods/tag_resource"
          - $ref: "#/components/x-stackQL-resources/tags/methods/untag_resource"
        delete: []
