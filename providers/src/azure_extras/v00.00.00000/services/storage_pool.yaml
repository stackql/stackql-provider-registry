openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Disk Pools
  description: Disk pools and iSCSI targets (retired upstream).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-08-01-stackql-generated
  x-serviceAlias: storage_pool
  x-sdkPackage: azure-mgmt-storagepool
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    Sku:
      type: object
      description: Sku for ARM resource. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: Sku name. Required.
        tier:
          type: string
          description: Sku tier.
      required:
        - name
    DiskPoolZoneInfo:
      type: object
      description: Disk Pool SKU Details. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        availabilityZones:
          type: array
          items:
            type: string
          description: 'Logical zone for Disk Pool resource; example: ["1"].'
        additionalCapabilities:
          type: array
          items:
            type: string
          description: List of additional capabilities for Disk Pool.
        sku:
          $ref: "#/components/schemas/Sku"
          description: Determines the SKU of VM deployed for Disk Pool.
    PagedDiskPoolZoneInfo:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DiskPoolZoneInfo"
        nextLink:
          type: string
    SystemMetadata:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The type of identity that last modified the resource.
    Disk:
      type: object
      description: Azure Managed Disk to attach to the Disk Pool. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Unique Azure Resource ID of the Managed Disk. Required.
      required:
        - id
    DiskPoolFlattened:
      type: object
      description: Flattened view of DiskPool ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        managedBy:
          type: string
          description: Azure resource id. Indicates if this resource is managed by another Azure resource.
        managedByExtended:
          type: array
          items:
            type: string
          description: List of Azure resource ids that manage this resource.
        systemData:
          $ref: "#/components/schemas/SystemMetadata"
          description: Resource metadata required by ARM RPC.
        sku:
          type: object
          properties:
            name:
              type: string
              description: Sku name.
            tier:
              type: string
              description: Sku tier.
        provisioningState:
          type: string
          description: 'State of the operation on the resource. Required. Known values are: "Invalid", "Succeeded", "Failed", "Canceled", "Pending", "Creating", "Updating", and "Deleting".'
        availabilityZones:
          type: array
          items:
            type: string
          description: 'Logical zone for Disk Pool resource; example: ["1"]. Required.'
        status:
          type: string
          description: 'Operational status of the Disk Pool. Required. Known values are: "Invalid", "Unknown", "Healthy", "Unhealthy", "Updating", "Running", "Stopped", and "Stopped (deallocated)".'
        disks:
          type: array
          items:
            $ref: "#/components/schemas/Disk"
          description: List of Azure Managed Disks to attach to a Disk Pool.
        subnetId:
          type: string
          description: Azure Resource ID of a Subnet for the Disk Pool. Required.
        additionalCapabilities:
          type: array
          items:
            type: string
          description: List of additional capabilities for Disk Pool.
    PagedDiskPoolFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DiskPoolFlattened"
        nextLink:
          type: string
    DiskPool:
      type: object
      description: Response for Disk Pool request. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        managedBy:
          type: string
          description: Azure resource id. Indicates if this resource is managed by another Azure resource.
        managedByExtended:
          type: array
          items:
            type: string
          description: List of Azure resource ids that manage this resource.
        systemData:
          $ref: "#/components/schemas/SystemMetadata"
          description: Resource metadata required by ARM RPC.
        properties:
          type: object
          properties:
            provisioningState:
              type: string
              description: 'State of the operation on the resource. Required. Known values are: "Invalid", "Succeeded", "Failed", "Canceled", "Pending", "Creating", "Updating", and "Deleting".'
            availabilityZones:
              type: array
              items:
                type: string
              description: 'Logical zone for Disk Pool resource; example: ["1"]. Required.'
            status:
              type: string
              description: 'Operational status of the Disk Pool. Required. Known values are: "Invalid", "Unknown", "Healthy", "Unhealthy", "Updating", "Running", "Stopped", and "Stopped (deallocated)".'
            disks:
              type: array
              items:
                $ref: "#/components/schemas/Disk"
              description: List of Azure Managed Disks to attach to a Disk Pool.
            subnetId:
              type: string
              description: Azure Resource ID of a Subnet for the Disk Pool. Required.
            additionalCapabilities:
              type: array
              items:
                type: string
              description: List of additional capabilities for Disk Pool.
        sku:
          type: object
          properties:
            name:
              type: string
              description: Sku name.
            tier:
              type: string
              description: Sku tier.
      required:
        - location
        - properties
    EndpointDependency:
      type: object
      description: A domain name that a service is reached at, including details of the current connection status.
      properties:
        domainName:
          type: string
          description: The domain name of the dependency.
        endpointDetails:
          type: array
          items:
            $ref: "#/components/schemas/EndpointDetail"
          description: The IP Addresses and Ports used when connecting to DomainName.
    EndpointDetail:
      type: object
      description: Current TCP connectivity information from the App Service Environment to a single endpoint.
      properties:
        ipAddress:
          type: string
          description: An IP Address that Domain Name currently resolves to.
        port:
          type: integer
          description: The port an endpoint is connected to.
        latency:
          type: number
          description: The time in milliseconds it takes for a TCP connection to be created from the App Service Environment to this IpAddress at this Port.
        isAccessible:
          type: boolean
          description: Whether it is possible to create a TCP connection from the App Service Environment to this IpAddress at this Port.
    OutboundEnvironmentEndpoint:
      type: object
      description: Endpoints accessed for a common purpose that the App Service Environment requires outbound network access to.
      properties:
        category:
          type: string
          description: The type of service accessed by the App Service Environment, e.g., Azure Storage, Azure SQL Database, and Azure Active Directory.
        endpoints:
          type: array
          items:
            $ref: "#/components/schemas/EndpointDependency"
          description: The endpoints that the App Service Environment reaches the service at.
    PagedOutboundEnvironmentEndpoint:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OutboundEnvironmentEndpoint"
        nextLink:
          type: string
    Acl:
      type: object
      description: Access Control List (ACL) for an iSCSI Target; defines LUN masking policy. All required parameters must be populated in order to send to Azure.
      properties:
        initiatorIqn:
          type: string
          description: 'iSCSI initiator IQN (iSCSI Qualified Name); example: "iqn.2005-03.org.iscsi:client". Required.'
        mappedLuns:
          type: array
          items:
            type: string
          description: List of LUN names mapped to the ACL. Required.
      required:
        - initiatorIqn
        - mappedLuns
    IscsiLun:
      type: object
      description: LUN to expose the Azure Managed Disk. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: 'User defined name for iSCSI LUN; example: "lun0". Required.'
        managedDiskAzureResourceId:
          type: string
          description: Azure Resource ID of the Managed Disk. Required.
        lun:
          type: integer
          description: Specifies the Logical Unit Number of the iSCSI LUN.
      required:
        - name
        - managedDiskAzureResourceId
    IscsiTargetFlattened:
      type: object
      description: Flattened view of IscsiTarget ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        systemData:
          $ref: "#/components/schemas/SystemMetadata"
          description: Resource metadata required by ARM RPC.
        managedBy:
          type: string
          description: Azure resource id. Indicates if this resource is managed by another Azure resource.
        managedByExtended:
          type: array
          items:
            type: string
          description: List of Azure resource ids that manage this resource.
        aclMode:
          type: string
          description: 'Mode for Target connectivity. Required. Known values are: "Dynamic" and "Static".'
        staticAcls:
          type: array
          items:
            $ref: "#/components/schemas/Acl"
          description: Access Control List (ACL) for an iSCSI Target; defines LUN masking policy.
        luns:
          type: array
          items:
            $ref: "#/components/schemas/IscsiLun"
          description: List of LUNs to be exposed through iSCSI Target.
        targetIqn:
          type: string
          description: 'iSCSI Target IQN (iSCSI Qualified Name); example: "iqn.2005-03.org.iscsi:server". Required.'
        provisioningState:
          type: string
          description: 'State of the operation on the resource. Required. Known values are: "Invalid", "Succeeded", "Failed", "Canceled", "Pending", "Creating", "Updating", and "Deleting".'
        status:
          type: string
          description: 'Operational status of the iSCSI Target. Required. Known values are: "Invalid", "Unknown", "Healthy", "Unhealthy", "Updating", "Running", "Stopped", and "Stopped (deallocated)".'
        endpoints:
          type: array
          items:
            type: string
          description: List of private IPv4 addresses to connect to the iSCSI Target.
        port:
          type: integer
          description: The port used by iSCSI Target portal group.
        sessions:
          type: array
          items:
            type: string
          description: List of identifiers for active sessions on the iSCSI target.
    PagedIscsiTargetFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/IscsiTargetFlattened"
        nextLink:
          type: string
    IscsiTarget:
      type: object
      description: Response for iSCSI Target requests. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        systemData:
          $ref: "#/components/schemas/SystemMetadata"
          description: Resource metadata required by ARM RPC.
        managedBy:
          type: string
          description: Azure resource id. Indicates if this resource is managed by another Azure resource.
        managedByExtended:
          type: array
          items:
            type: string
          description: List of Azure resource ids that manage this resource.
        properties:
          type: object
          properties:
            aclMode:
              type: string
              description: 'Mode for Target connectivity. Required. Known values are: "Dynamic" and "Static".'
            staticAcls:
              type: array
              items:
                $ref: "#/components/schemas/Acl"
              description: Access Control List (ACL) for an iSCSI Target; defines LUN masking policy.
            luns:
              type: array
              items:
                $ref: "#/components/schemas/IscsiLun"
              description: List of LUNs to be exposed through iSCSI Target.
            targetIqn:
              type: string
              description: 'iSCSI Target IQN (iSCSI Qualified Name); example: "iqn.2005-03.org.iscsi:server". Required.'
            provisioningState:
              type: string
              description: 'State of the operation on the resource. Required. Known values are: "Invalid", "Succeeded", "Failed", "Canceled", "Pending", "Creating", "Updating", and "Deleting".'
            status:
              type: string
              description: 'Operational status of the iSCSI Target. Required. Known values are: "Invalid", "Unknown", "Healthy", "Unhealthy", "Updating", "Running", "Stopped", and "Stopped (deallocated)".'
            endpoints:
              type: array
              items:
                type: string
              description: List of private IPv4 addresses to connect to the iSCSI Target.
            port:
              type: integer
              description: The port used by iSCSI Target portal group.
            sessions:
              type: array
              items:
                type: string
              description: List of identifiers for active sessions on the iSCSI target.
      required:
        - properties
    StoragePoolOperationDisplay:
      type: object
      description: Metadata about an operation. All required parameters must be populated in order to send to Azure.
      properties:
        provider:
          type: string
          description: Localized friendly form of the resource provider name. Required.
        resource:
          type: string
          description: Localized friendly form of the resource type related to this action/operation. Required.
        operation:
          type: string
          description: Localized friendly name for the operation, as it should be shown to the user. Required.
        description:
          type: string
          description: Localized friendly description for the operation, as it should be shown to the user. Required.
      required:
        - provider
        - resource
        - operation
        - description
    StoragePoolRPOperation:
      type: object
      description: Description of a StoragePool RP Operation. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: The name of the operation being performed on this particular object. Required.
        isDataAction:
          type: boolean
          description: Indicates whether the operation applies to data-plane. Required.
        actionType:
          type: string
          description: Indicates the action type.
        display:
          $ref: "#/components/schemas/StoragePoolOperationDisplay"
          description: Additional metadata about RP operation. Required.
        origin:
          type: string
          description: The intended executor of the operation; governs the display of the operation in the RBAC UX and the audit logs UX.
      required:
        - name
        - isDataAction
        - display
    PagedStoragePoolRPOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StoragePoolRPOperation"
        nextLink:
          type: string
    ResourceSkuCapability:
      type: object
      description: Capability a resource SKU has. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Capability name.
        value:
          type: string
          description: Capability value.
    ResourceSkuLocationInfo:
      type: object
      description: Zone and capability info for resource sku. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        location:
          type: string
          description: Location of the SKU.
        zones:
          type: array
          items:
            type: string
          description: List of availability zones where the SKU is supported.
        zoneDetails:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuZoneDetails"
          description: Details of capabilities available to a SKU in specific zones.
    ResourceSkuZoneDetails:
      type: object
      description: Describes The zonal capabilities of a SKU. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: array
          items:
            type: string
          description: The set of zones that the SKU is available in with the specified capabilities.
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuCapability"
          description: A list of capabilities that are available for the SKU in the specified list of zones.
    ResourceSkuRestrictions:
      type: object
      description: Describes scaling information of a SKU. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        type:
          type: string
          description: 'The type of restrictions. Known values are: "Location" and "Zone".'
        values:
          type: array
          items:
            type: string
          description: The value of restrictions. If the restriction type is set to location. This would be different locations where the SKU is restricted.
        restrictionInfo:
          $ref: "#/components/schemas/ResourceSkuRestrictionInfo"
          description: The information about the restriction where the SKU cannot be used.
        reasonCode:
          type: string
          description: 'The reason for restriction. Known values are: "QuotaId" and "NotAvailableForSubscription".'
    ResourceSkuRestrictionInfo:
      type: object
      description: Describes an available Compute SKU Restriction Information. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        locations:
          type: array
          items:
            type: string
          description: Locations where the SKU is restricted.
        zones:
          type: array
          items:
            type: string
          description: List of availability zones where the SKU is restricted.
    ResourceSkuInfo:
      type: object
      description: Resource SKU Details. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        apiVersion:
          type: string
          description: StoragePool RP API version.
        resourceType:
          type: string
          description: StoragePool resource type.
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuCapability"
          description: List of additional capabilities for StoragePool resource.
        locationInfo:
          $ref: "#/components/schemas/ResourceSkuLocationInfo"
          description: Zones and zone capabilities in those locations where the SKU is available.
        name:
          type: string
          description: Sku name.
        tier:
          type: string
          description: Sku tier.
        restrictions:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuRestrictions"
          description: The restrictions because of which SKU cannot be used. This is empty if there are no restrictions.
    PagedResourceSkuInfo:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuInfo"
        nextLink:
          type: string
  x-stackQL-resources:
    disk_pool_zones:
      id: azure_extras.storage_pool.disk_pool_zones
      name: disk_pool_zones
      title: disk_pool_zones
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StoragePool~1locations~1{location}~1diskPoolZones?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/disk_pool_zones/methods/list"
        insert: []
        update: []
        delete: []
    disk_pools:
      id: azure_extras.storage_pool.disk_pools
      name: disk_pools
      title: disk_pools
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}?api-version=2021-08-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}?api-version=2021-08-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}?api-version=2021-08-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StoragePool~1diskPools?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_outbound_network_dependencies_endpoints:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1outboundNetworkDependenciesEndpoints?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        start:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1start?api-version=2021-08-01/post"
          response:
            openAPIDocKey: "204"
        deallocate:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1deallocate?api-version=2021-08-01/post"
          response:
            openAPIDocKey: "204"
        upgrade:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1upgrade?api-version=2021-08-01/post"
          response:
            openAPIDocKey: "204"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/disk_pools/methods/get"
          - $ref: "#/components/x-stackQL-resources/disk_pools/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/disk_pools/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/disk_pools/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/disk_pools/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/disk_pools/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/disk_pools/methods/delete"
    iscsi_targets:
      id: azure_extras.storage_pool.iscsi_targets
      name: iscsi_targets
      title: iscsi_targets
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1iscsiTargets~1{iscsi_target_name}?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1iscsiTargets~1{iscsi_target_name}?api-version=2021-08-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1iscsiTargets~1{iscsi_target_name}?api-version=2021-08-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1iscsiTargets~1{iscsi_target_name}?api-version=2021-08-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_disk_pool:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StoragePool~1diskPools~1{disk_pool_name}~1iscsiTargets?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/iscsi_targets/methods/get"
          - $ref: "#/components/x-stackQL-resources/iscsi_targets/methods/list_by_disk_pool"
        insert:
          - $ref: "#/components/x-stackQL-resources/iscsi_targets/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/iscsi_targets/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/iscsi_targets/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/iscsi_targets/methods/delete"
    operations:
      id: azure_extras.storage_pool.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.StoragePool~1operations?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    resource_skus:
      id: azure_extras.storage_pool.resource_skus
      name: resource_skus
      title: resource_skus
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StoragePool~1locations~1{location}~1skus?api-version=2021-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_skus/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.StoragePool/locations/{location}/diskPoolZones?api-version=2021-08-01:
    get:
      operationId: DiskPoolZones_list
      description: Lists available Disk Pool Skus in an Azure location.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The location of the resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDiskPoolZoneInfo"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools/{disk_pool_name}?api-version=2021-08-01:
    get:
      operationId: DiskPools_get
      description: Get a Disk pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiskPoolFlattened"
    put:
      operationId: DiskPools_begin_create_or_update
      description: Create or Update Disk pool. This create or update operation can take 15 minutes to complete. This is expected service behavior.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: Determines the SKU of the Disk Pool. Required.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                managedBy:
                  type: string
                  description: Azure resource id. Indicates if this resource is managed by another Azure resource.
                managedByExtended:
                  type: array
                  items:
                    type: string
                  description: List of Azure resource ids that manage this resource.
                properties:
                  type: object
                  properties:
                    availabilityZones:
                      type: array
                      items:
                        type: string
                      description: 'Logical zone for Disk Pool resource; example: ["1"].'
                    disks:
                      type: array
                      items:
                        $ref: "#/components/schemas/Disk"
                      description: List of Azure Managed Disks to attach to a Disk Pool.
                    subnetId:
                      type: string
                      description: Azure Resource ID of a Subnet for the Disk Pool. Required.
                    additionalCapabilities:
                      type: array
                      items:
                        type: string
                      description: List of additional capabilities for a Disk Pool.
              required:
                - sku
                - location
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiskPool"
    patch:
      operationId: DiskPools_begin_update
      description: Update a Disk pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                managedBy:
                  type: string
                  description: Azure resource id. Indicates if this resource is managed by another Azure resource.
                managedByExtended:
                  type: array
                  items:
                    type: string
                  description: List of Azure resource ids that manage this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: Determines the SKU of the Disk Pool.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  type: object
                  properties:
                    disks:
                      type: array
                      items:
                        $ref: "#/components/schemas/Disk"
                      description: List of Azure Managed Disks to attach to a Disk Pool.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiskPool"
    delete:
      operationId: DiskPools_begin_delete
      description: Delete a Disk pool; attached disks are not affected. This delete operation can take 10 minutes to complete. This is expected service behavior.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.StoragePool/diskPools?api-version=2021-08-01:
    get:
      operationId: DiskPools_list_by_subscription
      description: Gets a list of Disk Pools in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDiskPoolFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools?api-version=2021-08-01:
    get:
      operationId: DiskPools_list_by_resource_group
      description: Gets a list of DiskPools in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDiskPoolFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools/{disk_pool_name}/outboundNetworkDependenciesEndpoints?api-version=2021-08-01:
    get:
      operationId: DiskPools_list_outbound_network_dependencies_endpoints
      description: Gets the network endpoints of all outbound dependencies of a Disk Pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOutboundEnvironmentEndpoint"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools/{disk_pool_name}/start?api-version=2021-08-01:
    post:
      operationId: DiskPools_begin_start
      description: The operation to start a Disk Pool. This start operation can take 10 minutes to complete. This is expected service behavior.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools/{disk_pool_name}/deallocate?api-version=2021-08-01:
    post:
      operationId: DiskPools_begin_deallocate
      description: Shuts down the Disk Pool and releases the compute resources. You are not billed for the compute resources that this Disk Pool uses. This operation can take 10 minutes to complete. This is expected service behavior.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools/{disk_pool_name}/upgrade?api-version=2021-08-01:
    post:
      operationId: DiskPools_begin_upgrade
      description: Upgrade replaces the underlying virtual machine hosts one at a time. This operation can take 10-15 minutes to complete. This is expected service behavior.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools/{disk_pool_name}/iscsiTargets/{iscsi_target_name}?api-version=2021-08-01:
    get:
      operationId: IscsiTargets_get
      description: Get an iSCSI Target.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: iscsi_target_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the iSCSI Target. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IscsiTargetFlattened"
    put:
      operationId: IscsiTargets_begin_create_or_update
      description: Create or Update an iSCSI Target.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: iscsi_target_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the iSCSI Target. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                managedBy:
                  type: string
                  description: Azure resource id. Indicates if this resource is managed by another Azure resource.
                managedByExtended:
                  type: array
                  items:
                    type: string
                  description: List of Azure resource ids that manage this resource.
                properties:
                  type: object
                  properties:
                    aclMode:
                      type: string
                      description: 'Mode for Target connectivity. Required. Known values are: "Dynamic" and "Static".'
                    targetIqn:
                      type: string
                      description: 'iSCSI Target IQN (iSCSI Qualified Name); example: "iqn.2005-03.org.iscsi:server".'
                    staticAcls:
                      type: array
                      items:
                        $ref: "#/components/schemas/Acl"
                      description: Access Control List (ACL) for an iSCSI Target; defines LUN masking policy.
                    luns:
                      type: array
                      items:
                        $ref: "#/components/schemas/IscsiLun"
                      description: List of LUNs to be exposed through iSCSI Target.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IscsiTarget"
    patch:
      operationId: IscsiTargets_begin_update
      description: Update an iSCSI Target.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: iscsi_target_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the iSCSI Target. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                managedBy:
                  type: string
                  description: Azure resource id. Indicates if this resource is managed by another Azure resource.
                managedByExtended:
                  type: array
                  items:
                    type: string
                  description: List of Azure resource ids that manage this resource.
                properties:
                  type: object
                  properties:
                    staticAcls:
                      type: array
                      items:
                        $ref: "#/components/schemas/Acl"
                      description: Access Control List (ACL) for an iSCSI Target; defines LUN masking policy.
                    luns:
                      type: array
                      items:
                        $ref: "#/components/schemas/IscsiLun"
                      description: List of LUNs to be exposed through iSCSI Target.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IscsiTarget"
    delete:
      operationId: IscsiTargets_begin_delete
      description: Delete an iSCSI Target.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: iscsi_target_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the iSCSI Target. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StoragePool/diskPools/{disk_pool_name}/iscsiTargets?api-version=2021-08-01:
    get:
      operationId: IscsiTargets_list_by_disk_pool
      description: Get iSCSI Targets in a Disk pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: disk_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Disk Pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedIscsiTargetFlattened"
  /providers/Microsoft.StoragePool/operations?api-version=2021-08-01:
    get:
      operationId: storagepool_list
      description: Gets a list of StoragePool operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStoragePoolRPOperation"
  /subscriptions/{subscription_id}/providers/Microsoft.StoragePool/locations/{location}/skus?api-version=2021-08-01:
    get:
      operationId: ResourceSkus_list
      description: Lists available StoragePool resources and skus in an Azure location.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The location of the resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourceSkuInfo"
