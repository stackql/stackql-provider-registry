openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Agriculture Platform
  description: Agriculture industry data platform (Microsoft.AgriculturePlatform).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-06-01-preview-stackql-generated
  x-serviceAlias: agriculture_platform
  x-sdkPackage: azure-mgmt-agricultureplatform
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    AgriServiceResourceProperties:
      type: object
      description: Details of the Agriculture AgriDataManager.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        config:
          $ref: "#/components/schemas/AgriServiceConfig"
          description: Config of the AgriService instance.
        managedOnBehalfOfConfiguration:
          $ref: "#/components/schemas/ManagedOnBehalfOfConfiguration"
          description: Managed On Behalf Of Configuration.
        dataConnectorCredentials:
          type: array
          items:
            $ref: "#/components/schemas/DataConnectorCredentialMap"
          description: Data connector credentials of AgriService instance.
        installedSolutions:
          type: array
          items:
            $ref: "#/components/schemas/InstalledSolutionMap"
          description: AgriService installed solutions.
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
        - Accepted
    AgriServiceConfig:
      type: object
      description: Config of the AgriService resource instance.
      properties:
        instanceUri:
          type: string
          description: Instance URI of the AgriService instance.
        version:
          type: string
          description: Version of AgriService instance.
        appServiceResourceId:
          type: string
          description: App service resource Id.
        cosmosDbResourceId:
          type: string
          description: Cosmos Db resource Id.
        storageAccountResourceId:
          type: string
          description: Storage account resource Id.
        keyVaultResourceId:
          type: string
          description: Key vault resource Id.
        redisCacheResourceId:
          type: string
          description: Redis cache resource Id.
    ManagedOnBehalfOfConfiguration:
      type: object
      description: Configuration of the managed on behalf of resource.
      properties:
        moboBrokerResources:
          type: array
          items:
            $ref: "#/components/schemas/MoboBrokerResource"
          description: Associated MoboBrokerResources. Required.
    MoboBrokerResource:
      type: object
      description: MoboBroker resource.
      properties:
        id:
          type: string
          description: "The fully qualified resource ID of the MoboBroker resource. Example: `/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}`. Required."
    DataConnectorCredentialMap:
      type: object
      description: Mapping of data connector credentials.
      properties:
        key:
          type: string
          description: The key representing the credential. Required.
        value:
          $ref: "#/components/schemas/DataConnectorCredentials"
          description: The data connector credential value. Required.
      required:
        - key
        - value
    DataConnectorCredentials:
      type: object
      description: The properties related to an AgriService data connector.
      properties:
        kind:
          $ref: "#/components/schemas/AuthCredentialsKind"
          description: 'Type of credential. Known values are: "OAuthClientCredentials" and "ApiKeyAuthCredentials".'
        clientId:
          type: string
          description: Client Id associated with the provider, if type of credentials is OAuthClientCredentials.
        keyVaultUri:
          type: string
          description: Uri of the key vault.
        keyName:
          type: string
          description: Name of the key vault key.
        keyVersion:
          type: string
          description: Version of the key vault key.
    AuthCredentialsKind:
      type: string
      enum:
        - OAuthClientCredentials
        - ApiKeyAuthCredentials
    InstalledSolutionMap:
      type: object
      description: Mapping of installed solutions.
      properties:
        key:
          type: string
          description: The key representing the installed solution. Required.
        value:
          $ref: "#/components/schemas/Solution"
          description: The installed solution value. Required.
      required:
        - key
        - value
    Solution:
      type: object
      description: Installed data manager for Agriculture solution detail.
      properties:
        applicationName:
          type: string
          description: Application name of the solution.
        partnerId:
          type: string
          description: Partner Id.
        marketPlacePublisherId:
          type: string
          description: Marketplace publisher Id.
        saasSubscriptionId:
          type: string
          description: Saas subscription Id.
        saasSubscriptionName:
          type: string
          description: Saas subscription name.
        planId:
          type: string
          description: Plan Id.
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    Sku:
      type: object
      description: The resource model definition representing SKU.
      properties:
        name:
          type: string
          description: The name of the SKU. Ex - P3. It is typically a letter+number code. Required.
        tier:
          $ref: "#/components/schemas/SkuTier"
          description: 'This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Known values are: "Free", "Basic", "Standard", and "Premium".'
        size:
          type: string
          description: The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code.
        family:
          type: string
          description: If the service has different generations of hardware, for the same SKU, then that can be captured here.
        capacity:
          type: integer
          description: If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted.
      required:
        - name
    SkuTier:
      type: string
      enum:
        - Free
        - Basic
        - Standard
        - Premium
    AgriServiceResourceFlattened:
      type: object
      description: Flattened view of AgriServiceResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU (Stock Keeping Unit) assigned to this resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        config:
          $ref: "#/components/schemas/AgriServiceConfig"
          description: Config of the AgriService instance.
        managedOnBehalfOfConfiguration:
          $ref: "#/components/schemas/ManagedOnBehalfOfConfiguration"
          description: Managed On Behalf Of Configuration.
        dataConnectorCredentials:
          type: array
          items:
            $ref: "#/components/schemas/DataConnectorCredentialMap"
          description: Data connector credentials of AgriService instance.
        installedSolutions:
          type: array
          items:
            $ref: "#/components/schemas/InstalledSolutionMap"
          description: AgriService installed solutions.
    PagedAgriServiceResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AgriServiceResourceFlattened"
        nextLink:
          type: string
    AgriServiceResource:
      type: object
      description: Schema of the AgriService resource from Microsoft.AgriculturePlatform resource provider.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AgriServiceResourceProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU (Stock Keeping Unit) assigned to this resource.
      required:
        - location
    DataManagerForAgricultureSolution:
      type: object
      description: Data Manager for Agriculture solution.
      properties:
        partnerId:
          type: string
          description: Partner Id. Required.
        solutionId:
          type: string
          description: Solution Id. Required.
        partnerTenantId:
          type: string
          description: Partner tenant Id. Required.
        dataAccessScopes:
          type: array
          items:
            type: string
          description: Data access scopes. Required.
        marketPlaceOfferDetails:
          $ref: "#/components/schemas/MarketPlaceOfferDetails"
          description: Marketplace offer details. Required.
        saasApplicationId:
          type: string
          description: Saas application Id. Required.
        accessAzureDataManagerForAgricultureApplicationId:
          type: string
          description: Entra application Id used to access azure data manager for agriculture instance. Required.
        accessAzureDataManagerForAgricultureApplicationName:
          type: string
          description: Entra application name used to access azure data manager for agriculture instance. Required.
        isValidateInput:
          type: boolean
          description: Whether solution inference will validate input. Required.
      required:
        - partnerId
        - solutionId
        - partnerTenantId
        - dataAccessScopes
        - marketPlaceOfferDetails
        - saasApplicationId
        - accessAzureDataManagerForAgricultureApplicationId
        - accessAzureDataManagerForAgricultureApplicationName
        - isValidateInput
    MarketPlaceOfferDetails:
      type: object
      description: Marketplace offer details of Agri solution.
      properties:
        saasOfferId:
          type: string
          description: Saas offer Id. Required.
        publisherId:
          type: string
          description: Publisher Id. Required.
      required:
        - saasOfferId
        - publisherId
    AvailableAgriSolutionListResult:
      type: object
      description: The list of available agri solutions.
      properties:
        solutions:
          type: array
          items:
            $ref: "#/components/schemas/DataManagerForAgricultureSolution"
          description: Agri solutions list. Required.
      required:
        - solutions
    AgriServiceResourceUpdateProperties:
      type: object
      description: The updatable properties of the AgriServiceResource.
      properties:
        config:
          $ref: "#/components/schemas/AgriServiceConfig"
          description: Config of the AgriService instance.
        dataConnectorCredentials:
          type: array
          items:
            $ref: "#/components/schemas/DataConnectorCredentialMap"
          description: Data connector credentials of AgriService instance.
        installedSolutions:
          type: array
          items:
            $ref: "#/components/schemas/InstalledSolutionMap"
          description: AgriService installed solutions.
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    agri_service:
      id: azure_extras.agriculture_platform.agri_service
      name: agri_service
      title: agri_service
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AgriculturePlatform~1agriServices~1{agri_service_resource_name}?api-version=2024-06-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AgriculturePlatform~1agriServices~1{agri_service_resource_name}?api-version=2024-06-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AgriculturePlatform~1agriServices~1{agri_service_resource_name}?api-version=2024-06-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AgriculturePlatform~1agriServices~1{agri_service_resource_name}?api-version=2024-06-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AgriculturePlatform~1agriServices?api-version=2024-06-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AgriculturePlatform~1agriServices?api-version=2024-06-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_available_solutions:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AgriculturePlatform~1agriServices~1{agri_service_resource_name}~1listAvailableSolutions?api-version=2024-06-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/agri_service/methods/get"
          - $ref: "#/components/x-stackQL-resources/agri_service/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/agri_service/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/agri_service/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/agri_service/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/agri_service/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/agri_service/methods/delete"
    operations:
      id: azure_extras.agriculture_platform.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AgriculturePlatform~1operations?api-version=2024-06-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AgriculturePlatform/agriServices/{agri_service_resource_name}?api-version=2024-06-01-preview:
    get:
      operationId: AgriService_get
      description: Get a AgriServiceResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: agri_service_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AgriService resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgriServiceResourceFlattened"
    put:
      operationId: AgriService_begin_create_or_update
      description: Create a AgriServiceResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: agri_service_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AgriService resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AgriServiceResourceProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: The SKU (Stock Keeping Unit) assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgriServiceResource"
    patch:
      operationId: AgriService_begin_update
      description: Update a AgriServiceResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: agri_service_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AgriService resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: The SKU (Stock Keeping Unit) assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/AgriServiceResourceUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgriServiceResource"
    delete:
      operationId: AgriService_begin_delete
      description: Delete a AgriServiceResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: agri_service_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AgriService resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AgriculturePlatform/agriServices?api-version=2024-06-01-preview:
    get:
      operationId: AgriService_list_by_resource_group
      description: List AgriServiceResource resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAgriServiceResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.AgriculturePlatform/agriServices?api-version=2024-06-01-preview:
    get:
      operationId: AgriService_list_by_subscription
      description: List AgriServiceResource resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAgriServiceResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AgriculturePlatform/agriServices/{agri_service_resource_name}/listAvailableSolutions?api-version=2024-06-01-preview:
    post:
      operationId: AgriService_list_available_solutions
      description: Returns the list of available agri solutions.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: agri_service_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AgriService resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailableAgriSolutionListResult"
  /providers/Microsoft.AgriculturePlatform/operations?api-version=2024-06-01-preview:
    get:
      operationId: agricultureplatform_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
