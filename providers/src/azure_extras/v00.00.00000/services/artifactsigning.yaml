openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Artifact Signing Mgmt Client
  description: Code Signing resource provider api.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-05-15-preview-stackql-generated
  x-serviceAlias: artifactsigning
  x-sdkPackage: azure-mgmt-artifactsigning
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    CertificateProfileProperties:
      type: object
      description: Properties of the certificate profile.
      properties:
        profileType:
          $ref: "#/components/schemas/ProfileType"
          description: 'Profile type of the certificate. Required. Known values are: "PublicTrust", "PrivateTrust", "PrivateTrustCIPolicy", "VBSEnclave", and "PublicTrustTest".'
        includeStreetAddress:
          type: boolean
          description: Whether to include STREET in the certificate subject name.
        includeCity:
          type: boolean
          description: Whether to include L in the certificate subject name. Applicable only for private trust, private trust ci profile types.
        includeState:
          type: boolean
          description: Whether to include S in the certificate subject name. Applicable only for private trust, private trust ci profile types.
        includeCountry:
          type: boolean
          description: Whether to include C in the certificate subject name. Applicable only for private trust, private trust ci profile types.
        includePostalCode:
          type: boolean
          description: Whether to include PC in the certificate subject name.
        identityValidationId:
          type: string
          description: Identity validation id used for the certificate subject name. Required.
        programType:
          type: string
          description: Indicates whether the resource is intended for a specific usage scenario.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Status of the current operation on certificate profile. Known values are: "Succeeded", "Failed", "Canceled", "Updating", "Deleting", and "Accepted".'
        status:
          $ref: "#/components/schemas/CertificateProfileStatus"
          description: 'Status of the certificate profile. Known values are: "Active", "Disabled", and "Suspended".'
        certificates:
          type: array
          items:
            $ref: "#/components/schemas/Certificate"
          description: List of renewed certificates.
      required:
        - profileType
        - identityValidationId
    ProfileType:
      type: string
      enum:
        - PublicTrust
        - PrivateTrust
        - PrivateTrustCIPolicy
        - VBSEnclave
        - PublicTrustTest
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Updating
        - Deleting
        - Accepted
    CertificateProfileStatus:
      type: string
      enum:
        - Active
        - Disabled
        - Suspended
    Certificate:
      type: object
      description: Properties of the certificate.
      properties:
        serialNumber:
          type: string
          description: Serial number of the certificate.
        enhancedKeyUsage:
          type: string
          description: Enhanced key usage of the certificate.
        subjectName:
          type: string
          description: Subject name of the certificate.
        thumbprint:
          type: string
          description: Thumbprint of the certificate.
        createdDate:
          type: string
          description: Certificate created date.
        expiryDate:
          type: string
          description: Certificate expiry date.
        status:
          $ref: "#/components/schemas/CertificateStatus"
          description: 'Status of the certificate. Known values are: "Active", "Expired", and "Revoked".'
        revocation:
          $ref: "#/components/schemas/Revocation"
          description: Revocations history of a certificate.
    CertificateStatus:
      type: string
      enum:
        - Active
        - Expired
        - Revoked
    Revocation:
      type: object
      description: Revocation details of the certificate.
      properties:
        requestedAt:
          type: string
          format: date-time
          description: The timestamp when the revocation is requested.
        effectiveAt:
          type: string
          format: date-time
          description: The timestamp when the revocation is effective.
        reason:
          type: string
          description: Reason for revocation.
        remarks:
          type: string
          description: Remarks for the revocation.
        status:
          $ref: "#/components/schemas/RevocationStatus"
          description: 'Status of the revocation. Known values are: "Succeeded", "InProgress", and "Failed".'
        failureReason:
          type: string
          description: Reason for the revocation failure.
    RevocationStatus:
      type: string
      enum:
        - Succeeded
        - InProgress
        - Failed
    CertificateProfileFlattened:
      type: object
      description: Flattened view of CertificateProfile ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        profileType:
          $ref: "#/components/schemas/ProfileType"
          description: 'Profile type of the certificate. Required. Known values are: "PublicTrust", "PrivateTrust", "PrivateTrustCIPolicy", "VBSEnclave", and "PublicTrustTest".'
        includeStreetAddress:
          type: boolean
          description: Whether to include STREET in the certificate subject name.
        includeCity:
          type: boolean
          description: Whether to include L in the certificate subject name. Applicable only for private trust, private trust ci profile types.
        includeState:
          type: boolean
          description: Whether to include S in the certificate subject name. Applicable only for private trust, private trust ci profile types.
        includeCountry:
          type: boolean
          description: Whether to include C in the certificate subject name. Applicable only for private trust, private trust ci profile types.
        includePostalCode:
          type: boolean
          description: Whether to include PC in the certificate subject name.
        identityValidationId:
          type: string
          description: Identity validation id used for the certificate subject name. Required.
        programType:
          type: string
          description: Indicates whether the resource is intended for a specific usage scenario.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Status of the current operation on certificate profile. Known values are: "Succeeded", "Failed", "Canceled", "Updating", "Deleting", and "Accepted".'
        status:
          $ref: "#/components/schemas/CertificateProfileStatus"
          description: 'Status of the certificate profile. Known values are: "Active", "Disabled", and "Suspended".'
        certificates:
          type: array
          items:
            $ref: "#/components/schemas/Certificate"
          description: List of renewed certificates.
    PagedCertificateProfileFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CertificateProfileFlattened"
        nextLink:
          type: string
    CertificateProfile:
      type: object
      description: Certificate profile resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/CertificateProfileProperties"
          description: The resource-specific properties for this resource.
    RevokeCertificate:
      type: object
      description: Defines the certificate revocation properties.
      properties:
        serialNumber:
          type: string
          description: Serial number of the certificate. Required.
        thumbprint:
          type: string
          description: Thumbprint of the certificate. Required.
        effectiveAt:
          type: string
          format: date-time
          description: The timestamp when the revocation is effective. Required.
        reason:
          type: string
          description: Reason for the revocation. Required.
        remarks:
          type: string
          description: Remarks for the revocation.
      required:
        - serialNumber
        - thumbprint
        - effectiveAt
        - reason
    CodeSigningAccountProperties:
      type: object
      description: Properties of the artifact signing account.
      properties:
        accountUri:
          type: string
          description: The URI of the artifact signing account which is used during signing files.
        sku:
          $ref: "#/components/schemas/AccountSku"
          description: SKU of the artifact signing account.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Status of the current operation on artifact signing account. Known values are: "Succeeded", "Failed", "Canceled", "Updating", "Deleting", and "Accepted".'
    AccountSku:
      type: object
      description: SKU of the artifact signing account.
      properties:
        name:
          $ref: "#/components/schemas/SkuName"
          description: 'Name of the SKU. Required. Known values are: "Basic" and "Premium".'
      required:
        - name
    SkuName:
      type: string
      enum:
        - Basic
        - Premium
    CodeSigningAccountFlattened:
      type: object
      description: Flattened view of CodeSigningAccount ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        accountUri:
          type: string
          description: The URI of the artifact signing account which is used during signing files.
        sku:
          $ref: "#/components/schemas/AccountSku"
          description: SKU of the artifact signing account.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Status of the current operation on artifact signing account. Known values are: "Succeeded", "Failed", "Canceled", "Updating", "Deleting", and "Accepted".'
    PagedCodeSigningAccountFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CodeSigningAccountFlattened"
        nextLink:
          type: string
    CodeSigningAccount:
      type: object
      description: Artifact signing account resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/CodeSigningAccountProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    CodeSigningAccountPatchProperties:
      type: object
      description: Properties of the artifact signing account.
      properties:
        sku:
          $ref: "#/components/schemas/AccountSkuPatch"
          description: SKU of the artifact signing account.
    AccountSkuPatch:
      type: object
      description: SKU of the artifact signing account.
      properties:
        name:
          $ref: "#/components/schemas/SkuName"
          description: 'Name of the SKU. Known values are: "Basic" and "Premium".'
    CheckNameAvailabilityResult:
      type: object
      description: The CheckNameAvailability operation response.
      properties:
        nameAvailable:
          type: boolean
          description: A boolean value that indicates whether the name is available for you to use. If true, the name is available. If false, the name has already been taken or is invalid and cannot be used.
        reason:
          $ref: "#/components/schemas/NameUnavailabilityReason"
          description: 'The reason that an artifact signing account name could not be used. The Reason element is only returned if nameAvailable is false. Known values are: "AccountNameInvalid" and "AlreadyExists".'
        message:
          type: string
          description: An error message explaining the Reason value in more detail.
    NameUnavailabilityReason:
      type: string
      enum:
        - AccountNameInvalid
        - AlreadyExists
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    certificate_profiles:
      id: azure_extras.artifactsigning.certificate_profiles
      name: certificate_profiles
      title: certificate_profiles
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}~1certificateProfiles~1{profile_name}?api-version=2026-05-15-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}~1certificateProfiles~1{profile_name}?api-version=2026-05-15-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}~1certificateProfiles~1{profile_name}?api-version=2026-05-15-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_code_signing_account:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}~1certificateProfiles?api-version=2026-05-15-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        revoke_certificates:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}~1certificateProfiles~1{profile_name}~1revokeCertificates?api-version=2026-05-15-preview/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/certificate_profiles/methods/get"
          - $ref: "#/components/x-stackQL-resources/certificate_profiles/methods/list_by_code_signing_account"
        insert:
          - $ref: "#/components/x-stackQL-resources/certificate_profiles/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/certificate_profiles/methods/delete"
    code_signing_accounts:
      id: azure_extras.artifactsigning.code_signing_accounts
      name: code_signing_accounts
      title: code_signing_accounts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}?api-version=2026-05-15-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}?api-version=2026-05-15-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}?api-version=2026-05-15-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts~1{account_name}?api-version=2026-05-15-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts?api-version=2026-05-15-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.CodeSigning~1codeSigningAccounts?api-version=2026-05-15-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.CodeSigning~1checkNameAvailability?api-version=2026-05-15-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/code_signing_accounts/methods/get"
          - $ref: "#/components/x-stackQL-resources/code_signing_accounts/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/code_signing_accounts/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/code_signing_accounts/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/code_signing_accounts/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/code_signing_accounts/methods/delete"
    operations:
      id: azure_extras.artifactsigning.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.CodeSigning~1operations?api-version=2026-05-15-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.CodeSigning/codeSigningAccounts/{account_name}/certificateProfiles/{profile_name}?api-version=2026-05-15-preview:
    get:
      operationId: CertificateProfiles_get
      description: Get details of a certificate profile.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: profile_name
          in: path
          required: true
          schema:
            type: string
          description: Certificate profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CertificateProfileFlattened"
    put:
      operationId: CertificateProfiles_begin_create
      description: Create a certificate profile.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: profile_name
          in: path
          required: true
          schema:
            type: string
          description: Certificate profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/CertificateProfileProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CertificateProfile"
    delete:
      operationId: CertificateProfiles_begin_delete
      description: Delete a certificate profile.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: profile_name
          in: path
          required: true
          schema:
            type: string
          description: Certificate profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.CodeSigning/codeSigningAccounts/{account_name}/certificateProfiles?api-version=2026-05-15-preview:
    get:
      operationId: CertificateProfiles_list_by_code_signing_account
      description: List certificate profiles under an artifact signing account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCertificateProfileFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.CodeSigning/codeSigningAccounts/{account_name}/certificateProfiles/{profile_name}/revokeCertificates?api-version=2026-05-15-preview:
    post:
      operationId: CertificateProfiles_revoke_certificates
      description: Revokes certificates under a certificate profile.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: profile_name
          in: path
          required: true
          schema:
            type: string
          description: Certificate profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                revokeCertificates:
                  type: array
                  items:
                    $ref: "#/components/schemas/RevokeCertificate"
                  description: List of certificates to be revoked in a certificate profile. Required.
              required:
                - revokeCertificates
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.CodeSigning/codeSigningAccounts/{account_name}?api-version=2026-05-15-preview:
    get:
      operationId: CodeSigningAccounts_get
      description: Get an artifact Signing Account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeSigningAccountFlattened"
    put:
      operationId: CodeSigningAccounts_begin_create
      description: Create an artifact Signing Account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/CodeSigningAccountProperties"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeSigningAccount"
    patch:
      operationId: CodeSigningAccounts_begin_update
      description: Update an artifact signing account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/CodeSigningAccountPatchProperties"
                  description: Properties of the artifact signing account.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeSigningAccount"
    delete:
      operationId: CodeSigningAccounts_begin_delete
      description: Delete an artifact signing account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Artifact Signing account name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.CodeSigning/codeSigningAccounts?api-version=2026-05-15-preview:
    get:
      operationId: CodeSigningAccounts_list_by_resource_group
      description: Lists artifact signing accounts within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCodeSigningAccountFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.CodeSigning/codeSigningAccounts?api-version=2026-05-15-preview:
    get:
      operationId: CodeSigningAccounts_list_by_subscription
      description: Lists artifact signing accounts within a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCodeSigningAccountFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.CodeSigning/checkNameAvailability?api-version=2026-05-15-preview:
    post:
      operationId: CodeSigningAccounts_check_name_availability
      description: Checks if the artifact signing account name is valid and is not already in use.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: The type of the resource, "Microsoft.CodeSigning/codeSigningAccounts". Required.
                name:
                  type: string
                  description: Artifact signing account name. Required.
              required:
                - type
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResult"
  /providers/Microsoft.CodeSigning/operations?api-version=2026-05-15-preview:
    get:
      operationId: artifactsigning_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
