openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: App Compliance Automation Tool for Microsoft 365
  description: Automate compliance evidence collection and reporting for Microsoft 365 app certification.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-06-27-stackql-generated
  x-serviceAlias: app_compliance_automation
  x-sdkPackage: azure-mgmt-appcomplianceautomation
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    EvidenceProperties:
      type: object
      description: Evidence's properties. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        evidenceType:
          type: string
          description: 'Evidence type. Known values are: "File", "AutoCollectedEvidence", and "Data".'
        filePath:
          type: string
          description: The path of the file in storage. Required.
        extraData:
          type: string
          description: Extra data considered as evidence.
        controlId:
          type: string
          description: Control id.
        responsibilityId:
          type: string
          description: Responsibility id.
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
      required:
        - filePath
    EvidenceResourceFlattened:
      type: object
      description: Flattened view of EvidenceResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        evidenceType:
          type: string
          description: 'Evidence type. Known values are: "File", "AutoCollectedEvidence", and "Data".'
        filePath:
          type: string
          description: The path of the file in storage. Required.
        extraData:
          type: string
          description: Extra data considered as evidence.
        controlId:
          type: string
          description: Control id.
        responsibilityId:
          type: string
          description: Responsibility id.
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
    PagedEvidenceResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/EvidenceResourceFlattened"
        nextLink:
          type: string
    EvidenceResource:
      type: object
      description: A class represent an AppComplianceAutomation evidence resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/EvidenceProperties"
          description: Evidence property. Required.
      required:
        - properties
    EvidenceFileDownloadResponse:
      type: object
      description: Object that includes all the possible response for the evidence file download operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        evidenceFile:
          $ref: "#/components/schemas/EvidenceFileDownloadResponseEvidenceFile"
          description: The uri of evidence file.
    EvidenceFileDownloadResponseEvidenceFile:
      type: object
      description: The uri of evidence file. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        url:
          type: string
          description: The url of evidence file.
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    StorageInfo:
      type: object
      description: The information of 'bring your own storage' account binding to the report.
      properties:
        subscriptionId:
          type: string
          description: The subscription id which 'bring your own storage' account belongs to.
        resourceGroup:
          type: string
          description: The resourceGroup which 'bring your own storage' account belongs to.
        accountName:
          type: string
          description: "'bring your own storage' account name."
        location:
          type: string
          description: The region of 'bring your own storage' account.
    ListInUseStorageAccountsResponse:
      type: object
      description: Parameters for listing in use storage accounts operation. If subscription list is null, it will check the user's all subscriptions.
      properties:
        storageAccountList:
          type: array
          items:
            $ref: "#/components/schemas/StorageInfo"
          description: The storage account list which in use in related reports.
    GetCollectionCountResponse:
      type: object
      description: The get collection count response.
      properties:
        count:
          type: integer
          description: The count of the specified resource.
    StatusItem:
      type: object
      description: Single status.
      properties:
        statusName:
          type: string
          description: Status name - e.g. "Active", "Failed".
        statusValue:
          type: string
          description: Status value. e.g. "100", or "100%".
    GetOverviewStatusResponse:
      type: object
      description: The get overview status response.
      properties:
        statusList:
          type: array
          items:
            $ref: "#/components/schemas/StatusItem"
          description: List of different status items.
    CheckNameAvailabilityResponse:
      type: object
      description: The check availability result.
      properties:
        nameAvailable:
          type: boolean
          description: Indicates if the resource name is available.
        reason:
          type: string
          description: 'The reason why the given name is not available. Known values are: "Invalid" and "AlreadyExists".'
        message:
          type: string
          description: Detailed reason why the given name is available.
    OnboardResponse:
      type: object
      description: Success. The response indicates given subscriptions has been onboarded.
      properties:
        subscriptionIds:
          type: array
          items:
            type: string
          description: List of subscription ids that are onboarded.
    TriggerEvaluationResponse:
      type: object
      description: Trigger evaluation response.
      properties:
        properties:
          $ref: "#/components/schemas/TriggerEvaluationProperty"
          description: trigger evaluation property.
    TriggerEvaluationProperty:
      type: object
      description: Trigger evaluation response. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        triggerTime:
          type: string
          format: date-time
          description: The time when the evaluation is triggered.
        evaluationEndTime:
          type: string
          format: date-time
          description: The time when the evaluation is end.
        resourceIds:
          type: array
          items:
            type: string
          description: List of resource ids to be evaluated.
        quickAssessments:
          type: array
          items:
            $ref: "#/components/schemas/QuickAssessment"
          description: List of quick assessments.
    QuickAssessment:
      type: object
      description: A class represent the quick assessment. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        resourceId:
          type: string
          description: Resource id.
        responsibilityId:
          type: string
          description: Responsibility id.
        timestamp:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        resourceStatus:
          type: string
          description: 'Quick assessment status. Known values are: "Healthy" and "Unhealthy".'
        displayName:
          type: string
          description: Quick assessment display name.
        description:
          type: string
          description: Quick assessment display name.
        remediationLink:
          type: string
          description: Link to remediation steps for this quick assessment.
    ReportProperties:
      type: object
      description: Create Report's properties. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        triggerTime:
          type: string
          format: date-time
          description: Report collection trigger time. Required.
        timeZone:
          type: string
          description: Report collection trigger time's time zone, the available list can be obtained by executing "Get-TimeZone -ListAvailable" in PowerShell. An example of valid timezone id is "Pacific Standard Time". Required.
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ResourceMetadata"
          description: List of resource data. Required.
        status:
          type: string
          description: 'Report status. Known values are: "Active", "Failed", "Reviewing", and "Disabled".'
        errors:
          type: array
          items:
            type: string
          description: List of report error codes.
        tenantId:
          type: string
          description: Report's tenant id.
        offerGuid:
          type: string
          description: A list of comma-separated offerGuids indicates a series of offerGuids that map to the report. For example, "00000000-0000-0000-0000-000000000001,00000000-0000-0000-0000-000000000002" and "00000000-0000-0000-0000-000000000003".
        nextTriggerTime:
          type: string
          format: date-time
          description: Report next collection trigger time.
        lastTriggerTime:
          type: string
          format: date-time
          description: Report last collection trigger time.
        subscriptions:
          type: array
          items:
            type: string
          description: List of subscription Ids.
        complianceStatus:
          $ref: "#/components/schemas/ReportComplianceStatus"
          description: Report compliance status.
        storageInfo:
          $ref: "#/components/schemas/StorageInfo"
          description: The information of 'bring your own storage' binding to the report.
        certRecords:
          type: array
          items:
            $ref: "#/components/schemas/CertSyncRecord"
          description: List of synchronized certification records.
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
      required:
        - triggerTime
        - timeZone
        - resources
    ResourceMetadata:
      type: object
      description: Single resource Id's metadata. All required parameters must be populated in order to send to server.
      properties:
        resourceId:
          type: string
          description: 'Resource Id - e.g. "/subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/rg1/providers/Microsoft.Compute/virtualMachines/vm1". # pylint: disable=line-too-long Required.'
        resourceType:
          type: string
          description: Resource type. e.g. "Microsoft.Compute/virtualMachines".
        resourceKind:
          type: string
          description: Resource kind.
        resourceOrigin:
          type: string
          description: 'Resource Origin. Known values are: "Azure", "AWS", and "GCP".'
        accountId:
          type: string
          description: Account Id. For example - the AWS account id.
      required:
        - resourceId
    ReportComplianceStatus:
      type: object
      description: A list which includes all the compliance result for one report. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        m365:
          $ref: "#/components/schemas/OverviewStatus"
          description: The Microsoft 365 certification name.
    OverviewStatus:
      type: object
      description: The overview of the compliance result for one report. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        passedCount:
          type: integer
          description: The count of all passed control.
        failedCount:
          type: integer
          description: The count of all failed control.
        manualCount:
          type: integer
          description: The count of all manual control.
        notApplicableCount:
          type: integer
          description: The count of all not applicable control.
        pendingCount:
          type: integer
          description: The count of all pending for approval control.
    CertSyncRecord:
      type: object
      description: A class represent the certification record synchronized from app compliance.
      properties:
        offerGuid:
          type: string
          description: The offerGuid which mapping to the reports.
        certificationStatus:
          type: string
          description: Indicates the status of certification process.
        ingestionStatus:
          type: string
          description: Indicates the status of compliance process.
        controls:
          type: array
          items:
            $ref: "#/components/schemas/ControlSyncRecord"
          description: The control records list to be synchronized.
    ControlSyncRecord:
      type: object
      description: A class represent the control record synchronized from app compliance.
      properties:
        controlId:
          type: string
          description: The Id of the control. e.g. "Operational_Security_10".
        controlStatus:
          type: string
          description: Control status synchronized from app compliance.
    ReportResourceFlattened:
      type: object
      description: Flattened view of ReportResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        triggerTime:
          type: string
          format: date-time
          description: Report collection trigger time. Required.
        timeZone:
          type: string
          description: Report collection trigger time's time zone, the available list can be obtained by executing "Get-TimeZone -ListAvailable" in PowerShell. An example of valid timezone id is "Pacific Standard Time". Required.
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ResourceMetadata"
          description: List of resource data. Required.
        status:
          type: string
          description: 'Report status. Known values are: "Active", "Failed", "Reviewing", and "Disabled".'
        errors:
          type: array
          items:
            type: string
          description: List of report error codes.
        tenantId:
          type: string
          description: Report's tenant id.
        offerGuid:
          type: string
          description: A list of comma-separated offerGuids indicates a series of offerGuids that map to the report. For example, "00000000-0000-0000-0000-000000000001,00000000-0000-0000-0000-000000000002" and "00000000-0000-0000-0000-000000000003".
        nextTriggerTime:
          type: string
          format: date-time
          description: Report next collection trigger time.
        lastTriggerTime:
          type: string
          format: date-time
          description: Report last collection trigger time.
        subscriptions:
          type: array
          items:
            type: string
          description: List of subscription Ids.
        complianceStatus:
          $ref: "#/components/schemas/ReportComplianceStatus"
          description: Report compliance status.
        storageInfo:
          $ref: "#/components/schemas/StorageInfo"
          description: The information of 'bring your own storage' binding to the report.
        certRecords:
          type: array
          items:
            $ref: "#/components/schemas/CertSyncRecord"
          description: List of synchronized certification records.
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
    PagedReportResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ReportResourceFlattened"
        nextLink:
          type: string
    ReportResource:
      type: object
      description: A class represent an AppComplianceAutomation report resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ReportProperties"
          description: Report property. Required.
      required:
        - properties
    ScopingQuestion:
      type: object
      description: The definition of a scoping question. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        questionId:
          type: string
          description: Question id. Required.
        superiorQuestionId:
          type: string
          description: Superior question id.
        inputType:
          type: string
          description: 'Input type of the question answer. Required. Known values are: "None", "Text", "Email", "MultilineText", "Url", "Number", "Boolean", "Telephone", "YesNoNa", "Date", "YearPicker", "SingleSelection", "SingleSelectDropdown", "MultiSelectCheckbox", "MultiSelectDropdown", "MultiSelectDropdownCustom", "Group", and "Upload".'
        optionIds:
          type: array
          items:
            type: string
          description: Option id list. Required.
        rules:
          type: array
          items:
            type: string
          description: The rule of the question. Required.
        showSubQuestionsValue:
          type: string
          description: The answer value to show the sub questions.
    ScopingQuestions:
      type: object
      description: Scoping question list.
      properties:
        questions:
          type: array
          items:
            $ref: "#/components/schemas/ScopingQuestion"
          description: List of scoping questions.
    ReportPatchProperties:
      type: object
      description: Patch Report's properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        triggerTime:
          type: string
          format: date-time
          description: Report collection trigger time.
        timeZone:
          type: string
          description: Report collection trigger time's time zone, the available list can be obtained by executing "Get-TimeZone -ListAvailable" in PowerShell. An example of valid timezone id is "Pacific Standard Time".
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ResourceMetadata"
          description: List of resource data.
        status:
          type: string
          description: 'Report status. Known values are: "Active", "Failed", "Reviewing", and "Disabled".'
        errors:
          type: array
          items:
            type: string
          description: List of report error codes.
        tenantId:
          type: string
          description: Report's tenant id.
        offerGuid:
          type: string
          description: A list of comma-separated offerGuids indicates a series of offerGuids that map to the report. For example, "00000000-0000-0000-0000-000000000001,00000000-0000-0000-0000-000000000002" and "00000000-0000-0000-0000-000000000003".
        nextTriggerTime:
          type: string
          format: date-time
          description: Report next collection trigger time.
        lastTriggerTime:
          type: string
          format: date-time
          description: Report last collection trigger time.
        subscriptions:
          type: array
          items:
            type: string
          description: List of subscription Ids.
        complianceStatus:
          $ref: "#/components/schemas/ReportComplianceStatus"
          description: Report compliance status.
        storageInfo:
          $ref: "#/components/schemas/StorageInfo"
          description: The information of 'bring your own storage' binding to the report.
        certRecords:
          type: array
          items:
            $ref: "#/components/schemas/CertSyncRecord"
          description: List of synchronized certification records.
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
    ReportFixResult:
      type: object
      description: Report fix result. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        result:
          type: string
          description: 'Indicates whether the fix action is Succeeded or Failed. Known values are: "Succeeded" and "Failed".'
        reason:
          type: string
          description: If the report fix action failed, to indicate the detailed failed reason.
    SyncCertRecordResponse:
      type: object
      description: Synchronize certification record response.
      properties:
        certRecord:
          $ref: "#/components/schemas/CertSyncRecord"
          description: certification record synchronized.
    ReportVerificationResult:
      type: object
      description: Report health status verification result. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        result:
          type: string
          description: 'Indicates whether the report verification action is Succeeded or Failed. Known values are: "Succeeded" and "Failed".'
        reason:
          type: string
          description: If the report verification action failed, to indicate the detailed failed reason.
    ScopingConfigurationProperties:
      type: object
      description: ScopingConfiguration's properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        answers:
          type: array
          items:
            $ref: "#/components/schemas/ScopingAnswer"
          description: List of scoping question answers.
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
    ScopingAnswer:
      type: object
      description: Scoping answer. All required parameters must be populated in order to send to server.
      properties:
        questionId:
          type: string
          description: Question id. Required.
        answers:
          type: array
          items:
            type: string
          description: Question answer value list. Required.
      required:
        - questionId
        - answers
    ScopingConfigurationResourceFlattened:
      type: object
      description: Flattened view of ScopingConfigurationResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        answers:
          type: array
          items:
            $ref: "#/components/schemas/ScopingAnswer"
          description: List of scoping question answers.
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
    PagedScopingConfigurationResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ScopingConfigurationResourceFlattened"
        nextLink:
          type: string
    ScopingConfigurationResource:
      type: object
      description: A class represent an AppComplianceAutomation scoping configuration resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ScopingConfigurationProperties"
          description: ScopingConfiguration property. Required.
      required:
        - properties
    SnapshotProperties:
      type: object
      description: Snapshot's properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        snapshotName:
          type: string
          description: Snapshot name.
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
        reportProperties:
          $ref: "#/components/schemas/ReportProperties"
          description: The report essential info.
        reportSystemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        complianceResults:
          type: array
          items:
            $ref: "#/components/schemas/ComplianceResult"
          description: List of compliance results.
    ComplianceResult:
      type: object
      description: A class represent the compliance result. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        complianceName:
          type: string
          description: The name of the compliance. e.g. "M365".
        categories:
          type: array
          items:
            $ref: "#/components/schemas/Category"
          description: List of categories.
    Category:
      type: object
      description: A class represent the compliance category. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        categoryName:
          type: string
          description: The name of the compliance category. e.g. "Operational Security".
        categoryStatus:
          type: string
          description: 'Category status. Known values are: "Passed", "Failed", "NotApplicable", and "PendingApproval".'
        controlFamilies:
          type: array
          items:
            $ref: "#/components/schemas/ControlFamily"
          description: List of control families.
    ControlFamily:
      type: object
      description: A class represent the control family. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        controlFamilyName:
          type: string
          description: The name of the control family. e.g. "Malware Protection - Anti-Virus".
        controlFamilyStatus:
          type: string
          description: 'The control family status. Known values are: "Passed", "Failed", "NotApplicable", and "PendingApproval".'
        controls:
          type: array
          items:
            $ref: "#/components/schemas/Control"
          description: List of controls.
    Control:
      type: object
      description: A class represent the control. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        controlId:
          type: string
          description: The Id of the control. e.g. "Operational_Security_10".
        controlName:
          type: string
          description: The name of the control. e.g. "Unsupported OS and Software.".
        controlFullName:
          type: string
          description: The full name of the control. e.g. "Validate that unsupported operating systems and software components are not in use.".
        controlDescription:
          type: string
          description: The control's description.
        controlDescriptionHyperLink:
          type: string
          description: The hyper link to the control's description'.
        controlStatus:
          type: string
          description: 'Control status. Known values are: "Passed", "Failed", "NotApplicable", and "PendingApproval".'
        responsibilities:
          type: array
          items:
            $ref: "#/components/schemas/Responsibility"
          description: List of customer responsibility.
    Responsibility:
      type: object
      description: A class represent the customer responsibility. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        responsibilityId:
          type: string
          description: The id of the customer responsibility.
        responsibilityTitle:
          type: string
          description: The title of the customer responsibility.
        responsibilityDescription:
          type: string
          description: The description of the customer responsibility.
        responsibilityType:
          type: string
          description: 'The type of customer responsibility. Known values are: "Automated", "ScopedManual", and "Manual".'
        responsibilitySeverity:
          type: string
          description: 'The severity level of this customer responsibility. Known values are: "High", "Medium", and "Low".'
        responsibilityStatus:
          type: string
          description: 'The status of this customer responsibility. Known values are: "Passed", "Failed", "NotApplicable", and "PendingApproval".'
        responsibilityEnvironment:
          type: string
          description: 'The supported cloud environment of this customer responsibility. Known values are: "Azure", "AWS", "GCP", and "General".'
        failedResourceCount:
          type: integer
          description: The count of all failed resources.
        totalResourceCount:
          type: integer
          description: The count of all resources.
        resourceList:
          type: array
          items:
            $ref: "#/components/schemas/ResponsibilityResource"
          description: List of resource.
        recommendationList:
          type: array
          items:
            $ref: "#/components/schemas/Recommendation"
          description: List of recommendation.
        guidance:
          type: string
          description: The evidence upload guidance description.
        justification:
          type: string
          description: The justification given by the user to clarify the reason.
        evidenceFiles:
          type: array
          items:
            type: string
          description: List of evidence file url.
    ResponsibilityResource:
      type: object
      description: A class represent the resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        resourceId:
          type: string
          description: The Id of the resource.
        accountId:
          type: string
          description: Account Id. For example - AWS account Id.
        resourceType:
          type: string
          description: The type of the resource. e.g. "Microsoft.SignalRService/SignalR".
        resourceOrigin:
          type: string
          description: 'Resource origin. Known values are: "Azure", "AWS", and "GCP".'
        resourceStatus:
          type: string
          description: 'Resource status. Known values are: "Healthy" and "Unhealthy".'
        resourceStatusChangeDate:
          type: string
          format: date-time
          description: The status change date for the resource.
        recommendationIds:
          type: array
          items:
            type: string
          description: List of recommendation id.
    Recommendation:
      type: object
      description: A class represent the recommendation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        recommendationId:
          type: string
          description: The Id of the recommendation.
        recommendationShortName:
          type: string
          description: The short name of the recommendation. e.g. "Invalid TLS config".
        recommendationSolutions:
          type: array
          items:
            $ref: "#/components/schemas/RecommendationSolution"
          description: List of recommendation solutions.
    RecommendationSolution:
      type: object
      description: A class represent the recommendation solution. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        recommendationSolutionIndex:
          type: string
          description: The index of the recommendation solution.
        recommendationSolutionContent:
          type: string
          description: The detail steps of the recommendation solution.
        isRecommendSolution:
          type: string
          description: 'Indicates whether this solution is the recommended. Known values are: "true" and "false".'
    SnapshotResourceFlattened:
      type: object
      description: Flattened view of SnapshotResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        snapshotName:
          type: string
          description: Snapshot name.
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        provisioningState:
          type: string
          description: 'Azure lifecycle management. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
        reportProperties:
          $ref: "#/components/schemas/ReportProperties"
          description: The report essential info.
        reportSystemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        complianceResults:
          type: array
          items:
            $ref: "#/components/schemas/ComplianceResult"
          description: List of compliance results.
    PagedSnapshotResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SnapshotResourceFlattened"
        nextLink:
          type: string
    DownloadResponse:
      type: object
      description: Object that includes all the possible response for the download operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        resourceList:
          type: array
          items:
            $ref: "#/components/schemas/ResourceItem"
          description: Resource list of the report.
        complianceReport:
          type: array
          items:
            $ref: "#/components/schemas/ComplianceReportItem"
          description: List of the compliance result.
        compliancePdfReport:
          $ref: "#/components/schemas/DownloadResponseCompliancePdfReport"
          description: Compliance pdf report.
        complianceDetailedPdfReport:
          $ref: "#/components/schemas/DownloadResponseComplianceDetailedPdfReport"
          description: The detailed compliance pdf report.
    ResourceItem:
      type: object
      description: Resource Id. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        subscriptionId:
          type: string
          description: The subscription Id of this resource.
        resourceGroup:
          type: string
          description: The resource group name of this resource.
        resourceType:
          type: string
          description: The resource type of this resource. e.g. "Microsoft.SignalRService/SignalR".
        resourceId:
          type: string
          description: 'The resource Id - e.g. "/subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/rg1/providers/Microsoft.Compute/virtualMachines/vm1". # pylint: disable=line-too-long'
    ComplianceReportItem:
      type: object
      description: Object that includes all the content for single compliance result. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        categoryName:
          type: string
          description: The category name.
        controlFamilyName:
          type: string
          description: The control family name.
        controlId:
          type: string
          description: The control Id - e.g. "1".
        controlName:
          type: string
          description: The control name.
        controlStatus:
          type: string
          description: 'Control status. Known values are: "Passed", "Failed", "NotApplicable", and "PendingApproval".'
        responsibilityTitle:
          type: string
          description: The title of the customer responsibility.
        responsibilityDescription:
          type: string
          description: The description of the customer responsibility.
        resourceId:
          type: string
          description: The Id of the resource.
        resourceType:
          type: string
          description: The type of the resource. e.g. "Microsoft.SignalRService/SignalR".
        resourceOrigin:
          type: string
          description: 'Resource origin. Known values are: "Azure", "AWS", and "GCP".'
        resourceStatus:
          type: string
          description: 'Resource status. Known values are: "Healthy" and "Unhealthy".'
        resourceStatusChangeDate:
          type: string
          format: date-time
          description: The status change date for the resource.
    DownloadResponseCompliancePdfReport:
      type: object
      description: Compliance pdf report. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        sasUri:
          type: string
          description: The uri of compliance pdf report.
    DownloadResponseComplianceDetailedPdfReport:
      type: object
      description: The detailed compliance pdf report. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        sasUri:
          type: string
          description: The uri of detailed compliance pdf report.
    WebhookProperties:
      type: object
      description: Webhook properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        webhookId:
          type: string
          description: Webhook id in database.
        status:
          type: string
          description: 'Webhook status. Known values are: "Enabled" and "Disabled".'
        tenantId:
          type: string
          description: Tenant id.
        sendAllEvents:
          type: string
          description: 'whether to send notification under any event. Known values are: "true" and "false".'
        events:
          type: array
          items:
            type: string
          description: under which event notification should be sent.
        payloadUrl:
          type: string
          description: webhook payload url.
        contentType:
          type: string
          description: content type. "application/json"
        webhookKey:
          type: string
          description: webhook secret token. If not set, this field value is null; otherwise, please set a string value.
        updateWebhookKey:
          type: string
          description: 'whether to update webhookKey. Known values are: "true" and "false".'
        webhookKeyEnabled:
          type: string
          description: 'whether webhookKey is enabled. Known values are: "true" and "false".'
        enableSslVerification:
          type: string
          description: 'whether to enable ssl verification. Known values are: "true" and "false".'
        deliveryStatus:
          type: string
          description: 'webhook deliveryStatus. Known values are: "Succeeded", "Failed", and "NotStarted".'
        provisioningState:
          type: string
          description: 'Azure Resource Provisioning State. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
    WebhookResourceFlattened:
      type: object
      description: Flattened view of WebhookResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        webhookId:
          type: string
          description: Webhook id in database.
        status:
          type: string
          description: 'Webhook status. Known values are: "Enabled" and "Disabled".'
        tenantId:
          type: string
          description: Tenant id.
        sendAllEvents:
          type: string
          description: 'whether to send notification under any event. Known values are: "true" and "false".'
        events:
          type: array
          items:
            type: string
          description: under which event notification should be sent.
        payloadUrl:
          type: string
          description: webhook payload url.
        contentType:
          type: string
          description: content type. "application/json"
        webhookKey:
          type: string
          description: webhook secret token. If not set, this field value is null; otherwise, please set a string value.
        updateWebhookKey:
          type: string
          description: 'whether to update webhookKey. Known values are: "true" and "false".'
        webhookKeyEnabled:
          type: string
          description: 'whether webhookKey is enabled. Known values are: "true" and "false".'
        enableSslVerification:
          type: string
          description: 'whether to enable ssl verification. Known values are: "true" and "false".'
        deliveryStatus:
          type: string
          description: 'webhook deliveryStatus. Known values are: "Succeeded", "Failed", and "NotStarted".'
        provisioningState:
          type: string
          description: 'Azure Resource Provisioning State. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", "Fixing", "Verifying", and "Updating".'
    PagedWebhookResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/WebhookResourceFlattened"
        nextLink:
          type: string
    WebhookResource:
      type: object
      description: A class represent an AppComplianceAutomation webhook resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/WebhookProperties"
          description: Webhook property. Required.
      required:
        - properties
  x-stackQL-resources:
    evidence:
      id: azure_extras.app_compliance_automation.evidence
      name: evidence
      title: evidence
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1evidences~1{evidence_name}?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1evidences~1{evidence_name}?api-version=2024-06-27/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1evidences~1{evidence_name}?api-version=2024-06-27/delete"
          response:
            openAPIDocKey: "204"
        list_by_report:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1evidences?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        download:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1evidences~1{evidence_name}~1download?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/evidence/methods/get"
          - $ref: "#/components/x-stackQL-resources/evidence/methods/list_by_report"
        insert:
          - $ref: "#/components/x-stackQL-resources/evidence/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/evidence/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/evidence/methods/delete"
    operations:
      id: azure_extras.app_compliance_automation.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1operations?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    provider_actions:
      id: azure_extras.app_compliance_automation.provider_actions
      name: provider_actions
      title: provider_actions
      methods:
        list_in_use_storage_accounts:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1listInUseStorageAccounts?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_collection_count:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1getCollectionCount?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_overview_status:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1getOverviewStatus?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        check_name_availability:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1checkNameAvailability?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        onboard:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1onboard?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        trigger_evaluation:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1triggerEvaluation?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/provider_actions/methods/list_in_use_storage_accounts"
        insert: []
        update: []
        delete: []
    report:
      id: azure_extras.app_compliance_automation.report
      name: report
      title: report
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}?api-version=2024-06-27/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}?api-version=2024-06-27/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}?api-version=2024-06-27/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        get_scoping_questions:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1getScopingQuestions?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        nested_resource_check_name_availability:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1checkNameAvailability?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        fix:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1fix?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        sync_cert_record:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1syncCertRecord?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        verify:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1verify?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/report/methods/get"
          - $ref: "#/components/x-stackQL-resources/report/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/report/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/report/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/report/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/report/methods/delete"
    scoping_configuration:
      id: azure_extras.app_compliance_automation.scoping_configuration
      name: scoping_configuration
      title: scoping_configuration
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1scopingConfigurations~1{scoping_configuration_name}?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1scopingConfigurations~1{scoping_configuration_name}?api-version=2024-06-27/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1scopingConfigurations~1{scoping_configuration_name}?api-version=2024-06-27/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1scopingConfigurations?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/scoping_configuration/methods/get"
          - $ref: "#/components/x-stackQL-resources/scoping_configuration/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/scoping_configuration/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/scoping_configuration/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/scoping_configuration/methods/delete"
    snapshot:
      id: azure_extras.app_compliance_automation.snapshot
      name: snapshot
      title: snapshot
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1snapshots~1{snapshot_name}?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1snapshots?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        download:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1snapshots~1{snapshot_name}~1download?api-version=2024-06-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/snapshot/methods/get"
          - $ref: "#/components/x-stackQL-resources/snapshot/methods/list"
        insert: []
        update: []
        delete: []
    webhook:
      id: azure_extras.app_compliance_automation.webhook
      name: webhook
      title: webhook
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1webhooks~1{webhook_name}?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1webhooks~1{webhook_name}?api-version=2024-06-27/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1webhooks~1{webhook_name}?api-version=2024-06-27/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1webhooks~1{webhook_name}?api-version=2024-06-27/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppComplianceAutomation~1reports~1{report_name}~1webhooks?api-version=2024-06-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/webhook/methods/get"
          - $ref: "#/components/x-stackQL-resources/webhook/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/webhook/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/webhook/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/webhook/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/webhook/methods/delete"
paths:
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/evidences/{evidence_name}?api-version=2024-06-27:
    get:
      operationId: Evidence_get
      description: Get the evidence metadata.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: evidence_name
          in: path
          required: true
          schema:
            type: string
          description: The evidence name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceResourceFlattened"
    put:
      operationId: Evidence_create_or_update
      description: Create or Update an evidence a specified report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: evidence_name
          in: path
          required: true
          schema:
            type: string
          description: The evidence name. Required.
        - name: offerGuid
          in: query
          required: false
          schema:
            type: string
          description: The offerGuid which mapping to the reports. Default value is None.
        - name: reportCreatorTenantId
          in: query
          required: false
          schema:
            type: string
          description: The tenant id of the report creator. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/EvidenceProperties"
                  description: Evidence property. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceResource"
    delete:
      operationId: Evidence_delete
      description: Delete an existent evidence from a specified report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: evidence_name
          in: path
          required: true
          schema:
            type: string
          description: The evidence name. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/evidences?api-version=2024-06-27:
    get:
      operationId: Evidence_list_by_report
      description: Returns a paginated list of evidences for a specified report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: Skip over when retrieving results. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: Number of elements to return when retrieving results. Default value is None.
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: OData Select statement. Limits the properties on each entry to just those requested, e.g. ?$select=reportName,id. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. Default value is None.
        - name: $orderby
          in: query
          required: false
          schema:
            type: string
          description: OData order by query option. Default value is None.
        - name: offerGuid
          in: query
          required: false
          schema:
            type: string
          description: The offerGuid which mapping to the reports. Default value is None.
        - name: reportCreatorTenantId
          in: query
          required: false
          schema:
            type: string
          description: The tenant id of the report creator. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedEvidenceResourceFlattened"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/evidences/{evidence_name}/download?api-version=2024-06-27:
    post:
      operationId: Evidence_download
      description: Download evidence file.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: evidence_name
          in: path
          required: true
          schema:
            type: string
          description: The evidence name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reportCreatorTenantId:
                  type: string
                  description: Tenant id.
                offerGuid:
                  type: string
                  description: The offerGuid which mapping to the reports.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceFileDownloadResponse"
  /providers/Microsoft.AppComplianceAutomation/operations?api-version=2024-06-27:
    get:
      operationId: appcomplianceautomation_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /providers/Microsoft.AppComplianceAutomation/listInUseStorageAccounts?api-version=2024-06-27:
    post:
      operationId: ProviderActions_list_in_use_storage_accounts
      description: List the storage accounts which are in use by related reports.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subscriptionIds:
                  type: array
                  items:
                    type: string
                  description: List of subscription ids to be query. If the list is null or empty, the API will query all the subscriptions of the user.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListInUseStorageAccountsResponse"
  /providers/Microsoft.AppComplianceAutomation/getCollectionCount?api-version=2024-06-27:
    post:
      operationId: ProviderActions_get_collection_count
      description: Get the count of reports.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetCollectionCountResponse"
  /providers/Microsoft.AppComplianceAutomation/getOverviewStatus?api-version=2024-06-27:
    post:
      operationId: ProviderActions_get_overview_status
      description: Get the resource overview status.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetOverviewStatusResponse"
  /providers/Microsoft.AppComplianceAutomation/checkNameAvailability?api-version=2024-06-27:
    post:
      operationId: ProviderActions_check_name_availability
      description: Check if the given name is available for a report.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The name of the resource for which availability needs to be checked.
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResponse"
  /providers/Microsoft.AppComplianceAutomation/onboard?api-version=2024-06-27:
    post:
      operationId: ProviderActions_begin_onboard
      description: Onboard given subscriptions to Microsoft.AppComplianceAutomation provider.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subscriptionIds:
                  type: array
                  items:
                    type: string
                  description: List of subscription ids to be onboarded. Required.
              required:
                - subscriptionIds
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardResponse"
  /providers/Microsoft.AppComplianceAutomation/triggerEvaluation?api-version=2024-06-27:
    post:
      operationId: ProviderActions_begin_trigger_evaluation
      description: Trigger quick evaluation for the given subscriptions.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                resourceIds:
                  type: array
                  items:
                    type: string
                  description: List of resource ids to be evaluated. Required.
              required:
                - resourceIds
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerEvaluationResponse"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}?api-version=2024-06-27:
    get:
      operationId: Report_get
      description: Get the AppComplianceAutomation report and its properties.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportResourceFlattened"
    put:
      operationId: Report_begin_create_or_update
      description: Create a new AppComplianceAutomation report or update an exiting AppComplianceAutomation report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ReportProperties"
                  description: Report property. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportResource"
    patch:
      operationId: Report_begin_update
      description: Update an exiting AppComplianceAutomation report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ReportPatchProperties"
                  description: Report property.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportResource"
    delete:
      operationId: Report_begin_delete
      description: Delete an AppComplianceAutomation report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.AppComplianceAutomation/reports?api-version=2024-06-27:
    get:
      operationId: Report_list
      description: Get the AppComplianceAutomation report list for the tenant.
      parameters:
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: Skip over when retrieving results. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: Number of elements to return when retrieving results. Default value is None.
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: OData Select statement. Limits the properties on each entry to just those requested, e.g. ?$select=reportName,id. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. Default value is None.
        - name: $orderby
          in: query
          required: false
          schema:
            type: string
          description: OData order by query option. Default value is None.
        - name: offerGuid
          in: query
          required: false
          schema:
            type: string
          description: The offerGuid which mapping to the reports. Default value is None.
        - name: reportCreatorTenantId
          in: query
          required: false
          schema:
            type: string
          description: The tenant id of the report creator. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedReportResourceFlattened"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/getScopingQuestions?api-version=2024-06-27:
    post:
      operationId: Report_get_scoping_questions
      description: "Fix the AppComplianceAutomation report error. e.g: App Compliance Automation Tool service unregistered, automation removed."
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopingQuestions"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/checkNameAvailability?api-version=2024-06-27:
    post:
      operationId: Report_nested_resource_check_name_availability
      description: "Checks the report's nested resource name availability, e.g: Webhooks, Evidences, Snapshots."
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The name of the resource for which availability needs to be checked.
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResponse"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/fix?api-version=2024-06-27:
    post:
      operationId: Report_begin_fix
      description: "Fix the AppComplianceAutomation report error. e.g: App Compliance Automation Tool service unregistered, automation removed."
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportFixResult"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/syncCertRecord?api-version=2024-06-27:
    post:
      operationId: Report_begin_sync_cert_record
      description: Synchronize attestation record from app compliance.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                certRecord:
                  $ref: "#/components/schemas/CertSyncRecord"
                  description: certification record to be synchronized. Required.
              required:
                - certRecord
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncCertRecordResponse"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/verify?api-version=2024-06-27:
    post:
      operationId: Report_begin_verify
      description: Verify the AppComplianceAutomation report health status.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportVerificationResult"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/scopingConfigurations/{scoping_configuration_name}?api-version=2024-06-27:
    get:
      operationId: ScopingConfiguration_get
      description: Get the AppComplianceAutomation scoping configuration of the specific report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: scoping_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The scoping configuration of the specific report. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopingConfigurationResourceFlattened"
    put:
      operationId: ScopingConfiguration_create_or_update
      description: Get the AppComplianceAutomation scoping configuration of the specific report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: scoping_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The scoping configuration of the specific report. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ScopingConfigurationProperties"
                  description: ScopingConfiguration property. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScopingConfigurationResource"
    delete:
      operationId: ScopingConfiguration_delete
      description: Clean the AppComplianceAutomation scoping configuration of the specific report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: scoping_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The scoping configuration of the specific report. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/scopingConfigurations?api-version=2024-06-27:
    get:
      operationId: ScopingConfiguration_list
      description: Returns a list format of the singleton scopingConfiguration for a specified report.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedScopingConfigurationResourceFlattened"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/snapshots/{snapshot_name}?api-version=2024-06-27:
    get:
      operationId: Snapshot_get
      description: Get the AppComplianceAutomation snapshot and its properties.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: snapshot_name
          in: path
          required: true
          schema:
            type: string
          description: Snapshot Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SnapshotResourceFlattened"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/snapshots?api-version=2024-06-27:
    get:
      operationId: Snapshot_list
      description: Get the AppComplianceAutomation snapshot list.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: Skip over when retrieving results. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: Number of elements to return when retrieving results. Default value is None.
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: OData Select statement. Limits the properties on each entry to just those requested, e.g. ?$select=reportName,id. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. Default value is None.
        - name: $orderby
          in: query
          required: false
          schema:
            type: string
          description: OData order by query option. Default value is None.
        - name: offerGuid
          in: query
          required: false
          schema:
            type: string
          description: The offerGuid which mapping to the reports. Default value is None.
        - name: reportCreatorTenantId
          in: query
          required: false
          schema:
            type: string
          description: The tenant id of the report creator. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSnapshotResourceFlattened"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/snapshots/{snapshot_name}/download?api-version=2024-06-27:
    post:
      operationId: Snapshot_begin_download
      description: "Download compliance needs from snapshot, like: Compliance Report, Resource List."
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: snapshot_name
          in: path
          required: true
          schema:
            type: string
          description: Snapshot Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reportCreatorTenantId:
                  type: string
                  description: Tenant id.
                downloadType:
                  type: string
                  description: 'Indicates the download type. Required. Known values are: "ComplianceReport", "CompliancePdfReport", "ComplianceDetailedPdfReport", and "ResourceList".'
                offerGuid:
                  type: string
                  description: The offerGuid which mapping to the reports.
              required:
                - downloadType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DownloadResponse"
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/webhooks/{webhook_name}?api-version=2024-06-27:
    get:
      operationId: Webhook_get
      description: Get the AppComplianceAutomation webhook and its properties.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: webhook_name
          in: path
          required: true
          schema:
            type: string
          description: Webhook Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookResourceFlattened"
    put:
      operationId: Webhook_create_or_update
      description: Create a new AppComplianceAutomation webhook or update an exiting AppComplianceAutomation webhook.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: webhook_name
          in: path
          required: true
          schema:
            type: string
          description: Webhook Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/WebhookProperties"
                  description: Webhook property. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookResource"
    patch:
      operationId: Webhook_update
      description: Update an exiting AppComplianceAutomation webhook.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: webhook_name
          in: path
          required: true
          schema:
            type: string
          description: Webhook Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/WebhookProperties"
                  description: Webhook property.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookResource"
    delete:
      operationId: Webhook_delete
      description: Delete an AppComplianceAutomation webhook.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: webhook_name
          in: path
          required: true
          schema:
            type: string
          description: Webhook Name. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.AppComplianceAutomation/reports/{report_name}/webhooks?api-version=2024-06-27:
    get:
      operationId: Webhook_list
      description: Get the AppComplianceAutomation webhook list.
      parameters:
        - name: report_name
          in: path
          required: true
          schema:
            type: string
          description: Report Name. Required.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: Skip over when retrieving results. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: Number of elements to return when retrieving results. Default value is None.
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: OData Select statement. Limits the properties on each entry to just those requested, e.g. ?$select=reportName,id. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. Default value is None.
        - name: $orderby
          in: query
          required: false
          schema:
            type: string
          description: OData order by query option. Default value is None.
        - name: offerGuid
          in: query
          required: false
          schema:
            type: string
          description: The offerGuid which mapping to the reports. Default value is None.
        - name: reportCreatorTenantId
          in: query
          required: false
          schema:
            type: string
          description: The tenant id of the report creator. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedWebhookResourceFlattened"
