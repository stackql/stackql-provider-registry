openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Education Management Client
  description: Education client provides access to education resources for Azure subscriptions.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-12-01-preview-stackql-generated
  x-serviceAlias: education
  x-sdkPackage: azure-mgmt-education
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    GrantDetailProperties:
      type: object
      description: Grant detail properties.
      properties:
        offerCap:
          $ref: "#/components/schemas/Amount"
          description: Offer Cap.
        effectiveDate:
          type: string
          format: date-time
          description: Grant Effective Date.
        offerType:
          $ref: "#/components/schemas/GrantType"
          description: 'Grant Offer Type. Known values are: "Student" and "Academic".'
        expirationDate:
          type: string
          format: date-time
          description: Expiration Date.
        status:
          $ref: "#/components/schemas/GrantStatus"
          description: 'Grant status. Known values are: "Active" and "Inactive".'
        allocatedBudget:
          $ref: "#/components/schemas/Amount"
          description: allocated budget.
    Amount:
      type: object
      description: The amount.
      properties:
        currency:
          type: string
          description: The type of currency being used for the value.
        value:
          type: number
          description: Amount value.
    GrantType:
      type: string
      enum:
        - Student
        - Academic
    GrantStatus:
      type: string
      enum:
        - Active
        - Inactive
    GrantDetailsFlattened:
      type: object
      description: Flattened view of GrantDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        offerCap:
          $ref: "#/components/schemas/Amount"
          description: Offer Cap.
        effectiveDate:
          type: string
          format: date-time
          description: Grant Effective Date.
        offerType:
          $ref: "#/components/schemas/GrantType"
          description: 'Grant Offer Type. Known values are: "Student" and "Academic".'
        expirationDate:
          type: string
          format: date-time
          description: Expiration Date.
        status:
          $ref: "#/components/schemas/GrantStatus"
          description: 'Grant status. Known values are: "Active" and "Inactive".'
        allocatedBudget:
          $ref: "#/components/schemas/Amount"
          description: allocated budget.
    PagedGrantDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/GrantDetailsFlattened"
        nextLink:
          type: string
    JoinRequestProperties:
      type: object
      description: Join request properties.
      properties:
        firstName:
          type: string
          description: First Name.
        lastName:
          type: string
          description: Last Name.
        email:
          type: string
          description: join request email.
        status:
          $ref: "#/components/schemas/JoinRequestStatus"
          description: 'Join request status. Known values are: "Pending" and "Denied".'
    JoinRequestStatus:
      type: string
      enum:
        - Pending
        - Denied
    JoinRequestDetailsFlattened:
      type: object
      description: Flattened view of JoinRequestDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        firstName:
          type: string
          description: First Name.
        lastName:
          type: string
          description: Last Name.
        email:
          type: string
          description: join request email.
        status:
          $ref: "#/components/schemas/JoinRequestStatus"
          description: 'Join request status. Known values are: "Pending" and "Denied".'
    PagedJoinRequestDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/JoinRequestDetailsFlattened"
        nextLink:
          type: string
    LabProperties:
      type: object
      description: Lab detail result properties.
      properties:
        displayName:
          type: string
          description: Lab Display Name. Required.
        budgetPerStudent:
          $ref: "#/components/schemas/Amount"
          description: Default monetary cap for each student in this lab. Required.
        description:
          type: string
          description: Detail description of this lab. Required.
        expirationDate:
          type: string
          format: date-time
          description: Default expiration date for each student in this lab. Required.
        effectiveDate:
          type: string
          format: date-time
          description: Lab creation date.
        status:
          $ref: "#/components/schemas/LabStatus"
          description: 'The status of this lab. Known values are: "Active", "Deleted", and "Pending".'
        maxStudentCount:
          type: number
          description: the total number of students that can be accepted to the lab.
        invitationCode:
          type: string
          description: invitation code for redeemable lab.
        totalBudget:
          $ref: "#/components/schemas/Amount"
          description: Total budget.
        totalAllocatedBudget:
          $ref: "#/components/schemas/Amount"
          description: Total allocated budget.
      required:
        - displayName
        - budgetPerStudent
        - description
        - expirationDate
    LabStatus:
      type: string
      enum:
        - Active
        - Deleted
        - Pending
    LabDetailsFlattened:
      type: object
      description: Flattened view of LabDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        displayName:
          type: string
          description: Lab Display Name. Required.
        budgetPerStudent:
          $ref: "#/components/schemas/Amount"
          description: Default monetary cap for each student in this lab. Required.
        description:
          type: string
          description: Detail description of this lab. Required.
        expirationDate:
          type: string
          format: date-time
          description: Default expiration date for each student in this lab. Required.
        effectiveDate:
          type: string
          format: date-time
          description: Lab creation date.
        status:
          $ref: "#/components/schemas/LabStatus"
          description: 'The status of this lab. Known values are: "Active", "Deleted", and "Pending".'
        maxStudentCount:
          type: number
          description: the total number of students that can be accepted to the lab.
        invitationCode:
          type: string
          description: invitation code for redeemable lab.
        totalBudget:
          $ref: "#/components/schemas/Amount"
          description: Total budget.
        totalAllocatedBudget:
          $ref: "#/components/schemas/Amount"
          description: Total allocated budget.
    PagedLabDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/LabDetailsFlattened"
        nextLink:
          type: string
    LabDetails:
      type: object
      description: Lab details.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/LabProperties"
          description: Get lab response properties.
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    OperationListResult:
      type: object
      description: A list of REST API operations supported by an Azure Resource Provider. It contains an URL link to get the next set of results.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
          description: The Operation items on this page. Required.
        nextLink:
          type: string
          description: The link to the next page of items.
    StudentLabProperties:
      type: object
      description: Student lab detail properties.
      properties:
        displayName:
          type: string
          description: Student lab Display Name.
        description:
          type: string
          description: Detail description of this lab.
        expirationDate:
          type: string
          format: date-time
          description: Date the lab will expire and by default will be the expiration date for each student in this lab.
        role:
          $ref: "#/components/schemas/StudentRole"
          description: 'Student Role. Known values are: "Student" and "Admin".'
        budget:
          $ref: "#/components/schemas/Amount"
          description: Student Budget.
        subscriptionId:
          type: string
          description: Subscription Id.
        status:
          $ref: "#/components/schemas/StudentLabStatus"
          description: 'Student Lab Status. Known values are: "Active", "Disabled", "Expired", "Pending", and "Deleted".'
        effectiveDate:
          type: string
          format: date-time
          description: User Added Date.
        labScope:
          type: string
          description: Lab Scope. /providers/Microsoft.Billing/billingAccounts/{billingAccountName}/billingProfiles/{billingProfileName}/invoiceSections/{invoiceSectionName}/providers/Microsoft.Education/labs/default.
    StudentRole:
      type: string
      enum:
        - Student
        - Admin
    StudentLabStatus:
      type: string
      enum:
        - Active
        - Disabled
        - Expired
        - Pending
        - Deleted
    StudentLabDetailsFlattened:
      type: object
      description: Flattened view of StudentLabDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        displayName:
          type: string
          description: Student lab Display Name.
        description:
          type: string
          description: Detail description of this lab.
        expirationDate:
          type: string
          format: date-time
          description: Date the lab will expire and by default will be the expiration date for each student in this lab.
        role:
          $ref: "#/components/schemas/StudentRole"
          description: 'Student Role. Known values are: "Student" and "Admin".'
        budget:
          $ref: "#/components/schemas/Amount"
          description: Student Budget.
        subscriptionId:
          type: string
          description: Subscription Id.
        status:
          $ref: "#/components/schemas/StudentLabStatus"
          description: 'Student Lab Status. Known values are: "Active", "Disabled", "Expired", "Pending", and "Deleted".'
        effectiveDate:
          type: string
          format: date-time
          description: User Added Date.
        labScope:
          type: string
          description: Lab Scope. /providers/Microsoft.Billing/billingAccounts/{billingAccountName}/billingProfiles/{billingProfileName}/invoiceSections/{invoiceSectionName}/providers/Microsoft.Education/labs/default.
    PagedStudentLabDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StudentLabDetailsFlattened"
        nextLink:
          type: string
    StudentProperties:
      type: object
      description: Student detail properties.
      properties:
        firstName:
          type: string
          description: First Name. Required.
        lastName:
          type: string
          description: Last Name. Required.
        email:
          type: string
          description: Student Email. Required.
        role:
          $ref: "#/components/schemas/StudentRole"
          description: 'Student Role. Required. Known values are: "Student" and "Admin".'
        budget:
          $ref: "#/components/schemas/Amount"
          description: Student Budget. Required.
        subscriptionId:
          type: string
          description: Subscription Id.
        expirationDate:
          type: string
          format: date-time
          description: Date this student is set to expire from the lab. Required.
        status:
          $ref: "#/components/schemas/StudentLabStatus"
          description: 'Student Lab Status. Known values are: "Active", "Disabled", "Expired", "Pending", and "Deleted".'
        effectiveDate:
          type: string
          format: date-time
          description: Date student was added to the lab.
        subscriptionAlias:
          type: string
          description: Subscription alias.
        subscriptionInviteLastSentDate:
          type: string
          format: date-time
          description: subscription invite last sent date.
      required:
        - firstName
        - lastName
        - email
        - role
        - budget
        - expirationDate
    StudentDetailsFlattened:
      type: object
      description: Flattened view of StudentDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        firstName:
          type: string
          description: First Name. Required.
        lastName:
          type: string
          description: Last Name. Required.
        email:
          type: string
          description: Student Email. Required.
        role:
          $ref: "#/components/schemas/StudentRole"
          description: 'Student Role. Required. Known values are: "Student" and "Admin".'
        budget:
          $ref: "#/components/schemas/Amount"
          description: Student Budget. Required.
        subscriptionId:
          type: string
          description: Subscription Id.
        expirationDate:
          type: string
          format: date-time
          description: Date this student is set to expire from the lab. Required.
        status:
          $ref: "#/components/schemas/StudentLabStatus"
          description: 'Student Lab Status. Known values are: "Active", "Disabled", "Expired", "Pending", and "Deleted".'
        effectiveDate:
          type: string
          format: date-time
          description: Date student was added to the lab.
        subscriptionAlias:
          type: string
          description: Subscription alias.
        subscriptionInviteLastSentDate:
          type: string
          format: date-time
          description: subscription invite last sent date.
    PagedStudentDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StudentDetailsFlattened"
        nextLink:
          type: string
    StudentDetails:
      type: object
      description: Student details.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/StudentProperties"
          description: Get student response properties.
  x-stackQL-resources:
    grants:
      id: azure_extras.education.grants
      name: grants
      title: grants
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1providers~1Microsoft.Education~1grants~1default?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
        list_all:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Education~1grants?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_raw:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1providers~1Microsoft.Education~1grants?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/grants/methods/get"
          - $ref: "#/components/x-stackQL-resources/grants/methods/list_all"
        insert: []
        update: []
        delete: []
    join_requests:
      id: azure_extras.education.join_requests
      name: join_requests
      title: join_requests
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1joinRequests~1{join_request_name}?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1joinRequests?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        approve:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1joinRequests~1{join_request_name}~1approve?api-version=2021-12-01-preview/post"
          response:
            openAPIDocKey: "200"
        deny:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1joinRequests~1{join_request_name}~1deny?api-version=2021-12-01-preview/post"
          response:
            openAPIDocKey: "200"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/join_requests/methods/get"
          - $ref: "#/components/x-stackQL-resources/join_requests/methods/list"
        insert: []
        update: []
        delete: []
    labs:
      id: azure_extras.education.labs
      name: labs
      title: labs
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
        create_or_update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default?api-version=2021-12-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default?api-version=2021-12-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_all:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1providers~1Microsoft.Education~1labs?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        generate_invite_code:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1generateInviteCode?api-version=2021-12-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_raw:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/labs/methods/get"
          - $ref: "#/components/x-stackQL-resources/labs/methods/list_all"
        insert:
          - $ref: "#/components/x-stackQL-resources/labs/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/labs/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/labs/methods/delete"
    operations:
      id: azure_extras.education.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Education~1operations?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    student_labs:
      id: azure_extras.education.student_labs
      name: student_labs
      title: student_labs
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Education~1studentLabs~1{student_lab_name}?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_all:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Education~1studentLabs?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/student_labs/methods/get"
          - $ref: "#/components/x-stackQL-resources/student_labs/methods/list_all"
        insert: []
        update: []
        delete: []
    students:
      id: azure_extras.education.students
      name: students
      title: students
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1students~1{student_alias}?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1students~1{student_alias}?api-version=2021-12-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1students~1{student_alias}?api-version=2021-12-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_name}~1billingProfiles~1{billing_profile_name}~1invoiceSections~1{invoice_section_name}~1providers~1Microsoft.Education~1labs~1default~1students?api-version=2021-12-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/students/methods/get"
          - $ref: "#/components/x-stackQL-resources/students/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/students/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/students/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/students/methods/delete"
    redeem_invitation_codes:
      id: azure_extras.education.redeem_invitation_codes
      name: redeem_invitation_codes
      title: redeem_invitation_codes
      methods:
        redeem_invitation_code:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Education~1redeemInvitationCode?api-version=2021-12-01-preview/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
paths:
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/providers/Microsoft.Education/grants/default?api-version=2021-12-01-preview:
    get:
      operationId: Grants_get
      description: Get details for a specific grant linked to the provided billing account and billing profile.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: includeAllocatedBudget
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to include information about budget that has been allocated. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrantDetailsFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/providers/Microsoft.Education/grants?api-version=2021-12-01-preview:
    get:
      operationId: Grants_list
      description: Get details for a specific grant linked to the provided billing account and billing profile.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: includeAllocatedBudget
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to include information about budget that has been allocated. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGrantDetailsFlattened"
  /providers/Microsoft.Education/grants?api-version=2021-12-01-preview:
    get:
      operationId: Grants_list_all
      description: Get a list of grants that Microsoft has provided.
      parameters:
        - name: includeAllocatedBudget
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to include information about budget that has been allocated. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGrantDetailsFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default/joinRequests/{join_request_name}?api-version=2021-12-01-preview:
    get:
      operationId: JoinRequests_get
      description: get student join requests.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: join_request_name
          in: path
          required: true
          schema:
            type: string
          description: The ID that uniquely identifies a join request. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JoinRequestDetailsFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default/joinRequests?api-version=2021-12-01-preview:
    get:
      operationId: JoinRequests_list
      description: get student join requests.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: includeDenied
          in: query
          required: false
          schema:
            type: boolean
          description: Include denied. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedJoinRequestDetailsFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default/joinRequests/{join_request_name}/approve?api-version=2021-12-01-preview:
    post:
      operationId: JoinRequests_approve
      description: Approve student joining the redeemable lab.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: join_request_name
          in: path
          required: true
          schema:
            type: string
          description: The ID that uniquely identifies a join request. Required.
      responses:
        "200":
          description: Success
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default/joinRequests/{join_request_name}/deny?api-version=2021-12-01-preview:
    post:
      operationId: JoinRequests_deny
      description: Deny student joining the redeemable lab.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: join_request_name
          in: path
          required: true
          schema:
            type: string
          description: The ID that uniquely identifies a join request. Required.
      responses:
        "200":
          description: Success
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default?api-version=2021-12-01-preview:
    get:
      operationId: Labs_get
      description: Get the details for a specific lab associated with the provided billing account name, billing profile name, and invoice section name.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: includeBudget
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to include budget information. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LabDetailsFlattened"
    put:
      operationId: Labs_create_or_update
      description: Create a new lab or update a previously created lab.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/LabProperties"
                  description: Get lab response properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LabDetails"
    delete:
      operationId: Labs_delete
      description: Delete a specific lab associated with the provided billing account name, billing profile name, and invoice section name. Note all students must be removed from the lab in order to delete the lab.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs?api-version=2021-12-01-preview:
    get:
      operationId: Labs_list
      description: Get the details for a specific lab associated with the provided billing account name, billing profile name, and invoice section name.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: includeBudget
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to include budget information. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLabDetailsFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/providers/Microsoft.Education/labs?api-version=2021-12-01-preview:
    get:
      operationId: Labs_list_all
      description: Get a list of labs associated with the provided billing account name and billing profile name.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: includeBudget
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to include budget information. Default value is None.
        - name: includeDeleted
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to show deleted items. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLabDetailsFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default/generateInviteCode?api-version=2021-12-01-preview:
    post:
      operationId: Labs_generate_invite_code
      description: Generate invite code for a lab.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: onlyUpdateStudentCountParameter
          in: query
          required: false
          schema:
            type: boolean
          description: set this flag to true if you want to update student count without generating a new invite code. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                maxStudentCount:
                  type: number
                  description: the total number of students that can be accepted to the lab.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LabDetails"
  /providers/Microsoft.Education/operations?api-version=2021-12-01-preview:
    get:
      operationId: education_list
      description: Lists all of the available Microsoft.Education API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationListResult"
  /providers/Microsoft.Education/studentLabs/{student_lab_name}?api-version=2021-12-01-preview:
    get:
      operationId: StudentLabs_get
      description: Get the details for a specified lab associated with the student lab.
      parameters:
        - name: student_lab_name
          in: path
          required: true
          schema:
            type: string
          description: The ID that uniquely identifies a student lab. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentLabDetailsFlattened"
  /providers/Microsoft.Education/studentLabs?api-version=2021-12-01-preview:
    get:
      operationId: StudentLabs_list_all
      description: Get a list of all labs associated with the caller of the API.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStudentLabDetailsFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default/students/{student_alias}?api-version=2021-12-01-preview:
    get:
      operationId: Students_get
      description: Get the details for a specific student in the specified lab by student alias.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: student_alias
          in: path
          required: true
          schema:
            type: string
          description: The student alias. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentDetailsFlattened"
    put:
      operationId: Students_create_or_update
      description: Create and add a new student to the specified lab or update the details of an existing student in a lab. Note the student must have a valid tenant to accept the lab after they have been added to lab.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: student_alias
          in: path
          required: true
          schema:
            type: string
          description: The student alias. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/StudentProperties"
                  description: Get student response properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentDetails"
    delete:
      operationId: Students_delete
      description: Delete the specified student based on the student alias.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: student_alias
          in: path
          required: true
          schema:
            type: string
          description: The student alias. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Billing/billingAccounts/{billing_account_name}/billingProfiles/{billing_profile_name}/invoiceSections/{invoice_section_name}/providers/Microsoft.Education/labs/default/students?api-version=2021-12-01-preview:
    get:
      operationId: Students_list
      description: Get a list of details about students that are associated with the specified lab.
      parameters:
        - name: billing_account_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing account. Required.
        - name: billing_profile_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the billing profile. Required.
        - name: invoice_section_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the invoice section. Required.
        - name: includeDeleted
          in: query
          required: false
          schema:
            type: boolean
          description: May be used to show deleted items. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStudentDetailsFlattened"
  /providers/Microsoft.Education/redeemInvitationCode?api-version=2021-12-01-preview:
    post:
      operationId: _EducationManagementClient_redeem_invitation_code
      description: Redeem invite code to join a redeemable lab.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                redeemCode:
                  type: string
                  description: redeem code. Required.
                firstName:
                  type: string
                  description: first name of requester. Required.
                lastName:
                  type: string
                  description: last name of requester. Required.
              required:
                - redeemCode
                - firstName
                - lastName
      responses:
        "200":
          description: Success
