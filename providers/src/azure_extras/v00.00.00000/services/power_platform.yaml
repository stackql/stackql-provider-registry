openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Power Platform
  description: Enterprise policies and private endpoint wiring for Power Platform environments.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2020-10-30-preview-stackql-generated
  x-serviceAlias: power_platform
  x-sdkPackage: azure-mgmt-powerplatform
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    AccountProperties:
      type: object
      description: The properties that define configuration for the account.
      properties:
        systemId:
          type: string
          description: The internally assigned unique identifier of the resource.
        description:
          type: string
          description: The description of the account.
    AccountFlattened:
      type: object
      description: Flattened view of Account ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        systemId:
          type: string
          description: The internally assigned unique identifier of the resource.
        description:
          type: string
          description: The description of the account.
    PagedAccountFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AccountFlattened"
        nextLink:
          type: string
    Account:
      type: object
      description: Definition of the account.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AccountProperties"
          description: The properties that define configuration for the account.
      required:
        - location
    Properties:
      type: object
      description: The properties that define configuration for the enterprise policy.
      properties:
        systemId:
          type: string
          description: The internally assigned unique identifier of the resource.
        lockbox:
          $ref: "#/components/schemas/PropertiesLockbox"
          description: Settings concerning lockbox.
        encryption:
          $ref: "#/components/schemas/PropertiesEncryption"
          description: The encryption settings for a configuration store.
        networkInjection:
          $ref: "#/components/schemas/PropertiesNetworkInjection"
          description: Settings concerning network injection.
        healthStatus:
          $ref: "#/components/schemas/HealthStatus"
          description: 'The health status of the resource. Known values are: "Undetermined", "Healthy", "Warning", and "Unhealthy".'
    PropertiesLockbox:
      type: object
      description: Settings concerning lockbox.
      properties:
        state:
          $ref: "#/components/schemas/State"
          description: 'lockbox configuration. Known values are: "Enabled", "Disabled", and "NotConfigured".'
    State:
      type: string
      enum:
        - Enabled
        - Disabled
        - NotConfigured
    PropertiesEncryption:
      type: object
      description: The encryption settings for a configuration store.
      properties:
        keyVault:
          $ref: "#/components/schemas/KeyVaultProperties"
          description: Key vault properties.
        state:
          $ref: "#/components/schemas/State"
          description: 'The state of onboarding, which only appears in the response. Known values are: "Enabled", "Disabled", and "NotConfigured".'
    KeyVaultProperties:
      type: object
      description: Settings concerning key vault encryption for a configuration store.
      properties:
        id:
          type: string
          description: Uri of KeyVault.
        key:
          $ref: "#/components/schemas/KeyProperties"
          description: Identity of the secret that includes name and version.
    KeyProperties:
      type: object
      description: Url and version of the KeyVault Secret.
      properties:
        name:
          type: string
          description: The identifier of the key vault key used to encrypt data.
        version:
          type: string
          description: The version of the identity which will be used to access key vault.
    PropertiesNetworkInjection:
      type: object
      description: Settings concerning network injection.
      properties:
        virtualNetworks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNetworkProperties"
          description: Network injection configuration.
    VirtualNetworkProperties:
      type: object
      description: Settings concerning the virtual network.
      properties:
        id:
          type: string
          description: Uri of the virtual network.
        subnet:
          $ref: "#/components/schemas/SubnetProperties"
          description: Properties of a subnet.
    SubnetProperties:
      type: object
      description: Properties of a subnet.
      properties:
        name:
          type: string
          description: Subnet name.
    HealthStatus:
      type: string
      enum:
        - Undetermined
        - Healthy
        - Warning
        - Unhealthy
    EnterprisePolicyIdentity:
      type: object
      description: The identity of the EnterprisePolicy.
      properties:
        systemAssignedIdentityPrincipalId:
          type: string
          description: The principal id of EnterprisePolicy identity.
        tenantId:
          type: string
          description: The tenant id associated with the EnterprisePolicy.
        type:
          $ref: "#/components/schemas/ResourceIdentityType"
          description: "The type of identity used for the EnterprisePolicy. Currently, the only supported type is 'SystemAssigned', which implicitly creates an identity. Known values are: \"SystemAssigned\" and \"None\"."
    ResourceIdentityType:
      type: string
      enum:
        - SystemAssigned
        - None
    EnterprisePolicyKind:
      type: string
      enum:
        - Lockbox
        - PrivateEndpoint
        - Encryption
        - NetworkInjection
        - Identity
    EnterprisePolicyFlattened:
      type: object
      description: Flattened view of EnterprisePolicy ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/EnterprisePolicyIdentity"
          description: The identity of the EnterprisePolicy.
        kind:
          $ref: "#/components/schemas/EnterprisePolicyKind"
          description: 'The kind (type) of Enterprise Policy. Required. Known values are: "Lockbox", "PrivateEndpoint", "Encryption", "NetworkInjection", and "Identity".'
        systemId:
          type: string
          description: The internally assigned unique identifier of the resource.
        lockbox:
          $ref: "#/components/schemas/PropertiesLockbox"
          description: Settings concerning lockbox.
        encryption:
          $ref: "#/components/schemas/PropertiesEncryption"
          description: The encryption settings for a configuration store.
        networkInjection:
          $ref: "#/components/schemas/PropertiesNetworkInjection"
          description: Settings concerning network injection.
        healthStatus:
          $ref: "#/components/schemas/HealthStatus"
          description: 'The health status of the resource. Known values are: "Undetermined", "Healthy", "Warning", and "Unhealthy".'
    PagedEnterprisePolicyFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/EnterprisePolicyFlattened"
        nextLink:
          type: string
    EnterprisePolicy:
      type: object
      description: Definition of the EnterprisePolicy.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/Properties"
          description: The properties that define configuration for the enterprise policy.
        identity:
          $ref: "#/components/schemas/EnterprisePolicyIdentity"
          description: The identity of the EnterprisePolicy.
        kind:
          $ref: "#/components/schemas/EnterprisePolicyKind"
          description: 'The kind (type) of Enterprise Policy. Required. Known values are: "Lockbox", "PrivateEndpoint", "Encryption", "NetworkInjection", and "Identity".'
      required:
        - location
        - kind
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    PrivateEndpointConnectionProperties:
      type: object
      description: Properties of the private endpoint connection.
      properties:
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider. Required.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
      required:
        - privateLinkServiceConnectionState
    PrivateEndpoint:
      type: object
      description: The private endpoint resource.
      properties:
        id:
          type: string
          description: The resource identifier of the private endpoint.
    PrivateLinkServiceConnectionState:
      type: object
      description: A collection of information about the state of the connection between service consumer and provider.
      properties:
        status:
          $ref: "#/components/schemas/PrivateEndpointServiceConnectionStatus"
          description: 'Indicates whether the connection has been Approved/Rejected/Removed by the owner of the service. Known values are: "Pending", "Approved", and "Rejected".'
        description:
          type: string
          description: The reason for approval/rejection of the connection.
        actionsRequired:
          type: string
          description: A message indicating if changes on the service provider require any updates on the consumer.
    PrivateEndpointServiceConnectionStatus:
      type: string
      enum:
        - Pending
        - Approved
        - Rejected
    PrivateEndpointConnectionProvisioningState:
      type: string
      enum:
        - Succeeded
        - Creating
        - Deleting
        - Failed
    PrivateEndpointConnectionFlattened:
      type: object
      description: Flattened view of PrivateEndpointConnection ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider. Required.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
    PagedPrivateEndpointConnectionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
        nextLink:
          type: string
    PrivateEndpointConnection:
      type: object
      description: A private endpoint connection.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
          description: Resource properties.
    PrivateLinkResourceProperties:
      type: object
      description: Properties of a private link resource.
      properties:
        groupId:
          type: string
          description: The private link resource group id.
        requiredMembers:
          type: array
          items:
            type: string
          description: The private link resource required member names.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The private link resource private link DNS zone name.
    PrivateLinkResourceFlattened:
      type: object
      description: Flattened view of PrivateLinkResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        groupId:
          type: string
          description: The private link resource group id.
        requiredMembers:
          type: array
          items:
            type: string
          description: The private link resource required member names.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The private link resource private link DNS zone name.
    PagedPrivateLinkResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateLinkResourceFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    accounts:
      id: azure_extras.power_platform.accounts
      name: accounts
      title: accounts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1accounts~1{account_name}?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1accounts~1{account_name}?api-version=2020-10-30-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1accounts~1{account_name}?api-version=2020-10-30-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1accounts~1{account_name}?api-version=2020-10-30-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1accounts?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.PowerPlatform~1accounts?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/get"
          - $ref: "#/components/x-stackQL-resources/accounts/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/accounts/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/delete"
    enterprise_policies:
      id: azure_extras.power_platform.enterprise_policies
      name: enterprise_policies
      title: enterprise_policies
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}?api-version=2020-10-30-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}?api-version=2020-10-30-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}?api-version=2020-10-30-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/enterprise_policies/methods/get"
          - $ref: "#/components/x-stackQL-resources/enterprise_policies/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/enterprise_policies/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/enterprise_policies/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/enterprise_policies/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/enterprise_policies/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/enterprise_policies/methods/delete"
    operations:
      id: azure_extras.power_platform.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.PowerPlatform~1operations?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    private_endpoint_connections:
      id: azure_extras.power_platform.private_endpoint_connections
      name: private_endpoint_connections
      title: private_endpoint_connections
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2020-10-30-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2020-10-30-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_enterprise_policy:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}~1privateEndpointConnections?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/list_by_enterprise_policy"
        insert:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/delete"
    private_link_resources:
      id: azure_extras.power_platform.private_link_resources
      name: private_link_resources
      title: private_link_resources
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}~1privateLinkResources~1{group_name}?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_enterprise_policy:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerPlatform~1enterprisePolicies~1{enterprise_policy_name}~1privateLinkResources?api-version=2020-10-30-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_link_resources/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_link_resources/methods/list_by_enterprise_policy"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/accounts/{account_name}?api-version=2020-10-30-preview:
    get:
      operationId: Accounts_get
      description: Get information about an account.
      parameters:
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the account. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountFlattened"
    put:
      operationId: Accounts_create_or_update
      description: Creates an account.
      parameters:
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the account. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AccountProperties"
                  description: The properties that define configuration for the account.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
    patch:
      operationId: Accounts_update
      description: Updates an account.
      parameters:
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the account. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives.
                properties:
                  $ref: "#/components/schemas/AccountProperties"
                  description: The properties that define configuration for the account.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
    delete:
      operationId: Accounts_delete
      description: Delete an account.
      parameters:
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the account. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/accounts?api-version=2020-10-30-preview:
    get:
      operationId: Accounts_list_by_resource_group
      description: Retrieve a list of accounts within a given resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAccountFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.PowerPlatform/accounts?api-version=2020-10-30-preview:
    get:
      operationId: Accounts_list_by_subscription
      description: Retrieve a list of accounts within a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAccountFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/enterprisePolicies/{enterprise_policy_name}?api-version=2020-10-30-preview:
    get:
      operationId: EnterprisePolicies_get
      description: Get information about an EnterprisePolicy.
      parameters:
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnterprisePolicyFlattened"
    put:
      operationId: EnterprisePolicies_create_or_update
      description: Creates an EnterprisePolicy.
      parameters:
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/Properties"
                  description: The properties that define configuration for the enterprise policy.
                identity:
                  $ref: "#/components/schemas/EnterprisePolicyIdentity"
                  description: The identity of the EnterprisePolicy.
                kind:
                  $ref: "#/components/schemas/EnterprisePolicyKind"
                  description: 'The kind (type) of Enterprise Policy. Required. Known values are: "Lockbox", "PrivateEndpoint", "Encryption", "NetworkInjection", and "Identity".'
              required:
                - location
                - kind
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnterprisePolicy"
    patch:
      operationId: EnterprisePolicies_update
      description: Updates an EnterprisePolicy.
      parameters:
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives.
                identity:
                  $ref: "#/components/schemas/EnterprisePolicyIdentity"
                  description: The identity of the EnterprisePolicy.
                kind:
                  $ref: "#/components/schemas/EnterprisePolicyKind"
                  description: 'The kind (type) of Enterprise Policy. Known values are: "Lockbox", "PrivateEndpoint", "Encryption", "NetworkInjection", and "Identity".'
                properties:
                  $ref: "#/components/schemas/Properties"
                  description: The properties that define configuration for the enterprise policy.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnterprisePolicy"
    delete:
      operationId: EnterprisePolicies_delete
      description: Delete an EnterprisePolicy.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/enterprisePolicies?api-version=2020-10-30-preview:
    get:
      operationId: EnterprisePolicies_list_by_resource_group
      description: Retrieve a list of EnterprisePolicies within a given resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedEnterprisePolicyFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.PowerPlatform/enterprisePolicies?api-version=2020-10-30-preview:
    get:
      operationId: EnterprisePolicies_list_by_subscription
      description: Retrieve a list of EnterprisePolicies within a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedEnterprisePolicyFlattened"
  /providers/Microsoft.PowerPlatform/operations?api-version=2020-10-30-preview:
    get:
      operationId: powerplatform_list
      description: Lists all of the available PowerPlatform REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/enterprisePolicies/{enterprise_policy_name}/privateEndpointConnections/{private_endpoint_connection_name}?api-version=2020-10-30-preview:
    get:
      operationId: PrivateEndpointConnections_get
      description: Gets a private endpoint connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
    put:
      operationId: PrivateEndpointConnections_create_or_update
      description: Approve or reject a private endpoint connection with a given name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
                  description: Resource properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnection"
    delete:
      operationId: PrivateEndpointConnections_delete
      description: Deletes a private endpoint connection with a given name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/enterprisePolicies/{enterprise_policy_name}/privateEndpointConnections?api-version=2020-10-30-preview:
    get:
      operationId: PrivateEndpointConnections_list_by_enterprise_policy
      description: List all private endpoint connections on an EnterprisePolicy.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateEndpointConnectionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/enterprisePolicies/{enterprise_policy_name}/privateLinkResources/{group_name}?api-version=2020-10-30-preview:
    get:
      operationId: PrivateLinkResources_get
      description: Gets the private link resources that need to be created for an EnterprisePolicy.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private link resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateLinkResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerPlatform/enterprisePolicies/{enterprise_policy_name}/privateLinkResources?api-version=2020-10-30-preview:
    get:
      operationId: PrivateLinkResources_list_by_enterprise_policy
      description: Gets the private link resources that need to be created for enterprisePolicy.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enterprise_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The EnterprisePolicy name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateLinkResourceFlattened"
