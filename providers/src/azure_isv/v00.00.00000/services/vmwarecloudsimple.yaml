openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Vmwarecloudsimple
  description: This is the Microsoft Azure VMWare Cloud Simple Management Client Library.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2019-04-01-stackql-generated
  x-serviceAlias: vmwarecloudsimple
  x-sdkPackage: azure-mgmt-vmwarecloudsimple
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    CustomizationSpecification:
      type: object
      description: The specification for Customization Policy.
      properties:
        identity:
          $ref: "#/components/schemas/CustomizationIdentity"
          description: Customization Identity. It contains data about user and hostname.
        nicSettings:
          type: array
          items:
            $ref: "#/components/schemas/CustomizationNicSetting"
          description: Network interface settings.
    CustomizationIdentity:
      type: object
      description: CustomizationIdentity.
      properties:
        data:
          type: string
          description: Windows Text Identity. Prepared data.
        hostName:
          $ref: "#/components/schemas/CustomizationHostName"
          description: Virtual machine host name settings.
        type:
          type: string
          description: 'Identity type. Known values are: "WINDOWS_TEXT", "WINDOWS", and "LINUX".'
        userData:
          $ref: "#/components/schemas/CustomizationIdentityUserData"
          description: Windows Identity. User data customization.
    CustomizationHostName:
      type: object
      description: Host name model.
      properties:
        name:
          type: string
          description: Hostname.
        type:
          type: string
          description: 'Type of host name. Known values are: "USER_DEFINED", "PREFIX_BASED", "FIXED", "VIRTUAL_MACHINE_NAME", and "CUSTOM_NAME".'
    CustomizationIdentityUserData:
      type: object
      description: Windows Identity. User data customization.
      properties:
        isPasswordPredefined:
          type: boolean
          description: Is password predefined in customization policy.
    CustomizationNicSetting:
      type: object
      description: CustomizationNicSetting.
      properties:
        adapter:
          $ref: "#/components/schemas/CustomizationIPSettings"
          description: The list of adapters' settings.
        macAddress:
          type: string
          description: NIC mac address.
    CustomizationIPSettings:
      type: object
      description: CustomizationIPSettings.
      properties:
        gateway:
          type: array
          items:
            type: string
          description: The list of gateways.
        ip:
          $ref: "#/components/schemas/CustomizationIPAddress"
          description: Ip address customization settings.
        subnetMask:
          type: string
          description: Adapter subnet mask.
    CustomizationIPAddress:
      type: object
      description: CustomizationIPAddress.
      properties:
        argument:
          type: string
          description: Argument when Custom ip type is selected.
        ipAddress:
          type: string
          description: Defined Ip Address when Fixed ip type is selected.
        type:
          type: string
          description: 'Customization Specification ip type. Known values are: "CUSTOM", "DHCP_IP", "FIXED_IP", and "USER_DEFINED".'
    CustomizationPolicyFlattened:
      type: object
      description: Flattened view of CustomizationPolicy ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Customization policy azure id.
        location:
          type: string
          description: Azure region.
        name:
          type: string
          description: Customization policy name.
        type:
          type: string
          description: ":vartype type: str"
        description:
          type: string
          description: Policy description.
        privateCloudId:
          type: string
          description: The Private cloud id.
        specification:
          $ref: "#/components/schemas/CustomizationSpecification"
          description: Detailed customization policy specification.
        version:
          type: string
          description: Policy version.
    PagedCustomizationPolicyFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CustomizationPolicyFlattened"
        nextLink:
          type: string
    Sku:
      type: object
      description: The purchase SKU for CloudSimple paid resources. All required parameters must be populated in order to send to Azure.
      properties:
        capacity:
          type: string
          description: The capacity of the SKU.
        description:
          type: string
          description: "dedicatedCloudNode example: 8 x Ten-Core Intel® Xeon® Processor E5-2640 v4 2.40GHz 25MB Cache (90W); 12 x 64GB PC4-19200 2400MHz DDR4 ECC Registered DIMM, ..."
        family:
          type: string
          description: If the service has different generations of hardware, for the same SKU, then that can be captured here.
        name:
          type: string
          description: The name of the SKU for VMWare CloudSimple Node. Required.
        tier:
          type: string
          description: The tier of the SKU.
      required:
        - name
    DedicatedCloudNodeFlattened:
      type: object
      description: Flattened view of DedicatedCloudNode ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/dedicatedCloudNodes/{dedicatedCloudNodeName}.
        location:
          type: string
          description: Azure region. Required.
        name:
          type: string
          description: "{dedicatedCloudNodeName}."
        sku:
          $ref: "#/components/schemas/Sku"
          description: Dedicated Cloud Nodes SKU.
        tags:
          type: object
          properties: {}
          description: Dedicated Cloud Nodes tags.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        availabilityZoneId:
          type: string
          description: Availability Zone id, e.g. "az1".
        availabilityZoneName:
          type: string
          description: Availability Zone name, e.g. "Availability Zone 1".
        cloudRackName:
          type: string
          description: VMWare Cloud Rack Name.
        created:
          type: string
          format: date-time
          description: date time the resource was created.
        nodesCount:
          type: integer
          description: count of nodes to create.
        placementGroupId:
          type: string
          description: Placement Group id, e.g. "n1".
        placementGroupName:
          type: string
          description: Placement Name, e.g. "Placement Group 1".
        privateCloudId:
          type: string
          description: Private Cloud Id.
        privateCloudName:
          type: string
          description: Resource Pool Name.
        provisioningState:
          type: string
          description: The provisioning status of the resource.
        purchaseId:
          type: string
          description: purchase id.
        status:
          type: string
          description: 'Node status, indicates is private cloud set up on this node or not. Known values are: "unused" and "used".'
        vmwareClusterName:
          type: string
          description: VMWare Cluster Name.
        skuDescription:
          type: object
          properties:
            id:
              type: string
              description: SKU's id.
            name:
              type: string
              description: SKU's name.
    PagedDedicatedCloudNodeFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DedicatedCloudNodeFlattened"
        nextLink:
          type: string
    DedicatedCloudNode:
      type: object
      description: Dedicated cloud node model. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/dedicatedCloudNodes/{dedicatedCloudNodeName}.
        location:
          type: string
          description: Azure region. Required.
        name:
          type: string
          description: "{dedicatedCloudNodeName}."
        sku:
          $ref: "#/components/schemas/Sku"
          description: Dedicated Cloud Nodes SKU.
        tags:
          type: object
          properties: {}
          description: Dedicated Cloud Nodes tags.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        properties:
          type: object
          properties:
            availabilityZoneId:
              type: string
              description: Availability Zone id, e.g. "az1".
            availabilityZoneName:
              type: string
              description: Availability Zone name, e.g. "Availability Zone 1".
            cloudRackName:
              type: string
              description: VMWare Cloud Rack Name.
            created:
              type: string
              format: date-time
              description: date time the resource was created.
            nodesCount:
              type: integer
              description: count of nodes to create.
            placementGroupId:
              type: string
              description: Placement Group id, e.g. "n1".
            placementGroupName:
              type: string
              description: Placement Name, e.g. "Placement Group 1".
            privateCloudId:
              type: string
              description: Private Cloud Id.
            privateCloudName:
              type: string
              description: Resource Pool Name.
            provisioningState:
              type: string
              description: The provisioning status of the resource.
            purchaseId:
              type: string
              description: purchase id.
            status:
              type: string
              description: 'Node status, indicates is private cloud set up on this node or not. Known values are: "unused" and "used".'
            vmwareClusterName:
              type: string
              description: VMWare Cluster Name.
            skuDescription:
              type: object
              properties:
                id:
                  type: string
                  description: SKU's id.
                name:
                  type: string
                  description: SKU's name.
      required:
        - location
    DedicatedCloudServiceFlattened:
      type: object
      description: Flattened view of DedicatedCloudService ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/dedicatedCloudServices/{dedicatedCloudServiceName}.
        location:
          type: string
          description: Azure region. Required.
        name:
          type: string
          description: "{dedicatedCloudServiceName}."
        tags:
          type: object
          properties: {}
          description: The list of tags.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        gatewaySubnet:
          type: string
          description: gateway Subnet for the account. It will collect the subnet address and always treat it as /28.
        isAccountOnboarded:
          type: string
          description: 'indicates whether account onboarded or not in a given region. Known values are: "notOnBoarded", "onBoarded", "onBoardingFailed", and "onBoarding".'
        nodes:
          type: integer
          description: total nodes purchased.
        serviceURL:
          type: string
          description: link to a service management web portal.
    PagedDedicatedCloudServiceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DedicatedCloudServiceFlattened"
        nextLink:
          type: string
    DedicatedCloudService:
      type: object
      description: Dedicated cloud service model. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/dedicatedCloudServices/{dedicatedCloudServiceName}.
        location:
          type: string
          description: Azure region. Required.
        name:
          type: string
          description: "{dedicatedCloudServiceName}."
        tags:
          type: object
          properties: {}
          description: The list of tags.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        properties:
          type: object
          properties:
            gatewaySubnet:
              type: string
              description: gateway Subnet for the account. It will collect the subnet address and always treat it as /28.
            isAccountOnboarded:
              type: string
              description: 'indicates whether account onboarded or not in a given region. Known values are: "notOnBoarded", "onBoarded", "onBoardingFailed", and "onBoarding".'
            nodes:
              type: integer
              description: total nodes purchased.
            serviceURL:
              type: string
              description: link to a service management web portal.
      required:
        - location
    OperationError:
      type: object
      description: Operation error model.
      properties:
        code:
          type: string
          description: Error's code.
        message:
          type: string
          description: Error's message.
    OperationResource:
      type: object
      description: Operation status response. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        endTime:
          type: string
          format: date-time
          description: End time of the operation.
        error:
          $ref: "#/components/schemas/OperationError"
          description: Error Message if operation failed.
        id:
          type: string
          description: Operation Id.
        name:
          type: string
          description: Operation ID.
        startTime:
          type: string
          format: date-time
          description: Start time of the operation.
        status:
          type: string
          description: Operation status.
    AvailableOperationDisplay:
      type: object
      description: Resource provider available operation display model.
      properties:
        description:
          type: string
          description: Description of the operation for display purposes.
        operation:
          type: string
          description: Name of the operation for display purposes.
        provider:
          type: string
          description: Name of the provider for display purposes.
        resource:
          type: string
          description: Name of the resource type for display purposes.
    AvailableOperationDisplayPropertyServiceSpecificationMetricsList:
      type: object
      description: List of available operation display property service specification metrics.
      properties:
        metricSpecifications:
          type: array
          items:
            $ref: "#/components/schemas/AvailableOperationDisplayPropertyServiceSpecificationMetricsItem"
          description: Metric specifications of operation.
    AvailableOperationDisplayPropertyServiceSpecificationMetricsItem:
      type: object
      description: Available operation display property service specification metrics item. All required parameters must be populated in order to send to Azure.
      properties:
        aggregationType:
          type: string
          description: "Metric's aggregation type for e.g. (Average, Total). Required. Known values are: \"Average\" and \"Total\"."
        displayDescription:
          type: string
          description: Metric's description. Required.
        displayName:
          type: string
          description: Human readable metric's name. Required.
        name:
          type: string
          description: Metric's name/id. Required.
        unit:
          type: string
          description: Metric's unit. Required.
      required:
        - aggregationType
        - displayDescription
        - displayName
        - name
        - unit
    AvailableOperationFlattened:
      type: object
      description: Flattened view of AvailableOperation ('properties' hoisted to the top level).
      properties:
        display:
          $ref: "#/components/schemas/AvailableOperationDisplay"
          description: The list of operations.
        isDataAction:
          type: boolean
          description: Indicating whether the operation is a data action or not.
        name:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}/{read|write|delete|action}."
        origin:
          type: string
          description: 'The origin of operation. Known values are: "user", "system", and "user,system".'
        serviceSpecification:
          $ref: "#/components/schemas/AvailableOperationDisplayPropertyServiceSpecificationMetricsList"
          description: The list of specification's service metrics.
    PagedAvailableOperationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AvailableOperationFlattened"
        nextLink:
          type: string
    ResourcePool:
      type: object
      description: Resource pool model. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: resource pool id (privateCloudId:vsphereId). Required.
        location:
          type: string
          description: Azure region.
        name:
          type: string
          description: "{ResourcePoolName}."
        privateCloudId:
          type: string
          description: The Private Cloud Id.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        properties:
          type: object
          properties:
            fullName:
              type: string
              description: Hierarchical resource pool name.
      required:
        - id
    VirtualMachineTemplate:
      type: object
      description: Virtual machine template model. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: virtual machine template id (privateCloudId:vsphereId).
        location:
          type: string
          description: Azure region.
        name:
          type: string
          description: "{virtualMachineTemplateName}."
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        properties:
          type: object
          properties:
            amountOfRam:
              type: integer
              description: The amount of memory.
            controllers:
              type: array
              items:
                $ref: "#/components/schemas/VirtualDiskController"
              description: The list of Virtual Disk Controllers.
            description:
              type: string
              description: The description of Virtual Machine Template.
            disks:
              type: array
              items:
                $ref: "#/components/schemas/VirtualDisk"
              description: The list of Virtual Disks.
            exposeToGuestVM:
              type: boolean
              description: Expose Guest OS or not.
            guestOS:
              type: string
              description: The Guest OS.
            guestOSType:
              type: string
              description: The Guest OS types.
            nics:
              type: array
              items:
                $ref: "#/components/schemas/VirtualNic"
              description: The list of Virtual NICs.
            numberOfCores:
              type: integer
              description: The number of CPU cores.
            path:
              type: string
              description: path to folder.
            privateCloudId:
              type: string
              description: The Private Cloud Id.
            vSphereNetworks:
              type: array
              items:
                type: string
              description: The list of VSphere networks.
            vSphereTags:
              type: array
              items:
                type: string
              description: The tags from VSphere.
            vmwaretools:
              type: string
              description: The VMware tools version.
    VirtualDiskController:
      type: object
      description: Virtual disk controller model. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Controller's id.
        name:
          type: string
          description: The display name of Controller.
        subType:
          type: string
          description: dik controller subtype (VMWARE_PARAVIRTUAL, BUS_PARALLEL, LSI_PARALLEL, LSI_SAS).
        type:
          type: string
          description: disk controller type (SCSI).
    VirtualDisk:
      type: object
      description: Virtual disk model. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        controllerId:
          type: string
          description: Disk's Controller id. Required.
        independenceMode:
          type: string
          description: "Disk's independence mode type. Required. Known values are: \"persistent\", \"independent_persistent\", and \"independent_nonpersistent\"."
        totalSize:
          type: integer
          description: Disk's total size. Required.
        virtualDiskId:
          type: string
          description: Disk's id.
        virtualDiskName:
          type: string
          description: Disk's display name.
      required:
        - controllerId
        - independenceMode
        - totalSize
    VirtualNic:
      type: object
      description: Virtual NIC model. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        customization:
          $ref: "#/components/schemas/GuestOSNICCustomization"
          description: guest OS customization for nic.
        ipAddresses:
          type: array
          items:
            type: string
          description: NIC ip address.
        macAddress:
          type: string
          description: NIC MAC address.
        network:
          $ref: "#/components/schemas/VirtualNetwork"
          description: Virtual Network. Required.
        nicType:
          type: string
          description: 'NIC type. Required. Known values are: "E1000", "E1000E", "PCNET32", "VMXNET", "VMXNET2", and "VMXNET3".'
        powerOnBoot:
          type: boolean
          description: Is NIC powered on/off on boot.
        virtualNicId:
          type: string
          description: NIC id.
        virtualNicName:
          type: string
          description: NIC name.
      required:
        - network
        - nicType
    GuestOSNICCustomization:
      type: object
      description: Guest OS nic customization.
      properties:
        allocation:
          type: string
          description: 'IP address allocation method. Known values are: "static" and "dynamic".'
        dnsServers:
          type: array
          items:
            type: string
          description: List of dns servers to use.
        gateway:
          type: array
          items:
            type: string
          description: Gateway addresses assigned to nic.
        ipAddress:
          type: string
          description: Static ip address for nic.
        mask:
          type: string
          description: Network mask for nic.
        primaryWinsServer:
          type: string
          description: primary WINS server for Windows.
        secondaryWinsServer:
          type: string
          description: secondary WINS server for Windows.
    VirtualNetwork:
      type: object
      description: Virtual network model. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        assignable:
          type: boolean
          description: can be used in vm creation/deletion.
        id:
          type: string
          description: virtual network id (privateCloudId:vsphereId). Required.
        location:
          type: string
          description: Azure region.
        name:
          type: string
          description: "{VirtualNetworkName}."
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        properties:
          type: object
          properties:
            privateCloudId:
              type: string
              description: The Private Cloud id.
      required:
        - id
    PrivateCloudFlattened:
      type: object
      description: Flattened view of PrivateCloud ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Azure Id, e.g. "/subscriptions/4da99247-a172-4ed6-8ae9-ebed2d12f839/providers/Microsoft.VMwareCloudSimple/privateClouds/cloud123".
        location:
          type: string
          description: Location where private cloud created, e.g "westus".
        name:
          type: string
          description: Private cloud name.
        type:
          type: string
          description: Azure Resource type. Default value is "Microsoft.VMwareCloudSimple/privateClouds".
        availabilityZoneId:
          type: string
          description: Availability Zone id, e.g. "az1".
        availabilityZoneName:
          type: string
          description: Availability Zone name, e.g. "Availability Zone 1".
        clustersNumber:
          type: integer
          description: Number of clusters.
        createdBy:
          type: string
          description: User's emails who created cloud.
        createdOn:
          type: string
          format: date-time
          description: When private cloud was created.
        dnsServers:
          type: array
          items:
            type: string
          description: Array of DNS servers.
        expires:
          type: string
          description: Expiration date of PC.
        nsxType:
          type: string
          description: Nsx Type, e.g. "Advanced".
        placementGroupId:
          type: string
          description: Placement Group id, e.g. "n1".
        placementGroupName:
          type: string
          description: Placement Group name.
        privateCloudId:
          type: string
          description: Id of a private cloud.
        resourcePools:
          type: array
          items:
            $ref: "#/components/schemas/ResourcePool"
          description: The list of Resource Pools.
        state:
          type: string
          description: Private Cloud state, e.g. "operational".
        totalCpuCores:
          type: integer
          description: Number of cores.
        totalNodes:
          type: integer
          description: Number of nodes.
        totalRam:
          type: integer
          description: Memory size.
        totalStorage:
          type: number
          description: Disk space in TB.
        vSphereVersion:
          type: string
          description: e.g. "6.5u2".
        vcenterFqdn:
          type: string
          description: FQDN for vcenter access.
        vcenterRefid:
          type: string
          description: Vcenter ip address.
        virtualMachineTemplates:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineTemplate"
          description: The list of Virtual Machine Templates.
        virtualNetworks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNetwork"
          description: The list of Virtual Networks.
        vrOpsEnabled:
          type: boolean
          description: Is Vrops enabled/disabled.
    PagedPrivateCloudFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateCloudFlattened"
        nextLink:
          type: string
    ResourcePoolFlattened:
      type: object
      description: Flattened view of ResourcePool ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: resource pool id (privateCloudId:vsphereId). Required.
        location:
          type: string
          description: Azure region.
        name:
          type: string
          description: "{ResourcePoolName}."
        privateCloudId:
          type: string
          description: The Private Cloud Id.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        fullName:
          type: string
          description: Hierarchical resource pool name.
    PagedResourcePoolFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourcePoolFlattened"
        nextLink:
          type: string
    SkuAvailability:
      type: object
      description: SKU availability model. All required parameters must be populated in order to send to Azure.
      properties:
        dedicatedAvailabilityZoneId:
          type: string
          description: CloudSimple Availability Zone id.
        dedicatedAvailabilityZoneName:
          type: string
          description: CloudSimple Availability Zone Name.
        dedicatedPlacementGroupId:
          type: string
          description: CloudSimple Placement Group Id.
        dedicatedPlacementGroupName:
          type: string
          description: CloudSimple Placement Group name.
        limit:
          type: integer
          description: indicates how many resources of a given SKU is available in a AZ->PG. Required.
        resourceType:
          type: string
          description: resource type e.g. DedicatedCloudNodes.
        skuId:
          type: string
          description: sku id.
        skuName:
          type: string
          description: sku name.
      required:
        - limit
    PagedSkuAvailability:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SkuAvailability"
        nextLink:
          type: string
    UsageName:
      type: object
      description: User name model.
      properties:
        localizedValue:
          type: string
          description: e.g. "Virtual Machines".
        value:
          type: string
          description: resource type or resource type sku name, e.g. virtualMachines.
    Usage:
      type: object
      description: Usage model. All required parameters must be populated in order to send to Azure.
      properties:
        currentValue:
          type: integer
          description: The current usage value. Required.
        limit:
          type: integer
          description: limit of a given sku in a region for a subscription. The maximum permitted value for the usage quota. If there is no limit, this value will be -1. Required.
        name:
          $ref: "#/components/schemas/UsageName"
          description: Usage name value and localized name.
        unit:
          type: string
          description: "The usages' unit. Known values are: \"Count\", \"Bytes\", \"Seconds\", \"Percent\", \"CountPerSecond\", and \"BytesPerSecond\"."
      required:
        - currentValue
        - limit
    PagedUsage:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Usage"
        nextLink:
          type: string
    VirtualMachineTemplateFlattened:
      type: object
      description: Flattened view of VirtualMachineTemplate ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: virtual machine template id (privateCloudId:vsphereId).
        location:
          type: string
          description: Azure region.
        name:
          type: string
          description: "{virtualMachineTemplateName}."
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        amountOfRam:
          type: integer
          description: The amount of memory.
        controllers:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDiskController"
          description: The list of Virtual Disk Controllers.
        description:
          type: string
          description: The description of Virtual Machine Template.
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDisk"
          description: The list of Virtual Disks.
        exposeToGuestVM:
          type: boolean
          description: Expose Guest OS or not.
        guestOS:
          type: string
          description: The Guest OS.
        guestOSType:
          type: string
          description: The Guest OS types.
        nics:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNic"
          description: The list of Virtual NICs.
        numberOfCores:
          type: integer
          description: The number of CPU cores.
        path:
          type: string
          description: path to folder.
        privateCloudId:
          type: string
          description: The Private Cloud Id.
        vSphereNetworks:
          type: array
          items:
            type: string
          description: The list of VSphere networks.
        vSphereTags:
          type: array
          items:
            type: string
          description: The tags from VSphere.
        vmwaretools:
          type: string
          description: The VMware tools version.
    PagedVirtualMachineTemplateFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineTemplateFlattened"
        nextLink:
          type: string
    GuestOSCustomization:
      type: object
      description: Guest OS Customization properties.
      properties:
        dnsServers:
          type: array
          items:
            type: string
          description: List of dns servers to use.
        hostName:
          type: string
          description: Virtual Machine hostname.
        password:
          type: string
          description: Password for login.
        policyId:
          type: string
          description: id of customization policy.
        username:
          type: string
          description: Username for login.
    VirtualMachineFlattened:
      type: object
      description: Flattened view of VirtualMachine ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/virtualMachines/{virtualMachineName}.
        location:
          type: string
          description: Azure region. Required.
        name:
          type: string
          description: "{virtualMachineName}."
        tags:
          type: object
          properties: {}
          description: The list of tags.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        amountOfRam:
          type: integer
          description: The amount of memory.
        controllers:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDiskController"
          description: The list of Virtual Disks' Controllers.
        customization:
          $ref: "#/components/schemas/GuestOSCustomization"
          description: Virtual machine properties.
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDisk"
          description: The list of Virtual Disks.
        dnsname:
          type: string
          description: The DNS name of Virtual Machine in VCenter.
        exposeToGuestVM:
          type: boolean
          description: Expose Guest OS or not.
        folder:
          type: string
          description: The path to virtual machine folder in VCenter.
        guestOS:
          type: string
          description: The name of Guest OS.
        guestOSType:
          type: string
          description: 'The Guest OS type. Known values are: "linux", "windows", and "other".'
        nics:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNic"
          description: The list of Virtual NICs.
        numberOfCores:
          type: integer
          description: The number of CPU cores.
        password:
          type: string
          description: Password for login. Deprecated - use customization property.
        privateCloudId:
          type: string
          description: Private Cloud Id.
        provisioningState:
          type: string
          description: The provisioning status of the resource.
        publicIP:
          type: string
          description: The public ip of Virtual Machine.
        resourcePool:
          $ref: "#/components/schemas/ResourcePool"
          description: Virtual Machines Resource Pool.
        status:
          type: string
          description: 'The status of Virtual machine. Known values are: "running", "suspended", "poweredoff", "updating", "deallocating", and "deleting".'
        templateId:
          type: string
          description: Virtual Machine Template Id.
        username:
          type: string
          description: Username for login. Deprecated - use customization property.
        vSphereNetworks:
          type: array
          items:
            type: string
          description: The list of Virtual VSphere Networks.
        vmId:
          type: string
          description: The internal id of Virtual Machine in VCenter.
        vmwaretools:
          type: string
          description: VMware tools version.
    PagedVirtualMachineFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineFlattened"
        nextLink:
          type: string
    VirtualMachine:
      type: object
      description: Virtual machine model. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/virtualMachines/{virtualMachineName}.
        location:
          type: string
          description: Azure region. Required.
        name:
          type: string
          description: "{virtualMachineName}."
        tags:
          type: object
          properties: {}
          description: The list of tags.
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        properties:
          type: object
          properties:
            amountOfRam:
              type: integer
              description: The amount of memory.
            controllers:
              type: array
              items:
                $ref: "#/components/schemas/VirtualDiskController"
              description: The list of Virtual Disks' Controllers.
            customization:
              $ref: "#/components/schemas/GuestOSCustomization"
              description: Virtual machine properties.
            disks:
              type: array
              items:
                $ref: "#/components/schemas/VirtualDisk"
              description: The list of Virtual Disks.
            dnsname:
              type: string
              description: The DNS name of Virtual Machine in VCenter.
            exposeToGuestVM:
              type: boolean
              description: Expose Guest OS or not.
            folder:
              type: string
              description: The path to virtual machine folder in VCenter.
            guestOS:
              type: string
              description: The name of Guest OS.
            guestOSType:
              type: string
              description: 'The Guest OS type. Known values are: "linux", "windows", and "other".'
            nics:
              type: array
              items:
                $ref: "#/components/schemas/VirtualNic"
              description: The list of Virtual NICs.
            numberOfCores:
              type: integer
              description: The number of CPU cores.
            password:
              type: string
              description: Password for login. Deprecated - use customization property.
            privateCloudId:
              type: string
              description: Private Cloud Id.
            provisioningState:
              type: string
              description: The provisioning status of the resource.
            publicIP:
              type: string
              description: The public ip of Virtual Machine.
            resourcePool:
              $ref: "#/components/schemas/ResourcePool"
              description: Virtual Machines Resource Pool.
            status:
              type: string
              description: 'The status of Virtual machine. Known values are: "running", "suspended", "poweredoff", "updating", "deallocating", and "deleting".'
            templateId:
              type: string
              description: Virtual Machine Template Id.
            username:
              type: string
              description: Username for login. Deprecated - use customization property.
            vSphereNetworks:
              type: array
              items:
                type: string
              description: The list of Virtual VSphere Networks.
            vmId:
              type: string
              description: The internal id of Virtual Machine in VCenter.
            vmwaretools:
              type: string
              description: VMware tools version.
      required:
        - location
    VirtualNetworkFlattened:
      type: object
      description: Flattened view of VirtualNetwork ('properties' hoisted to the top level).
      properties:
        assignable:
          type: boolean
          description: can be used in vm creation/deletion.
        id:
          type: string
          description: virtual network id (privateCloudId:vsphereId). Required.
        location:
          type: string
          description: Azure region.
        name:
          type: string
          description: "{VirtualNetworkName}."
        type:
          type: string
          description: "{resourceProviderNamespace}/{resourceType}."
        privateCloudId:
          type: string
          description: The Private Cloud id.
    PagedVirtualNetworkFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNetworkFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    customization_policies:
      id: azure_isv.vmwarecloudsimple.customization_policies
      name: customization_policies
      title: customization_policies
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1customizationPolicies~1{customization_policy_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1customizationPolicies?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/customization_policies/methods/get"
          - $ref: "#/components/x-stackQL-resources/customization_policies/methods/list"
        insert: []
        update: []
        delete: []
    dedicated_cloud_nodes:
      id: azure_isv.vmwarecloudsimple.dedicated_cloud_nodes
      name: dedicated_cloud_nodes
      title: dedicated_cloud_nodes
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudNodes~1{dedicated_cloud_node_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudNodes~1{dedicated_cloud_node_name}?api-version=2019-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudNodes~1{dedicated_cloud_node_name}?api-version=2019-04-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudNodes~1{dedicated_cloud_node_name}?api-version=2019-04-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudNodes?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudNodes?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_nodes/methods/get"
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_nodes/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_nodes/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_nodes/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_nodes/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_nodes/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_nodes/methods/delete"
    dedicated_cloud_services:
      id: azure_isv.vmwarecloudsimple.dedicated_cloud_services
      name: dedicated_cloud_services
      title: dedicated_cloud_services
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudServices~1{dedicated_cloud_service_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudServices~1{dedicated_cloud_service_name}?api-version=2019-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudServices~1{dedicated_cloud_service_name}?api-version=2019-04-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudServices~1{dedicated_cloud_service_name}?api-version=2019-04-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudServices?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1dedicatedCloudServices?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_services/methods/get"
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_services/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_services/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_services/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_services/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_services/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/dedicated_cloud_services/methods/delete"
    operations:
      id: azure_isv.vmwarecloudsimple.operations
      name: operations
      title: operations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1operationResults~1{operation_id}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.VMwareCloudSimple~1operations?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/get"
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    private_clouds:
      id: azure_isv.vmwarecloudsimple.private_clouds
      name: private_clouds
      title: private_clouds
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_clouds/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_clouds/methods/list"
        insert: []
        update: []
        delete: []
    resource_pools:
      id: azure_isv.vmwarecloudsimple.resource_pools
      name: resource_pools
      title: resource_pools
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1resourcePools~1{resource_pool_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1resourcePools?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/get"
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/list"
        insert: []
        update: []
        delete: []
    skus_availability:
      id: azure_isv.vmwarecloudsimple.skus_availability
      name: skus_availability
      title: skus_availability
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1availabilities?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/skus_availability/methods/list"
        insert: []
        update: []
        delete: []
    usages:
      id: azure_isv.vmwarecloudsimple.usages
      name: usages
      title: usages
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1usages?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/usages/methods/list"
        insert: []
        update: []
        delete: []
    virtual_machine_templates:
      id: azure_isv.vmwarecloudsimple.virtual_machine_templates
      name: virtual_machine_templates
      title: virtual_machine_templates
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1virtualMachineTemplates?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/list"
        insert: []
        update: []
        delete: []
    virtual_machines:
      id: azure_isv.vmwarecloudsimple.virtual_machines
      name: virtual_machines
      title: virtual_machines
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines~1{virtual_machine_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines~1{virtual_machine_name}?api-version=2019-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines~1{virtual_machine_name}?api-version=2019-04-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines~1{virtual_machine_name}?api-version=2019-04-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        start:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines~1{virtual_machine_name}~1start?api-version=2019-04-01/post"
          response:
            openAPIDocKey: "204"
        stop:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.VMwareCloudSimple~1virtualMachines~1{virtual_machine_name}~1stop?api-version=2019-04-01/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_machines/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_machines/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/virtual_machines/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_machines/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_machines/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/virtual_machines/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_machines/methods/delete"
    virtual_networks:
      id: azure_isv.vmwarecloudsimple.virtual_networks
      name: virtual_networks
      title: virtual_networks
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1virtualNetworks~1{virtual_network_name}?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.VMwareCloudSimple~1locations~1{region_id}~1privateClouds~1{pc_name}~1virtualNetworks?api-version=2019-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/customizationPolicies/{customization_policy_name}?api-version=2019-04-01:
    get:
      operationId: CustomizationPolicies_get
      description: Implements get of customization policy. Returns customization policy by its name.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: customization_policy_name
          in: path
          required: true
          schema:
            type: string
          description: customization policy name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomizationPolicyFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/customizationPolicies?api-version=2019-04-01:
    get:
      operationId: CustomizationPolicies_list
      description: Implements get of customization policies list. Returns list of customization policies in region for private cloud.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. only type is allowed here as a filter e.g. $filter=type eq 'xxxx'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCustomizationPolicyFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/dedicatedCloudNodes/{dedicated_cloud_node_name}?api-version=2019-04-01:
    get:
      operationId: DedicatedCloudNodes_get
      description: Implements dedicated cloud node GET method. Returns dedicated cloud node.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_node_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud node name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCloudNodeFlattened"
    put:
      operationId: DedicatedCloudNodes_begin_create_or_update
      description: Implements dedicated cloud node PUT method. Returns dedicated cloud node by its name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_node_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud node name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: Referer
          in: header
          required: true
          schema:
            type: string
          description: referer url. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Azure region. Required.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: Dedicated Cloud Nodes SKU.
                tags:
                  type: object
                  properties: {}
                  description: Dedicated Cloud Nodes tags.
                properties:
                  type: object
                  properties:
                    availabilityZoneId:
                      type: string
                      description: Availability Zone id, e.g. "az1".
                    nodesCount:
                      type: integer
                      description: count of nodes to create.
                    placementGroupId:
                      type: string
                      description: Placement Group id, e.g. "n1".
                    purchaseId:
                      type: string
                      description: purchase id.
                    skuDescription:
                      type: object
                      properties:
                        id:
                          type: string
                          description: SKU's id.
                        name:
                          type: string
                          description: SKU's name.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCloudNode"
    delete:
      operationId: DedicatedCloudNodes_delete
      description: Implements dedicated cloud node DELETE method. Delete dedicated cloud node.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_node_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud node name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: DedicatedCloudNodes_update
      description: Implements dedicated cloud node PATCH method. Patches dedicated node properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_node_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud node name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: The tags key:value pairs.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCloudNode"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/dedicatedCloudNodes?api-version=2019-04-01:
    get:
      operationId: DedicatedCloudNodes_list_by_subscription
      description: Implements list of dedicated cloud nodes within subscription method. Returns list of dedicate cloud nodes within subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. Default value is None.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: to be used by nextLink implementation. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDedicatedCloudNodeFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/dedicatedCloudNodes?api-version=2019-04-01:
    get:
      operationId: DedicatedCloudNodes_list_by_resource_group
      description: Implements list of dedicated cloud nodes within RG method. Returns list of dedicate cloud nodes within resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. Default value is None.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: to be used by nextLink implementation. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDedicatedCloudNodeFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/dedicatedCloudServices/{dedicated_cloud_service_name}?api-version=2019-04-01:
    get:
      operationId: DedicatedCloudServices_get
      description: Implements dedicatedCloudService GET method. Returns Dedicate Cloud Service.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_service_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud Service name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCloudServiceFlattened"
    put:
      operationId: DedicatedCloudServices_create_or_update
      description: Implements dedicated cloud service PUT method. Create dedicate cloud service.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_service_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud Service name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Azure region. Required.
                tags:
                  type: object
                  properties: {}
                  description: The list of tags.
                properties:
                  type: object
                  properties:
                    gatewaySubnet:
                      type: string
                      description: gateway Subnet for the account. It will collect the subnet address and always treat it as /28.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCloudService"
    delete:
      operationId: DedicatedCloudServices_begin_delete
      description: Implements dedicatedCloudService DELETE method. Delete dedicate cloud service.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_service_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud service name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: DedicatedCloudServices_update
      description: Implements dedicatedCloudService PATCH method. Patch dedicated cloud service's properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: dedicated_cloud_service_name
          in: path
          required: true
          schema:
            type: string
          description: dedicated cloud service name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: The tags key:value pairs.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCloudService"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/dedicatedCloudServices?api-version=2019-04-01:
    get:
      operationId: DedicatedCloudServices_list_by_subscription
      description: Implements list of dedicatedCloudService objects within subscription method. Returns list of dedicated cloud services within a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. Default value is None.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: to be used by nextLink implementation. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDedicatedCloudServiceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/dedicatedCloudServices?api-version=2019-04-01:
    get:
      operationId: DedicatedCloudServices_list_by_resource_group
      description: Implements list of dedicatedCloudService objects within RG method. Returns list of dedicated cloud services within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. Default value is None.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: to be used by nextLink implementation. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDedicatedCloudServiceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/operationResults/{operation_id}?api-version=2019-04-01:
    get:
      operationId: vmwarecloudsimple_get
      description: Implements get of async operation. Return an async operation.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: operation id. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: Referer
          in: header
          required: true
          schema:
            type: string
          description: referer url. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationResource"
  /providers/Microsoft.VMwareCloudSimple/operations?api-version=2019-04-01:
    get:
      operationId: vmwarecloudsimple_list
      description: Implements list of available operations. Return list of operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAvailableOperationFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}?api-version=2019-04-01:
    get:
      operationId: PrivateClouds_get
      description: Implements private cloud GET method. Returns private cloud by its name.
      parameters:
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateCloudFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds?api-version=2019-04-01:
    get:
      operationId: PrivateClouds_list
      description: Implements private cloud list GET method. Returns list of private clouds in particular region.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateCloudFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/resourcePools/{resource_pool_name}?api-version=2019-04-01:
    get:
      operationId: ResourcePools_get
      description: Implements get of resource pool. Returns resource pool templates by its name.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: resource_pool_name
          in: path
          required: true
          schema:
            type: string
          description: resource pool id (vsphereId). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourcePoolFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/resourcePools?api-version=2019-04-01:
    get:
      operationId: ResourcePools_list
      description: Implements get of resource pools list. Returns list of resource pools in region for private cloud.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourcePoolFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/availabilities?api-version=2019-04-01:
    get:
      operationId: SkusAvailability_list
      description: Implements SkuAvailability List method. Returns list of available resources in region.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: skuId
          in: query
          required: false
          schema:
            type: string
          description: sku id, if no sku is passed availability for all skus will be returned. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSkuAvailability"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/usages?api-version=2019-04-01:
    get:
      operationId: Usages_list
      description: Implements Usages List method. Returns list of usage in region.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. only name.value is allowed here as a filter e.g. $filter=name.value eq 'xxxx'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedUsage"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/virtualMachineTemplates/{virtual_machine_template_name}?api-version=2019-04-01:
    get:
      operationId: VirtualMachineTemplates_get
      description: Implements virtual machine template GET method. Returns virtual machine templates by its name.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: virtual machine template id (vsphereId). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineTemplateFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/virtualMachineTemplates?api-version=2019-04-01:
    get:
      operationId: VirtualMachineTemplates_list
      description: Implements list of available VM templates. Returns list of virtual machine templates in region for private cloud.
      parameters:
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: resourcePoolName
          in: query
          required: true
          schema:
            type: string
          description: Resource pool used to derive vSphere cluster which contains VM templates. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineTemplateFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/virtualMachines/{virtual_machine_name}?api-version=2019-04-01:
    get:
      operationId: VirtualMachines_get
      description: Implements virtual machine GET method. Get virtual machine.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: virtual_machine_name
          in: path
          required: true
          schema:
            type: string
          description: virtual machine name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineFlattened"
    put:
      operationId: VirtualMachines_begin_create_or_update
      description: Implements virtual machine PUT method. Create Or Update Virtual Machine.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: virtual_machine_name
          in: path
          required: true
          schema:
            type: string
          description: virtual machine name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: Referer
          in: header
          required: true
          schema:
            type: string
          description: referer url. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Azure region. Required.
                tags:
                  type: object
                  properties: {}
                  description: The list of tags.
                properties:
                  type: object
                  properties:
                    amountOfRam:
                      type: integer
                      description: The amount of memory.
                    customization:
                      $ref: "#/components/schemas/GuestOSCustomization"
                      description: Virtual machine properties.
                    disks:
                      type: array
                      items:
                        $ref: "#/components/schemas/VirtualDisk"
                      description: The list of Virtual Disks.
                    exposeToGuestVM:
                      type: boolean
                      description: Expose Guest OS or not.
                    nics:
                      type: array
                      items:
                        $ref: "#/components/schemas/VirtualNic"
                      description: The list of Virtual NICs.
                    numberOfCores:
                      type: integer
                      description: The number of CPU cores.
                    password:
                      type: string
                      description: Password for login. Deprecated - use customization property.
                    privateCloudId:
                      type: string
                      description: Private Cloud Id.
                    resourcePool:
                      $ref: "#/components/schemas/ResourcePool"
                      description: Virtual Machines Resource Pool.
                    templateId:
                      type: string
                      description: Virtual Machine Template Id.
                    username:
                      type: string
                      description: Username for login. Deprecated - use customization property.
                    vSphereNetworks:
                      type: array
                      items:
                        type: string
                      description: The list of Virtual VSphere Networks.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachine"
    delete:
      operationId: VirtualMachines_begin_delete
      description: Implements virtual machine DELETE method. Delete virtual machine.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: virtual_machine_name
          in: path
          required: true
          schema:
            type: string
          description: virtual machine name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: Referer
          in: header
          required: true
          schema:
            type: string
          description: referer url. Required.
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: VirtualMachines_begin_update
      description: Implements virtual machine PATCH method. Patch virtual machine properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: virtual_machine_name
          in: path
          required: true
          schema:
            type: string
          description: virtual machine name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: The tags key:value pairs.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachine"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/virtualMachines?api-version=2019-04-01:
    get:
      operationId: VirtualMachines_list_by_subscription
      description: Implements list virtual machine within subscription method. Returns list virtual machine within subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. Default value is None.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: to be used by nextLink implementation. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/virtualMachines?api-version=2019-04-01:
    get:
      operationId: VirtualMachines_list_by_resource_group
      description: Implements list virtual machine within RG method. Returns list of virtual machine within resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. Default value is None.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: to be used by nextLink implementation. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/virtualMachines/{virtual_machine_name}/start?api-version=2019-04-01:
    post:
      operationId: VirtualMachines_begin_start
      description: Implements a start method for a virtual machine. Power on virtual machine.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: virtual_machine_name
          in: path
          required: true
          schema:
            type: string
          description: virtual machine name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: Referer
          in: header
          required: true
          schema:
            type: string
          description: referer url. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.VMwareCloudSimple/virtualMachines/{virtual_machine_name}/stop?api-version=2019-04-01:
    post:
      operationId: VirtualMachines_begin_stop
      description: "Implements shutdown, poweroff, and suspend method for a virtual machine. Power off virtual machine, options: shutdown, poweroff, and suspend."
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: virtual_machine_name
          in: path
          required: true
          schema:
            type: string
          description: virtual machine name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: Referer
          in: header
          required: true
          schema:
            type: string
          description: referer url. Required.
        - name: mode
          in: query
          required: false
          schema:
            type: string
          description: 'query stop mode parameter (reboot, shutdown, etc...). Known values are: "reboot", "suspend", "shutdown", and "poweroff". Default value is None.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                mode:
                  type: string
                  description: 'mode indicates a type of stop operation - reboot, suspend, shutdown or power-off. Known values are: "reboot", "suspend", "shutdown", and "poweroff".'
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/virtualNetworks/{virtual_network_name}?api-version=2019-04-01:
    get:
      operationId: VirtualNetworks_get
      description: Implements virtual network GET method. Return virtual network by its name.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: virtual network id (vsphereId). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetworkFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.VMwareCloudSimple/locations/{region_id}/privateClouds/{pc_name}/virtualNetworks?api-version=2019-04-01:
    get:
      operationId: VirtualNetworks_list
      description: Implements list available virtual networks within a subscription method. Return list of virtual networks in location for private cloud.
      parameters:
        - name: region_id
          in: path
          required: true
          schema:
            type: string
          description: The region Id (westus, eastus). Required.
        - name: pc_name
          in: path
          required: true
          schema:
            type: string
          description: The private cloud name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: resourcePoolName
          in: query
          required: true
          schema:
            type: string
          description: Resource pool used to derive vSphere cluster which contains virtual networks. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualNetworkFlattened"
