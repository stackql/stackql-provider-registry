openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Native Dynatrace Service
  description: Dynatrace environments with unified Azure billing and monitored-resource wiring.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-04-27-stackql-generated
  x-serviceAlias: dynatrace
  x-sdkPackage: azure-mgmt-dynatrace
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    IdentityProperties:
      type: object
      description: The properties of the managed service identities assigned to this resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        tenantId:
          type: string
          description: The Active Directory tenant id of the principal.
        principalId:
          type: string
          description: The active directory identifier of this principal.
        type:
          type: string
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "SystemAssigned", "UserAssigned", and "SystemAndUserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    DynatraceEnvironmentProperties:
      type: object
      description: Properties of the Dynatrace environment.
      properties:
        userId:
          type: string
          description: User id.
        accountInfo:
          $ref: "#/components/schemas/AccountInfo"
          description: Dynatrace Account Information.
        environmentInfo:
          $ref: "#/components/schemas/EnvironmentInfo"
          description: Dynatrace Environment Information.
        singleSignOnProperties:
          $ref: "#/components/schemas/DynatraceSingleSignOnProperties"
          description: The details of a Dynatrace single sign-on.
    AccountInfo:
      type: object
      description: Dynatrace Account Information.
      properties:
        accountId:
          type: string
          description: Account Id of the account this environment is linked to.
        regionId:
          type: string
          description: Region in which the account is created.
    EnvironmentInfo:
      type: object
      description: Dynatrace Environment Information.
      properties:
        environmentId:
          type: string
          description: Id of the environment created.
        ingestionKey:
          type: string
          description: Ingestion key of the environment.
        logsIngestionEndpoint:
          type: string
          description: Ingestion endpoint used for sending logs.
        landingURL:
          type: string
          description: Landing URL for Dynatrace environment.
    DynatraceSingleSignOnProperties:
      type: object
      description: The details of a Dynatrace single sign-on. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        singleSignOnState:
          type: string
          description: 'State of Single Sign On. Known values are: "Initial", "Enable", "Disable", and "Existing".'
        enterpriseAppId:
          type: string
          description: Version of the Dynatrace agent installed on the VM.
        singleSignOnUrl:
          type: string
          description: The login URL specific to this Dynatrace Environment.
        aadDomains:
          type: array
          items:
            type: string
          description: array of Aad(azure active directory) domains.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    UserInfo:
      type: object
      description: User info.
      properties:
        firstName:
          type: string
          description: First Name of the user.
        lastName:
          type: string
          description: Last Name of the user.
        emailAddress:
          type: string
          description: Email of the user used by Dynatrace for contacting them if needed.
        phoneNumber:
          type: string
          description: Phone number of the user used by Dynatrace for contacting them if needed.
        country:
          type: string
          description: Country of the user.
    PlanData:
      type: object
      description: Billing plan information.
      properties:
        usageType:
          type: string
          description: different usage type like PAYG/COMMITTED. this could be enum.
        billingCycle:
          type: string
          description: different billing cycles like MONTHLY/WEEKLY. this could be enum.
        planDetails:
          type: string
          description: plan id as published by Dynatrace.
        effectiveDate:
          type: string
          format: date-time
          description: date when plan was applied.
    MonitorResourceFlattened:
      type: object
      description: Flattened view of MonitorResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: System metadata for this resource.
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: The managed service identities assigned to this resource.
        monitoringStatus:
          type: string
          description: 'Status of the monitor. Known values are: "Enabled" and "Disabled".'
        marketplaceSubscriptionStatus:
          type: string
          description: 'Marketplace subscription status. Known values are: "Active" and "Suspended".'
        dynatraceEnvironmentProperties:
          $ref: "#/components/schemas/DynatraceEnvironmentProperties"
          description: Properties of the Dynatrace environment.
        userInfo:
          $ref: "#/components/schemas/UserInfo"
          description: User info.
        planData:
          $ref: "#/components/schemas/PlanData"
          description: Billing plan information.
        liftrResourceCategory:
          type: string
          description: 'Liftr Resource category. Known values are: "Unknown" and "MonitorLogs".'
        liftrResourcePreference:
          type: integer
          description: The priority of the resource.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    PagedMonitorResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MonitorResourceFlattened"
        nextLink:
          type: string
    MonitorResource:
      type: object
      description: Dynatrace Monitor Resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: System metadata for this resource.
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: The managed service identities assigned to this resource.
        properties:
          type: object
          properties:
            monitoringStatus:
              type: string
              description: 'Status of the monitor. Known values are: "Enabled" and "Disabled".'
            marketplaceSubscriptionStatus:
              type: string
              description: 'Marketplace subscription status. Known values are: "Active" and "Suspended".'
            dynatraceEnvironmentProperties:
              $ref: "#/components/schemas/DynatraceEnvironmentProperties"
              description: Properties of the Dynatrace environment.
            userInfo:
              $ref: "#/components/schemas/UserInfo"
              description: User info.
            planData:
              $ref: "#/components/schemas/PlanData"
              description: Billing plan information.
            liftrResourceCategory:
              type: string
              description: 'Liftr Resource category. Known values are: "Unknown" and "MonitorLogs".'
            liftrResourcePreference:
              type: integer
              description: The priority of the resource.
            provisioningState:
              type: string
              description: 'Provisioning state of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
      required:
        - location
    MonitoredResource:
      type: object
      description: Details of resource being monitored by Dynatrace monitor resource.
      properties:
        id:
          type: string
          description: The ARM id of the resource.
        sendingMetrics:
          type: string
          description: 'Flag indicating if resource is sending metrics to Dynatrace. Known values are: "Enabled" and "Disabled".'
        reasonForMetricsStatus:
          type: string
          description: Reason for why the resource is sending metrics (or why it is not sending).
        sendingLogs:
          type: string
          description: 'Flag indicating if resource is sending logs to Dynatrace. Known values are: "Enabled" and "Disabled".'
        reasonForLogsStatus:
          type: string
          description: Reason for why the resource is sending logs (or why it is not sending).
    PagedMonitoredResource:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredResource"
        nextLink:
          type: string
    VMInfo:
      type: object
      description: Details of VM Resource having Dynatrace OneAgent installed.
      properties:
        resourceId:
          type: string
          description: Azure VM resource ID.
        version:
          type: string
          description: Version of the Dynatrace agent installed on the VM.
        monitoringType:
          type: string
          description: 'The monitoring mode of OneAgent. Known values are: "CLOUD_INFRASTRUCTURE" and "FULL_STACK".'
        autoUpdateSetting:
          type: string
          description: 'Update settings of OneAgent. Known values are: "ENABLED" and "DISABLED".'
        updateStatus:
          type: string
          description: 'The current update status of OneAgent. Known values are: "INCOMPATIBLE", "OUTDATED", "SCHEDULED", "SUPPRESSED", "UNKNOWN", "UP2DATE", "UPDATE_IN_PROGRESS", "UPDATE_PENDING", and "UPDATE_PROBLEM".'
        availabilityState:
          type: string
          description: 'The availability state of OneAgent. Known values are: "CRASHED", "LOST", "MONITORED", "PRE_MONITORED", "SHUTDOWN", "UNEXPECTED_SHUTDOWN", "UNKNOWN", and "UNMONITORED".'
        logModule:
          type: string
          description: 'Tells whether log modules are enabled or not. Known values are: "ENABLED" and "DISABLED".'
        hostGroup:
          type: string
          description: The name of the host group.
        hostName:
          type: string
          description: The name of the host.
    PagedVMInfo:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VMInfo"
        nextLink:
          type: string
    AppServiceInfo:
      type: object
      description: Details of App Services having Dynatrace OneAgent installed.
      properties:
        resourceId:
          type: string
          description: App service resource ID.
        version:
          type: string
          description: Version of the Dynatrace agent installed on the App Service.
        monitoringType:
          type: string
          description: 'The monitoring mode of OneAgent. Known values are: "CLOUD_INFRASTRUCTURE" and "FULL_STACK".'
        autoUpdateSetting:
          type: string
          description: 'Update settings of OneAgent. Known values are: "ENABLED" and "DISABLED".'
        updateStatus:
          type: string
          description: 'The current update status of OneAgent. Known values are: "INCOMPATIBLE", "OUTDATED", "SCHEDULED", "SUPPRESSED", "UNKNOWN", "UP2DATE", "UPDATE_IN_PROGRESS", "UPDATE_PENDING", and "UPDATE_PROBLEM".'
        availabilityState:
          type: string
          description: 'The availability state of OneAgent. Known values are: "CRASHED", "LOST", "MONITORED", "PRE_MONITORED", "SHUTDOWN", "UNEXPECTED_SHUTDOWN", "UNKNOWN", and "UNMONITORED".'
        logModule:
          type: string
          description: 'Tells whether log modules are enabled or not. Known values are: "ENABLED" and "DISABLED".'
        hostGroup:
          type: string
          description: The name of the host group.
        hostName:
          type: string
          description: The name of the host.
    PagedAppServiceInfo:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AppServiceInfo"
        nextLink:
          type: string
    LinkableEnvironmentResponse:
      type: object
      description: Response for getting all the linkable environments.
      properties:
        environmentId:
          type: string
          description: environment id for which user is an admin.
        environmentName:
          type: string
          description: Name of the environment.
        planData:
          $ref: "#/components/schemas/PlanData"
          description: Billing plan information.
    PagedLinkableEnvironmentResponse:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/LinkableEnvironmentResponse"
        nextLink:
          type: string
    VMExtensionPayload:
      type: object
      description: Response of payload to be passed while installing VM agent.
      properties:
        ingestionKey:
          type: string
          description: Ingestion key of the environment.
        environmentId:
          type: string
          description: Id of the environment created.
    MarketplaceSaaSResourceDetailsResponse:
      type: object
      description: Marketplace SaaS resource details linked to the given tenant Id.
      properties:
        marketplaceSaaSResourceId:
          type: string
          description: Id of the Marketplace SaaS Resource.
        planId:
          type: string
          description: Id of the plan.
        marketplaceSubscriptionStatus:
          type: string
          description: 'Marketplace subscription status. Known values are: "Active" and "Suspended".'
    MetricsStatusResponse:
      type: object
      description: Response of get metrics status operation.
      properties:
        azureResourceIds:
          type: array
          items:
            type: string
          description: Azure resource IDs.
    SSODetailsResponse:
      type: object
      description: SSO details from the Dynatrace partner.
      properties:
        isSsoEnabled:
          type: string
          description: 'Whether the SSO is enabled for this resource or not. Known values are: "Enabled" and "Disabled".'
        metadataUrl:
          type: string
          description: URL for Azure AD metadata.
        singleSignOnUrl:
          type: string
          description: The login URL specific to this Dynatrace Environment.
        aadDomains:
          type: array
          items:
            type: string
          description: array of Aad(azure active directory) domains.
        adminUsers:
          type: array
          items:
            type: string
          description: Array of admin user emails.
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    DynatraceSingleSignOnResourceFlattened:
      type: object
      description: Flattened view of DynatraceSingleSignOnResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: System metadata for this resource.
        singleSignOnState:
          type: string
          description: 'State of Single Sign On. Known values are: "Initial", "Enable", "Disable", and "Existing".'
        enterpriseAppId:
          type: string
          description: Version of the Dynatrace agent installed on the VM.
        singleSignOnUrl:
          type: string
          description: The login URL specific to this Dynatrace Environment.
        aadDomains:
          type: array
          items:
            type: string
          description: array of Aad(azure active directory) domains.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    PagedDynatraceSingleSignOnResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DynatraceSingleSignOnResourceFlattened"
        nextLink:
          type: string
    DynatraceSingleSignOnResource:
      type: object
      description: Single sign-on configurations for a given monitor resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: System metadata for this resource.
        properties:
          type: object
          properties:
            singleSignOnState:
              type: string
              description: 'State of Single Sign On. Known values are: "Initial", "Enable", "Disable", and "Existing".'
            enterpriseAppId:
              type: string
              description: Version of the Dynatrace agent installed on the VM.
            singleSignOnUrl:
              type: string
              description: The login URL specific to this Dynatrace Environment.
            aadDomains:
              type: array
              items:
                type: string
              description: array of Aad(azure active directory) domains.
            provisioningState:
              type: string
              description: 'Provisioning state of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    LogRules:
      type: object
      description: Set of rules for sending logs for the Monitor resource.
      properties:
        sendAadLogs:
          type: string
          description: 'Flag specifying if AAD logs should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        sendSubscriptionLogs:
          type: string
          description: 'Flag specifying if subscription logs should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        sendActivityLogs:
          type: string
          description: 'Flag specifying if activity logs from Azure resources should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        filteringTags:
          type: array
          items:
            $ref: "#/components/schemas/FilteringTag"
          description: List of filtering tags to be used for capturing logs. This only takes effect if SendActivityLogs flag is enabled. If empty, all resources will be captured. If only Exclude action is specified, the rules will apply to the list of all available resources. If Include actions are specified, the rules will only include resources with the associated tags.
    FilteringTag:
      type: object
      description: The definition of a filtering tag. Filtering tags are used for capturing resources and include/exclude them from being monitored.
      properties:
        name:
          type: string
          description: The name (also known as the key) of the tag.
        value:
          type: string
          description: The value of the tag.
        action:
          type: string
          description: 'Valid actions for a filtering tag. Exclusion takes priority over inclusion. Known values are: "Include" and "Exclude".'
    MetricRules:
      type: object
      description: Set of rules for sending metrics for the Monitor resource.
      properties:
        sendingMetrics:
          type: string
          description: 'Flag specifying if metrics from Azure resources should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        filteringTags:
          type: array
          items:
            $ref: "#/components/schemas/FilteringTag"
          description: List of filtering tags to be used for capturing metrics. If empty, all resources will be captured. If only Exclude action is specified, the rules will apply to the list of all available resources. If Include actions are specified, the rules will only include resources with the associated tags.
    TagRuleFlattened:
      type: object
      description: Flattened view of TagRule ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: System metadata for this resource.
        logRules:
          $ref: "#/components/schemas/LogRules"
          description: Set of rules for sending logs for the Monitor resource.
        metricRules:
          $ref: "#/components/schemas/MetricRules"
          description: Set of rules for sending metrics for the Monitor resource.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    PagedTagRuleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TagRuleFlattened"
        nextLink:
          type: string
    TagRule:
      type: object
      description: Tag rules for a monitor resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: System metadata for this resource.
        properties:
          type: object
          properties:
            logRules:
              $ref: "#/components/schemas/LogRules"
              description: Set of rules for sending logs for the Monitor resource.
            metricRules:
              $ref: "#/components/schemas/MetricRules"
              description: Set of rules for sending metrics for the Monitor resource.
            provisioningState:
              type: string
              description: 'Provisioning state of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
  x-stackQL-resources:
    monitors:
      id: azure_isv.dynatrace.monitors
      name: monitors
      title: monitors
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}?api-version=2023-04-27/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}?api-version=2023-04-27/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}?api-version=2023-04-27/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription_id:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Dynatrace.Observability~1monitors?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_monitored_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1listMonitoredResources?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_hosts:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1listHosts?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_app_services:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1listAppServices?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_linkable_environments:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1listLinkableEnvironments?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_vm_host_payload:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1getVMHostPayload?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_marketplace_saas_resource_details:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Dynatrace.Observability~1getMarketplaceSaaSResourceDetails?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_metric_status:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1getMetricStatus?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_sso_details:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1getSSODetails?api-version=2023-04-27/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list_linkable_environments"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/get_sso_details"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/get"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/get_marketplace_saas_resource_details"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list_by_subscription_id"
        insert:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/delete"
    operations:
      id: azure_isv.dynatrace.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Dynatrace.Observability~1operations?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    single_sign_on:
      id: azure_isv.dynatrace.single_sign_on
      name: single_sign_on
      title: single_sign_on
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1singleSignOnConfigurations~1{configuration_name}?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1singleSignOnConfigurations~1{configuration_name}?api-version=2023-04-27/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1singleSignOnConfigurations?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/single_sign_on/methods/get"
          - $ref: "#/components/x-stackQL-resources/single_sign_on/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/single_sign_on/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/single_sign_on/methods/create_or_update"
        delete: []
    tag_rules:
      id: azure_isv.dynatrace.tag_rules
      name: tag_rules
      title: tag_rules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2023-04-27/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2023-04-27/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Dynatrace.Observability~1monitors~1{monitor_name}~1tagRules?api-version=2023-04-27/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/get"
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}?api-version=2023-04-27:
    get:
      operationId: Monitors_get
      description: Get a MonitorResource. Get a MonitorResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitorResourceFlattened"
    put:
      operationId: Monitors_begin_create_or_update
      description: Create a MonitorResource. Create a MonitorResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                identity:
                  $ref: "#/components/schemas/IdentityProperties"
                  description: The managed service identities assigned to this resource.
                properties:
                  type: object
                  properties:
                    monitoringStatus:
                      type: string
                      description: 'Status of the monitor. Known values are: "Enabled" and "Disabled".'
                    marketplaceSubscriptionStatus:
                      type: string
                      description: 'Marketplace subscription status. Known values are: "Active" and "Suspended".'
                    dynatraceEnvironmentProperties:
                      $ref: "#/components/schemas/DynatraceEnvironmentProperties"
                      description: Properties of the Dynatrace environment.
                    userInfo:
                      $ref: "#/components/schemas/UserInfo"
                      description: User info.
                    planData:
                      $ref: "#/components/schemas/PlanData"
                      description: Billing plan information.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitorResource"
    patch:
      operationId: Monitors_update
      description: Update a MonitorResource. Update a MonitorResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitorResource"
    delete:
      operationId: Monitors_begin_delete
      description: Delete a MonitorResource. Delete a MonitorResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Dynatrace.Observability/monitors?api-version=2023-04-27:
    get:
      operationId: Monitors_list_by_subscription_id
      description: List all MonitorResource by subscriptionId. List all MonitorResource by subscriptionId.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitorResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors?api-version=2023-04-27:
    get:
      operationId: Monitors_list_by_resource_group
      description: List MonitorResource resources by resource group. List MonitorResource resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitorResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/listMonitoredResources?api-version=2023-04-27:
    post:
      operationId: Monitors_list_monitored_resources
      description: List the resources currently being monitored by the Dynatrace monitor resource. List the resources currently being monitored by the Dynatrace monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitoredResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/listHosts?api-version=2023-04-27:
    post:
      operationId: Monitors_list_hosts
      description: List the VM/VMSS resources currently being monitored by the Dynatrace resource. List the VM/VMSS resources currently being monitored by the Dynatrace resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVMInfo"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/listAppServices?api-version=2023-04-27:
    post:
      operationId: Monitors_list_app_services
      description: Gets list of App Services with Dynatrace PaaS OneAgent enabled. Gets list of App Services with Dynatrace PaaS OneAgent enabled.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAppServiceInfo"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/listLinkableEnvironments?api-version=2023-04-27:
    post:
      operationId: Monitors_list_linkable_environments
      description: Gets all the Dynatrace environments that a user can link a azure resource to. Gets all the Dynatrace environments that a user can link a azure resource to.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tenantId:
                  type: string
                  description: Tenant Id of the user in which they want to link the environment. Required.
                userPrincipal:
                  type: string
                  description: user principal id of the user. Required.
                region:
                  type: string
                  description: Azure region in which we want to link the environment. Required.
              required:
                - tenantId
                - userPrincipal
                - region
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLinkableEnvironmentResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/getVMHostPayload?api-version=2023-04-27:
    post:
      operationId: Monitors_get_vm_host_payload
      description: Returns the payload that needs to be passed in the request body for installing Dynatrace agent on a VM. Returns the payload that needs to be passed in the request body for installing Dynatrace agent on a VM.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VMExtensionPayload"
  /subscriptions/{subscription_id}/providers/Dynatrace.Observability/getMarketplaceSaaSResourceDetails?api-version=2023-04-27:
    post:
      operationId: Monitors_get_marketplace_saas_resource_details
      description: Get Marketplace SaaS resource details of a tenant under a specific subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tenantId:
                  type: string
                  description: Tenant Id. Required.
              required:
                - tenantId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketplaceSaaSResourceDetailsResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/getMetricStatus?api-version=2023-04-27:
    post:
      operationId: Monitors_get_metric_status
      description: Get metric status.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitor resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricsStatusResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/getSSODetails?api-version=2023-04-27:
    post:
      operationId: Monitors_get_sso_details
      description: Gets the SSO configuration details from the partner. Gets the SSO configuration details from the partner.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                userPrincipal:
                  type: string
                  description: user principal id of the user. Required.
              required:
                - userPrincipal
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SSODetailsResponse"
  /providers/Dynatrace.Observability/operations?api-version=2023-04-27:
    get:
      operationId: dynatrace_list
      description: List the operations for Dynatrace.Observability. List the operations for Dynatrace.Observability.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/singleSignOnConfigurations/{configuration_name}?api-version=2023-04-27:
    get:
      operationId: SingleSignOn_get
      description: Get a DynatraceSingleSignOnResource. Get a DynatraceSingleSignOnResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Single Sign On Configuration Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DynatraceSingleSignOnResourceFlattened"
    put:
      operationId: SingleSignOn_begin_create_or_update
      description: Create a DynatraceSingleSignOnResource. Create a DynatraceSingleSignOnResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Single Sign On Configuration Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    singleSignOnState:
                      type: string
                      description: 'State of Single Sign On. Known values are: "Initial", "Enable", "Disable", and "Existing".'
                    enterpriseAppId:
                      type: string
                      description: Version of the Dynatrace agent installed on the VM.
                    singleSignOnUrl:
                      type: string
                      description: The login URL specific to this Dynatrace Environment.
                    aadDomains:
                      type: array
                      items:
                        type: string
                      description: array of Aad(azure active directory) domains.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DynatraceSingleSignOnResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/singleSignOnConfigurations?api-version=2023-04-27:
    get:
      operationId: SingleSignOn_list
      description: List all DynatraceSingleSignOnResource by monitorName. List all DynatraceSingleSignOnResource by monitorName.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDynatraceSingleSignOnResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/tagRules/{rule_set_name}?api-version=2023-04-27:
    get:
      operationId: TagRules_get
      description: Get a TagRule. Get a TagRule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRuleFlattened"
    put:
      operationId: TagRules_begin_create_or_update
      description: Create a TagRule. Create a TagRule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    logRules:
                      $ref: "#/components/schemas/LogRules"
                      description: Set of rules for sending logs for the Monitor resource.
                    metricRules:
                      $ref: "#/components/schemas/MetricRules"
                      description: Set of rules for sending metrics for the Monitor resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRule"
    delete:
      operationId: TagRules_begin_delete
      description: Delete a TagRule. Delete a TagRule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Dynatrace.Observability/monitors/{monitor_name}/tagRules?api-version=2023-04-27:
    get:
      operationId: TagRules_list
      description: List all TagRule by monitorName. List all TagRule by monitorName.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTagRuleFlattened"
