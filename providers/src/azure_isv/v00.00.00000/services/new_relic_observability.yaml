openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Native New Relic Service
  description: New Relic organizations with unified Azure billing and telemetry forwarding.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-05-01-preview-stackql-generated
  x-serviceAlias: new_relic_observability
  x-sdkPackage: azure-mgmt-newrelicobservability
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    AccountResourceFlattened:
      type: object
      description: Flattened view of AccountResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        organizationId:
          type: string
          description: organization id.
        accountId:
          type: string
          description: account id.
        accountName:
          type: string
          description: account name.
        region:
          type: string
          description: Region where New Relic account is present.
    PagedAccountResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AccountResourceFlattened"
        nextLink:
          type: string
    MarketplaceSaaSInfo:
      type: object
      description: Marketplace SAAS Info of the resource.
      properties:
        marketplaceSubscriptionId:
          type: string
          description: Marketplace Subscription Id. This is a GUID-formatted string.
        marketplaceSubscriptionName:
          type: string
          description: "Marketplace Subscription Details: SAAS Name."
        marketplaceResourceId:
          type: string
          description: "Marketplace Subscription Details: Resource URI."
        marketplaceStatus:
          type: string
          description: "Marketplace Subscription Details: SaaS Subscription Status."
        billedAzureSubscriptionId:
          type: string
          description: The Azure Subscription ID to which the Marketplace Subscription belongs and gets billed into.
        publisherId:
          type: string
          description: Publisher Id of the Marketplace offer.
        offerId:
          type: string
          description: Offer Id of the Marketplace offer,.
    PartnerBillingEntity:
      type: object
      description: Partner Billing details associated with the resource.
      properties:
        organizationId:
          type: string
          description: The New Relic Organization Id.
        organizationName:
          type: string
          description: The New Relic Organization Name.
    BillingInfoResponse:
      type: object
      description: Marketplace Subscription and Organization details to which resource gets billed into.
      properties:
        marketplaceSaasInfo:
          $ref: "#/components/schemas/MarketplaceSaaSInfo"
          description: Marketplace Subscription details.
        partnerBillingEntity:
          $ref: "#/components/schemas/PartnerBillingEntity"
          description: "Partner Billing Entity details: Organization Info."
    ConnectedPartnerResourceProperties:
      type: object
      description: Connected Partner Resource Properties.
      properties:
        accountName:
          type: string
          description: NewRelic account name.
        accountId:
          type: string
          description: NewRelic Account Id.
        azureResourceId:
          type: string
          description: The azure resource Id of the deployment.
        location:
          type: string
          description: The location of the deployment.
    ConnectedPartnerResourcesListFormatFlattened:
      type: object
      description: Flattened view of ConnectedPartnerResourcesListFormat ('properties' hoisted to the top level).
      properties:
        accountName:
          type: string
          description: NewRelic account name.
        accountId:
          type: string
          description: NewRelic Account Id.
        azureResourceId:
          type: string
          description: The azure resource Id of the deployment.
        location:
          type: string
          description: The location of the deployment.
    PagedConnectedPartnerResourcesListFormatFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ConnectedPartnerResourcesListFormatFlattened"
        nextLink:
          type: string
    SubscriptionList:
      type: object
      description: The request to update subscriptions needed to be monitored by the NewRelic monitor resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        patchOperation:
          type: string
          description: 'The operation for the patch on the resource. Known values are: "AddBegin", "AddComplete", "DeleteBegin", "DeleteComplete", and "Active".'
        monitoredSubscriptionList:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredSubscription"
          description: List of subscriptions and the state of the monitoring.
        provisioningState:
          type: string
          description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    MonitoredSubscription:
      type: object
      description: The list of subscriptions and it's monitoring status by current NewRelic monitor.
      properties:
        subscriptionId:
          type: string
          description: The subscriptionId to be monitored.
        status:
          type: string
          description: 'The state of monitoring. Known values are: "InProgress", "Active", "Failed", and "Deleting".'
        error:
          type: string
          description: The reason of not monitoring the subscription.
        tagRules:
          $ref: "#/components/schemas/MonitoringTagRulesProperties"
          description: The resource-specific properties for this resource.
    MonitoringTagRulesProperties:
      type: object
      description: The resource-specific properties for this resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          type: string
          description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        logRules:
          $ref: "#/components/schemas/LogRules"
          description: Set of rules for sending logs for the Monitor resource.
        metricRules:
          $ref: "#/components/schemas/MetricRules"
          description: Set of rules for sending metrics for the Monitor resource.
    LogRules:
      type: object
      description: Set of rules for sending logs for the Monitor resource.
      properties:
        sendAadLogs:
          type: string
          description: 'Flag specifying if AAD logs should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        sendSubscriptionLogs:
          type: string
          description: 'Flag specifying if subscription logs should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        sendActivityLogs:
          type: string
          description: 'Flag specifying if activity logs from Azure resources should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        filteringTags:
          type: array
          items:
            $ref: "#/components/schemas/FilteringTag"
          description: List of filtering tags to be used for capturing logs. This only takes effect if SendActivityLogs flag is enabled. If empty, all resources will be captured. If only Exclude action is specified, the rules will apply to the list of all available resources. If Include actions are specified, the rules will only include resources with the associated tags.
    FilteringTag:
      type: object
      description: The definition of a filtering tag. Filtering tags are used for capturing resources and include/exclude them from being monitored.
      properties:
        name:
          type: string
          description: The name (also known as the key) of the tag.
        value:
          type: string
          description: The value of the tag.
        action:
          type: string
          description: 'Valid actions for a filtering tag. Exclusion takes priority over inclusion. Known values are: "Include" and "Exclude".'
    MetricRules:
      type: object
      description: Set of rules for sending metrics for the Monitor resource.
      properties:
        sendMetrics:
          type: string
          description: 'Flag specifying if metrics should be sent for the Monitor resource. Known values are: "Enabled" and "Disabled".'
        filteringTags:
          type: array
          items:
            $ref: "#/components/schemas/FilteringTag"
          description: List of filtering tags to be used for capturing metrics.
        userEmail:
          type: string
          description: User Email.
    MonitoredSubscriptionPropertiesFlattened:
      type: object
      description: Flattened view of MonitoredSubscriptionProperties ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: Name of the monitored subscription resource.
        id:
          type: string
          description: The id of the monitored subscription resource.
        type:
          type: string
          description: The type of the monitored subscription resource.
        patchOperation:
          type: string
          description: 'The operation for the patch on the resource. Known values are: "AddBegin", "AddComplete", "DeleteBegin", "DeleteComplete", and "Active".'
        monitoredSubscriptionList:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredSubscription"
          description: List of subscriptions and the state of the monitoring.
        provisioningState:
          type: string
          description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    PagedMonitoredSubscriptionPropertiesFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredSubscriptionPropertiesFlattened"
        nextLink:
          type: string
    MonitoredSubscriptionProperties:
      type: object
      description: The request to update subscriptions needed to be monitored by the NewRelic monitor resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Name of the monitored subscription resource.
        id:
          type: string
          description: The id of the monitored subscription resource.
        type:
          type: string
          description: The type of the monitored subscription resource.
        properties:
          $ref: "#/components/schemas/SubscriptionList"
          description: The request to update subscriptions needed to be monitored by the NewRelic monitor resource.
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities). Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          type: string
          description: 'Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned, UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: "The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}. The dictionary values can be empty objects ({}) in requests."
      required:
        - type
    NewRelicAccountProperties:
      type: object
      description: Properties of the NewRelic account.
      properties:
        userId:
          type: string
          description: User id.
        accountInfo:
          $ref: "#/components/schemas/AccountInfo"
          description: NewRelic Account Information.
        organizationInfo:
          $ref: "#/components/schemas/OrganizationInfo"
          description: NewRelic Organization Information.
        singleSignOnProperties:
          $ref: "#/components/schemas/NewRelicSingleSignOnProperties"
          description: date when plan was applied.
    AccountInfo:
      type: object
      description: Account Info of the NewRelic account.
      properties:
        accountId:
          type: string
          description: Account id.
        ingestionKey:
          type: string
          description: ingestion key of account.
        region:
          type: string
          description: Region where New Relic account is present.
    OrganizationInfo:
      type: object
      description: Organization Info of the NewRelic account.
      properties:
        organizationId:
          type: string
          description: Organization id.
    NewRelicSingleSignOnProperties:
      type: object
      description: Single sign on Info of the NewRelic account.
      properties:
        singleSignOnState:
          type: string
          description: 'Single sign-on state. Known values are: "Initial", "Enable", "Disable", and "Existing".'
        enterpriseAppId:
          type: string
          description: The Id of the Enterprise App used for Single sign-on.
        singleSignOnUrl:
          type: string
          description: The login URL specific to this NewRelic Organization.
        provisioningState:
          type: string
          description: 'Provisioning state. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
    UserInfo:
      type: object
      description: User Info of NewRelic Monitor resource.
      properties:
        firstName:
          type: string
          description: First name.
        lastName:
          type: string
          description: Last name.
        emailAddress:
          type: string
          description: User Email.
        phoneNumber:
          type: string
          description: Contact phone number.
        country:
          type: string
          description: country if user.
    PlanData:
      type: object
      description: Plan data of NewRelic Monitor resource.
      properties:
        usageType:
          type: string
          description: 'Different usage type like PAYG/COMMITTED. this could be enum. Known values are: "PAYG" and "COMMITTED".'
        billingCycle:
          type: string
          description: Different billing cycles like Monthly/Weekly.
        planDetails:
          type: string
          description: plan id as published by NewRelic.
        effectiveDate:
          type: string
          format: date-time
          description: date when plan was applied.
    SaaSData:
      type: object
      description: SaaS details.
      properties:
        saaSResourceId:
          type: string
          description: SaaS resource id.
    NewRelicMonitorResourceFlattened:
      type: object
      description: Flattened view of NewRelicMonitorResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        provisioningState:
          type: string
          description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        monitoringStatus:
          type: string
          description: 'MonitoringStatus of the resource. Known values are: "Enabled" and "Disabled".'
        marketplaceSubscriptionStatus:
          type: string
          description: 'NewRelic Organization properties of the resource. Known values are: "Active" and "Suspended".'
        marketplaceSubscriptionId:
          type: string
          description: Marketplace Subscription Id.
        newRelicAccountProperties:
          $ref: "#/components/schemas/NewRelicAccountProperties"
          description: MarketplaceSubscriptionStatus of the resource.
        userInfo:
          $ref: "#/components/schemas/UserInfo"
          description: User Info.
        planData:
          $ref: "#/components/schemas/PlanData"
          description: Plan details.
        saaSData:
          $ref: "#/components/schemas/SaaSData"
          description: SaaS details.
        liftrResourceCategory:
          type: string
          description: 'Liftr resource category. Known values are: "Unknown" and "MonitorLogs".'
        liftrResourcePreference:
          type: integer
          description: Liftr resource preference. The priority of the resource.
        orgCreationSource:
          type: string
          description: 'Source of org creation. Known values are: "LIFTR" and "NEWRELIC".'
        accountCreationSource:
          type: string
          description: 'Source of account creation. Known values are: "LIFTR" and "NEWRELIC".'
        subscriptionState:
          type: string
          description: State of the Azure Subscription containing the monitor resource.
        saaSAzureSubscriptionStatus:
          type: string
          description: Status of Azure Subscription where Marketplace SaaS is located.
    PagedNewRelicMonitorResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/NewRelicMonitorResourceFlattened"
        nextLink:
          type: string
    NewRelicMonitorResource:
      type: object
      description: A Monitor Resource by NewRelic. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        properties:
          type: object
          properties:
            provisioningState:
              type: string
              description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
            monitoringStatus:
              type: string
              description: 'MonitoringStatus of the resource. Known values are: "Enabled" and "Disabled".'
            marketplaceSubscriptionStatus:
              type: string
              description: 'NewRelic Organization properties of the resource. Known values are: "Active" and "Suspended".'
            marketplaceSubscriptionId:
              type: string
              description: Marketplace Subscription Id.
            newRelicAccountProperties:
              $ref: "#/components/schemas/NewRelicAccountProperties"
              description: MarketplaceSubscriptionStatus of the resource.
            userInfo:
              $ref: "#/components/schemas/UserInfo"
              description: User Info.
            planData:
              $ref: "#/components/schemas/PlanData"
              description: Plan details.
            saaSData:
              $ref: "#/components/schemas/SaaSData"
              description: SaaS details.
            liftrResourceCategory:
              type: string
              description: 'Liftr resource category. Known values are: "Unknown" and "MonitorLogs".'
            liftrResourcePreference:
              type: integer
              description: Liftr resource preference. The priority of the resource.
            orgCreationSource:
              type: string
              description: 'Source of org creation. Known values are: "LIFTR" and "NEWRELIC".'
            accountCreationSource:
              type: string
              description: 'Source of account creation. Known values are: "LIFTR" and "NEWRELIC".'
            subscriptionState:
              type: string
              description: State of the Azure Subscription containing the monitor resource.
            saaSAzureSubscriptionStatus:
              type: string
              description: Status of Azure Subscription where Marketplace SaaS is located.
      required:
        - location
    AppServiceInfo:
      type: object
      description: Details of VM Resource having NewRelic OneAgent installed.
      properties:
        azureResourceId:
          type: string
          description: Azure App service resource ID.
        agentVersion:
          type: string
          description: Version of the NewRelic agent installed on the App service.
        agentStatus:
          type: string
          description: Status of the NewRelic agent installed on the App service.
    PagedAppServiceInfo:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AppServiceInfo"
        nextLink:
          type: string
    VMInfo:
      type: object
      description: Details of VM Resource having NewRelic OneAgent installed.
      properties:
        vmId:
          type: string
          description: Azure VM resource ID.
        agentVersion:
          type: string
          description: Version of the NewRelic agent installed on the VM.
        agentStatus:
          type: string
          description: Status of the NewRelic agent installed on the VM.
    PagedVMInfo:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VMInfo"
        nextLink:
          type: string
    MonitoredResource:
      type: object
      description: Details of resource being monitored by NewRelic monitor resource.
      properties:
        id:
          type: string
          description: The ARM id of the resource.
        sendingMetrics:
          type: string
          description: 'Flag indicating if resource is sending metrics to NewRelic. Known values are: "Enabled" and "Disabled".'
        reasonForMetricsStatus:
          type: string
          description: Reason for why the resource is sending metrics (or why it is not sending).
        sendingLogs:
          type: string
          description: 'Flag indicating if resource is sending logs to NewRelic. Known values are: "Enabled" and "Disabled".'
        reasonForLogsStatus:
          type: string
          description: Reason for why the resource is sending logs (or why it is not sending).
    PagedMonitoredResource:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredResource"
        nextLink:
          type: string
    LinkedResource:
      type: object
      description: The definition of a linked resource.
      properties:
        id:
          type: string
          description: The ARM id of the linked resource.
    PagedLinkedResource:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/LinkedResource"
        nextLink:
          type: string
    MetricsStatusResponse:
      type: object
      description: Response of get metrics status Operation.
      properties:
        azureResourceIds:
          type: array
          items:
            type: string
          description: Azure resource IDs.
    VMExtensionPayload:
      type: object
      description: Response of payload to be passed while installing VM agent.
      properties:
        ingestionKey:
          type: string
          description: Ingestion key of the account.
    LatestLinkedSaaSResponse:
      type: object
      description: Response of get latest linked SaaS resource operation.
      properties:
        saaSResourceId:
          type: string
          description: SaaS resource id.
        isHiddenSaaS:
          type: boolean
          description: Flag indicating if the SaaS resource is hidden.
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    OrganizationResourceFlattened:
      type: object
      description: Flattened view of OrganizationResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        organizationId:
          type: string
          description: organization id.
        organizationName:
          type: string
          description: organization name.
        billingSource:
          type: string
          description: 'Billing source. Known values are: "AZURE" and "NEWRELIC".'
    PagedOrganizationResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OrganizationResourceFlattened"
        nextLink:
          type: string
    PlanDataResourceFlattened:
      type: object
      description: Flattened view of PlanDataResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        planData:
          $ref: "#/components/schemas/PlanData"
          description: Plan details.
        orgCreationSource:
          type: string
          description: 'Source of org creation. Known values are: "LIFTR" and "NEWRELIC".'
        accountCreationSource:
          type: string
          description: 'Source of account creation. Known values are: "LIFTR" and "NEWRELIC".'
    PagedPlanDataResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PlanDataResourceFlattened"
        nextLink:
          type: string
    SaaSResourceDetailsResponse:
      type: object
      description: Marketplace SaaS resource details. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        saasId:
          type: string
          description: Id of the Marketplace SaaS Resource.
    TagRuleFlattened:
      type: object
      description: Flattened view of TagRule ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          type: string
          description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        logRules:
          $ref: "#/components/schemas/LogRules"
          description: Set of rules for sending logs for the Monitor resource.
        metricRules:
          $ref: "#/components/schemas/MetricRules"
          description: Set of rules for sending metrics for the Monitor resource.
    PagedTagRuleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TagRuleFlattened"
        nextLink:
          type: string
    TagRule:
      type: object
      description: A tag rule belonging to NewRelic account. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            provisioningState:
              type: string
              description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
            logRules:
              $ref: "#/components/schemas/LogRules"
              description: Set of rules for sending logs for the Monitor resource.
            metricRules:
              $ref: "#/components/schemas/MetricRules"
              description: Set of rules for sending metrics for the Monitor resource.
  x-stackQL-resources:
    accounts:
      id: azure_isv.new_relic_observability.accounts
      name: accounts
      title: accounts
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1NewRelic.Observability~1accounts?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/list"
        insert: []
        update: []
        delete: []
    billing_info:
      id: azure_isv.new_relic_observability.billing_info
      name: billing_info
      title: billing_info
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1getBillingInfo?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/billing_info/methods/get"
        insert: []
        update: []
        delete: []
    connected_partner_resources:
      id: azure_isv.new_relic_observability.connected_partner_resources
      name: connected_partner_resources
      title: connected_partner_resources
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1listConnectedPartnerResources?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/connected_partner_resources/methods/list"
        insert: []
        update: []
        delete: []
    monitored_subscriptions:
      id: azure_isv.new_relic_observability.monitored_subscriptions
      name: monitored_subscriptions
      title: monitored_subscriptions
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2025-05-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2025-05-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2025-05-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1monitoredSubscriptions?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/get"
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/delete"
    monitors:
      id: azure_isv.new_relic_observability.monitors
      name: monitors
      title: monitors
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}?api-version=2025-05-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}?api-version=2025-05-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}?api-version=2025-05-01-preview/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1NewRelic.Observability~1monitors?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_app_services:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1listAppServices?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_hosts:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1listHosts?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_monitored_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1monitoredResources?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_linked_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1listLinkedResources?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        get_metric_rules:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1getMetricRules?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_metric_status:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1getMetricStatus?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        switch_billing:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1switchBilling?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        refresh_ingestion_key:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1refreshIngestionKey?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
        vm_host_payload:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1vmHostPayloads?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        latest_linked_saas:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1latestLinkedSaaS?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        link_saas:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1linkSaaS?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        resubscribe:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1resubscribe?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list_app_services"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/get"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/delete"
    operations:
      id: azure_isv.new_relic_observability.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1NewRelic.Observability~1operations?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    organizations:
      id: azure_isv.new_relic_observability.organizations
      name: organizations
      title: organizations
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1NewRelic.Observability~1organizations?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/list"
        insert: []
        update: []
        delete: []
    plans:
      id: azure_isv.new_relic_observability.plans
      name: plans
      title: plans
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1NewRelic.Observability~1plans?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/plans/methods/list"
        insert: []
        update: []
        delete: []
    saas:
      id: azure_isv.new_relic_observability.saas
      name: saas
      title: saas
      methods:
        activate_resource:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1NewRelic.Observability~1activateSaaS?api-version=2025-05-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
    tag_rules:
      id: azure_isv.new_relic_observability.tag_rules
      name: tag_rules
      title: tag_rules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2025-05-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2025-05-01-preview/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2025-05-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_new_relic_monitor_resource:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1NewRelic.Observability~1monitors~1{monitor_name}~1tagRules?api-version=2025-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/get"
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/list_by_new_relic_monitor_resource"
        insert:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/delete"
paths:
  /subscriptions/{subscription_id}/providers/NewRelic.Observability/accounts?api-version=2025-05-01-preview:
    get:
      operationId: Accounts_list
      description: Lists all the New Relic accounts linked to your email address, helping you understand the existing accounts that have been created.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: userEmail
          in: query
          required: true
          schema:
            type: string
          description: User Email. Required.
        - name: location
          in: query
          required: true
          schema:
            type: string
          description: Location for NewRelic. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAccountResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/getBillingInfo?api-version=2025-05-01-preview:
    post:
      operationId: BillingInfo_get
      description: Retrieves marketplace and organization information mapped to the given New Relic monitor resource. Retrieves marketplace and organization information mapped to the given New Relic monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingInfoResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/listConnectedPartnerResources?api-version=2025-05-01-preview:
    post:
      operationId: ConnectedPartnerResources_list
      description: List of all active deployments that are associated with the marketplace subscription linked to the given monitor. List of all active deployments that are associated with the marketplace subscription linked to the given monitor.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConnectedPartnerResourcesListFormatFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/monitoredSubscriptions/{configuration_name}?api-version=2025-05-01-preview:
    get:
      operationId: MonitoredSubscriptions_get
      description: Lists all the subscriptions currently being monitored by the NewRelic monitor resource. Lists all the subscriptions currently being monitored by the NewRelic monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The configuration name. Only 'default' value is supported. "default" Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoredSubscriptionPropertiesFlattened"
    put:
      operationId: MonitoredSubscriptions_begin_create_or_update
      description: Add subscriptions to be monitored by the New Relic monitor resource, enabling observability and monitoring. Add subscriptions to be monitored by the New Relic monitor resource, enabling observability and monitoring.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The configuration name. Only 'default' value is supported. "default" Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/SubscriptionList"
                  description: The request to update subscriptions needed to be monitored by the NewRelic monitor resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoredSubscriptionProperties"
    patch:
      operationId: MonitoredSubscriptions_begin_update
      description: Update subscriptions to be monitored by the New Relic monitor resource, ensuring optimal observability and performance. Update subscriptions to be monitored by the New Relic monitor resource, ensuring optimal observability and performance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The configuration name. Only 'default' value is supported. "default" Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/SubscriptionList"
                  description: The request to update subscriptions needed to be monitored by the NewRelic monitor resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoredSubscriptionProperties"
    delete:
      operationId: MonitoredSubscriptions_begin_delete
      description: Delete subscriptions being monitored by the New Relic monitor resource, removing their observability and monitoring capabilities. Delete subscriptions being monitored by the New Relic monitor resource, removing their observability and monitoring capabilities.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The configuration name. Only 'default' value is supported. "default" Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/monitoredSubscriptions?api-version=2025-05-01-preview:
    get:
      operationId: MonitoredSubscriptions_list
      description: Lists all the subscriptions currently being monitored by the NewRelic monitor resource. Lists all the subscriptions currently being monitored by the NewRelic monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitoredSubscriptionPropertiesFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}?api-version=2025-05-01-preview:
    get:
      operationId: Monitors_get
      description: Retrieves the properties and configuration details of a specific New Relic monitor resource, providing insight into its setup and status.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewRelicMonitorResourceFlattened"
    put:
      operationId: Monitors_begin_create_or_update
      description: Creates a new or updates an existing New Relic monitor resource in your Azure subscription. This sets up the integration between Azure and your New Relic account, enabling observability and monitoring of your Azure resources through New Relic.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                properties:
                  type: object
                  properties:
                    newRelicAccountProperties:
                      $ref: "#/components/schemas/NewRelicAccountProperties"
                      description: MarketplaceSubscriptionStatus of the resource.
                    userInfo:
                      $ref: "#/components/schemas/UserInfo"
                      description: User Info.
                    planData:
                      $ref: "#/components/schemas/PlanData"
                      description: Plan details.
                    saaSData:
                      $ref: "#/components/schemas/SaaSData"
                      description: SaaS details.
                    orgCreationSource:
                      type: string
                      description: 'Source of org creation. Known values are: "LIFTR" and "NEWRELIC".'
                    accountCreationSource:
                      type: string
                      description: 'Source of account creation. Known values are: "LIFTR" and "NEWRELIC".'
                    subscriptionState:
                      type: string
                      description: State of the Azure Subscription containing the monitor resource.
                    saaSAzureSubscriptionStatus:
                      type: string
                      description: Status of Azure Subscription where Marketplace SaaS is located.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewRelicMonitorResource"
    patch:
      operationId: Monitors_begin_update
      description: Updates an existing New Relic monitor resource from your Azure subscription.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  type: object
                  properties:
                    newRelicAccountProperties:
                      $ref: "#/components/schemas/NewRelicAccountProperties"
                      description: MarketplaceSubscriptionStatus of the resource.
                    userInfo:
                      $ref: "#/components/schemas/UserInfo"
                      description: User Info.
                    planData:
                      $ref: "#/components/schemas/PlanData"
                      description: Plan details.
                    saaSData:
                      $ref: "#/components/schemas/SaaSData"
                      description: SaaS details.
                    orgCreationSource:
                      type: string
                      description: 'Source of org creation. Known values are: "LIFTR" and "NEWRELIC".'
                    accountCreationSource:
                      type: string
                      description: 'Source of account creation. Known values are: "LIFTR" and "NEWRELIC".'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewRelicMonitorResource"
    delete:
      operationId: Monitors_begin_delete
      description: Deletes an existing New Relic monitor resource from your Azure subscription, removing the integration and stopping the observability of your Azure resources through New Relic.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: userEmail
          in: query
          required: true
          schema:
            type: string
          description: User Email. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/NewRelic.Observability/monitors?api-version=2025-05-01-preview:
    get:
      operationId: Monitors_list_by_subscription
      description: Lists all New Relic monitor resources either within a specific subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedNewRelicMonitorResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors?api-version=2025-05-01-preview:
    get:
      operationId: Monitors_list_by_resource_group
      description: Retrieves a list of all New Relic monitor resources either a specific resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedNewRelicMonitorResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/listAppServices?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_list_app_services
      description: Lists the app service resources currently being monitored by the New Relic resource, helping you understand which app services are under monitoring.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                azureResourceIds:
                  type: array
                  items:
                    type: string
                  description: Azure resource IDs.
                userEmail:
                  type: string
                  description: User Email. Required.
              required:
                - userEmail
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAppServiceInfo"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/listHosts?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_list_hosts
      description: Lists all VM resources currently being monitored by the New Relic monitor resource, helping you manage observability.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                vmIds:
                  type: array
                  items:
                    type: string
                  description: VM resource IDs.
                userEmail:
                  type: string
                  description: User Email. Required.
              required:
                - userEmail
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVMInfo"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/monitoredResources?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_list_monitored_resources
      description: Lists all Azure resources that are currently being monitored by the specified New Relic monitor resource, providing insight into the coverage of your observability setup.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitoredResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/listLinkedResources?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_list_linked_resources
      description: Lists all Azure resources that are linked to the same New Relic organization as the specified monitor resource, helping you understand the scope of integration. Lists all Azure resources that are linked to the same New Relic organization as the specified monitor resource, helping you understand the scope of integration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLinkedResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/getMetricRules?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_get_metric_rules
      description: Retrieves the metric rules that are configured in the New Relic monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                userEmail:
                  type: string
                  description: User Email. Required.
              required:
                - userEmail
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricRules"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/getMetricStatus?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_get_metric_status
      description: Retrieves the metric status that are configured in the New Relic monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                azureResourceIds:
                  type: array
                  items:
                    type: string
                  description: Azure resource IDs.
                userEmail:
                  type: string
                  description: User Email. Required.
              required:
                - userEmail
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricsStatusResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/switchBilling?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_switch_billing
      description: Switches the billing for the New Relic Monitor resource to be billed by Azure Marketplace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                azureResourceId:
                  type: string
                  description: Azure resource Id.
                organizationId:
                  type: string
                  description: Organization id.
                planData:
                  $ref: "#/components/schemas/PlanData"
                  description: Plan details.
                userEmail:
                  type: string
                  description: User Email. Required.
              required:
                - userEmail
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewRelicMonitorResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/refreshIngestionKey?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_refresh_ingestion_key
      description: Refreshes the ingestion key for all monitors linked to the same account associated to the underlying monitor.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/vmHostPayloads?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_vm_host_payload
      description: Returns the payload that needs to be passed in the request body for installing the New Relic agent on a VM, providing the necessary configuration details.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VMExtensionPayload"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/latestLinkedSaaS?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_latest_linked_saas
      description: Returns the latest SaaS linked to the newrelic organization of the underlying monitor.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LatestLinkedSaaSResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/linkSaaS?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_begin_link_saas
      description: Links a new SaaS to the newrelic organization of the underlying monitor. Links a new SaaS to the newrelic organization of the underlying monitor.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                saaSResourceId:
                  type: string
                  description: SaaS resource id.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewRelicMonitorResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/resubscribe?api-version=2025-05-01-preview:
    post:
      operationId: Monitors_begin_resubscribe
      description: Resubscribes the New Relic Organization of the underline Monitor Resource to be billed by Azure Marketplace. Resubscribes the New Relic Organization of the underline Monitor Resource to be billed by Azure Marketplace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                planId:
                  type: string
                  description: Newly selected plan Id to create the new Marketplace subscription for Resubscribe.
                termId:
                  type: string
                  description: Newly selected term Id to create the new Marketplace subscription for Resubscribe.
                subscriptionId:
                  type: string
                  description: Newly selected Azure Subscription Id in which the new Marketplace subscription will be created for Resubscribe.
                resourceGroup:
                  type: string
                  description: Newly selected Azure resource group in which the new Marketplace subscription will be created for Resubscribe.
                organizationId:
                  type: string
                  description: Organization Id of the NewRelic Organization that needs to be resubscribed.
                publisherId:
                  type: string
                  description: Publisher Id of the NewRelic offer that needs to be resubscribed.
                offerId:
                  type: string
                  description: Offer Id of the NewRelic offer that needs to be resubscribed.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewRelicMonitorResource"
  /providers/NewRelic.Observability/operations?api-version=2025-05-01-preview:
    get:
      operationId: newrelicobservability_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/providers/NewRelic.Observability/organizations?api-version=2025-05-01-preview:
    get:
      operationId: Organizations_list
      description: Lists all the New Relic organizations linked to your email address, helping you understand the existing organizations that have been created.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: userEmail
          in: query
          required: true
          schema:
            type: string
          description: User Email. Required.
        - name: location
          in: query
          required: true
          schema:
            type: string
          description: Location for NewRelic. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOrganizationResourceFlattened"
  /subscriptions/{subscription_id}/providers/NewRelic.Observability/plans?api-version=2025-05-01-preview:
    get:
      operationId: Plans_list
      description: Lists the plans data linked to your organization, providing an overview of the available plans.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: accountId
          in: query
          required: false
          schema:
            type: string
          description: Account Id. Default value is None.
        - name: organizationId
          in: query
          required: false
          schema:
            type: string
          description: Organization Id. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPlanDataResourceFlattened"
  /subscriptions/{subscription_id}/providers/NewRelic.Observability/activateSaaS?api-version=2025-05-01-preview:
    post:
      operationId: SaaS_activate_resource
      description: Resolve the token to get the SaaS resource ID and activate the SaaS resource.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                saasGuid:
                  type: string
                  description: SaaS guid for Activate and Validate SaaS Resource. Required.
                publisherId:
                  type: string
                  description: Publisher Id for NewRelic resource. Required.
              required:
                - saasGuid
                - publisherId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SaaSResourceDetailsResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/tagRules/{rule_set_name}?api-version=2025-05-01-preview:
    get:
      operationId: TagRules_get
      description: Retrieves the details of the tag rules for a specific New Relic monitor resource, providing insight into its setup and status.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the TagRule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRuleFlattened"
    put:
      operationId: TagRules_begin_create_or_update
      description: Creates a new set of tag rules for a specific New Relic monitor resource, determining which Azure resources are monitored based on their tags.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the TagRule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    logRules:
                      $ref: "#/components/schemas/LogRules"
                      description: Set of rules for sending logs for the Monitor resource.
                    metricRules:
                      $ref: "#/components/schemas/MetricRules"
                      description: Set of rules for sending metrics for the Monitor resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRule"
    delete:
      operationId: TagRules_begin_delete
      description: Deletes a tag rule set for a given New Relic monitor resource, removing fine-grained control over observability based on resource tags.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the TagRule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: TagRules_update
      description: Updates the tag rules for a specific New Relic monitor resource, allowing you to modify the rules that control which Azure resources are monitored.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the TagRule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    logRules:
                      $ref: "#/components/schemas/LogRules"
                      description: Set of rules for sending logs for the Monitor resource.
                    metricRules:
                      $ref: "#/components/schemas/MetricRules"
                      description: Set of rules for sending metrics for the Monitor resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRule"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/NewRelic.Observability/monitors/{monitor_name}/tagRules?api-version=2025-05-01-preview:
    get:
      operationId: TagRules_list_by_new_relic_monitor_resource
      description: Lists all tag rules associated with a specific New Relic monitor resource, helping you manage and audit the rules that control resource monitoring.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Monitors resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTagRuleFlattened"
