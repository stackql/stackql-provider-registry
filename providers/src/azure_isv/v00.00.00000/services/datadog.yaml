openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Datadog - An Azure Native ISV Service
  description: Datadog organizations with unified Azure billing, log and metric forwarding.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-01-01-stackql-generated
  x-serviceAlias: datadog
  x-sdkPackage: azure-mgmt-datadog
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    CreateResourceSupportedProperties:
      type: object
      description: Datadog resource can be created or not properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The ARM id of the subscription.
        creationSupported:
          type: boolean
          description: Indicates if selected subscription supports Datadog resource creation, if not it is already being monitored for the selected organization via multi subscription feature.
    CreateResourceSupportedResponseFlattened:
      type: object
      description: Flattened view of CreateResourceSupportedResponse ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: The ARM id of the subscription.
        creationSupported:
          type: boolean
          description: Indicates if selected subscription supports Datadog resource creation, if not it is already being monitored for the selected organization via multi subscription feature.
    PagedCreateResourceSupportedResponseFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CreateResourceSupportedResponseFlattened"
        nextLink:
          type: string
    DatadogAgreementProperties:
      type: object
      description: Terms properties.
      properties:
        publisher:
          type: string
          description: Publisher identifier string.
        product:
          type: string
          description: Product identifier string.
        plan:
          type: string
          description: Plan identifier string.
        licenseTextLink:
          type: string
          description: Link to HTML with Microsoft and Publisher terms.
        privacyPolicyLink:
          type: string
          description: Link to the privacy policy of the publisher.
        retrieveDatetime:
          type: string
          format: date-time
          description: Date and time in UTC of when the terms were accepted. This is empty if Accepted is false.
        signature:
          type: string
          description: Terms signature.
        accepted:
          type: boolean
          description: If any version of the terms have been accepted, otherwise false.
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    DatadogAgreementResourceFlattened:
      type: object
      description: Flattened view of DatadogAgreementResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: ARM id of the resource.
        name:
          type: string
          description: Name of the agreement.
        type:
          type: string
          description: The type of the resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        publisher:
          type: string
          description: Publisher identifier string.
        product:
          type: string
          description: Product identifier string.
        plan:
          type: string
          description: Plan identifier string.
        licenseTextLink:
          type: string
          description: Link to HTML with Microsoft and Publisher terms.
        privacyPolicyLink:
          type: string
          description: Link to the privacy policy of the publisher.
        retrieveDatetime:
          type: string
          format: date-time
          description: Date and time in UTC of when the terms were accepted. This is empty if Accepted is false.
        signature:
          type: string
          description: Terms signature.
        accepted:
          type: boolean
          description: If any version of the terms have been accepted, otherwise false.
    PagedDatadogAgreementResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DatadogAgreementResourceFlattened"
        nextLink:
          type: string
    DatadogAgreementResource:
      type: object
      description: DatadogAgreementResource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: ARM id of the resource.
        name:
          type: string
          description: Name of the agreement.
        type:
          type: string
          description: The type of the resource.
        properties:
          $ref: "#/components/schemas/DatadogAgreementProperties"
          description: Represents the properties of the resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
    SubscriptionList:
      type: object
      description: The request to update subscriptions needed to be monitored by the Datadog monitor resource.
      properties:
        operation:
          type: string
          description: 'The operation for the patch on the resource. Known values are: "AddBegin", "AddComplete", "DeleteBegin", "DeleteComplete", and "Active".'
        monitoredSubscriptionList:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredSubscription"
          description: List of subscriptions and the state of the monitoring.
    MonitoredSubscription:
      type: object
      description: The list of subscriptions and it's monitoring status by current Datadog monitor.
      properties:
        subscriptionId:
          type: string
          description: The subscriptionId to be monitored.
        status:
          type: string
          description: 'The state of monitoring. Known values are: "InProgress", "Active", "Failed", and "Deleting".'
        error:
          type: string
          description: The reason of not monitoring the subscription.
        tagRules:
          $ref: "#/components/schemas/MonitoringTagRulesProperties"
          description: Definition of the properties for a TagRules resource.
    MonitoringTagRulesProperties:
      type: object
      description: Definition of the properties for a TagRules resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          type: string
          description: 'Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        logRules:
          $ref: "#/components/schemas/LogRules"
          description: Set of rules for sending logs for the Monitor resource.
        metricRules:
          $ref: "#/components/schemas/MetricRules"
          description: Set of rules for sending metrics for the Monitor resource.
        automuting:
          type: boolean
          description: Configuration to enable/disable auto-muting flag.
    LogRules:
      type: object
      description: Set of rules for sending logs for the Monitor resource.
      properties:
        sendAadLogs:
          type: boolean
          description: Flag specifying if AAD logs should be sent for the Monitor resource.
        sendSubscriptionLogs:
          type: boolean
          description: Flag specifying if Azure subscription logs should be sent for the Monitor resource.
        sendResourceLogs:
          type: boolean
          description: Flag specifying if Azure resource logs should be sent for the Monitor resource.
        filteringTags:
          type: array
          items:
            $ref: "#/components/schemas/FilteringTag"
          description: List of filtering tags to be used for capturing logs. This only takes effect if SendResourceLogs flag is enabled. If empty, all resources will be captured. If only Exclude action is specified, the rules will apply to the list of all available resources. If Include actions are specified, the rules will only include resources with the associated tags.
    FilteringTag:
      type: object
      description: The definition of a filtering tag. Filtering tags are used for capturing resources and include/exclude them from being monitored.
      properties:
        name:
          type: string
          description: The name (also known as the key) of the tag.
        value:
          type: string
          description: The value of the tag.
        action:
          type: string
          description: 'Valid actions for a filtering tag. Exclusion takes priority over inclusion. Known values are: "Include" and "Exclude".'
    MetricRules:
      type: object
      description: Set of rules for sending metrics for the Monitor resource.
      properties:
        filteringTags:
          type: array
          items:
            $ref: "#/components/schemas/FilteringTag"
          description: List of filtering tags to be used for capturing metrics. If empty, all resources will be captured. If only Exclude action is specified, the rules will apply to the list of all available resources. If Include actions are specified, the rules will only include resources with the associated tags.
    MonitoredSubscriptionPropertiesFlattened:
      type: object
      description: Flattened view of MonitoredSubscriptionProperties ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: Name of the monitored subscription resource.
        id:
          type: string
          description: The id of the monitored subscription resource.
        type:
          type: string
          description: The type of the monitored subscription resource.
        operation:
          type: string
          description: 'The operation for the patch on the resource. Known values are: "AddBegin", "AddComplete", "DeleteBegin", "DeleteComplete", and "Active".'
        monitoredSubscriptionList:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredSubscription"
          description: List of subscriptions and the state of the monitoring.
    PagedMonitoredSubscriptionPropertiesFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredSubscriptionPropertiesFlattened"
        nextLink:
          type: string
    MonitoredSubscriptionProperties:
      type: object
      description: The request to update subscriptions needed to be monitored by the Datadog monitor resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Name of the monitored subscription resource.
        id:
          type: string
          description: The id of the monitored subscription resource.
        type:
          type: string
          description: The type of the monitored subscription resource.
        properties:
          $ref: "#/components/schemas/SubscriptionList"
          description: The request to update subscriptions needed to be monitored by the Datadog monitor resource.
    ResourceSku:
      type: object
      description: ResourceSku. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: Name of the SKU in {PlanId} format. For Terraform, the only allowed value is 'linking'. Required.
      required:
        - name
    MonitorProperties:
      type: object
      description: Properties specific to the monitor resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          type: string
          description: 'Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        monitoringStatus:
          type: string
          description: 'Flag specifying if the resource monitoring is enabled or disabled. Known values are: "Enabled" and "Disabled".'
        marketplaceSubscriptionStatus:
          type: string
          description: 'Flag specifying the Marketplace Subscription Status of the resource. If payment is not made in time, the resource will go in Suspended state. Known values are: "Provisioning", "Active", "Suspended", and "Unsubscribed".'
        datadogOrganizationProperties:
          $ref: "#/components/schemas/DatadogOrganizationProperties"
          description: Specify the Datadog organization name. In the case of linking to existing organizations, Id, ApiKey, and Applicationkey is required as well.
        userInfo:
          $ref: "#/components/schemas/UserInfo"
          description: Includes name, email and optionally, phone number. User Information can't be null.
        liftrResourceCategory:
          type: string
          description: 'Known values are: "Unknown" and "MonitorLogs".'
        liftrResourcePreference:
          type: integer
          description: The priority of the resource.
    DatadogOrganizationProperties:
      type: object
      description: Specify the Datadog organization name. In the case of linking to existing organizations, Id, ApiKey, and Applicationkey is required as well.
      properties:
        name:
          type: string
          description: Name of the Datadog organization.
        id:
          type: string
          description: Id of the Datadog organization.
        linkingAuthCode:
          type: string
          description: The auth code used to linking to an existing Datadog organization.
        linkingClientId:
          type: string
          description: The client_id from an existing in exchange for an auth token to link organization.
        redirectUri:
          type: string
          description: The redirect URI for linking.
        apiKey:
          type: string
          description: Api key associated to the Datadog organization.
        applicationKey:
          type: string
          description: Application key associated to the Datadog organization.
        enterpriseAppId:
          type: string
          description: The Id of the Enterprise App used for Single sign on.
        cspm:
          type: boolean
          description: The configuration which describes the state of cloud security posture management. This collects configuration information for all resources in a subscription and track conformance to industry benchmarks.
    UserInfo:
      type: object
      description: Includes name, email and optionally, phone number. User Information can't be null.
      properties:
        name:
          type: string
          description: Name of the user.
        emailAddress:
          type: string
          description: Email of the user used by Datadog for contacting them if needed.
        phoneNumber:
          type: string
          description: Phone number of the user used by Datadog for contacting them if needed.
    IdentityProperties:
      type: object
      description: IdentityProperties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        principalId:
          type: string
          description: The identity ID.
        tenantId:
          type: string
          description: The tenant ID of resource.
        type:
          type: string
          description: "Specifies the identity type of the Datadog Monitor. At this time the only allowed value is 'SystemAssigned'. Known values are: \"SystemAssigned\" and \"UserAssigned\"."
    DatadogMonitorResourceFlattened:
      type: object
      description: Flattened view of DatadogMonitorResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: ARM id of the monitor resource.
        name:
          type: string
          description: Name of the monitor resource.
        type:
          type: string
          description: The type of the monitor resource.
        sku:
          $ref: "#/components/schemas/ResourceSku"
          description: ":vartype sku: ~azure.mgmt.datadog.models.ResourceSku"
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: ":vartype identity: ~azure.mgmt.datadog.models.IdentityProperties"
        tags:
          type: object
          properties: {}
          description: Dictionary of .
        location:
          type: string
          description: Required.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        provisioningState:
          type: string
          description: 'Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        monitoringStatus:
          type: string
          description: 'Flag specifying if the resource monitoring is enabled or disabled. Known values are: "Enabled" and "Disabled".'
        marketplaceSubscriptionStatus:
          type: string
          description: 'Flag specifying the Marketplace Subscription Status of the resource. If payment is not made in time, the resource will go in Suspended state. Known values are: "Provisioning", "Active", "Suspended", and "Unsubscribed".'
        datadogOrganizationProperties:
          $ref: "#/components/schemas/DatadogOrganizationProperties"
          description: Specify the Datadog organization name. In the case of linking to existing organizations, Id, ApiKey, and Applicationkey is required as well.
        userInfo:
          $ref: "#/components/schemas/UserInfo"
          description: Includes name, email and optionally, phone number. User Information can't be null.
        liftrResourceCategory:
          type: string
          description: 'Known values are: "Unknown" and "MonitorLogs".'
        liftrResourcePreference:
          type: integer
          description: The priority of the resource.
    PagedDatadogMonitorResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DatadogMonitorResourceFlattened"
        nextLink:
          type: string
    DatadogApiKey:
      type: object
      description: DatadogApiKey. All required parameters must be populated in order to send to Azure.
      properties:
        createdBy:
          type: string
          description: The user that created the API key.
        name:
          type: string
          description: The name of the API key.
        key:
          type: string
          description: The value of the API key. Required.
        created:
          type: string
          description: The time of creation of the API key.
      required:
        - key
    PagedDatadogApiKey:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DatadogApiKey"
        nextLink:
          type: string
    DatadogHostMetadata:
      type: object
      description: DatadogHostMetadata.
      properties:
        agentVersion:
          type: string
          description: The agent version.
        installMethod:
          $ref: "#/components/schemas/DatadogInstallMethod"
          description: ":vartype install_method: ~azure.mgmt.datadog.models.DatadogInstallMethod"
        logsAgent:
          $ref: "#/components/schemas/DatadogLogsAgent"
          description: ":vartype logs_agent: ~azure.mgmt.datadog.models.DatadogLogsAgent"
    DatadogInstallMethod:
      type: object
      description: DatadogInstallMethod.
      properties:
        tool:
          type: string
          description: The tool.
        toolVersion:
          type: string
          description: The tool version.
        installerVersion:
          type: string
          description: The installer version.
    DatadogLogsAgent:
      type: object
      description: DatadogLogsAgent.
      properties:
        transport:
          type: string
          description: The transport.
    DatadogHost:
      type: object
      description: DatadogHost.
      properties:
        name:
          type: string
          description: The name of the host.
        aliases:
          type: array
          items:
            type: string
          description: The aliases for the host installed via the Datadog agent.
        apps:
          type: array
          items:
            type: string
          description: The Datadog integrations reporting metrics for the host.
        meta:
          $ref: "#/components/schemas/DatadogHostMetadata"
          description: ":vartype meta: ~azure.mgmt.datadog.models.DatadogHostMetadata"
    PagedDatadogHost:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DatadogHost"
        nextLink:
          type: string
    LinkedResource:
      type: object
      description: The definition of a linked resource.
      properties:
        id:
          type: string
          description: The ARM id of the linked resource.
    PagedLinkedResource:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/LinkedResource"
        nextLink:
          type: string
    MonitoredResource:
      type: object
      description: The properties of a resource currently being monitored by the Datadog monitor resource.
      properties:
        id:
          type: string
          description: The ARM id of the resource.
        sendingMetrics:
          type: boolean
          description: Flag indicating if resource is sending metrics to Datadog.
        reasonForMetricsStatus:
          type: string
          description: Reason for why the resource is sending metrics (or why it is not sending).
        sendingLogs:
          type: boolean
          description: Flag indicating if resource is sending logs to Datadog.
        reasonForLogsStatus:
          type: string
          description: Reason for why the resource is sending logs (or why it is not sending).
    PagedMonitoredResource:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MonitoredResource"
        nextLink:
          type: string
    DatadogMonitorResource:
      type: object
      description: DatadogMonitorResource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: ARM id of the monitor resource.
        name:
          type: string
          description: Name of the monitor resource.
        type:
          type: string
          description: The type of the monitor resource.
        sku:
          $ref: "#/components/schemas/ResourceSku"
          description: ":vartype sku: ~azure.mgmt.datadog.models.ResourceSku"
        properties:
          $ref: "#/components/schemas/MonitorProperties"
          description: Properties specific to the monitor resource.
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: ":vartype identity: ~azure.mgmt.datadog.models.IdentityProperties"
        tags:
          type: object
          properties: {}
          description: Dictionary of .
        location:
          type: string
          description: Required.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
      required:
        - location
    MonitorUpdateProperties:
      type: object
      description: The set of properties that can be update in a PATCH request to a monitor resource.
      properties:
        monitoringStatus:
          type: string
          description: 'Flag specifying if the resource monitoring is enabled or disabled. Known values are: "Enabled" and "Disabled".'
        cspm:
          type: boolean
          description: The new cloud security posture management value of the monitor resource. This collects configuration information for all resources in a subscription and track conformance to industry benchmarks.
    DatadogSetPasswordLink:
      type: object
      description: DatadogSetPasswordLink.
      properties:
        setPasswordLink:
          type: string
          description: ":vartype set_password_link: str"
    OperationDisplay:
      type: object
      description: The object that represents the operation.
      properties:
        provider:
          type: string
          description: Service provider, i.e., Microsoft.Datadog.
        resource:
          type: string
          description: Type on which the operation is performed, e.g., 'monitors'.
        operation:
          type: string
          description: Operation type, e.g., read, write, delete, etc.
        description:
          type: string
          description: Description of the operation, e.g., 'Write monitors'.
    OperationResult:
      type: object
      description: A Microsoft.Datadog REST API operation.
      properties:
        name:
          type: string
          description: Operation name, i.e., {provider}/{resource}/{operation}.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that represents the operation.
        isDataAction:
          type: boolean
          description: Indicates whether the operation is a data action.
    PagedOperationResult:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OperationResult"
        nextLink:
          type: string
    DatadogSingleSignOnProperties:
      type: object
      description: DatadogSingleSignOnProperties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          type: string
          description: 'Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        singleSignOnState:
          type: string
          description: 'Various states of the SSO resource. Known values are: "Initial", "Enable", "Disable", and "Existing".'
        enterpriseAppId:
          type: string
          description: The Id of the Enterprise App used for Single sign-on.
        singleSignOnUrl:
          type: string
          description: The login URL specific to this Datadog Organization.
    DatadogSingleSignOnResourceFlattened:
      type: object
      description: Flattened view of DatadogSingleSignOnResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: ARM id of the resource.
        name:
          type: string
          description: Name of the configuration.
        type:
          type: string
          description: The type of the resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        provisioningState:
          type: string
          description: 'Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        singleSignOnState:
          type: string
          description: 'Various states of the SSO resource. Known values are: "Initial", "Enable", "Disable", and "Existing".'
        enterpriseAppId:
          type: string
          description: The Id of the Enterprise App used for Single sign-on.
        singleSignOnUrl:
          type: string
          description: The login URL specific to this Datadog Organization.
    PagedDatadogSingleSignOnResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DatadogSingleSignOnResourceFlattened"
        nextLink:
          type: string
    DatadogSingleSignOnResource:
      type: object
      description: DatadogSingleSignOnResource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: ARM id of the resource.
        name:
          type: string
          description: Name of the configuration.
        type:
          type: string
          description: The type of the resource.
        properties:
          $ref: "#/components/schemas/DatadogSingleSignOnProperties"
          description: ":vartype properties: ~azure.mgmt.datadog.models.DatadogSingleSignOnProperties"
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
    MonitoringTagRulesFlattened:
      type: object
      description: Flattened view of MonitoringTagRules ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: Name of the rule set.
        id:
          type: string
          description: The id of the rule set.
        type:
          type: string
          description: The type of the rule set.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        provisioningState:
          type: string
          description: 'Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", "Deleted", and "NotSpecified".'
        logRules:
          $ref: "#/components/schemas/LogRules"
          description: Set of rules for sending logs for the Monitor resource.
        metricRules:
          $ref: "#/components/schemas/MetricRules"
          description: Set of rules for sending metrics for the Monitor resource.
        automuting:
          type: boolean
          description: Configuration to enable/disable auto-muting flag.
    PagedMonitoringTagRulesFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MonitoringTagRulesFlattened"
        nextLink:
          type: string
    MonitoringTagRules:
      type: object
      description: Capture logs and metrics of Azure resources based on ARM tags. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Name of the rule set.
        id:
          type: string
          description: The id of the rule set.
        type:
          type: string
          description: The type of the rule set.
        properties:
          $ref: "#/components/schemas/MonitoringTagRulesProperties"
          description: Definition of the properties for a TagRules resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
  x-stackQL-resources:
    creation_supported:
      id: azure_isv.datadog.creation_supported
      name: creation_supported
      title: creation_supported
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Datadog~1subscriptionStatuses~1default?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
        list_raw:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Datadog~1subscriptionStatuses?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/creation_supported/methods/get"
        insert: []
        update: []
        delete: []
    marketplace_agreements:
      id: azure_isv.datadog.marketplace_agreements
      name: marketplace_agreements
      title: marketplace_agreements
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Datadog~1agreements?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Datadog~1agreements~1default?api-version=2023-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/marketplace_agreements/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/marketplace_agreements/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/marketplace_agreements/methods/create_or_update"
        delete: []
    monitored_subscriptions:
      id: azure_isv.datadog.monitored_subscriptions
      name: monitored_subscriptions
      title: monitored_subscriptions
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        createor_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2023-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2023-01-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1monitoredSubscriptions~1{configuration_name}?api-version=2023-01-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1monitoredSubscriptions?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/get"
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/createor_update"
        update:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/monitored_subscriptions/methods/delete"
    monitors:
      id: azure_isv.datadog.monitors
      name: monitors
      title: monitors
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}?api-version=2023-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}?api-version=2023-01-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}?api-version=2023-01-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Datadog~1monitors?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_api_keys:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1listApiKeys?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_hosts:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1listHosts?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_linked_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1listLinkedResources?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_monitored_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1listMonitoredResources?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        get_default_key:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1getDefaultKey?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        set_default_key:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1setDefaultKey?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        refresh_set_password_link:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1refreshSetPasswordLink?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/get"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/monitors/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/monitors/methods/delete"
    operations:
      id: azure_isv.datadog.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Datadog~1operations?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    single_sign_on_configurations:
      id: azure_isv.datadog.single_sign_on_configurations
      name: single_sign_on_configurations
      title: single_sign_on_configurations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1singleSignOnConfigurations~1{configuration_name}?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1singleSignOnConfigurations~1{configuration_name}?api-version=2023-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1singleSignOnConfigurations?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/single_sign_on_configurations/methods/get"
          - $ref: "#/components/x-stackQL-resources/single_sign_on_configurations/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/single_sign_on_configurations/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/single_sign_on_configurations/methods/create_or_update"
        delete: []
    tag_rules:
      id: azure_isv.datadog.tag_rules
      name: tag_rules
      title: tag_rules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1tagRules~1{rule_set_name}?api-version=2023-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Datadog~1monitors~1{monitor_name}~1tagRules?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/get"
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/tag_rules/methods/create_or_update"
        delete: []
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.Datadog/subscriptionStatuses/default?api-version=2023-01-01:
    get:
      operationId: CreationSupported_get
      description: Informs if the current subscription is being already monitored for selected Datadog organization. Informs if the current subscription is being already monitored for selected Datadog organization.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: datadogOrganizationId
          in: query
          required: true
          schema:
            type: string
          description: Datadog Organization Id. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateResourceSupportedResponseFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Datadog/subscriptionStatuses?api-version=2023-01-01:
    get:
      operationId: CreationSupported_list
      description: Informs if the current subscription is being already monitored for selected Datadog organization. Informs if the current subscription is being already monitored for selected Datadog organization.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: datadogOrganizationId
          in: query
          required: true
          schema:
            type: string
          description: Datadog Organization Id. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCreateResourceSupportedResponseFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Datadog/agreements?api-version=2023-01-01:
    get:
      operationId: MarketplaceAgreements_list
      description: List Datadog marketplace agreements in the subscription. List Datadog marketplace agreements in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatadogAgreementResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Datadog/agreements/default?api-version=2023-01-01:
    put:
      operationId: MarketplaceAgreements_create_or_update
      description: Create Datadog marketplace agreement in the subscription. Create Datadog marketplace agreement in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/DatadogAgreementProperties"
                  description: Represents the properties of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogAgreementResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/monitoredSubscriptions/{configuration_name}?api-version=2023-01-01:
    get:
      operationId: MonitoredSubscriptions_get
      description: List the subscriptions currently being monitored by the Datadog monitor resource. List the subscriptions currently being monitored by the Datadog monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The configuration name. Only 'default' value is supported. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoredSubscriptionPropertiesFlattened"
    put:
      operationId: MonitoredSubscriptions_begin_createor_update
      description: Add the subscriptions that should be monitored by the Datadog monitor resource. Add the subscriptions that should be monitored by the Datadog monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The configuration name. Only 'default' value is supported. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/SubscriptionList"
                  description: The request to update subscriptions needed to be monitored by the Datadog monitor resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoredSubscriptionProperties"
    patch:
      operationId: MonitoredSubscriptions_begin_update
      description: Updates the subscriptions that are being monitored by the Datadog monitor resource. Updates the subscriptions that are being monitored by the Datadog monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The configuration name. Only 'default' value is supported. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/SubscriptionList"
                  description: The request to update subscriptions needed to be monitored by the Datadog monitor resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoredSubscriptionProperties"
    delete:
      operationId: MonitoredSubscriptions_begin_delete
      description: Updates the subscriptions that are being monitored by the Datadog monitor resource. Updates the subscriptions that are being monitored by the Datadog monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Configuration name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/monitoredSubscriptions?api-version=2023-01-01:
    get:
      operationId: MonitoredSubscriptions_list
      description: List the subscriptions currently being monitored by the Datadog monitor resource. List the subscriptions currently being monitored by the Datadog monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitoredSubscriptionPropertiesFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}?api-version=2023-01-01:
    get:
      operationId: Monitors_get
      description: Get the properties of a specific monitor resource. Get the properties of a specific monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogMonitorResourceFlattened"
    put:
      operationId: Monitors_begin_create
      description: Create a monitor resource. Create a monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                sku:
                  $ref: "#/components/schemas/ResourceSku"
                  description: ":vartype sku: ~azure.mgmt.datadog.models.ResourceSku"
                properties:
                  $ref: "#/components/schemas/MonitorProperties"
                  description: Properties specific to the monitor resource.
                identity:
                  $ref: "#/components/schemas/IdentityProperties"
                  description: ":vartype identity: ~azure.mgmt.datadog.models.IdentityProperties"
                tags:
                  type: object
                  properties: {}
                  description: Dictionary of .
                location:
                  type: string
                  description: Required.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogMonitorResource"
    patch:
      operationId: Monitors_begin_update
      description: Update a monitor resource. Update a monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/MonitorUpdateProperties"
                  description: The set of properties that can be update in a PATCH request to a monitor resource.
                tags:
                  type: object
                  properties: {}
                  description: The new tags of the monitor resource.
                sku:
                  $ref: "#/components/schemas/ResourceSku"
                  description: ":vartype sku: ~azure.mgmt.datadog.models.ResourceSku"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogMonitorResource"
    delete:
      operationId: Monitors_begin_delete
      description: Delete a monitor resource. Delete a monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Datadog/monitors?api-version=2023-01-01:
    get:
      operationId: Monitors_list
      description: List all monitors under the specified subscription. List all monitors under the specified subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatadogMonitorResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors?api-version=2023-01-01:
    get:
      operationId: Monitors_list_by_resource_group
      description: List all monitors under the specified resource group. List all monitors under the specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatadogMonitorResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/listApiKeys?api-version=2023-01-01:
    post:
      operationId: Monitors_list_api_keys
      description: List the api keys for a given monitor resource. List the api keys for a given monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatadogApiKey"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/listHosts?api-version=2023-01-01:
    post:
      operationId: Monitors_list_hosts
      description: List the hosts for a given monitor resource. List the hosts for a given monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatadogHost"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/listLinkedResources?api-version=2023-01-01:
    post:
      operationId: Monitors_list_linked_resources
      description: List all Azure resources associated to the same Datadog organization as the target resource. List all Azure resources associated to the same Datadog organization as the target resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLinkedResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/listMonitoredResources?api-version=2023-01-01:
    post:
      operationId: Monitors_list_monitored_resources
      description: List the resources currently being monitored by the Datadog monitor resource. List the resources currently being monitored by the Datadog monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitoredResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/getDefaultKey?api-version=2023-01-01:
    post:
      operationId: Monitors_get_default_key
      description: Get the default api key. Get the default api key.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogApiKey"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/setDefaultKey?api-version=2023-01-01:
    post:
      operationId: Monitors_set_default_key
      description: Set the default api key. Set the default api key.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                createdBy:
                  type: string
                  description: The user that created the API key.
                name:
                  type: string
                  description: The name of the API key.
                key:
                  type: string
                  description: The value of the API key. Required.
                created:
                  type: string
                  description: The time of creation of the API key.
              required:
                - key
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/refreshSetPasswordLink?api-version=2023-01-01:
    post:
      operationId: Monitors_refresh_set_password_link
      description: Refresh the set password link and return a latest one. Refresh the set password link and return a latest one.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogSetPasswordLink"
  /providers/Microsoft.Datadog/operations?api-version=2023-01-01:
    get:
      operationId: datadog_list
      description: List all operations provided by Microsoft.Datadog for the 2023-01-01 api version. List all operations provided by Microsoft.Datadog for the 2023-01-01 api version.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperationResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/singleSignOnConfigurations/{configuration_name}?api-version=2023-01-01:
    get:
      operationId: SingleSignOnConfigurations_get
      description: Gets the datadog single sign-on resource for the given Monitor. Gets the datadog single sign-on resource for the given Monitor.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Configuration name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogSingleSignOnResourceFlattened"
    put:
      operationId: SingleSignOnConfigurations_begin_create_or_update
      description: Configures single-sign-on for this resource. Configures single-sign-on for this resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Configuration name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/DatadogSingleSignOnProperties"
                  description: ":vartype properties: ~azure.mgmt.datadog.models.DatadogSingleSignOnProperties"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatadogSingleSignOnResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/singleSignOnConfigurations?api-version=2023-01-01:
    get:
      operationId: SingleSignOnConfigurations_list
      description: List the single sign-on configurations for a given monitor resource. List the single sign-on configurations for a given monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatadogSingleSignOnResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/tagRules/{rule_set_name}?api-version=2023-01-01:
    get:
      operationId: TagRules_get
      description: Get a tag rule set for a given monitor resource. Get a tag rule set for a given monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Rule set name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoringTagRulesFlattened"
    put:
      operationId: TagRules_create_or_update
      description: Create or update a tag rule set for a given monitor resource. Create or update a tag rule set for a given monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: rule_set_name
          in: path
          required: true
          schema:
            type: string
          description: Rule set name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/MonitoringTagRulesProperties"
                  description: Definition of the properties for a TagRules resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoringTagRules"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Datadog/monitors/{monitor_name}/tagRules?api-version=2023-01-01:
    get:
      operationId: TagRules_list
      description: List the tag rules for a given monitor resource. List the tag rules for a given monitor resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: monitor_name
          in: path
          required: true
          schema:
            type: string
          description: Monitor resource name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMonitoringTagRulesFlattened"
