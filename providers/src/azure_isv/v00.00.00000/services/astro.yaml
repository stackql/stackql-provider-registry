openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Astro Mgmt Client
  description: AstroMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-08-01-stackql-generated
  x-serviceAlias: astro
  x-sdkPackage: azure-mgmt-astro
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    LiftrBaseDataOrganizationProperties:
      type: object
      description: Properties specific to Data Organization resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        marketplace:
          $ref: "#/components/schemas/LiftrBaseMarketplaceDetails"
          description: Marketplace details of the resource. Required.
        user:
          $ref: "#/components/schemas/LiftrBaseUserDetails"
          description: Details of the user. Required.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
        partnerOrganizationProperties:
          $ref: "#/components/schemas/LiftrBaseDataPartnerOrganizationProperties"
          description: Organization properties.
      required:
        - marketplace
        - user
    LiftrBaseMarketplaceDetails:
      type: object
      description: Marketplace details for an organization. All required parameters must be populated in order to send to Azure.
      properties:
        subscriptionId:
          type: string
          description: Azure subscription id for the the marketplace offer is purchased from. Required.
        subscriptionStatus:
          type: string
          description: 'Marketplace subscription status. Known values are: "PendingFulfillmentStart", "Subscribed", "Suspended", and "Unsubscribed".'
        offerDetails:
          $ref: "#/components/schemas/LiftrBaseOfferDetails"
          description: Offer details for the marketplace that is selected by the user. Required.
      required:
        - subscriptionId
        - offerDetails
    LiftrBaseOfferDetails:
      type: object
      description: Offer details for the marketplace that is selected by the user. All required parameters must be populated in order to send to Azure.
      properties:
        publisherId:
          type: string
          description: Publisher Id for the marketplace offer. Required.
        offerId:
          type: string
          description: Offer Id for the marketplace offer. Required.
        planId:
          type: string
          description: Plan Id for the marketplace offer. Required.
        planName:
          type: string
          description: Plan Name for the marketplace offer.
        termUnit:
          type: string
          description: Plan Display Name for the marketplace offer.
        termId:
          type: string
          description: Plan Display Name for the marketplace offer.
      required:
        - publisherId
        - offerId
        - planId
    LiftrBaseUserDetails:
      type: object
      description: User details for an organization. All required parameters must be populated in order to send to Azure.
      properties:
        firstName:
          type: string
          description: First name of the user. Required.
        lastName:
          type: string
          description: Last name of the user. Required.
        emailAddress:
          type: string
          description: Email address of the user. Required.
        upn:
          type: string
          description: User's principal name.
        phoneNumber:
          type: string
          description: User's phone number.
      required:
        - firstName
        - lastName
        - emailAddress
    LiftrBaseDataPartnerOrganizationProperties:
      type: object
      description: Properties specific to Partner's organization. All required parameters must be populated in order to send to Azure.
      properties:
        organizationId:
          type: string
          description: Organization Id in partner's system.
        workspaceId:
          type: string
          description: Workspace Id in partner's system.
        organizationName:
          type: string
          description: Organization name in partner's system. Required.
        workspaceName:
          type: string
          description: Workspace name in partner's system.
        singleSignOnProperties:
          $ref: "#/components/schemas/LiftrBaseSingleSignOnProperties"
          description: Single Sign On properties for the organization.
      required:
        - organizationName
    LiftrBaseSingleSignOnProperties:
      type: object
      description: Properties specific to Single Sign On Resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        singleSignOnState:
          type: string
          description: 'State of the Single Sign On for the organization. Known values are: "Initial", "Enable", and "Disable".'
        enterpriseAppId:
          type: string
          description: AAD enterprise application Id used to setup SSO.
        singleSignOnUrl:
          type: string
          description: URL for SSO to be used by the partner to redirect the user to their system.
        aadDomains:
          type: array
          items:
            type: string
          description: List of AAD domains fetched from Microsoft Graph for user.
        provisioningState:
          type: string
          description: 'Provisioning State of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities). Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          type: string
          description: 'Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned, UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: "The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}. The dictionary values can be empty objects ({}) in requests."
      required:
        - type
    OrganizationResourceFlattened:
      type: object
      description: Flattened view of OrganizationResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        marketplace:
          $ref: "#/components/schemas/LiftrBaseMarketplaceDetails"
          description: Marketplace details of the resource. Required.
        user:
          $ref: "#/components/schemas/LiftrBaseUserDetails"
          description: Details of the user. Required.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
        partnerOrganizationProperties:
          $ref: "#/components/schemas/LiftrBaseDataPartnerOrganizationProperties"
          description: Organization properties.
    PagedOrganizationResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OrganizationResourceFlattened"
        nextLink:
          type: string
    OrganizationResource:
      type: object
      description: Organization Resource by Astronomer. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/LiftrBaseDataOrganizationProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    OrganizationResourceUpdateProperties:
      type: object
      description: The updatable properties of the OrganizationResource.
      properties:
        user:
          $ref: "#/components/schemas/LiftrBaseUserDetailsUpdate"
          description: Details of the user.
        partnerOrganizationProperties:
          $ref: "#/components/schemas/LiftrBaseDataPartnerOrganizationPropertiesUpdate"
          description: Organization properties.
    LiftrBaseUserDetailsUpdate:
      type: object
      description: User details for an organization.
      properties:
        firstName:
          type: string
          description: First name of the user.
        lastName:
          type: string
          description: Last name of the user.
        emailAddress:
          type: string
          description: Email address of the user.
        upn:
          type: string
          description: User's principal name.
        phoneNumber:
          type: string
          description: User's phone number.
    LiftrBaseDataPartnerOrganizationPropertiesUpdate:
      type: object
      description: Properties specific to Partner's organization.
      properties:
        organizationId:
          type: string
          description: Organization Id in partner's system.
        workspaceId:
          type: string
          description: Workspace Id in partner's system.
        organizationName:
          type: string
          description: Organization name in partner's system.
        workspaceName:
          type: string
          description: Workspace name in partner's system.
        singleSignOnProperties:
          $ref: "#/components/schemas/LiftrBaseSingleSignOnProperties"
          description: Single Sign On properties for the organization.
  x-stackQL-resources:
    operations:
      id: azure_isv.astro.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Astronomer.Astro~1operations?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    organizations:
      id: azure_isv.astro.organizations
      name: organizations
      title: organizations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Astronomer.Astro~1organizations~1{organization_name}?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Astronomer.Astro~1organizations~1{organization_name}?api-version=2023-08-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Astronomer.Astro~1organizations~1{organization_name}?api-version=2023-08-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Astronomer.Astro~1organizations~1{organization_name}?api-version=2023-08-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Astronomer.Astro~1organizations?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Astronomer.Astro~1organizations?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/get"
          - $ref: "#/components/x-stackQL-resources/organizations/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/organizations/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/delete"
paths:
  /providers/Astronomer.Astro/operations?api-version=2023-08-01:
    get:
      operationId: astro_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Astronomer.Astro/organizations/{organization_name}?api-version=2023-08-01:
    get:
      operationId: Organizations_get
      description: Get a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organizations resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResourceFlattened"
    put:
      operationId: Organizations_begin_create_or_update
      description: Create a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organizations resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/LiftrBaseDataOrganizationProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResource"
    patch:
      operationId: Organizations_begin_update
      description: Update a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organizations resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/OrganizationResourceUpdateProperties"
                  description: The updatable properties of the OrganizationResource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResource"
    delete:
      operationId: Organizations_begin_delete
      description: Delete a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organizations resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Astronomer.Astro/organizations?api-version=2023-08-01:
    get:
      operationId: Organizations_list_by_subscription
      description: List OrganizationResource resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOrganizationResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Astronomer.Astro/organizations?api-version=2023-08-01:
    get:
      operationId: Organizations_list_by_resource_group
      description: List OrganizationResource resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOrganizationResourceFlattened"
