openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Redis Management Client
  description: REST API for Azure Redis Cache Service.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-08-01-preview-stackql-generated
  x-serviceAlias: redis
  x-sdkPackage: azure-mgmt-redis
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    RedisCacheAccessPolicyAssignmentProperties:
      type: object
      description: Properties for an access policy assignment.
      properties:
        provisioningState:
          $ref: "#/components/schemas/AccessPolicyAssignmentProvisioningState"
          description: 'Provisioning state of an access policy assignment set. Known values are: "Updating", "Succeeded", "Deleting", "Deleted", "Canceled", and "Failed".'
        objectId:
          type: string
          description: Object Id to assign access policy to. Required.
        objectIdAlias:
          type: string
          description: User friendly name for object id. Also represents username for token based authentication. Required.
        accessPolicyName:
          type: string
          description: The name of the access policy that is being assigned. Required.
      required:
        - objectId
        - objectIdAlias
        - accessPolicyName
    AccessPolicyAssignmentProvisioningState:
      type: string
      enum:
        - Updating
        - Succeeded
        - Deleting
        - Deleted
        - Canceled
        - Failed
    RedisCacheAccessPolicyAssignmentFlattened:
      type: object
      description: Flattened view of RedisCacheAccessPolicyAssignment ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/AccessPolicyAssignmentProvisioningState"
          description: 'Provisioning state of an access policy assignment set. Known values are: "Updating", "Succeeded", "Deleting", "Deleted", "Canceled", and "Failed".'
        objectId:
          type: string
          description: Object Id to assign access policy to. Required.
        objectIdAlias:
          type: string
          description: User friendly name for object id. Also represents username for token based authentication. Required.
        accessPolicyName:
          type: string
          description: The name of the access policy that is being assigned. Required.
    PagedRedisCacheAccessPolicyAssignmentFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RedisCacheAccessPolicyAssignmentFlattened"
        nextLink:
          type: string
    RedisCacheAccessPolicyAssignment:
      type: object
      description: Response to an operation on access policy assignment.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/RedisCacheAccessPolicyAssignmentProperties"
          description: Properties of an access policy assignment.
    RedisCacheAccessPolicyProperties:
      type: object
      description: All properties of an access policy.
      properties:
        provisioningState:
          $ref: "#/components/schemas/AccessPolicyProvisioningState"
          description: 'Provisioning state of access policy. Known values are: "Updating", "Succeeded", "Deleting", "Deleted", "Canceled", and "Failed".'
        type:
          $ref: "#/components/schemas/AccessPolicyType"
          description: 'Built-In or Custom access policy. Known values are: "Custom" and "BuiltIn".'
        permissions:
          type: string
          description: Permissions for the access policy. Learn how to configure permissions at `https://aka.ms/redis/AADPreRequisites `_. Required.
      required:
        - permissions
    AccessPolicyProvisioningState:
      type: string
      enum:
        - Updating
        - Succeeded
        - Deleting
        - Deleted
        - Canceled
        - Failed
    AccessPolicyType:
      type: string
      enum:
        - Custom
        - BuiltIn
    RedisCacheAccessPolicyFlattened:
      type: object
      description: Flattened view of RedisCacheAccessPolicy ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/AccessPolicyProvisioningState"
          description: 'Provisioning state of access policy. Known values are: "Updating", "Succeeded", "Deleting", "Deleted", "Canceled", and "Failed".'
        permissions:
          type: string
          description: Permissions for the access policy. Learn how to configure permissions at `https://aka.ms/redis/AADPreRequisites `_. Required.
    PagedRedisCacheAccessPolicyFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RedisCacheAccessPolicyFlattened"
        nextLink:
          type: string
    RedisCacheAccessPolicy:
      type: object
      description: Response to get/put access policy.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/RedisCacheAccessPolicyProperties"
          description: Properties of an access policy.
    OperationStatusResult:
      type: object
      description: The current status of an async operation.
      properties:
        id:
          type: string
          description: Fully qualified ID for the async operation.
        name:
          type: string
          description: Name of the async operation.
        status:
          type: string
          description: Operation status. Required.
        percentComplete:
          type: number
          description: Percent of the operation that is complete.
        startTime:
          type: string
          format: date-time
          description: The start time of the operation.
        endTime:
          type: string
          format: date-time
          description: The end time of the operation.
        operations:
          type: array
          items:
            $ref: "#/components/schemas/OperationStatusResult"
          description: The operations list.
        error:
          $ref: "#/components/schemas/ErrorDetail"
          description: If present, details of the operation error.
      required:
        - status
    ErrorDetail:
      type: object
      description: The error detail.
      properties:
        code:
          type: string
          description: The error code.
        message:
          type: string
          description: The error message.
        target:
          type: string
          description: The error target.
        details:
          type: array
          items:
            $ref: "#/components/schemas/ErrorDetail"
          description: The error details.
        additionalInfo:
          type: array
          items:
            $ref: "#/components/schemas/ErrorAdditionalInfo"
          description: The error additional info.
    ErrorAdditionalInfo:
      type: object
      description: The resource management error additional info.
      properties:
        type:
          type: string
          description: The additional info type.
        info:
          type: object
          properties: {}
          description: The additional info.
    OperationStatus:
      type: object
      description: Asynchronous operation status.
      properties:
        id:
          type: string
          description: Fully qualified ID for the async operation.
        name:
          type: string
          description: Name of the async operation.
        status:
          type: string
          description: Operation status. Required.
        percentComplete:
          type: number
          description: Percent of the operation that is complete.
        startTime:
          type: string
          format: date-time
          description: The start time of the operation.
        endTime:
          type: string
          format: date-time
          description: The end time of the operation.
        operations:
          type: array
          items:
            $ref: "#/components/schemas/OperationStatusResult"
          description: The operations list.
        error:
          $ref: "#/components/schemas/ErrorDetail"
          description: If present, details of the operation error.
        properties:
          type: object
          properties: {}
          description: Additional properties from RP, only when operation is successful.
      required:
        - status
    RedisFirewallRuleProperties:
      type: object
      description: Specifies a range of IP addresses permitted to connect to the cache.
      properties:
        startIP:
          type: string
          description: lowest IP address included in the range. Required.
        endIP:
          type: string
          description: highest IP address included in the range. Required.
      required:
        - startIP
        - endIP
    RedisFirewallRuleFlattened:
      type: object
      description: Flattened view of RedisFirewallRule ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        startIP:
          type: string
          description: lowest IP address included in the range. Required.
        endIP:
          type: string
          description: highest IP address included in the range. Required.
    PagedRedisFirewallRuleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RedisFirewallRuleFlattened"
        nextLink:
          type: string
    RedisFirewallRule:
      type: object
      description: A firewall rule on a redis cache has a name, and describes a contiguous range of IP addresses permitted to connect.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/RedisFirewallRuleProperties"
          description: redis cache firewall rule properties. Required.
      required:
        - properties
    RedisLinkedServerProperties:
      type: object
      description: Properties of a linked server to be returned in get/put response.
      properties:
        linkedRedisCacheId:
          type: string
          description: Fully qualified resourceId of the linked redis cache. Required.
        linkedRedisCacheLocation:
          type: string
          description: Location of the linked redis cache. Required.
        serverRole:
          $ref: "#/components/schemas/ReplicationRole"
          description: 'Role of the linked server. Required. Known values are: "Primary" and "Secondary".'
        geoReplicatedPrimaryHostName:
          type: string
          description: The unchanging DNS name which will always point to current geo-primary cache among the linked redis caches for seamless Geo Failover experience.
        primaryHostName:
          type: string
          description: The changing DNS name that resolves to the current geo-primary cache among the linked redis caches before or after the Geo Failover.
        provisioningState:
          type: string
          description: Terminal state of the link between primary and secondary redis cache.
      required:
        - linkedRedisCacheId
        - linkedRedisCacheLocation
        - serverRole
    ReplicationRole:
      type: string
      enum:
        - Primary
        - Secondary
    RedisLinkedServerWithPropertiesFlattened:
      type: object
      description: Flattened view of RedisLinkedServerWithProperties ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        linkedRedisCacheId:
          type: string
          description: Fully qualified resourceId of the linked redis cache. Required.
        linkedRedisCacheLocation:
          type: string
          description: Location of the linked redis cache. Required.
        serverRole:
          $ref: "#/components/schemas/ReplicationRole"
          description: 'Role of the linked server. Required. Known values are: "Primary" and "Secondary".'
        geoReplicatedPrimaryHostName:
          type: string
          description: The unchanging DNS name which will always point to current geo-primary cache among the linked redis caches for seamless Geo Failover experience.
        primaryHostName:
          type: string
          description: The changing DNS name that resolves to the current geo-primary cache among the linked redis caches before or after the Geo Failover.
        provisioningState:
          type: string
          description: Terminal state of the link between primary and secondary redis cache.
    PagedRedisLinkedServerWithPropertiesFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RedisLinkedServerWithPropertiesFlattened"
        nextLink:
          type: string
    RedisLinkedServerCreateProperties:
      type: object
      description: Create properties for a linked server.
      properties:
        linkedRedisCacheId:
          type: string
          description: Fully qualified resourceId of the linked redis cache. Required.
        linkedRedisCacheLocation:
          type: string
          description: Location of the linked redis cache. Required.
        serverRole:
          $ref: "#/components/schemas/ReplicationRole"
          description: 'Role of the linked server. Required. Known values are: "Primary" and "Secondary".'
        geoReplicatedPrimaryHostName:
          type: string
          description: The unchanging DNS name which will always point to current geo-primary cache among the linked redis caches for seamless Geo Failover experience.
        primaryHostName:
          type: string
          description: The changing DNS name that resolves to the current geo-primary cache among the linked redis caches before or after the Geo Failover.
      required:
        - linkedRedisCacheId
        - linkedRedisCacheLocation
        - serverRole
    RedisLinkedServerWithProperties:
      type: object
      description: Response to put/get linked server (with properties) for Redis cache.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/RedisLinkedServerProperties"
          description: Properties of the linked server.
    OperationDisplay:
      type: object
      description: The object that describes the operation.
      properties:
        provider:
          type: string
          description: Friendly name of the resource provider.
        operation:
          type: string
          description: "Operation type: read, write, delete, listKeys/action, etc."
        resource:
          type: string
          description: Resource type on which the operation is performed.
        description:
          type: string
          description: Friendly name of the operation.
    Operation:
      type: object
      description: REST API operation.
      properties:
        name:
          type: string
          description: "Operation name: {provider}/{resource}/{operation}."
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that describes the operation.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    ScheduleEntries:
      type: object
      description: List of patch schedules for a Redis cache.
      properties:
        scheduleEntries:
          type: array
          items:
            $ref: "#/components/schemas/ScheduleEntry"
          description: List of patch schedules for a Redis cache. Required.
      required:
        - scheduleEntries
    ScheduleEntry:
      type: object
      description: Patch schedule entry for a Premium Redis Cache.
      properties:
        dayOfWeek:
          $ref: "#/components/schemas/DayOfWeek"
          description: 'Day of the week when a cache can be patched. Required. Known values are: "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday", "Everyday", and "Weekend".'
        startHourUtc:
          type: integer
          description: Start hour after which cache patching can start. Required.
        maintenanceWindow:
          type: string
          description: ISO8601 timespan specifying how much time cache patching can take.
      required:
        - dayOfWeek
        - startHourUtc
    DayOfWeek:
      type: string
      enum:
        - Monday
        - Tuesday
        - Wednesday
        - Thursday
        - Friday
        - Saturday
        - Sunday
        - Everyday
        - Weekend
    RedisPatchScheduleFlattened:
      type: object
      description: Flattened view of RedisPatchSchedule ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        location:
          type: string
          description: The geo-location where the resource lives.
        scheduleEntries:
          type: array
          items:
            $ref: "#/components/schemas/ScheduleEntry"
          description: List of patch schedules for a Redis cache. Required.
    PagedRedisPatchScheduleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RedisPatchScheduleFlattened"
        nextLink:
          type: string
    RedisPatchSchedule:
      type: object
      description: Response to put/get patch schedules for Redis cache.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ScheduleEntries"
          description: List of patch schedules for a Redis cache. Required.
        location:
          type: string
          description: The geo-location where the resource lives.
      required:
        - properties
    PrivateEndpointConnectionProperties:
      type: object
      description: Properties of the private endpoint connection.
      properties:
        groupIds:
          type: array
          items:
            type: string
          description: The group ids for the private endpoint resource.
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider. Required.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
      required:
        - privateLinkServiceConnectionState
    PrivateEndpoint:
      type: object
      description: The private endpoint resource.
      properties:
        id:
          type: string
          description: The resource identifier of the private endpoint.
    PrivateLinkServiceConnectionState:
      type: object
      description: A collection of information about the state of the connection between service consumer and provider.
      properties:
        status:
          $ref: "#/components/schemas/PrivateEndpointServiceConnectionStatus"
          description: 'Indicates whether the connection has been Approved/Rejected/Removed by the owner of the service. Known values are: "Pending", "Approved", and "Rejected".'
        description:
          type: string
          description: The reason for approval/rejection of the connection.
        actionsRequired:
          type: string
          description: A message indicating if changes on the service provider require any updates on the consumer.
    PrivateEndpointServiceConnectionStatus:
      type: string
      enum:
        - Pending
        - Approved
        - Rejected
    PrivateEndpointConnectionProvisioningState:
      type: string
      enum:
        - Succeeded
        - Creating
        - Deleting
        - Failed
    PrivateEndpointConnectionFlattened:
      type: object
      description: Flattened view of PrivateEndpointConnection ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        groupIds:
          type: array
          items:
            type: string
          description: The group ids for the private endpoint resource.
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider. Required.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
    PagedPrivateEndpointConnectionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
        nextLink:
          type: string
    PrivateEndpointConnection:
      type: object
      description: The Private Endpoint Connection resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
          description: Resource properties.
    PrivateLinkResourceProperties:
      type: object
      description: Properties of a private link resource.
      properties:
        groupId:
          type: string
          description: The private link resource group id.
        requiredMembers:
          type: array
          items:
            type: string
          description: The private link resource required member names.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The private link resource private link DNS zone name.
    PrivateLinkResourceFlattened:
      type: object
      description: Flattened view of PrivateLinkResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        groupId:
          type: string
          description: The private link resource group id.
        requiredMembers:
          type: array
          items:
            type: string
          description: The private link resource required member names.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The private link resource private link DNS zone name.
    PagedPrivateLinkResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateLinkResourceFlattened"
        nextLink:
          type: string
    RedisProperties:
      type: object
      description: Properties of the redis cache.
      properties:
        redisConfiguration:
          $ref: "#/components/schemas/RedisCommonPropertiesRedisConfiguration"
          description: "All Redis Settings. Few possible keys: rdb-backup-enabled,rdb-storage-connection-string,rdb-backup-frequency,maxmemory-delta, maxmemory-policy,notify-keyspace-events, aof-backup-enabled, aof-storage-connection-string-0, aof-storage-connection-string-1 etc."
        redisVersion:
          type: string
          description: "Redis version. This should be in the form 'major[.minor]' (only 'major' is required) or the value 'latest' which refers to the latest stable Redis version that is available. Supported versions: 4.0, 6.0 (latest). Default value is 'latest'."
        enableNonSslPort:
          type: boolean
          description: Specifies whether the non-ssl Redis server port (6379) is enabled.
        replicasPerMaster:
          type: integer
          description: The number of replicas to be created per primary.
        replicasPerPrimary:
          type: integer
          description: The number of replicas to be created per primary.
        tenantSettings:
          type: object
          properties: {}
          description: A dictionary of tenant settings.
        shardCount:
          type: integer
          description: The number of shards to be created on a Premium Cluster Cache.
        minimumTlsVersion:
          $ref: "#/components/schemas/TlsVersion"
          description: "Optional: requires clients to use a specified TLS version (or higher) to connect (e,g, '1.0', '1.1', '1.2'). Known values are: \"1.0\", \"1.1\", and \"1.2\"."
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: "Whether or not public endpoint access is allowed for this cache. Value is optional but if passed in, must be 'Enabled' or 'Disabled'. If 'Disabled', private endpoints are the exclusive access method. Known values are: \"Enabled\" and \"Disabled\"."
        updateChannel:
          $ref: "#/components/schemas/UpdateChannel"
          description: "Optional: Specifies the update channel for the monthly Redis updates your Redis Cache will receive. Caches using 'Preview' update channel get latest Redis updates at least 4 weeks ahead of 'Stable' channel caches. Default value is 'Stable'. Known values are: \"Stable\" and \"Preview\"."
        disableAccessKeyAuthentication:
          type: boolean
          description: Authentication to Redis through access keys is disabled when set as true. Default value is false.
        zonalAllocationPolicy:
          $ref: "#/components/schemas/ZonalAllocationPolicy"
          description: "Optional: Specifies how availability zones are allocated to the Redis cache. 'Automatic' enables zone redundancy and Azure will automatically select zones based on regional availability and capacity. 'UserDefined' will select availability zones passed in by you using the 'zones' parameter. 'NoZones' will produce a non-zonal cache. If 'zonalAllocationPolicy' is not passed, it will be set to 'UserDefined' when zones are passed in, otherwise, it will be set to 'Automatic' in regions where zones are supported and 'NoZones' in regions where zones are not supported. Known values are: \"Automatic\", \"UserDefined\", and \"NoZones\"."
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the Redis cache to deploy. Required.
        subnetId:
          type: string
          description: "The full resource ID of a subnet in a virtual network to deploy the Redis cache in. Example format: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/Microsoft.{Network|ClassicNetwork}/VirtualNetworks/vnet1/subnets/subnet1."
        staticIP:
          type: string
          description: Static IP address. Optionally, may be specified when deploying a Redis cache inside an existing Azure Virtual Network; auto assigned by default.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Redis instance provisioning status. Known values are: "Creating", "Deleting", "Disabled", "Failed", "Linking", "Provisioning", "RecoveringScaleFailure", "Scaling", "Succeeded", "Unlinking", "Unprovisioning", "Updating", "ConfiguringAAD", "Migrating", "MigrationFailed", "MigrationSucceeded", "MigrationCancelling", and "MigrationCancellationFailed".'
        hostName:
          type: string
          description: Redis host name.
        port:
          type: integer
          description: Redis non-SSL port.
        sslPort:
          type: integer
          description: Redis SSL port.
        accessKeys:
          $ref: "#/components/schemas/RedisAccessKeys"
          description: The keys of the Redis cache - not set if this object is not the response to Create or Update redis cache.
        linkedServers:
          type: array
          items:
            $ref: "#/components/schemas/RedisLinkedServer"
          description: List of the linked servers associated with the cache.
        instances:
          type: array
          items:
            $ref: "#/components/schemas/RedisInstanceDetails"
          description: List of the Redis instances associated with the cache.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connection associated with the specified redis cache.
        targetAmrResourceId:
          type: string
          description: The resource ID of the target Azure Managed Redis resource that this Azure Cache for Redis resource is being migrated to.
      required:
        - sku
    RedisCommonPropertiesRedisConfiguration:
      type: object
      description: "All Redis Settings. Few possible keys: rdb-backup-enabled,rdb-storage-connection-string,rdb-backup-frequency,maxmemory-delta, maxmemory-policy,notify-keyspace-events, aof-backup-enabled, aof-storage-connection-string-0, aof-storage-connection-string-1 etc."
      properties:
        rdb-backup-enabled:
          type: string
          description: Specifies whether the RDB backup is enabled.
        rdb-backup-frequency:
          type: string
          description: "Specifies the frequency for creating rdb backup in minutes. Valid values: (15, 30, 60, 360, 720, 1440)."
        rdb-backup-max-snapshot-count:
          type: string
          description: Specifies the maximum number of snapshots for rdb backup.
        rdb-storage-connection-string:
          type: string
          description: The storage account connection string for storing rdb file.
        aof-backup-enabled:
          type: string
          description: Specifies whether the aof backup is enabled.
        aof-storage-connection-string-0:
          type: string
          description: First storage account connection string.
        aof-storage-connection-string-1:
          type: string
          description: Second storage account connection string.
        maxfragmentationmemory-reserved:
          type: string
          description: Value in megabytes reserved for fragmentation per shard.
        maxmemory-policy:
          type: string
          description: The eviction strategy used when your data won't fit within its memory limit.
        maxmemory-reserved:
          type: string
          description: Value in megabytes reserved for non-cache usage per shard e.g. failover.
        maxmemory-delta:
          type: string
          description: Value in megabytes reserved for non-cache usage per shard e.g. failover.
        maxclients:
          type: string
          description: The max clients config.
        notify-keyspace-events:
          type: string
          description: The keyspace events which should be monitored.
        preferred-data-archive-auth-method:
          type: string
          description: Preferred auth method to communicate to storage account used for data archive, specify SAS or ManagedIdentity, default value is SAS.
        preferred-data-persistence-auth-method:
          type: string
          description: Preferred auth method to communicate to storage account used for data persistence, specify SAS or ManagedIdentity, default value is SAS.
        zonal-configuration:
          type: string
          description: Zonal Configuration.
        authnotrequired:
          type: string
          description: Specifies whether the authentication is disabled. Setting this property is highly discouraged from security point of view; you should never disable authentication using this property!.
        storage-subscription-id:
          type: string
          description: SubscriptionId of the storage account for persistence (aof/rdb) using ManagedIdentity.
        aad-enabled:
          type: string
          description: Specifies whether AAD based authentication has been enabled or disabled for the cache.
    TlsVersion:
      type: string
      enum:
        - "1.0"
        - "1.1"
        - "1.2"
    PublicNetworkAccess:
      type: string
      enum:
        - Enabled
        - Disabled
    UpdateChannel:
      type: string
      enum:
        - Stable
        - Preview
    ZonalAllocationPolicy:
      type: string
      enum:
        - Automatic
        - UserDefined
        - NoZones
    Sku:
      type: object
      description: SKU parameters supplied to the create Redis operation.
      properties:
        name:
          $ref: "#/components/schemas/SkuName"
          description: 'The type of Redis cache to deploy. Valid values: (Basic, Standard, Premium). Required. Known values are: "Basic", "Standard", and "Premium".'
        family:
          $ref: "#/components/schemas/SkuFamily"
          description: 'The SKU family to use. Valid values: (C, P). (C = Basic/Standard, P = Premium). Required. Known values are: "C" and "P".'
        capacity:
          type: integer
          description: "The size of the Redis cache to deploy. Valid values: for C (Basic/Standard) family (0, 1, 2, 3, 4, 5, 6), for P (Premium) family (1, 2, 3, 4). Required."
      required:
        - name
        - family
        - capacity
    SkuName:
      type: string
      enum:
        - Basic
        - Standard
        - Premium
    SkuFamily:
      type: string
      enum:
        - C
        - P
    ProvisioningState:
      type: string
      enum:
        - Creating
        - Deleting
        - Disabled
        - Failed
        - Linking
        - Provisioning
        - RecoveringScaleFailure
        - Scaling
        - Succeeded
        - Unlinking
        - Unprovisioning
        - Updating
        - ConfiguringAAD
        - Migrating
        - MigrationFailed
        - MigrationSucceeded
        - MigrationCancelling
        - MigrationCancellationFailed
    RedisAccessKeys:
      type: object
      description: Redis cache access keys.
      properties:
        primaryKey:
          type: string
          description: The current primary key that clients can use to authenticate with Redis cache.
        secondaryKey:
          type: string
          description: The current secondary key that clients can use to authenticate with Redis cache.
    RedisLinkedServer:
      type: object
      description: Linked server Id.
      properties:
        id:
          type: string
          description: Linked server Id.
    RedisInstanceDetails:
      type: object
      description: Details of single instance of redis.
      properties:
        sslPort:
          type: integer
          description: Redis instance SSL port.
        nonSslPort:
          type: integer
          description: If enableNonSslPort is true, provides Redis instance Non-SSL port.
        zone:
          type: string
          description: If the Cache uses availability zones, specifies availability zone where this instance is located.
        shardId:
          type: integer
          description: If clustering is enabled, the Shard ID of Redis Instance.
        isMaster:
          type: boolean
          description: Specifies whether the instance is a primary node.
        isPrimary:
          type: boolean
          description: Specifies whether the instance is a primary node.
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned, UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned, UserAssigned
    RedisResourceFlattened:
      type: object
      description: Flattened view of RedisResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        zones:
          type: array
          items:
            type: string
          description: The availability zones.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        redisConfiguration:
          $ref: "#/components/schemas/RedisCommonPropertiesRedisConfiguration"
          description: "All Redis Settings. Few possible keys: rdb-backup-enabled,rdb-storage-connection-string,rdb-backup-frequency,maxmemory-delta, maxmemory-policy,notify-keyspace-events, aof-backup-enabled, aof-storage-connection-string-0, aof-storage-connection-string-1 etc."
        redisVersion:
          type: string
          description: "Redis version. This should be in the form 'major[.minor]' (only 'major' is required) or the value 'latest' which refers to the latest stable Redis version that is available. Supported versions: 4.0, 6.0 (latest). Default value is 'latest'."
        enableNonSslPort:
          type: boolean
          description: Specifies whether the non-ssl Redis server port (6379) is enabled.
        replicasPerMaster:
          type: integer
          description: The number of replicas to be created per primary.
        replicasPerPrimary:
          type: integer
          description: The number of replicas to be created per primary.
        tenantSettings:
          type: object
          properties: {}
          description: A dictionary of tenant settings.
        shardCount:
          type: integer
          description: The number of shards to be created on a Premium Cluster Cache.
        minimumTlsVersion:
          $ref: "#/components/schemas/TlsVersion"
          description: "Optional: requires clients to use a specified TLS version (or higher) to connect (e,g, '1.0', '1.1', '1.2'). Known values are: \"1.0\", \"1.1\", and \"1.2\"."
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: "Whether or not public endpoint access is allowed for this cache. Value is optional but if passed in, must be 'Enabled' or 'Disabled'. If 'Disabled', private endpoints are the exclusive access method. Known values are: \"Enabled\" and \"Disabled\"."
        updateChannel:
          $ref: "#/components/schemas/UpdateChannel"
          description: "Optional: Specifies the update channel for the monthly Redis updates your Redis Cache will receive. Caches using 'Preview' update channel get latest Redis updates at least 4 weeks ahead of 'Stable' channel caches. Default value is 'Stable'. Known values are: \"Stable\" and \"Preview\"."
        disableAccessKeyAuthentication:
          type: boolean
          description: Authentication to Redis through access keys is disabled when set as true. Default value is false.
        zonalAllocationPolicy:
          $ref: "#/components/schemas/ZonalAllocationPolicy"
          description: "Optional: Specifies how availability zones are allocated to the Redis cache. 'Automatic' enables zone redundancy and Azure will automatically select zones based on regional availability and capacity. 'UserDefined' will select availability zones passed in by you using the 'zones' parameter. 'NoZones' will produce a non-zonal cache. If 'zonalAllocationPolicy' is not passed, it will be set to 'UserDefined' when zones are passed in, otherwise, it will be set to 'Automatic' in regions where zones are supported and 'NoZones' in regions where zones are not supported. Known values are: \"Automatic\", \"UserDefined\", and \"NoZones\"."
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the Redis cache to deploy. Required.
        subnetId:
          type: string
          description: "The full resource ID of a subnet in a virtual network to deploy the Redis cache in. Example format: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/Microsoft.{Network|ClassicNetwork}/VirtualNetworks/vnet1/subnets/subnet1."
        staticIP:
          type: string
          description: Static IP address. Optionally, may be specified when deploying a Redis cache inside an existing Azure Virtual Network; auto assigned by default.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Redis instance provisioning status. Known values are: "Creating", "Deleting", "Disabled", "Failed", "Linking", "Provisioning", "RecoveringScaleFailure", "Scaling", "Succeeded", "Unlinking", "Unprovisioning", "Updating", "ConfiguringAAD", "Migrating", "MigrationFailed", "MigrationSucceeded", "MigrationCancelling", and "MigrationCancellationFailed".'
        hostName:
          type: string
          description: Redis host name.
        port:
          type: integer
          description: Redis non-SSL port.
        sslPort:
          type: integer
          description: Redis SSL port.
        accessKeys:
          $ref: "#/components/schemas/RedisAccessKeys"
          description: The keys of the Redis cache - not set if this object is not the response to Create or Update redis cache.
        linkedServers:
          type: array
          items:
            $ref: "#/components/schemas/RedisLinkedServer"
          description: List of the linked servers associated with the cache.
        instances:
          type: array
          items:
            $ref: "#/components/schemas/RedisInstanceDetails"
          description: List of the Redis instances associated with the cache.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connection associated with the specified redis cache.
        targetAmrResourceId:
          type: string
          description: The resource ID of the target Azure Managed Redis resource that this Azure Cache for Redis resource is being migrated to.
    PagedRedisResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RedisResourceFlattened"
        nextLink:
          type: string
    UpgradeNotification:
      type: object
      description: Properties of upgrade notification.
      properties:
        name:
          type: string
          description: Name of upgrade notification.
        timestamp:
          type: string
          format: date-time
          description: Timestamp when upgrade notification occurred.
        upsellNotification:
          type: object
          properties: {}
          description: Details about this upgrade notification.
    PagedUpgradeNotification:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/UpgradeNotification"
        nextLink:
          type: string
    RedisCreateProperties:
      type: object
      description: Properties supplied to Create Redis operation.
      properties:
        redisConfiguration:
          $ref: "#/components/schemas/RedisCommonPropertiesRedisConfiguration"
          description: "All Redis Settings. Few possible keys: rdb-backup-enabled,rdb-storage-connection-string,rdb-backup-frequency,maxmemory-delta, maxmemory-policy,notify-keyspace-events, aof-backup-enabled, aof-storage-connection-string-0, aof-storage-connection-string-1 etc."
        redisVersion:
          type: string
          description: "Redis version. This should be in the form 'major[.minor]' (only 'major' is required) or the value 'latest' which refers to the latest stable Redis version that is available. Supported versions: 4.0, 6.0 (latest). Default value is 'latest'."
        enableNonSslPort:
          type: boolean
          description: Specifies whether the non-ssl Redis server port (6379) is enabled.
        replicasPerMaster:
          type: integer
          description: The number of replicas to be created per primary.
        replicasPerPrimary:
          type: integer
          description: The number of replicas to be created per primary.
        tenantSettings:
          type: object
          properties: {}
          description: A dictionary of tenant settings.
        shardCount:
          type: integer
          description: The number of shards to be created on a Premium Cluster Cache.
        minimumTlsVersion:
          $ref: "#/components/schemas/TlsVersion"
          description: "Optional: requires clients to use a specified TLS version (or higher) to connect (e,g, '1.0', '1.1', '1.2'). Known values are: \"1.0\", \"1.1\", and \"1.2\"."
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: "Whether or not public endpoint access is allowed for this cache. Value is optional but if passed in, must be 'Enabled' or 'Disabled'. If 'Disabled', private endpoints are the exclusive access method. Known values are: \"Enabled\" and \"Disabled\"."
        updateChannel:
          $ref: "#/components/schemas/UpdateChannel"
          description: "Optional: Specifies the update channel for the monthly Redis updates your Redis Cache will receive. Caches using 'Preview' update channel get latest Redis updates at least 4 weeks ahead of 'Stable' channel caches. Default value is 'Stable'. Known values are: \"Stable\" and \"Preview\"."
        disableAccessKeyAuthentication:
          type: boolean
          description: Authentication to Redis through access keys is disabled when set as true. Default value is false.
        zonalAllocationPolicy:
          $ref: "#/components/schemas/ZonalAllocationPolicy"
          description: "Optional: Specifies how availability zones are allocated to the Redis cache. 'Automatic' enables zone redundancy and Azure will automatically select zones based on regional availability and capacity. 'UserDefined' will select availability zones passed in by you using the 'zones' parameter. 'NoZones' will produce a non-zonal cache. If 'zonalAllocationPolicy' is not passed, it will be set to 'UserDefined' when zones are passed in, otherwise, it will be set to 'Automatic' in regions where zones are supported and 'NoZones' in regions where zones are not supported. Known values are: \"Automatic\", \"UserDefined\", and \"NoZones\"."
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the Redis cache to deploy. Required.
        subnetId:
          type: string
          description: "The full resource ID of a subnet in a virtual network to deploy the Redis cache in. Example format: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/Microsoft.{Network|ClassicNetwork}/VirtualNetworks/vnet1/subnets/subnet1."
        staticIP:
          type: string
          description: Static IP address. Optionally, may be specified when deploying a Redis cache inside an existing Azure Virtual Network; auto assigned by default.
      required:
        - sku
    RedisResource:
      type: object
      description: A single Redis item in List or Get Operation.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/RedisProperties"
          description: Redis cache properties. Required.
        zones:
          type: array
          items:
            type: string
          description: The availability zones.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
        - properties
    RedisUpdateProperties:
      type: object
      description: Patchable properties of the redis cache.
      properties:
        redisConfiguration:
          $ref: "#/components/schemas/RedisCommonPropertiesRedisConfiguration"
          description: "All Redis Settings. Few possible keys: rdb-backup-enabled,rdb-storage-connection-string,rdb-backup-frequency,maxmemory-delta, maxmemory-policy,notify-keyspace-events, aof-backup-enabled, aof-storage-connection-string-0, aof-storage-connection-string-1 etc."
        redisVersion:
          type: string
          description: "Redis version. This should be in the form 'major[.minor]' (only 'major' is required) or the value 'latest' which refers to the latest stable Redis version that is available. Supported versions: 4.0, 6.0 (latest). Default value is 'latest'."
        enableNonSslPort:
          type: boolean
          description: Specifies whether the non-ssl Redis server port (6379) is enabled.
        replicasPerMaster:
          type: integer
          description: The number of replicas to be created per primary.
        replicasPerPrimary:
          type: integer
          description: The number of replicas to be created per primary.
        tenantSettings:
          type: object
          properties: {}
          description: A dictionary of tenant settings.
        shardCount:
          type: integer
          description: The number of shards to be created on a Premium Cluster Cache.
        minimumTlsVersion:
          $ref: "#/components/schemas/TlsVersion"
          description: "Optional: requires clients to use a specified TLS version (or higher) to connect (e,g, '1.0', '1.1', '1.2'). Known values are: \"1.0\", \"1.1\", and \"1.2\"."
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: "Whether or not public endpoint access is allowed for this cache. Value is optional but if passed in, must be 'Enabled' or 'Disabled'. If 'Disabled', private endpoints are the exclusive access method. Known values are: \"Enabled\" and \"Disabled\"."
        updateChannel:
          $ref: "#/components/schemas/UpdateChannel"
          description: "Optional: Specifies the update channel for the monthly Redis updates your Redis Cache will receive. Caches using 'Preview' update channel get latest Redis updates at least 4 weeks ahead of 'Stable' channel caches. Default value is 'Stable'. Known values are: \"Stable\" and \"Preview\"."
        disableAccessKeyAuthentication:
          type: boolean
          description: Authentication to Redis through access keys is disabled when set as true. Default value is false.
        zonalAllocationPolicy:
          $ref: "#/components/schemas/ZonalAllocationPolicy"
          description: "Optional: Specifies how availability zones are allocated to the Redis cache. 'Automatic' enables zone redundancy and Azure will automatically select zones based on regional availability and capacity. 'UserDefined' will select availability zones passed in by you using the 'zones' parameter. 'NoZones' will produce a non-zonal cache. If 'zonalAllocationPolicy' is not passed, it will be set to 'UserDefined' when zones are passed in, otherwise, it will be set to 'Automatic' in regions where zones are supported and 'NoZones' in regions where zones are not supported. Known values are: \"Automatic\", \"UserDefined\", and \"NoZones\"."
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the Redis cache to deploy.
    RedisKeyType:
      type: string
      enum:
        - Primary
        - Secondary
    RebootType:
      type: string
      enum:
        - PrimaryNode
        - SecondaryNode
        - AllNodes
    RedisForceRebootResponse:
      type: object
      description: Response to force reboot for Redis cache.
      properties:
        message:
          type: string
          description: Status message.
  x-stackQL-resources:
    access_policy_assignment:
      id: azure_isv.redis.access_policy_assignment
      name: access_policy_assignment
      title: access_policy_assignment
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicyAssignments~1{access_policy_assignment_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicyAssignments~1{access_policy_assignment_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicyAssignments~1{access_policy_assignment_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicyAssignments?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/access_policy_assignment/methods/get"
          - $ref: "#/components/x-stackQL-resources/access_policy_assignment/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/access_policy_assignment/methods/create_update"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/access_policy_assignment/methods/delete"
    access_policy:
      id: azure_isv.redis.access_policy
      name: access_policy
      title: access_policy
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicies~1{access_policy_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicies~1{access_policy_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicies~1{access_policy_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1accessPolicies?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/access_policy/methods/get"
          - $ref: "#/components/x-stackQL-resources/access_policy/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/access_policy/methods/create_update"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/access_policy/methods/delete"
    async_operation_status:
      id: azure_isv.redis.async_operation_status
      name: async_operation_status
      title: async_operation_status
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Cache~1locations~1{location}~1asyncOperations~1{operation_id}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/async_operation_status/methods/get"
        insert: []
        update: []
        delete: []
    firewall_rules:
      id: azure_isv.redis.firewall_rules
      name: firewall_rules
      title: firewall_rules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1firewallRules~1{rule_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1firewallRules~1{rule_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1firewallRules~1{rule_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1firewallRules?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/get"
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/delete"
    linked_server:
      id: azure_isv.redis.linked_server
      name: linked_server
      title: linked_server
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1linkedServers~1{linked_server_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1linkedServers~1{linked_server_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1linkedServers~1{linked_server_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1linkedServers?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/linked_server/methods/get"
          - $ref: "#/components/x-stackQL-resources/linked_server/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/linked_server/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/linked_server/methods/delete"
    operations:
      id: azure_isv.redis.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Cache~1operations?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    patch_schedules:
      id: azure_isv.redis.patch_schedules
      name: patch_schedules
      title: patch_schedules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1patchSchedules~1{default_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1patchSchedules~1{default_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1patchSchedules~1{default_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_redis_resource:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1patchSchedules?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/patch_schedules/methods/get"
          - $ref: "#/components/x-stackQL-resources/patch_schedules/methods/list_by_redis_resource"
        insert:
          - $ref: "#/components/x-stackQL-resources/patch_schedules/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/patch_schedules/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/patch_schedules/methods/delete"
    private_endpoint_connections:
      id: azure_isv.redis.private_endpoint_connections
      name: private_endpoint_connections
      title: private_endpoint_connections
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        put:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1privateEndpointConnections?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/list"
        insert: []
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/delete"
    private_link_resources:
      id: azure_isv.redis.private_link_resources
      name: private_link_resources
      title: private_link_resources
      methods:
        list_by_redis_cache:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1privateLinkResources?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_link_resources/methods/list_by_redis_cache"
        insert: []
        update: []
        delete: []
    redis:
      id: azure_isv.redis.redis
      name: redis
      title: redis
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}?api-version=2025-08-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Cache~1redis?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_upgrade_notifications:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1listUpgradeNotifications?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
        list_keys:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1listKeys?api-version=2025-08-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        regenerate_key:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1regenerateKey?api-version=2025-08-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        force_reboot:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1forceReboot?api-version=2025-08-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        import_data:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1import?api-version=2025-08-01-preview/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        export_data:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{name}~1export?api-version=2025-08-01-preview/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        flush_cache:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Cache~1redis~1{cache_name}~1flush?api-version=2025-08-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Cache~1checkNameAvailability?api-version=2025-08-01-preview/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/redis/methods/list_upgrade_notifications"
          - $ref: "#/components/x-stackQL-resources/redis/methods/get"
          - $ref: "#/components/x-stackQL-resources/redis/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/redis/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/redis/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/redis/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/redis/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/accessPolicyAssignments/{access_policy_assignment_name}?api-version=2025-08-01-preview:
    get:
      operationId: AccessPolicyAssignment_get
      description: Gets the list of assignments for an access policy of a redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: access_policy_assignment_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the access policy assignment. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisCacheAccessPolicyAssignmentFlattened"
    put:
      operationId: AccessPolicyAssignment_begin_create_update
      description: Adds the access policy assignment to the specified users.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: access_policy_assignment_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the access policy assignment. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RedisCacheAccessPolicyAssignmentProperties"
                  description: Properties of an access policy assignment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisCacheAccessPolicyAssignment"
    delete:
      operationId: AccessPolicyAssignment_begin_delete
      description: Deletes the access policy assignment from a redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: access_policy_assignment_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the access policy assignment. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/accessPolicyAssignments?api-version=2025-08-01-preview:
    get:
      operationId: AccessPolicyAssignment_list
      description: Gets the list of access policy assignments associated with this redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRedisCacheAccessPolicyAssignmentFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/accessPolicies/{access_policy_name}?api-version=2025-08-01-preview:
    get:
      operationId: AccessPolicy_get
      description: Gets the detailed information about an access policy of a redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: access_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the access policy that is being added to the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisCacheAccessPolicyFlattened"
    put:
      operationId: AccessPolicy_begin_create_update
      description: Adds an access policy to the redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: access_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the access policy that is being added to the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RedisCacheAccessPolicyProperties"
                  description: Properties of an access policy.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisCacheAccessPolicy"
    delete:
      operationId: AccessPolicy_begin_delete
      description: Deletes the access policy from a redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: access_policy_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the access policy that is being added to the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/accessPolicies?api-version=2025-08-01-preview:
    get:
      operationId: AccessPolicy_list
      description: Gets the list of access policies associated with this redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRedisCacheAccessPolicyFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Cache/locations/{location}/asyncOperations/{operation_id}?api-version=2025-08-01-preview:
    get:
      operationId: AsyncOperationStatus_get
      description: For checking the ongoing status of an operation.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The location at which operation was triggered. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of asynchronous operation. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/firewallRules/{rule_name}?api-version=2025-08-01-preview:
    get:
      operationId: FirewallRules_get
      description: Gets a single firewall rule in a specified redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisFirewallRuleFlattened"
    put:
      operationId: FirewallRules_create_or_update
      description: Create or update a redis cache firewall rule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RedisFirewallRuleProperties"
                  description: redis cache firewall rule properties. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisFirewallRule"
    delete:
      operationId: FirewallRules_delete
      description: Deletes a single firewall rule in a specified redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/firewallRules?api-version=2025-08-01-preview:
    get:
      operationId: FirewallRules_list
      description: Gets all firewall rules in the specified redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRedisFirewallRuleFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/linkedServers/{linked_server_name}?api-version=2025-08-01-preview:
    get:
      operationId: LinkedServer_get
      description: Gets the detailed information about a linked server of a redis cache (requires Premium SKU).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the redis cache. Required.
        - name: linked_server_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisLinkedServerWithProperties. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisLinkedServerWithPropertiesFlattened"
    put:
      operationId: LinkedServer_begin_create
      description: Adds a linked server to the Redis cache (requires Premium SKU).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the redis cache. Required.
        - name: linked_server_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisLinkedServerWithProperties. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RedisLinkedServerCreateProperties"
                  description: Properties required to create a linked server. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisLinkedServerWithProperties"
    delete:
      operationId: LinkedServer_begin_delete
      description: Deletes the linked server from a redis cache (requires Premium SKU).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the redis cache. Required.
        - name: linked_server_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisLinkedServerWithProperties. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/linkedServers?api-version=2025-08-01-preview:
    get:
      operationId: LinkedServer_list
      description: Gets the list of linked servers associated with this redis cache (requires Premium SKU).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRedisLinkedServerWithPropertiesFlattened"
  /providers/Microsoft.Cache/operations?api-version=2025-08-01-preview:
    get:
      operationId: redis_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/patchSchedules/{default_name}?api-version=2025-08-01-preview:
    get:
      operationId: PatchSchedules_get
      description: Gets the patching schedule of a redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the redis cache. Required.
        - name: default_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisPatchSchedule. "default" Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisPatchScheduleFlattened"
    put:
      operationId: PatchSchedules_create_or_update
      description: Create or replace the patching schedule for Redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the redis cache. Required.
        - name: default_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisPatchSchedule. "default" Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ScheduleEntries"
                  description: List of patch schedules for a Redis cache. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisPatchSchedule"
    delete:
      operationId: PatchSchedules_delete
      description: Deletes the patching schedule of a redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the redis cache. Required.
        - name: default_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisPatchSchedule. "default" Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/patchSchedules?api-version=2025-08-01-preview:
    get:
      operationId: PatchSchedules_list_by_redis_resource
      description: Gets all patch schedules in the specified redis cache (there is only one).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRedisPatchScheduleFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/privateEndpointConnections/{private_endpoint_connection_name}?api-version=2025-08-01-preview:
    get:
      operationId: PrivateEndpointConnections_get
      description: Gets the specified private endpoint connection associated with the redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the Azure resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
    put:
      operationId: PrivateEndpointConnections_begin_put
      description: Update the state of specified private endpoint connection associated with the redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the Azure resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
                  description: Resource properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnection"
    delete:
      operationId: PrivateEndpointConnections_delete
      description: Deletes the specified private endpoint connection associated with the redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the Azure resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/privateEndpointConnections?api-version=2025-08-01-preview:
    get:
      operationId: PrivateEndpointConnections_list
      description: List all the private endpoint connections associated with the redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateEndpointConnectionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/privateLinkResources?api-version=2025-08-01-preview:
    get:
      operationId: PrivateLinkResources_list_by_redis_cache
      description: Gets the private link resources that need to be created for a redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateLinkResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}?api-version=2025-08-01-preview:
    get:
      operationId: Redis_get
      description: Gets a Redis cache (resource description).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisResourceFlattened"
    put:
      operationId: Redis_begin_create
      description: Create or replace (overwrite/recreate, with potential downtime) an existing Redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RedisCreateProperties"
                  description: Redis cache properties. Required.
                zones:
                  type: array
                  items:
                    type: string
                  description: A list of availability zones denoting where the resource needs to come from.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The identity of the resource.
              required:
                - properties
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisResource"
    patch:
      operationId: Redis_begin_update
      description: Update an existing Redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RedisUpdateProperties"
                  description: Redis cache properties.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The identity of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisResource"
    delete:
      operationId: Redis_begin_delete
      description: Deletes a Redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis?api-version=2025-08-01-preview:
    get:
      operationId: Redis_list_by_resource_group
      description: Lists all Redis caches in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRedisResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Cache/redis?api-version=2025-08-01-preview:
    get:
      operationId: Redis_list_by_subscription
      description: Gets all Redis caches in the specified subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRedisResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/listUpgradeNotifications?api-version=2025-08-01-preview:
    get:
      operationId: Redis_list_upgrade_notifications
      description: "[Deprecated] Gets any upgrade notifications for a Redis cache."
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: history
          in: query
          required: true
          schema:
            type: number
          description: how many minutes in past to look for upgrade notifications. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedUpgradeNotification"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/listKeys?api-version=2025-08-01-preview:
    post:
      operationId: Redis_list_keys
      description: Retrieve a Redis cache's access keys. This operation requires write permission to the cache resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisAccessKeys"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/regenerateKey?api-version=2025-08-01-preview:
    post:
      operationId: Redis_regenerate_key
      description: Regenerate Redis cache's access keys. This operation requires write permission to the cache resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                keyType:
                  $ref: "#/components/schemas/RedisKeyType"
                  description: 'The Redis access key to regenerate. Required. Known values are: "Primary" and "Secondary".'
              required:
                - keyType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisAccessKeys"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/forceReboot?api-version=2025-08-01-preview:
    post:
      operationId: Redis_force_reboot
      description: Reboot specified Redis node(s). This operation requires write permission to the cache resource. There can be potential data loss.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                rebootType:
                  $ref: "#/components/schemas/RebootType"
                  description: 'Which Redis node(s) to reboot. Depending on this value data loss is possible. Known values are: "PrimaryNode", "SecondaryNode", and "AllNodes".'
                shardId:
                  type: integer
                  description: If clustering is enabled, the ID of the shard to be rebooted.
                ports:
                  type: array
                  items:
                    type: integer
                  description: A list of redis instances to reboot, specified by per-instance SSL ports or non-SSL ports.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedisForceRebootResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/import?api-version=2025-08-01-preview:
    post:
      operationId: Redis_begin_import_data
      description: Import data into Redis cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                format:
                  type: string
                  description: File format.
                files:
                  type: array
                  items:
                    type: string
                  description: files to import. Required.
                preferred-data-archive-auth-method:
                  type: string
                  description: Preferred auth method to communicate to storage account used for data archive, specify SAS or ManagedIdentity, default value is SAS.
                storage-subscription-id:
                  type: string
                  description: Subscription id of the storage container containing files to import using Managed Identity.
              required:
                - files
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{name}/export?api-version=2025-08-01-preview:
    post:
      operationId: Redis_begin_export_data
      description: Export data from the redis cache to blobs in a container.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the RedisResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                format:
                  type: string
                  description: File format.
                prefix:
                  type: string
                  description: Prefix to use for exported files. Required.
                container:
                  type: string
                  description: Container name to export to. Required.
                preferred-data-archive-auth-method:
                  type: string
                  description: Preferred auth method to communicate to storage account used for data archive, specify SAS or ManagedIdentity, default value is SAS.
                storage-subscription-id:
                  type: string
                  description: Subscription id of the storage container for data to be exported using ManagedIdentity.
              required:
                - prefix
                - container
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Cache/redis/{cache_name}/flush?api-version=2025-08-01-preview:
    post:
      operationId: Redis_begin_flush_cache
      description: Deletes all of the keys in a cache.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cache_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Redis cache. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatusResult"
  /subscriptions/{subscription_id}/providers/Microsoft.Cache/checkNameAvailability?api-version=2025-08-01-preview:
    post:
      operationId: Redis_check_name_availability
      description: Checks that the redis cache name is valid and is not already in use.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name. Required.
                type:
                  type: string
                  description: Resource type. The only legal value of this property for checking redis cache name availability is 'Microsoft.Cache/redis'. Required.
              required:
                - name
                - type
      responses:
        "200":
          description: Success
