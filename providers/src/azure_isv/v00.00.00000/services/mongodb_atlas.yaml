openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: MongoDB Atlas - An Azure Native ISV Service
  description: MongoDB Atlas organizations provisioned and billed through Azure.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-03-01-preview-stackql-generated
  x-serviceAlias: mongodb_atlas
  x-sdkPackage: azure-mgmt-mongodbatlas
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    ClusterProperties:
      type: object
      description: Properties specific to a MongoDB Atlas Cluster.
      properties:
        clusterName:
          type: string
          description: Name of the MongoDB Atlas Cluster.
        clusterTier:
          $ref: "#/components/schemas/ClusterTier"
          description: 'Cluster tier (FREE, FLEX, M10, M30). Required. Known values are: "FREE", "FLEX", "M10", and "M30".'
        regionName:
          type: string
          description: Azure region where the cluster is deployed. Required.
        mongoDbVersion:
          type: string
          description: MongoDB version running on the cluster.
        backups:
          type: boolean
          description: Whether backups are active for the cluster; null if undetermined.
        state:
          type: string
          description: Current state of the cluster.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
      required:
        - clusterTier
        - regionName
    ClusterTier:
      type: string
      enum:
        - FREE
        - FLEX
        - M10
        - M30
    ResourceProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
    ClusterFlattened:
      type: object
      description: Flattened view of Cluster ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        clusterName:
          type: string
          description: Name of the MongoDB Atlas Cluster.
        clusterTier:
          $ref: "#/components/schemas/ClusterTier"
          description: 'Cluster tier (FREE, FLEX, M10, M30). Required. Known values are: "FREE", "FLEX", "M10", and "M30".'
        regionName:
          type: string
          description: Azure region where the cluster is deployed. Required.
        mongoDbVersion:
          type: string
          description: MongoDB version running on the cluster.
        backups:
          type: boolean
          description: Whether backups are active for the cluster; null if undetermined.
        state:
          type: string
          description: Current state of the cluster.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ClusterFlattened"
        nextLink:
          type: string
    Cluster:
      type: object
      description: The MongoDB Atlas Cluster resource type. A cluster is a managed database deployment within a project.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ClusterProperties"
          description: The resource-specific properties for this resource.
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    OrganizationProperties:
      type: object
      description: Properties specific to Organization.
      properties:
        marketplace:
          $ref: "#/components/schemas/MarketplaceDetails"
          description: Marketplace details of the resource. Required.
        user:
          $ref: "#/components/schemas/UserDetails"
          description: Details of the user. Required.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
        partnerProperties:
          $ref: "#/components/schemas/PartnerProperties"
          description: MongoDB properties.
      required:
        - marketplace
        - user
    MarketplaceDetails:
      type: object
      description: Marketplace details for an organization.
      properties:
        subscriptionId:
          type: string
          description: Azure subscription id for the the marketplace offer is purchased from. Required.
        subscriptionStatus:
          $ref: "#/components/schemas/MarketplaceSubscriptionStatus"
          description: 'Marketplace subscription status. Known values are: "PendingFulfillmentStart", "Subscribed", "Suspended", and "Unsubscribed".'
        offerDetails:
          $ref: "#/components/schemas/OfferDetails"
          description: Offer details for the marketplace that is selected by the user. Required.
      required:
        - subscriptionId
        - offerDetails
    MarketplaceSubscriptionStatus:
      type: string
      enum:
        - PendingFulfillmentStart
        - Subscribed
        - Suspended
        - Unsubscribed
    OfferDetails:
      type: object
      description: Offer details for the marketplace that is selected by the user.
      properties:
        publisherId:
          type: string
          description: Publisher Id for the marketplace offer. Required.
        offerId:
          type: string
          description: Offer Id for the marketplace offer. Required.
        planId:
          type: string
          description: Plan Id for the marketplace offer. Required.
        planName:
          type: string
          description: Plan Name for the marketplace offer.
        termUnit:
          type: string
          description: Plan Display Name for the marketplace offer.
        termId:
          type: string
          description: Plan Display Name for the marketplace offer.
      required:
        - publisherId
        - offerId
        - planId
    UserDetails:
      type: object
      description: User details for an organization.
      properties:
        firstName:
          type: string
          description: First name of the user. Required.
        lastName:
          type: string
          description: Last name of the user. Required.
        emailAddress:
          type: string
          description: Email address of the user. Required.
        upn:
          type: string
          description: User's principal name.
        phoneNumber:
          type: string
          description: User's phone number.
        companyName:
          type: string
          description: Company Name.
      required:
        - firstName
        - lastName
        - emailAddress
    PartnerProperties:
      type: object
      description: MongoDB specific Properties.
      properties:
        organizationId:
          type: string
          description: Organization Id in MongoDB system.
        redirectUrl:
          type: string
          description: Redirect URL for the MongoDB.
        organizationName:
          type: string
          description: Organization name in MongoDB system. Required.
      required:
        - organizationName
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    OrganizationResourceFlattened:
      type: object
      description: Flattened view of OrganizationResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        marketplace:
          $ref: "#/components/schemas/MarketplaceDetails"
          description: Marketplace details of the resource. Required.
        user:
          $ref: "#/components/schemas/UserDetails"
          description: Details of the user. Required.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
        partnerProperties:
          $ref: "#/components/schemas/PartnerProperties"
          description: MongoDB properties.
    PagedOrganizationResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OrganizationResourceFlattened"
        nextLink:
          type: string
    OrganizationResource:
      type: object
      description: The resource model definition for an Azure Organization.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/OrganizationProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    OrganizationResourceUpdateProperties:
      type: object
      description: The updatable properties of the OrganizationResource.
      properties:
        user:
          $ref: "#/components/schemas/UserDetails"
          description: Details of the user.
        partnerProperties:
          $ref: "#/components/schemas/PartnerProperties"
          description: MongoDB properties.
    ProjectProperties:
      type: object
      description: Properties specific to a MongoDB Atlas Project.
      properties:
        projectId:
          type: string
          description: Atlas project id.
        projectName:
          type: string
          description: Atlas project name.
        organizationId:
          type: string
          description: Atlas organization id.
        clusterCount:
          type: integer
          description: Number of clusters in the project.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
    ProjectFlattened:
      type: object
      description: Flattened view of Project ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        projectId:
          type: string
          description: Atlas project id.
        projectName:
          type: string
          description: Atlas project name.
        organizationId:
          type: string
          description: Atlas organization id.
        clusterCount:
          type: integer
          description: Number of clusters in the project.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedProjectFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ProjectFlattened"
        nextLink:
          type: string
    Project:
      type: object
      description: The MongoDB Atlas Project resource type. A project is a logical grouping of clusters within an organization.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ProjectProperties"
          description: The resource-specific properties for this resource.
    TierRegions:
      type: object
      description: Cluster tier and its supported regions.
      properties:
        tier:
          $ref: "#/components/schemas/ClusterTier"
          description: 'Cluster tier name. Required. Known values are: "FREE", "FLEX", "M10", and "M30".'
        regions:
          type: array
          items:
            type: string
          description: Supported region names. Required.
      required:
        - tier
        - regions
    RegionsByTierResponse:
      type: object
      description: Response for regions by cluster tier.
      properties:
        organizationId:
          type: string
          description: Atlas organization id. Required.
        projectId:
          type: string
          description: Atlas project id. Required.
        regionsByTier:
          type: array
          items:
            $ref: "#/components/schemas/TierRegions"
          description: List of cluster tiers and their supported regions. Required.
    TierLimitReachedResponse:
      type: object
      description: Response for tier limit check.
      properties:
        limits:
          type: array
          items:
            $ref: "#/components/schemas/ProjectLimitStatus"
          description: List of project limit statuses. Required.
    ProjectLimitStatus:
      type: object
      description: Usage and limit status for a resource quota in a MongoDB Atlas project.
      properties:
        type:
          $ref: "#/components/schemas/ClusterTier"
          description: 'Type of the limit. Required. Known values are: "FREE", "FLEX", "M10", and "M30".'
        maximum:
          type: integer
          description: Maximum allowed value. Required.
        current:
          type: integer
          description: Current value. Required.
        isReached:
          type: boolean
          description: Whether the limit has been reached. Required.
      required:
        - type
        - maximum
        - current
        - isReached
  x-stackQL-resources:
    clusters:
      id: azure_isv.mongodb_atlas.clusters
      name: clusters
      title: clusters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}~1clusters~1{cluster_name}?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}~1clusters~1{cluster_name}?api-version=2026-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}~1clusters~1{cluster_name}?api-version=2026-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}~1clusters?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/get"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/delete"
    operations:
      id: azure_isv.mongodb_atlas.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1MongoDB.Atlas~1operations?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    organizations:
      id: azure_isv.mongodb_atlas.organizations
      name: organizations
      title: organizations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}?api-version=2026-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}?api-version=2026-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}?api-version=2026-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1MongoDB.Atlas~1organizations?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/get"
          - $ref: "#/components/x-stackQL-resources/organizations/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/organizations/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/organizations/methods/delete"
    projects:
      id: azure_isv.mongodb_atlas.projects
      name: projects
      title: projects
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}?api-version=2026-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}?api-version=2026-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_cluster_tier_regions:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}~1clusterTierRegions?api-version=2026-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        tier_limit_reached:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1MongoDB.Atlas~1organizations~1{organization_name}~1projects~1{project_name}~1tierLimitReached?api-version=2026-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/projects/methods/get"
          - $ref: "#/components/x-stackQL-resources/projects/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/projects/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/projects/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/projects/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations/{organization_name}/projects/{project_name}/clusters/{cluster_name}?api-version=2026-03-01-preview:
    get:
      operationId: Clusters_get
      description: Get a Cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClusterFlattened"
    put:
      operationId: Clusters_begin_create_or_update
      description: Create a Cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ClusterProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cluster"
    delete:
      operationId: Clusters_begin_delete
      description: Delete a Cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations/{organization_name}/projects/{project_name}/clusters?api-version=2026-03-01-preview:
    get:
      operationId: Clusters_list
      description: List Cluster resources by Project.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterFlattened"
  /providers/MongoDB.Atlas/operations?api-version=2026-03-01-preview:
    get:
      operationId: mongodbatlas_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations/{organization_name}?api-version=2026-03-01-preview:
    get:
      operationId: Organizations_get
      description: Get a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResourceFlattened"
    put:
      operationId: Organizations_begin_create_or_update
      description: Create a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/OrganizationProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResource"
    patch:
      operationId: Organizations_begin_update
      description: Update a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/OrganizationResourceUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResource"
    delete:
      operationId: Organizations_begin_delete
      description: Delete a OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations?api-version=2026-03-01-preview:
    get:
      operationId: Organizations_list_by_resource_group
      description: List OrganizationResource resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOrganizationResourceFlattened"
  /subscriptions/{subscription_id}/providers/MongoDB.Atlas/organizations?api-version=2026-03-01-preview:
    get:
      operationId: Organizations_list_by_subscription
      description: List OrganizationResource resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOrganizationResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations/{organization_name}/projects/{project_name}?api-version=2026-03-01-preview:
    get:
      operationId: Projects_get
      description: Get a Project.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectFlattened"
    put:
      operationId: Projects_begin_create_or_update
      description: Create a Project.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ProjectProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
    delete:
      operationId: Projects_begin_delete
      description: Delete a Project.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations/{organization_name}/projects?api-version=2026-03-01-preview:
    get:
      operationId: Projects_list
      description: List Project resources by OrganizationResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedProjectFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations/{organization_name}/projects/{project_name}/clusterTierRegions?api-version=2026-03-01-preview:
    post:
      operationId: Projects_list_cluster_tier_regions
      description: List available regions by cluster tier for the project.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegionsByTierResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/MongoDB.Atlas/organizations/{organization_name}/projects/{project_name}/tierLimitReached?api-version=2026-03-01-preview:
    post:
      operationId: Projects_tier_limit_reached
      description: Check if tier limit is reached for the project.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: organization_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Organization resource. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the MongoDB Atlas Project resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TierLimitReachedResponse"
