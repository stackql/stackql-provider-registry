openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Native Qumulo Scalable File Service
  description: Qumulo multi-protocol scale-out file systems on Azure.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-04-16-stackql-generated
  x-serviceAlias: qumulo
  x-sdkPackage: azure-mgmt-qumulo
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    FileSystemResourceProperties:
      type: object
      description: Properties specific to the Qumulo File System resource.
      properties:
        marketplaceDetails:
          $ref: "#/components/schemas/MarketplaceDetails"
          description: Marketplace details. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", and "Deleted".'
        storageSku:
          type: string
          description: Storage Sku. Required.
        userDetails:
          $ref: "#/components/schemas/UserDetails"
          description: User Details. Required.
        delegatedSubnetId:
          type: string
          description: Delegated subnet id for Vnet injection. Required.
        performanceTier:
          type: string
          description: Pre-Provisioned Performance of the Resource.
        clusterLoginUrl:
          type: string
          description: File system Id of the resource.
        privateIPs:
          type: array
          items:
            type: string
          description: Private IPs of the resource.
        adminPassword:
          type: string
          description: Initial administrator password of the resource. Required.
        availabilityZone:
          type: string
          description: Availability zone.
      required:
        - marketplaceDetails
        - storageSku
        - userDetails
        - delegatedSubnetId
        - adminPassword
    MarketplaceDetails:
      type: object
      description: MarketplaceDetails of Qumulo FileSystem resource.
      properties:
        marketplaceSubscriptionId:
          type: string
          description: Marketplace Subscription Id.
        planId:
          type: string
          description: Plan Id. Required.
        offerId:
          type: string
          description: Offer Id. Required.
        publisherId:
          type: string
          description: Publisher Id.
        termUnit:
          type: string
          description: Term Unit.
        marketplaceSubscriptionStatus:
          $ref: "#/components/schemas/MarketplaceSubscriptionStatus"
          description: 'Marketplace subscription status. Known values are: "PendingFulfillmentStart", "Subscribed", "Suspended", and "Unsubscribed".'
      required:
        - planId
        - offerId
    MarketplaceSubscriptionStatus:
      type: string
      enum:
        - PendingFulfillmentStart
        - Subscribed
        - Suspended
        - Unsubscribed
    ProvisioningState:
      type: string
      enum:
        - Accepted
        - Creating
        - Updating
        - Deleting
        - Succeeded
        - Failed
        - Canceled
        - Deleted
    UserDetails:
      type: object
      description: User Details of Qumulo FileSystem resource.
      properties:
        email:
          type: string
          description: User Email. Required.
      required:
        - email
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    FileSystemResourceFlattened:
      type: object
      description: Flattened view of FileSystemResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        marketplaceDetails:
          $ref: "#/components/schemas/MarketplaceDetails"
          description: Marketplace details. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning State of the resource. Known values are: "Accepted", "Creating", "Updating", "Deleting", "Succeeded", "Failed", "Canceled", and "Deleted".'
        storageSku:
          type: string
          description: Storage Sku. Required.
        userDetails:
          $ref: "#/components/schemas/UserDetails"
          description: User Details. Required.
        delegatedSubnetId:
          type: string
          description: Delegated subnet id for Vnet injection. Required.
        performanceTier:
          type: string
          description: Pre-Provisioned Performance of the Resource.
        clusterLoginUrl:
          type: string
          description: File system Id of the resource.
        privateIPs:
          type: array
          items:
            type: string
          description: Private IPs of the resource.
        adminPassword:
          type: string
          description: Initial administrator password of the resource. Required.
        availabilityZone:
          type: string
          description: Availability zone.
    PagedFileSystemResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FileSystemResourceFlattened"
        nextLink:
          type: string
    FileSystemResource:
      type: object
      description: Concrete tracked resource types can be created by aliasing this type using a specific property type.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/FileSystemResourceProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    FileSystemResourceUpdateProperties:
      type: object
      description: The updatable properties of the FileSystemResource.
      properties:
        marketplaceDetails:
          $ref: "#/components/schemas/MarketplaceDetails"
          description: Marketplace details.
        userDetails:
          $ref: "#/components/schemas/UserDetails"
          description: User Details.
        delegatedSubnetId:
          type: string
          description: Delegated subnet id for Vnet injection.
        performanceTier:
          type: string
          description: Pre-Provisioned Performance of the Resource.
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    file_systems:
      id: azure_isv.qumulo.file_systems
      name: file_systems
      title: file_systems
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Qumulo.Storage~1fileSystems~1{file_system_name}?api-version=2026-04-16/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Qumulo.Storage~1fileSystems~1{file_system_name}?api-version=2026-04-16/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Qumulo.Storage~1fileSystems~1{file_system_name}?api-version=2026-04-16/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Qumulo.Storage~1fileSystems~1{file_system_name}?api-version=2026-04-16/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Qumulo.Storage~1fileSystems?api-version=2026-04-16/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Qumulo.Storage~1fileSystems?api-version=2026-04-16/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/file_systems/methods/get"
          - $ref: "#/components/x-stackQL-resources/file_systems/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/file_systems/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/file_systems/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/file_systems/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/file_systems/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/file_systems/methods/delete"
    operations:
      id: azure_isv.qumulo.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Qumulo.Storage~1operations?api-version=2026-04-16/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Qumulo.Storage/fileSystems/{file_system_name}?api-version=2026-04-16:
    get:
      operationId: FileSystems_get
      description: Get a FileSystemResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: file_system_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the File System resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileSystemResourceFlattened"
    put:
      operationId: FileSystems_begin_create_or_update
      description: Create a FileSystemResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: file_system_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the File System resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/FileSystemResourceProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileSystemResource"
    patch:
      operationId: FileSystems_update
      description: Update a FileSystemResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: file_system_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the File System resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/FileSystemResourceUpdateProperties"
                  description: The updatable properties of the FileSystemResource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileSystemResource"
    delete:
      operationId: FileSystems_begin_delete
      description: Delete a FileSystemResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: file_system_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the File System resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Qumulo.Storage/fileSystems?api-version=2026-04-16:
    get:
      operationId: FileSystems_list_by_resource_group
      description: List FileSystemResource resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFileSystemResourceFlattened"
  /subscriptions/{subscription_id}/providers/Qumulo.Storage/fileSystems?api-version=2026-04-16:
    get:
      operationId: FileSystems_list_by_subscription
      description: List FileSystemResource resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFileSystemResourceFlattened"
  /providers/Qumulo.Storage/operations?api-version=2026-04-16:
    get:
      operationId: qumulo_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
