id: snowflake
name: snowflake
version: v00.00.00000
providerServices:
  account:
    id: account:v00.00.00000
    name: account
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/account.yaml
    title: Snowflake Account API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the account service, combining the
      vendor spec(s): account.yaml, managed-account.yaml.
  apps:
    id: apps:v00.00.00000
    name: apps
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/apps.yaml
    title: Snowflake Apps API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the apps service, combining the vendor
      spec(s): artifact-repository.yaml, image-repository.yaml, notebook.yaml,
      service.yaml, streamlit.yaml.
  cortex:
    id: cortex:v00.00.00000
    name: cortex
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/cortex.yaml
    title: Snowflake Cortex API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the cortex service, combining the
      vendor spec(s): cortex-analyst.yaml, cortex-generic-anthropic.yaml,
      cortex-generic-openai.yaml, cortex-inference.yaml,
      cortex-search-service.yaml.
  databases:
    id: databases:v00.00.00000
    name: databases
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/databases.yaml
    title: Snowflake Databases API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the databases service, combining the
      vendor spec(s): database.yaml, schema.yaml.
  functions:
    id: functions:v00.00.00000
    name: functions
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/functions.yaml
    title: Snowflake Functions API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the functions service, combining the
      vendor spec(s): function.yaml, procedure.yaml, user-defined-function.yaml.
  grants:
    id: grants:v00.00.00000
    name: grants
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/grants.yaml
    title: Snowflake Grants API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the grants service, combining the
      vendor spec(s): grant.yaml.
  integrations:
    id: integrations:v00.00.00000
    name: integrations
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/integrations.yaml
    title: Snowflake Integrations API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the integrations service, combining the
      vendor spec(s): api-integration.yaml, catalog-integration.yaml,
      external-volume.yaml, notification-integration.yaml.
  pipelines:
    id: pipelines:v00.00.00000
    name: pipelines
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/pipelines.yaml
    title: Snowflake Pipelines API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the pipelines service, combining the
      vendor spec(s): alert.yaml, pipe.yaml, sequence.yaml, stage.yaml,
      task.yaml.
  roles:
    id: roles:v00.00.00000
    name: roles
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/roles.yaml
    title: Snowflake Roles API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the roles service, combining the vendor
      spec(s): database-role.yaml, role.yaml, user.yaml.
  security:
    id: security:v00.00.00000
    name: security
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/security.yaml
    title: Snowflake Security API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the security service, combining the
      vendor spec(s): network-policy.yaml, network-rule.yaml,
      password-policy.yaml, secret.yaml, tag.yaml.
  sqlapi:
    id: sqlapi:v00.00.00000
    name: sqlapi
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/sqlapi.yaml
    title: Snowflake Sqlapi API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the sqlapi service, combining the
      vendor spec(s): result.yaml, sqlapi.yaml.
  tables:
    id: tables:v00.00.00000
    name: tables
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/tables.yaml
    title: Snowflake Tables API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the tables service, combining the
      vendor spec(s): dynamic-table.yaml, event-table.yaml, iceberg-table.yaml,
      stream.yaml, table.yaml, view.yaml.
  warehouses:
    id: warehouses:v00.00.00000
    name: warehouses
    preferred: true
    service:
      $ref: snowflake/v00.00.00000/services/warehouses.yaml
    title: Snowflake Warehouses API
    version: v00.00.00000
    description: >-
      The Snowflake REST API surface for the warehouses service, combining the
      vendor spec(s): compute-pool.yaml, warehouse.yaml.
config:
  auth:
    type: bearer
    credentialsenvvar: SNOWFLAKE_PAT
  snake_case_aliases: true
