openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Arc Data Management Client
  description: The AzureArcData management API provides a RESTful set of web APIs to manage Azure Data Services on Azure Arc Resources.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2022-03-01-preview-stackql-generated
  x-serviceAlias: azurearcdata
  x-sdkPackage: azure-mgmt-azurearcdata
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    ActiveDirectoryConnectorProperties:
      type: object
      description: The properties of an Active Directory connector resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        domainServiceAccountLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Username and password for domain service account authentication.
        provisioningState:
          type: string
          description: The provisioning state of the Active Directory connector resource.
        spec:
          $ref: "#/components/schemas/ActiveDirectoryConnectorSpec"
          description: null. Required.
        status:
          $ref: "#/components/schemas/ActiveDirectoryConnectorStatus"
          description: null.
      required:
        - spec
    BasicLoginInformation:
      type: object
      description: Username and password for basic login authentication.
      properties:
        username:
          type: string
          description: Login username.
        password:
          type: string
          description: Login password.
    ActiveDirectoryConnectorSpec:
      type: object
      description: The specifications of the AD Kubernetes resource. All required parameters must be populated in order to send to server.
      properties:
        activeDirectory:
          $ref: "#/components/schemas/ActiveDirectoryConnectorDomainDetails"
          description: null. Required.
        dns:
          $ref: "#/components/schemas/ActiveDirectoryConnectorDNSDetails"
          description: null. Required.
      required:
        - activeDirectory
        - dns
    ActiveDirectoryConnectorDomainDetails:
      type: object
      description: Active Directory domain details. All required parameters must be populated in order to send to server.
      properties:
        realm:
          type: string
          description: Name (uppercase) of the Active Directory domain that this AD connector will be associated with. Required.
        netbiosDomainName:
          type: string
          description: NETBIOS name of the Active Directory domain.
        serviceAccountProvisioning:
          type: string
          description: 'The service account provisioning mode for this Active Directory connector. Known values are: "automatic" and "manual".'
        ouDistinguishedName:
          type: string
          description: The distinguished name of the Active Directory Organizational Unit.
        domainControllers:
          $ref: "#/components/schemas/ActiveDirectoryDomainControllers"
          description: null. Required.
      required:
        - realm
        - domainControllers
    ActiveDirectoryDomainControllers:
      type: object
      description: Details about the Active Directory domain controllers associated with this AD connector instance.
      properties:
        primaryDomainController:
          $ref: "#/components/schemas/ActiveDirectoryDomainController"
          description: Information about the Primary Domain Controller (PDC) in the AD domain.
        secondaryDomainControllers:
          type: array
          items:
            $ref: "#/components/schemas/ActiveDirectoryDomainController"
          description: null.
    ActiveDirectoryDomainController:
      type: object
      description: Information about a domain controller in the AD domain. All required parameters must be populated in order to send to server.
      properties:
        hostname:
          type: string
          description: Fully-qualified domain name of a domain controller in the AD domain. Required.
      required:
        - hostname
    ActiveDirectoryConnectorDNSDetails:
      type: object
      description: DNS server details. All required parameters must be populated in order to send to server.
      properties:
        domainName:
          type: string
          description: DNS domain name for which DNS lookups should be forwarded to the Active Directory DNS servers.
        nameserverIPAddresses:
          type: array
          items:
            type: string
          description: List of Active Directory DNS server IP addresses. Required.
        replicas:
          type: integer
          description: Replica count for DNS proxy service. Default value is 1.
        preferK8sDnsForPtrLookups:
          type: boolean
          description: Flag indicating whether to prefer Kubernetes DNS server response over AD DNS server response for IP address lookups.
      required:
        - nameserverIPAddresses
    ActiveDirectoryConnectorStatus:
      type: object
      description: The status of the Kubernetes custom resource.
      properties:
        "":
          type: object
          properties: {}
          description: Unmatched properties from the message are deserialized to this collection.
        lastUpdateTime:
          type: string
          description: The time that the custom resource was last updated.
        observedGeneration:
          type: integer
          description: The version of the replicaSet associated with the AD connector custom resource.
        state:
          type: string
          description: The state of the AD connector custom resource.
    ActiveDirectoryConnectorResourceFlattened:
      type: object
      description: Flattened view of ActiveDirectoryConnectorResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        domainServiceAccountLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Username and password for domain service account authentication.
        provisioningState:
          type: string
          description: The provisioning state of the Active Directory connector resource.
        spec:
          $ref: "#/components/schemas/ActiveDirectoryConnectorSpec"
          description: null. Required.
        status:
          $ref: "#/components/schemas/ActiveDirectoryConnectorStatus"
          description: null.
    PagedActiveDirectoryConnectorResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ActiveDirectoryConnectorResourceFlattened"
        nextLink:
          type: string
    ActiveDirectoryConnectorResource:
      type: object
      description: Active directory connector resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ActiveDirectoryConnectorProperties"
          description: null. Required.
      required:
        - properties
    ExtendedLocation:
      type: object
      description: The complex type of the extended location.
      properties:
        name:
          type: string
          description: The name of the extended location.
        type:
          type: string
          description: The type of the extended location. "CustomLocation"
    DataControllerProperties:
      type: object
      description: The data controller properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        infrastructure:
          type: string
          description: 'The infrastructure the data controller is running on. Known values are: "azure", "gcp", "aws", "alibaba", "onpremises", and "other".'
        onPremiseProperty:
          $ref: "#/components/schemas/OnPremiseProperty"
          description: Properties from the Kubernetes data controller.
        k8sRaw:
          type: object
          properties: {}
          description: The raw kubernetes information.
        uploadWatermark:
          $ref: "#/components/schemas/UploadWatermark"
          description: Properties on upload watermark. Mostly timestamp for each upload data type.
        lastUploadedDate:
          type: string
          format: date-time
          description: Last uploaded date from Kubernetes cluster. Defaults to current date time.
        basicLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Deprecated. Azure Arc Data Services data controller no longer expose any endpoint. All traffic are exposed through Kubernetes native API.
        metricsDashboardCredential:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Login credential for metrics dashboard on the Kubernetes cluster.
        logsDashboardCredential:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Login credential for logs dashboard on the Kubernetes cluster.
        logAnalyticsWorkspaceConfig:
          $ref: "#/components/schemas/LogAnalyticsWorkspaceConfig"
          description: Log analytics workspace id and primary key.
        uploadServicePrincipal:
          $ref: "#/components/schemas/UploadServicePrincipal"
          description: Deprecated. Service principal is deprecated in favor of Arc Kubernetes service extension managed identity.
        provisioningState:
          type: string
          description: The provisioning state of the Arc Data Controller resource.
        clusterId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the connected cluster the custom location belongs to.
        extensionId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the extension the custom location belongs to.
    OnPremiseProperty:
      type: object
      description: Properties from the Kubernetes data controller. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: A globally unique ID identifying the associated Kubernetes cluster. Required.
        publicSigningKey:
          type: string
          description: Certificate that contains the Kubernetes cluster public key used to verify signing. Required.
        signingCertificateThumbprint:
          type: string
          description: Unique thumbprint returned to customer to verify the certificate being uploaded.
      required:
        - id
        - publicSigningKey
    UploadWatermark:
      type: object
      description: Properties on upload watermark. Mostly timestamp for each upload data type.
      properties:
        metrics:
          type: string
          format: date-time
          description: Last uploaded date for metrics from kubernetes cluster. Defaults to current date time.
        logs:
          type: string
          format: date-time
          description: Last uploaded date for logs from kubernetes cluster. Defaults to current date time.
        usages:
          type: string
          format: date-time
          description: Last uploaded date for usages from kubernetes cluster. Defaults to current date time.
    LogAnalyticsWorkspaceConfig:
      type: object
      description: Log analytics workspace id and primary key.
      properties:
        workspaceId:
          type: string
          description: Azure Log Analytics workspace ID.
        primaryKey:
          type: string
          description: Primary key of the workspace.
    UploadServicePrincipal:
      type: object
      description: Service principal for uploading billing, metrics and logs.
      properties:
        clientId:
          type: string
          description: Client ID of the service principal for uploading data.
        tenantId:
          type: string
          description: Tenant ID of the service principal.
        authority:
          type: string
          description: "Authority for the service principal. Example: https://login.microsoftonline.com/."
        clientSecret:
          type: string
          description: Secret of the service principal.
    DataControllerResourceFlattened:
      type: object
      description: Flattened view of DataControllerResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extendedLocation of the resource.
        infrastructure:
          type: string
          description: 'The infrastructure the data controller is running on. Known values are: "azure", "gcp", "aws", "alibaba", "onpremises", and "other".'
        onPremiseProperty:
          $ref: "#/components/schemas/OnPremiseProperty"
          description: Properties from the Kubernetes data controller.
        k8sRaw:
          type: object
          properties: {}
          description: The raw kubernetes information.
        uploadWatermark:
          $ref: "#/components/schemas/UploadWatermark"
          description: Properties on upload watermark. Mostly timestamp for each upload data type.
        lastUploadedDate:
          type: string
          format: date-time
          description: Last uploaded date from Kubernetes cluster. Defaults to current date time.
        basicLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Deprecated. Azure Arc Data Services data controller no longer expose any endpoint. All traffic are exposed through Kubernetes native API.
        metricsDashboardCredential:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Login credential for metrics dashboard on the Kubernetes cluster.
        logsDashboardCredential:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Login credential for logs dashboard on the Kubernetes cluster.
        logAnalyticsWorkspaceConfig:
          $ref: "#/components/schemas/LogAnalyticsWorkspaceConfig"
          description: Log analytics workspace id and primary key.
        uploadServicePrincipal:
          $ref: "#/components/schemas/UploadServicePrincipal"
          description: Deprecated. Service principal is deprecated in favor of Arc Kubernetes service extension managed identity.
        provisioningState:
          type: string
          description: The provisioning state of the Arc Data Controller resource.
        clusterId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the connected cluster the custom location belongs to.
        extensionId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the extension the custom location belongs to.
    PagedDataControllerResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DataControllerResourceFlattened"
        nextLink:
          type: string
    DataControllerResource:
      type: object
      description: Data controller resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extendedLocation of the resource.
        properties:
          $ref: "#/components/schemas/DataControllerProperties"
          description: The data controller's properties. Required.
      required:
        - location
        - properties
    OperationDisplay:
      type: object
      description: Display metadata associated with the operation. All required parameters must be populated in order to send to server.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name. Required.
        resource:
          type: string
          description: The localized friendly form of the resource type related to this action/operation. Required.
        operation:
          type: string
          description: The localized friendly name for the operation. Required.
        description:
          type: string
          description: The localized friendly description for the operation. Required.
      required:
        - provider
        - resource
        - operation
        - description
    Operation:
      type: object
      description: Azure Data Services on Azure Arc operation definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: The name of the operation being performed on this particular object. Required.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The localized display information for this particular operation / action. Required.
        origin:
          type: string
          description: 'The intended executor of the operation. Known values are: "user" and "system".'
        isDataAction:
          type: boolean
          description: Indicates whether the operation is a data action. Required.
        properties:
          type: object
          properties: {}
          description: Additional descriptions for the operation.
      required:
        - name
        - display
        - isDataAction
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    PostgresInstanceProperties:
      type: object
      description: Postgres Instance properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        dataControllerId:
          type: string
          description: The data controller id.
        admin:
          type: string
          description: The instance admin.
        basicLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Username and password for basic authentication.
        k8sRaw:
          type: object
          properties: {}
          description: The raw kubernetes information.
        lastUploadedDate:
          type: string
          format: date-time
          description: Last uploaded date from Kubernetes cluster. Defaults to current date time.
        provisioningState:
          type: string
          description: The provisioning state of the Azure Arc-enabled PostgreSQL instance.
    PostgresInstanceSku:
      type: object
      description: The resource model definition representing SKU for Azure Database for PostgresSQL - Azure Arc. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: The name of the SKU. It is typically a letter+number code. Required.
        dev:
          type: boolean
          description: Whether dev/test is enabled. When the dev field is set to true, the resource is used for dev/test purpose.
        size:
          type: string
          description: The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code.
        family:
          type: string
          description: If the service has different generations of hardware, for the same SKU, then that can be captured here.
        capacity:
          type: integer
          description: If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted.
        tier:
          type: string
          description: This field is required to be implemented by the Resource Provider if the service has more than one tier. Default value is "Hyperscale".
      required:
        - name
    PostgresInstanceFlattened:
      type: object
      description: Flattened view of PostgresInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extendedLocation of the resource.
        sku:
          $ref: "#/components/schemas/PostgresInstanceSku"
          description: Resource sku.
        dataControllerId:
          type: string
          description: The data controller id.
        admin:
          type: string
          description: The instance admin.
        basicLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Username and password for basic authentication.
        k8sRaw:
          type: object
          properties: {}
          description: The raw kubernetes information.
        lastUploadedDate:
          type: string
          format: date-time
          description: Last uploaded date from Kubernetes cluster. Defaults to current date time.
        provisioningState:
          type: string
          description: The provisioning state of the Azure Arc-enabled PostgreSQL instance.
    PagedPostgresInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PostgresInstanceFlattened"
        nextLink:
          type: string
    PostgresInstance:
      type: object
      description: A Postgres Instance. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extendedLocation of the resource.
        properties:
          $ref: "#/components/schemas/PostgresInstanceProperties"
          description: null. Required.
        sku:
          $ref: "#/components/schemas/PostgresInstanceSku"
          description: Resource sku.
      required:
        - location
        - properties
    SqlManagedInstanceProperties:
      type: object
      description: Properties of sqlManagedInstance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        dataControllerId:
          type: string
          description: null.
        admin:
          type: string
          description: The instance admin user.
        startTime:
          type: string
          description: The instance start time.
        endTime:
          type: string
          description: The instance end time.
        k8sRaw:
          $ref: "#/components/schemas/SqlManagedInstanceK8SRaw"
          description: The raw kubernetes information.
        basicLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Username and password for basic authentication.
        lastUploadedDate:
          type: string
          format: date-time
          description: Last uploaded date from Kubernetes cluster. Defaults to current date time.
        provisioningState:
          type: string
          description: The provisioning state of the Arc-enabled SQL Managed Instance resource.
        activeDirectoryInformation:
          $ref: "#/components/schemas/ActiveDirectoryInformation"
          description: Active Directory information related to this SQL Managed Instance.
        licenseType:
          type: string
          description: 'The license type to apply for this managed instance. Known values are: "BasePrice", "LicenseIncluded", and "DisasterRecovery".'
        clusterId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the connected cluster the custom location belongs to.
        extensionId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the extension the custom location belongs to.
    SqlManagedInstanceK8SRaw:
      type: object
      description: The raw kubernetes information.
      properties:
        "":
          type: object
          properties: {}
          description: Unmatched properties from the message are deserialized to this collection.
        spec:
          $ref: "#/components/schemas/SqlManagedInstanceK8SSpec"
          description: The kubernetes spec information.
    SqlManagedInstanceK8SSpec:
      type: object
      description: The kubernetes spec information.
      properties:
        "":
          type: object
          properties: {}
          description: Unmatched properties from the message are deserialized to this collection.
        scheduling:
          $ref: "#/components/schemas/K8SScheduling"
          description: The kubernetes scheduling information.
        replicas:
          type: integer
          description: This option specifies the number of SQL Managed Instance replicas that will be deployed in your Kubernetes cluster for high availability purposes. If sku.tier is BusinessCritical, allowed values are '2' or '3' with default of '3'. If sku.tier is GeneralPurpose, replicas must be '1'.
    K8SScheduling:
      type: object
      description: The kubernetes scheduling information.
      properties:
        "":
          type: object
          properties: {}
          description: Unmatched properties from the message are deserialized to this collection.
        default:
          $ref: "#/components/schemas/K8SSchedulingOptions"
          description: The kubernetes scheduling options. It describes restrictions used to help Kubernetes select appropriate nodes to host the database service.
    K8SSchedulingOptions:
      type: object
      description: The kubernetes scheduling options. It describes restrictions used to help Kubernetes select appropriate nodes to host the database service.
      properties:
        "":
          type: object
          properties: {}
          description: Unmatched properties from the message are deserialized to this collection.
        resources:
          $ref: "#/components/schemas/K8SResourceRequirements"
          description: The kubernetes resource limits and requests used to restrict or reserve resource usage.
    K8SResourceRequirements:
      type: object
      description: The kubernetes resource limits and requests used to restrict or reserve resource usage.
      properties:
        "":
          type: object
          properties: {}
          description: Unmatched properties from the message are deserialized to this collection.
        requests:
          type: object
          properties: {}
          description: Requests for a kubernetes resource type (e.g 'cpu', 'memory'). The 'cpu' request must be less than or equal to 'cpu' limit. Default 'cpu' is 2, minimum is 1. Default 'memory' is '4Gi', minimum is '2Gi. If sku.tier is GeneralPurpose, maximum 'cpu' is 24 and maximum 'memory' is '128Gi'.
        limits:
          type: object
          properties: {}
          description: Limits for a kubernetes resource type (e.g 'cpu', 'memory'). The 'cpu' request must be less than or equal to 'cpu' limit. Default 'cpu' is 2, minimum is 1. Default 'memory' is '4Gi', minimum is '2Gi. If sku.tier is GeneralPurpose, maximum 'cpu' is 24 and maximum 'memory' is '128Gi'.
    ActiveDirectoryInformation:
      type: object
      description: Active Directory information that related to the resource.
      properties:
        keytabInformation:
          $ref: "#/components/schemas/KeytabInformation"
          description: Keytab information that is used for the Sql Managed Instance when Active Directory authentication is used.
    KeytabInformation:
      type: object
      description: Keytab used for authenticate with Active Directory.
      properties:
        keytab:
          type: string
          description: A base64-encoded keytab.
    SqlManagedInstanceSku:
      type: object
      description: The resource model definition representing SKU for Azure Managed Instance - Azure Arc. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: The name of the SKU. Required. Default value is "vCore".
        tier:
          type: string
          description: 'The pricing tier for the instance. Known values are: "GeneralPurpose" and "BusinessCritical".'
        dev:
          type: boolean
          description: Whether dev/test is enabled. When the dev field is set to true, the resource is used for dev/test purpose.
        size:
          type: string
          description: The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code.
        family:
          type: string
          description: The SKU family.
        capacity:
          type: integer
          description: The SKU capacity.
      required:
        - name
    SqlManagedInstanceFlattened:
      type: object
      description: Flattened view of SqlManagedInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extendedLocation of the resource.
        sku:
          $ref: "#/components/schemas/SqlManagedInstanceSku"
          description: Resource sku.
        dataControllerId:
          type: string
          description: null.
        admin:
          type: string
          description: The instance admin user.
        startTime:
          type: string
          description: The instance start time.
        endTime:
          type: string
          description: The instance end time.
        k8sRaw:
          $ref: "#/components/schemas/SqlManagedInstanceK8SRaw"
          description: The raw kubernetes information.
        basicLoginInformation:
          $ref: "#/components/schemas/BasicLoginInformation"
          description: Username and password for basic authentication.
        lastUploadedDate:
          type: string
          format: date-time
          description: Last uploaded date from Kubernetes cluster. Defaults to current date time.
        provisioningState:
          type: string
          description: The provisioning state of the Arc-enabled SQL Managed Instance resource.
        activeDirectoryInformation:
          $ref: "#/components/schemas/ActiveDirectoryInformation"
          description: Active Directory information related to this SQL Managed Instance.
        licenseType:
          type: string
          description: 'The license type to apply for this managed instance. Known values are: "BasePrice", "LicenseIncluded", and "DisasterRecovery".'
        clusterId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the connected cluster the custom location belongs to.
        extensionId:
          type: string
          description: If a CustomLocation is provided, this contains the ARM id of the extension the custom location belongs to.
    PagedSqlManagedInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SqlManagedInstanceFlattened"
        nextLink:
          type: string
    SqlManagedInstance:
      type: object
      description: A SqlManagedInstance. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/SqlManagedInstanceProperties"
          description: null. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extendedLocation of the resource.
        sku:
          $ref: "#/components/schemas/SqlManagedInstanceSku"
          description: Resource sku.
      required:
        - location
        - properties
    SqlServerInstanceProperties:
      type: object
      description: Properties of SqlServerInstance. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        version:
          type: string
          description: 'SQL Server version. Known values are: "SQL Server 2012", "SQL Server 2014", "SQL Server 2016", "SQL Server 2017", "SQL Server 2019", "SQL Server 2022", and "Unknown".'
        edition:
          type: string
          description: 'SQL Server edition. Known values are: "Evaluation", "Enterprise", "Standard", "Web", "Developer", and "Express".'
        containerResourceId:
          type: string
          description: ARM Resource id of the container resource (Azure Arc for Servers). Required.
        createTime:
          type: string
          description: The time when the resource was created.
        vCore:
          type: string
          description: The number of logical processors used by the SQL Server instance.
        status:
          type: string
          description: 'The cloud connectivity status. Required. Known values are: "Connected", "Disconnected", "Registered", and "Unknown".'
        patchLevel:
          type: string
          description: SQL Server update level.
        collation:
          type: string
          description: SQL Server collation.
        currentVersion:
          type: string
          description: SQL Server current version.
        instanceName:
          type: string
          description: SQL Server instance name.
        tcpDynamicPorts:
          type: string
          description: Dynamic TCP ports used by SQL Server.
        tcpStaticPorts:
          type: string
          description: Static TCP ports used by SQL Server.
        productId:
          type: string
          description: SQL Server product ID.
        licenseType:
          type: string
          description: 'SQL Server license type. Known values are: "Undefined", "Free", "HADR", "ServerCAL", "LicenseOnly", "PAYG", and "Paid".'
        azureDefenderStatusLastUpdated:
          type: string
          format: date-time
          description: Timestamp of last Azure Defender status update.
        azureDefenderStatus:
          type: string
          description: 'Status of Azure Defender. Known values are: "Protected", "Unprotected", and "Unknown".'
        provisioningState:
          type: string
          description: The provisioning state of the Arc-enabled SQL Server resource.
        hostType:
          type: string
          description: 'Type of host for Azure Arc SQL Server. Known values are: "Azure Virtual Machine", "Azure VMWare Virtual Machine", "Azure Kubernetes Service", "AWS VMWare Virtual Machine", "AWS Kubernetes Service", "GCP VMWare Virtual Machine", "GCP Kubernetes Service", "Container", "Virtual Machine", "Physical Server", "AWS Virtual Machine", "GCP Virtual Machine", and "Other".'
      required:
        - containerResourceId
        - status
    SqlServerInstanceFlattened:
      type: object
      description: Flattened view of SqlServerInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        version:
          type: string
          description: 'SQL Server version. Known values are: "SQL Server 2012", "SQL Server 2014", "SQL Server 2016", "SQL Server 2017", "SQL Server 2019", "SQL Server 2022", and "Unknown".'
        edition:
          type: string
          description: 'SQL Server edition. Known values are: "Evaluation", "Enterprise", "Standard", "Web", "Developer", and "Express".'
        containerResourceId:
          type: string
          description: ARM Resource id of the container resource (Azure Arc for Servers). Required.
        createTime:
          type: string
          description: The time when the resource was created.
        vCore:
          type: string
          description: The number of logical processors used by the SQL Server instance.
        status:
          type: string
          description: 'The cloud connectivity status. Required. Known values are: "Connected", "Disconnected", "Registered", and "Unknown".'
        patchLevel:
          type: string
          description: SQL Server update level.
        collation:
          type: string
          description: SQL Server collation.
        currentVersion:
          type: string
          description: SQL Server current version.
        instanceName:
          type: string
          description: SQL Server instance name.
        tcpDynamicPorts:
          type: string
          description: Dynamic TCP ports used by SQL Server.
        tcpStaticPorts:
          type: string
          description: Static TCP ports used by SQL Server.
        productId:
          type: string
          description: SQL Server product ID.
        licenseType:
          type: string
          description: 'SQL Server license type. Known values are: "Undefined", "Free", "HADR", "ServerCAL", "LicenseOnly", "PAYG", and "Paid".'
        azureDefenderStatusLastUpdated:
          type: string
          format: date-time
          description: Timestamp of last Azure Defender status update.
        azureDefenderStatus:
          type: string
          description: 'Status of Azure Defender. Known values are: "Protected", "Unprotected", and "Unknown".'
        provisioningState:
          type: string
          description: The provisioning state of the Arc-enabled SQL Server resource.
        hostType:
          type: string
          description: 'Type of host for Azure Arc SQL Server. Known values are: "Azure Virtual Machine", "Azure VMWare Virtual Machine", "Azure Kubernetes Service", "AWS VMWare Virtual Machine", "AWS Kubernetes Service", "GCP VMWare Virtual Machine", "GCP Kubernetes Service", "Container", "Virtual Machine", "Physical Server", "AWS Virtual Machine", "GCP Virtual Machine", and "Other".'
    PagedSqlServerInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SqlServerInstanceFlattened"
        nextLink:
          type: string
    SqlServerInstance:
      type: object
      description: A SqlServerInstance. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/SqlServerInstanceProperties"
          description: null.
      required:
        - location
  x-stackQL-resources:
    active_directory_connectors:
      id: azure.azurearcdata.active_directory_connectors
      name: active_directory_connectors
      title: active_directory_connectors
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}~1activeDirectoryConnectors~1{active_directory_connector_name}?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}~1activeDirectoryConnectors~1{active_directory_connector_name}?api-version=2022-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}~1activeDirectoryConnectors~1{active_directory_connector_name}?api-version=2022-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}~1activeDirectoryConnectors?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/active_directory_connectors/methods/get"
          - $ref: "#/components/x-stackQL-resources/active_directory_connectors/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/active_directory_connectors/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/active_directory_connectors/methods/delete"
    data_controllers:
      id: azure.azurearcdata.data_controllers
      name: data_controllers
      title: data_controllers
      methods:
        list_in_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzureArcData~1dataControllers?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_in_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_data_controller:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        put_data_controller:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}?api-version=2022-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_data_controller:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}?api-version=2022-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        patch_data_controller:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1dataControllers~1{data_controller_name}?api-version=2022-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/data_controllers/methods/get_data_controller"
          - $ref: "#/components/x-stackQL-resources/data_controllers/methods/list_in_group"
          - $ref: "#/components/x-stackQL-resources/data_controllers/methods/list_in_subscription"
        insert: []
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/data_controllers/methods/delete_data_controller"
    operations:
      id: azure.azurearcdata.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AzureArcData~1operations?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    postgres_instances:
      id: azure.azurearcdata.postgres_instances
      name: postgres_instances
      title: postgres_instances
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1postgresInstances~1{postgres_instance_name}?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1postgresInstances~1{postgres_instance_name}?api-version=2022-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1postgresInstances~1{postgres_instance_name}?api-version=2022-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1postgresInstances~1{postgres_instance_name}?api-version=2022-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzureArcData~1postgresInstances?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1postgresInstances?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/postgres_instances/methods/get"
          - $ref: "#/components/x-stackQL-resources/postgres_instances/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/postgres_instances/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/postgres_instances/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/postgres_instances/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/postgres_instances/methods/delete"
    sql_managed_instances:
      id: azure.azurearcdata.sql_managed_instances
      name: sql_managed_instances
      title: sql_managed_instances
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlManagedInstances~1{sql_managed_instance_name}?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlManagedInstances~1{sql_managed_instance_name}?api-version=2022-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlManagedInstances~1{sql_managed_instance_name}?api-version=2022-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlManagedInstances~1{sql_managed_instance_name}?api-version=2022-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzureArcData~1sqlManagedInstances?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlManagedInstances?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/sql_managed_instances/methods/get"
          - $ref: "#/components/x-stackQL-resources/sql_managed_instances/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/sql_managed_instances/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/sql_managed_instances/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/sql_managed_instances/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/sql_managed_instances/methods/delete"
    sql_server_instances:
      id: azure.azurearcdata.sql_server_instances
      name: sql_server_instances
      title: sql_server_instances
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlServerInstances~1{sql_server_instance_name}?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlServerInstances~1{sql_server_instance_name}?api-version=2022-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlServerInstances~1{sql_server_instance_name}?api-version=2022-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlServerInstances~1{sql_server_instance_name}?api-version=2022-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzureArcData~1sqlServerInstances?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureArcData~1sqlServerInstances?api-version=2022-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/sql_server_instances/methods/get"
          - $ref: "#/components/x-stackQL-resources/sql_server_instances/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/sql_server_instances/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/sql_server_instances/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/sql_server_instances/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/sql_server_instances/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/dataControllers/{data_controller_name}/activeDirectoryConnectors/{active_directory_connector_name}?api-version=2022-03-01-preview:
    get:
      operationId: ActiveDirectoryConnectors_get
      description: Retrieves an Active Directory connector resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: active_directory_connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Active Directory connector instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActiveDirectoryConnectorResourceFlattened"
    put:
      operationId: ActiveDirectoryConnectors_begin_create
      description: Creates or replaces an Active Directory connector resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: active_directory_connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Active Directory connector instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ActiveDirectoryConnectorProperties"
                  description: null. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActiveDirectoryConnectorResource"
    delete:
      operationId: ActiveDirectoryConnectors_begin_delete
      description: Deletes an Active Directory connector resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: active_directory_connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Active Directory connector instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/dataControllers/{data_controller_name}/activeDirectoryConnectors?api-version=2022-03-01-preview:
    get:
      operationId: ActiveDirectoryConnectors_list
      description: List the active directory connectors associated with the given data controller. List the active directory connectors associated with the given data controller.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedActiveDirectoryConnectorResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.AzureArcData/dataControllers?api-version=2022-03-01-preview:
    get:
      operationId: DataControllers_list_in_subscription
      description: List dataController resources in the subscription. List dataController resources in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDataControllerResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/dataControllers?api-version=2022-03-01-preview:
    get:
      operationId: DataControllers_list_in_group
      description: List dataController resources in the resource group. List dataController resources in the resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDataControllerResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/dataControllers/{data_controller_name}?api-version=2022-03-01-preview:
    get:
      operationId: DataControllers_get_data_controller
      description: Retrieves a dataController resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataControllerResourceFlattened"
    put:
      operationId: DataControllers_begin_put_data_controller
      description: Creates or replaces a dataController resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extendedLocation of the resource.
                properties:
                  $ref: "#/components/schemas/DataControllerProperties"
                  description: The data controller's properties. Required.
              required:
                - location
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataControllerResource"
    delete:
      operationId: DataControllers_begin_delete_data_controller
      description: Deletes a dataController resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: DataControllers_begin_patch_data_controller
      description: Updates a dataController resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: data_controller_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the data controller. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/DataControllerProperties"
                  description: The data controller's properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataControllerResource"
  /providers/Microsoft.AzureArcData/operations?api-version=2022-03-01-preview:
    get:
      operationId: azurearcdata_list
      description: Lists all of the available Azure Data Services on Azure Arc API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/postgresInstances/{postgres_instance_name}?api-version=2022-03-01-preview:
    get:
      operationId: PostgresInstances_get
      description: Retrieves a postgres Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: postgres_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of Postgres Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostgresInstanceFlattened"
    put:
      operationId: PostgresInstances_begin_create
      description: Creates or replaces a postgres Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: postgres_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of Postgres Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extendedLocation of the resource.
                properties:
                  $ref: "#/components/schemas/PostgresInstanceProperties"
                  description: null. Required.
                sku:
                  $ref: "#/components/schemas/PostgresInstanceSku"
                  description: Resource sku.
              required:
                - location
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostgresInstance"
    delete:
      operationId: PostgresInstances_begin_delete
      description: Deletes a postgres Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: postgres_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of Postgres Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: PostgresInstances_update
      description: Updates a postgres Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: postgres_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of Postgres Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/PostgresInstanceProperties"
                  description: Postgres Instance properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostgresInstance"
  /subscriptions/{subscription_id}/providers/Microsoft.AzureArcData/postgresInstances?api-version=2022-03-01-preview:
    get:
      operationId: PostgresInstances_list
      description: List postgres Instance resources in the subscription. List postgres Instance resources in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPostgresInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/postgresInstances?api-version=2022-03-01-preview:
    get:
      operationId: PostgresInstances_list_by_resource_group
      description: List postgres Instance resources in the resource group. Get a postgres Instances list by Resource group name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPostgresInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/sqlManagedInstances/{sql_managed_instance_name}?api-version=2022-03-01-preview:
    get:
      operationId: SqlManagedInstances_get
      description: Retrieves a SQL Managed Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_managed_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Managed Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SqlManagedInstanceFlattened"
    put:
      operationId: SqlManagedInstances_begin_create
      description: Creates or replaces a SQL Managed Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_managed_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Managed Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/SqlManagedInstanceProperties"
                  description: null. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extendedLocation of the resource.
                sku:
                  $ref: "#/components/schemas/SqlManagedInstanceSku"
                  description: Resource sku.
              required:
                - location
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SqlManagedInstance"
    delete:
      operationId: SqlManagedInstances_begin_delete
      description: Deletes a SQL Managed Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_managed_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Managed Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: SqlManagedInstances_update
      description: Updates a SQL Managed Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_managed_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Managed Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SqlManagedInstance"
  /subscriptions/{subscription_id}/providers/Microsoft.AzureArcData/sqlManagedInstances?api-version=2022-03-01-preview:
    get:
      operationId: SqlManagedInstances_list
      description: List sqlManagedInstance resources in the subscription. List sqlManagedInstance resources in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSqlManagedInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/sqlManagedInstances?api-version=2022-03-01-preview:
    get:
      operationId: SqlManagedInstances_list_by_resource_group
      description: List sqlManagedInstance resources in the resource group. Gets all sqlManagedInstances in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSqlManagedInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/sqlServerInstances/{sql_server_instance_name}?api-version=2022-03-01-preview:
    get:
      operationId: SqlServerInstances_get
      description: Retrieves a SQL Server Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_server_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Server Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SqlServerInstanceFlattened"
    put:
      operationId: SqlServerInstances_begin_create
      description: Creates or replaces a SQL Server Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_server_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Server Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/SqlServerInstanceProperties"
                  description: null.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SqlServerInstance"
    delete:
      operationId: SqlServerInstances_begin_delete
      description: Deletes a SQL Server Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_server_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Server Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: SqlServerInstances_update
      description: Updates a SQL Server Instance resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: sql_server_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of SQL Server Instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SqlServerInstance"
  /subscriptions/{subscription_id}/providers/Microsoft.AzureArcData/sqlServerInstances?api-version=2022-03-01-preview:
    get:
      operationId: SqlServerInstances_list
      description: List sqlServerInstance resources in the subscription. List sqlServerInstance resources in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSqlServerInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureArcData/sqlServerInstances?api-version=2022-03-01-preview:
    get:
      operationId: SqlServerInstances_list_by_resource_group
      description: List sqlServerInstance resources in the resource group. Gets all sqlServerInstances in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSqlServerInstanceFlattened"
