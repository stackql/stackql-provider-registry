openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Kubernetes Service - Deployment Safeguards
  description: Policy-backed deployment safeguards for AKS clusters.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-05-02-preview-stackql-generated
  x-serviceAlias: container_service_safeguards
  x-sdkPackage: azure-mgmt-containerservicesafeguards
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    DeploymentSafeguardsProperties:
      type: object
      description: DeploymentSafeguards Properties.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning State. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Updating", and "Deleting".'
        level:
          $ref: "#/components/schemas/DeploymentSafeguardsLevel"
          description: 'The deployment safeguards level. Possible values are Warn and Enforce. Required. Known values are: "Warn" and "Enforce".'
        excludedNamespaces:
          type: array
          items:
            type: string
          description: User defined list of namespaces to exclude from Deployment Safeguards. Deployments in these namespaces will not be checked against any safeguards.
        systemExcludedNamespaces:
          type: array
          items:
            type: string
          description: System defined list of namespaces excluded from Deployment Safeguards. These are determined by the underlying provider (such as AKS), and cannot be changed. Deployments in these namespaces will not be checked. Required.
        podSecurityStandardsLevel:
          $ref: "#/components/schemas/PodSecurityStandardsLevel"
          description: 'The pod security standards level. Known values are: "Privileged", "Baseline", and "Restricted".'
      required:
        - level
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Creating
        - Updating
        - Deleting
    DeploymentSafeguardsLevel:
      type: string
      enum:
        - Warn
        - Enforce
    PodSecurityStandardsLevel:
      type: string
      enum:
        - Privileged
        - Baseline
        - Restricted
    DeploymentSafeguardFlattened:
      type: object
      description: Flattened view of DeploymentSafeguard ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        eTag:
          type: string
          description: If eTag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning State. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Updating", and "Deleting".'
        level:
          $ref: "#/components/schemas/DeploymentSafeguardsLevel"
          description: 'The deployment safeguards level. Possible values are Warn and Enforce. Required. Known values are: "Warn" and "Enforce".'
        excludedNamespaces:
          type: array
          items:
            type: string
          description: User defined list of namespaces to exclude from Deployment Safeguards. Deployments in these namespaces will not be checked against any safeguards.
        systemExcludedNamespaces:
          type: array
          items:
            type: string
          description: System defined list of namespaces excluded from Deployment Safeguards. These are determined by the underlying provider (such as AKS), and cannot be changed. Deployments in these namespaces will not be checked. Required.
        podSecurityStandardsLevel:
          $ref: "#/components/schemas/PodSecurityStandardsLevel"
          description: 'The pod security standards level. Known values are: "Privileged", "Baseline", and "Restricted".'
    PagedDeploymentSafeguardFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentSafeguardFlattened"
        nextLink:
          type: string
    DeploymentSafeguard:
      type: object
      description: Deployment Safeguards.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/DeploymentSafeguardsProperties"
          description: The resource-specific properties for this resource.
        eTag:
          type: string
          description: If eTag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.
    OperationDisplay:
      type: object
      description: Localized display information for and operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    deployment_safeguards:
      id: azure.container_service_safeguards.deployment_safeguards
      name: deployment_safeguards
      title: deployment_safeguards
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ContainerService~1deploymentSafeguards~1default?api-version=2025-05-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ContainerService~1deploymentSafeguards~1default?api-version=2025-05-02-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ContainerService~1deploymentSafeguards~1default?api-version=2025-05-02-preview/delete"
          response:
            openAPIDocKey: "204"
        list_raw:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ContainerService~1deploymentSafeguards?api-version=2025-05-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/deployment_safeguards/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/deployment_safeguards/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/deployment_safeguards/methods/delete"
    operations:
      id: azure.container_service_safeguards.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ContainerService~1operations?api-version=2025-05-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /{resource_uri}/providers/Microsoft.ContainerService/deploymentSafeguards/default?api-version=2025-05-02-preview:
    get:
      operationId: DeploymentSafeguards_get
      description: Fetch a deployment safeguard by name.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentSafeguardFlattened"
    put:
      operationId: DeploymentSafeguards_begin_create
      description: Creates or updates a deploymentSafeguard.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/DeploymentSafeguardsProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentSafeguard"
    delete:
      operationId: DeploymentSafeguards_begin_delete
      description: Delete DeploymentSafeguards.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ContainerService/deploymentSafeguards?api-version=2025-05-02-preview:
    get:
      operationId: DeploymentSafeguards_list
      description: List DeploymentSafeguards by parent resource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDeploymentSafeguardFlattened"
  /providers/Microsoft.ContainerService/operations?api-version=2025-05-02-preview:
    get:
      operationId: containerservicesafeguards_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
