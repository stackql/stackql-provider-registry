openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Private Dns Management Client
  description: The Private DNS Management Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-06-01-stackql-generated
  x-serviceAlias: privatedns
  x-sdkPackage: azure-mgmt-privatedns
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    PrivateZoneFlattened:
      type: object
      description: Flattened view of PrivateZone ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource Id for the resource. Example - '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Network/privateDnsZones/{privateDnsZoneName}'. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Example - 'Microsoft.Network/privateDnsZones'.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The Azure Region where the resource lives.
        etag:
          type: string
          description: The ETag of the zone.
        maxNumberOfRecordSets:
          type: integer
          description: The maximum number of record sets that can be created in this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        numberOfRecordSets:
          type: integer
          description: The current number of record sets in this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        maxNumberOfVirtualNetworkLinks:
          type: integer
          description: The maximum number of virtual networks that can be linked to this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        numberOfVirtualNetworkLinks:
          type: integer
          description: The current number of virtual networks that are linked to this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        maxNumberOfVirtualNetworkLinksWithRegistration:
          type: integer
          description: The maximum number of virtual networks that can be linked to this Private DNS zone with registration enabled. This is a read-only property and any attempt to set this value will be ignored.
        numberOfVirtualNetworkLinksWithRegistration:
          type: integer
          description: The current number of virtual networks that are linked to this Private DNS zone with registration enabled. This is a read-only property and any attempt to set this value will be ignored.
        provisioningState:
          type: string
          description: 'The provisioning state of the resource. This is a read-only property and any attempt to set this value will be ignored. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        internalId:
          type: string
          description: Private zone internal Id.
    PagedPrivateZoneFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateZoneFlattened"
        nextLink:
          type: string
    PrivateZone:
      type: object
      description: Describes a Private DNS zone. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource Id for the resource. Example - '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Network/privateDnsZones/{privateDnsZoneName}'. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Example - 'Microsoft.Network/privateDnsZones'.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The Azure Region where the resource lives.
        etag:
          type: string
          description: The ETag of the zone.
        properties:
          type: object
          properties:
            maxNumberOfRecordSets:
              type: integer
              description: The maximum number of record sets that can be created in this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            numberOfRecordSets:
              type: integer
              description: The current number of record sets in this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            maxNumberOfVirtualNetworkLinks:
              type: integer
              description: The maximum number of virtual networks that can be linked to this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            numberOfVirtualNetworkLinks:
              type: integer
              description: The current number of virtual networks that are linked to this Private DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            maxNumberOfVirtualNetworkLinksWithRegistration:
              type: integer
              description: The maximum number of virtual networks that can be linked to this Private DNS zone with registration enabled. This is a read-only property and any attempt to set this value will be ignored.
            numberOfVirtualNetworkLinksWithRegistration:
              type: integer
              description: The current number of virtual networks that are linked to this Private DNS zone with registration enabled. This is a read-only property and any attempt to set this value will be ignored.
            provisioningState:
              type: string
              description: 'The provisioning state of the resource. This is a read-only property and any attempt to set this value will be ignored. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
            internalId:
              type: string
              description: Private zone internal Id.
    ARecord:
      type: object
      description: An A record.
      properties:
        ipv4Address:
          type: string
          description: The IPv4 address of this A record.
    AaaaRecord:
      type: object
      description: An AAAA record.
      properties:
        ipv6Address:
          type: string
          description: The IPv6 address of this AAAA record.
    CnameRecord:
      type: object
      description: A CNAME record.
      properties:
        cname:
          type: string
          description: The canonical name for this CNAME record.
    MxRecord:
      type: object
      description: An MX record.
      properties:
        preference:
          type: integer
          description: The preference value for this MX record.
        exchange:
          type: string
          description: The domain name of the mail host for this MX record.
    PtrRecord:
      type: object
      description: A PTR record.
      properties:
        ptrdname:
          type: string
          description: The PTR target domain name for this PTR record.
    SoaRecord:
      type: object
      description: An SOA record.
      properties:
        host:
          type: string
          description: The domain name of the authoritative name server for this SOA record.
        email:
          type: string
          description: The email contact for this SOA record.
        serialNumber:
          type: integer
          description: The serial number for this SOA record.
        refreshTime:
          type: integer
          description: The refresh value for this SOA record.
        retryTime:
          type: integer
          description: The retry time for this SOA record.
        expireTime:
          type: integer
          description: The expire time for this SOA record.
        minimumTtl:
          type: integer
          description: The minimum value for this SOA record. By convention this is used to determine the negative caching duration.
    SrvRecord:
      type: object
      description: An SRV record.
      properties:
        priority:
          type: integer
          description: The priority value for this SRV record.
        weight:
          type: integer
          description: The weight value for this SRV record.
        port:
          type: integer
          description: The port value for this SRV record.
        target:
          type: string
          description: The target domain name for this SRV record.
    TxtRecord:
      type: object
      description: A TXT record.
      properties:
        value:
          type: array
          items:
            type: string
          description: The text value of this TXT record.
    RecordSetFlattened:
      type: object
      description: Flattened view of RecordSet ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource Id for the resource. Example - '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Network/privateDnsZones/{privateDnsZoneName}'. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Example - 'Microsoft.Network/privateDnsZones'.
        etag:
          type: string
          description: The ETag of the record set.
        metadata:
          type: object
          properties: {}
          description: The metadata attached to the record set.
        ttl:
          type: integer
          description: The TTL (time-to-live) of the records in the record set.
        fqdn:
          type: string
          description: Fully qualified domain name of the record set.
        isAutoRegistered:
          type: boolean
          description: Is the record set auto-registered in the Private DNS zone through a virtual network link?.
        aRecords:
          type: array
          items:
            $ref: "#/components/schemas/ARecord"
          description: The list of A records in the record set.
        aaaaRecords:
          type: array
          items:
            $ref: "#/components/schemas/AaaaRecord"
          description: The list of AAAA records in the record set.
        cnameRecord:
          $ref: "#/components/schemas/CnameRecord"
          description: The CNAME record in the record set.
        mxRecords:
          type: array
          items:
            $ref: "#/components/schemas/MxRecord"
          description: The list of MX records in the record set.
        ptrRecords:
          type: array
          items:
            $ref: "#/components/schemas/PtrRecord"
          description: The list of PTR records in the record set.
        soaRecord:
          $ref: "#/components/schemas/SoaRecord"
          description: The SOA record in the record set.
        srvRecords:
          type: array
          items:
            $ref: "#/components/schemas/SrvRecord"
          description: The list of SRV records in the record set.
        txtRecords:
          type: array
          items:
            $ref: "#/components/schemas/TxtRecord"
          description: The list of TXT records in the record set.
    PagedRecordSetFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RecordSetFlattened"
        nextLink:
          type: string
    RecordSet:
      type: object
      description: Describes a DNS record set (a collection of DNS records with the same name and type) in a Private DNS zone. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource Id for the resource. Example - '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Network/privateDnsZones/{privateDnsZoneName}'. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Example - 'Microsoft.Network/privateDnsZones'.
        etag:
          type: string
          description: The ETag of the record set.
        properties:
          type: object
          properties:
            metadata:
              type: object
              properties: {}
              description: The metadata attached to the record set.
            ttl:
              type: integer
              description: The TTL (time-to-live) of the records in the record set.
            fqdn:
              type: string
              description: Fully qualified domain name of the record set.
            isAutoRegistered:
              type: boolean
              description: Is the record set auto-registered in the Private DNS zone through a virtual network link?.
            aRecords:
              type: array
              items:
                $ref: "#/components/schemas/ARecord"
              description: The list of A records in the record set.
            aaaaRecords:
              type: array
              items:
                $ref: "#/components/schemas/AaaaRecord"
              description: The list of AAAA records in the record set.
            cnameRecord:
              $ref: "#/components/schemas/CnameRecord"
              description: The CNAME record in the record set.
            mxRecords:
              type: array
              items:
                $ref: "#/components/schemas/MxRecord"
              description: The list of MX records in the record set.
            ptrRecords:
              type: array
              items:
                $ref: "#/components/schemas/PtrRecord"
              description: The list of PTR records in the record set.
            soaRecord:
              $ref: "#/components/schemas/SoaRecord"
              description: The SOA record in the record set.
            srvRecords:
              type: array
              items:
                $ref: "#/components/schemas/SrvRecord"
              description: The list of SRV records in the record set.
            txtRecords:
              type: array
              items:
                $ref: "#/components/schemas/TxtRecord"
              description: The list of TXT records in the record set.
    SubResource:
      type: object
      description: Reference to another subresource.
      properties:
        id:
          type: string
          description: Resource ID.
    VirtualNetworkLinkFlattened:
      type: object
      description: Flattened view of VirtualNetworkLink ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource Id for the resource. Example - '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Network/privateDnsZones/{privateDnsZoneName}'. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Example - 'Microsoft.Network/privateDnsZones'.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The Azure Region where the resource lives.
        etag:
          type: string
          description: The ETag of the virtual network link.
        virtualNetwork:
          $ref: "#/components/schemas/SubResource"
          description: The reference of the virtual network.
        registrationEnabled:
          type: boolean
          description: Is auto-registration of virtual machine records in the virtual network in the Private DNS zone enabled?.
        resolutionPolicy:
          type: string
          description: "The resolution policy on the virtual network link. Only applicable for virtual network links to privatelink zones, and for A,AAAA,CNAME queries. When set to 'NxDomainRedirect', Azure DNS resolver falls back to public resolution if private dns query resolution results in non-existent domain response. Known values are: \"Default\" and \"NxDomainRedirect\"."
        virtualNetworkLinkState:
          type: string
          description: "The status of the virtual network link to the Private DNS zone. Possible values are 'InProgress' and 'Done'. This is a read-only property and any attempt to set this value will be ignored. Known values are: \"InProgress\" and \"Completed\"."
        provisioningState:
          type: string
          description: 'The provisioning state of the resource. This is a read-only property and any attempt to set this value will be ignored. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
    PagedVirtualNetworkLinkFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNetworkLinkFlattened"
        nextLink:
          type: string
    VirtualNetworkLink:
      type: object
      description: Describes a link to virtual network for a Private DNS zone. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource Id for the resource. Example - '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Network/privateDnsZones/{privateDnsZoneName}'. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Example - 'Microsoft.Network/privateDnsZones'.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The Azure Region where the resource lives.
        etag:
          type: string
          description: The ETag of the virtual network link.
        properties:
          type: object
          properties:
            virtualNetwork:
              $ref: "#/components/schemas/SubResource"
              description: The reference of the virtual network.
            registrationEnabled:
              type: boolean
              description: Is auto-registration of virtual machine records in the virtual network in the Private DNS zone enabled?.
            resolutionPolicy:
              type: string
              description: "The resolution policy on the virtual network link. Only applicable for virtual network links to privatelink zones, and for A,AAAA,CNAME queries. When set to 'NxDomainRedirect', Azure DNS resolver falls back to public resolution if private dns query resolution results in non-existent domain response. Known values are: \"Default\" and \"NxDomainRedirect\"."
            virtualNetworkLinkState:
              type: string
              description: "The status of the virtual network link to the Private DNS zone. Possible values are 'InProgress' and 'Done'. This is a read-only property and any attempt to set this value will be ignored. Known values are: \"InProgress\" and \"Completed\"."
            provisioningState:
              type: string
              description: 'The provisioning state of the resource. This is a read-only property and any attempt to set this value will be ignored. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
  x-stackQL-resources:
    private_zones:
      id: azure.privatedns.private_zones
      name: private_zones
      title: private_zones
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}?api-version=2024-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}?api-version=2024-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}?api-version=2024-06-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Network~1privateDnsZones?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_zones/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_zones/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/private_zones/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/private_zones/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/private_zones/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/private_zones/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/private_zones/methods/delete"
    record_sets:
      id: azure.privatedns.record_sets
      name: record_sets
      title: record_sets
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2024-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2024-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2024-06-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1ALL?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_type:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1{record_type}?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/get"
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/list_by_type"
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/delete"
    virtual_network_links:
      id: azure.privatedns.virtual_network_links
      name: virtual_network_links
      title: virtual_network_links
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1virtualNetworkLinks~1{virtual_network_link_name}?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1virtualNetworkLinks~1{virtual_network_link_name}?api-version=2024-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1virtualNetworkLinks~1{virtual_network_link_name}?api-version=2024-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1virtualNetworkLinks~1{virtual_network_link_name}?api-version=2024-06-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1privateDnsZones~1{private_zone_name}~1virtualNetworkLinks?api-version=2024-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_network_links/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_network_links/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_network_links/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_network_links/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/virtual_network_links/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_network_links/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/privateDnsZones/{private_zone_name}?api-version=2024-06-01:
    get:
      operationId: PrivateZones_get
      description: Gets a Private DNS zone. Retrieves the zone properties, but not the virtual networks links or the record sets within the zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateZoneFlattened"
    put:
      operationId: PrivateZones_begin_create_or_update
      description: Creates or updates a Private DNS zone. Does not modify Links to virtual networks or DNS records within the zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the Private DNS zone. Omit this value to always overwrite the current zone. Specify the last-seen ETag value to prevent accidentally overwriting any concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new Private DNS zone to be created, but to prevent updating an existing zone. Other values will be ignored. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The Azure Region where the resource lives.
                etag:
                  type: string
                  description: The ETag of the zone.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateZone"
    patch:
      operationId: PrivateZones_begin_update
      description: Updates a Private DNS zone. Does not modify virtual network links or DNS records within the zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the Private DNS zone. Omit this value to always overwrite the current zone. Specify the last-seen ETag value to prevent accidentally overwriting any concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The Azure Region where the resource lives.
                etag:
                  type: string
                  description: The ETag of the zone.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateZone"
    delete:
      operationId: PrivateZones_begin_delete
      description: "Deletes a Private DNS zone. WARNING: All DNS records in the zone will also be deleted. This operation cannot be undone. Private DNS zone cannot be deleted unless all virtual network links to it are removed."
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the Private DNS zone. Omit this value to always delete the current zone. Specify the last-seen ETag value to prevent accidentally deleting any concurrent changes. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Network/privateDnsZones?api-version=2024-06-01:
    get:
      operationId: PrivateZones_list
      description: Lists the Private DNS zones in all resource groups in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of Private DNS zones to return. If not specified, returns up to 100 zones. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateZoneFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/privateDnsZones?api-version=2024-06-01:
    get:
      operationId: PrivateZones_list_by_resource_group
      description: Lists the Private DNS zones within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. If not specified, returns up to 100 record sets. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateZoneFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/privateDnsZones/{private_zone_name}/{record_type}/{relative_record_set_name}?api-version=2024-06-01:
    get:
      operationId: RecordSets_get
      description: Gets a record set.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Known values are: "A", "AAAA", "CNAME", "MX", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordSetFlattened"
    put:
      operationId: RecordSets_create_or_update
      description: Creates or updates a record set within a Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Record sets of type SOA can be updated but not created (they are created when the Private DNS zone is created). Known values are: "A", "AAAA", "CNAME", "MX", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the record set. Omit this value to always overwrite the current record set. Specify the last-seen ETag value to prevent accidentally overwriting any concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new record set to be created, but to prevent updating an existing record set. Other values will be ignored. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                etag:
                  type: string
                  description: The ETag of the record set.
                properties:
                  type: object
                  properties:
                    metadata:
                      type: object
                      properties: {}
                      description: The metadata attached to the record set.
                    ttl:
                      type: integer
                      description: The TTL (time-to-live) of the records in the record set.
                    aRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/ARecord"
                      description: The list of A records in the record set.
                    aaaaRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/AaaaRecord"
                      description: The list of AAAA records in the record set.
                    cnameRecord:
                      $ref: "#/components/schemas/CnameRecord"
                      description: The CNAME record in the record set.
                    mxRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/MxRecord"
                      description: The list of MX records in the record set.
                    ptrRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/PtrRecord"
                      description: The list of PTR records in the record set.
                    soaRecord:
                      $ref: "#/components/schemas/SoaRecord"
                      description: The SOA record in the record set.
                    srvRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/SrvRecord"
                      description: The list of SRV records in the record set.
                    txtRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/TxtRecord"
                      description: The list of TXT records in the record set.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordSet"
    patch:
      operationId: RecordSets_update
      description: Updates a record set within a Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Known values are: "A", "AAAA", "CNAME", "MX", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the record set. Omit this value to always overwrite the current record set. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                etag:
                  type: string
                  description: The ETag of the record set.
                properties:
                  type: object
                  properties:
                    metadata:
                      type: object
                      properties: {}
                      description: The metadata attached to the record set.
                    ttl:
                      type: integer
                      description: The TTL (time-to-live) of the records in the record set.
                    aRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/ARecord"
                      description: The list of A records in the record set.
                    aaaaRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/AaaaRecord"
                      description: The list of AAAA records in the record set.
                    cnameRecord:
                      $ref: "#/components/schemas/CnameRecord"
                      description: The CNAME record in the record set.
                    mxRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/MxRecord"
                      description: The list of MX records in the record set.
                    ptrRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/PtrRecord"
                      description: The list of PTR records in the record set.
                    soaRecord:
                      $ref: "#/components/schemas/SoaRecord"
                      description: The SOA record in the record set.
                    srvRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/SrvRecord"
                      description: The list of SRV records in the record set.
                    txtRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/TxtRecord"
                      description: The list of TXT records in the record set.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordSet"
    delete:
      operationId: RecordSets_delete
      description: Deletes a record set from a Private DNS zone. This operation cannot be undone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Record sets of type SOA cannot be deleted (they are deleted when the Private DNS zone is deleted). Known values are: "A", "AAAA", "CNAME", "MX", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the record set. Omit this value to always delete the current record set. Specify the last-seen ETag value to prevent accidentally deleting any concurrent changes. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/privateDnsZones/{private_zone_name}/ALL?api-version=2024-06-01:
    get:
      operationId: RecordSets_list
      description: Lists all record sets in a Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. If not specified, returns up to 100 record sets. Default value is None.
        - name: $recordsetnamesuffix
          in: query
          required: false
          schema:
            type: string
          description: The suffix label of the record set name to be used to filter the record set enumeration. If this parameter is specified, the returned enumeration will only contain records that end with ".". Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRecordSetFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/privateDnsZones/{private_zone_name}/{record_type}?api-version=2024-06-01:
    get:
      operationId: RecordSets_list_by_type
      description: Lists the record sets of a specified type in a Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of record sets to enumerate. Known values are: "A", "AAAA", "CNAME", "MX", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. If not specified, returns up to 100 record sets. Default value is None.
        - name: $recordsetnamesuffix
          in: query
          required: false
          schema:
            type: string
          description: The suffix label of the record set name to be used to filter the record set enumeration. If this parameter is specified, the returned enumeration will only contain records that end with ".". Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRecordSetFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/privateDnsZones/{private_zone_name}/virtualNetworkLinks/{virtual_network_link_name}?api-version=2024-06-01:
    get:
      operationId: VirtualNetworkLinks_get
      description: Gets a virtual network link to the specified Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: virtual_network_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the virtual network link. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetworkLinkFlattened"
    put:
      operationId: VirtualNetworkLinks_begin_create_or_update
      description: Creates or updates a virtual network link to the specified Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: virtual_network_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the virtual network link. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the virtual network link to the Private DNS zone. Omit this value to always overwrite the current virtual network link. Specify the last-seen ETag value to prevent accidentally overwriting any concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new virtual network link to the Private DNS zone to be created, but to prevent updating an existing link. Other values will be ignored. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The Azure Region where the resource lives.
                etag:
                  type: string
                  description: The ETag of the virtual network link.
                properties:
                  type: object
                  properties:
                    virtualNetwork:
                      $ref: "#/components/schemas/SubResource"
                      description: The reference of the virtual network.
                    registrationEnabled:
                      type: boolean
                      description: Is auto-registration of virtual machine records in the virtual network in the Private DNS zone enabled?.
                    resolutionPolicy:
                      type: string
                      description: "The resolution policy on the virtual network link. Only applicable for virtual network links to privatelink zones, and for A,AAAA,CNAME queries. When set to 'NxDomainRedirect', Azure DNS resolver falls back to public resolution if private dns query resolution results in non-existent domain response. Known values are: \"Default\" and \"NxDomainRedirect\"."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetworkLink"
    patch:
      operationId: VirtualNetworkLinks_begin_update
      description: Updates a virtual network link to the specified Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: virtual_network_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the virtual network link. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the virtual network link to the Private DNS zone. Omit this value to always overwrite the current virtual network link. Specify the last-seen ETag value to prevent accidentally overwriting any concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The Azure Region where the resource lives.
                etag:
                  type: string
                  description: The ETag of the virtual network link.
                properties:
                  type: object
                  properties:
                    virtualNetwork:
                      $ref: "#/components/schemas/SubResource"
                      description: The reference of the virtual network.
                    registrationEnabled:
                      type: boolean
                      description: Is auto-registration of virtual machine records in the virtual network in the Private DNS zone enabled?.
                    resolutionPolicy:
                      type: string
                      description: "The resolution policy on the virtual network link. Only applicable for virtual network links to privatelink zones, and for A,AAAA,CNAME queries. When set to 'NxDomainRedirect', Azure DNS resolver falls back to public resolution if private dns query resolution results in non-existent domain response. Known values are: \"Default\" and \"NxDomainRedirect\"."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetworkLink"
    delete:
      operationId: VirtualNetworkLinks_begin_delete
      description: "Deletes a virtual network link to the specified Private DNS zone. WARNING: In case of a registration virtual network, all auto-registered DNS records in the zone for the virtual network will also be deleted. This operation cannot be undone."
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: virtual_network_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the virtual network link. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the virtual network link to the Private DNS zone. Omit this value to always delete the current zone. Specify the last-seen ETag value to prevent accidentally deleting any concurrent changes. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/privateDnsZones/{private_zone_name}/virtualNetworkLinks?api-version=2024-06-01:
    get:
      operationId: VirtualNetworkLinks_list
      description: Lists the virtual network links to the specified Private DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: private_zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Private DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of virtual network links to return. If not specified, returns up to 100 virtual network links. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualNetworkLinkFlattened"
