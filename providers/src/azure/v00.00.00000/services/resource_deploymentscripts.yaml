openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Deployment Scripts Client
  description: DeploymentScripts Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2020-10-01-stackql-generated
  x-serviceAlias: resource_deploymentscripts
  x-sdkPackage: azure-mgmt-resource-deploymentscripts
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    ManagedServiceIdentity:
      type: object
      description: Managed identity generic object. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        type:
          type: string
          description: Type of the managed identity. "UserAssigned"
        tenantId:
          type: string
          description: ID of the Azure Active Directory.
        userAssignedIdentities:
          type: object
          properties: {}
          description: The list of user-assigned managed identities associated with the resource. Key is the Azure resource Id of the managed identity.
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    DeploymentScript:
      type: object
      description: "Deployment script object. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AzureCliScript, AzurePowerShellScript Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server."
      properties:
        id:
          type: string
          description: String Id used to locate any resource on Azure.
        name:
          type: string
          description: Name of this resource.
        type:
          type: string
          description: Type of this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: Optional property. Managed identity to be used for this deployment script. Currently, only user-assigned MSI is supported.
        location:
          type: string
          description: The location of the ACI and the storage account for the deployment script. Required.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        kind:
          type: string
          description: 'Type of the script. Required. Known values are: "AzurePowerShell" and "AzureCLI".'
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system metadata related to this resource.
      required:
        - location
        - kind
    PagedDeploymentScript:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentScript"
        nextLink:
          type: string
    ScriptLog:
      type: object
      description: Script execution log object. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: String Id used to locate any resource on Azure.
        name:
          type: string
          description: Name of this resource.
        type:
          type: string
          description: Type of this resource.
        properties:
          type: object
          properties:
            log:
              type: string
              description: Script execution logs in text format.
    ScriptLogFlattened:
      type: object
      description: Flattened view of ScriptLog ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: String Id used to locate any resource on Azure.
        name:
          type: string
          description: Name of this resource.
        type:
          type: string
          description: Type of this resource.
        log:
          type: string
          description: Script execution logs in text format.
    ScriptLogsListFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ScriptLogFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    deployment_scripts:
      id: azure.resource_deploymentscripts.deployment_scripts
      name: deployment_scripts
      title: deployment_scripts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.Resources~1deploymentScripts~1{script_name}?api-version=2020-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.Resources~1deploymentScripts~1{script_name}?api-version=2020-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.Resources~1deploymentScripts~1{script_name}?api-version=2020-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.Resources~1deploymentScripts~1{script_name}?api-version=2020-10-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Resources~1deploymentScripts?api-version=2020-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.Resources~1deploymentScripts?api-version=2020-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        get_logs:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.Resources~1deploymentScripts~1{script_name}~1logs?api-version=2020-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_logs_default:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.Resources~1deploymentScripts~1{script_name}~1logs~1default?api-version=2020-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/deployment_scripts/methods/get"
          - $ref: "#/components/x-stackQL-resources/deployment_scripts/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/deployment_scripts/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/deployment_scripts/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/deployment_scripts/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/deployment_scripts/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.Resources/deploymentScripts/{script_name}?api-version=2020-10-01:
    get:
      operationId: DeploymentScripts_get
      description: Gets a deployment script with a given name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: script_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the deployment script. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentScript"
    put:
      operationId: DeploymentScripts_begin_create
      description: Creates a deployment script.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: script_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the deployment script. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: Optional property. Managed identity to be used for this deployment script. Currently, only user-assigned MSI is supported.
                location:
                  type: string
                  description: The location of the ACI and the storage account for the deployment script. Required.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                kind:
                  type: string
                  description: 'Type of the script. Required. Known values are: "AzurePowerShell" and "AzureCLI".'
              required:
                - location
                - kind
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentScript"
    patch:
      operationId: DeploymentScripts_update
      description: Updates deployment script tags with specified values.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: script_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the deployment script. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags to be updated.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentScript"
    delete:
      operationId: DeploymentScripts_delete
      description: Deletes a deployment script. When operation completes, status code 200 returned without content.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: script_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the deployment script. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Resources/deploymentScripts?api-version=2020-10-01:
    get:
      operationId: DeploymentScripts_list_by_subscription
      description: Lists all deployment scripts for a given subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDeploymentScript"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.Resources/deploymentScripts?api-version=2020-10-01:
    get:
      operationId: DeploymentScripts_list_by_resource_group
      description: Lists deployments scripts.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDeploymentScript"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.Resources/deploymentScripts/{script_name}/logs?api-version=2020-10-01:
    get:
      operationId: DeploymentScripts_get_logs
      description: Gets deployment script logs for a given deployment script name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: script_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the deployment script. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScriptLogsListFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.Resources/deploymentScripts/{script_name}/logs/default?api-version=2020-10-01:
    get:
      operationId: DeploymentScripts_get_logs_default
      description: Gets deployment script logs for a given deployment script name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: script_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the deployment script. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: tail
          in: query
          required: false
          schema:
            type: integer
          description: The number of lines to show from the tail of the deployment script log. Valid value is a positive number up to 1000. If 'tail' is not provided, all available logs are shown up to container instance log capacity of 4mb. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScriptLogFlattened"
