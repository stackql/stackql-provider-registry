openapi: 3.0.0
servers:
  - url: https://{endpoint}
    variables:
      endpoint:
        default: ""
        description: The service endpoint host (no scheme), e.g. myaccount.table.cosmos.azure.com:443 - value of the client `endpoint` parameter.
info:
  title: Azure AI Discovery (Data Plane)
  description: Data-plane operations for the Azure AI discovery surface.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-02-01-preview-stackql-generated
  x-serviceAlias: ai_discovery
  x-sdkPackage: azure-ai-discovery
  x-plane: data
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    ByType:
      type: string
      enum:
        - User
        - Application
        - System
    Conversation:
      type: object
      description: A conversation.
      properties:
        name:
          type: string
          description: The conversation name. Required.
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
        createdBy:
          type: string
          description: The ID of the user who created this resource.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who created this resource. Known values are: "User", "Application", and "System".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp when the resource was last updated.
        lastModifiedBy:
          type: string
          description: The ID of the user who updated this resource.
        lastModifiedByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who updated this resource. Known values are: "User", "Application", and "System".'
        displayName:
          type: string
          description: The title.
        investigationName:
          type: string
          description: The Name of the associated Investigation.
        projectName:
          type: string
          description: The name of the associated Project.
    InvestigationStatus:
      type: string
      enum:
        - Created
        - Validated
        - Failed
    Tag:
      type: object
      description: Definition of Tag.
      properties:
        key:
          type: string
          description: Property key.
        value:
          type: string
          description: Property value.
    Investigation:
      type: object
      description: A investigation list item.
      properties:
        name:
          type: string
          description: The investigation name. Required.
        projectName:
          type: string
          description: The parent project name. Required.
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
        createdBy:
          type: string
          description: The ID of the user who created this resource.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who created this resource. Known values are: "User", "Application", and "System".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp when the resource was last updated.
        lastModifiedBy:
          type: string
          description: The ID of the user who updated this resource.
        lastModifiedByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who updated this resource. Known values are: "User", "Application", and "System".'
        status:
          $ref: "#/components/schemas/InvestigationStatus"
          description: 'The status. Known values are: "Created", "Validated", and "Failed".'
        description:
          type: string
          description: The description.
        tags:
          type: array
          items:
            $ref: "#/components/schemas/Tag"
          description: The tags.
        displayName:
          type: string
          description: The title.
    OperationState:
      type: string
      enum:
        - NotStarted
        - Running
        - Succeeded
        - Failed
        - Canceled
    ResourceOperationStatusInvestigationInvestigationError:
      type: object
      description: Provides status details for long running operations.
      properties:
        id:
          type: string
          description: The unique ID of the operation. Required.
        status:
          $ref: "#/components/schemas/OperationState"
          description: 'The status of the operation. Required. Known values are: "NotStarted", "Running", "Succeeded", "Failed", and "Canceled".'
        error:
          type: object
          properties: {}
          description: Error object that describes the error when status is "Failed".
        result:
          $ref: "#/components/schemas/Investigation"
          description: The result of the operation.
      required:
        - id
        - status
    DiscoveryEngineStatus:
      type: string
      enum:
        - Inactive
        - Active
    DiscoveryEngine:
      type: object
      description: Discovery engine instance.
      properties:
        discoveryEngineStatus:
          $ref: "#/components/schemas/DiscoveryEngineStatus"
          description: 'The Discovery Engine status. Required. Known values are: "Inactive" and "Active".'
        systemPrompt:
          type: string
          description: The system prompt.
        configuration:
          type: object
          properties: {}
          description: The Discovery Engine configuration.
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
        createdBy:
          type: string
          description: The ID of the user who created this resource.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who created this resource. Known values are: "User", "Application", and "System".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp when the resource was last updated.
        lastModifiedBy:
          type: string
          description: The ID of the user who updated this resource.
        lastModifiedByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who updated this resource. Known values are: "User", "Application", and "System".'
      required:
        - discoveryEngineStatus
    WorkingMemoryEntry:
      type: object
      description: Working memory entry.
      properties:
        content:
          type: string
          description: The content of the working memory entry. Required.
        type:
          $ref: "#/components/schemas/WorkingMemoryEntryType"
          description: The type of the working memory entry. Required. "thought"
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
      required:
        - content
        - type
    WorkingMemoryEntryType:
      type: string
      enum:
        - thought
    PagedWorkingMemoryEntry:
      type: object
      description: Paged collection of WorkingMemoryEntry items.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/WorkingMemoryEntry"
          description: The WorkingMemoryEntry items on this page. Required.
        nextLink:
          type: string
          description: The link to the next page of items.
      required:
        - value
    StorageAssetReference:
      type: object
      description: Reference to a storage asset with identity information.
      properties:
        id:
          type: string
          description: The ARM resource ID of the storage asset. Required.
        userAssignedIdentity:
          type: string
          description: The ARM resource ID of the User Assigned Managed Identity to access the storage asset.
      required:
        - id
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Accepted
        - Provisioning
        - Updating
        - Deleting
    IndexingStatus:
      type: string
      enum:
        - NotStarted
        - Running
        - Succeeded
        - Canceled
        - Failed
    KnowledgeBaseVersion:
      type: object
      description: A version.
      properties:
        name:
          type: string
          description: The version name. Required.
        id:
          type: string
          description: The ID for the resource.
        version:
          type: string
          description: Version. Required.
        bookshelfName:
          type: string
          description: The name of the associated Bookshelf tracked resource. Required.
        storageAssetReferences:
          type: array
          items:
            $ref: "#/components/schemas/StorageAssetReference"
          description: Storage asset references to index.
        knowledgeBaseUrl:
          type: string
          description: URL to access the knowledge base.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Accepted", "Provisioning", "Updating", and "Deleting".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
        createdBy:
          type: string
          description: The ID of the user who created this resource.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who created this resource. Known values are: "User", "Application", and "System".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp when the resource was last updated.
        lastModifiedBy:
          type: string
          description: The ID of the user who updated this resource.
        lastModifiedByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who updated this resource. Known values are: "User", "Application", and "System".'
        tags:
          type: array
          items:
            $ref: "#/components/schemas/Tag"
          description: The tags.
        description:
          type: string
          description: The description. Required.
        copilotInstruction:
          type: string
          description: The copilot instruction. Required.
        status:
          $ref: "#/components/schemas/IndexingStatus"
          description: 'The status. Known values are: "NotStarted", "Running", "Succeeded", "Canceled", and "Failed".'
      required:
        - description
        - copilotInstruction
    PagedKnowledgeBaseVersion:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/KnowledgeBaseVersion"
        nextLink:
          type: string
    KnowledgeBaseOperationStatus:
      type: object
      description: Status of a KnowledgeBase operation.
      properties:
        id:
          type: string
          description: The unique ID of the operation. Required.
        status:
          $ref: "#/components/schemas/OperationState"
          description: 'The status of the operation. Required. Known values are: "NotStarted", "Running", "Succeeded", "Failed", and "Canceled".'
        error:
          type: object
          properties: {}
          description: Error object that describes the error when status is 'Failed'.
        result:
          $ref: "#/components/schemas/KnowledgeBaseVersion"
          description: The KnowledgeBaseVersion result of the operation.
      required:
        - id
        - status
    KnowledgeBase:
      type: object
      description: A knowledgeBase.
      properties:
        name:
          type: string
          description: The knowledgeBase name. Required.
        id:
          type: string
          description: The ID for the resource.
        version:
          type: string
          description: Version. Required.
        bookshelfName:
          type: string
          description: The name of the associated Bookshelf tracked resource. Required.
        storageAssetReferences:
          type: array
          items:
            $ref: "#/components/schemas/StorageAssetReference"
          description: Storage asset references to index.
        knowledgeBaseUrl:
          type: string
          description: URL to access the knowledge base.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Accepted", "Provisioning", "Updating", and "Deleting".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
        createdBy:
          type: string
          description: The ID of the user who created this resource.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who created this resource. Known values are: "User", "Application", and "System".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp when the resource was last updated.
        lastModifiedBy:
          type: string
          description: The ID of the user who updated this resource.
        lastModifiedByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who updated this resource. Known values are: "User", "Application", and "System".'
        tags:
          type: array
          items:
            $ref: "#/components/schemas/Tag"
          description: The tags.
        description:
          type: string
          description: The description. Required.
        copilotInstruction:
          type: string
          description: The copilot instruction. Required.
        status:
          $ref: "#/components/schemas/IndexingStatus"
          description: 'The status. Known values are: "NotStarted", "Running", "Succeeded", "Canceled", and "Failed".'
      required:
        - description
        - copilotInstruction
    PagedKnowledgeBase:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/KnowledgeBase"
        nextLink:
          type: string
    TaskPriority:
      type: string
      enum:
        - Low
        - Medium
        - High
    TaskAssignee:
      type: object
      description: Task assignee information.
      properties:
        id:
          type: string
          description: The unique identifier of the assignee. Required.
        type:
          $ref: "#/components/schemas/ByType"
          description: 'Type of assignee (User, Application, System, or custom type). Required. Known values are: "User", "Application", and "System".'
      required:
        - id
        - type
    TaskComment:
      type: object
      description: Task comment.
      properties:
        timestamp:
          type: string
          format: date-time
          description: When the comment was created.
        createdBy:
          type: string
          description: ID of the user or application who created the comment. Required.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'Type of creator (User, Application, System, or custom type). Required. Known values are: "User", "Application", and "System".'
        text:
          type: string
          description: The comment text content. Required.
      required:
        - createdBy
        - createdByType
        - text
    TaskStatus:
      type: string
      enum:
        - New
        - OnHold
        - Complete
        - Removed
        - FlaggedHuman
        - FlaggedAi
        - Executing
        - ExecutionDone
        - Stale
        - Failed
        - Incomplete
    ExecutionHistoryEntry:
      type: object
      description: Execution history entry for a task.
      properties:
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the entry was created (ISO 8601 UTC format). Required.
        action:
          type: string
          description: The action that was performed (controlled vocabulary; semi-open). Required.
        createdBy:
          type: string
          description: Identifier of who created this entry (GUID for user | resourceId for application | arbitrary string for other types). Required.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'Type of entity that created this entry (User, Application, System, or custom type). Required. Known values are: "User", "Application", and "System".'
        summary:
          type: string
          description: Brief summary of the execution event.
        responseMessageText:
          type: string
          description: Detailed completion message.
        responseMessageId:
          type: string
          description: Run or message ID for full details.
        additionalDetails:
          type: object
          properties: {}
          description: Freeform key-value pairs for additional details.
      required:
        - createdAt
        - action
        - createdBy
        - createdByType
    TaskResult:
      type: object
      description: Task execution result.
      properties:
        text:
          type: string
          description: The text content of the task result.
        storageAssetIds:
          type: array
          items:
            type: string
          description: Array of storage asset identifiers related to the result.
    Task:
      type: object
      description: Task resource.
      properties:
        name:
          type: string
          description: The unique identifier of the task. Required.
        title:
          type: string
          description: The title of the task.
        priority:
          $ref: "#/components/schemas/TaskPriority"
          description: 'The priority of the task. Known values are: "Low", "Medium", and "High".'
        description:
          type: string
          description: The description of the task.
        validationRequirements:
          type: array
          items:
            type: string
          description: Array of validation requirements for the task.
        parentId:
          type: string
          description: ID of the parent task if this is a subtask.
        dependsOn:
          type: array
          items:
            type: string
          description: IDs of tasks that must complete before this task can be executed.
        relatedTo:
          type: array
          items:
            type: string
          description: IDs of tasks that are related to this task.
        assignedTo:
          $ref: "#/components/schemas/TaskAssignee"
          description: Application or user assigned to this task.
        comments:
          type: array
          items:
            $ref: "#/components/schemas/TaskComment"
          description: Comments or notes about the task.
        status:
          $ref: "#/components/schemas/TaskStatus"
          description: 'The current status of the task. Known values are: "New", "OnHold", "Complete", "Removed", "FlaggedHuman", "FlaggedAi", "Executing", "ExecutionDone", "Stale", "Failed", and "Incomplete".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
        createdBy:
          type: string
          description: The ID of the user who created this resource.
        createdByType:
          $ref: "#/components/schemas/ByType"
          description: 'Type of entity that created the resource (User, Application, System, or custom type). Known values are: "User", "Application", and "System".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp when the resource was last updated.
        lastModifiedBy:
          type: string
          description: The ID of the user who updated this resource.
        lastModifiedByType:
          $ref: "#/components/schemas/ByType"
          description: 'The type of user who updated this resource. Known values are: "User", "Application", and "System".'
        executionHistory:
          type: array
          items:
            $ref: "#/components/schemas/ExecutionHistoryEntry"
          description: History of execution events for this task.
        investigationId:
          type: string
          description: The investigation identifier associated with the task.
        taskResult:
          $ref: "#/components/schemas/TaskResult"
          description: Task execution result with text and storage assets.
        storageAssetIds:
          type: array
          items:
            type: string
          description: List of storage assets related to the task.
    PagedTask:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Task"
        nextLink:
          type: string
    RunResult:
      type: object
      description: Run result.
      properties:
        status:
          type: string
          description: Status of the run.
        runtimeDetails:
          type: string
          description: Human-readable details about the run status. Required.
        createdAt:
          type: string
          format: date-time
          description: The timestamp when the resource was created.
        completedAt:
          type: string
          format: date-time
          description: The time the run completed.
        createdBy:
          type: string
          description: The user that started the tool run.
        toolReport:
          $ref: "#/components/schemas/RunResultToolReport"
          description: Details provided by the tool (rather than the platform).
        outputData:
          type: array
          items:
            $ref: "#/components/schemas/OutputDataUri"
          description: Output data URIs. Required.
        debugInfo:
          type: string
          description: Debugging information. Required.
      required:
        - runtimeDetails
        - outputData
        - debugInfo
    RunResultToolReport:
      type: object
      description: RunResultToolReport.
      properties:
        percentageComplete:
          type: integer
          description: Percentage compete. Required.
        statusInformation:
          type: object
          properties: {}
          description: ":vartype status_information: any"
        logs:
          type: string
          description: Logs from the tool.
      required:
        - percentageComplete
    OutputDataUri:
      type: object
      description: Model for providing the URI of the output collected from a specific container path.
      properties:
        storageUri:
          type: string
          description: The URI of the output data. Required.
        mountPath:
          type: string
          description: The path within the container from which the output was collected. Required.
      required:
        - storageUri
        - mountPath
    OperationStatusRunResultError:
      type: object
      description: Provides status details for long running operations.
      properties:
        id:
          type: string
          description: The unique ID of the operation. Required.
        status:
          $ref: "#/components/schemas/OperationState"
          description: 'The status of the operation. Required. Known values are: "NotStarted", "Running", "Succeeded", "Failed", and "Canceled".'
        error:
          type: object
          properties: {}
          description: Error object that describes the error when status is "Failed".
        result:
          $ref: "#/components/schemas/RunResult"
          description: The result of the operation.
      required:
        - id
        - status
    Operation:
      type: object
      description: Summary information for an operation.
      properties:
        id:
          type: string
          description: Operation id. Required.
        nodepoolId:
          type: string
          description: The nodepool the operation targets. Required.
        status:
          $ref: "#/components/schemas/RunStatus"
          description: 'Current status of the operation. Required. Known values are: "NotStarted", "Running", "Succeeded", "Canceled", and "Failed".'
        runtimeDetails:
          type: string
          description: Human-readable details about the run status. Required.
        createdAt:
          type: string
          format: date-time
          description: When the operation was submitted. Required.
        completedAt:
          type: string
          format: date-time
          description: When the operation completed.
        createdBy:
          type: string
          description: The user who created the operation.
      required:
        - id
        - nodepoolId
        - status
        - runtimeDetails
        - createdAt
    RunStatus:
      type: string
      enum:
        - NotStarted
        - Running
        - Succeeded
        - Canceled
        - Failed
    PagedOperation:
      type: object
      description: Paged collection of Operation items.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
          description: The Operation items on this page. Required.
        nextLink:
          type: string
          description: The link to the next page of items.
      required:
        - value
    ComputeUsage:
      type: object
      description: Overview of compute usage for a project.
      properties:
        supercomputers:
          type: object
          properties: {}
          description: Index of information for each supercomputer in the workspace of a project. Indexed by the (short) name of the supercomputer. Required.
      required:
        - supercomputers
  x-stackQL-resources:
    conversations:
      id: azure.ai_discovery.conversations
      name: conversations
      title: conversations
      methods:
        get:
          operation:
            $ref: "#/paths/~1conversations~1{conversation_name}?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update:
          operation:
            $ref: "#/paths/~1conversations~1{conversation_name}?api-version=2026-02-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1conversations~1{conversation_name}?api-version=2026-02-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1conversations?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
        create:
          operation:
            $ref: "#/paths/~1conversations?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/conversations/methods/get"
          - $ref: "#/components/x-stackQL-resources/conversations/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/conversations/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/conversations/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/conversations/methods/delete"
    investigations:
      id: azure.ai_discovery.investigations
      name: investigations
      title: investigations
      methods:
        get:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        create_or_update:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}?api-version=2026-02-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        create_or_replace:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}?api-version=2026-02-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}?api-version=2026-02-01-preview/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
        get_operation_status:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1operations~1{operation_id}?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_discovery_engine:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1discoveryEngine?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_discovery_engine:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1discoveryEngine?api-version=2026-02-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_discovery_engine_memory:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1discoveryEngine~1workingMemory?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
        start_discovery_engine:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1discoveryEngine:start?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        stop_discovery_engine:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1discoveryEngine:stop?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/investigations/methods/get_operation_status"
          - $ref: "#/components/x-stackQL-resources/investigations/methods/get"
          - $ref: "#/components/x-stackQL-resources/investigations/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/investigations/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/investigations/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/investigations/methods/delete"
    knowledge_base_versions:
      id: azure.ai_discovery.knowledge_base_versions
      name: knowledge_base_versions
      title: knowledge_base_versions
      methods:
        get:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1{version_name}?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        create_or_update:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1{version_name}?api-version=2026-02-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1{version_name}?api-version=2026-02-01-preview/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
        get_latest_version:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1@latest?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        delete_latest_version:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1@latest?api-version=2026-02-01-preview/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_operation_status:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1{version_name}~1operations~1{operation_id}?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        start_indexing:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1{version_name}:startIndexing?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        cancel_indexing:
          operation:
            $ref: "#/paths/~1knowledgeBases~1{knowledge_base_name}~1versions~1{version_name}:cancelIndexing?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/knowledge_base_versions/methods/get_operation_status"
          - $ref: "#/components/x-stackQL-resources/knowledge_base_versions/methods/get"
          - $ref: "#/components/x-stackQL-resources/knowledge_base_versions/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/knowledge_base_versions/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/knowledge_base_versions/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/knowledge_base_versions/methods/delete"
    knowledge_bases:
      id: azure.ai_discovery.knowledge_bases
      name: knowledge_bases
      title: knowledge_bases
      methods:
        list:
          operation:
            $ref: "#/paths/~1knowledgeBases?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/knowledge_bases/methods/list"
        insert: []
        update: []
        delete: []
    tasks:
      id: azure.ai_discovery.tasks
      name: tasks
      title: tasks
      methods:
        get:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks~1{task_name}?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks~1{task_name}?api-version=2026-02-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks~1{task_name}?api-version=2026-02-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
        create:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        start:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks~1{task_name}:start?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        add_comment:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks~1{task_name}:addComment?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        add_execution_history:
          operation:
            $ref: "#/paths/~1projects~1{project_name}~1investigations~1{investigation_name}~1tasks~1{task_name}:addExecutionHistory?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/get"
          - $ref: "#/components/x-stackQL-resources/tasks/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/delete"
    tools:
      id: azure.ai_discovery.tools
      name: tools
      title: tools
      methods:
        get_run_status:
          operation:
            $ref: "#/paths/~1tools~1projects~1{project_name}~1operations~1{operation_id}?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
        get_operations:
          operation:
            $ref: "#/paths/~1tools~1projects~1{project_name}~1operations?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
        get_compute_usage:
          operation:
            $ref: "#/paths/~1tools~1projects~1{project_name}~1computeUsage?api-version=2026-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        run:
          operation:
            $ref: "#/paths/~1tools~1projects~1{project_name}:run?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        cancel_run:
          operation:
            $ref: "#/paths/~1tools~1projects~1{project_name}~1operations~1{operation_id}:cancel?api-version=2026-02-01-preview/post"
          response:
            openAPIDocKey: "200"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tools/methods/get_run_status"
          - $ref: "#/components/x-stackQL-resources/tools/methods/get_operations"
        insert: []
        update: []
        delete: []
paths:
  /conversations/{conversation_name}?api-version=2026-02-01-preview:
    get:
      operationId: Conversations_get
      description: Fetch a Conversation by name.
      parameters:
        - name: conversation_name
          in: path
          required: true
          schema:
            type: string
          description: The conversation name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conversation"
    patch:
      operationId: Conversations_update
      description: Updates a Conversation.
      parameters:
        - name: conversation_name
          in: path
          required: true
          schema:
            type: string
          description: The conversation name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                displayName:
                  type: string
                  description: The title.
                investigationName:
                  type: string
                  description: The Name of the associated Investigation.
                projectName:
                  type: string
                  description: The name of the associated Project.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conversation"
    delete:
      operationId: Conversations_delete
      description: Deletes a Conversation.
      parameters:
        - name: conversation_name
          in: path
          required: true
          schema:
            type: string
          description: The conversation name. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /conversations?api-version=2026-02-01-preview:
    get:
      operationId: Conversations_list
      description: List Conversation resources.
      parameters:
        - name: investigationName
          in: query
          required: false
          schema:
            type: string
          description: The name of the associated Investigation. Default value is None.
        - name: projectName
          in: query
          required: false
          schema:
            type: string
          description: The name of the associated Project. Default value is None.
        - name: createdSince
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: The oldest creation timestamp to keep. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conversation"
    post:
      operationId: Conversations_create
      description: Creates a Conversation.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conversation"
  /projects/{project_name}/investigations/{investigation_name}?api-version=2026-02-01-preview:
    get:
      operationId: Investigations_get
      description: Fetch a Investigation by name.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Investigation"
    patch:
      operationId: Investigations_create_or_update
      description: Updates an Investigation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  type: string
                  description: The description.
                tags:
                  type: array
                  items:
                    $ref: "#/components/schemas/Tag"
                  description: The tags.
                displayName:
                  type: string
                  description: The title.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Investigation"
    put:
      operationId: Investigations_create_or_replace
      description: Creates an Investigation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  type: string
                  description: The description.
                tags:
                  type: array
                  items:
                    $ref: "#/components/schemas/Tag"
                  description: The tags.
                displayName:
                  type: string
                  description: The title.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Investigation"
    delete:
      operationId: Investigations_begin_delete
      description: Delete a Investigation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Investigation"
  /projects/{project_name}/investigations?api-version=2026-02-01-preview:
    get:
      operationId: Investigations_list
      description: List Investigation resources.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: createdSince
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: The oldest creation timestamp to keep. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Investigation"
  /projects/{project_name}/investigations/{investigation_name}/operations/{operation_id}?api-version=2026-02-01-preview:
    get:
      operationId: Investigations_get_operation_status
      description: Get the status of a long-running operation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: The unique ID of the operation. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceOperationStatusInvestigationInvestigationError"
  /projects/{project_name}/investigations/{investigation_name}/discoveryEngine?api-version=2026-02-01-preview:
    get:
      operationId: Investigations_get_discovery_engine
      description: Get the discovery engine for an investigation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryEngine"
    patch:
      operationId: Investigations_update_discovery_engine
      description: Update the discovery engine for an investigation. This will create the discovery engine if it does not already exist.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                discoveryEngineStatus:
                  $ref: "#/components/schemas/DiscoveryEngineStatus"
                  description: 'The Discovery Engine status. Known values are: "Inactive" and "Active".'
                systemPrompt:
                  type: string
                  description: The system prompt.
                configuration:
                  type: object
                  properties: {}
                  description: The Discovery Engine configuration.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryEngine"
  /projects/{project_name}/investigations/{investigation_name}/discoveryEngine/workingMemory?api-version=2026-02-01-preview:
    get:
      operationId: Investigations_get_discovery_engine_memory
      description: List discovery engine working memory entries for an investigation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: skip
          in: query
          required: false
          schema:
            type: integer
          description: Skip results. Default value is None.
        - name: top
          in: query
          required: false
          schema:
            type: integer
          description: Query the top results. Default value is None.
        - name: maxPageSize
          in: query
          required: false
          schema:
            type: integer
          description: Bound the number of results that come back in one response. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedWorkingMemoryEntry"
  /projects/{project_name}/investigations/{investigation_name}/discoveryEngine:start?api-version=2026-02-01-preview:
    post:
      operationId: Investigations_start_discovery_engine
      description: Start the discovery engine for an investigation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryEngine"
  /projects/{project_name}/investigations/{investigation_name}/discoveryEngine:stop?api-version=2026-02-01-preview:
    post:
      operationId: Investigations_stop_discovery_engine
      description: Stop the discovery engine for an investigation.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryEngine"
  /knowledgeBases/{knowledge_base_name}/versions/{version_name}?api-version=2026-02-01-preview:
    get:
      operationId: KnowledgeBaseVersions_get
      description: Fetch a KnowledgeBaseVersion by name.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: The knowledgeBase name. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseVersion"
    patch:
      operationId: KnowledgeBaseVersions_create_or_update
      description: Creates or updates a KnowledgeBaseVersion.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: The knowledgeBase name. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                storageAssetReferences:
                  type: array
                  items:
                    $ref: "#/components/schemas/StorageAssetReference"
                  description: Storage asset references to index.
                tags:
                  type: array
                  items:
                    $ref: "#/components/schemas/Tag"
                  description: The tags.
                description:
                  type: string
                  description: The description. Required.
                copilotInstruction:
                  type: string
                  description: The copilot instruction. Required.
              required:
                - description
                - copilotInstruction
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseVersion"
    delete:
      operationId: KnowledgeBaseVersions_begin_delete
      description: Delete a KnowledgeBaseVersion.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: The knowledgeBase name. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseVersion"
  /knowledgeBases/{knowledge_base_name}/versions?api-version=2026-02-01-preview:
    get:
      operationId: KnowledgeBaseVersions_list
      description: List KnowledgeBaseVersion resources.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: The knowledgeBase name. Required.
        - name: createdSince
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: The oldest creation timestamp to keep. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedKnowledgeBaseVersion"
  /knowledgeBases/{knowledge_base_name}/versions/@latest?api-version=2026-02-01-preview:
    get:
      operationId: KnowledgeBaseVersions_get_latest_version
      description: Get the most recent version of KnowledgeBase.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the KnowledgeBase. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseVersion"
    delete:
      operationId: KnowledgeBaseVersions_begin_delete_latest_version
      description: Delete the most recent version of KnowledgeBase.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the KnowledgeBase. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseVersion"
  /knowledgeBases/{knowledge_base_name}/versions/{version_name}/operations/{operation_id}?api-version=2026-02-01-preview:
    get:
      operationId: KnowledgeBaseVersions_get_operation_status
      description: Get the status of a long-running operation.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: The knowledgeBase name. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version name. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: The unique ID of the operation. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseOperationStatus"
  /knowledgeBases/{knowledge_base_name}/versions/{version_name}:startIndexing?api-version=2026-02-01-preview:
    post:
      operationId: KnowledgeBaseVersions_begin_start_indexing
      description: Start indexing.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: The knowledgeBase name. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseVersion"
  /knowledgeBases/{knowledge_base_name}/versions/{version_name}:cancelIndexing?api-version=2026-02-01-preview:
    post:
      operationId: KnowledgeBaseVersions_begin_cancel_indexing
      description: Cancel indexing.
      parameters:
        - name: knowledge_base_name
          in: path
          required: true
          schema:
            type: string
          description: The knowledgeBase name. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeBaseVersion"
  /knowledgeBases?api-version=2026-02-01-preview:
    get:
      operationId: KnowledgeBases_list
      description: List KnowledgeBase resources.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedKnowledgeBase"
  /projects/{project_name}/investigations/{investigation_name}/tasks/{task_name}?api-version=2026-02-01-preview:
    get:
      operationId: Tasks_get
      description: Get a task by ID.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The unique identifier of the task. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
    patch:
      operationId: Tasks_update
      description: Patch (partial update) a task (e.g. status, description, validation requirements, dependencies, result).
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The unique identifier of the task. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  description: The title of the task.
                priority:
                  $ref: "#/components/schemas/TaskPriority"
                  description: 'The priority of the task. Known values are: "Low", "Medium", and "High".'
                description:
                  type: string
                  description: The description of the task.
                validationRequirements:
                  type: array
                  items:
                    type: string
                  description: Array of validation requirements for the task.
                parentId:
                  type: string
                  description: ID of the parent task if this is a subtask.
                dependsOn:
                  type: array
                  items:
                    type: string
                  description: IDs of tasks that must complete before this task can be executed.
                relatedTo:
                  type: array
                  items:
                    type: string
                  description: IDs of tasks that are related to this task.
                assignedTo:
                  $ref: "#/components/schemas/TaskAssignee"
                  description: Application or user assigned to this task.
                comments:
                  type: array
                  items:
                    $ref: "#/components/schemas/TaskComment"
                  description: Comments or notes about the task.
                status:
                  $ref: "#/components/schemas/TaskStatus"
                  description: 'The current status of the task. Known values are: "New", "OnHold", "Complete", "Removed", "FlaggedHuman", "FlaggedAi", "Executing", "ExecutionDone", "Stale", "Failed", and "Incomplete".'
                createdByType:
                  $ref: "#/components/schemas/ByType"
                  description: 'Type of entity that created the resource (User, Application, System, or custom type). Known values are: "User", "Application", and "System".'
                investigationId:
                  type: string
                  description: The investigation identifier associated with the task.
                taskResult:
                  $ref: "#/components/schemas/TaskResult"
                  description: Task execution result with text and storage assets.
                storageAssetIds:
                  type: array
                  items:
                    type: string
                  description: List of storage assets related to the task.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
    delete:
      operationId: Tasks_delete
      description: Delete a task by ID.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The unique identifier of the task. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /projects/{project_name}/investigations/{investigation_name}/tasks?api-version=2026-02-01-preview:
    get:
      operationId: Tasks_list
      description: List tasks with optional OData filters.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: filter
          in: query
          required: false
          schema:
            type: string
          description: "OData filter expression. Supported fields: investigationId, status, createdByType, priority, createdAt, lastModifiedAt. Example: status eq 'new' or status eq 'executing'. Default value is None."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTask"
    post:
      operationId: Tasks_create
      description: Create a new task.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  description: The title of the task.
                priority:
                  $ref: "#/components/schemas/TaskPriority"
                  description: 'The priority of the task. Known values are: "Low", "Medium", and "High".'
                description:
                  type: string
                  description: The description of the task.
                validationRequirements:
                  type: array
                  items:
                    type: string
                  description: Array of validation requirements for the task.
                parentId:
                  type: string
                  description: ID of the parent task if this is a subtask.
                dependsOn:
                  type: array
                  items:
                    type: string
                  description: IDs of tasks that must complete before this task can be executed.
                relatedTo:
                  type: array
                  items:
                    type: string
                  description: IDs of tasks that are related to this task.
                assignedTo:
                  $ref: "#/components/schemas/TaskAssignee"
                  description: Application or user assigned to this task.
                comments:
                  type: array
                  items:
                    $ref: "#/components/schemas/TaskComment"
                  description: Comments or notes about the task.
                status:
                  $ref: "#/components/schemas/TaskStatus"
                  description: 'The current status of the task. Known values are: "New", "OnHold", "Complete", "Removed", "FlaggedHuman", "FlaggedAi", "Executing", "ExecutionDone", "Stale", "Failed", and "Incomplete".'
                createdByType:
                  $ref: "#/components/schemas/ByType"
                  description: 'Type of entity that created the resource (User, Application, System, or custom type). Known values are: "User", "Application", and "System".'
                investigationId:
                  type: string
                  description: The investigation identifier associated with the task.
                taskResult:
                  $ref: "#/components/schemas/TaskResult"
                  description: Task execution result with text and storage assets.
                storageAssetIds:
                  type: array
                  items:
                    type: string
                  description: List of storage assets related to the task.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
  /projects/{project_name}/investigations/{investigation_name}/tasks/{task_name}:start?api-version=2026-02-01-preview:
    post:
      operationId: Tasks_start
      description: Start execution of a task.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The task name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                assignee:
                  $ref: "#/components/schemas/TaskAssignee"
                  description: Assignee that will execute the task. Optional.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
  /projects/{project_name}/investigations/{investigation_name}/tasks/{task_name}:addComment?api-version=2026-02-01-preview:
    post:
      operationId: Tasks_add_comment
      description: Add a comment to a task.
      parameters:
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The task name. Required.
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                timestamp:
                  type: string
                  format: date-time
                  description: When the comment was created.
                createdBy:
                  type: string
                  description: ID of the user or application who created the comment. Required.
                createdByType:
                  $ref: "#/components/schemas/ByType"
                  description: 'Type of creator (User, Application, System, or custom type). Required. Known values are: "User", "Application", and "System".'
                text:
                  type: string
                  description: The comment text content. Required.
              required:
                - createdBy
                - createdByType
                - text
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
  /projects/{project_name}/investigations/{investigation_name}/tasks/{task_name}:addExecutionHistory?api-version=2026-02-01-preview:
    post:
      operationId: Tasks_add_execution_history
      description: Add an execution history entry to a task.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: The project name. Required.
        - name: investigation_name
          in: path
          required: true
          schema:
            type: string
          description: The investigation name. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The task name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                createdAt:
                  type: string
                  format: date-time
                  description: Timestamp when the entry was created (ISO 8601 UTC format). Required.
                action:
                  type: string
                  description: The action that was performed (controlled vocabulary; semi-open). Required.
                createdBy:
                  type: string
                  description: Identifier of who created this entry (GUID for user | resourceId for application | arbitrary string for other types). Required.
                createdByType:
                  $ref: "#/components/schemas/ByType"
                  description: 'Type of entity that created this entry (User, Application, System, or custom type). Required. Known values are: "User", "Application", and "System".'
                summary:
                  type: string
                  description: Brief summary of the execution event.
                responseMessageText:
                  type: string
                  description: Detailed completion message.
                responseMessageId:
                  type: string
                  description: Run or message ID for full details.
                additionalDetails:
                  type: object
                  properties: {}
                  description: Freeform key-value pairs for additional details.
              required:
                - createdAt
                - action
                - createdBy
                - createdByType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
  /tools/projects/{project_name}/operations/{operation_id}?api-version=2026-02-01-preview:
    get:
      operationId: Tools_get_run_status
      description: Used for to poll status of a Tool run.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the associated Project. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: The unique ID of the operation. Required.
        - name: logCount
          in: query
          required: false
          schema:
            type: integer
          description: Number of log lines to return (0-2500). Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatusRunResultError"
  /tools/projects/{project_name}/operations?api-version=2026-02-01-preview:
    get:
      operationId: Tools_get_operations
      description: List tool runs.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the associated Project. Required.
        - name: skip
          in: query
          required: false
          schema:
            type: integer
          description: Skip results (pagination control). Default value is None.
        - name: top
          in: query
          required: false
          schema:
            type: integer
          description: Query the top results (pagination control). Default value is None.
        - name: maxPageSize
          in: query
          required: false
          schema:
            type: integer
          description: Bound the number of results that come back in one response (pagination control). Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /tools/projects/{project_name}/computeUsage?api-version=2026-02-01-preview:
    get:
      operationId: Tools_get_compute_usage
      description: Examine compute usage.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the associated Project. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComputeUsage"
  /tools/projects/{project_name}:run?api-version=2026-02-01-preview:
    post:
      operationId: Tools_begin_run
      description: Run the specified tool in the context of the specified project.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the associated Project. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunResult"
  /tools/projects/{project_name}/operations/{operation_id}:cancel?api-version=2026-02-01-preview:
    post:
      operationId: Tools_cancel_run
      description: Cancel an ongoing tool run.
      parameters:
        - name: project_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the associated Project. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: ID of the operation to cancel. Required.
      responses:
        "200":
          description: Success
