openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Marketplace Ordering
  description: Accept and manage marketplace legal terms for offers and plans.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-01-01-stackql-generated
  x-serviceAlias: marketplace_ordering
  x-sdkPackage: azure-mgmt-marketplaceordering
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    AgreementTermsFlattened:
      type: object
      description: Flattened view of AgreementTerms ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system meta data relating to this resource.
        publisher:
          type: string
          description: Publisher identifier string of image being deployed.
        product:
          type: string
          description: Offer identifier string of image being deployed.
        plan:
          type: string
          description: Plan identifier string of image being deployed.
        licenseTextLink:
          type: string
          description: Link to HTML with Microsoft and Publisher terms.
        privacyPolicyLink:
          type: string
          description: Link to the privacy policy of the publisher.
        marketplaceTermsLink:
          type: string
          description: Link to HTML with Azure Marketplace terms.
        retrieveDatetime:
          type: string
          description: Date and time in UTC of when the terms were accepted. This is empty if Accepted is false.
        signature:
          type: string
          description: Terms signature.
        accepted:
          type: boolean
          description: If any version of the terms have been accepted, otherwise false.
    OldAgreementTerms:
      type: object
      description: Terms properties for provided Publisher/Offer/Plan tuple. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        properties:
          type: object
          properties:
            id:
              type: string
              description: A unique identifier of the agreement.
            publisher:
              type: string
              description: Publisher identifier string of image being deployed.
            offer:
              type: string
              description: Offer identifier string of image being deployed.
            signDate:
              type: string
              format: date-time
              description: Date and time in UTC of when the terms were accepted. This is empty if state is cancelled.
            cancelDate:
              type: string
              format: date-time
              description: Date and time in UTC of when the terms were cancelled. This is empty if state is active.
            state:
              type: string
              description: 'Whether the agreement is active or cancelled. Known values are: "Active" and "Canceled".'
    OldAgreementTermsFlattened:
      type: object
      description: Flattened view of OldAgreementTerms ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        publisher:
          type: string
          description: Publisher identifier string of image being deployed.
        offer:
          type: string
          description: Offer identifier string of image being deployed.
        signDate:
          type: string
          format: date-time
          description: Date and time in UTC of when the terms were accepted. This is empty if state is cancelled.
        cancelDate:
          type: string
          format: date-time
          description: Date and time in UTC of when the terms were cancelled. This is empty if state is active.
        state:
          type: string
          description: 'Whether the agreement is active or cancelled. Known values are: "Active" and "Canceled".'
    OldAgreementTermsListFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OldAgreementTermsFlattened"
        nextLink:
          type: string
    AgreementTerms:
      type: object
      description: Terms properties for provided Publisher/Offer/Plan tuple. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system meta data relating to this resource.
        properties:
          type: object
          properties:
            publisher:
              type: string
              description: Publisher identifier string of image being deployed.
            product:
              type: string
              description: Offer identifier string of image being deployed.
            plan:
              type: string
              description: Plan identifier string of image being deployed.
            licenseTextLink:
              type: string
              description: Link to HTML with Microsoft and Publisher terms.
            privacyPolicyLink:
              type: string
              description: Link to the privacy policy of the publisher.
            marketplaceTermsLink:
              type: string
              description: Link to HTML with Azure Marketplace terms.
            retrieveDatetime:
              type: string
              description: Date and time in UTC of when the terms were accepted. This is empty if Accepted is false.
            signature:
              type: string
              description: Terms signature.
            accepted:
              type: boolean
              description: If any version of the terms have been accepted, otherwise false.
    OperationDisplay:
      type: object
      description: The object that represents the operation.
      properties:
        provider:
          type: string
          description: "Service provider: Microsoft.MarketplaceOrdering."
        resource:
          type: string
          description: "Resource on which the operation is performed: Agreement, virtualmachine, etc."
        operation:
          type: string
          description: "Operation type: Get Agreement, Sign Agreement, Cancel Agreement etc."
        description:
          type: string
          description: Operation description.
    Operation:
      type: object
      description: Microsoft.MarketplaceOrdering REST API operation.
      properties:
        name:
          type: string
          description: "Operation name: {provider}/{resource}/{operation}."
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that represents the operation.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    marketplace_agreements:
      id: azure.marketplace_ordering.marketplace_agreements
      name: marketplace_agreements
      title: marketplace_agreements
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.MarketplaceOrdering~1offerTypes~1{offer_type}~1publishers~1{publisher_id}~1offers~1{offer_id}~1plans~1{plan_id}~1agreements~1current?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.MarketplaceOrdering~1offerTypes~1{offer_type}~1publishers~1{publisher_id}~1offers~1{offer_id}~1plans~1{plan_id}~1agreements~1current?api-version=2021-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.MarketplaceOrdering~1agreements?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_agreement:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.MarketplaceOrdering~1agreements~1{publisher_id}~1offers~1{offer_id}~1plans~1{plan_id}?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        sign:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.MarketplaceOrdering~1agreements~1{publisher_id}~1offers~1{offer_id}~1plans~1{plan_id}~1sign?api-version=2021-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        cancel:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.MarketplaceOrdering~1agreements~1{publisher_id}~1offers~1{offer_id}~1plans~1{plan_id}~1cancel?api-version=2021-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/marketplace_agreements/methods/get"
          - $ref: "#/components/x-stackQL-resources/marketplace_agreements/methods/get_agreement"
          - $ref: "#/components/x-stackQL-resources/marketplace_agreements/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/marketplace_agreements/methods/create"
        update: []
        delete: []
    operations:
      id: azure.marketplace_ordering.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.MarketplaceOrdering~1operations?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.MarketplaceOrdering/offerTypes/{offer_type}/publishers/{publisher_id}/offers/{offer_id}/plans/{plan_id}/agreements/current?api-version=2021-01-01:
    get:
      operationId: MarketplaceAgreements_get
      description: Get marketplace terms.
      parameters:
        - name: offer_type
          in: path
          required: true
          schema:
            type: string
          description: Offer Type, currently only virtualmachine type is supported. "virtualmachine" Required.
        - name: publisher_id
          in: path
          required: true
          schema:
            type: string
          description: Publisher identifier string of image being deployed. Required.
        - name: offer_id
          in: path
          required: true
          schema:
            type: string
          description: Offer identifier string of image being deployed. Required.
        - name: plan_id
          in: path
          required: true
          schema:
            type: string
          description: Plan identifier string of image being deployed. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgreementTermsFlattened"
    put:
      operationId: MarketplaceAgreements_create
      description: Save marketplace terms.
      parameters:
        - name: offer_type
          in: path
          required: true
          schema:
            type: string
          description: Offer Type, currently only virtualmachine type is supported. "virtualmachine" Required.
        - name: publisher_id
          in: path
          required: true
          schema:
            type: string
          description: Publisher identifier string of image being deployed. Required.
        - name: offer_id
          in: path
          required: true
          schema:
            type: string
          description: Offer identifier string of image being deployed. Required.
        - name: plan_id
          in: path
          required: true
          schema:
            type: string
          description: Plan identifier string of image being deployed. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    publisher:
                      type: string
                      description: Publisher identifier string of image being deployed.
                    product:
                      type: string
                      description: Offer identifier string of image being deployed.
                    plan:
                      type: string
                      description: Plan identifier string of image being deployed.
                    licenseTextLink:
                      type: string
                      description: Link to HTML with Microsoft and Publisher terms.
                    privacyPolicyLink:
                      type: string
                      description: Link to the privacy policy of the publisher.
                    marketplaceTermsLink:
                      type: string
                      description: Link to HTML with Azure Marketplace terms.
                    retrieveDatetime:
                      type: string
                      description: Date and time in UTC of when the terms were accepted. This is empty if Accepted is false.
                    signature:
                      type: string
                      description: Terms signature.
                    accepted:
                      type: boolean
                      description: If any version of the terms have been accepted, otherwise false.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgreementTerms"
  /subscriptions/{subscription_id}/providers/Microsoft.MarketplaceOrdering/agreements?api-version=2021-01-01:
    get:
      operationId: MarketplaceAgreements_list
      description: List marketplace agreements in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OldAgreementTermsListFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.MarketplaceOrdering/agreements/{publisher_id}/offers/{offer_id}/plans/{plan_id}?api-version=2021-01-01:
    get:
      operationId: MarketplaceAgreements_get_agreement
      description: Get marketplace agreement.
      parameters:
        - name: publisher_id
          in: path
          required: true
          schema:
            type: string
          description: Publisher identifier string of image being deployed. Required.
        - name: offer_id
          in: path
          required: true
          schema:
            type: string
          description: Offer identifier string of image being deployed. Required.
        - name: plan_id
          in: path
          required: true
          schema:
            type: string
          description: Plan identifier string of image being deployed. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OldAgreementTermsFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.MarketplaceOrdering/agreements/{publisher_id}/offers/{offer_id}/plans/{plan_id}/sign?api-version=2021-01-01:
    post:
      operationId: MarketplaceAgreements_sign
      description: Sign marketplace terms.
      parameters:
        - name: publisher_id
          in: path
          required: true
          schema:
            type: string
          description: Publisher identifier string of image being deployed. Required.
        - name: offer_id
          in: path
          required: true
          schema:
            type: string
          description: Offer identifier string of image being deployed. Required.
        - name: plan_id
          in: path
          required: true
          schema:
            type: string
          description: Plan identifier string of image being deployed. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OldAgreementTerms"
  /subscriptions/{subscription_id}/providers/Microsoft.MarketplaceOrdering/agreements/{publisher_id}/offers/{offer_id}/plans/{plan_id}/cancel?api-version=2021-01-01:
    post:
      operationId: MarketplaceAgreements_cancel
      description: Cancel marketplace terms.
      parameters:
        - name: publisher_id
          in: path
          required: true
          schema:
            type: string
          description: Publisher identifier string of image being deployed. Required.
        - name: offer_id
          in: path
          required: true
          schema:
            type: string
          description: Offer identifier string of image being deployed. Required.
        - name: plan_id
          in: path
          required: true
          schema:
            type: string
          description: Plan identifier string of image being deployed. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OldAgreementTerms"
  /providers/Microsoft.MarketplaceOrdering/operations?api-version=2021-01-01:
    get:
      operationId: marketplaceordering_list
      description: Lists all of the available Microsoft.MarketplaceOrdering REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
