openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Monitor Workspaces Mgmt Client
  description: MonitorWorkspacesMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-10-03-stackql-generated
  x-serviceAlias: monitorworkspaces
  x-sdkPackage: azure-mgmt-monitorworkspaces
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    AzureMonitorWorkspace:
      type: object
      description: Properties of an Azure Monitor Workspace.
      properties:
        accountId:
          type: string
          description: The immutable Id of the Azure Monitor Workspace. This property is read-only.
        metrics:
          $ref: "#/components/schemas/AzureMonitorWorkspaceMetrics"
          description: Properties related to the metrics container in the Azure Monitor Workspace.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state of the Azure Monitor Workspace. Set to Succeeded if everything is healthy. Known values are: "Succeeded", "Failed", and "Canceled".'
        defaultIngestionSettings:
          $ref: "#/components/schemas/AzureMonitorWorkspaceDefaultIngestionSettings"
          description: The Data Collection Rule and Endpoint used for ingestion by default.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connections.
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: 'Gets or sets allow or disallow public network access to Azure Monitor Workspace. Known values are: "Enabled" and "Disabled".'
    AzureMonitorWorkspaceMetrics:
      type: object
      description: Properties related to the metrics container in the Azure Monitor Workspace.
      properties:
        prometheusQueryEndpoint:
          type: string
          description: The Prometheus query endpoint for the Azure Monitor Workspace.
        internalId:
          type: string
          description: An internal identifier for the metrics container. Only to be used by the system.
        enableAccessUsingResourcePermissions:
          type: boolean
          description: Flag that indicates whether to enable access using resource permissions.
    ResourceProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
    AzureMonitorWorkspaceDefaultIngestionSettings:
      type: object
      description: The Data Collection Rule and Endpoint used for ingestion by default.
      properties:
        dataCollectionRuleResourceId:
          type: string
          description: The Azure resource Id of the default data collection rule for this Azure Monitor Workspace.
        dataCollectionEndpointResourceId:
          type: string
          description: The Azure resource Id of the default data collection endpoint for this Azure Monitor Workspace.
        dataCollectionRuleImmutableId:
          type: string
          description: The immutable Id of the default data collection rule for this Azure Monitor Workspace.
        ingestionEndpoints:
          $ref: "#/components/schemas/IngestionEndpoints"
          description: The ingestion endpoints for this Azure Monitor Workspace.
    IngestionEndpoints:
      type: object
      description: The ingestion endpoints for an Azure Monitor Workspace.
      properties:
        metrics:
          type: string
          description: The metrics ingestion endpoint for this Azure Monitor Workspace.
    PrivateEndpointConnection:
      type: object
      description: The private endpoint connection resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
          description: The private endpoint connection properties.
    PrivateEndpointConnectionProperties:
      type: object
      description: Properties of the private endpoint connection.
      properties:
        groupIds:
          type: array
          items:
            type: string
          description: The group ids for the private endpoint resource.
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider. Required.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
      required:
        - privateLinkServiceConnectionState
    PrivateEndpoint:
      type: object
      description: The private endpoint resource.
      properties:
        id:
          type: string
          description: The resource identifier of the private endpoint.
    PrivateLinkServiceConnectionState:
      type: object
      description: A collection of information about the state of the connection between service consumer and provider.
      properties:
        status:
          $ref: "#/components/schemas/PrivateEndpointServiceConnectionStatus"
          description: 'Indicates whether the connection has been Approved/Rejected/Removed by the owner of the service. Known values are: "Pending", "Approved", and "Rejected".'
        description:
          type: string
          description: The reason for approval/rejection of the connection.
        actionsRequired:
          type: string
          description: A message indicating if changes on the service provider require any updates on the consumer.
    PrivateEndpointServiceConnectionStatus:
      type: string
      enum:
        - Pending
        - Approved
        - Rejected
    PrivateEndpointConnectionProvisioningState:
      type: string
      enum:
        - Succeeded
        - Creating
        - Deleting
        - Failed
    PublicNetworkAccess:
      type: string
      enum:
        - Enabled
        - Disabled
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    AzureMonitorWorkspaceResourceFlattened:
      type: object
      description: Flattened view of AzureMonitorWorkspaceResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        etag:
          type: string
          description: '"If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.").'
        accountId:
          type: string
          description: The immutable Id of the Azure Monitor Workspace. This property is read-only.
        metrics:
          $ref: "#/components/schemas/AzureMonitorWorkspaceMetrics"
          description: Properties related to the metrics container in the Azure Monitor Workspace.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state of the Azure Monitor Workspace. Set to Succeeded if everything is healthy. Known values are: "Succeeded", "Failed", and "Canceled".'
        defaultIngestionSettings:
          $ref: "#/components/schemas/AzureMonitorWorkspaceDefaultIngestionSettings"
          description: The Data Collection Rule and Endpoint used for ingestion by default.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connections.
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: 'Gets or sets allow or disallow public network access to Azure Monitor Workspace. Known values are: "Enabled" and "Disabled".'
    PagedAzureMonitorWorkspaceResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AzureMonitorWorkspaceResourceFlattened"
        nextLink:
          type: string
    AzureMonitorWorkspaceResource:
      type: object
      description: An Azure Monitor Workspace definition.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AzureMonitorWorkspace"
          description: Resource properties.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        etag:
          type: string
          description: '"If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.").'
      required:
        - location
    IssueProperties:
      type: object
      description: The issue properties.
      properties:
        title:
          type: string
          description: The issue title. Required.
        status:
          $ref: "#/components/schemas/Status"
          description: 'The issue status. Required. Known values are: "New", "InProgress", "Mitigated", "Closed", and "Canceled".'
        severity:
          type: string
          description: The issue severity. Required.
        investigations:
          type: array
          items:
            $ref: "#/components/schemas/InvestigationMetadata"
          description: The list of investigations in the issue. Required.
        impactTime:
          type: string
          format: date-time
          description: The issue impact time (in UTC). Required.
        investigationsCount:
          type: integer
          description: The number of investigations in the issue. Required.
        background:
          $ref: "#/components/schemas/Background"
          description: The issue background information.
        notifications:
          $ref: "#/components/schemas/Notifications"
          description: The issue notification settings.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
      required:
        - title
        - status
        - severity
        - impactTime
    Status:
      type: string
      enum:
        - New
        - InProgress
        - Mitigated
        - Closed
        - Canceled
    InvestigationMetadata:
      type: object
      description: Properties of the current investigation.
      properties:
        id:
          type: string
          description: The unique identifier of the investigation. Required.
        createdAt:
          type: string
          format: date-time
          description: The creation time of the investigation (in UTC). Required.
      required:
        - id
        - createdAt
    Background:
      type: object
      description: The issue background information.
      properties:
        type:
          type: string
          description: The background type.
        text:
          type: string
          description: The background text.
        details:
          type: array
          items:
            $ref: "#/components/schemas/BackgroundDetails"
          description: The background details.
    BackgroundDetails:
      type: object
      description: A background details element.
      properties:
        name:
          type: string
          description: The background details name. Required.
        value:
          type: string
          description: The background details value. Required.
      required:
        - name
        - value
    Notifications:
      type: object
      description: Issue notification settings.
      properties:
        updateTypes:
          type: array
          items:
            $ref: "#/components/schemas/IssueNotificationType"
          description: The types of updates that trigger notifications.
        actionGroupIds:
          type: array
          items:
            type: string
          description: The action group IDs to notify.
        excludeDefaultActionGroups:
          type: boolean
          description: Whether to exclude default action groups from notifications.
    IssueNotificationType:
      type: object
      description: "Base properties for an issue notification type. You probably want to use the sub-classes and not this class directly. Known sub-classes are: IssueCreationNotificationType, OnChangeNotificationType, TimeBasedUpdatesNotificationType"
      properties:
        updateType:
          type: string
          description: 'The type of update that triggers the notification. Required. Known values are: "IssueCreation", "TimeBased", and "OnChange".'
      required:
        - updateType
    IssueResourceFlattened:
      type: object
      description: Flattened view of IssueResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        title:
          type: string
          description: The issue title. Required.
        status:
          $ref: "#/components/schemas/Status"
          description: 'The issue status. Required. Known values are: "New", "InProgress", "Mitigated", "Closed", and "Canceled".'
        severity:
          type: string
          description: The issue severity. Required.
        investigations:
          type: array
          items:
            $ref: "#/components/schemas/InvestigationMetadata"
          description: The list of investigations in the issue. Required.
        impactTime:
          type: string
          format: date-time
          description: The issue impact time (in UTC). Required.
        investigationsCount:
          type: integer
          description: The number of investigations in the issue. Required.
        background:
          $ref: "#/components/schemas/Background"
          description: The issue background information.
        notifications:
          $ref: "#/components/schemas/Notifications"
          description: The issue notification settings.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state of the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedIssueResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/IssueResourceFlattened"
        nextLink:
          type: string
    PagedRelatedAlert:
      type: object
      description: Paged collection of RelatedAlert items.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RelatedAlert"
          description: The RelatedAlert items on this page. Required.
        nextLink:
          type: string
          description: The link to the next page of items.
      required:
        - value
    RelatedAlert:
      type: object
      description: Properties of an alert which is related to the issue.
      properties:
        id:
          type: string
          description: The alert ID. Required.
        relevance:
          $ref: "#/components/schemas/Relevance"
          description: "The alerts's relevance status. Required. Known values are: \"None\", \"Relevant\", and \"Irrelevant\"."
        origin:
          $ref: "#/components/schemas/Origin"
          description: The source that related the alert to the issue. Required.
        addedAt:
          type: string
          format: date-time
          description: The time this relation was added to the issue (in UTC). Required.
        lastModifiedAt:
          type: string
          format: date-time
          description: The last update time of this relation (in UTC). Required.
      required:
        - id
        - relevance
    Relevance:
      type: string
      enum:
        - None
        - Relevant
        - Irrelevant
    Origin:
      type: object
      description: Details about the origin of the entity - the source that added it to the issue.
      properties:
        addedBy:
          type: string
          description: The ID of the origin - for example, in case of 'Manual', the user ID/app ID, and in case of 'Automatic', the name of the automatic system. Required.
        addedByType:
          $ref: "#/components/schemas/AddedByType"
          description: 'The source of the origin - Manual or Automatic. Required. Known values are: "Manual" and "Automatic".'
      required:
        - addedBy
        - addedByType
    AddedByType:
      type: string
      enum:
        - Manual
        - Automatic
    PagedRelatedResource:
      type: object
      description: Paged collection of RelatedResource items.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RelatedResource"
          description: The RelatedResource items on this page. Required.
        nextLink:
          type: string
          description: The link to the next page of items.
      required:
        - value
    RelatedResource:
      type: object
      description: Properties of a resource which is related to the issue.
      properties:
        id:
          type: string
          description: The resource ID. Required.
        relevance:
          $ref: "#/components/schemas/Relevance"
          description: "The resource's relevance status. Required. Known values are: \"None\", \"Relevant\", and \"Irrelevant\"."
        origin:
          $ref: "#/components/schemas/Origin"
          description: The source that related the resource to the issue. Required.
        addedAt:
          type: string
          format: date-time
          description: The time this relation was added to the issue (in UTC). Required.
        lastModifiedAt:
          type: string
          format: date-time
          description: The last update time of this relation (in UTC). Required.
      required:
        - id
        - relevance
    IssueResource:
      type: object
      description: The Issue resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/IssueProperties"
          description: The resource-specific properties for this resource.
    IssuePropertiesUpdate:
      type: object
      description: The issue properties for update.
      properties:
        title:
          type: string
          description: The issue title.
        status:
          $ref: "#/components/schemas/Status"
          description: 'The issue status. Known values are: "New", "InProgress", "Mitigated", "Closed", and "Canceled".'
        severity:
          type: string
          description: The issue severity.
        impactTime:
          type: string
          format: date-time
          description: The issue impact time (in UTC).
        background:
          $ref: "#/components/schemas/Background"
          description: The issue background information.
        notifications:
          $ref: "#/components/schemas/Notifications"
          description: The issue notification settings.
    InvestigationResult:
      type: object
      description: Details about the investigation result.
      properties:
        id:
          type: string
          description: The identifier of the investigation. Required.
        origin:
          $ref: "#/components/schemas/Origin"
          description: The origin of the investigation.
        createdAt:
          type: string
          format: date-time
          description: The creation time of the investigation (in UTC).
        lastModifiedAt:
          type: string
          format: date-time
          description: The last update time of the investigation (in UTC).
        result:
          type: string
          description: The result of this investigation. Required.
      required:
        - id
        - result
    RelatedAlerts:
      type: object
      description: A list of related alerts.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RelatedAlert"
          description: A list of related alerts. Required.
      required:
        - value
    RelatedResources:
      type: object
      description: A list of related resources.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RelatedResource"
          description: A list of related resources. Required.
      required:
        - value
    BackgroundVisualization:
      type: object
      description: The issue background visualization.
      properties:
        visualization:
          type: string
          description: The background visualization content, in Adaptive Card format. Required.
        origin:
          $ref: "#/components/schemas/Origin"
          description: The background visualization origin. Required.
      required:
        - visualization
    MetricsContainer:
      type: object
      description: Properties of a metrics container.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state of the metrics container. Known values are: "Succeeded", "Failed", and "Canceled".'
        version:
          type: string
          description: The version of Metrics Query Service that this AMW will use for all metric queries.
    MetricsContainerResourceFlattened:
      type: object
      description: Flattened view of MetricsContainerResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state of the metrics container. Known values are: "Succeeded", "Failed", and "Canceled".'
        version:
          type: string
          description: The version of Metrics Query Service that this AMW will use for all metric queries.
    PagedMetricsContainerResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MetricsContainerResourceFlattened"
        nextLink:
          type: string
    MetricsContainerResource:
      type: object
      description: Metrics container resource for an Azure Monitor Workspace.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/MetricsContainer"
          description: The resource-specific properties for this resource.
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    ArmOrigin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/ArmOrigin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    azure_monitor_workspaces:
      id: azure.monitorworkspaces.azure_monitor_workspaces
      name: azure_monitor_workspaces
      title: azure_monitor_workspaces
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}?api-version=2025-10-03/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}?api-version=2025-10-03/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}?api-version=2025-10-03/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Monitor~1accounts?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/azure_monitor_workspaces/methods/get"
          - $ref: "#/components/x-stackQL-resources/azure_monitor_workspaces/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/azure_monitor_workspaces/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/azure_monitor_workspaces/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/azure_monitor_workspaces/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/azure_monitor_workspaces/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/azure_monitor_workspaces/methods/delete"
    issue:
      id: azure.monitorworkspaces.issue
      name: issue
      title: issue
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}?api-version=2025-10-03/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}?api-version=2025-10-03/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}?api-version=2025-10-03/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_alerts:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1listAlerts?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1listResources?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        add_investigation_result:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1addInvestigationResult?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        fetch_investigation_result:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1fetchInvestigationResult?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        add_or_update_alerts:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1addOrUpdateAlerts?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        add_or_update_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1addOrUpdateResources?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        fetch_background_visualization:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1fetchBackgroundVisualization?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        set_background_visualization:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1issues~1{issue_name}~1setBackgroundVisualization?api-version=2025-10-03/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/issue/methods/get"
          - $ref: "#/components/x-stackQL-resources/issue/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/issue/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/issue/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/issue/methods/delete"
    metrics_containers:
      id: azure.monitorworkspaces.metrics_containers
      name: metrics_containers
      title: metrics_containers
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1metricsContainers~1{metrics_container_name}?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1metricsContainers~1{metrics_container_name}?api-version=2025-10-03/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_azure_monitor_workspace:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Monitor~1accounts~1{azure_monitor_workspace_name}~1metricsContainers?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/metrics_containers/methods/get"
          - $ref: "#/components/x-stackQL-resources/metrics_containers/methods/list_by_azure_monitor_workspace"
        insert:
          - $ref: "#/components/x-stackQL-resources/metrics_containers/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/metrics_containers/methods/create_or_update"
        delete: []
    operations:
      id: azure.monitorworkspaces.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Monitor~1operations?api-version=2025-10-03/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}?api-version=2025-10-03:
    get:
      operationId: AzureMonitorWorkspaces_get
      description: Returns the specified Azure Monitor Workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureMonitorWorkspaceResourceFlattened"
    put:
      operationId: AzureMonitorWorkspaces_create_or_update
      description: Creates or updates an Azure Monitor Workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AzureMonitorWorkspace"
                  description: Resource properties.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureMonitorWorkspaceResource"
    patch:
      operationId: AzureMonitorWorkspaces_update
      description: Updates part of an Azure Monitor Workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                properties:
                  $ref: "#/components/schemas/AzureMonitorWorkspace"
                  description: Resource properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureMonitorWorkspaceResource"
    delete:
      operationId: AzureMonitorWorkspaces_begin_delete
      description: Deletes an Azure Monitor Workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts?api-version=2025-10-03:
    get:
      operationId: AzureMonitorWorkspaces_list_by_resource_group
      description: Lists all Azure Monitor Workspaces in the specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureMonitorWorkspaceResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Monitor/accounts?api-version=2025-10-03:
    get:
      operationId: AzureMonitorWorkspaces_list_by_subscription
      description: Lists all Azure Monitor Workspaces in the specified subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureMonitorWorkspaceResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}?api-version=2025-10-03:
    get:
      operationId: Issue_get
      description: Get issue properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssueResourceFlattened"
    put:
      operationId: Issue_create
      description: Create a new issue or updates an existing one.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: related
          in: query
          required: false
          schema:
            type: string
          description: "Related resource or alert that is to be added to the issue (default: empty - the issue will be created without any related resources or alerts). Default value is None."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/IssueProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssueResource"
    patch:
      operationId: Issue_update
      description: Update an issue.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/IssuePropertiesUpdate"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssueResource"
    delete:
      operationId: Issue_delete
      description: Delete an issue.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues?api-version=2025-10-03:
    get:
      operationId: Issue_list
      description: List all issues under the parent.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedIssueResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/listAlerts?api-version=2025-10-03:
    post:
      operationId: Issue_list_alerts
      description: List all alerts in the issue - this method uses pagination to return all alerts.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filter:
                  type: string
                  description: "The filter to apply on the operation. For example, to filter by relevance, use \"$filter=relevance eq 'Relevant'\". Note: this property is currently a placeholder and is not in use."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRelatedAlert"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/listResources?api-version=2025-10-03:
    post:
      operationId: Issue_list_resources
      description: List all resources in the issue - this method uses pagination to return all resources.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filter:
                  type: string
                  description: "The filter to apply on the operation. For example, to filter by relevance, use \"$filter=relevance eq 'Relevant'\". Note: this property is currently a placeholder and is not in use."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRelatedResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/addInvestigationResult?api-version=2025-10-03:
    post:
      operationId: Issue_add_investigation_result
      description: Adds investigation result.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: The identifier of the investigation. Required.
                origin:
                  $ref: "#/components/schemas/Origin"
                  description: The origin of the investigation.
                createdAt:
                  type: string
                  format: date-time
                  description: The creation time of the investigation (in UTC).
                lastModifiedAt:
                  type: string
                  format: date-time
                  description: The last update time of the investigation (in UTC).
                result:
                  type: string
                  description: The result of this investigation. Required.
              required:
                - id
                - result
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestigationResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/fetchInvestigationResult?api-version=2025-10-03:
    post:
      operationId: Issue_fetch_investigation_result
      description: Fetch investigation result.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                investigationId:
                  type: string
                  description: The unique identifier of the investigation. Required.
              required:
                - investigationId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestigationResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/addOrUpdateAlerts?api-version=2025-10-03:
    post:
      operationId: Issue_add_or_update_alerts
      description: Add or update alerts associated with an issue.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                value:
                  type: array
                  items:
                    $ref: "#/components/schemas/RelatedAlert"
                  description: A list of related alerts. Required.
              required:
                - value
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RelatedAlerts"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/addOrUpdateResources?api-version=2025-10-03:
    post:
      operationId: Issue_add_or_update_resources
      description: Add or update resources associated with an issue.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                value:
                  type: array
                  items:
                    $ref: "#/components/schemas/RelatedResource"
                  description: A list of related resources. Required.
              required:
                - value
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RelatedResources"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/fetchBackgroundVisualization?api-version=2025-10-03:
    post:
      operationId: Issue_fetch_background_visualization
      description: Fetch the background visualization of the issue.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackgroundVisualization"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/issues/{issue_name}/setBackgroundVisualization?api-version=2025-10-03:
    post:
      operationId: Issue_set_background_visualization
      description: Set the background visualization for the issue.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: issue_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the IssueResource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                visualization:
                  type: string
                  description: The background visualization content, in Adaptive Card format. Required.
              required:
                - visualization
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/metricsContainers/{metrics_container_name}?api-version=2025-10-03:
    get:
      operationId: MetricsContainers_get
      description: Gets metrics container settings for a monitoring account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: metrics_container_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the MetricsContainer. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricsContainerResourceFlattened"
    put:
      operationId: MetricsContainers_create_or_update
      description: Creates or updates metrics container settings for a monitoring account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: metrics_container_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the MetricsContainer. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/MetricsContainer"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricsContainerResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Monitor/accounts/{azure_monitor_workspace_name}/metricsContainers?api-version=2025-10-03:
    get:
      operationId: MetricsContainers_list_by_azure_monitor_workspace
      description: Lists metrics containers for a monitoring account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_monitor_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Monitor Workspace. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMetricsContainerResourceFlattened"
  /providers/Microsoft.Monitor/operations?api-version=2025-10-03:
    get:
      operationId: monitorworkspaces_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
