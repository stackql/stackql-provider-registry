openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Managed Identities
  description: User-assigned managed identities and federated identity credentials (Microsoft.ManagedIdentity).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-05-31-preview-stackql-generated
  x-serviceAlias: msi
  x-sdkPackage: azure-mgmt-msi
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    FederatedIdentityCredentialProperties:
      type: object
      description: Federated identity credential properties.
      properties:
        issuer:
          type: string
          description: The URL of the issuer to be trusted. Required.
        subject:
          type: string
          description: The identifier of the external identity.
        audiences:
          type: array
          items:
            type: string
          description: The list of audiences that can appear in the issued token. Required.
        claimsMatchingExpression:
          $ref: "#/components/schemas/ClaimsMatchingExpression"
          description: Object for defining the allowed identifiers of external identities. Either 'subject' or 'claimsMatchingExpression' must be defined, but not both. Introduced in 2025-01-31-preview.
      required:
        - issuer
        - audiences
    ClaimsMatchingExpression:
      type: object
      description: Object for defining the allowed identifiers of external identities. Introduced in 2025-01-31-preview.
      properties:
        value:
          type: string
          description: Wildcard-based expression for matching incoming subject claims. Required.
        languageVersion:
          type: integer
          description: Specifies the version of the flexible fic language used in the expression. Required.
      required:
        - value
        - languageVersion
    FederatedIdentityCredentialFlattened:
      type: object
      description: Flattened view of FederatedIdentityCredential ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        issuer:
          type: string
          description: The URL of the issuer to be trusted. Required.
        subject:
          type: string
          description: The identifier of the external identity.
        audiences:
          type: array
          items:
            type: string
          description: The list of audiences that can appear in the issued token. Required.
        claimsMatchingExpression:
          $ref: "#/components/schemas/ClaimsMatchingExpression"
          description: Object for defining the allowed identifiers of external identities. Either 'subject' or 'claimsMatchingExpression' must be defined, but not both. Introduced in 2025-01-31-preview.
    PagedFederatedIdentityCredentialFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FederatedIdentityCredentialFlattened"
        nextLink:
          type: string
    FederatedIdentityCredential:
      type: object
      description: Describes a federated identity credential.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/FederatedIdentityCredentialProperties"
          description: The properties associated with the federated identity credential.
    OperationDisplay:
      type: object
      description: Operation Display.
      properties:
        provider:
          type: string
          description: Resource Provider Name.
        operation:
          type: string
          description: Operation Type.
        resource:
          type: string
          description: Resource Type.
        description:
          type: string
          description: Operation description.
    Operation:
      type: object
      description: Microsoft.ManagedIdentity Operation.
      properties:
        name:
          type: string
          description: Operation Name.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Operation Display.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    SystemAssignedIdentityProperties:
      type: object
      description: System Assigned Identity properties.
      properties:
        tenantId:
          type: string
          description: The id of the tenant which the identity belongs to.
        principalId:
          type: string
          description: The id of the service principal object associated with the created identity.
        clientId:
          type: string
          description: The id of the app associated with the identity. This is a random generated UUID by MSI.
        clientSecretUrl:
          type: string
          description: The ManagedServiceIdentity DataPlane URL that can be queried to obtain the identity credentials.
    SystemAssignedIdentityFlattened:
      type: object
      description: Flattened view of SystemAssignedIdentity ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        location:
          type: string
          description: Required.
        tags:
          type: object
          properties: {}
          description: ":vartype tags: dict[str, str]"
        tenantId:
          type: string
          description: The id of the tenant which the identity belongs to.
        principalId:
          type: string
          description: The id of the service principal object associated with the created identity.
        clientId:
          type: string
          description: The id of the app associated with the identity. This is a random generated UUID by MSI.
        clientSecretUrl:
          type: string
          description: The ManagedServiceIdentity DataPlane URL that can be queried to obtain the identity credentials.
    UserAssignedIdentityProperties:
      type: object
      description: User Assigned Identity properties.
      properties:
        tenantId:
          type: string
          description: The id of the tenant which the identity belongs to.
        principalId:
          type: string
          description: The id of the service principal object associated with the created identity.
        clientId:
          type: string
          description: The id of the app associated with the identity. This is a random generated UUID by MSI.
        isolationScope:
          $ref: "#/components/schemas/IsolationScope"
          description: 'Enum to configure regional restrictions on identity assignment, as necessary. Known values are: "None" and "Regional".'
        assignmentRestrictions:
          $ref: "#/components/schemas/AssignmentRestrictions"
          description: Restrictions on which resource providers this identity can be assigned to.
    IsolationScope:
      type: string
      enum:
        - None
        - Regional
    AssignmentRestrictions:
      type: object
      description: Configuration to restrict identity assignment to specific resource providers or resource types.
      properties:
        providers:
          type: array
          items:
            type: string
          description: "List of resource providers or resource providers with resource types that this identity can be assigned to (case-insensitive). Examples: 'Microsoft.Compute', 'Microsoft.Storage/Accounts', 'Microsoft.Network/VirtualNetworks'."
    IdentityFlattened:
      type: object
      description: Flattened view of Identity ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        tenantId:
          type: string
          description: The id of the tenant which the identity belongs to.
        principalId:
          type: string
          description: The id of the service principal object associated with the created identity.
        clientId:
          type: string
          description: The id of the app associated with the identity. This is a random generated UUID by MSI.
        isolationScope:
          $ref: "#/components/schemas/IsolationScope"
          description: 'Enum to configure regional restrictions on identity assignment, as necessary. Known values are: "None" and "Regional".'
        assignmentRestrictions:
          $ref: "#/components/schemas/AssignmentRestrictions"
          description: Restrictions on which resource providers this identity can be assigned to.
    PagedIdentityFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/IdentityFlattened"
        nextLink:
          type: string
    Identity:
      type: object
      description: Describes an identity resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/UserAssignedIdentityProperties"
          description: The properties associated with the identity.
      required:
        - location
  x-stackQL-resources:
    federated_identity_credentials:
      id: azure.msi.federated_identity_credentials
      name: federated_identity_credentials
      title: federated_identity_credentials
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}~1federatedIdentityCredentials~1{federated_identity_credential_resource_name}?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}~1federatedIdentityCredentials~1{federated_identity_credential_resource_name}?api-version=2025-05-31-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}~1federatedIdentityCredentials~1{federated_identity_credential_resource_name}?api-version=2025-05-31-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}~1federatedIdentityCredentials?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/federated_identity_credentials/methods/get"
          - $ref: "#/components/x-stackQL-resources/federated_identity_credentials/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/federated_identity_credentials/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/federated_identity_credentials/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/federated_identity_credentials/methods/delete"
    operations:
      id: azure.msi.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ManagedIdentity~1operations?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    system_assigned_identities:
      id: azure.msi.system_assigned_identities
      name: system_assigned_identities
      title: system_assigned_identities
      methods:
        get_by_scope:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedIdentity~1identities~1default?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/system_assigned_identities/methods/get_by_scope"
        insert: []
        update: []
        delete: []
    user_assigned_identities:
      id: azure.msi.user_assigned_identities
      name: user_assigned_identities
      title: user_assigned_identities
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}?api-version=2025-05-31-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}?api-version=2025-05-31-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities~1{resource_name}?api-version=2025-05-31-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ManagedIdentity~1userAssignedIdentities?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/user_assigned_identities/methods/get"
          - $ref: "#/components/x-stackQL-resources/user_assigned_identities/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/user_assigned_identities/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/user_assigned_identities/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/user_assigned_identities/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/user_assigned_identities/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/user_assigned_identities/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{resource_name}/federatedIdentityCredentials/{federated_identity_credential_resource_name}?api-version=2025-05-31-preview:
    get:
      operationId: FederatedIdentityCredentials_get
      description: Gets the federated identity credential.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: federated_identity_credential_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the federated identity credential resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FederatedIdentityCredentialFlattened"
    put:
      operationId: FederatedIdentityCredentials_create_or_update
      description: Create or update a federated identity credential under the specified user assigned identity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: federated_identity_credential_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the federated identity credential resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/FederatedIdentityCredentialProperties"
                  description: The properties associated with the federated identity credential.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FederatedIdentityCredential"
    delete:
      operationId: FederatedIdentityCredentials_delete
      description: Deletes the federated identity credential.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: federated_identity_credential_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the federated identity credential resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{resource_name}/federatedIdentityCredentials?api-version=2025-05-31-preview:
    get:
      operationId: FederatedIdentityCredentials_list
      description: Lists all the federated identity credentials under the specified user assigned identity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: Number of records to return. Default value is None.
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: A skip token is used to continue retrieving items after an operation returns a partial result. If a previous response contains a nextLink element, the value of the nextLink element will include a skipToken parameter that specifies a starting point to use for subsequent calls. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFederatedIdentityCredentialFlattened"
  /providers/Microsoft.ManagedIdentity/operations?api-version=2025-05-31-preview:
    get:
      operationId: msi_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /{scope}/providers/Microsoft.ManagedIdentity/identities/default?api-version=2025-05-31-preview:
    get:
      operationId: SystemAssignedIdentities_get_by_scope
      description: Gets the systemAssignedIdentity available under the specified RP scope.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemAssignedIdentityFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{resource_name}?api-version=2025-05-31-preview:
    get:
      operationId: UserAssignedIdentities_get
      description: Gets the identity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentityFlattened"
    put:
      operationId: UserAssignedIdentities_create_or_update
      description: Create or update an identity in the specified subscription and resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/UserAssignedIdentityProperties"
                  description: The properties associated with the identity.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identity"
    patch:
      operationId: UserAssignedIdentities_update
      description: Update an identity in the specified subscription and resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: The geo-location where the resource lives.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/UserAssignedIdentityProperties"
                  description: The properties associated with the identity.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identity"
    delete:
      operationId: UserAssignedIdentities_delete
      description: Deletes the identity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the identity resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities?api-version=2025-05-31-preview:
    get:
      operationId: UserAssignedIdentities_list_by_resource_group
      description: Lists all the userAssignedIdentities available under the specified ResourceGroup.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedIdentityFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.ManagedIdentity/userAssignedIdentities?api-version=2025-05-31-preview:
    get:
      operationId: UserAssignedIdentities_list_by_subscription
      description: Lists all the userAssignedIdentities available under the specified subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedIdentityFlattened"
