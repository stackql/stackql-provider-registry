openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Managed Services Client
  description: The specification for ManagedServices.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2020-02-01-preview-stackql-generated
  x-serviceAlias: managedservices
  x-sdkPackage: azure-mgmt-managedservices
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    MarketplaceRegistrationDefinitionProperties:
      type: object
      description: The properties of the marketplace registration definition. All required parameters must be populated in order to send to server.
      properties:
        managedByTenantId:
          type: string
          description: The identifier of the managedBy tenant. Required.
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/Authorization"
          description: The collection of authorization objects describing the access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant. Required.
        eligibleAuthorizations:
          type: array
          items:
            $ref: "#/components/schemas/EligibleAuthorization"
          description: The collection of eligible authorization objects describing the just-in-time access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant.
        offerDisplayName:
          type: string
          description: The marketplace offer display name.
        publisherDisplayName:
          type: string
          description: The marketplace publisher display name.
        planDisplayName:
          type: string
          description: The marketplace plan display name.
      required:
        - managedByTenantId
        - authorizations
    Authorization:
      type: object
      description: The Azure Active Directory principal identifier and Azure built-in role that describes the access the principal will receive on the delegated resource in the managed tenant. All required parameters must be populated in order to send to server.
      properties:
        principalId:
          type: string
          description: The identifier of the Azure Active Directory principal. Required.
        principalIdDisplayName:
          type: string
          description: The display name of the Azure Active Directory principal.
        roleDefinitionId:
          type: string
          description: The identifier of the Azure built-in role that defines the permissions that the Azure Active Directory principal will have on the projected scope. Required.
        delegatedRoleDefinitionIds:
          type: array
          items:
            type: string
          description: The delegatedRoleDefinitionIds field is required when the roleDefinitionId refers to the User Access Administrator Role. It is the list of role definition ids which define all the permissions that the user in the authorization can assign to other principals.
      required:
        - principalId
        - roleDefinitionId
    EligibleAuthorization:
      type: object
      description: The Azure Active Directory principal identifier, Azure built-in role, and just-in-time access policy that describes the just-in-time access the principal will receive on the delegated resource in the managed tenant. All required parameters must be populated in order to send to server.
      properties:
        principalId:
          type: string
          description: The identifier of the Azure Active Directory principal. Required.
        principalIdDisplayName:
          type: string
          description: The display name of the Azure Active Directory principal.
        roleDefinitionId:
          type: string
          description: The identifier of the Azure built-in role that defines the permissions that the Azure Active Directory principal will have on the projected scope. Required.
        justInTimeAccessPolicy:
          $ref: "#/components/schemas/JustInTimeAccessPolicy"
          description: The just-in-time access policy setting.
      required:
        - principalId
        - roleDefinitionId
    JustInTimeAccessPolicy:
      type: object
      description: Just-in-time access policy setting. All required parameters must be populated in order to send to server.
      properties:
        multiFactorAuthProvider:
          type: string
          description: 'The multi-factor authorization provider to be used for just-in-time access requests. Known values are: "Azure" and "None".'
        maximumActivationDuration:
          type: string
          description: The maximum access duration in ISO 8601 format for just-in-time access requests.
        managedByTenantApprovers:
          type: array
          items:
            $ref: "#/components/schemas/EligibleApprover"
          description: The list of managedByTenant approvers for the eligible authorization.
      required:
        - multiFactorAuthProvider
    EligibleApprover:
      type: object
      description: Defines the Azure Active Directory principal that can approve any just-in-time access requests by the principal defined in the EligibleAuthorization. All required parameters must be populated in order to send to server.
      properties:
        principalId:
          type: string
          description: The identifier of the Azure Active Directory principal. Required.
        principalIdDisplayName:
          type: string
          description: The display name of the Azure Active Directory principal.
      required:
        - principalId
    Plan:
      type: object
      description: The details for the Managed Services offer’s plan in Azure Marketplace. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: Azure Marketplace plan name. Required.
        publisher:
          type: string
          description: Azure Marketplace publisher ID. Required.
        product:
          type: string
          description: Azure Marketplace product code. Required.
        version:
          type: string
          description: Azure Marketplace plan's version. Required.
      required:
        - name
        - publisher
        - product
        - version
    MarketplaceRegistrationDefinitionFlattened:
      type: object
      description: Flattened view of MarketplaceRegistrationDefinition ('properties' hoisted to the top level).
      properties:
        plan:
          $ref: "#/components/schemas/Plan"
          description: The details for the Managed Services offer’s plan in Azure Marketplace.
        id:
          type: string
          description: The fully qualified path of the marketplace registration definition.
        type:
          type: string
          description: The type of the Azure resource (Microsoft.ManagedServices/marketplaceRegistrationDefinitions).
        name:
          type: string
          description: The name of the marketplace registration definition.
        managedByTenantId:
          type: string
          description: The identifier of the managedBy tenant. Required.
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/Authorization"
          description: The collection of authorization objects describing the access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant. Required.
        eligibleAuthorizations:
          type: array
          items:
            $ref: "#/components/schemas/EligibleAuthorization"
          description: The collection of eligible authorization objects describing the just-in-time access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant.
        offerDisplayName:
          type: string
          description: The marketplace offer display name.
        publisherDisplayName:
          type: string
          description: The marketplace publisher display name.
        planDisplayName:
          type: string
          description: The marketplace plan display name.
    PagedMarketplaceRegistrationDefinitionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MarketplaceRegistrationDefinitionFlattened"
        nextLink:
          type: string
    Operation:
      type: object
      description: The object that describes a single Microsoft.ManagedServices operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: "The operation name with the format: {provider}/{resource}/{operation}."
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that represents the operation.
    OperationDisplay:
      type: object
      description: The object that represents the operation.
      properties:
        provider:
          type: string
          description: The service provider.
        resource:
          type: string
          description: The resource on which the operation is performed.
        operation:
          type: string
          description: The operation type.
        description:
          type: string
          description: The description of the operation.
    OperationList:
      type: object
      description: The list of the operations. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
          description: The list of Microsoft.ManagedServices operations.
    RegistrationAssignmentProperties:
      type: object
      description: The properties of the registration assignment. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        registrationDefinitionId:
          type: string
          description: The fully qualified path of the registration definition. Required.
        provisioningState:
          type: string
          description: 'The current provisioning state of the registration assignment. Known values are: "NotSpecified", "Accepted", "Running", "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        registrationDefinition:
          $ref: "#/components/schemas/RegistrationAssignmentPropertiesRegistrationDefinition"
          description: The registration definition associated with the registration assignment.
      required:
        - registrationDefinitionId
    RegistrationAssignmentPropertiesRegistrationDefinition:
      type: object
      description: The registration definition associated with the registration assignment. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        properties:
          $ref: "#/components/schemas/RegistrationAssignmentPropertiesRegistrationDefinitionProperties"
          description: The properties of the registration definition associated with the registration assignment.
        plan:
          $ref: "#/components/schemas/Plan"
          description: The details for the Managed Services offer’s plan in Azure Marketplace.
        id:
          type: string
          description: The fully qualified path of the registration definition.
        type:
          type: string
          description: The type of the Azure resource (Microsoft.ManagedServices/registrationDefinitions).
        name:
          type: string
          description: The name of the registration definition.
    RegistrationAssignmentPropertiesRegistrationDefinitionProperties:
      type: object
      description: The properties of the registration definition associated with the registration assignment.
      properties:
        description:
          type: string
          description: The description of the registration definition.
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/Authorization"
          description: The collection of authorization objects describing the access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant.
        eligibleAuthorizations:
          type: array
          items:
            $ref: "#/components/schemas/EligibleAuthorization"
          description: The collection of eligible authorization objects describing the just-in-time access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant.
        registrationDefinitionName:
          type: string
          description: The name of the registration definition.
        provisioningState:
          type: string
          description: 'The current provisioning state of the registration definition. Known values are: "NotSpecified", "Accepted", "Running", "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        manageeTenantId:
          type: string
          description: The identifier of the managed tenant.
        manageeTenantName:
          type: string
          description: The name of the managed tenant.
        managedByTenantId:
          type: string
          description: The identifier of the managedBy tenant.
        managedByTenantName:
          type: string
          description: The name of the managedBy tenant.
    RegistrationAssignmentFlattened:
      type: object
      description: Flattened view of RegistrationAssignment ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: The fully qualified path of the registration assignment.
        type:
          type: string
          description: The type of the Azure resource (Microsoft.ManagedServices/registrationAssignments).
        name:
          type: string
          description: The name of the registration assignment.
        registrationDefinitionId:
          type: string
          description: The fully qualified path of the registration definition. Required.
        provisioningState:
          type: string
          description: 'The current provisioning state of the registration assignment. Known values are: "NotSpecified", "Accepted", "Running", "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        registrationDefinition:
          $ref: "#/components/schemas/RegistrationAssignmentPropertiesRegistrationDefinition"
          description: The registration definition associated with the registration assignment.
    PagedRegistrationAssignmentFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RegistrationAssignmentFlattened"
        nextLink:
          type: string
    RegistrationAssignment:
      type: object
      description: The registration assignment. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        properties:
          $ref: "#/components/schemas/RegistrationAssignmentProperties"
          description: The properties of a registration assignment.
        id:
          type: string
          description: The fully qualified path of the registration assignment.
        type:
          type: string
          description: The type of the Azure resource (Microsoft.ManagedServices/registrationAssignments).
        name:
          type: string
          description: The name of the registration assignment.
    RegistrationDefinitionProperties:
      type: object
      description: The properties of a registration definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        description:
          type: string
          description: The description of the registration definition.
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/Authorization"
          description: The collection of authorization objects describing the access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant. Required.
        eligibleAuthorizations:
          type: array
          items:
            $ref: "#/components/schemas/EligibleAuthorization"
          description: The collection of eligible authorization objects describing the just-in-time access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant.
        registrationDefinitionName:
          type: string
          description: The name of the registration definition.
        managedByTenantId:
          type: string
          description: The identifier of the managedBy tenant. Required.
        provisioningState:
          type: string
          description: 'The current provisioning state of the registration definition. Known values are: "NotSpecified", "Accepted", "Running", "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        manageeTenantId:
          type: string
          description: The identifier of the managed tenant.
        manageeTenantName:
          type: string
          description: The name of the managed tenant.
        managedByTenantName:
          type: string
          description: The name of the managedBy tenant.
      required:
        - authorizations
        - managedByTenantId
    RegistrationDefinitionFlattened:
      type: object
      description: Flattened view of RegistrationDefinition ('properties' hoisted to the top level).
      properties:
        plan:
          $ref: "#/components/schemas/Plan"
          description: The details for the Managed Services offer’s plan in Azure Marketplace.
        id:
          type: string
          description: The fully qualified path of the registration definition.
        type:
          type: string
          description: The type of the Azure resource (Microsoft.ManagedServices/registrationDefinitions).
        name:
          type: string
          description: The name of the registration definition.
        description:
          type: string
          description: The description of the registration definition.
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/Authorization"
          description: The collection of authorization objects describing the access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant. Required.
        eligibleAuthorizations:
          type: array
          items:
            $ref: "#/components/schemas/EligibleAuthorization"
          description: The collection of eligible authorization objects describing the just-in-time access Azure Active Directory principals in the managedBy tenant will receive on the delegated resource in the managed tenant.
        registrationDefinitionName:
          type: string
          description: The name of the registration definition.
        managedByTenantId:
          type: string
          description: The identifier of the managedBy tenant. Required.
        provisioningState:
          type: string
          description: 'The current provisioning state of the registration definition. Known values are: "NotSpecified", "Accepted", "Running", "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        manageeTenantId:
          type: string
          description: The identifier of the managed tenant.
        manageeTenantName:
          type: string
          description: The name of the managed tenant.
        managedByTenantName:
          type: string
          description: The name of the managedBy tenant.
    PagedRegistrationDefinitionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RegistrationDefinitionFlattened"
        nextLink:
          type: string
    RegistrationDefinition:
      type: object
      description: The registration definition. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        properties:
          $ref: "#/components/schemas/RegistrationDefinitionProperties"
          description: The properties of a registration definition.
        plan:
          $ref: "#/components/schemas/Plan"
          description: The details for the Managed Services offer’s plan in Azure Marketplace.
        id:
          type: string
          description: The fully qualified path of the registration definition.
        type:
          type: string
          description: The type of the Azure resource (Microsoft.ManagedServices/registrationDefinitions).
        name:
          type: string
          description: The name of the registration definition.
  x-stackQL-resources:
    marketplace_registration_definitions:
      id: azure.managedservices.marketplace_registration_definitions
      name: marketplace_registration_definitions
      title: marketplace_registration_definitions
      methods:
        get:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1marketplaceRegistrationDefinitions~1{marketplace_identifier}?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1marketplaceRegistrationDefinitions?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/marketplace_registration_definitions/methods/get"
          - $ref: "#/components/x-stackQL-resources/marketplace_registration_definitions/methods/list"
        insert: []
        update: []
        delete: []
    marketplace_registration_definitions_without_scope:
      id: azure.managedservices.marketplace_registration_definitions_without_scope
      name: marketplace_registration_definitions_without_scope
      title: marketplace_registration_definitions_without_scope
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ManagedServices~1marketplaceRegistrationDefinitions~1{marketplace_identifier}?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ManagedServices~1marketplaceRegistrationDefinitions?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/marketplace_registration_definitions_without_scope/methods/get"
          - $ref: "#/components/x-stackQL-resources/marketplace_registration_definitions_without_scope/methods/list"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.managedservices.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ManagedServices~1operations?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    registration_assignments:
      id: azure.managedservices.registration_assignments
      name: registration_assignments
      title: registration_assignments
      methods:
        get:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationAssignments~1{registration_assignment_id}?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
        create_or_update:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationAssignments~1{registration_assignment_id}?api-version=2020-02-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationAssignments~1{registration_assignment_id}?api-version=2020-02-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationAssignments?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/registration_assignments/methods/get"
          - $ref: "#/components/x-stackQL-resources/registration_assignments/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/registration_assignments/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/registration_assignments/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/registration_assignments/methods/delete"
    registration_definitions:
      id: azure.managedservices.registration_definitions
      name: registration_definitions
      title: registration_definitions
      methods:
        get:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationDefinitions~1{registration_definition_id}?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationDefinitions~1{registration_definition_id}?api-version=2020-02-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationDefinitions~1{registration_definition_id}?api-version=2020-02-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1{scope}~1providers~1Microsoft.ManagedServices~1registrationDefinitions?api-version=2020-02-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/registration_definitions/methods/get"
          - $ref: "#/components/x-stackQL-resources/registration_definitions/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/registration_definitions/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/registration_definitions/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/registration_definitions/methods/delete"
paths:
  /{scope}/providers/Microsoft.ManagedServices/marketplaceRegistrationDefinitions/{marketplace_identifier}?api-version=2020-02-01-preview:
    get:
      operationId: MarketplaceRegistrationDefinitions_get
      description: Get the marketplace registration definition for the marketplace identifier.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
        - name: marketplace_identifier
          in: path
          required: true
          schema:
            type: string
          description: "The Azure Marketplace identifier. Expected formats: {publisher}.{product[-preview]}.{planName}.{version} or {publisher}.{product[-preview]}.{planName} or {publisher}.{product[-preview]} or {publisher}). Required."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketplaceRegistrationDefinitionFlattened"
  /{scope}/providers/Microsoft.ManagedServices/marketplaceRegistrationDefinitions?api-version=2020-02-01-preview:
    get:
      operationId: MarketplaceRegistrationDefinitions_list
      description: Gets a list of the marketplace registration definitions for the marketplace identifier.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter query parameter to filter marketplace registration definitions by plan identifier, publisher, version etc. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMarketplaceRegistrationDefinitionFlattened"
  /providers/Microsoft.ManagedServices/marketplaceRegistrationDefinitions/{marketplace_identifier}?api-version=2020-02-01-preview:
    get:
      operationId: MarketplaceRegistrationDefinitionsWithoutScope_get
      description: Get the marketplace registration definition for the marketplace identifier.
      parameters:
        - name: marketplace_identifier
          in: path
          required: true
          schema:
            type: string
          description: "The Azure Marketplace identifier. Expected formats: {publisher}.{product[-preview]}.{planName}.{version} or {publisher}.{product[-preview]}.{planName} or {publisher}.{product[-preview]} or {publisher}). Required."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketplaceRegistrationDefinitionFlattened"
  /providers/Microsoft.ManagedServices/marketplaceRegistrationDefinitions?api-version=2020-02-01-preview:
    get:
      operationId: MarketplaceRegistrationDefinitionsWithoutScope_list
      description: Gets a list of the marketplace registration definitions for the marketplace identifier.
      parameters:
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter query parameter to filter marketplace registration definitions by plan identifier, publisher, version etc. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMarketplaceRegistrationDefinitionFlattened"
  /providers/Microsoft.ManagedServices/operations?api-version=2020-02-01-preview:
    get:
      operationId: managedservices_list
      description: Gets a list of the operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationList"
  /{scope}/providers/Microsoft.ManagedServices/registrationAssignments/{registration_assignment_id}?api-version=2020-02-01-preview:
    get:
      operationId: RegistrationAssignments_get
      description: Gets the details of the specified registration assignment.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
        - name: registration_assignment_id
          in: path
          required: true
          schema:
            type: string
          description: The GUID of the registration assignment. Required.
        - name: $expandRegistrationDefinition
          in: query
          required: false
          schema:
            type: boolean
          description: The flag indicating whether to return the registration definition details along with the registration assignment details. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegistrationAssignmentFlattened"
    put:
      operationId: RegistrationAssignments_begin_create_or_update
      description: Creates or updates a registration assignment.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
        - name: registration_assignment_id
          in: path
          required: true
          schema:
            type: string
          description: The GUID of the registration assignment. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RegistrationAssignmentProperties"
                  description: The properties of a registration assignment.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegistrationAssignment"
    delete:
      operationId: RegistrationAssignments_begin_delete
      description: Deletes the specified registration assignment.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
        - name: registration_assignment_id
          in: path
          required: true
          schema:
            type: string
          description: The GUID of the registration assignment. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{scope}/providers/Microsoft.ManagedServices/registrationAssignments?api-version=2020-02-01-preview:
    get:
      operationId: RegistrationAssignments_list
      description: Gets a list of the registration assignments.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
        - name: $expandRegistrationDefinition
          in: query
          required: false
          schema:
            type: boolean
          description: The flag indicating whether to return the registration definition details along with the registration assignment details. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRegistrationAssignmentFlattened"
  /{scope}/providers/Microsoft.ManagedServices/registrationDefinitions/{registration_definition_id}?api-version=2020-02-01-preview:
    get:
      operationId: RegistrationDefinitions_get
      description: Gets the registration definition details.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
        - name: registration_definition_id
          in: path
          required: true
          schema:
            type: string
          description: The GUID of the registration definition. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegistrationDefinitionFlattened"
    put:
      operationId: RegistrationDefinitions_begin_create_or_update
      description: Creates or updates a registration definition.
      parameters:
        - name: registration_definition_id
          in: path
          required: true
          schema:
            type: string
          description: The GUID of the registration definition. Required.
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/RegistrationDefinitionProperties"
                  description: The properties of a registration definition.
                plan:
                  $ref: "#/components/schemas/Plan"
                  description: The details for the Managed Services offer’s plan in Azure Marketplace.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegistrationDefinition"
    delete:
      operationId: RegistrationDefinitions_delete
      description: Deletes the registration definition.
      parameters:
        - name: registration_definition_id
          in: path
          required: true
          schema:
            type: string
          description: The GUID of the registration definition. Required.
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{scope}/providers/Microsoft.ManagedServices/registrationDefinitions?api-version=2020-02-01-preview:
    get:
      operationId: RegistrationDefinitions_list
      description: Gets a list of the registration definitions.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: The scope of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRegistrationDefinitionFlattened"
