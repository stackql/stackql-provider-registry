openapi: 3.0.0
servers:
  - url: https://{endpoint}
    variables:
      endpoint:
        default: ""
        description: The service endpoint host (no scheme).
info:
  title: Ai Voicelive
  description: Azure AI VoiceLive client library for Python
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: unversioned-stackql-generated
  x-serviceAlias: ai_voicelive
  x-sdkPackage: azure-ai-voicelive
  x-plane: data
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas: {}
  x-stackQL-resources:
    voice_live_force_models:
      id: azure.ai_voicelive.voice_live_force_models
      name: voice_live_force_models
      title: voice_live_force_models
      methods:
        voice_live_force_models:
          operation:
            $ref: "#/paths/~1/post"
          response:
            openAPIDocKey: "200"
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
paths:
  /:
    post:
      operationId: _BuilderFallback_voice_live_force_models
      parameters: []
      responses:
        "200":
          description: Success
