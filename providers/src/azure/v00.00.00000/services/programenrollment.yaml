openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Program Enrollment Mgmt Client
  description: ProgramEnrollmentMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-03-01-preview-stackql-generated
  x-serviceAlias: programenrollment
  x-sdkPackage: azure-mgmt-programenrollment
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    EduEnrollmentProperties:
      type: object
      description: Details of the Program EduEnrollment.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", and "Deleting".'
        domains:
          type: array
          items:
            $ref: "#/components/schemas/DomainGroup"
          description: The domain groups associated with this enrollment. Required.
        failureReason:
          $ref: "#/components/schemas/ErrorDetail"
          description: Failure detail when provisioningState is Failed. Omitted otherwise.
      required:
        - domains
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
    DomainGroup:
      type: object
      description: A group of edu domains scoped to an Entra tenant.
      properties:
        domainNames:
          type: array
          items:
            type: string
          description: The edu domain names in this group. Required.
        tenantId:
          type: string
          description: The Entra tenant ID that owns these domains. Defaults to the caller's tenant if omitted.
        state:
          $ref: "#/components/schemas/DomainGroupState"
          description: 'The assessment state of this domain group. Known values are: "Pending", "ActionRequired", "Failed", and "Succeeded".'
        failureReason:
          $ref: "#/components/schemas/ErrorDetail"
          description: Failure detail when state is Failed or ActionRequired. Omitted otherwise.
      required:
        - domainNames
    DomainGroupState:
      type: string
      enum:
        - Pending
        - ActionRequired
        - Failed
        - Succeeded
    ErrorDetail:
      type: object
      description: The error detail.
      properties:
        code:
          type: string
          description: The error code.
        message:
          type: string
          description: The error message.
        target:
          type: string
          description: The error target.
        details:
          type: array
          items:
            $ref: "#/components/schemas/ErrorDetail"
          description: The error details.
        additionalInfo:
          type: array
          items:
            $ref: "#/components/schemas/ErrorAdditionalInfo"
          description: The error additional info.
    ErrorAdditionalInfo:
      type: object
      description: The resource management error additional info.
      properties:
        type:
          type: string
          description: The additional info type.
        info:
          type: object
          properties: {}
          description: The additional info.
    EduEnrollmentFlattened:
      type: object
      description: Flattened view of EduEnrollment ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", and "Deleting".'
        domains:
          type: array
          items:
            $ref: "#/components/schemas/DomainGroup"
          description: The domain groups associated with this enrollment. Required.
        failureReason:
          $ref: "#/components/schemas/ErrorDetail"
          description: Failure detail when provisioningState is Failed. Omitted otherwise.
    PagedEduEnrollmentFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/EduEnrollmentFlattened"
        nextLink:
          type: string
    EduEnrollment:
      type: object
      description: An education program enrollment that groups Entra domains under a single sovereign/edu program scope.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/EduEnrollmentProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    edu_enrollments:
      id: azure.programenrollment.edu_enrollments
      name: edu_enrollments
      title: edu_enrollments
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ProgramEnrollment~1eduEnrollments~1{enrollment_name}?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ProgramEnrollment~1eduEnrollments~1{enrollment_name}?api-version=2026-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ProgramEnrollment~1eduEnrollments~1{enrollment_name}?api-version=2026-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ProgramEnrollment~1eduEnrollments~1{enrollment_name}?api-version=2026-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ProgramEnrollment~1eduEnrollments?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ProgramEnrollment~1eduEnrollments?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/edu_enrollments/methods/get"
          - $ref: "#/components/x-stackQL-resources/edu_enrollments/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/edu_enrollments/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/edu_enrollments/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/edu_enrollments/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/edu_enrollments/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/edu_enrollments/methods/delete"
    operations:
      id: azure.programenrollment.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ProgramEnrollment~1operations?api-version=2026-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ProgramEnrollment/eduEnrollments/{enrollment_name}?api-version=2026-03-01-preview:
    get:
      operationId: EduEnrollments_get
      description: Gets the specified edu enrollment.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enrollment_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the edu enrollment. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EduEnrollmentFlattened"
    put:
      operationId: EduEnrollments_begin_create_or_update
      description: Creates or updates the specified edu enrollment.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enrollment_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the edu enrollment. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/EduEnrollmentProperties"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EduEnrollment"
    patch:
      operationId: EduEnrollments_update
      description: Updates the specified edu enrollment.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enrollment_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the edu enrollment. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EduEnrollment"
    delete:
      operationId: EduEnrollments_begin_delete
      description: Deletes the specified edu enrollment.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: enrollment_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the edu enrollment. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ProgramEnrollment/eduEnrollments?api-version=2026-03-01-preview:
    get:
      operationId: EduEnrollments_list_by_resource_group
      description: Lists the edu enrollments in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedEduEnrollmentFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.ProgramEnrollment/eduEnrollments?api-version=2026-03-01-preview:
    get:
      operationId: EduEnrollments_list_by_subscription
      description: Lists the edu enrollments in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedEduEnrollmentFlattened"
  /providers/Microsoft.ProgramEnrollment/operations?api-version=2026-03-01-preview:
    get:
      operationId: programenrollment_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
