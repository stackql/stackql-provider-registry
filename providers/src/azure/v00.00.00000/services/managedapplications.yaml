openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Managed Applications Mgmt Client
  description: ARM applications.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-07-01-stackql-generated
  x-serviceAlias: managedapplications
  x-sdkPackage: azure-mgmt-managedapplications
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    Sku:
      type: object
      description: SKU for the resource. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: The SKU name. Required.
        tier:
          type: string
          description: The SKU tier.
        size:
          type: string
          description: The SKU size.
        family:
          type: string
          description: The SKU family.
        model:
          type: string
          description: The SKU model.
        capacity:
          type: integer
          description: The SKU capacity.
      required:
        - name
    ApplicationAuthorization:
      type: object
      description: The managed application provider authorization. All required parameters must be populated in order to send to Azure.
      properties:
        principalId:
          type: string
          description: The provider's principal identifier. This is the identity that the provider will use to call ARM to manage the managed application resources. Required.
        roleDefinitionId:
          type: string
          description: The provider's role definition identifier. This role will define all the permissions that the provider must have on the managed application's container resource group. This role definition cannot have permission to delete the resource group. Required.
      required:
        - principalId
        - roleDefinitionId
    ApplicationDefinitionArtifact:
      type: object
      description: Application definition artifact. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: 'The managed application definition artifact name. Required. Known values are: "NotSpecified", "ApplicationResourceTemplate", "CreateUiDefinition", and "MainTemplateParameters".'
        uri:
          type: string
          description: The managed application definition artifact blob uri. Required.
        type:
          type: string
          description: 'The managed application definition artifact type. Required. Known values are: "NotSpecified", "Template", and "Custom".'
      required:
        - name
        - uri
        - type
    ApplicationNotificationPolicy:
      type: object
      description: Managed application notification policy. All required parameters must be populated in order to send to Azure.
      properties:
        notificationEndpoints:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationNotificationEndpoint"
          description: The managed application notification endpoint. Required.
      required:
        - notificationEndpoints
    ApplicationNotificationEndpoint:
      type: object
      description: Managed application notification endpoint. All required parameters must be populated in order to send to Azure.
      properties:
        uri:
          type: string
          description: The managed application notification endpoint uri. Required.
      required:
        - uri
    ApplicationPackageLockingPolicyDefinition:
      type: object
      description: Managed application locking policy.
      properties:
        allowedActions:
          type: array
          items:
            type: string
          description: The deny assignment excluded actions.
        allowedDataActions:
          type: array
          items:
            type: string
          description: The deny assignment excluded data actions.
    ApplicationDeploymentPolicy:
      type: object
      description: Managed application deployment policy. All required parameters must be populated in order to send to Azure.
      properties:
        deploymentMode:
          type: string
          description: 'The managed application deployment mode. Required. Known values are: "NotSpecified", "Incremental", and "Complete".'
      required:
        - deploymentMode
    ApplicationManagementPolicy:
      type: object
      description: Managed application management policy.
      properties:
        mode:
          type: string
          description: 'The managed application management mode. Known values are: "NotSpecified", "Unmanaged", and "Managed".'
    ApplicationPolicy:
      type: object
      description: Managed application policy.
      properties:
        name:
          type: string
          description: The policy name.
        policyDefinitionId:
          type: string
          description: The policy definition Id.
        parameters:
          type: string
          description: The policy parameters.
    ApplicationDefinitionFlattened:
      type: object
      description: Flattened view of ApplicationDefinition ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the resource.
        lockLevel:
          type: string
          description: 'The managed application lock level. Required. Known values are: "CanNotDelete", "ReadOnly", and "None".'
        displayName:
          type: string
          description: The managed application definition display name.
        isEnabled:
          type: boolean
          description: A value indicating whether the package is enabled or not.
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationAuthorization"
          description: The managed application provider authorizations.
        artifacts:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationDefinitionArtifact"
          description: The collection of managed application artifacts. The portal will use the files specified as artifacts to construct the user experience of creating a managed application from a managed application definition.
        description:
          type: string
          description: The managed application definition description.
        packageFileUri:
          type: string
          description: The managed application definition package file Uri. Use this element.
        storageAccountId:
          type: string
          description: The storage account id for bring your own storage scenario.
        mainTemplate:
          type: object
          properties: {}
          description: The inline main template json which has resources to be provisioned. It can be a JObject or well-formed JSON string.
        createUiDefinition:
          type: object
          properties: {}
          description: The createUiDefinition json for the backing template with Microsoft.Solutions/applications resource. It can be a JObject or well-formed JSON string.
        notificationPolicy:
          $ref: "#/components/schemas/ApplicationNotificationPolicy"
          description: The managed application notification policy.
        lockingPolicy:
          $ref: "#/components/schemas/ApplicationPackageLockingPolicyDefinition"
          description: The managed application locking policy.
        deploymentPolicy:
          $ref: "#/components/schemas/ApplicationDeploymentPolicy"
          description: The managed application deployment policy.
        managementPolicy:
          $ref: "#/components/schemas/ApplicationManagementPolicy"
          description: The managed application management policy that determines publisher's access to the managed resource group.
        policies:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationPolicy"
          description: The managed application provider policies.
    PagedApplicationDefinitionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationDefinitionFlattened"
        nextLink:
          type: string
    ApplicationDefinition:
      type: object
      description: Information about managed application definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the resource.
        properties:
          type: object
          properties:
            lockLevel:
              type: string
              description: 'The managed application lock level. Required. Known values are: "CanNotDelete", "ReadOnly", and "None".'
            displayName:
              type: string
              description: The managed application definition display name.
            isEnabled:
              type: boolean
              description: A value indicating whether the package is enabled or not.
            authorizations:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationAuthorization"
              description: The managed application provider authorizations.
            artifacts:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationDefinitionArtifact"
              description: The collection of managed application artifacts. The portal will use the files specified as artifacts to construct the user experience of creating a managed application from a managed application definition.
            description:
              type: string
              description: The managed application definition description.
            packageFileUri:
              type: string
              description: The managed application definition package file Uri. Use this element.
            storageAccountId:
              type: string
              description: The storage account id for bring your own storage scenario.
            mainTemplate:
              type: object
              properties: {}
              description: The inline main template json which has resources to be provisioned. It can be a JObject or well-formed JSON string.
            createUiDefinition:
              type: object
              properties: {}
              description: The createUiDefinition json for the backing template with Microsoft.Solutions/applications resource. It can be a JObject or well-formed JSON string.
            notificationPolicy:
              $ref: "#/components/schemas/ApplicationNotificationPolicy"
              description: The managed application notification policy.
            lockingPolicy:
              $ref: "#/components/schemas/ApplicationPackageLockingPolicyDefinition"
              description: The managed application locking policy.
            deploymentPolicy:
              $ref: "#/components/schemas/ApplicationDeploymentPolicy"
              description: The managed application deployment policy.
            managementPolicy:
              $ref: "#/components/schemas/ApplicationManagementPolicy"
              description: The managed application management policy that determines publisher's access to the managed resource group.
            policies:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationPolicy"
              description: The managed application provider policies.
      required:
        - properties
    Plan:
      type: object
      description: Plan for the managed application. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: The plan name. Required.
        publisher:
          type: string
          description: The publisher ID. Required.
        product:
          type: string
          description: The product code. Required.
        promotionCode:
          type: string
          description: The promotion code.
        version:
          type: string
          description: The plan's version. Required.
      required:
        - name
        - publisher
        - product
        - version
    Identity:
      type: object
      description: Identity for the resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        principalId:
          type: string
          description: The principal ID of resource identity.
        tenantId:
          type: string
          description: The tenant ID of resource.
        type:
          type: string
          description: 'The identity type. Known values are: "SystemAssigned", "UserAssigned", "SystemAssigned, UserAssigned", and "None".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: "The list of user identities associated with the resource. The user identity dictionary key references will be resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}'."
    ApplicationBillingDetailsDefinition:
      type: object
      description: Managed application billing details definition.
      properties:
        resourceUsageId:
          type: string
          description: The managed application resource usage Id.
    ApplicationJitAccessPolicy:
      type: object
      description: Managed application Jit access policy. All required parameters must be populated in order to send to Azure.
      properties:
        jitAccessEnabled:
          type: boolean
          description: Whether the JIT access is enabled. Required.
        jitApprovalMode:
          type: string
          description: 'JIT approval mode. Known values are: "NotSpecified", "AutoApprove", and "ManualApprove".'
        jitApprovers:
          type: array
          items:
            $ref: "#/components/schemas/JitApproverDefinition"
          description: The JIT approvers.
        maximumJitAccessDuration:
          type: string
          description: The maximum duration JIT access is granted. This is an ISO8601 time period value.
      required:
        - jitAccessEnabled
    JitApproverDefinition:
      type: object
      description: JIT approver definition. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: The approver service principal Id. Required.
        type:
          type: string
          description: 'The approver type. Known values are: "user" and "group".'
        displayName:
          type: string
          description: The approver display name.
      required:
        - id
    ApplicationPackageContact:
      type: object
      description: The application package contact information. All required parameters must be populated in order to send to Azure.
      properties:
        contactName:
          type: string
          description: The contact name.
        email:
          type: string
          description: The contact email. Required.
        phone:
          type: string
          description: The contact phone number. Required.
      required:
        - email
        - phone
    ApplicationPackageSupportUrls:
      type: object
      description: The appliance package support URLs.
      properties:
        publicAzure:
          type: string
          description: The public azure support URL.
        governmentCloud:
          type: string
          description: The government cloud support URL.
    ApplicationArtifact:
      type: object
      description: Managed application artifact. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: 'The managed application artifact name. Required. Known values are: "NotSpecified", "ViewDefinition", "Authorizations", and "CustomRoleDefinition".'
        uri:
          type: string
          description: The managed application artifact blob uri. Required.
        type:
          type: string
          description: 'The managed application artifact type. Required. Known values are: "NotSpecified", "Template", and "Custom".'
      required:
        - name
        - uri
        - type
    ApplicationClientDetails:
      type: object
      description: The application client details to track the entity creating/updating the managed app resource.
      properties:
        oid:
          type: string
          description: The client Oid.
        puid:
          type: string
          description: The client Puid.
        applicationId:
          type: string
          description: The client application Id.
    ApplicationFlattened:
      type: object
      description: Flattened view of Application ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the resource.
        plan:
          $ref: "#/components/schemas/Plan"
          description: The plan information.
        kind:
          type: string
          description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog. Required.
        identity:
          $ref: "#/components/schemas/Identity"
          description: The identity of the resource.
        managedResourceGroupId:
          type: string
          description: The managed resource group Id.
        applicationDefinitionId:
          type: string
          description: The fully qualified path of managed application definition Id.
        parameters:
          type: object
          properties: {}
          description: Name and value pairs that define the managed application parameters. It can be a JObject or a well formed JSON string.
        outputs:
          type: object
          properties: {}
          description: Name and value pairs that define the managed application outputs.
        provisioningState:
          type: string
          description: 'The managed application provisioning state. Known values are: "NotSpecified", "Accepted", "Running", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        billingDetails:
          $ref: "#/components/schemas/ApplicationBillingDetailsDefinition"
          description: The managed application billing details.
        jitAccessPolicy:
          $ref: "#/components/schemas/ApplicationJitAccessPolicy"
          description: The managed application Jit access policy.
        publisherTenantId:
          type: string
          description: The publisher tenant Id.
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationAuthorization"
          description: The read-only authorizations property that is retrieved from the application package.
        managementMode:
          type: string
          description: 'The managed application management mode. Known values are: "NotSpecified", "Unmanaged", and "Managed".'
        customerSupport:
          $ref: "#/components/schemas/ApplicationPackageContact"
          description: The read-only customer support property that is retrieved from the application package.
        supportUrls:
          $ref: "#/components/schemas/ApplicationPackageSupportUrls"
          description: The read-only support URLs property that is retrieved from the application package.
        artifacts:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationArtifact"
          description: The collection of managed application artifacts.
        createdBy:
          $ref: "#/components/schemas/ApplicationClientDetails"
          description: The client entity that created the JIT request.
        updatedBy:
          $ref: "#/components/schemas/ApplicationClientDetails"
          description: The client entity that last updated the JIT request.
    PagedApplicationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationFlattened"
        nextLink:
          type: string
    Application:
      type: object
      description: Information about managed application. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the resource.
        plan:
          $ref: "#/components/schemas/Plan"
          description: The plan information.
        kind:
          type: string
          description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog. Required.
        identity:
          $ref: "#/components/schemas/Identity"
          description: The identity of the resource.
        properties:
          type: object
          properties:
            managedResourceGroupId:
              type: string
              description: The managed resource group Id.
            applicationDefinitionId:
              type: string
              description: The fully qualified path of managed application definition Id.
            parameters:
              type: object
              properties: {}
              description: Name and value pairs that define the managed application parameters. It can be a JObject or a well formed JSON string.
            outputs:
              type: object
              properties: {}
              description: Name and value pairs that define the managed application outputs.
            provisioningState:
              type: string
              description: 'The managed application provisioning state. Known values are: "NotSpecified", "Accepted", "Running", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
            billingDetails:
              $ref: "#/components/schemas/ApplicationBillingDetailsDefinition"
              description: The managed application billing details.
            jitAccessPolicy:
              $ref: "#/components/schemas/ApplicationJitAccessPolicy"
              description: The managed application Jit access policy.
            publisherTenantId:
              type: string
              description: The publisher tenant Id.
            authorizations:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationAuthorization"
              description: The read-only authorizations property that is retrieved from the application package.
            managementMode:
              type: string
              description: 'The managed application management mode. Known values are: "NotSpecified", "Unmanaged", and "Managed".'
            customerSupport:
              $ref: "#/components/schemas/ApplicationPackageContact"
              description: The read-only customer support property that is retrieved from the application package.
            supportUrls:
              $ref: "#/components/schemas/ApplicationPackageSupportUrls"
              description: The read-only support URLs property that is retrieved from the application package.
            artifacts:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationArtifact"
              description: The collection of managed application artifacts.
            createdBy:
              $ref: "#/components/schemas/ApplicationClientDetails"
              description: The client entity that created the JIT request.
            updatedBy:
              $ref: "#/components/schemas/ApplicationClientDetails"
              description: The client entity that last updated the JIT request.
      required:
        - kind
    AllowedUpgradePlansResult:
      type: object
      description: The array of plan.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Plan"
          description: The array of plans.
    ManagedIdentityTokenResult:
      type: object
      description: The array of managed identity tokens.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ManagedIdentityToken"
          description: The array of managed identity tokens.
    ManagedIdentityToken:
      type: object
      description: The managed identity token for the managed app resource.
      properties:
        accessToken:
          type: string
          description: The requested access token.
        expiresIn:
          type: string
          description: The number of seconds the access token will be valid.
        expiresOn:
          type: string
          description: The timespan when the access token expires. This is represented as the number of seconds from epoch.
        notBefore:
          type: string
          description: The timespan when the access token takes effect. This is represented as the number of seconds from epoch.
        authorizationAudience:
          type: string
          description: The aud (audience) the access token was request for. This is the same as what was provided in the listTokens request.
        resourceId:
          type: string
          description: The Azure resource ID for the issued token. This is either the managed application ID or the user-assigned identity ID.
        tokenType:
          type: string
          description: The type of the token.
    PlanPatchable:
      type: object
      description: Plan for the managed application.
      properties:
        name:
          type: string
          description: The plan name.
        publisher:
          type: string
          description: The publisher ID.
        product:
          type: string
          description: The product code.
        promotionCode:
          type: string
          description: The promotion code.
        version:
          type: string
          description: The plan's version.
    ApplicationPatchable:
      type: object
      description: Information about managed application. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: "#/components/schemas/Sku"
          description: The SKU of the resource.
        plan:
          $ref: "#/components/schemas/PlanPatchable"
          description: The plan information.
        kind:
          type: string
          description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog.
        identity:
          $ref: "#/components/schemas/Identity"
          description: The identity of the resource.
        properties:
          type: object
          properties:
            managedResourceGroupId:
              type: string
              description: The managed resource group Id.
            applicationDefinitionId:
              type: string
              description: The fully qualified path of managed application definition Id.
            parameters:
              type: object
              properties: {}
              description: Name and value pairs that define the managed application parameters. It can be a JObject or a well formed JSON string.
            outputs:
              type: object
              properties: {}
              description: Name and value pairs that define the managed application outputs.
            provisioningState:
              type: string
              description: 'The managed application provisioning state. Known values are: "NotSpecified", "Accepted", "Running", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
            billingDetails:
              $ref: "#/components/schemas/ApplicationBillingDetailsDefinition"
              description: The managed application billing details.
            jitAccessPolicy:
              $ref: "#/components/schemas/ApplicationJitAccessPolicy"
              description: The managed application Jit access policy.
            publisherTenantId:
              type: string
              description: The publisher tenant Id.
            authorizations:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationAuthorization"
              description: The read-only authorizations property that is retrieved from the application package.
            managementMode:
              type: string
              description: 'The managed application management mode. Known values are: "NotSpecified", "Unmanaged", and "Managed".'
            customerSupport:
              $ref: "#/components/schemas/ApplicationPackageContact"
              description: The read-only customer support property that is retrieved from the application package.
            supportUrls:
              $ref: "#/components/schemas/ApplicationPackageSupportUrls"
              description: The read-only support URLs property that is retrieved from the application package.
            artifacts:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationArtifact"
              description: The collection of managed application artifacts.
            createdBy:
              $ref: "#/components/schemas/ApplicationClientDetails"
              description: The client entity that created the JIT request.
            updatedBy:
              $ref: "#/components/schemas/ApplicationClientDetails"
              description: The client entity that last updated the JIT request.
    JitRequestMetadata:
      type: object
      description: The JIT request metadata.
      properties:
        originRequestId:
          type: string
          description: The origin request id.
        requestorId:
          type: string
          description: The requestor id.
        tenantDisplayName:
          type: string
          description: The publisher's tenant name.
        subjectDisplayName:
          type: string
          description: The subject display name.
    UpdateAccessDefinition:
      type: object
      description: Update access request definition. All required parameters must be populated in order to send to Azure.
      properties:
        approver:
          type: string
          description: The approver name.
        metadata:
          $ref: "#/components/schemas/JitRequestMetadata"
          description: The JIT request metadata. Required.
        status:
          type: string
          description: 'The JIT status. Required. Known values are: "NotSpecified", "Elevate", and "Remove".'
        subStatus:
          type: string
          description: 'The JIT status. Required. Known values are: "NotSpecified", "Approved", "Denied", "Failed", "Expired", and "Timeout".'
      required:
        - metadata
        - status
        - subStatus
    JitAuthorizationPolicies:
      type: object
      description: The JIT authorization policies. All required parameters must be populated in order to send to Azure.
      properties:
        principalId:
          type: string
          description: The the principal id that will be granted JIT access. Required.
        roleDefinitionId:
          type: string
          description: The role definition id that will be granted to the Principal. Required.
      required:
        - principalId
        - roleDefinitionId
    JitSchedulingPolicy:
      type: object
      description: The JIT scheduling policies. All required parameters must be populated in order to send to Azure.
      properties:
        type:
          type: string
          description: 'The type of JIT schedule. Required. Known values are: "NotSpecified", "Once", and "Recurring".'
        duration:
          type: string
          description: The required duration of the JIT request. Required.
        startTime:
          type: string
          format: date-time
          description: The start time of the request. Required.
      required:
        - type
        - duration
        - startTime
    JitRequestDefinitionFlattened:
      type: object
      description: Flattened view of JitRequestDefinition ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        applicationResourceId:
          type: string
          description: The parent application id.
        publisherTenantId:
          type: string
          description: The publisher tenant id.
        jitAuthorizationPolicies:
          type: array
          items:
            $ref: "#/components/schemas/JitAuthorizationPolicies"
          description: The JIT authorization policies.
        jitSchedulingPolicy:
          $ref: "#/components/schemas/JitSchedulingPolicy"
          description: The JIT request properties.
        provisioningState:
          type: string
          description: 'The JIT request provisioning state. Known values are: "NotSpecified", "Accepted", "Running", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        jitRequestState:
          type: string
          description: 'The JIT request state. Known values are: "NotSpecified", "Pending", "Approved", "Denied", "Failed", "Canceled", "Expired", and "Timeout".'
        createdBy:
          $ref: "#/components/schemas/ApplicationClientDetails"
          description: The client entity that created the JIT request.
        updatedBy:
          $ref: "#/components/schemas/ApplicationClientDetails"
          description: The client entity that last updated the JIT request.
    JitRequestDefinition:
      type: object
      description: Information about JIT request definition. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        properties:
          type: object
          properties:
            applicationResourceId:
              type: string
              description: The parent application id.
            publisherTenantId:
              type: string
              description: The publisher tenant id.
            jitAuthorizationPolicies:
              type: array
              items:
                $ref: "#/components/schemas/JitAuthorizationPolicies"
              description: The JIT authorization policies.
            jitSchedulingPolicy:
              $ref: "#/components/schemas/JitSchedulingPolicy"
              description: The JIT request properties.
            provisioningState:
              type: string
              description: 'The JIT request provisioning state. Known values are: "NotSpecified", "Accepted", "Running", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
            jitRequestState:
              type: string
              description: 'The JIT request state. Known values are: "NotSpecified", "Pending", "Approved", "Denied", "Failed", "Canceled", "Expired", and "Timeout".'
            createdBy:
              $ref: "#/components/schemas/ApplicationClientDetails"
              description: The client entity that created the JIT request.
            updatedBy:
              $ref: "#/components/schemas/ApplicationClientDetails"
              description: The client entity that last updated the JIT request.
    JitRequestDefinitionListResultFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/JitRequestDefinitionFlattened"
        nextLink:
          type: string
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    application_definitions:
      id: azure.managedapplications.application_definitions
      name: application_definitions
      title: application_definitions
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applicationDefinitions~1{application_definition_name}?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applicationDefinitions~1{application_definition_name}?api-version=2021-07-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applicationDefinitions~1{application_definition_name}?api-version=2021-07-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applicationDefinitions~1{application_definition_name}?api-version=2021-07-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applicationDefinitions?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Solutions~1applicationDefinitions?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/application_definitions/methods/get"
          - $ref: "#/components/x-stackQL-resources/application_definitions/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/application_definitions/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/application_definitions/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/application_definitions/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/application_definitions/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/application_definitions/methods/delete"
    applications:
      id: azure.managedapplications.applications
      name: applications
      title: applications
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}?api-version=2021-07-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}?api-version=2021-07-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}?api-version=2021-07-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Solutions~1applications?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_allowed_upgrade_plans:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}~1listAllowedUpgradePlans?api-version=2021-07-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_tokens:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}~1listTokens?api-version=2021-07-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_by_id:
          operation:
            $ref: "#/paths/~1{application_id}?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        delete_by_id:
          operation:
            $ref: "#/paths/~1{application_id}?api-version=2021-07-01/delete"
          response:
            openAPIDocKey: "204"
        create_or_update_by_id:
          operation:
            $ref: "#/paths/~1{application_id}?api-version=2021-07-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update_by_id:
          operation:
            $ref: "#/paths/~1{application_id}?api-version=2021-07-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        refresh_permissions:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}~1refreshPermissions?api-version=2021-07-01/post"
          response:
            openAPIDocKey: "204"
        update_access:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1applications~1{application_name}~1updateAccess?api-version=2021-07-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/applications/methods/get"
          - $ref: "#/components/x-stackQL-resources/applications/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/applications/methods/list_by_subscription"
          - $ref: "#/components/x-stackQL-resources/applications/methods/get_by_id"
        insert:
          - $ref: "#/components/x-stackQL-resources/applications/methods/create_or_update"
          - $ref: "#/components/x-stackQL-resources/applications/methods/create_or_update_by_id"
        update:
          - $ref: "#/components/x-stackQL-resources/applications/methods/update"
          - $ref: "#/components/x-stackQL-resources/applications/methods/update_by_id"
        replace:
          - $ref: "#/components/x-stackQL-resources/applications/methods/create_or_update"
          - $ref: "#/components/x-stackQL-resources/applications/methods/create_or_update_by_id"
        delete:
          - $ref: "#/components/x-stackQL-resources/applications/methods/delete"
          - $ref: "#/components/x-stackQL-resources/applications/methods/delete_by_id"
    jit_requests:
      id: azure.managedapplications.jit_requests
      name: jit_requests
      title: jit_requests
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1jitRequests~1{jit_request_name}?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1jitRequests~1{jit_request_name}?api-version=2021-07-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1jitRequests~1{jit_request_name}?api-version=2021-07-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1jitRequests~1{jit_request_name}?api-version=2021-07-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Solutions~1jitRequests?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Solutions~1jitRequests?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/jit_requests/methods/get"
          - $ref: "#/components/x-stackQL-resources/jit_requests/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/jit_requests/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/jit_requests/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/jit_requests/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/jit_requests/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/jit_requests/methods/delete"
    operations:
      id: azure.managedapplications.operations
      name: operations
      title: operations
      methods:
        list_operations:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Solutions~1operations?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list_operations"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applicationDefinitions/{application_definition_name}?api-version=2021-07-01:
    get:
      operationId: ApplicationDefinitions_get
      description: Gets the managed application definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_definition_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application definition. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDefinitionFlattened"
    put:
      operationId: ApplicationDefinitions_create_or_update
      description: Creates or updates a managed application definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_definition_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application definition. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: The SKU of the resource.
                properties:
                  type: object
                  properties:
                    lockLevel:
                      type: string
                      description: 'The managed application lock level. Required. Known values are: "CanNotDelete", "ReadOnly", and "None".'
                    displayName:
                      type: string
                      description: The managed application definition display name.
                    isEnabled:
                      type: boolean
                      description: A value indicating whether the package is enabled or not.
                    authorizations:
                      type: array
                      items:
                        $ref: "#/components/schemas/ApplicationAuthorization"
                      description: The managed application provider authorizations.
                    artifacts:
                      type: array
                      items:
                        $ref: "#/components/schemas/ApplicationDefinitionArtifact"
                      description: The collection of managed application artifacts. The portal will use the files specified as artifacts to construct the user experience of creating a managed application from a managed application definition.
                    description:
                      type: string
                      description: The managed application definition description.
                    packageFileUri:
                      type: string
                      description: The managed application definition package file Uri. Use this element.
                    storageAccountId:
                      type: string
                      description: The storage account id for bring your own storage scenario.
                    mainTemplate:
                      type: object
                      properties: {}
                      description: The inline main template json which has resources to be provisioned. It can be a JObject or well-formed JSON string.
                    createUiDefinition:
                      type: object
                      properties: {}
                      description: The createUiDefinition json for the backing template with Microsoft.Solutions/applications resource. It can be a JObject or well-formed JSON string.
                    notificationPolicy:
                      $ref: "#/components/schemas/ApplicationNotificationPolicy"
                      description: The managed application notification policy.
                    lockingPolicy:
                      $ref: "#/components/schemas/ApplicationPackageLockingPolicyDefinition"
                      description: The managed application locking policy.
                    deploymentPolicy:
                      $ref: "#/components/schemas/ApplicationDeploymentPolicy"
                      description: The managed application deployment policy.
                    managementPolicy:
                      $ref: "#/components/schemas/ApplicationManagementPolicy"
                      description: The managed application management policy that determines publisher's access to the managed resource group.
                    policies:
                      type: array
                      items:
                        $ref: "#/components/schemas/ApplicationPolicy"
                      description: The managed application provider policies.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDefinition"
    delete:
      operationId: ApplicationDefinitions_delete
      description: Deletes the managed application definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_definition_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application definition. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: ApplicationDefinitions_update
      description: Updates the managed application definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_definition_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application definition. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Application definition tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDefinition"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applicationDefinitions?api-version=2021-07-01:
    get:
      operationId: ApplicationDefinitions_list_by_resource_group
      description: Lists the managed application definitions in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedApplicationDefinitionFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Solutions/applicationDefinitions?api-version=2021-07-01:
    get:
      operationId: ApplicationDefinitions_list_by_subscription
      description: Lists all the application definitions within a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedApplicationDefinitionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications/{application_name}?api-version=2021-07-01:
    get:
      operationId: Applications_get
      description: Gets the managed application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationFlattened"
    put:
      operationId: Applications_begin_create_or_update
      description: Creates or updates a managed application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: The SKU of the resource.
                plan:
                  $ref: "#/components/schemas/Plan"
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog. Required.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: "#/components/schemas/ApplicationJitAccessPolicy"
                      description: The managed application Jit access policy.
              required:
                - kind
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Application"
    delete:
      operationId: Applications_begin_delete
      description: Deletes the managed application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Applications_begin_update
      description: Updates an existing managed application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: The SKU of the resource.
                plan:
                  $ref: "#/components/schemas/PlanPatchable"
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: "#/components/schemas/ApplicationJitAccessPolicy"
                      description: The managed application Jit access policy.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationPatchable"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications?api-version=2021-07-01:
    get:
      operationId: Applications_list_by_resource_group
      description: Lists all the applications within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedApplicationFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Solutions/applications?api-version=2021-07-01:
    get:
      operationId: Applications_list_by_subscription
      description: Lists all the applications within a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedApplicationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications/{application_name}/listAllowedUpgradePlans?api-version=2021-07-01:
    post:
      operationId: Applications_list_allowed_upgrade_plans
      description: List allowed upgrade plans for application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AllowedUpgradePlansResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications/{application_name}/listTokens?api-version=2021-07-01:
    post:
      operationId: Applications_list_tokens
      description: List tokens for application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                authorizationAudience:
                  type: string
                  description: The authorization audience.
                userAssignedIdentities:
                  type: array
                  items:
                    type: string
                  description: The user assigned identities.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedIdentityTokenResult"
  /{application_id}?api-version=2021-07-01:
    get:
      operationId: Applications_get_by_id
      description: Gets the managed application.
      parameters:
        - name: application_id
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified ID of the managed application, including the managed application name and the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationFlattened"
    delete:
      operationId: Applications_begin_delete_by_id
      description: Deletes the managed application.
      parameters:
        - name: application_id
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified ID of the managed application, including the managed application name and the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}. Required.
      responses:
        "204":
          description: Success (no content / accepted)
    put:
      operationId: Applications_begin_create_or_update_by_id
      description: Creates or updates a managed application.
      parameters:
        - name: application_id
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified ID of the managed application, including the managed application name and the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: The SKU of the resource.
                plan:
                  $ref: "#/components/schemas/Plan"
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog. Required.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: "#/components/schemas/ApplicationJitAccessPolicy"
                      description: The managed application Jit access policy.
              required:
                - kind
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Application"
    patch:
      operationId: Applications_begin_update_by_id
      description: Updates an existing managed application.
      parameters:
        - name: application_id
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified ID of the managed application, including the managed application name and the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: The SKU of the resource.
                plan:
                  $ref: "#/components/schemas/PlanPatchable"
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: "#/components/schemas/ApplicationJitAccessPolicy"
                      description: The managed application Jit access policy.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationPatchable"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications/{application_name}/refreshPermissions?api-version=2021-07-01:
    post:
      operationId: Applications_begin_refresh_permissions
      description: Refresh Permissions for application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications/{application_name}/updateAccess?api-version=2021-07-01:
    post:
      operationId: Applications_begin_update_access
      description: Update access for application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the managed application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                approver:
                  type: string
                  description: The approver name.
                metadata:
                  $ref: "#/components/schemas/JitRequestMetadata"
                  description: The JIT request metadata. Required.
                status:
                  type: string
                  description: 'The JIT status. Required. Known values are: "NotSpecified", "Elevate", and "Remove".'
                subStatus:
                  type: string
                  description: 'The JIT status. Required. Known values are: "NotSpecified", "Approved", "Denied", "Failed", "Expired", and "Timeout".'
              required:
                - metadata
                - status
                - subStatus
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateAccessDefinition"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/jitRequests/{jit_request_name}?api-version=2021-07-01:
    get:
      operationId: JitRequests_get
      description: Gets the JIT request.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: jit_request_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the JIT request. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JitRequestDefinitionFlattened"
    put:
      operationId: JitRequests_begin_create_or_update
      description: Creates or updates the JIT request.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: jit_request_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the JIT request. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  type: object
                  properties:
                    applicationResourceId:
                      type: string
                      description: The parent application id.
                    jitAuthorizationPolicies:
                      type: array
                      items:
                        $ref: "#/components/schemas/JitAuthorizationPolicies"
                      description: The JIT authorization policies.
                    jitSchedulingPolicy:
                      $ref: "#/components/schemas/JitSchedulingPolicy"
                      description: The JIT request properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JitRequestDefinition"
    patch:
      operationId: JitRequests_update
      description: Updates the JIT request.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: jit_request_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the JIT request. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Jit request tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JitRequestDefinition"
    delete:
      operationId: JitRequests_delete
      description: Deletes the JIT request.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: jit_request_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the JIT request. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Solutions/jitRequests?api-version=2021-07-01:
    get:
      operationId: JitRequests_list_by_subscription
      description: Lists all JIT requests within the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JitRequestDefinitionListResultFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/jitRequests?api-version=2021-07-01:
    get:
      operationId: JitRequests_list_by_resource_group
      description: Lists all JIT requests within the resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JitRequestDefinitionListResultFlattened"
  /providers/Microsoft.Solutions/operations?api-version=2021-07-01:
    get:
      operationId: ManagedApplicationsMgmtClient_list_operations
      description: Lists all of the available Microsoft.Solutions REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
