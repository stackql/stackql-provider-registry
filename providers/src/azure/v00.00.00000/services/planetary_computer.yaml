openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Microsoft Planetary Computer Pro
  description: Management plane for geospatial catalogs (Microsoft.Orbital geo-catalogs).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-04-15-stackql-generated
  x-serviceAlias: planetary_computer
  x-sdkPackage: azure-mgmt-planetarycomputer
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    GeoCatalogProperties:
      type: object
      description: The details of the Microsoft Planetary Computer Pro GeoCatalog.
      properties:
        tier:
          $ref: "#/components/schemas/CatalogTier"
          description: Tier of the catalog. This cannot be changed after the catalog is created. If not provided, defaults to Basic. "Basic"
        catalogUri:
          type: string
          description: The URI of the catalog management UI.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        autoGeneratedDomainNameLabelScope:
          $ref: "#/components/schemas/AutoGeneratedDomainNameLabelScope"
          description: 'The scope of the auto-generated domain name label. If not provided, defaults to TenantReuse. Known values are: "NoReuse", "TenantReuse", "SubscriptionReuse", and "ResourceGroupReuse".'
    CatalogTier:
      type: string
      enum:
        - Basic
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
        - Accepted
    AutoGeneratedDomainNameLabelScope:
      type: string
      enum:
        - NoReuse
        - TenantReuse
        - SubscriptionReuse
        - ResourceGroupReuse
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    GeoCatalogFlattened:
      type: object
      description: Flattened view of GeoCatalog ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        tier:
          $ref: "#/components/schemas/CatalogTier"
          description: Tier of the catalog. This cannot be changed after the catalog is created. If not provided, defaults to Basic. "Basic"
        catalogUri:
          type: string
          description: The URI of the catalog management UI.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        autoGeneratedDomainNameLabelScope:
          $ref: "#/components/schemas/AutoGeneratedDomainNameLabelScope"
          description: 'The scope of the auto-generated domain name label. If not provided, defaults to TenantReuse. Known values are: "NoReuse", "TenantReuse", "SubscriptionReuse", and "ResourceGroupReuse".'
    PagedGeoCatalogFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/GeoCatalogFlattened"
        nextLink:
          type: string
    GeoCatalog:
      type: object
      description: A Microsoft Planetary Computer Pro GeoCatalog resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/GeoCatalogProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    ManagedServiceIdentityUpdate:
      type: object
      description: The template for adding optional properties.
      properties:
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
  x-stackQL-resources:
    geo_catalogs:
      id: azure.planetary_computer.geo_catalogs
      name: geo_catalogs
      title: geo_catalogs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1geoCatalogs~1{catalog_name}?api-version=2026-04-15/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1geoCatalogs~1{catalog_name}?api-version=2026-04-15/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1geoCatalogs~1{catalog_name}?api-version=2026-04-15/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1geoCatalogs~1{catalog_name}?api-version=2026-04-15/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1geoCatalogs?api-version=2026-04-15/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Orbital~1geoCatalogs?api-version=2026-04-15/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/geo_catalogs/methods/get"
          - $ref: "#/components/x-stackQL-resources/geo_catalogs/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/geo_catalogs/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/geo_catalogs/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/geo_catalogs/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/geo_catalogs/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/geoCatalogs/{catalog_name}?api-version=2026-04-15:
    get:
      operationId: GeoCatalogs_get
      description: Get a GeoCatalog.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: catalog_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoCatalogFlattened"
    put:
      operationId: GeoCatalogs_begin_create
      description: Create a GeoCatalog.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: catalog_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/GeoCatalogProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoCatalog"
    patch:
      operationId: GeoCatalogs_begin_update
      description: Update a GeoCatalog.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: catalog_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentityUpdate"
                  description: The managed service identity properties to update.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoCatalog"
    delete:
      operationId: GeoCatalogs_begin_delete
      description: Delete a GeoCatalog.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: catalog_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/geoCatalogs?api-version=2026-04-15:
    get:
      operationId: GeoCatalogs_list_by_resource_group
      description: List GeoCatalog resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGeoCatalogFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Orbital/geoCatalogs?api-version=2026-04-15:
    get:
      operationId: GeoCatalogs_list_by_subscription
      description: List GeoCatalog resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGeoCatalogFlattened"
