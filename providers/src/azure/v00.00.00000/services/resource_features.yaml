openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Feature Client
  description: Feature Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-07-01-stackql-generated
  x-serviceAlias: resource_features
  x-sdkPackage: azure-mgmt-resource-features
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    FeatureProperties:
      type: object
      description: Information about feature.
      properties:
        state:
          type: string
          description: The registration state of the feature for the subscription.
    FeatureResultFlattened:
      type: object
      description: Flattened view of FeatureResult ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: The name of the feature.
        id:
          type: string
          description: The resource ID of the feature.
        type:
          type: string
          description: The resource type of the feature.
        state:
          type: string
          description: The registration state of the feature for the subscription.
    PagedFeatureResultFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FeatureResultFlattened"
        nextLink:
          type: string
    FeatureResult:
      type: object
      description: Previewed feature information.
      properties:
        name:
          type: string
          description: The name of the feature.
        properties:
          $ref: "#/components/schemas/FeatureProperties"
          description: Properties of the previewed feature.
        id:
          type: string
          description: The resource ID of the feature.
        type:
          type: string
          description: The resource type of the feature.
    SubscriptionFeatureRegistrationProperties:
      type: object
      description: SubscriptionFeatureRegistrationProperties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        tenantId:
          type: string
          description: The tenantId.
        subscriptionId:
          type: string
          description: The subscriptionId.
        featureName:
          type: string
          description: The featureName.
        displayName:
          type: string
          description: The featureDisplayName.
        providerNamespace:
          type: string
          description: The providerNamespace.
        state:
          type: string
          description: 'The state. Known values are: "NotSpecified", "NotRegistered", "Pending", "Registering", "Registered", "Unregistering", and "Unregistered".'
        authorizationProfile:
          $ref: "#/components/schemas/AuthorizationProfile"
          description: Authorization Profile.
        metadata:
          type: object
          properties: {}
          description: Key-value pairs for meta data.
        releaseDate:
          type: string
          format: date-time
          description: The feature release date.
        registrationDate:
          type: string
          format: date-time
          description: The feature registration date.
        documentationLink:
          type: string
          description: The feature documentation link.
        approvalType:
          type: string
          description: 'The feature approval type. Known values are: "NotSpecified", "ApprovalRequired", and "AutoApproval".'
        shouldFeatureDisplayInPortal:
          type: boolean
          description: Indicates whether feature should be displayed in Portal.
        description:
          type: string
          description: The feature description.
    AuthorizationProfile:
      type: object
      description: Authorization Profile. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        requestedTime:
          type: string
          format: date-time
          description: The requested time.
        requester:
          type: string
          description: The requester.
        requesterObjectId:
          type: string
          description: The requester object id.
        approvedTime:
          type: string
          format: date-time
          description: The approved time.
        approver:
          type: string
          description: The approver.
    SubscriptionFeatureRegistrationFlattened:
      type: object
      description: Flattened view of SubscriptionFeatureRegistration ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Azure resource Id.
        name:
          type: string
          description: Azure resource name.
        type:
          type: string
          description: Azure resource type.
        tenantId:
          type: string
          description: The tenantId.
        subscriptionId:
          type: string
          description: The subscriptionId.
        featureName:
          type: string
          description: The featureName.
        displayName:
          type: string
          description: The featureDisplayName.
        providerNamespace:
          type: string
          description: The providerNamespace.
        state:
          type: string
          description: 'The state. Known values are: "NotSpecified", "NotRegistered", "Pending", "Registering", "Registered", "Unregistering", and "Unregistered".'
        authorizationProfile:
          $ref: "#/components/schemas/AuthorizationProfile"
          description: Authorization Profile.
        metadata:
          type: object
          properties: {}
          description: Key-value pairs for meta data.
        releaseDate:
          type: string
          format: date-time
          description: The feature release date.
        registrationDate:
          type: string
          format: date-time
          description: The feature registration date.
        documentationLink:
          type: string
          description: The feature documentation link.
        approvalType:
          type: string
          description: 'The feature approval type. Known values are: "NotSpecified", "ApprovalRequired", and "AutoApproval".'
        shouldFeatureDisplayInPortal:
          type: boolean
          description: Indicates whether feature should be displayed in Portal.
        description:
          type: string
          description: The feature description.
    PagedSubscriptionFeatureRegistrationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SubscriptionFeatureRegistrationFlattened"
        nextLink:
          type: string
    SubscriptionFeatureRegistration:
      type: object
      description: Subscription feature registration details. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Azure resource Id.
        name:
          type: string
          description: Azure resource name.
        type:
          type: string
          description: Azure resource type.
        properties:
          $ref: "#/components/schemas/SubscriptionFeatureRegistrationProperties"
          description: ":vartype properties: ~azure.mgmt.resource.features.models.SubscriptionFeatureRegistrationProperties"
    OperationDisplay:
      type: object
      description: The object that represents the operation.
      properties:
        provider:
          type: string
          description: "Service provider: Microsoft.Features."
        resource:
          type: string
          description: "Resource on which the operation is performed: Profile, endpoint, etc."
        operation:
          type: string
          description: "Operation type: Read, write, delete, etc."
    Operation:
      type: object
      description: Microsoft.Features operation.
      properties:
        name:
          type: string
          description: "Operation name: {provider}/{resource}/{operation}."
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that represents the operation.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    features:
      id: azure.resource_features.features
      name: features
      title: features
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1providers~1{resource_provider_namespace}~1features~1{feature_name}?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_all:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1features?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1providers~1{resource_provider_namespace}~1features?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        register:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1providers~1{resource_provider_namespace}~1features~1{feature_name}~1register?api-version=2021-07-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        unregister:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1providers~1{resource_provider_namespace}~1features~1{feature_name}~1unregister?api-version=2021-07-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/features/methods/get"
          - $ref: "#/components/x-stackQL-resources/features/methods/list"
          - $ref: "#/components/x-stackQL-resources/features/methods/list_all"
        insert: []
        update: []
        delete: []
    subscription_feature_registrations:
      id: azure.resource_features.subscription_feature_registrations
      name: subscription_feature_registrations
      title: subscription_feature_registrations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1featureProviders~1{provider_namespace}~1subscriptionFeatureRegistrations~1{feature_name}?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1featureProviders~1{provider_namespace}~1subscriptionFeatureRegistrations~1{feature_name}?api-version=2021-07-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1featureProviders~1{provider_namespace}~1subscriptionFeatureRegistrations~1{feature_name}?api-version=2021-07-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1featureProviders~1{provider_namespace}~1subscriptionFeatureRegistrations?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_all_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Features~1subscriptionFeatureRegistrations?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/subscription_feature_registrations/methods/get"
          - $ref: "#/components/x-stackQL-resources/subscription_feature_registrations/methods/list_by_subscription"
          - $ref: "#/components/x-stackQL-resources/subscription_feature_registrations/methods/list_all_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/subscription_feature_registrations/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/subscription_feature_registrations/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/subscription_feature_registrations/methods/delete"
    operations:
      id: azure.resource_features.operations
      name: operations
      title: operations
      methods:
        list_operations:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Features~1operations?api-version=2021-07-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list_operations"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.Features/providers/{resource_provider_namespace}/features/{feature_name}?api-version=2021-07-01:
    get:
      operationId: Features_get
      description: Gets the preview feature with the specified name.
      parameters:
        - name: resource_provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The resource provider namespace for the feature. Required.
        - name: feature_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the feature to get. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeatureResultFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Features/features?api-version=2021-07-01:
    get:
      operationId: Features_list_all
      description: Gets all the preview features that are available through AFEC for the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFeatureResultFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Features/providers/{resource_provider_namespace}/features?api-version=2021-07-01:
    get:
      operationId: Features_list
      description: Gets all the preview features in a provider namespace that are available through AFEC for the subscription.
      parameters:
        - name: resource_provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The namespace of the resource provider for getting features. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFeatureResultFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Features/providers/{resource_provider_namespace}/features/{feature_name}/register?api-version=2021-07-01:
    post:
      operationId: Features_register
      description: Registers the preview feature for the subscription.
      parameters:
        - name: resource_provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The namespace of the resource provider. Required.
        - name: feature_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the feature to register. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeatureResult"
  /subscriptions/{subscription_id}/providers/Microsoft.Features/providers/{resource_provider_namespace}/features/{feature_name}/unregister?api-version=2021-07-01:
    post:
      operationId: Features_unregister
      description: Unregisters the preview feature for the subscription.
      parameters:
        - name: resource_provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The namespace of the resource provider. Required.
        - name: feature_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the feature to unregister. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeatureResult"
  /subscriptions/{subscription_id}/providers/Microsoft.Features/featureProviders/{provider_namespace}/subscriptionFeatureRegistrations/{feature_name}?api-version=2021-07-01:
    get:
      operationId: SubscriptionFeatureRegistrations_get
      description: Returns a feature registration.
      parameters:
        - name: provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The provider namespace. Required.
        - name: feature_name
          in: path
          required: true
          schema:
            type: string
          description: The feature name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionFeatureRegistrationFlattened"
    put:
      operationId: SubscriptionFeatureRegistrations_create_or_update
      description: Create or update a feature registration.
      parameters:
        - name: provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The provider namespace. Required.
        - name: feature_name
          in: path
          required: true
          schema:
            type: string
          description: The feature name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/SubscriptionFeatureRegistrationProperties"
                  description: ":vartype properties: ~azure.mgmt.resource.features.models.SubscriptionFeatureRegistrationProperties"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionFeatureRegistration"
    delete:
      operationId: SubscriptionFeatureRegistrations_delete
      description: Deletes a feature registration.
      parameters:
        - name: provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The provider namespace. Required.
        - name: feature_name
          in: path
          required: true
          schema:
            type: string
          description: The feature name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Features/featureProviders/{provider_namespace}/subscriptionFeatureRegistrations?api-version=2021-07-01:
    get:
      operationId: SubscriptionFeatureRegistrations_list_by_subscription
      description: Returns subscription feature registrations for given subscription and provider namespace.
      parameters:
        - name: provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The provider namespace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSubscriptionFeatureRegistrationFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Features/subscriptionFeatureRegistrations?api-version=2021-07-01:
    get:
      operationId: SubscriptionFeatureRegistrations_list_all_by_subscription
      description: Returns subscription feature registrations for given subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSubscriptionFeatureRegistrationFlattened"
  /providers/Microsoft.Features/operations?api-version=2021-07-01:
    get:
      operationId: _FeatureClient_list_operations
      description: Lists all of the available Microsoft.Features REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
