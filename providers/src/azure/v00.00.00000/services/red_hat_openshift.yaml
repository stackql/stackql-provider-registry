openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Red Hat OpenShift
  description: Managed OpenShift clusters, machine pools, secrets and sync resources.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-07-25-stackql-generated
  x-serviceAlias: red_hat_openshift
  x-sdkPackage: azure-mgmt-redhatopenshift
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    OpenShiftClusterProperties:
      type: object
      description: OpenShiftClusterProperties represents an OpenShift cluster's properties.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The cluster provisioning state. Known values are: "AdminUpdating", "Canceled", "Creating", "Deleting", "Failed", "Succeeded", and "Updating".'
        clusterProfile:
          $ref: "#/components/schemas/ClusterProfile"
          description: The cluster profile.
        consoleProfile:
          $ref: "#/components/schemas/ConsoleProfile"
          description: The console profile.
        servicePrincipalProfile:
          $ref: "#/components/schemas/ServicePrincipalProfile"
          description: The cluster service principal profile.
        platformWorkloadIdentityProfile:
          $ref: "#/components/schemas/PlatformWorkloadIdentityProfile"
          description: The workload identity profile.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: The cluster network profile.
        masterProfile:
          $ref: "#/components/schemas/MasterProfile"
          description: The cluster master profile.
        workerProfiles:
          type: array
          items:
            $ref: "#/components/schemas/WorkerProfile"
          description: The cluster worker profiles.
        workerProfilesStatus:
          type: array
          items:
            $ref: "#/components/schemas/WorkerProfile"
          description: The cluster worker profiles status.
        apiserverProfile:
          $ref: "#/components/schemas/APIServerProfile"
          description: The cluster API server profile.
        ingressProfiles:
          type: array
          items:
            $ref: "#/components/schemas/IngressProfile"
          description: The cluster ingress profiles.
    ProvisioningState:
      type: string
      enum:
        - AdminUpdating
        - Canceled
        - Creating
        - Deleting
        - Failed
        - Succeeded
        - Updating
    ClusterProfile:
      type: object
      description: ClusterProfile represents a cluster profile.
      properties:
        pullSecret:
          type: string
          description: The pull secret for the cluster.
        domain:
          type: string
          description: The domain for the cluster.
        version:
          type: string
          description: The version of the cluster.
        resourceGroupId:
          type: string
          description: The ID of the cluster resource group.
        fipsValidatedModules:
          $ref: "#/components/schemas/FipsValidatedModules"
          description: 'If FIPS validated crypto modules are used. Known values are: "Disabled" and "Enabled".'
        oidcIssuer:
          type: string
          description: The URL of the managed OIDC issuer in a workload identity cluster.
    FipsValidatedModules:
      type: string
      enum:
        - Disabled
        - Enabled
    ConsoleProfile:
      type: object
      description: ConsoleProfile represents a console profile.
      properties:
        url:
          type: string
          description: The URL to access the cluster console.
    ServicePrincipalProfile:
      type: object
      description: ServicePrincipalProfile represents a service principal profile.
      properties:
        clientId:
          type: string
          description: The client ID used for the cluster.
        clientSecret:
          type: string
          description: The client secret used for the cluster.
    PlatformWorkloadIdentityProfile:
      type: object
      description: PlatformWorkloadIdentityProfile encapsulates all information that is specific to workload identity clusters.
      properties:
        upgradeableTo:
          type: string
          description: UpgradeableTo stores a single OpenShift version a workload identity cluster can be upgraded to.
        platformWorkloadIdentities:
          type: object
          properties: {}
          description: Dictionary of .
    NetworkProfile:
      type: object
      description: NetworkProfile represents a network profile.
      properties:
        podCidr:
          type: string
          description: The CIDR used for OpenShift/Kubernetes Pods.
        serviceCidr:
          type: string
          description: The CIDR used for OpenShift/Kubernetes Services.
        outboundType:
          $ref: "#/components/schemas/OutboundType"
          description: 'The OutboundType used for egress traffic. Known values are: "Loadbalancer" and "UserDefinedRouting".'
        loadBalancerProfile:
          $ref: "#/components/schemas/LoadBalancerProfile"
          description: The cluster load balancer profile.
        preconfiguredNSG:
          $ref: "#/components/schemas/PreconfiguredNSG"
          description: 'Specifies whether subnets are pre-attached with an NSG. Known values are: "Disabled" and "Enabled".'
    OutboundType:
      type: string
      enum:
        - Loadbalancer
        - UserDefinedRouting
    LoadBalancerProfile:
      type: object
      description: LoadBalancerProfile represents the profile of the cluster public load balancer.
      properties:
        managedOutboundIps:
          $ref: "#/components/schemas/ManagedOutboundIPs"
          description: The desired managed outbound IPs for the cluster public load balancer.
        effectiveOutboundIps:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveOutboundIP"
          description: The list of effective outbound IP addresses of the public load balancer.
    ManagedOutboundIPs:
      type: object
      description: ManagedOutboundIPs represents the desired managed outbound IPs for the cluster public load balancer.
      properties:
        count:
          type: integer
          description: Count represents the desired number of IPv4 outbound IPs created and managed by Azure for the cluster public load balancer. Allowed values are in the range of 1 - 20. The default value is 1.
    EffectiveOutboundIP:
      type: object
      description: EffectiveOutboundIP represents an effective outbound IP resource of the cluster public load balancer.
      properties:
        id:
          type: string
          description: The fully qualified Azure resource id of an IP address resource.
    PreconfiguredNSG:
      type: string
      enum:
        - Disabled
        - Enabled
    MasterProfile:
      type: object
      description: MasterProfile represents a master profile.
      properties:
        vmSize:
          type: string
          description: The size of the master VMs.
        subnetId:
          type: string
          description: The Azure resource ID of the master subnet.
        encryptionAtHost:
          $ref: "#/components/schemas/EncryptionAtHost"
          description: 'Whether master virtual machines are encrypted at host. Known values are: "Disabled" and "Enabled".'
        diskEncryptionSetId:
          type: string
          description: The resource ID of an associated DiskEncryptionSet, if applicable.
    EncryptionAtHost:
      type: string
      enum:
        - Disabled
        - Enabled
    WorkerProfile:
      type: object
      description: WorkerProfile represents a worker profile.
      properties:
        name:
          type: string
          description: The worker profile name.
        vmSize:
          type: string
          description: The size of the worker VMs.
        diskSizeGB:
          type: integer
          description: The disk size of the worker VMs.
        subnetId:
          type: string
          description: The Azure resource ID of the worker subnet.
        count:
          type: integer
          description: The number of worker VMs.
        encryptionAtHost:
          $ref: "#/components/schemas/EncryptionAtHost"
          description: 'Whether master virtual machines are encrypted at host. Known values are: "Disabled" and "Enabled".'
        diskEncryptionSetId:
          type: string
          description: The resource ID of an associated DiskEncryptionSet, if applicable.
    APIServerProfile:
      type: object
      description: APIServerProfile represents an API server profile.
      properties:
        visibility:
          $ref: "#/components/schemas/Visibility"
          description: 'API server visibility. Known values are: "Private" and "Public".'
        url:
          type: string
          description: The URL to access the cluster API server.
        ip:
          type: string
          description: The IP of the cluster API server.
    Visibility:
      type: string
      enum:
        - Private
        - Public
    IngressProfile:
      type: object
      description: IngressProfile represents an ingress profile.
      properties:
        name:
          type: string
          description: The ingress profile name.
        visibility:
          $ref: "#/components/schemas/Visibility"
          description: 'Ingress visibility. Known values are: "Private" and "Public".'
        ip:
          type: string
          description: The IP of the ingress.
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    OpenShiftClusterFlattened:
      type: object
      description: Flattened view of OpenShiftCluster ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The cluster provisioning state. Known values are: "AdminUpdating", "Canceled", "Creating", "Deleting", "Failed", "Succeeded", and "Updating".'
        clusterProfile:
          $ref: "#/components/schemas/ClusterProfile"
          description: The cluster profile.
        consoleProfile:
          $ref: "#/components/schemas/ConsoleProfile"
          description: The console profile.
        servicePrincipalProfile:
          $ref: "#/components/schemas/ServicePrincipalProfile"
          description: The cluster service principal profile.
        platformWorkloadIdentityProfile:
          $ref: "#/components/schemas/PlatformWorkloadIdentityProfile"
          description: The workload identity profile.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: The cluster network profile.
        masterProfile:
          $ref: "#/components/schemas/MasterProfile"
          description: The cluster master profile.
        workerProfiles:
          type: array
          items:
            $ref: "#/components/schemas/WorkerProfile"
          description: The cluster worker profiles.
        workerProfilesStatus:
          type: array
          items:
            $ref: "#/components/schemas/WorkerProfile"
          description: The cluster worker profiles status.
        apiserverProfile:
          $ref: "#/components/schemas/APIServerProfile"
          description: The cluster API server profile.
        ingressProfiles:
          type: array
          items:
            $ref: "#/components/schemas/IngressProfile"
          description: The cluster ingress profiles.
    PagedOpenShiftClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OpenShiftClusterFlattened"
        nextLink:
          type: string
    OpenShiftCluster:
      type: object
      description: OpenShiftCluster represents an Azure Red Hat OpenShift cluster.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/OpenShiftClusterProperties"
          description: The cluster properties.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    OpenShiftClusterAdminKubeconfig:
      type: object
      description: OpenShiftClusterAdminKubeconfig represents an OpenShift cluster's admin kubeconfig.
      properties:
        kubeconfig:
          type: string
          description: The base64-encoded kubeconfig file.
    OpenShiftClusterCredentials:
      type: object
      description: OpenShiftClusterCredentials represents an OpenShift cluster's credentials.
      properties:
        kubeadminUsername:
          type: string
          description: The username for the kubeadmin user.
        kubeadminPassword:
          type: string
          description: The password for the kubeadmin user.
    OpenShiftVersionProperties:
      type: object
      description: OpenShiftVersionProperties represents the properties of an OpenShiftVersion.
      properties:
        version:
          type: string
          description: Version represents the version to create the cluster at.
    OpenShiftVersionFlattened:
      type: object
      description: Flattened view of OpenShiftVersion ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        version:
          type: string
          description: Version represents the version to create the cluster at.
    PagedOpenShiftVersionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OpenShiftVersionFlattened"
        nextLink:
          type: string
    Display:
      type: object
      description: Display represents the display details of an operation.
      properties:
        provider:
          type: string
          description: Friendly name of the resource provider.
        resource:
          type: string
          description: Resource type on which the operation is performed.
        operation:
          type: string
          description: "Operation type: read, write, delete, listKeys/action, etc."
        description:
          type: string
          description: Friendly name of the operation.
    Operation:
      type: object
      description: Operation represents an RP operation.
      properties:
        name:
          type: string
          description: "Operation name: {provider}/{resource}/{operation}."
        display:
          $ref: "#/components/schemas/Display"
          description: The object that describes the operation.
        origin:
          type: string
          description: Sources of requests to this operation. Comma separated list with valid values user or system, e.g. "user,system".
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    PlatformWorkloadIdentityRoleSetProperties:
      type: object
      description: PlatformWorkloadIdentityRoleSetProperties represents the properties of a PlatformWorkloadIdentityRoleSet resource.
      properties:
        openShiftVersion:
          type: string
          description: OpenShiftVersion represents the version associated with this set of roles.
        platformWorkloadIdentityRoles:
          type: array
          items:
            $ref: "#/components/schemas/PlatformWorkloadIdentityRole"
          description: PlatformWorkloadIdentityRoles represents the set of roles associated with this version.
    PlatformWorkloadIdentityRole:
      type: object
      description: PlatformWorkloadIdentityRole represents a mapping from a particular OCP operator to the built-in role that should be assigned to that operator's corresponding managed identity.
      properties:
        operatorName:
          type: string
          description: OperatorName represents the name of the operator that this role is for.
        roleDefinitionName:
          type: string
          description: RoleDefinitionName represents the name of the role.
        roleDefinitionId:
          type: string
          description: RoleDefinitionID represents the resource ID of the role definition.
    PlatformWorkloadIdentityRoleSetFlattened:
      type: object
      description: Flattened view of PlatformWorkloadIdentityRoleSet ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        openShiftVersion:
          type: string
          description: OpenShiftVersion represents the version associated with this set of roles.
        platformWorkloadIdentityRoles:
          type: array
          items:
            $ref: "#/components/schemas/PlatformWorkloadIdentityRole"
          description: PlatformWorkloadIdentityRoles represents the set of roles associated with this version.
    PagedPlatformWorkloadIdentityRoleSetFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PlatformWorkloadIdentityRoleSetFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    open_shift_clusters:
      id: azure.red_hat_openshift.open_shift_clusters
      name: open_shift_clusters
      title: open_shift_clusters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters~1{resource_name}?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters~1{resource_name}?api-version=2025-07-25/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters~1{resource_name}?api-version=2025-07-25/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters~1{resource_name}?api-version=2025-07-25/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_admin_credentials:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters~1{resource_name}~1listAdminCredentials?api-version=2025-07-25/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_credentials:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.RedHatOpenShift~1openShiftClusters~1{resource_name}~1listCredentials?api-version=2025-07-25/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/open_shift_clusters/methods/get"
          - $ref: "#/components/x-stackQL-resources/open_shift_clusters/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/open_shift_clusters/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/open_shift_clusters/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/open_shift_clusters/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/open_shift_clusters/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/open_shift_clusters/methods/delete"
    open_shift_versions:
      id: azure.red_hat_openshift.open_shift_versions
      name: open_shift_versions
      title: open_shift_versions
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.RedHatOpenShift~1locations~1{location}~1openShiftVersions~1{open_shift_version}?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.RedHatOpenShift~1locations~1{location}~1openShiftVersions?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/open_shift_versions/methods/get"
          - $ref: "#/components/x-stackQL-resources/open_shift_versions/methods/list"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.red_hat_openshift.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.RedHatOpenShift~1operations?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    platform_workload_identity_role_set:
      id: azure.red_hat_openshift.platform_workload_identity_role_set
      name: platform_workload_identity_role_set
      title: platform_workload_identity_role_set
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.RedHatOpenShift~1locations~1{location}~1platformWorkloadIdentityRoleSets~1{open_shift_minor_version}?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/platform_workload_identity_role_set/methods/get"
        insert: []
        update: []
        delete: []
    platform_workload_identity_role_sets:
      id: azure.red_hat_openshift.platform_workload_identity_role_sets
      name: platform_workload_identity_role_sets
      title: platform_workload_identity_role_sets
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.RedHatOpenShift~1locations~1{location}~1platformWorkloadIdentityRoleSets?api-version=2025-07-25/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/platform_workload_identity_role_sets/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.RedHatOpenShift/openShiftClusters/{resource_name}?api-version=2025-07-25:
    get:
      operationId: OpenShiftClusters_get
      description: Gets a OpenShift cluster with the specified subscription, resource group and resource name. The operation returns properties of a OpenShift cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OpenShift cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenShiftClusterFlattened"
    put:
      operationId: OpenShiftClusters_begin_create_or_update
      description: Creates or updates a OpenShift cluster with the specified subscription, resource group and resource name. The operation returns properties of a OpenShift cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OpenShift cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/OpenShiftClusterProperties"
                  description: The cluster properties.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenShiftCluster"
    patch:
      operationId: OpenShiftClusters_begin_update
      description: Creates or updates a OpenShift cluster with the specified subscription, resource group and resource name. The operation returns properties of a OpenShift cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OpenShift cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: The resource tags.
                properties:
                  $ref: "#/components/schemas/OpenShiftClusterProperties"
                  description: The cluster properties.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: Identity stores information about the cluster MSI(s) in a workload identity cluster.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenShiftCluster"
    delete:
      operationId: OpenShiftClusters_begin_delete
      description: Deletes a OpenShift cluster with the specified subscription, resource group and resource name. The operation returns nothing.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OpenShift cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.RedHatOpenShift/openShiftClusters?api-version=2025-07-25:
    get:
      operationId: OpenShiftClusters_list
      description: Lists OpenShift clusters in the specified subscription. The operation returns properties of each OpenShift cluster.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOpenShiftClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.RedHatOpenShift/openShiftClusters?api-version=2025-07-25:
    get:
      operationId: OpenShiftClusters_list_by_resource_group
      description: Lists OpenShift clusters in the specified subscription and resource group. The operation returns properties of each OpenShift cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOpenShiftClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.RedHatOpenShift/openShiftClusters/{resource_name}/listAdminCredentials?api-version=2025-07-25:
    post:
      operationId: OpenShiftClusters_list_admin_credentials
      description: Lists admin kubeconfig of an OpenShift cluster with the specified subscription, resource group and resource name. The operation returns the admin kubeconfig.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OpenShift cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenShiftClusterAdminKubeconfig"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.RedHatOpenShift/openShiftClusters/{resource_name}/listCredentials?api-version=2025-07-25:
    post:
      operationId: OpenShiftClusters_list_credentials
      description: Lists credentials of an OpenShift cluster with the specified subscription, resource group and resource name. The operation returns the credentials.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OpenShift cluster resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenShiftClusterCredentials"
  /subscriptions/{subscription_id}/providers/Microsoft.RedHatOpenShift/locations/{location}/openShiftVersions/{open_shift_version}?api-version=2025-07-25:
    get:
      operationId: OpenShiftVersions_get
      description: Gets an available OpenShift version to install in the specified location. This operation returns installable OpenShift version as a string.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: open_shift_version
          in: path
          required: true
          schema:
            type: string
          description: The desired version value of the OpenShiftVersion resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenShiftVersionFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.RedHatOpenShift/locations/{location}/openShiftVersions?api-version=2025-07-25:
    get:
      operationId: OpenShiftVersions_list
      description: Lists all OpenShift versions available to install in the specified location. The operation returns the installable OpenShift versions as a string.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOpenShiftVersionFlattened"
  /providers/Microsoft.RedHatOpenShift/operations?api-version=2025-07-25:
    get:
      operationId: redhatopenshift_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/providers/Microsoft.RedHatOpenShift/locations/{location}/platformWorkloadIdentityRoleSets/{open_shift_minor_version}?api-version=2025-07-25:
    get:
      operationId: PlatformWorkloadIdentityRoleSet_get
      description: Gets a mapping of an OpenShift version to identity requirements, which includes operatorName, roleDefinitionName, roleDefinitionId, and serviceAccounts. This operation returns Platform Workload Identity Role Set as a string.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: open_shift_minor_version
          in: path
          required: true
          schema:
            type: string
          description: The desired version value of the PlatformWorkloadIdentityRoleSet resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformWorkloadIdentityRoleSetFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.RedHatOpenShift/locations/{location}/platformWorkloadIdentityRoleSets?api-version=2025-07-25:
    get:
      operationId: PlatformWorkloadIdentityRoleSets_list
      description: Lists a mapping of OpenShift versions to identity requirements, which include operatorName, roleDefinitionName, roleDefinitionId, and serviceAccounts. This operation returns a list of Platform Workload Identity Role Sets as a string.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPlatformWorkloadIdentityRoleSetFlattened"
