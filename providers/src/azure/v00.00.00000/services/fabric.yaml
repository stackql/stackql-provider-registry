openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Fabric Mgmt Client
  description: FabricMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-11-01-stackql-generated
  x-serviceAlias: fabric
  x-sdkPackage: azure-mgmt-fabric
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    FabricCapacityProperties:
      type: object
      description: The Microsoft Fabric capacity properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The current deployment state of Microsoft Fabric resource. The provisioningState is to indicate states for resource provisioning. Known values are: "Succeeded", "Failed", "Canceled", "Deleting", "Provisioning", and "Updating".'
        state:
          $ref: "#/components/schemas/ResourceState"
          description: 'The current state of Microsoft Fabric resource. The state is to indicate more states outside of resource provisioning. Known values are: "Active", "Provisioning", "Failed", "Updating", "Deleting", "Suspending", "Suspended", "Pausing", "Paused", "Resuming", "Scaling", and "Preparing".'
        administration:
          $ref: "#/components/schemas/CapacityAdministration"
          description: The capacity administration. Required.
      required:
        - administration
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Deleting
        - Provisioning
        - Updating
    ResourceState:
      type: string
      enum:
        - Active
        - Provisioning
        - Failed
        - Updating
        - Deleting
        - Suspending
        - Suspended
        - Pausing
        - Paused
        - Resuming
        - Scaling
        - Preparing
    CapacityAdministration:
      type: object
      description: The administration properties of the Fabric capacity resource.
      properties:
        members:
          type: array
          items:
            type: string
          description: An array of administrator user identities. Required.
      required:
        - members
    RpSku:
      type: object
      description: Represents the SKU name and Azure pricing tier for Microsoft Fabric capacity resource.
      properties:
        name:
          type: string
          description: The name of the SKU level. Required.
        tier:
          $ref: "#/components/schemas/RpSkuTier"
          description: The name of the Azure pricing tier to which the SKU applies. Required. "Fabric"
      required:
        - name
        - tier
    RpSkuTier:
      type: string
      enum:
        - Fabric
    FabricCapacityFlattened:
      type: object
      description: Flattened view of FabricCapacity ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        sku:
          $ref: "#/components/schemas/RpSku"
          description: The SKU details. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The current deployment state of Microsoft Fabric resource. The provisioningState is to indicate states for resource provisioning. Known values are: "Succeeded", "Failed", "Canceled", "Deleting", "Provisioning", and "Updating".'
        state:
          $ref: "#/components/schemas/ResourceState"
          description: 'The current state of Microsoft Fabric resource. The state is to indicate more states outside of resource provisioning. Known values are: "Active", "Provisioning", "Failed", "Updating", "Deleting", "Suspending", "Suspended", "Pausing", "Paused", "Resuming", "Scaling", and "Preparing".'
        administration:
          $ref: "#/components/schemas/CapacityAdministration"
          description: The capacity administration. Required.
    PagedFabricCapacityFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FabricCapacityFlattened"
        nextLink:
          type: string
    FabricCapacity:
      type: object
      description: Fabric Capacity resource. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/FabricCapacityProperties"
          description: The resource-specific properties for this resource. Required.
        sku:
          $ref: "#/components/schemas/RpSku"
          description: The SKU details. Required.
      required:
        - location
        - properties
        - sku
    RpSkuDetailsForExistingResource:
      type: object
      description: An object that represents SKU details for existing resources.
      properties:
        resourceType:
          type: string
          description: The resource type. Required.
        sku:
          $ref: "#/components/schemas/RpSku"
          description: The SKU details. Required.
      required:
        - resourceType
        - sku
    PagedRpSkuDetailsForExistingResource:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RpSkuDetailsForExistingResource"
        nextLink:
          type: string
    RpSkuDetailsForNewResource:
      type: object
      description: The SKU details.
      properties:
        resourceType:
          type: string
          description: The resource type. Required.
        name:
          type: string
          description: The SKU's name. Required.
        locations:
          type: array
          items:
            type: string
          description: The list of available locations for the SKU. Required.
      required:
        - resourceType
        - name
        - locations
    PagedRpSkuDetailsForNewResource:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RpSkuDetailsForNewResource"
        nextLink:
          type: string
    FabricCapacityUpdateProperties:
      type: object
      description: The updatable properties of the FabricCapacity.
      properties:
        administration:
          $ref: "#/components/schemas/CapacityAdministration"
          description: The capacity administration.
    CheckNameAvailabilityResponse:
      type: object
      description: The check availability result.
      properties:
        nameAvailable:
          type: boolean
          description: Indicates if the resource name is available.
        reason:
          $ref: "#/components/schemas/CheckNameAvailabilityReason"
          description: 'The reason why the given name is not available. Known values are: "Invalid" and "AlreadyExists".'
        message:
          type: string
          description: Detailed reason why the given name is not available.
    CheckNameAvailabilityReason:
      type: string
      enum:
        - Invalid
        - AlreadyExists
    OperationDisplay:
      type: object
      description: Localized display information for and operation. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    fabric_capacities:
      id: azure.fabric.fabric_capacities
      name: fabric_capacities
      title: fabric_capacities
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities~1{capacity_name}?api-version=2023-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities~1{capacity_name}?api-version=2023-11-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities~1{capacity_name}?api-version=2023-11-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities~1{capacity_name}?api-version=2023-11-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities?api-version=2023-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Fabric~1capacities?api-version=2023-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_skus_for_capacity:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities~1{capacity_name}~1skus?api-version=2023-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_skus:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Fabric~1skus?api-version=2023-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        resume:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities~1{capacity_name}~1resume?api-version=2023-11-01/post"
          response:
            openAPIDocKey: "204"
        suspend:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Fabric~1capacities~1{capacity_name}~1suspend?api-version=2023-11-01/post"
          response:
            openAPIDocKey: "204"
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Fabric~1locations~1{location}~1checkNameAvailability?api-version=2023-11-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/fabric_capacities/methods/get"
          - $ref: "#/components/x-stackQL-resources/fabric_capacities/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/fabric_capacities/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/fabric_capacities/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/fabric_capacities/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/fabric_capacities/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/fabric_capacities/methods/delete"
    operations:
      id: azure.fabric.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Fabric~1operations?api-version=2023-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Fabric/capacities/{capacity_name}?api-version=2023-11-01:
    get:
      operationId: FabricCapacities_get
      description: Get a FabricCapacity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Microsoft Fabric capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FabricCapacityFlattened"
    put:
      operationId: FabricCapacities_begin_create_or_update
      description: Create a FabricCapacity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Microsoft Fabric capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/FabricCapacityProperties"
                  description: The resource-specific properties for this resource. Required.
                sku:
                  $ref: "#/components/schemas/RpSku"
                  description: The SKU details. Required.
              required:
                - location
                - properties
                - sku
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FabricCapacity"
    patch:
      operationId: FabricCapacities_begin_update
      description: Update a FabricCapacity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Microsoft Fabric capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                sku:
                  $ref: "#/components/schemas/RpSku"
                  description: The SKU details.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/FabricCapacityUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FabricCapacity"
    delete:
      operationId: FabricCapacities_begin_delete
      description: Delete a FabricCapacity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Microsoft Fabric capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Fabric/capacities?api-version=2023-11-01:
    get:
      operationId: FabricCapacities_list_by_resource_group
      description: List FabricCapacity resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFabricCapacityFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Fabric/capacities?api-version=2023-11-01:
    get:
      operationId: FabricCapacities_list_by_subscription
      description: List FabricCapacity resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFabricCapacityFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Fabric/capacities/{capacity_name}/skus?api-version=2023-11-01:
    get:
      operationId: FabricCapacities_list_skus_for_capacity
      description: List eligible SKUs for a Microsoft Fabric resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the capacity. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRpSkuDetailsForExistingResource"
  /subscriptions/{subscription_id}/providers/Microsoft.Fabric/skus?api-version=2023-11-01:
    get:
      operationId: FabricCapacities_list_skus
      description: List eligible SKUs for Microsoft Fabric resource provider.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRpSkuDetailsForNewResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Fabric/capacities/{capacity_name}/resume?api-version=2023-11-01:
    post:
      operationId: FabricCapacities_begin_resume
      description: Resume operation of the specified Fabric capacity instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Microsoft Fabric capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Fabric/capacities/{capacity_name}/suspend?api-version=2023-11-01:
    post:
      operationId: FabricCapacities_begin_suspend
      description: Suspend operation of the specified Fabric capacity instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Microsoft Fabric capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Fabric/locations/{location}/checkNameAvailability?api-version=2023-11-01:
    post:
      operationId: FabricCapacities_check_name_availability
      description: Implements local CheckNameAvailability operations.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The name of the resource for which availability needs to be checked.
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResponse"
  /providers/Microsoft.Fabric/operations?api-version=2023-11-01:
    get:
      operationId: fabric_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
