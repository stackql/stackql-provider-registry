openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Template Specs Client
  description: TemplateSpecs Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-05-01-stackql-generated
  x-serviceAlias: resource_templatespecs
  x-sdkPackage: azure-mgmt-resource-templatespecs
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    LinkedTemplateArtifact:
      type: object
      description: Represents a Template Spec artifact containing an embedded Azure Resource Manager template for use as a linked template. All required parameters must be populated in order to send to server.
      properties:
        path:
          type: string
          description: A filesystem safe relative path of the artifact. Required.
        template:
          type: object
          properties: {}
          description: The Azure Resource Manager template. Required.
      required:
        - path
        - template
    TemplateSpecVersionFlattened:
      type: object
      description: Flattened view of TemplateSpecVersion ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: String Id used to locate any resource on Azure.
        name:
          type: string
          description: Name of this resource.
        type:
          type: string
          description: Type of this resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        location:
          type: string
          description: The location of the Template Spec Version. It must match the location of the parent Template Spec. Required.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        description:
          type: string
          description: Template Spec version description.
        linkedTemplates:
          type: array
          items:
            $ref: "#/components/schemas/LinkedTemplateArtifact"
          description: An array of linked template artifacts.
        metadata:
          type: object
          properties: {}
          description: The version metadata. Metadata is an open-ended object and is typically a collection of key-value pairs.
        mainTemplate:
          type: object
          properties: {}
          description: The main Azure Resource Manager template content.
        uiFormDefinition:
          type: object
          properties: {}
          description: The Azure Resource Manager template UI definition content.
    PagedTemplateSpecVersionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TemplateSpecVersionFlattened"
        nextLink:
          type: string
    TemplateSpecVersion:
      type: object
      description: Template Spec Version object. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: String Id used to locate any resource on Azure.
        name:
          type: string
          description: Name of this resource.
        type:
          type: string
          description: Type of this resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        location:
          type: string
          description: The location of the Template Spec Version. It must match the location of the parent Template Spec. Required.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        properties:
          type: object
          properties:
            description:
              type: string
              description: Template Spec version description.
            linkedTemplates:
              type: array
              items:
                $ref: "#/components/schemas/LinkedTemplateArtifact"
              description: An array of linked template artifacts.
            metadata:
              type: object
              properties: {}
              description: The version metadata. Metadata is an open-ended object and is typically a collection of key-value pairs.
            mainTemplate:
              type: object
              properties: {}
              description: The main Azure Resource Manager template content.
            uiFormDefinition:
              type: object
              properties: {}
              description: The Azure Resource Manager template UI definition content.
      required:
        - location
    TemplateSpecFlattened:
      type: object
      description: Flattened view of TemplateSpec ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: String Id used to locate any resource on Azure.
        name:
          type: string
          description: Name of this resource.
        type:
          type: string
          description: Type of this resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        location:
          type: string
          description: The location of the Template Spec. It cannot be changed after Template Spec creation. It must be one of the supported Azure locations. Required.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        description:
          type: string
          description: Template Spec description.
        displayName:
          type: string
          description: Template Spec display name.
        metadata:
          type: object
          properties: {}
          description: The Template Spec metadata. Metadata is an open-ended object and is typically a collection of key-value pairs.
        versions:
          type: object
          properties: {}
          description: High-level information about the versions within this Template Spec. The keys are the version names. Only populated if the $expand query parameter is set to 'versions'.
    PagedTemplateSpecFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TemplateSpecFlattened"
        nextLink:
          type: string
    TemplateSpec:
      type: object
      description: Template Spec object. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: String Id used to locate any resource on Azure.
        name:
          type: string
          description: Name of this resource.
        type:
          type: string
          description: Type of this resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        location:
          type: string
          description: The location of the Template Spec. It cannot be changed after Template Spec creation. It must be one of the supported Azure locations. Required.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        properties:
          type: object
          properties:
            description:
              type: string
              description: Template Spec description.
            displayName:
              type: string
              description: Template Spec display name.
            metadata:
              type: object
              properties: {}
              description: The Template Spec metadata. Metadata is an open-ended object and is typically a collection of key-value pairs.
            versions:
              type: object
              properties: {}
              description: High-level information about the versions within this Template Spec. The keys are the version names. Only populated if the $expand query parameter is set to 'versions'.
      required:
        - location
  x-stackQL-resources:
    template_spec_versions:
      id: azure.resource_templatespecs.template_spec_versions
      name: template_spec_versions
      title: template_spec_versions
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}~1versions~1{template_spec_version}?api-version=2021-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}~1versions~1{template_spec_version}?api-version=2021-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}~1versions~1{template_spec_version}?api-version=2021-05-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}~1versions~1{template_spec_version}?api-version=2021-05-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}~1versions?api-version=2021-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/template_spec_versions/methods/get"
          - $ref: "#/components/x-stackQL-resources/template_spec_versions/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/template_spec_versions/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/template_spec_versions/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/template_spec_versions/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/template_spec_versions/methods/delete"
    template_specs:
      id: azure.resource_templatespecs.template_specs
      name: template_specs
      title: template_specs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}?api-version=2021-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}?api-version=2021-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}?api-version=2021-05-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1{template_spec_name}?api-version=2021-05-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Resources~1templateSpecs~1?api-version=2021-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Resources~1templateSpecs~1?api-version=2021-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/template_specs/methods/get"
          - $ref: "#/components/x-stackQL-resources/template_specs/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/template_specs/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/template_specs/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/template_specs/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/template_specs/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/template_specs/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Resources/templateSpecs/{template_spec_name}/versions/{template_spec_version}?api-version=2021-05-01:
    get:
      operationId: TemplateSpecVersions_get
      description: Gets a Template Spec version from a specific Template Spec.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: template_spec_version
          in: path
          required: true
          schema:
            type: string
          description: The version of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateSpecVersionFlattened"
    put:
      operationId: TemplateSpecVersions_create_or_update
      description: Creates or updates a Template Spec version.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: template_spec_version
          in: path
          required: true
          schema:
            type: string
          description: The version of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: The location of the Template Spec Version. It must match the location of the parent Template Spec. Required.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  type: object
                  properties:
                    description:
                      type: string
                      description: Template Spec version description.
                    linkedTemplates:
                      type: array
                      items:
                        $ref: "#/components/schemas/LinkedTemplateArtifact"
                      description: An array of linked template artifacts.
                    metadata:
                      type: object
                      properties: {}
                      description: The version metadata. Metadata is an open-ended object and is typically a collection of key-value pairs.
                    mainTemplate:
                      type: object
                      properties: {}
                      description: The main Azure Resource Manager template content.
                    uiFormDefinition:
                      type: object
                      properties: {}
                      description: The Azure Resource Manager template UI definition content.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateSpecVersion"
    patch:
      operationId: TemplateSpecVersions_update
      description: Updates Template Spec Version tags with specified values.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: template_spec_version
          in: path
          required: true
          schema:
            type: string
          description: The version of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateSpecVersion"
    delete:
      operationId: TemplateSpecVersions_delete
      description: Deletes a specific version from a Template Spec. When operation completes, status code 200 returned without content.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: template_spec_version
          in: path
          required: true
          schema:
            type: string
          description: The version of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Resources/templateSpecs/{template_spec_name}/versions?api-version=2021-05-01:
    get:
      operationId: TemplateSpecVersions_list
      description: Lists all the Template Spec versions in the specified Template Spec.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTemplateSpecVersionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Resources/templateSpecs/{template_spec_name}?api-version=2021-05-01:
    get:
      operationId: TemplateSpecs_get
      description: Gets a Template Spec with a given name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $expand
          in: query
          required: false
          schema:
            type: string
          description: Allows for expansion of additional Template Spec details in the response. Optional. "versions" Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateSpecFlattened"
    put:
      operationId: TemplateSpecs_create_or_update
      description: Creates or updates a Template Spec.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: The location of the Template Spec. It cannot be changed after Template Spec creation. It must be one of the supported Azure locations. Required.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  type: object
                  properties:
                    description:
                      type: string
                      description: Template Spec description.
                    displayName:
                      type: string
                      description: Template Spec display name.
                    metadata:
                      type: object
                      properties: {}
                      description: The Template Spec metadata. Metadata is an open-ended object and is typically a collection of key-value pairs.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateSpec"
    patch:
      operationId: TemplateSpecs_update
      description: Updates Template Spec tags with specified values.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateSpec"
    delete:
      operationId: TemplateSpecs_delete
      description: Deletes a Template Spec by name. When operation completes, status code 200 returned without content.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: template_spec_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Template Spec. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Resources/templateSpecs/?api-version=2021-05-01:
    get:
      operationId: TemplateSpecs_list_by_subscription
      description: Lists all the Template Specs within the specified subscriptions.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $expand
          in: query
          required: false
          schema:
            type: string
          description: Allows for expansion of additional Template Spec details in the response. Optional. "versions" Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTemplateSpecFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Resources/templateSpecs/?api-version=2021-05-01:
    get:
      operationId: TemplateSpecs_list_by_resource_group
      description: Lists all the Template Specs within the specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $expand
          in: query
          required: false
          schema:
            type: string
          description: Allows for expansion of additional Template Spec details in the response. Optional. "versions" Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTemplateSpecFlattened"
