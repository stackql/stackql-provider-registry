openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Confidential Ledger
  description: Management plane for tamper-proof confidential ledgers backed by hardware enclaves.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2022-05-13-stackql-generated
  x-serviceAlias: confidential_ledger
  x-sdkPackage: azure-mgmt-confidentialledger
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    LedgerProperties:
      type: object
      description: Additional Confidential Ledger properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        ledgerName:
          type: string
          description: Unique name for the Confidential Ledger.
        ledgerUri:
          type: string
          description: Endpoint for calling Ledger Service.
        identityServiceUri:
          type: string
          description: Endpoint for accessing network identity.
        ledgerInternalNamespace:
          type: string
          description: Internal namespace for the Ledger.
        ledgerType:
          type: string
          description: 'Type of Confidential Ledger. Known values are: "Unknown", "Public", and "Private".'
        provisioningState:
          type: string
          description: 'Provisioning state of Ledger Resource. Known values are: "Unknown", "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Updating".'
        aadBasedSecurityPrincipals:
          type: array
          items:
            $ref: "#/components/schemas/AADBasedSecurityPrincipal"
          description: Array of all AAD based Security Principals.
        certBasedSecurityPrincipals:
          type: array
          items:
            $ref: "#/components/schemas/CertBasedSecurityPrincipal"
          description: Array of all cert based Security Principals.
    AADBasedSecurityPrincipal:
      type: object
      description: AAD based security principal with associated Ledger RoleName.
      properties:
        principalId:
          type: string
          description: UUID/GUID based Principal Id of the Security Principal.
        tenantId:
          type: string
          description: UUID/GUID based Tenant Id of the Security Principal.
        ledgerRoleName:
          type: string
          description: 'LedgerRole associated with the Security Principal of Ledger. Known values are: "Reader", "Contributor", and "Administrator".'
    CertBasedSecurityPrincipal:
      type: object
      description: Cert based security principal with Ledger RoleName.
      properties:
        cert:
          type: string
          description: Public key of the user cert (.pem or .cer).
        ledgerRoleName:
          type: string
          description: 'LedgerRole associated with the Security Principal of Ledger. Known values are: "Reader", "Contributor", and "Administrator".'
    ConfidentialLedgerFlattened:
      type: object
      description: Flattened view of ConfidentialLedger ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: Name of the Resource.
        id:
          type: string
          description: Fully qualified resource Id for the resource.
        type:
          type: string
          description: The type of the resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        location:
          type: string
          description: The Azure location where the Confidential Ledger is running.
        tags:
          type: object
          properties: {}
          description: Additional tags for Confidential Ledger.
        ledgerName:
          type: string
          description: Unique name for the Confidential Ledger.
        ledgerUri:
          type: string
          description: Endpoint for calling Ledger Service.
        identityServiceUri:
          type: string
          description: Endpoint for accessing network identity.
        ledgerInternalNamespace:
          type: string
          description: Internal namespace for the Ledger.
        ledgerType:
          type: string
          description: 'Type of Confidential Ledger. Known values are: "Unknown", "Public", and "Private".'
        provisioningState:
          type: string
          description: 'Provisioning state of Ledger Resource. Known values are: "Unknown", "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Updating".'
        aadBasedSecurityPrincipals:
          type: array
          items:
            $ref: "#/components/schemas/AADBasedSecurityPrincipal"
          description: Array of all AAD based Security Principals.
        certBasedSecurityPrincipals:
          type: array
          items:
            $ref: "#/components/schemas/CertBasedSecurityPrincipal"
          description: Array of all cert based Security Principals.
    PagedConfidentialLedgerFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ConfidentialLedgerFlattened"
        nextLink:
          type: string
    ConfidentialLedger:
      type: object
      description: Confidential Ledger. Contains the properties of Confidential Ledger Resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Name of the Resource.
        id:
          type: string
          description: Fully qualified resource Id for the resource.
        type:
          type: string
          description: The type of the resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        location:
          type: string
          description: The Azure location where the Confidential Ledger is running.
        tags:
          type: object
          properties: {}
          description: Additional tags for Confidential Ledger.
        properties:
          $ref: "#/components/schemas/LedgerProperties"
          description: Properties of Confidential Ledger Resource.
    ResourceProviderOperationDisplay:
      type: object
      description: Describes the properties of the Operation.
      properties:
        provider:
          type: string
          description: Name of the resource provider.
        resource:
          type: string
          description: Name of the resource type.
        operation:
          type: string
          description: Name of the resource provider operation.
        description:
          type: string
          description: Description of the resource provider operation.
    ResourceProviderOperationDefinition:
      type: object
      description: Describes the Resource Provider Operation.
      properties:
        name:
          type: string
          description: Resource provider operation name.
        isDataAction:
          type: boolean
          description: Indicates whether the operation is data action or not.
        display:
          $ref: "#/components/schemas/ResourceProviderOperationDisplay"
          description: Details about the operations.
    PagedResourceProviderOperationDefinition:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourceProviderOperationDefinition"
        nextLink:
          type: string
    CheckNameAvailabilityResponse:
      type: object
      description: The check availability result.
      properties:
        nameAvailable:
          type: boolean
          description: Indicates if the resource name is available.
        reason:
          type: string
          description: 'The reason why the given name is not available. Known values are: "Invalid" and "AlreadyExists".'
        message:
          type: string
          description: Detailed reason why the given name is available.
  x-stackQL-resources:
    ledger:
      id: azure.confidential_ledger.ledger
      name: ledger
      title: ledger
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConfidentialLedger~1ledgers~1{ledger_name}?api-version=2022-05-13/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConfidentialLedger~1ledgers~1{ledger_name}?api-version=2022-05-13/delete"
          response:
            openAPIDocKey: "204"
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConfidentialLedger~1ledgers~1{ledger_name}?api-version=2022-05-13/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConfidentialLedger~1ledgers~1{ledger_name}?api-version=2022-05-13/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConfidentialLedger~1ledgers?api-version=2022-05-13/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConfidentialLedger~1ledgers?api-version=2022-05-13/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/ledger/methods/get"
          - $ref: "#/components/x-stackQL-resources/ledger/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/ledger/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/ledger/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/ledger/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/ledger/methods/delete"
    operations:
      id: azure.confidential_ledger.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ConfidentialLedger~1operations?api-version=2022-05-13/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    check_name_availabilities:
      id: azure.confidential_ledger.check_name_availabilities
      name: check_name_availabilities
      title: check_name_availabilities
      methods:
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConfidentialLedger~1checkNameAvailability?api-version=2022-05-13/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/check_name_availabilities/methods/check_name_availability"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConfidentialLedger/ledgers/{ledger_name}?api-version=2022-05-13:
    get:
      operationId: Ledger_get
      description: Retrieves information about a Confidential Ledger resource. Retrieves the properties of a Confidential Ledger.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: ledger_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Confidential Ledger. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfidentialLedgerFlattened"
    delete:
      operationId: Ledger_begin_delete
      description: Deletes a Confidential Ledger resource. Deletes an existing Confidential Ledger.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: ledger_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Confidential Ledger. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    put:
      operationId: Ledger_begin_create
      description: Creates a Confidential Ledger. Creates a Confidential Ledger with the specified ledger parameters.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: ledger_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Confidential Ledger. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: The Azure location where the Confidential Ledger is running.
                tags:
                  type: object
                  properties: {}
                  description: Additional tags for Confidential Ledger.
                properties:
                  $ref: "#/components/schemas/LedgerProperties"
                  description: Properties of Confidential Ledger Resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfidentialLedger"
    patch:
      operationId: Ledger_begin_update
      description: Update Confidential Ledger properties. Updates properties of Confidential Ledger.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: ledger_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Confidential Ledger. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: The Azure location where the Confidential Ledger is running.
                tags:
                  type: object
                  properties: {}
                  description: Additional tags for Confidential Ledger.
                properties:
                  $ref: "#/components/schemas/LedgerProperties"
                  description: Properties of Confidential Ledger Resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfidentialLedger"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConfidentialLedger/ledgers?api-version=2022-05-13:
    get:
      operationId: Ledger_list_by_resource_group
      description: Retrieves information about all Confidential Ledger resources under the given subscription and resource group. Retrieves the properties of all Confidential Ledgers.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. eg. $filter=ledgerType eq 'Public'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConfidentialLedgerFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.ConfidentialLedger/ledgers?api-version=2022-05-13:
    get:
      operationId: Ledger_list_by_subscription
      description: Retrieves information about all Confidential Ledger resources under the given subscription. Retrieves the properties of all Confidential Ledgers.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the list operation. eg. $filter=ledgerType eq 'Public'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConfidentialLedgerFlattened"
  /providers/Microsoft.ConfidentialLedger/operations?api-version=2022-05-13:
    get:
      operationId: confidentialledger_list
      description: Retrieves a list of available API operations under this Resource Provider. Retrieves a list of available API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourceProviderOperationDefinition"
  /subscriptions/{subscription_id}/providers/Microsoft.ConfidentialLedger/checkNameAvailability?api-version=2022-05-13:
    post:
      operationId: _ConfidentialLedger_check_name_availability
      description: To check whether a resource name is available.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The name of the resource for which availability needs to be checked.
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResponse"
