openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Domain Registration Mgmt Client
  description: DomainRegistrationMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-11-01-stackql-generated
  x-serviceAlias: domainregistration
  x-sdkPackage: azure-mgmt-domainregistration
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    CsmOperationDisplay:
      type: object
      description: Meta data about operation used for display in portal.
      properties:
        provider:
          type: string
          description: Provider name.
        resource:
          type: string
          description: Resource type.
        operation:
          type: string
          description: Operation name.
        description:
          type: string
          description: Operation description.
    CsmOperationDescriptionProperties:
      type: object
      description: Properties available for a Microsoft.Web resource provider operation.
      properties:
        serviceSpecification:
          $ref: "#/components/schemas/ServiceSpecification"
          description: Resource metrics service provided by Microsoft.Insights resource provider.
    ServiceSpecification:
      type: object
      description: Resource metrics service provided by Microsoft.Insights resource provider.
      properties:
        metricSpecifications:
          type: array
          items:
            $ref: "#/components/schemas/MetricSpecification"
          description: Resource metrics service name.
        logSpecifications:
          type: array
          items:
            $ref: "#/components/schemas/LogSpecification"
          description: Resource logs service provided by Microsoft.Insights resource provider.
    MetricSpecification:
      type: object
      description: Definition of a single resource metric.
      properties:
        name:
          type: string
          description: Name of the resource metric.
        displayName:
          type: string
          description: Display name of the resource metric.
        displayDescription:
          type: string
          description: Description of the resource metric.
        unit:
          type: string
          description: Resource metric unit.
        aggregationType:
          type: string
          description: Resource metric aggregation type.
        supportsInstanceLevelAggregation:
          type: boolean
          description: Resource metric supported aggregation types.
        enableRegionalMdmAccount:
          type: boolean
          description: Resource metric supported time grain types.
        sourceMdmAccount:
          type: string
          description: Resource metric source MDM account.
        sourceMdmNamespace:
          type: string
          description: Resource metric source MDM namespace.
        metricFilterPattern:
          type: string
          description: Resource metric filter pattern.
        fillGapWithZero:
          type: boolean
          description: Resource metric fill gap with zero.
        isInternal:
          type: boolean
          description: Resource metric is internal.
        dimensions:
          type: array
          items:
            $ref: "#/components/schemas/Dimension"
          description: Resource metric dimensions.
        category:
          type: string
          description: Resource metric category.
        availabilities:
          type: array
          items:
            $ref: "#/components/schemas/MetricAvailability"
          description: Resource metric availability.
        supportedTimeGrainTypes:
          type: array
          items:
            type: string
          description: Resource metric supported time grain types.
        supportedAggregationTypes:
          type: array
          items:
            type: string
          description: Resource metric supported aggregation types.
    Dimension:
      type: object
      description: Dimension of a resource metric. For e.g. instance specific HTTP requests for a web app, where instance name is dimension of the metric HTTP request.
      properties:
        name:
          type: string
          description: Name of the dimension.
        displayName:
          type: string
          description: Display name of the dimension.
        internalName:
          type: string
          description: Dimension of the internal name.
        toBeExportedForShoebox:
          type: boolean
          description: Dimension to be exported for shoebox.
    MetricAvailability:
      type: object
      description: Retention policy of a resource metric.
      properties:
        timeGrain:
          type: string
          description: Metric availability time grain.
        blobDuration:
          type: string
          description: Metric availability blob duration.
    LogSpecification:
      type: object
      description: Log Definition of a single resource metric.
      properties:
        name:
          type: string
          description: Name of the log.
        displayName:
          type: string
          description: Display name of the log.
        blobDuration:
          type: string
          description: Blob duration of the log.
        logFilterPattern:
          type: string
          description: Log filtered pattern of the log.
    CsmOperationDescriptionFlattened:
      type: object
      description: Flattened view of CsmOperationDescription ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: Operation name, e.g. Microsoft.Web/sites/write.
        isDataAction:
          type: boolean
          description: Operation display name.
        display:
          $ref: "#/components/schemas/CsmOperationDisplay"
          description: Meta data about operation used for display in portal.
        origin:
          type: string
          description: Origin of the operation, e.g. "system" or "user".
        serviceSpecification:
          $ref: "#/components/schemas/ServiceSpecification"
          description: Resource metrics service provided by Microsoft.Insights resource provider.
    PagedCsmOperationDescriptionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CsmOperationDescriptionFlattened"
        nextLink:
          type: string
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    DomainProperties:
      type: object
      description: Domain resource specific properties.
      properties:
        contactAdmin:
          $ref: "#/components/schemas/Contact"
          description: Administrative contact. Required.
        contactBilling:
          $ref: "#/components/schemas/Contact"
          description: Billing contact. Required.
        contactRegistrant:
          $ref: "#/components/schemas/Contact"
          description: Registrant contact. Required.
        contactTech:
          $ref: "#/components/schemas/Contact"
          description: Technical contact. Required.
        registrationStatus:
          $ref: "#/components/schemas/DomainStatus"
          description: 'Domain registration status. Known values are: "Active", "Awaiting", "Cancelled", "Confiscated", "Disabled", "Excluded", "Expired", "Failed", "Held", "Locked", "Parked", "Pending", "Reserved", "Reverted", "Suspended", "Transferred", "Unknown", "Unlocked", "Unparked", "Updated", and "JsonConverterFailed".'
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Domain provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", and "Deleting".'
        nameServers:
          type: array
          items:
            type: string
          description: Name servers.
        privacy:
          type: boolean
          description: true if domain privacy is enabled for this domain; otherwise, false.
        createdTime:
          type: string
          format: date-time
          description: Domain creation timestamp.
        expirationTime:
          type: string
          format: date-time
          description: Domain expiration timestamp.
        lastRenewedTime:
          type: string
          format: date-time
          description: Timestamp when the domain was renewed last time.
        autoRenew:
          type: boolean
          description: true if the domain should be automatically renewed; otherwise, false.
        readyForDnsRecordManagement:
          type: boolean
          description: true if Azure can assign this domain to App Service apps; otherwise, false. This value will be true if domain registration status is active and \n it is hosted on name servers Azure has programmatic access to.
        managedHostNames:
          type: array
          items:
            $ref: "#/components/schemas/HostName"
          description: All hostnames derived from the domain and assigned to Azure resources.
        consent:
          $ref: "#/components/schemas/DomainPurchaseConsent"
          description: Legal agreement consent. Required.
        domainNotRenewableReasons:
          type: array
          items:
            $ref: "#/components/schemas/ResourceNotRenewableReason"
          description: Reasons why domain is not renewable.
        dnsType:
          $ref: "#/components/schemas/DnsType"
          description: 'Current DNS type. Known values are: "AzureDns" and "DefaultDomainRegistrarDns".'
        dnsZoneId:
          type: string
          description: Azure DNS Zone to use.
        targetDnsType:
          $ref: "#/components/schemas/DnsType"
          description: 'Target DNS type (would be used for migration). Known values are: "AzureDns" and "DefaultDomainRegistrarDns".'
        authCode:
          type: string
          description: Authorization code for the domain.
      required:
        - contactAdmin
        - contactBilling
        - contactRegistrant
        - contactTech
        - consent
    Contact:
      type: object
      description: Contact information for domain registration. If 'Domain Privacy' option is not selected then the contact information is made publicly available through the Whois directories as per ICANN requirements.
      properties:
        addressMailing:
          $ref: "#/components/schemas/Address"
          description: Mailing address.
        email:
          type: string
          description: Email address. Required.
        fax:
          type: string
          description: Fax number.
        jobTitle:
          type: string
          description: Job title.
        nameFirst:
          type: string
          description: First name. Required.
        nameLast:
          type: string
          description: Last name. Required.
        nameMiddle:
          type: string
          description: Middle name.
        organization:
          type: string
          description: Organization contact belongs to.
        phone:
          type: string
          description: Phone number. Required.
      required:
        - email
        - nameFirst
        - nameLast
        - phone
    Address:
      type: object
      description: Address information for domain registration.
      properties:
        address1:
          type: string
          description: First line of an Address. Required.
        address2:
          type: string
          description: The second line of the Address. Optional.
        city:
          type: string
          description: The city for the address. Required.
        country:
          type: string
          description: The country for the address. Required.
        postalCode:
          type: string
          description: The postal code for the address. Required.
        state:
          type: string
          description: The state or province for the address. Required.
      required:
        - address1
        - city
        - country
        - postalCode
        - state
    DomainStatus:
      type: string
      enum:
        - Active
        - Awaiting
        - Cancelled
        - Confiscated
        - Disabled
        - Excluded
        - Expired
        - Failed
        - Held
        - Locked
        - Parked
        - Pending
        - Reserved
        - Reverted
        - Suspended
        - Transferred
        - Unknown
        - Unlocked
        - Unparked
        - Updated
        - JsonConverterFailed
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - InProgress
        - Deleting
    HostName:
      type: object
      description: Details of a hostname derived from a domain.
      properties:
        name:
          type: string
          description: Name of the hostname.
        siteNames:
          type: array
          items:
            type: string
          description: List of apps the hostname is assigned to. This list will have more than one app only if the hostname is pointing to a Traffic Manager.
        azureResourceName:
          type: string
          description: Name of the Azure resource the hostname is assigned to. If it is assigned to a Traffic Manager then it will be the Traffic Manager name otherwise it will be the app name.
        azureResourceType:
          $ref: "#/components/schemas/AzureResourceType"
          description: 'Type of the Azure resource the hostname is assigned to. Known values are: "Website" and "TrafficManager".'
        customHostNameDnsRecordType:
          $ref: "#/components/schemas/CustomHostNameDnsRecordType"
          description: 'Type of the DNS record. Known values are: "CName" and "A".'
        hostNameType:
          $ref: "#/components/schemas/HostNameType"
          description: 'Type of the hostname. Known values are: "Verified" and "Managed".'
    AzureResourceType:
      type: string
      enum:
        - Website
        - TrafficManager
    CustomHostNameDnsRecordType:
      type: string
      enum:
        - CName
        - A
    HostNameType:
      type: string
      enum:
        - Verified
        - Managed
    DomainPurchaseConsent:
      type: object
      description: Domain purchase consent object, representing acceptance of applicable legal agreements.
      properties:
        agreementKeys:
          type: array
          items:
            type: string
          description: List of applicable legal agreement keys. This list can be retrieved using ListLegalAgreements API under TopLevelDomain resource.
        agreedBy:
          type: string
          description: Client IP address.
        agreedAt:
          type: string
          format: date-time
          description: Timestamp when the agreements were accepted.
    ResourceNotRenewableReason:
      type: string
      enum:
        - RegistrationStatusNotSupportedForRenewal
        - ExpirationNotInRenewalTimeRange
        - SubscriptionNotActive
    DnsType:
      type: string
      enum:
        - AzureDns
        - DefaultDomainRegistrarDns
    DomainFlattened:
      type: object
      description: Flattened view of Domain ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        kind:
          type: string
          description: Kind of resource.
        contactAdmin:
          $ref: "#/components/schemas/Contact"
          description: Administrative contact. Required.
        contactBilling:
          $ref: "#/components/schemas/Contact"
          description: Billing contact. Required.
        contactRegistrant:
          $ref: "#/components/schemas/Contact"
          description: Registrant contact. Required.
        contactTech:
          $ref: "#/components/schemas/Contact"
          description: Technical contact. Required.
        registrationStatus:
          $ref: "#/components/schemas/DomainStatus"
          description: 'Domain registration status. Known values are: "Active", "Awaiting", "Cancelled", "Confiscated", "Disabled", "Excluded", "Expired", "Failed", "Held", "Locked", "Parked", "Pending", "Reserved", "Reverted", "Suspended", "Transferred", "Unknown", "Unlocked", "Unparked", "Updated", and "JsonConverterFailed".'
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Domain provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", and "Deleting".'
        nameServers:
          type: array
          items:
            type: string
          description: Name servers.
        privacy:
          type: boolean
          description: true if domain privacy is enabled for this domain; otherwise, false.
        createdTime:
          type: string
          format: date-time
          description: Domain creation timestamp.
        expirationTime:
          type: string
          format: date-time
          description: Domain expiration timestamp.
        lastRenewedTime:
          type: string
          format: date-time
          description: Timestamp when the domain was renewed last time.
        autoRenew:
          type: boolean
          description: true if the domain should be automatically renewed; otherwise, false.
        readyForDnsRecordManagement:
          type: boolean
          description: true if Azure can assign this domain to App Service apps; otherwise, false. This value will be true if domain registration status is active and \n it is hosted on name servers Azure has programmatic access to.
        managedHostNames:
          type: array
          items:
            $ref: "#/components/schemas/HostName"
          description: All hostnames derived from the domain and assigned to Azure resources.
        consent:
          $ref: "#/components/schemas/DomainPurchaseConsent"
          description: Legal agreement consent. Required.
        domainNotRenewableReasons:
          type: array
          items:
            $ref: "#/components/schemas/ResourceNotRenewableReason"
          description: Reasons why domain is not renewable.
        dnsType:
          $ref: "#/components/schemas/DnsType"
          description: 'Current DNS type. Known values are: "AzureDns" and "DefaultDomainRegistrarDns".'
        dnsZoneId:
          type: string
          description: Azure DNS Zone to use.
        targetDnsType:
          $ref: "#/components/schemas/DnsType"
          description: 'Target DNS type (would be used for migration). Known values are: "AzureDns" and "DefaultDomainRegistrarDns".'
        authCode:
          type: string
          description: Authorization code for the domain.
    PagedDomainFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DomainFlattened"
        nextLink:
          type: string
    Domain:
      type: object
      description: Information about a domain.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/DomainProperties"
          description: Domain resource specific properties.
        kind:
          type: string
          description: Kind of resource.
      required:
        - location
    DomainOwnershipIdentifierProperties:
      type: object
      description: DomainOwnershipIdentifier resource specific properties.
      properties:
        ownershipId:
          type: string
          description: Ownership Id.
    DomainOwnershipIdentifierFlattened:
      type: object
      description: Flattened view of DomainOwnershipIdentifier ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        kind:
          type: string
          description: Kind of resource.
        ownershipId:
          type: string
          description: Ownership Id.
    PagedDomainOwnershipIdentifierFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DomainOwnershipIdentifierFlattened"
        nextLink:
          type: string
    NameIdentifier:
      type: object
      description: Identifies an object.
      properties:
        name:
          type: string
          description: Name of the object.
    PagedNameIdentifier:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/NameIdentifier"
        nextLink:
          type: string
    DomainControlCenterSsoRequest:
      type: object
      description: Single sign-on request information for domain management.
      properties:
        url:
          type: string
          description: URL where the single sign-on request is to be made.
        postParameterKey:
          type: string
          description: Post parameter key.
        postParameterValue:
          type: string
          description: Post parameter value. Client should use 'application/x-www-form-urlencoded' encoding for this value.
    DomainPatchResourceProperties:
      type: object
      description: DomainPatchResource resource specific properties.
      properties:
        contactAdmin:
          $ref: "#/components/schemas/Contact"
          description: Administrative contact. Required.
        contactBilling:
          $ref: "#/components/schemas/Contact"
          description: Billing contact. Required.
        contactRegistrant:
          $ref: "#/components/schemas/Contact"
          description: Registrant contact. Required.
        contactTech:
          $ref: "#/components/schemas/Contact"
          description: Technical contact. Required.
        registrationStatus:
          $ref: "#/components/schemas/DomainStatus"
          description: 'Domain registration status. Known values are: "Active", "Awaiting", "Cancelled", "Confiscated", "Disabled", "Excluded", "Expired", "Failed", "Held", "Locked", "Parked", "Pending", "Reserved", "Reverted", "Suspended", "Transferred", "Unknown", "Unlocked", "Unparked", "Updated", and "JsonConverterFailed".'
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Domain provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", and "Deleting".'
        nameServers:
          type: array
          items:
            type: string
          description: Name servers.
        privacy:
          type: boolean
          description: true if domain privacy is enabled for this domain; otherwise, false.
        createdTime:
          type: string
          format: date-time
          description: Domain creation timestamp.
        expirationTime:
          type: string
          format: date-time
          description: Domain expiration timestamp.
        lastRenewedTime:
          type: string
          format: date-time
          description: Timestamp when the domain was renewed last time.
        autoRenew:
          type: boolean
          description: true if the domain should be automatically renewed; otherwise, false.
        readyForDnsRecordManagement:
          type: boolean
          description: true if Azure can assign this domain to App Service apps; otherwise, false. This value will be true if domain registration status is active and \n it is hosted on name servers Azure has programmatic access to.
        managedHostNames:
          type: array
          items:
            $ref: "#/components/schemas/HostName"
          description: All hostnames derived from the domain and assigned to Azure resources.
        consent:
          $ref: "#/components/schemas/DomainPurchaseConsent"
          description: Legal agreement consent. Required.
        domainNotRenewableReasons:
          type: array
          items:
            $ref: "#/components/schemas/ResourceNotRenewableReason"
          description: Reasons why domain is not renewable.
        dnsType:
          $ref: "#/components/schemas/DnsType"
          description: 'Current DNS type. Known values are: "AzureDns" and "DefaultDomainRegistrarDns".'
        dnsZoneId:
          type: string
          description: Azure DNS Zone to use.
        targetDnsType:
          $ref: "#/components/schemas/DnsType"
          description: 'Target DNS type (would be used for migration). Known values are: "AzureDns" and "DefaultDomainRegistrarDns".'
        authCode:
          type: string
          description: Authorization code for the domain.
      required:
        - contactAdmin
        - contactBilling
        - contactRegistrant
        - contactTech
        - consent
    DomainOwnershipIdentifier:
      type: object
      description: Domain ownership Identifier.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/DomainOwnershipIdentifierProperties"
          description: DomainOwnershipIdentifier resource specific properties.
        kind:
          type: string
          description: Kind of resource.
    DomainAvailabilityCheckResult:
      type: object
      description: Domain availability check result.
      properties:
        name:
          type: string
          description: Name of the domain.
        available:
          type: boolean
          description: true if domain can be purchased using CreateDomain API; otherwise, false.
        domainType:
          $ref: "#/components/schemas/DomainType"
          description: 'Valid values are Regular domain: Azure will charge the full price of domain registration, SoftDeleted: Purchasing this domain will simply restore it and this operation will not cost anything. Known values are: "Regular" and "SoftDeleted".'
    DomainType:
      type: string
      enum:
        - Regular
        - SoftDeleted
    TopLevelDomainProperties:
      type: object
      description: TopLevelDomain resource specific properties.
      properties:
        privacy:
          type: boolean
          description: If true, then the top level domain supports domain privacy; otherwise, false.
    TopLevelDomainFlattened:
      type: object
      description: Flattened view of TopLevelDomain ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        kind:
          type: string
          description: Kind of resource.
        privacy:
          type: boolean
          description: If true, then the top level domain supports domain privacy; otherwise, false.
    PagedTopLevelDomainFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TopLevelDomainFlattened"
        nextLink:
          type: string
    TldLegalAgreement:
      type: object
      description: Legal agreement for a top level domain.
      properties:
        agreementKey:
          type: string
          description: Unique identifier for the agreement. Required.
        title:
          type: string
          description: Agreement title. Required.
        content:
          type: string
          description: Agreement details. Required.
        url:
          type: string
          description: URL where a copy of the agreement details is hosted.
      required:
        - agreementKey
        - title
        - content
    PagedTldLegalAgreement:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TldLegalAgreement"
        nextLink:
          type: string
  x-stackQL-resources:
    domain_registration_provider:
      id: azure.domainregistration.domain_registration_provider
      name: domain_registration_provider
      title: domain_registration_provider
      methods:
        list_operations:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.DomainRegistration~1operations?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/domain_registration_provider/methods/list_operations"
        insert: []
        update: []
        delete: []
    domains:
      id: azure.domainregistration.domains
      name: domains
      title: domains
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}?api-version=2024-11-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}?api-version=2024-11-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}?api-version=2024-11-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DomainRegistration~1domains?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_ownership_identifiers:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}~1domainOwnershipIdentifiers?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_recommendations:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DomainRegistration~1listDomainRecommendations?api-version=2024-11-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_ownership_identifier:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}~1domainOwnershipIdentifiers~1{name}?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update_ownership_identifier:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}~1domainOwnershipIdentifiers~1{name}?api-version=2024-11-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update_ownership_identifier:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}~1domainOwnershipIdentifiers~1{name}?api-version=2024-11-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_ownership_identifier:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}~1domainOwnershipIdentifiers~1{name}?api-version=2024-11-01/delete"
          response:
            openAPIDocKey: "204"
        get_control_center_sso_request:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DomainRegistration~1generateSsoRequest?api-version=2024-11-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        renew:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}~1renew?api-version=2024-11-01/post"
          response:
            openAPIDocKey: "200"
        transfer_out:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DomainRegistration~1domains~1{domain_name}~1transferOut?api-version=2024-11-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        check_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DomainRegistration~1checkDomainAvailability?api-version=2024-11-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/domains/methods/get_ownership_identifier"
          - $ref: "#/components/x-stackQL-resources/domains/methods/get"
          - $ref: "#/components/x-stackQL-resources/domains/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/domains/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/domains/methods/create_or_update"
          - $ref: "#/components/x-stackQL-resources/domains/methods/create_or_update_ownership_identifier"
        update:
          - $ref: "#/components/x-stackQL-resources/domains/methods/update_ownership_identifier"
          - $ref: "#/components/x-stackQL-resources/domains/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/domains/methods/create_or_update"
          - $ref: "#/components/x-stackQL-resources/domains/methods/create_or_update_ownership_identifier"
        delete:
          - $ref: "#/components/x-stackQL-resources/domains/methods/delete_ownership_identifier"
          - $ref: "#/components/x-stackQL-resources/domains/methods/delete"
    top_level_domains:
      id: azure.domainregistration.top_level_domains
      name: top_level_domains
      title: top_level_domains
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DomainRegistration~1topLevelDomains~1{name}?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DomainRegistration~1topLevelDomains?api-version=2024-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_agreements:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DomainRegistration~1topLevelDomains~1{name}~1listAgreements?api-version=2024-11-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/top_level_domains/methods/get"
          - $ref: "#/components/x-stackQL-resources/top_level_domains/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /providers/Microsoft.DomainRegistration/operations?api-version=2024-11-01:
    get:
      operationId: DomainRegistrationProvider_list_operations
      description: Implements Csm operations Api to exposes the list of available Csm Apis under the resource provider. Description for Implements Csm operations Api to exposes the list of available Csm Apis under the resource provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCsmOperationDescriptionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DomainRegistration/domains/{domain_name}?api-version=2024-11-01:
    get:
      operationId: Domains_get
      description: Get a domain. Description for Get a domain.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainFlattened"
    put:
      operationId: Domains_begin_create_or_update
      description: Creates or updates a domain. Description for Creates or updates a domain.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/DomainProperties"
                  description: Domain resource specific properties.
                kind:
                  type: string
                  description: Kind of resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Domain"
    patch:
      operationId: Domains_update
      description: Creates or updates a domain. Description for Creates or updates a domain.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                kind:
                  type: string
                  description: Kind of resource.
                properties:
                  $ref: "#/components/schemas/DomainPatchResourceProperties"
                  description: DomainPatchResource resource specific properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Domain"
    delete:
      operationId: Domains_delete
      description: Delete a domain. Description for Delete a domain.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: forceHardDeleteDomain
          in: query
          required: false
          schema:
            type: boolean
          description: Specify true to delete the domain immediately. The default is false which deletes the domain after 24 hours. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.DomainRegistration/domains?api-version=2024-11-01:
    get:
      operationId: Domains_list
      description: Get all domains in a subscription. Description for Get all domains in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDomainFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DomainRegistration/domains?api-version=2024-11-01:
    get:
      operationId: Domains_list_by_resource_group
      description: Get all domains in a resource group. Description for Get all domains in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDomainFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DomainRegistration/domains/{domain_name}/domainOwnershipIdentifiers?api-version=2024-11-01:
    get:
      operationId: Domains_list_ownership_identifiers
      description: Lists domain ownership identifiers. Description for Lists domain ownership identifiers.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDomainOwnershipIdentifierFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.DomainRegistration/listDomainRecommendations?api-version=2024-11-01:
    post:
      operationId: Domains_list_recommendations
      description: Get domain name recommendations based on keywords. Description for Get domain name recommendations based on keywords.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                keywords:
                  type: string
                  description: Keywords to be used for generating domain recommendations.
                maxDomainRecommendations:
                  type: integer
                  description: Maximum number of recommendations.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedNameIdentifier"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DomainRegistration/domains/{domain_name}/domainOwnershipIdentifiers/{name}?api-version=2024-11-01:
    get:
      operationId: Domains_get_ownership_identifier
      description: Get ownership identifier for domain. Description for Get ownership identifier for domain.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of identifier. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainOwnershipIdentifierFlattened"
    put:
      operationId: Domains_create_or_update_ownership_identifier
      description: Creates an ownership identifier for a domain or updates identifier details for an existing identifier. Description for Creates an ownership identifier for a domain or updates identifier details for an existing identifier.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of identifier. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/DomainOwnershipIdentifierProperties"
                  description: DomainOwnershipIdentifier resource specific properties.
                kind:
                  type: string
                  description: Kind of resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainOwnershipIdentifier"
    patch:
      operationId: Domains_update_ownership_identifier
      description: Creates an ownership identifier for a domain or updates identifier details for an existing identifier. Description for Creates an ownership identifier for a domain or updates identifier details for an existing identifier.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of identifier. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/DomainOwnershipIdentifierProperties"
                  description: DomainOwnershipIdentifier resource specific properties.
                kind:
                  type: string
                  description: Kind of resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainOwnershipIdentifier"
    delete:
      operationId: Domains_delete_ownership_identifier
      description: Delete ownership identifier for domain. Description for Delete ownership identifier for domain.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of identifier. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.DomainRegistration/generateSsoRequest?api-version=2024-11-01:
    post:
      operationId: Domains_get_control_center_sso_request
      description: Generate a single sign-on request for the domain management portal. Description for Generate a single sign-on request for the domain management portal.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainControlCenterSsoRequest"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DomainRegistration/domains/{domain_name}/renew?api-version=2024-11-01:
    post:
      operationId: Domains_renew
      description: Renew a domain. Description for Renew a domain.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DomainRegistration/domains/{domain_name}/transferOut?api-version=2024-11-01:
    put:
      operationId: Domains_transfer_out
      description: Transfer out domain to another registrar. Transfer out domain to another registrar.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: domain_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Domain"
  /subscriptions/{subscription_id}/providers/Microsoft.DomainRegistration/checkDomainAvailability?api-version=2024-11-01:
    post:
      operationId: Domains_check_availability
      description: Check if a domain is available for registration. Description for Check if a domain is available for registration.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of the object.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainAvailabilityCheckResult"
  /subscriptions/{subscription_id}/providers/Microsoft.DomainRegistration/topLevelDomains/{name}?api-version=2024-11-01:
    get:
      operationId: TopLevelDomains_get
      description: Get details of a top-level domain. Description for Get details of a top-level domain.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the top-level domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopLevelDomainFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.DomainRegistration/topLevelDomains?api-version=2024-11-01:
    get:
      operationId: TopLevelDomains_list
      description: Get all top-level domains supported for registration. Description for Get all top-level domains supported for registration.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTopLevelDomainFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.DomainRegistration/topLevelDomains/{name}/listAgreements?api-version=2024-11-01:
    post:
      operationId: TopLevelDomains_list_agreements
      description: Gets all legal agreements that user needs to accept before purchasing a domain. Description for Gets all legal agreements that user needs to accept before purchasing a domain.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the top-level domain. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                includePrivacy:
                  type: boolean
                  description: If true, then the list of agreements will include agreements for domain privacy as well; otherwise, false.
                forTransfer:
                  type: boolean
                  description: If true, then the list of agreements will include agreements for domain transfer as well; otherwise, false.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTldLegalAgreement"
