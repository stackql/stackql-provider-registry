openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure DocumentDB (MongoDB compatibility)
  description: MongoDB vCore-compatible clusters, firewall rules, replicas and private endpoints (Microsoft.DocumentDB mongoClusters).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-06-01-stackql-generated
  x-serviceAlias: mongo_cluster
  x-sdkPackage: azure-mgmt-mongocluster
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    FirewallRuleProperties:
      type: object
      description: The properties of a mongo cluster firewall rule.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the firewall rule. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", "Updating", and "Dropping".'
        startIpAddress:
          type: string
          description: The start IP address of the mongo cluster firewall rule. Must be IPv4 format. Required.
        endIpAddress:
          type: string
          description: The end IP address of the mongo cluster firewall rule. Must be IPv4 format. Required.
      required:
        - startIpAddress
        - endIpAddress
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - InProgress
        - Updating
        - Dropping
    FirewallRuleFlattened:
      type: object
      description: Flattened view of FirewallRule ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the firewall rule. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", "Updating", and "Dropping".'
        startIpAddress:
          type: string
          description: The start IP address of the mongo cluster firewall rule. Must be IPv4 format. Required.
        endIpAddress:
          type: string
          description: The end IP address of the mongo cluster firewall rule. Must be IPv4 format. Required.
    PagedFirewallRuleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FirewallRuleFlattened"
        nextLink:
          type: string
    FirewallRule:
      type: object
      description: Represents a mongo cluster firewall rule.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/FirewallRuleProperties"
          description: The resource-specific properties for this resource.
    MongoClusterProperties:
      type: object
      description: The properties of a mongo cluster.
      properties:
        createMode:
          $ref: "#/components/schemas/CreateMode"
          description: 'The mode to create a mongo cluster. Known values are: "Default", "PointInTimeRestore", "GeoReplica", and "Replica".'
        restoreParameters:
          $ref: "#/components/schemas/MongoClusterRestoreParameters"
          description: The parameters to create a point-in-time restore mongo cluster.
        replicaParameters:
          $ref: "#/components/schemas/MongoClusterReplicaParameters"
          description: The parameters to create a replica mongo cluster.
        administrator:
          $ref: "#/components/schemas/AdministratorProperties"
          description: The local administrator properties for the mongo cluster.
        serverVersion:
          type: string
          description: The Mongo DB server version. Defaults to the latest available version if not specified.
        connectionString:
          type: string
          description: The default mongo connection string for the cluster.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the mongo cluster. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", "Updating", and "Dropping".'
        clusterStatus:
          $ref: "#/components/schemas/MongoClusterStatus"
          description: 'The status of the mongo cluster. Known values are: "Ready", "Provisioning", "Updating", "Starting", "Stopping", "Stopped", and "Dropping".'
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: 'Whether or not public endpoint access is allowed for this mongo cluster. Known values are: "Enabled" and "Disabled".'
        highAvailability:
          $ref: "#/components/schemas/HighAvailabilityProperties"
          description: The high availability properties of the mongo cluster.
        storage:
          $ref: "#/components/schemas/StorageProperties"
          description: The storage properties of the mongo cluster.
        sharding:
          $ref: "#/components/schemas/ShardingProperties"
          description: The sharding properties of the mongo cluster.
        compute:
          $ref: "#/components/schemas/ComputeProperties"
          description: The compute properties of the mongo cluster.
        backup:
          $ref: "#/components/schemas/BackupProperties"
          description: The backup properties of the mongo cluster.
        dataApi:
          $ref: "#/components/schemas/DataApiProperties"
          description: The Data API properties of the mongo cluster.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connections.
        previewFeatures:
          type: array
          items:
            $ref: "#/components/schemas/PreviewFeature"
          description: List of private endpoint connections.
        replica:
          $ref: "#/components/schemas/ReplicationProperties"
          description: The replication properties for the mongo cluster.
        infrastructureVersion:
          type: string
          description: The infrastructure version the cluster is provisioned on.
        authConfig:
          $ref: "#/components/schemas/AuthConfigProperties"
          description: The authentication configuration for the cluster.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the cluster. Depends on identity being configured.
        networkBypassMode:
          $ref: "#/components/schemas/NetworkBypassMode"
          description: "The network bypass mode for the cluster. Setting to 'AzureCosmosDB' allows Azure Cosmos DB service to bypass network restrictions. Known values are: \"None\" and \"AzureCosmosDB\"."
    CreateMode:
      type: string
      enum:
        - Default
        - PointInTimeRestore
        - GeoReplica
        - Replica
    MongoClusterRestoreParameters:
      type: object
      description: Parameters used for restore operations.
      properties:
        pointInTimeUTC:
          type: string
          format: date-time
          description: UTC point in time to restore a mongo cluster.
        sourceResourceId:
          type: string
          description: Resource ID to locate the source cluster to restore.
    MongoClusterReplicaParameters:
      type: object
      description: Parameters used for replica operations.
      properties:
        sourceResourceId:
          type: string
          description: The id of the replication source cluster. Required.
        sourceLocation:
          type: string
          description: The location of the source cluster. Required.
      required:
        - sourceResourceId
        - sourceLocation
    AdministratorProperties:
      type: object
      description: The local administrator login properties.
      properties:
        userName:
          type: string
          description: The administrator user name.
        password:
          type: string
          description: The administrator password.
    MongoClusterStatus:
      type: string
      enum:
        - Ready
        - Provisioning
        - Updating
        - Starting
        - Stopping
        - Stopped
        - Dropping
    PublicNetworkAccess:
      type: string
      enum:
        - Enabled
        - Disabled
    HighAvailabilityProperties:
      type: object
      description: The high availability properties of the cluster.
      properties:
        targetMode:
          $ref: "#/components/schemas/HighAvailabilityMode"
          description: 'The target high availability mode requested for the cluster. Known values are: "Disabled", "SameZone", and "ZoneRedundantPreferred".'
    HighAvailabilityMode:
      type: string
      enum:
        - Disabled
        - SameZone
        - ZoneRedundantPreferred
    StorageProperties:
      type: object
      description: The storage properties of the cluster. This includes the data storage size and scaling applied to servers in the cluster.
      properties:
        sizeGb:
          type: integer
          description: The size of the data disk assigned to each server.
        type:
          $ref: "#/components/schemas/StorageType"
          description: 'The type of storage to provision the cluster servers with. Known values are: "PremiumSSD" and "PremiumSSDv2".'
    StorageType:
      type: string
      enum:
        - PremiumSSD
        - PremiumSSDv2
    ShardingProperties:
      type: object
      description: The sharding properties of the cluster. This includes the shard count and scaling options for the cluster.
      properties:
        shardCount:
          type: integer
          description: Number of shards to provision on the cluster.
    ComputeProperties:
      type: object
      description: The compute properties of the cluster. This includes the virtual-cores/memory and scaling options applied to servers in the cluster.
      properties:
        tier:
          type: string
          description: "The compute tier to assign to the cluster, where each tier maps to a virtual-core and memory size. Example values: 'M30', 'M40'."
    BackupProperties:
      type: object
      description: The backup properties of the cluster. This includes the earliest restore time and retention settings.
      properties:
        earliestRestoreTime:
          type: string
          description: Earliest restore timestamp in UTC ISO8601 format.
    DataApiProperties:
      type: object
      description: Data API properties.
      properties:
        mode:
          $ref: "#/components/schemas/DataApiMode"
          description: 'The mode to indicate whether the Mongo Data API is enabled for a cluster. Known values are: "Enabled" and "Disabled".'
    DataApiMode:
      type: string
      enum:
        - Enabled
        - Disabled
    PrivateEndpointConnection:
      type: object
      description: The private endpoint connection resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
          description: The private endpoint connection properties.
    PrivateEndpointConnectionProperties:
      type: object
      description: Properties of the private endpoint connection.
      properties:
        groupIds:
          type: array
          items:
            type: string
          description: The group ids for the private endpoint resource.
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider. Required.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
      required:
        - privateLinkServiceConnectionState
    PrivateEndpoint:
      type: object
      description: The private endpoint resource.
      properties:
        id:
          type: string
          description: The resource identifier of the private endpoint.
    PrivateLinkServiceConnectionState:
      type: object
      description: A collection of information about the state of the connection between service consumer and provider.
      properties:
        status:
          $ref: "#/components/schemas/PrivateEndpointServiceConnectionStatus"
          description: 'Indicates whether the connection has been Approved/Rejected/Removed by the owner of the service. Known values are: "Pending", "Approved", and "Rejected".'
        description:
          type: string
          description: The reason for approval/rejection of the connection.
        actionsRequired:
          type: string
          description: A message indicating if changes on the service provider require any updates on the consumer.
    PrivateEndpointServiceConnectionStatus:
      type: string
      enum:
        - Pending
        - Approved
        - Rejected
    PrivateEndpointConnectionProvisioningState:
      type: string
      enum:
        - Succeeded
        - Creating
        - Deleting
        - Failed
    PreviewFeature:
      type: string
      enum:
        - GeoReplicas
    ReplicationProperties:
      type: object
      description: Replica properties of the mongo cluster.
      properties:
        sourceResourceId:
          type: string
          description: The resource id the source cluster for the replica cluster.
        role:
          $ref: "#/components/schemas/ReplicationRole"
          description: 'The replication role of the cluster. Known values are: "Primary", "AsyncReplica", and "GeoAsyncReplica".'
        replicationState:
          $ref: "#/components/schemas/ReplicationState"
          description: 'The replication link state of the replica cluster. Known values are: "Active", "Catchup", "Provisioning", "Updating", "Broken", and "Reconfiguring".'
    ReplicationRole:
      type: string
      enum:
        - Primary
        - AsyncReplica
        - GeoAsyncReplica
    ReplicationState:
      type: string
      enum:
        - Active
        - Catchup
        - Provisioning
        - Updating
        - Broken
        - Reconfiguring
    AuthConfigProperties:
      type: object
      description: The authentication configuration for the Mongo cluster.
      properties:
        allowedModes:
          type: array
          items:
            $ref: "#/components/schemas/AuthenticationMode"
          description: Allowed authentication modes for data access on the cluster.
    AuthenticationMode:
      type: string
      enum:
        - NativeAuth
        - MicrosoftEntraID
    EncryptionProperties:
      type: object
      description: The encryption configuration for the mongo cluster.
      properties:
        customerManagedKeyEncryption:
          $ref: "#/components/schemas/CustomerManagedKeyEncryptionProperties"
          description: Customer managed key encryption settings.
    CustomerManagedKeyEncryptionProperties:
      type: object
      description: Customer managed key encryption settings.
      properties:
        keyEncryptionKeyIdentity:
          $ref: "#/components/schemas/KeyEncryptionKeyIdentity"
          description: The identity used to access the key encryption key.
        keyEncryptionKeyUrl:
          type: string
          description: The URI of the key vault key used for encryption.
    KeyEncryptionKeyIdentity:
      type: object
      description: The identity used for key encryption key.
      properties:
        identityType:
          $ref: "#/components/schemas/KeyEncryptionKeyIdentityType"
          description: The type of identity. Only 'UserAssignedIdentity' is supported. "UserAssignedIdentity"
        userAssignedIdentityResourceId:
          type: string
          description: The user assigned identity resource id.
    KeyEncryptionKeyIdentityType:
      type: string
      enum:
        - UserAssignedIdentity
    NetworkBypassMode:
      type: string
      enum:
        - None
        - AzureCosmosDB
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    MongoClusterFlattened:
      type: object
      description: Flattened view of MongoCluster ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        createMode:
          $ref: "#/components/schemas/CreateMode"
          description: 'The mode to create a mongo cluster. Known values are: "Default", "PointInTimeRestore", "GeoReplica", and "Replica".'
        restoreParameters:
          $ref: "#/components/schemas/MongoClusterRestoreParameters"
          description: The parameters to create a point-in-time restore mongo cluster.
        replicaParameters:
          $ref: "#/components/schemas/MongoClusterReplicaParameters"
          description: The parameters to create a replica mongo cluster.
        administrator:
          $ref: "#/components/schemas/AdministratorProperties"
          description: The local administrator properties for the mongo cluster.
        serverVersion:
          type: string
          description: The Mongo DB server version. Defaults to the latest available version if not specified.
        connectionString:
          type: string
          description: The default mongo connection string for the cluster.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the mongo cluster. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", "Updating", and "Dropping".'
        clusterStatus:
          $ref: "#/components/schemas/MongoClusterStatus"
          description: 'The status of the mongo cluster. Known values are: "Ready", "Provisioning", "Updating", "Starting", "Stopping", "Stopped", and "Dropping".'
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: 'Whether or not public endpoint access is allowed for this mongo cluster. Known values are: "Enabled" and "Disabled".'
        highAvailability:
          $ref: "#/components/schemas/HighAvailabilityProperties"
          description: The high availability properties of the mongo cluster.
        storage:
          $ref: "#/components/schemas/StorageProperties"
          description: The storage properties of the mongo cluster.
        sharding:
          $ref: "#/components/schemas/ShardingProperties"
          description: The sharding properties of the mongo cluster.
        compute:
          $ref: "#/components/schemas/ComputeProperties"
          description: The compute properties of the mongo cluster.
        backup:
          $ref: "#/components/schemas/BackupProperties"
          description: The backup properties of the mongo cluster.
        dataApi:
          $ref: "#/components/schemas/DataApiProperties"
          description: The Data API properties of the mongo cluster.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connections.
        previewFeatures:
          type: array
          items:
            $ref: "#/components/schemas/PreviewFeature"
          description: List of private endpoint connections.
        replica:
          $ref: "#/components/schemas/ReplicationProperties"
          description: The replication properties for the mongo cluster.
        infrastructureVersion:
          type: string
          description: The infrastructure version the cluster is provisioned on.
        authConfig:
          $ref: "#/components/schemas/AuthConfigProperties"
          description: The authentication configuration for the cluster.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the cluster. Depends on identity being configured.
        networkBypassMode:
          $ref: "#/components/schemas/NetworkBypassMode"
          description: "The network bypass mode for the cluster. Setting to 'AzureCosmosDB' allows Azure Cosmos DB service to bypass network restrictions. Known values are: \"None\" and \"AzureCosmosDB\"."
    PagedMongoClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MongoClusterFlattened"
        nextLink:
          type: string
    MongoCluster:
      type: object
      description: Represents a mongo cluster resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/MongoClusterProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    ConnectionString:
      type: object
      description: Connection string for the mongo cluster.
      properties:
        connectionString:
          type: string
          description: Value of the connection string.
        description:
          type: string
          description: Description of the connection string.
        name:
          type: string
          description: Name of the connection string.
    ListConnectionStringsResult:
      type: object
      description: The connection strings for the given mongo cluster.
      properties:
        connectionStrings:
          type: array
          items:
            $ref: "#/components/schemas/ConnectionString"
          description: An array that contains the connection strings for a mongo cluster.
    MongoClusterUpdateProperties:
      type: object
      description: The updatable properties of the MongoCluster.
      properties:
        administrator:
          $ref: "#/components/schemas/AdministratorProperties"
          description: The local administrator properties for the mongo cluster.
        serverVersion:
          type: string
          description: The Mongo DB server version. Defaults to the latest available version if not specified.
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: 'Whether or not public endpoint access is allowed for this mongo cluster. Known values are: "Enabled" and "Disabled".'
        highAvailability:
          $ref: "#/components/schemas/HighAvailabilityProperties"
          description: The high availability properties of the mongo cluster.
        storage:
          $ref: "#/components/schemas/StorageProperties"
          description: The storage properties of the mongo cluster.
        sharding:
          $ref: "#/components/schemas/ShardingProperties"
          description: The sharding properties of the mongo cluster.
        compute:
          $ref: "#/components/schemas/ComputeProperties"
          description: The compute properties of the mongo cluster.
        backup:
          $ref: "#/components/schemas/BackupProperties"
          description: The backup properties of the mongo cluster.
        dataApi:
          $ref: "#/components/schemas/DataApiProperties"
          description: The Data API properties of the mongo cluster.
        previewFeatures:
          type: array
          items:
            $ref: "#/components/schemas/PreviewFeature"
          description: List of private endpoint connections.
        authConfig:
          $ref: "#/components/schemas/AuthConfigProperties"
          description: The authentication configuration for the cluster.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the cluster. Depends on identity being configured.
        networkBypassMode:
          $ref: "#/components/schemas/NetworkBypassMode"
          description: "The network bypass mode for the cluster. Setting to 'AzureCosmosDB' allows Azure Cosmos DB service to bypass network restrictions. Known values are: \"None\" and \"AzureCosmosDB\"."
    CheckNameAvailabilityReason:
      type: string
      enum:
        - Invalid
        - AlreadyExists
    CheckNameAvailabilityResponse:
      type: object
      description: The check availability result.
      properties:
        nameAvailable:
          type: boolean
          description: Indicates if the resource name is available.
        reason:
          $ref: "#/components/schemas/CheckNameAvailabilityReason"
          description: 'The reason why the given name is not available. Known values are: "Invalid" and "AlreadyExists".'
        message:
          type: string
          description: Detailed reason why the given name is not available.
    PromoteOption:
      type: string
      enum:
        - Forced
    PromoteMode:
      type: string
      enum:
        - Switchover
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    PrivateEndpointConnectionResourceFlattened:
      type: object
      description: Flattened view of PrivateEndpointConnectionResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        groupIds:
          type: array
          items:
            type: string
          description: The group ids for the private endpoint resource.
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider. Required.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
    PagedPrivateEndpointConnectionResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnectionResourceFlattened"
        nextLink:
          type: string
    PrivateEndpointConnectionResource:
      type: object
      description: Concrete proxy resource types can be created by aliasing this type using a specific property type.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
          description: The resource-specific properties for this resource.
    PrivateLinkResourceProperties:
      type: object
      description: Properties of a private link resource.
      properties:
        groupId:
          type: string
          description: The private link resource group id.
        requiredMembers:
          type: array
          items:
            type: string
          description: The private link resource required member names.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The private link resource private link DNS zone name.
    PrivateLinkResourceFlattened:
      type: object
      description: Flattened view of PrivateLinkResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        groupId:
          type: string
          description: The private link resource group id.
        requiredMembers:
          type: array
          items:
            type: string
          description: The private link resource required member names.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The private link resource private link DNS zone name.
    PagedPrivateLinkResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateLinkResourceFlattened"
        nextLink:
          type: string
    ReplicaFlattened:
      type: object
      description: Flattened view of Replica ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        createMode:
          $ref: "#/components/schemas/CreateMode"
          description: 'The mode to create a mongo cluster. Known values are: "Default", "PointInTimeRestore", "GeoReplica", and "Replica".'
        restoreParameters:
          $ref: "#/components/schemas/MongoClusterRestoreParameters"
          description: The parameters to create a point-in-time restore mongo cluster.
        replicaParameters:
          $ref: "#/components/schemas/MongoClusterReplicaParameters"
          description: The parameters to create a replica mongo cluster.
        administrator:
          $ref: "#/components/schemas/AdministratorProperties"
          description: The local administrator properties for the mongo cluster.
        serverVersion:
          type: string
          description: The Mongo DB server version. Defaults to the latest available version if not specified.
        connectionString:
          type: string
          description: The default mongo connection string for the cluster.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the mongo cluster. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", "Updating", and "Dropping".'
        clusterStatus:
          $ref: "#/components/schemas/MongoClusterStatus"
          description: 'The status of the mongo cluster. Known values are: "Ready", "Provisioning", "Updating", "Starting", "Stopping", "Stopped", and "Dropping".'
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccess"
          description: 'Whether or not public endpoint access is allowed for this mongo cluster. Known values are: "Enabled" and "Disabled".'
        highAvailability:
          $ref: "#/components/schemas/HighAvailabilityProperties"
          description: The high availability properties of the mongo cluster.
        storage:
          $ref: "#/components/schemas/StorageProperties"
          description: The storage properties of the mongo cluster.
        sharding:
          $ref: "#/components/schemas/ShardingProperties"
          description: The sharding properties of the mongo cluster.
        compute:
          $ref: "#/components/schemas/ComputeProperties"
          description: The compute properties of the mongo cluster.
        backup:
          $ref: "#/components/schemas/BackupProperties"
          description: The backup properties of the mongo cluster.
        dataApi:
          $ref: "#/components/schemas/DataApiProperties"
          description: The Data API properties of the mongo cluster.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connections.
        previewFeatures:
          type: array
          items:
            $ref: "#/components/schemas/PreviewFeature"
          description: List of private endpoint connections.
        replica:
          $ref: "#/components/schemas/ReplicationProperties"
          description: The replication properties for the mongo cluster.
        infrastructureVersion:
          type: string
          description: The infrastructure version the cluster is provisioned on.
        authConfig:
          $ref: "#/components/schemas/AuthConfigProperties"
          description: The authentication configuration for the cluster.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the cluster. Depends on identity being configured.
        networkBypassMode:
          $ref: "#/components/schemas/NetworkBypassMode"
          description: "The network bypass mode for the cluster. Setting to 'AzureCosmosDB' allows Azure Cosmos DB service to bypass network restrictions. Known values are: \"None\" and \"AzureCosmosDB\"."
    PagedReplicaFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ReplicaFlattened"
        nextLink:
          type: string
    UserProperties:
      type: object
      description: Definition of Mongo user resource on a cluster.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the user. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", "Updating", and "Dropping".'
        identityProvider:
          $ref: "#/components/schemas/IdentityProvider"
          description: The user's identity provider definition.
        roles:
          type: array
          items:
            $ref: "#/components/schemas/DatabaseRole"
          description: Database roles that are assigned to the user.
    IdentityProvider:
      type: object
      description: "Defines a user's identity provider definition. You probably want to use the sub-classes and not this class directly. Known sub-classes are: EntraIdentityProvider"
      properties:
        type:
          type: string
          description: The type of identity provider that the user belongs to. Required. "MicrosoftEntraID"
      required:
        - type
    DatabaseRole:
      type: object
      description: Database role definition that is assigned to a user.
      properties:
        db:
          type: string
          description: Database scope that the role is assigned to. Required.
        role:
          $ref: "#/components/schemas/UserRole"
          description: The role that is assigned to the user on the database scope. Required. "root"
      required:
        - db
        - role
    UserRole:
      type: string
      enum:
        - root
    UserFlattened:
      type: object
      description: Flattened view of User ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the user. Known values are: "Succeeded", "Failed", "Canceled", "InProgress", "Updating", and "Dropping".'
        identityProvider:
          $ref: "#/components/schemas/IdentityProvider"
          description: The user's identity provider definition.
        roles:
          type: array
          items:
            $ref: "#/components/schemas/DatabaseRole"
          description: Database roles that are assigned to the user.
    PagedUserFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/UserFlattened"
        nextLink:
          type: string
    User:
      type: object
      description: Represents a Mongo cluster user.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/UserProperties"
          description: The resource-specific properties for this resource.
  x-stackQL-resources:
    firewall_rules:
      id: azure.mongo_cluster.firewall_rules
      name: firewall_rules
      title: firewall_rules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1firewallRules~1{firewall_rule_name}?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1firewallRules~1{firewall_rule_name}?api-version=2026-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1firewallRules~1{firewall_rule_name}?api-version=2026-06-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_mongo_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1firewallRules?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/get"
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/list_by_mongo_cluster"
        insert:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/delete"
    mongo_clusters:
      id: azure.mongo_cluster.mongo_clusters
      name: mongo_clusters
      title: mongo_clusters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}?api-version=2026-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}?api-version=2026-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}?api-version=2026-06-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DocumentDB~1mongoClusters?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_connection_strings:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1listConnectionStrings?api-version=2026-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DocumentDB~1locations~1{location}~1checkMongoClusterNameAvailability?api-version=2026-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        promote:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1promote?api-version=2026-06-01/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/get"
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/check_name_availability"
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/mongo_clusters/methods/delete"
    operations:
      id: azure.mongo_cluster.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.DocumentDB~1operations?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    private_endpoint_connections:
      id: azure.mongo_cluster.private_endpoint_connections
      name: private_endpoint_connections
      title: private_endpoint_connections
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2026-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2026-06-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_mongo_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1privateEndpointConnections?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/list_by_mongo_cluster"
        insert:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/delete"
    private_links:
      id: azure.mongo_cluster.private_links
      name: private_links
      title: private_links
      methods:
        list_by_mongo_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1privateLinkResources?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_links/methods/list_by_mongo_cluster"
        insert: []
        update: []
        delete: []
    replicas:
      id: azure.mongo_cluster.replicas
      name: replicas
      title: replicas
      methods:
        list_by_parent:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1replicas?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/replicas/methods/list_by_parent"
        insert: []
        update: []
        delete: []
    users:
      id: azure.mongo_cluster.users
      name: users
      title: users
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1users~1{user_name}?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1users~1{user_name}?api-version=2026-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1users~1{user_name}?api-version=2026-06-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_mongo_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DocumentDB~1mongoClusters~1{mongo_cluster_name}~1users?api-version=2026-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/users/methods/get"
          - $ref: "#/components/x-stackQL-resources/users/methods/list_by_mongo_cluster"
        insert:
          - $ref: "#/components/x-stackQL-resources/users/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/users/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/users/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/firewallRules/{firewall_rule_name}?api-version=2026-06-01:
    get:
      operationId: FirewallRules_get
      description: Gets information about a mongo cluster firewall rule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: firewall_rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FirewallRuleFlattened"
    put:
      operationId: FirewallRules_begin_create_or_update
      description: Creates a new firewall rule or updates an existing firewall rule on a mongo cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: firewall_rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/FirewallRuleProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FirewallRule"
    delete:
      operationId: FirewallRules_begin_delete
      description: Deletes a mongo cluster firewall rule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: firewall_rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/firewallRules?api-version=2026-06-01:
    get:
      operationId: FirewallRules_list_by_mongo_cluster
      description: List all the firewall rules in a given mongo cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFirewallRuleFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}?api-version=2026-06-01:
    get:
      operationId: MongoClusters_get
      description: Gets information about a mongo cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MongoClusterFlattened"
    put:
      operationId: MongoClusters_begin_create_or_update
      description: Create or update a mongo cluster. Update overwrites all properties for the resource. To only modify some of the properties, use PATCH.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/MongoClusterProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MongoCluster"
    patch:
      operationId: MongoClusters_begin_update
      description: Updates an existing mongo cluster. The request body can contain one to many of the properties present in the normal mongo cluster definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/MongoClusterUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MongoCluster"
    delete:
      operationId: MongoClusters_begin_delete
      description: Deletes a mongo cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.DocumentDB/mongoClusters?api-version=2026-06-01:
    get:
      operationId: MongoClusters_list
      description: List all the mongo clusters in a given subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMongoClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters?api-version=2026-06-01:
    get:
      operationId: MongoClusters_list_by_resource_group
      description: List all the mongo clusters in a given resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMongoClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/listConnectionStrings?api-version=2026-06-01:
    post:
      operationId: MongoClusters_list_connection_strings
      description: List mongo cluster connection strings. This includes the default connection string using SCRAM-SHA-256, as well as other connection strings supported by the cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListConnectionStringsResult"
  /subscriptions/{subscription_id}/providers/Microsoft.DocumentDB/locations/{location}/checkMongoClusterNameAvailability?api-version=2026-06-01:
    post:
      operationId: MongoClusters_check_name_availability
      description: Check if mongo cluster name is available for use.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The name of the resource for which availability needs to be checked.
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/promote?api-version=2026-06-01:
    post:
      operationId: MongoClusters_begin_promote
      description: Promotes a replica mongo cluster to a primary role.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                promoteOption:
                  $ref: "#/components/schemas/PromoteOption"
                  description: The promote option to apply to the operation. Required. "Forced"
                mode:
                  $ref: "#/components/schemas/PromoteMode"
                  description: The mode to apply to the promote operation. Value is optional and default value is 'Switchover'. "Switchover"
              required:
                - promoteOption
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.DocumentDB/operations?api-version=2026-06-01:
    get:
      operationId: mongocluster_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/privateEndpointConnections/{private_endpoint_connection_name}?api-version=2026-06-01:
    get:
      operationId: PrivateEndpointConnections_get
      description: Get a specific private connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the Azure resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnectionResourceFlattened"
    put:
      operationId: PrivateEndpointConnections_begin_create
      description: Create a Private endpoint connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the Azure resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnectionResource"
    delete:
      operationId: PrivateEndpointConnections_begin_delete
      description: Delete the private endpoint connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the Azure resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/privateEndpointConnections?api-version=2026-06-01:
    get:
      operationId: PrivateEndpointConnections_list_by_mongo_cluster
      description: List existing private connections.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateEndpointConnectionResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/privateLinkResources?api-version=2026-06-01:
    get:
      operationId: PrivateLinks_list_by_mongo_cluster
      description: list private links on the given resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateLinkResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/replicas?api-version=2026-06-01:
    get:
      operationId: Replicas_list_by_parent
      description: List all the replicas for the mongo cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedReplicaFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/users/{user_name}?api-version=2026-06-01:
    get:
      operationId: Users_get
      description: Gets the defintion of a Mongo cluster user.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: user_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster user. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserFlattened"
    put:
      operationId: Users_begin_create_or_update
      description: Creates a new user or updates an existing user on a mongo cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: user_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster user. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/UserProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
    delete:
      operationId: Users_begin_delete
      description: Deletes a mongo cluster user.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: user_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster user. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DocumentDB/mongoClusters/{mongo_cluster_name}/users?api-version=2026-06-01:
    get:
      operationId: Users_list_by_mongo_cluster
      description: List all the users on a mongo cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: mongo_cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the mongo cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedUserFlattened"
