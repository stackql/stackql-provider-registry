openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: BareMetal Infrastructure
  description: Manage BareMetal Instances for large in-memory workloads.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-08-09-stackql-generated
  x-serviceAlias: bare_metal_infrastructure
  x-sdkPackage: azure-mgmt-baremetalinfrastructure
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    HardwareProfile:
      type: object
      description: Specifies the hardware settings for the AzureBareMetal instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        hardwareType:
          type: string
          description: 'Name of the hardware type (vendor and/or their product name). Known values are: "Cisco_UCS" and "HPE".'
        azureBareMetalInstanceSize:
          type: string
          description: 'Specifies the AzureBareMetal instance SKU. Known values are: "S72m", "S144m", "S72", "S144", "S192", "S192m", "S192xm", "S96", "S112", "S224", "S224m", "S224om", "S224oo", "S224oom", "S224ooo", "S384", "S384m", "S384xm", "S384xxm", "S448", "S448m", "S448om", "S448oo", "S448oom", "S448ooo", "S576m", "S576xm", "S672", "S672m", "S672om", "S672oo", "S672oom", "S672ooo", "S768", "S768m", "S768xm", "S896", "S896m", "S896om", "S896oo", "S896oom", "S896ooo", and "S960m".'
    StorageProfile:
      type: object
      description: Specifies the storage settings for the AzureBareMetal instance disks. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        nfsIpAddress:
          type: string
          description: IP Address to connect to storage.
        osDisks:
          type: array
          items:
            $ref: "#/components/schemas/Disk"
          description: Specifies information about the operating system disk used by baremetal instance.
    Disk:
      type: object
      description: Specifies the disk information fo the AzureBareMetal instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The disk name.
        diskSizeGB:
          type: integer
          description: Specifies the size of an empty data disk in gigabytes.
        lun:
          type: integer
          description: Specifies the logical unit number of the data disk. This value is used to identify data disks within the VM and therefore must be unique for each data disk attached to a VM.
    OSProfile:
      type: object
      description: Specifies the operating system settings for the AzureBareMetal instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        computerName:
          type: string
          description: Specifies the host OS name of the AzureBareMetal instance.
        osType:
          type: string
          description: This property allows you to specify the type of the OS.
        version:
          type: string
          description: Specifies version of operating system.
        sshPublicKey:
          type: string
          description: Specifies the SSH public key used to access the operating system.
    NetworkProfile:
      type: object
      description: Specifies the network settings for the AzureBareMetal instance disks. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/IpAddress"
          description: Specifies the network interfaces for the AzureBareMetal instance.
        circuitId:
          type: string
          description: Specifies the circuit id for connecting to express route.
    IpAddress:
      type: object
      description: Specifies the IP address of the network interface.
      properties:
        ipAddress:
          type: string
          description: Specifies the IP address of the network interface.
    AzureBareMetalInstanceFlattened:
      type: object
      description: Flattened view of AzureBareMetalInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system metadata relating to this resource.
        hardwareProfile:
          $ref: "#/components/schemas/HardwareProfile"
          description: Specifies the hardware settings for the AzureBareMetal instance.
        storageProfile:
          $ref: "#/components/schemas/StorageProfile"
          description: Specifies the storage settings for the AzureBareMetal instance disks.
        osProfile:
          $ref: "#/components/schemas/OSProfile"
          description: Specifies the operating system settings for the AzureBareMetal instance.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Specifies the network settings for the AzureBareMetal instance.
        azureBareMetalInstanceId:
          type: string
          description: Specifies the AzureBareMetal instance unique ID.
        powerState:
          type: string
          description: 'Resource power state. Known values are: "starting", "started", "stopping", "stopped", "restarting", and "unknown".'
        proximityPlacementGroup:
          type: string
          description: Resource proximity placement group.
        hwRevision:
          type: string
          description: Hardware revision of an AzureBareMetal instance.
        partnerNodeId:
          type: string
          description: ARM ID of another AzureBareMetalInstance that will share a network with this AzureBareMetalInstance.
        provisioningState:
          type: string
          description: 'State of provisioning of the AzureBareMetalInstance. Known values are: "Accepted", "Creating", "Updating", "Failed", "Succeeded", "Deleting", and "Migrating".'
    PagedAzureBareMetalInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AzureBareMetalInstanceFlattened"
        nextLink:
          type: string
    AzureBareMetalInstance:
      type: object
      description: AzureBareMetal instance info on Azure (ARM properties and AzureBareMetal properties). Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system metadata relating to this resource.
        properties:
          type: object
          properties:
            hardwareProfile:
              $ref: "#/components/schemas/HardwareProfile"
              description: Specifies the hardware settings for the AzureBareMetal instance.
            storageProfile:
              $ref: "#/components/schemas/StorageProfile"
              description: Specifies the storage settings for the AzureBareMetal instance disks.
            osProfile:
              $ref: "#/components/schemas/OSProfile"
              description: Specifies the operating system settings for the AzureBareMetal instance.
            networkProfile:
              $ref: "#/components/schemas/NetworkProfile"
              description: Specifies the network settings for the AzureBareMetal instance.
            azureBareMetalInstanceId:
              type: string
              description: Specifies the AzureBareMetal instance unique ID.
            powerState:
              type: string
              description: 'Resource power state. Known values are: "starting", "started", "stopping", "stopped", "restarting", and "unknown".'
            proximityPlacementGroup:
              type: string
              description: Resource proximity placement group.
            hwRevision:
              type: string
              description: Hardware revision of an AzureBareMetal instance.
            partnerNodeId:
              type: string
              description: ARM ID of another AzureBareMetalInstance that will share a network with this AzureBareMetalInstance.
            provisioningState:
              type: string
              description: 'State of provisioning of the AzureBareMetalInstance. Known values are: "Accepted", "Creating", "Updating", "Failed", "Succeeded", "Deleting", and "Migrating".'
      required:
        - location
    Display:
      type: object
      description: Detailed BareMetal operation information. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name.
        resource:
          type: string
          description: The localized friendly form of the resource type related to this action/operation.
        operation:
          type: string
          description: The localized friendly name for the operation as shown to the user.
        description:
          type: string
          description: The localized friendly description for the operation as shown to the user.
    Operation:
      type: object
      description: AzureBareMetal operation information. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The name of the operation being performed on this particular object. This name should match the action name that appears in RBAC / the event service.
        display:
          $ref: "#/components/schemas/Display"
          description: Displayed AzureBareMetal operation information.
        isDataAction:
          type: boolean
          description: indicates whether an operation is a data action or not.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    azure_bare_metal_instances:
      id: azure.bare_metal_infrastructure.azure_bare_metal_instances
      name: azure_bare_metal_instances
      title: azure_bare_metal_instances
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.BareMetalInfrastructure~1bareMetalInstances~1{azure_bare_metal_instance_name}?api-version=2021-08-09/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.BareMetalInfrastructure~1bareMetalInstances~1{azure_bare_metal_instance_name}?api-version=2021-08-09/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.BareMetalInfrastructure~1bareMetalInstances?api-version=2021-08-09/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.BareMetalInfrastructure~1bareMetalInstances?api-version=2021-08-09/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/azure_bare_metal_instances/methods/get"
          - $ref: "#/components/x-stackQL-resources/azure_bare_metal_instances/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/azure_bare_metal_instances/methods/list_by_subscription"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/azure_bare_metal_instances/methods/update"
        delete: []
    operations:
      id: azure.bare_metal_infrastructure.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.BareMetalInfrastructure~1operations?api-version=2021-08-09/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.BareMetalInfrastructure/bareMetalInstances/{azure_bare_metal_instance_name}?api-version=2021-08-09:
    get:
      operationId: AzureBareMetalInstances_get
      description: Gets an Azure BareMetal instance. Gets an Azure BareMetal instance for the specified subscription, resource group, and instance name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_bare_metal_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Azure BareMetal on Azure instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureBareMetalInstanceFlattened"
    patch:
      operationId: AzureBareMetalInstances_update
      description: Patches the Tags field of a Azure BareMetal instance. Patches the Tags field of a Azure BareMetal instance for the specified subscription, resource group, and instance name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_bare_metal_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Azure BareMetal on Azure instance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Tags field of the AzureBareMetal instance.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureBareMetalInstance"
  /subscriptions/{subscription_id}/providers/Microsoft.BareMetalInfrastructure/bareMetalInstances?api-version=2021-08-09:
    get:
      operationId: AzureBareMetalInstances_list_by_subscription
      description: Gets a list of Azure BareMetal instances in the specified subscription. Gets a list of AzureBareMetal instances in the specified subscription. The operations returns various properties of each Azure BareMetal instance.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureBareMetalInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.BareMetalInfrastructure/bareMetalInstances?api-version=2021-08-09:
    get:
      operationId: AzureBareMetalInstances_list_by_resource_group
      description: Gets a list of Azure BareMetal instances in the specified subscription and resource group. Gets a list of AzureBareMetal instances in the specified subscription and resource group. The operations returns various properties of each Azure BareMetal instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureBareMetalInstanceFlattened"
  /providers/Microsoft.BareMetalInfrastructure/operations?api-version=2021-08-09:
    get:
      operationId: baremetalinfrastructure_list
      description: Gets a list of AzureBareMetal management operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
