openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Impact Reporting Mgmt Client
  description: ImpactReportingMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-05-01-preview-stackql-generated
  x-serviceAlias: impactreporting
  x-sdkPackage: azure-mgmt-impactreporting
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    ConnectorProperties:
      type: object
      description: Details of the Connector. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        connectorId:
          type: string
          description: unique id of the connector. Required.
        tenantId:
          type: string
          description: tenant id of this connector. Required.
        connectorType:
          $ref: "#/components/schemas/Platform"
          description: connector type. Required. "AzureMonitor"
        lastRunTimeStamp:
          type: string
          format: date-time
          description: last run time stamp of this connector in UTC time zone. Required.
      required:
        - connectorType
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
    Platform:
      type: string
      enum:
        - AzureMonitor
    ConnectorFlattened:
      type: object
      description: Flattened view of Connector ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        connectorId:
          type: string
          description: unique id of the connector. Required.
        tenantId:
          type: string
          description: tenant id of this connector. Required.
        connectorType:
          $ref: "#/components/schemas/Platform"
          description: connector type. Required. "AzureMonitor"
        lastRunTimeStamp:
          type: string
          format: date-time
          description: last run time stamp of this connector in UTC time zone. Required.
    PagedConnectorFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ConnectorFlattened"
        nextLink:
          type: string
    Connector:
      type: object
      description: A connector is a resource that can be used to proactively report impacts against workloads in Azure to Microsoft. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ConnectorProperties"
          description: The resource-specific properties for this resource.
    ConnectorUpdateProperties:
      type: object
      description: The updatable properties of the Connector.
      properties:
        connectorType:
          $ref: "#/components/schemas/Platform"
          description: connector type. "AzureMonitor"
    ImpactCategoryProperties:
      type: object
      description: Impact category properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        categoryId:
          type: string
          description: Unique ID of the category. Required.
        parentCategoryId:
          type: string
          description: Unique ID of the parent category.
        description:
          type: string
          description: Description of the category.
        requiredImpactProperties:
          type: array
          items:
            $ref: "#/components/schemas/RequiredImpactProperties"
          description: The workloadImpact properties which are required when reporting with the impact category.
      required:
        - categoryId
    RequiredImpactProperties:
      type: object
      description: Required impact properties.
      properties:
        name:
          type: string
          description: Name of the property. Required.
        allowedValues:
          type: array
          items:
            type: string
          description: Allowed values values for the property.
      required:
        - name
    ImpactCategoryFlattened:
      type: object
      description: Flattened view of ImpactCategory ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        categoryId:
          type: string
          description: Unique ID of the category. Required.
        parentCategoryId:
          type: string
          description: Unique ID of the parent category.
        description:
          type: string
          description: Description of the category.
        requiredImpactProperties:
          type: array
          items:
            $ref: "#/components/schemas/RequiredImpactProperties"
          description: The workloadImpact properties which are required when reporting with the impact category.
    PagedImpactCategoryFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ImpactCategoryFlattened"
        nextLink:
          type: string
    InsightProperties:
      type: object
      description: Impact category properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        category:
          type: string
          description: category of the insight. Required.
        status:
          type: string
          description: status of the insight. example resolved, repaired, other.
        eventId:
          type: string
          description: Identifier of the event that has been correlated with this insight. This can be used to aggregate insights for the same event.
        groupId:
          type: string
          description: Identifier that can be used to group similar insights.
        content:
          $ref: "#/components/schemas/Content"
          description: Contains title & description for the insight. Required.
        eventTime:
          type: string
          format: date-time
          description: Time of the event, which has been correlated the impact.
        insightUniqueId:
          type: string
          description: unique id of the insight. Required.
        impact:
          $ref: "#/components/schemas/ImpactDetails"
          description: details of of the impact for which insight has been generated. Required.
        additionalDetails:
          type: object
          properties: {}
          description: additional details of the insight.
      required:
        - category
        - content
        - insightUniqueId
        - impact
    Content:
      type: object
      description: Article details of the insight like title, description etc.
      properties:
        title:
          type: string
          description: Title of the insight. Required.
        description:
          type: string
          description: Description of the insight. Required.
      required:
        - title
        - description
    ImpactDetails:
      type: object
      description: details of of the impact for which insight has been generated.
      properties:
        impactedResourceId:
          type: string
          description: List of impacted Azure resources. Required.
        startTime:
          type: string
          format: date-time
          description: Time at which impact was started according to reported impact. Required.
        endTime:
          type: string
          format: date-time
          description: Time at which impact was ended according to reported impact.
        impactId:
          type: string
          description: Azure Id of the impact. Required.
      required:
        - impactedResourceId
        - startTime
        - impactId
    InsightFlattened:
      type: object
      description: Flattened view of Insight ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        category:
          type: string
          description: category of the insight. Required.
        status:
          type: string
          description: status of the insight. example resolved, repaired, other.
        eventId:
          type: string
          description: Identifier of the event that has been correlated with this insight. This can be used to aggregate insights for the same event.
        groupId:
          type: string
          description: Identifier that can be used to group similar insights.
        content:
          $ref: "#/components/schemas/Content"
          description: Contains title & description for the insight. Required.
        eventTime:
          type: string
          format: date-time
          description: Time of the event, which has been correlated the impact.
        insightUniqueId:
          type: string
          description: unique id of the insight. Required.
        impact:
          $ref: "#/components/schemas/ImpactDetails"
          description: details of of the impact for which insight has been generated. Required.
        additionalDetails:
          type: object
          properties: {}
          description: additional details of the insight.
    PagedInsightFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/InsightFlattened"
        nextLink:
          type: string
    Insight:
      type: object
      description: Insight resource. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/InsightProperties"
          description: The resource-specific properties for this resource.
    OperationDisplay:
      type: object
      description: Localized display information for and operation. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    WorkloadImpactProperties:
      type: object
      description: Workload impact properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        startDateTime:
          type: string
          format: date-time
          description: Time at which impact was observed. Required.
        endDateTime:
          type: string
          format: date-time
          description: Time at which impact has ended.
        impactedResourceId:
          type: string
          description: Azure resource id of the impacted resource. Required.
        impactUniqueId:
          type: string
          description: Unique ID of the impact (UUID).
        reportedTimeUtc:
          type: string
          format: date-time
          description: Time at which impact is reported.
        impactCategory:
          type: string
          description: Category of the impact, details can found from /impactCategories API. Required.
        impactDescription:
          type: string
          description: A detailed description of the impact.
        armCorrelationIds:
          type: array
          items:
            type: string
          description: The ARM correlation ids, this is important field for control plane related impacts.
        performance:
          type: array
          items:
            $ref: "#/components/schemas/Performance"
          description: Details about performance issue. Applicable for performance impacts.
        connectivity:
          $ref: "#/components/schemas/Connectivity"
          description: Details about connectivity issue. Applicable when root resource causing the issue is not identified. For example, when a VM is impacted due to a network issue, the impacted resource is identified as the VM, but the root cause is the network. In such cases, the connectivity field will have the details about the network issue.
        additionalProperties:
          type: object
          properties: {}
          description: Additional fields related to impact, applicable fields per resource type are list under /impactCategories API.
        errorDetails:
          $ref: "#/components/schemas/ErrorDetailProperties"
          description: ARM error code and error message associated with the impact.
        workload:
          $ref: "#/components/schemas/Workload"
          description: Information about the impacted workload.
        impactGroupId:
          type: string
          description: Use this field to group impacts.
        confidenceLevel:
          $ref: "#/components/schemas/ConfidenceLevel"
          description: 'Degree of confidence on the impact being a platform issue. Known values are: "Low", "Medium", and "High".'
        clientIncidentDetails:
          $ref: "#/components/schemas/ClientIncidentDetails"
          description: "Client incident details ex: incidentId , incident source."
      required:
        - startDateTime
        - impactedResourceId
        - impactCategory
    Performance:
      type: object
      description: Details about impacted performance metrics. Applicable for performance related impact.
      properties:
        metricName:
          type: string
          description: "Name of the Metric examples: Disk, IOPs, CPU, GPU, Memory, details can be found from /impactCategories API."
        expected:
          type: number
          description: Threshold value for the metric.
        actual:
          type: number
          description: Observed value for the metric.
        expectedValueRange:
          $ref: "#/components/schemas/ExpectedValueRange"
          description: Max and Min Threshold values for the metric.
        unit:
          $ref: "#/components/schemas/MetricUnit"
          description: 'Unit of the metric ex: Bytes, Percentage, Count, Seconds, Milliseconds, Bytes/Second, Count/Second, etc.., Other. Known values are: "ByteSeconds", "Bytes", "BytesPerSecond", "Cores", "Count", "CountPerSecond", "MilliCores", "MilliSeconds", "NanoCores", "Percent", "Seconds", and "Other".'
    ExpectedValueRange:
      type: object
      description: Max and Min Threshold values for the metric.
      properties:
        min:
          type: number
          description: Min threshold value for the metric. Required.
        max:
          type: number
          description: Max threshold value for the metric. Required.
      required:
        - min
        - max
    MetricUnit:
      type: string
      enum:
        - ByteSeconds
        - Bytes
        - BytesPerSecond
        - Cores
        - Count
        - CountPerSecond
        - MilliCores
        - MilliSeconds
        - NanoCores
        - Percent
        - Seconds
        - Other
    Connectivity:
      type: object
      description: Details about connectivity issue. Applicable when root resource causing the issue is not identified. For example, when a VM is impacted due to a network issue, the impacted resource could be VM or the network. In such cases, the connectivity field will have the details about both VM and network.
      properties:
        protocol:
          $ref: "#/components/schemas/Protocol"
          description: 'Protocol used for the connection. Known values are: "TCP", "UDP", "HTTP", "HTTPS", "RDP", "FTP", "SSH", and "Other".'
        port:
          type: integer
          description: Port number for the connection.
        source:
          $ref: "#/components/schemas/SourceOrTarget"
          description: Source from which the connection was attempted.
        target:
          $ref: "#/components/schemas/SourceOrTarget"
          description: target which connection was attempted.
    Protocol:
      type: string
      enum:
        - TCP
        - UDP
        - HTTP
        - HTTPS
        - RDP
        - FTP
        - SSH
        - Other
    SourceOrTarget:
      type: object
      description: Resource details.
      properties:
        azureResourceId:
          type: string
          description: Azure resource id, example /subscription/{subscription}/resourceGroup/{rg}/Microsoft.compute/virtualMachine/{vmName}.
    ErrorDetailProperties:
      type: object
      description: ARM error code and error message associated with the impact.
      properties:
        errorCode:
          type: string
          description: ARM Error code associated with the impact.
        errorMessage:
          type: string
          description: ARM Error Message associated with the impact.
    Workload:
      type: object
      description: Information about the impacted workload.
      properties:
        context:
          type: string
          description: the scenario for the workload.
        toolset:
          $ref: "#/components/schemas/Toolset"
          description: 'Tool used to interact with Azure. SDK, AzPortal, etc.., Other. Known values are: "Terraform", "Puppet", "Chef", "SDK", "Ansible", "ARM", "Portal", "Shell", and "Other".'
    Toolset:
      type: string
      enum:
        - Terraform
        - Puppet
        - Chef
        - SDK
        - Ansible
        - ARM
        - Portal
        - Shell
        - Other
    ConfidenceLevel:
      type: string
      enum:
        - Low
        - Medium
        - High
    ClientIncidentDetails:
      type: object
      description: "Client incident details ex: incidentId , incident source."
      properties:
        clientIncidentId:
          type: string
          description: "Client incident id. ex : id of the incident created to investigate and address the impact if any."
        clientIncidentSource:
          $ref: "#/components/schemas/IncidentSource"
          description: 'Client incident source. ex : source system name where the incident is created. Known values are: "AzureDevops", "ICM", "Jira", "ServiceNow", and "Other".'
    IncidentSource:
      type: string
      enum:
        - AzureDevops
        - ICM
        - Jira
        - ServiceNow
        - Other
    WorkloadImpactFlattened:
      type: object
      description: Flattened view of WorkloadImpact ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", and "Canceled".'
        startDateTime:
          type: string
          format: date-time
          description: Time at which impact was observed. Required.
        endDateTime:
          type: string
          format: date-time
          description: Time at which impact has ended.
        impactedResourceId:
          type: string
          description: Azure resource id of the impacted resource. Required.
        impactUniqueId:
          type: string
          description: Unique ID of the impact (UUID).
        reportedTimeUtc:
          type: string
          format: date-time
          description: Time at which impact is reported.
        impactCategory:
          type: string
          description: Category of the impact, details can found from /impactCategories API. Required.
        impactDescription:
          type: string
          description: A detailed description of the impact.
        armCorrelationIds:
          type: array
          items:
            type: string
          description: The ARM correlation ids, this is important field for control plane related impacts.
        performance:
          type: array
          items:
            $ref: "#/components/schemas/Performance"
          description: Details about performance issue. Applicable for performance impacts.
        connectivity:
          $ref: "#/components/schemas/Connectivity"
          description: Details about connectivity issue. Applicable when root resource causing the issue is not identified. For example, when a VM is impacted due to a network issue, the impacted resource is identified as the VM, but the root cause is the network. In such cases, the connectivity field will have the details about the network issue.
        additionalProperties:
          type: object
          properties: {}
          description: Additional fields related to impact, applicable fields per resource type are list under /impactCategories API.
        errorDetails:
          $ref: "#/components/schemas/ErrorDetailProperties"
          description: ARM error code and error message associated with the impact.
        workload:
          $ref: "#/components/schemas/Workload"
          description: Information about the impacted workload.
        impactGroupId:
          type: string
          description: Use this field to group impacts.
        confidenceLevel:
          $ref: "#/components/schemas/ConfidenceLevel"
          description: 'Degree of confidence on the impact being a platform issue. Known values are: "Low", "Medium", and "High".'
        clientIncidentDetails:
          $ref: "#/components/schemas/ClientIncidentDetails"
          description: "Client incident details ex: incidentId , incident source."
    PagedWorkloadImpactFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/WorkloadImpactFlattened"
        nextLink:
          type: string
    WorkloadImpact:
      type: object
      description: Workload Impact properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/WorkloadImpactProperties"
          description: The resource-specific properties for this resource.
  x-stackQL-resources:
    connectors:
      id: azure.impactreporting.connectors
      name: connectors
      title: connectors
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1connectors~1{connector_name}?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1connectors~1{connector_name}?api-version=2024-05-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1connectors~1{connector_name}?api-version=2024-05-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1connectors~1{connector_name}?api-version=2024-05-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1connectors?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/connectors/methods/get"
          - $ref: "#/components/x-stackQL-resources/connectors/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/connectors/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/connectors/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/connectors/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/connectors/methods/delete"
    impact_categories:
      id: azure.impactreporting.impact_categories
      name: impact_categories
      title: impact_categories
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1impactCategories~1{impact_category_name}?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1impactCategories?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/impact_categories/methods/get"
          - $ref: "#/components/x-stackQL-resources/impact_categories/methods/list_by_subscription"
        insert: []
        update: []
        delete: []
    insights:
      id: azure.impactreporting.insights
      name: insights
      title: insights
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts~1{workload_impact_name}~1insights~1{insight_name}?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts~1{workload_impact_name}~1insights~1{insight_name}?api-version=2024-05-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts~1{workload_impact_name}~1insights~1{insight_name}?api-version=2024-05-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts~1{workload_impact_name}~1insights?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/insights/methods/get"
          - $ref: "#/components/x-stackQL-resources/insights/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/insights/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/insights/methods/delete"
    operations:
      id: azure.impactreporting.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Impact~1operations?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    workload_impacts:
      id: azure.impactreporting.workload_impacts
      name: workload_impacts
      title: workload_impacts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts~1{workload_impact_name}?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts~1{workload_impact_name}?api-version=2024-05-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts~1{workload_impact_name}?api-version=2024-05-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Impact~1workloadImpacts?api-version=2024-05-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/workload_impacts/methods/get"
          - $ref: "#/components/x-stackQL-resources/workload_impacts/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/workload_impacts/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/workload_impacts/methods/delete"
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/connectors/{connector_name}?api-version=2024-05-01-preview:
    get:
      operationId: Connectors_get
      description: Get a Connector.
      parameters:
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the connector. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorFlattened"
    put:
      operationId: Connectors_begin_create_or_update
      description: Create a Connector.
      parameters:
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the connector. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ConnectorProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Connector"
    patch:
      operationId: Connectors_update
      description: Update a Connector.
      parameters:
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the connector. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ConnectorUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Connector"
    delete:
      operationId: Connectors_delete
      description: Delete a Connector.
      parameters:
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the connector. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/connectors?api-version=2024-05-01-preview:
    get:
      operationId: Connectors_list_by_subscription
      description: List Connector resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConnectorFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/impactCategories/{impact_category_name}?api-version=2024-05-01-preview:
    get:
      operationId: ImpactCategories_get
      description: Get a ImpactCategory.
      parameters:
        - name: impact_category_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the impact category. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImpactCategoryFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/impactCategories?api-version=2024-05-01-preview:
    get:
      operationId: ImpactCategories_list_by_subscription
      description: List ImpactCategory resources by subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: resourceType
          in: query
          required: true
          schema:
            type: string
          description: Filter by resource type. Required.
        - name: categoryName
          in: query
          required: false
          schema:
            type: string
          description: Filter by category name. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedImpactCategoryFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/workloadImpacts/{workload_impact_name}/insights/{insight_name}?api-version=2024-05-01-preview:
    get:
      operationId: Insights_get
      description: Get Insight resources by workloadImpactName and insightName.
      parameters:
        - name: workload_impact_name
          in: path
          required: true
          schema:
            type: string
          description: workloadImpact resource. Required.
        - name: insight_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the insight. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InsightFlattened"
    put:
      operationId: Insights_create
      description: Create Insight resource, This is Admin only operation.
      parameters:
        - name: workload_impact_name
          in: path
          required: true
          schema:
            type: string
          description: workloadImpact resource. Required.
        - name: insight_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the insight. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/InsightProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Insight"
    delete:
      operationId: Insights_delete
      description: Delete Insight resource, This is Admin only operation.
      parameters:
        - name: workload_impact_name
          in: path
          required: true
          schema:
            type: string
          description: workloadImpact resource. Required.
        - name: insight_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the insight. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/workloadImpacts/{workload_impact_name}/insights?api-version=2024-05-01-preview:
    get:
      operationId: Insights_list_by_subscription
      description: List Insight resources by workloadImpactName.
      parameters:
        - name: workload_impact_name
          in: path
          required: true
          schema:
            type: string
          description: workloadImpact resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedInsightFlattened"
  /providers/Microsoft.Impact/operations?api-version=2024-05-01-preview:
    get:
      operationId: impactreporting_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/workloadImpacts/{workload_impact_name}?api-version=2024-05-01-preview:
    get:
      operationId: WorkloadImpacts_get
      description: Get a WorkloadImpact.
      parameters:
        - name: workload_impact_name
          in: path
          required: true
          schema:
            type: string
          description: workloadImpact resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkloadImpactFlattened"
    put:
      operationId: WorkloadImpacts_begin_create
      description: Create a WorkloadImpact.
      parameters:
        - name: workload_impact_name
          in: path
          required: true
          schema:
            type: string
          description: workloadImpact resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/WorkloadImpactProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkloadImpact"
    delete:
      operationId: WorkloadImpacts_delete
      description: Delete a WorkloadImpact.
      parameters:
        - name: workload_impact_name
          in: path
          required: true
          schema:
            type: string
          description: workloadImpact resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Impact/workloadImpacts?api-version=2024-05-01-preview:
    get:
      operationId: WorkloadImpacts_list_by_subscription
      description: List WorkloadImpact resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedWorkloadImpactFlattened"
