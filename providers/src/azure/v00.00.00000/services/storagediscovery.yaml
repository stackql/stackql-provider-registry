openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Storage Discovery Mgmt Client
  description: The Azure Storage Discovery Management API.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-09-01-stackql-generated
  x-serviceAlias: storagediscovery
  x-sdkPackage: azure-mgmt-storagediscovery
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    OperationDisplay:
      type: object
      description: Localized display information for and operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    StorageDiscoveryWorkspaceProperties:
      type: object
      description: Storage Discovery Workspace Properties.
      properties:
        sku:
          $ref: "#/components/schemas/StorageDiscoverySku"
          description: 'The storage discovery sku. Known values are: "Standard" and "Free".'
        description:
          type: string
          description: The description of the storage discovery workspace.
        workspaceRoots:
          type: array
          items:
            type: string
          description: The view level storage discovery data estate. Required.
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/StorageDiscoveryScope"
          description: The scopes of the storage discovery workspace. Required.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", and "Canceled".'
      required:
        - workspaceRoots
        - scopes
    StorageDiscoverySku:
      type: string
      enum:
        - Standard
        - Free
    StorageDiscoveryScope:
      type: object
      description: Storage Discovery Scope. This had added validations.
      properties:
        displayName:
          type: string
          description: Display name of the collection. Required.
        resourceTypes:
          type: array
          items:
            $ref: "#/components/schemas/StorageDiscoveryResourceType"
          description: Resource types for the collection. Required.
        tagKeysOnly:
          type: array
          items:
            type: string
          description: The storage account tags keys to filter.
        tags:
          type: object
          properties: {}
          description: Resource tags.
      required:
        - displayName
        - resourceTypes
    StorageDiscoveryResourceType:
      type: string
      enum:
        - Microsoft.Storage/storageAccounts
    ResourceProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
    StorageDiscoveryWorkspaceFlattened:
      type: object
      description: Flattened view of StorageDiscoveryWorkspace ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        sku:
          $ref: "#/components/schemas/StorageDiscoverySku"
          description: 'The storage discovery sku. Known values are: "Standard" and "Free".'
        description:
          type: string
          description: The description of the storage discovery workspace.
        workspaceRoots:
          type: array
          items:
            type: string
          description: The view level storage discovery data estate. Required.
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/StorageDiscoveryScope"
          description: The scopes of the storage discovery workspace. Required.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedStorageDiscoveryWorkspaceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StorageDiscoveryWorkspaceFlattened"
        nextLink:
          type: string
    StorageDiscoveryWorkspace:
      type: object
      description: A Storage Discovery Workspace resource. This resource configures the collection of storage account metrics.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/StorageDiscoveryWorkspaceProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    StorageDiscoveryWorkspacePropertiesUpdate:
      type: object
      description: The template for adding updateable properties.
      properties:
        sku:
          $ref: "#/components/schemas/StorageDiscoverySku"
          description: 'The storage discovery sku. Known values are: "Standard" and "Free".'
        description:
          type: string
          description: The description of the storage discovery workspace.
        workspaceRoots:
          type: array
          items:
            type: string
          description: The view level storage discovery data estate.
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/StorageDiscoveryScope"
          description: The scopes of the storage discovery workspace.
  x-stackQL-resources:
    operations:
      id: azure.storagediscovery.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.StorageDiscovery~1operations?api-version=2025-09-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    storage_discovery_workspaces:
      id: azure.storagediscovery.storage_discovery_workspaces
      name: storage_discovery_workspaces
      title: storage_discovery_workspaces
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageDiscovery~1storageDiscoveryWorkspaces~1{storage_discovery_workspace_name}?api-version=2025-09-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageDiscovery~1storageDiscoveryWorkspaces~1{storage_discovery_workspace_name}?api-version=2025-09-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageDiscovery~1storageDiscoveryWorkspaces~1{storage_discovery_workspace_name}?api-version=2025-09-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageDiscovery~1storageDiscoveryWorkspaces~1{storage_discovery_workspace_name}?api-version=2025-09-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageDiscovery~1storageDiscoveryWorkspaces?api-version=2025-09-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StorageDiscovery~1storageDiscoveryWorkspaces?api-version=2025-09-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/storage_discovery_workspaces/methods/get"
          - $ref: "#/components/x-stackQL-resources/storage_discovery_workspaces/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/storage_discovery_workspaces/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/storage_discovery_workspaces/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/storage_discovery_workspaces/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/storage_discovery_workspaces/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/storage_discovery_workspaces/methods/delete"
paths:
  /providers/Microsoft.StorageDiscovery/operations?api-version=2025-09-01:
    get:
      operationId: storagediscovery_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StorageDiscovery/storageDiscoveryWorkspaces/{storage_discovery_workspace_name}?api-version=2025-09-01:
    get:
      operationId: StorageDiscoveryWorkspaces_get
      description: Get a StorageDiscoveryWorkspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_discovery_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the StorageDiscoveryWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageDiscoveryWorkspaceFlattened"
    put:
      operationId: StorageDiscoveryWorkspaces_create_or_update
      description: Create a StorageDiscoveryWorkspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_discovery_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the StorageDiscoveryWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/StorageDiscoveryWorkspaceProperties"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageDiscoveryWorkspace"
    patch:
      operationId: StorageDiscoveryWorkspaces_update
      description: Update a StorageDiscoveryWorkspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_discovery_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the StorageDiscoveryWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/StorageDiscoveryWorkspacePropertiesUpdate"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageDiscoveryWorkspace"
    delete:
      operationId: StorageDiscoveryWorkspaces_delete
      description: Delete a StorageDiscoveryWorkspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_discovery_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the StorageDiscoveryWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StorageDiscovery/storageDiscoveryWorkspaces?api-version=2025-09-01:
    get:
      operationId: StorageDiscoveryWorkspaces_list_by_resource_group
      description: List StorageDiscoveryWorkspace resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStorageDiscoveryWorkspaceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.StorageDiscovery/storageDiscoveryWorkspaces?api-version=2025-09-01:
    get:
      operationId: StorageDiscoveryWorkspaces_list_by_subscription
      description: List StorageDiscoveryWorkspace resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStorageDiscoveryWorkspaceFlattened"
