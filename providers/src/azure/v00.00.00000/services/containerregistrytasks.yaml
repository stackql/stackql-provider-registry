openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Container Registry Tasks Mgmt Client
  description: The Microsoft Azure Container Registry management API provides create, read, update, and delete functionality for Azure Container Registry resources including registries, replications, webhooks, tasks, runs, and other registry components.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-03-01-preview-stackql-generated
  x-serviceAlias: containerregistrytasks
  x-sdkPackage: azure-mgmt-containerregistrytasks
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    AgentPoolProperties:
      type: object
      description: The properties of agent pool.
      properties:
        count:
          type: integer
          description: The count of agent machine.
        tier:
          type: string
          description: The Tier of agent machine.
        os:
          $ref: "#/components/schemas/OS"
          description: 'The OS of agent machine. Known values are: "Windows" and "Linux".'
        virtualNetworkSubnetResourceId:
          type: string
          description: The Virtual Network Subnet Resource Id of the agent machine.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of this agent pool. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
    OS:
      type: string
      enum:
        - Windows
        - Linux
    ProvisioningState:
      type: string
      enum:
        - Creating
        - Updating
        - Deleting
        - Succeeded
        - Failed
        - Canceled
    AgentPoolFlattened:
      type: object
      description: Flattened view of AgentPool ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        count:
          type: integer
          description: The count of agent machine.
        tier:
          type: string
          description: The Tier of agent machine.
        os:
          $ref: "#/components/schemas/OS"
          description: 'The OS of agent machine. Known values are: "Windows" and "Linux".'
        virtualNetworkSubnetResourceId:
          type: string
          description: The Virtual Network Subnet Resource Id of the agent machine.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of this agent pool. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
    PagedAgentPoolFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AgentPoolFlattened"
        nextLink:
          type: string
    AgentPoolQueueStatus:
      type: object
      description: The QueueStatus of Agent Pool.
      properties:
        count:
          type: integer
          description: The number of pending runs in the queue.
    AgentPool:
      type: object
      description: The agentpool that has the ARM resource and properties. The agentpool will have all information to create an agent pool.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AgentPoolProperties"
          description: The properties associated with the agent pool.
      required:
        - location
    AgentPoolPropertiesUpdateParameters:
      type: object
      description: AgentPoolPropertiesUpdateParameters.
      properties:
        count:
          type: integer
          description: The count of agent machine.
    SourceUploadDefinition:
      type: object
      description: The properties of a response to source upload request.
      properties:
        uploadUrl:
          type: string
          description: The URL where the client can upload the source.
        relativePath:
          type: string
          description: The relative path to the source. This is used to submit the subsequent queue build request.
    Run:
      type: object
      description: Run resource properties.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/RunProperties"
          description: The properties of a run.
    RunProperties:
      type: object
      description: The properties for a run.
      properties:
        runId:
          type: string
          description: The unique identifier for the run.
        status:
          $ref: "#/components/schemas/RunStatus"
          description: 'The current status of the run. Known values are: "Queued", "Started", "Running", "Succeeded", "Failed", "Canceled", "Error", and "Timeout".'
        lastUpdatedTime:
          type: string
          format: date-time
          description: The last updated time for the run.
        runType:
          $ref: "#/components/schemas/RunType"
          description: 'The type of run. Known values are: "QuickBuild", "QuickRun", "AutoBuild", and "AutoRun".'
        agentPoolName:
          type: string
          description: The dedicated agent pool for the run.
        createTime:
          type: string
          format: date-time
          description: The time the run was scheduled.
        startTime:
          type: string
          format: date-time
          description: The time the run started.
        finishTime:
          type: string
          format: date-time
          description: The time the run finished.
        outputImages:
          type: array
          items:
            $ref: "#/components/schemas/ImageDescriptor"
          description: The list of all images that were generated from the run. This is applicable if the run generates base image dependencies.
        task:
          type: string
          description: The task against which run was scheduled.
        imageUpdateTrigger:
          $ref: "#/components/schemas/ImageUpdateTrigger"
          description: The image update trigger that caused the run. This is applicable if the task has base image trigger configured.
        sourceTrigger:
          $ref: "#/components/schemas/SourceTriggerDescriptor"
          description: The source trigger that caused the run.
        timerTrigger:
          $ref: "#/components/schemas/TimerTriggerDescriptor"
          description: The timer trigger that caused the run.
        platform:
          $ref: "#/components/schemas/PlatformProperties"
          description: The platform properties against which the run will happen.
        agentConfiguration:
          $ref: "#/components/schemas/AgentProperties"
          description: The machine configuration of the run agent.
        sourceRegistryAuth:
          type: string
          description: The scope of the credentials that were used to login to the source registry during this run.
        customRegistries:
          type: array
          items:
            type: string
          description: The list of custom registries that were logged in during this run.
        runErrorMessage:
          type: string
          description: The error message received from backend systems after the run is scheduled.
        updateTriggerToken:
          type: string
          description: The update trigger token passed for the Run.
        logArtifact:
          $ref: "#/components/schemas/ImageDescriptor"
          description: The image description for the log artifact.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of a run. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        isArchiveEnabled:
          type: boolean
          description: The value that indicates whether archiving is enabled or not.
    RunStatus:
      type: string
      enum:
        - Queued
        - Started
        - Running
        - Succeeded
        - Failed
        - Canceled
        - Error
        - Timeout
    RunType:
      type: string
      enum:
        - QuickBuild
        - QuickRun
        - AutoBuild
        - AutoRun
    ImageDescriptor:
      type: object
      description: Properties for a registry image.
      properties:
        registry:
          type: string
          description: The registry login server.
        repository:
          type: string
          description: The repository name.
        tag:
          type: string
          description: The tag name.
        digest:
          type: string
          description: The sha256-based digest of the image manifest.
    ImageUpdateTrigger:
      type: object
      description: The image update trigger that caused a build.
      properties:
        id:
          type: string
          description: The unique ID of the trigger.
        timestamp:
          type: string
          format: date-time
          description: The timestamp when the image update happened.
        images:
          type: array
          items:
            $ref: "#/components/schemas/ImageDescriptor"
          description: The list of image updates that caused the build.
    SourceTriggerDescriptor:
      type: object
      description: The source trigger that caused a run.
      properties:
        id:
          type: string
          description: The unique ID of the trigger.
        eventType:
          type: string
          description: The event type of the trigger.
        commitId:
          type: string
          description: The unique ID that identifies a commit.
        pullRequestId:
          type: string
          description: The unique ID that identifies pull request.
        repositoryUrl:
          type: string
          description: The repository URL.
        branchName:
          type: string
          description: The branch name in the repository.
        providerType:
          type: string
          description: The source control provider type.
    TimerTriggerDescriptor:
      type: object
      description: TimerTriggerDescriptor.
      properties:
        timerTriggerName:
          type: string
          description: The timer trigger name that caused the run.
        scheduleOccurrence:
          type: string
          description: The occurrence that triggered the run.
    PlatformProperties:
      type: object
      description: The platform properties against which the run has to happen.
      properties:
        os:
          $ref: "#/components/schemas/OS"
          description: 'The operating system type required for the run. Required. Known values are: "Windows" and "Linux".'
        architecture:
          $ref: "#/components/schemas/Architecture"
          description: 'The OS architecture. Known values are: "amd64", "x86", "386", "arm", and "arm64".'
        variant:
          $ref: "#/components/schemas/Variant"
          description: 'Variant of the CPU. Known values are: "v6", "v7", and "v8".'
      required:
        - os
    Architecture:
      type: string
      enum:
        - amd64
        - x86
        - "386"
        - arm
        - arm64
    Variant:
      type: string
      enum:
        - v6
        - v7
        - v8
    AgentProperties:
      type: object
      description: The properties that determine the run agent configuration.
      properties:
        cpu:
          type: integer
          description: The CPU configuration in terms of number of cores required for the run.
    RunFlattened:
      type: object
      description: Flattened view of Run ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        runId:
          type: string
          description: The unique identifier for the run.
        status:
          $ref: "#/components/schemas/RunStatus"
          description: 'The current status of the run. Known values are: "Queued", "Started", "Running", "Succeeded", "Failed", "Canceled", "Error", and "Timeout".'
        lastUpdatedTime:
          type: string
          format: date-time
          description: The last updated time for the run.
        runType:
          $ref: "#/components/schemas/RunType"
          description: 'The type of run. Known values are: "QuickBuild", "QuickRun", "AutoBuild", and "AutoRun".'
        agentPoolName:
          type: string
          description: The dedicated agent pool for the run.
        createTime:
          type: string
          format: date-time
          description: The time the run was scheduled.
        startTime:
          type: string
          format: date-time
          description: The time the run started.
        finishTime:
          type: string
          format: date-time
          description: The time the run finished.
        outputImages:
          type: array
          items:
            $ref: "#/components/schemas/ImageDescriptor"
          description: The list of all images that were generated from the run. This is applicable if the run generates base image dependencies.
        task:
          type: string
          description: The task against which run was scheduled.
        imageUpdateTrigger:
          $ref: "#/components/schemas/ImageUpdateTrigger"
          description: The image update trigger that caused the run. This is applicable if the task has base image trigger configured.
        sourceTrigger:
          $ref: "#/components/schemas/SourceTriggerDescriptor"
          description: The source trigger that caused the run.
        timerTrigger:
          $ref: "#/components/schemas/TimerTriggerDescriptor"
          description: The timer trigger that caused the run.
        platform:
          $ref: "#/components/schemas/PlatformProperties"
          description: The platform properties against which the run will happen.
        agentConfiguration:
          $ref: "#/components/schemas/AgentProperties"
          description: The machine configuration of the run agent.
        sourceRegistryAuth:
          type: string
          description: The scope of the credentials that were used to login to the source registry during this run.
        customRegistries:
          type: array
          items:
            type: string
          description: The list of custom registries that were logged in during this run.
        runErrorMessage:
          type: string
          description: The error message received from backend systems after the run is scheduled.
        updateTriggerToken:
          type: string
          description: The update trigger token passed for the Run.
        logArtifact:
          $ref: "#/components/schemas/ImageDescriptor"
          description: The image description for the log artifact.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of a run. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        isArchiveEnabled:
          type: boolean
          description: The value that indicates whether archiving is enabled or not.
    PagedRunFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RunFlattened"
        nextLink:
          type: string
    RunGetLogResult:
      type: object
      description: The result of get log link operation.
      properties:
        logLink:
          type: string
          description: The link to logs for a run on a azure container registry.
        logArtifactLink:
          type: string
          description: The link to logs in registry for a run on a azure container registry.
    TaskRunProperties:
      type: object
      description: The properties of task run.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of this task run. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        runRequest:
          $ref: "#/components/schemas/RunRequest"
          description: The request (parameters) for the run.
        runResult:
          $ref: "#/components/schemas/Run"
          description: The result of this task run.
        forceUpdateTag:
          type: string
          description: How the run should be forced to rerun even if the run request configuration has not changed.
    RunRequest:
      type: object
      description: "The request parameters for scheduling a run. You probably want to use the sub-classes and not this class directly. Known sub-classes are: DockerBuildRequest, EncodedTaskRunRequest, FileTaskRunRequest, TaskRunRequest"
      properties:
        type:
          type: string
          description: The type of the run request. Required. Default value is None.
        isArchiveEnabled:
          type: boolean
          description: The value that indicates whether archiving is enabled for the run or not.
        agentPoolName:
          type: string
          description: The dedicated agent pool for the run.
        logTemplate:
          type: string
          description: The template that describes the repository and tag information for run log artifact.
      required:
        - type
    IdentityProperties:
      type: object
      description: Managed identity for the resource.
      properties:
        principalId:
          type: string
          description: The principal ID of resource identity.
        tenantId:
          type: string
          description: The tenant ID of resource.
        type:
          $ref: "#/components/schemas/ResourceIdentityType"
          description: 'The identity type. Known values are: "SystemAssigned", "UserAssigned", "SystemAssigned, UserAssigned", and "None".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: "The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/ providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}'."
    ResourceIdentityType:
      type: string
      enum:
        - SystemAssigned
        - UserAssigned
        - SystemAssigned, UserAssigned
        - None
    TaskRunFlattened:
      type: object
      description: Flattened view of TaskRun ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: Identity for the resource.
        location:
          type: string
          description: The location of the resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of this task run. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        runRequest:
          $ref: "#/components/schemas/RunRequest"
          description: The request (parameters) for the run.
        runResult:
          $ref: "#/components/schemas/Run"
          description: The result of this task run.
        forceUpdateTag:
          type: string
          description: How the run should be forced to rerun even if the run request configuration has not changed.
    PagedTaskRunFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TaskRunFlattened"
        nextLink:
          type: string
    TaskRun:
      type: object
      description: The task run that has the ARM resource and properties. The task run will have the information of request and result of a run.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/TaskRunProperties"
          description: The properties associated with the task run, i.e., request and result of the run.
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: Identity for the resource.
        location:
          type: string
          description: The location of the resource.
    TaskRunPropertiesUpdateParameters:
      type: object
      description: The properties of a task run update parameters.
      properties:
        runRequest:
          $ref: "#/components/schemas/RunRequest"
          description: The request (parameters) for the new run.
        forceUpdateTag:
          type: string
          description: How the run should be forced to rerun even if the run request configuration has not changed.
    TaskProperties:
      type: object
      description: The properties of a task.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the task. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        creationDate:
          type: string
          format: date-time
          description: The creation date of task.
        status:
          $ref: "#/components/schemas/TaskStatus"
          description: 'The current status of task. Known values are: "Disabled" and "Enabled".'
        platform:
          $ref: "#/components/schemas/PlatformProperties"
          description: The platform properties against which the run has to happen.
        agentConfiguration:
          $ref: "#/components/schemas/AgentProperties"
          description: The machine configuration of the run agent.
        agentPoolName:
          type: string
          description: The dedicated agent pool for the task.
        timeout:
          type: integer
          description: Run timeout in seconds.
        step:
          $ref: "#/components/schemas/TaskStepProperties"
          description: The properties of a task step.
        trigger:
          $ref: "#/components/schemas/TriggerProperties"
          description: The properties that describe all triggers for the task.
        credentials:
          $ref: "#/components/schemas/Credentials"
          description: The properties that describes a set of credentials that will be used when this run is invoked.
        logTemplate:
          type: string
          description: The template that describes the repository and tag information for run log artifact.
        isSystemTask:
          type: boolean
          description: The value of this property indicates whether the task resource is system task or not.
    TaskStatus:
      type: string
      enum:
        - Disabled
        - Enabled
    TaskStepProperties:
      type: object
      description: "Base properties for any task step. You probably want to use the sub-classes and not this class directly. Known sub-classes are: DockerBuildStep, EncodedTaskStep, FileTaskStep"
      properties:
        type:
          type: string
          description: 'The type of the step. Required. Known values are: "Docker", "FileTask", and "EncodedTask".'
        baseImageDependencies:
          type: array
          items:
            $ref: "#/components/schemas/BaseImageDependency"
          description: List of base image dependencies for a step.
        contextPath:
          type: string
          description: The URL(absolute or relative) of the source context for the task step.
        contextAccessToken:
          type: string
          description: The token (git PAT or SAS token of storage account blob) associated with the context for a step.
      required:
        - type
    BaseImageDependency:
      type: object
      description: Properties that describe a base image dependency.
      properties:
        type:
          $ref: "#/components/schemas/BaseImageDependencyType"
          description: 'The type of the base image dependency. Known values are: "BuildTime" and "RunTime".'
        registry:
          type: string
          description: The registry login server.
        repository:
          type: string
          description: The repository name.
        tag:
          type: string
          description: The tag name.
        digest:
          type: string
          description: The sha256-based digest of the image manifest.
    BaseImageDependencyType:
      type: string
      enum:
        - BuildTime
        - RunTime
    TriggerProperties:
      type: object
      description: The properties of a trigger.
      properties:
        timerTriggers:
          type: array
          items:
            $ref: "#/components/schemas/TimerTrigger"
          description: The collection of timer triggers.
        sourceTriggers:
          type: array
          items:
            $ref: "#/components/schemas/SourceTrigger"
          description: The collection of triggers based on source code repository.
        baseImageTrigger:
          $ref: "#/components/schemas/BaseImageTrigger"
          description: The trigger based on base image dependencies.
    TimerTrigger:
      type: object
      description: The properties of a timer trigger.
      properties:
        schedule:
          type: string
          description: The CRON expression for the task schedule. Required.
        status:
          $ref: "#/components/schemas/TriggerStatus"
          description: 'The current status of trigger. Known values are: "Disabled" and "Enabled".'
        name:
          type: string
          description: The name of the trigger. Required.
      required:
        - schedule
        - name
    TriggerStatus:
      type: string
      enum:
        - Disabled
        - Enabled
    SourceTrigger:
      type: object
      description: The properties of a source based trigger.
      properties:
        sourceRepository:
          $ref: "#/components/schemas/SourceProperties"
          description: The properties that describes the source(code) for the task. Required.
        sourceTriggerEvents:
          type: array
          items:
            $ref: "#/components/schemas/SourceTriggerEvent"
          description: The source event corresponding to the trigger. Required.
        status:
          $ref: "#/components/schemas/TriggerStatus"
          description: 'The current status of trigger. Known values are: "Disabled" and "Enabled".'
        name:
          type: string
          description: The name of the trigger. Required.
      required:
        - sourceRepository
        - sourceTriggerEvents
        - name
    SourceProperties:
      type: object
      description: The properties of the source code repository.
      properties:
        sourceControlType:
          $ref: "#/components/schemas/SourceControlType"
          description: 'The type of source control service. Required. Known values are: "Github" and "VisualStudioTeamService".'
        repositoryUrl:
          type: string
          description: The full URL to the source code repository. Required.
        branch:
          type: string
          description: The branch name of the source code.
        sourceControlAuthProperties:
          $ref: "#/components/schemas/AuthInfo"
          description: The authorization properties for accessing the source code repository and to set up webhooks for notifications.
      required:
        - sourceControlType
        - repositoryUrl
    SourceControlType:
      type: string
      enum:
        - Github
        - VisualStudioTeamService
    AuthInfo:
      type: object
      description: The authorization properties for accessing the source code repository.
      properties:
        tokenType:
          $ref: "#/components/schemas/TokenType"
          description: 'The type of Auth token. Required. Known values are: "PAT" and "OAuth".'
        token:
          type: string
          description: The access token used to access the source control provider. Required.
        refreshToken:
          type: string
          description: The refresh token used to refresh the access token.
        scope:
          type: string
          description: The scope of the access token.
        expiresIn:
          type: integer
          description: Time in seconds that the token remains valid.
      required:
        - tokenType
        - token
    TokenType:
      type: string
      enum:
        - PAT
        - OAuth
    SourceTriggerEvent:
      type: string
      enum:
        - commit
        - pullrequest
    BaseImageTrigger:
      type: object
      description: The trigger based on base image dependency.
      properties:
        baseImageTriggerType:
          $ref: "#/components/schemas/BaseImageTriggerType"
          description: 'The type of the auto trigger for base image dependency updates. Required. Known values are: "All" and "Runtime".'
        updateTriggerEndpoint:
          type: string
          description: The endpoint URL for receiving update triggers.
        updateTriggerPayloadType:
          $ref: "#/components/schemas/UpdateTriggerPayloadType"
          description: 'Type of Payload body for Base image update triggers. Known values are: "Default" and "Token".'
        status:
          $ref: "#/components/schemas/TriggerStatus"
          description: 'The current status of trigger. Known values are: "Disabled" and "Enabled".'
        name:
          type: string
          description: The name of the trigger. Required.
      required:
        - baseImageTriggerType
        - name
    BaseImageTriggerType:
      type: string
      enum:
        - All
        - Runtime
    UpdateTriggerPayloadType:
      type: string
      enum:
        - Default
        - Token
    Credentials:
      type: object
      description: The parameters that describes a set of credentials that will be used when a run is invoked.
      properties:
        sourceRegistry:
          $ref: "#/components/schemas/SourceRegistryCredentials"
          description: Describes the credential parameters for accessing the source registry.
        customRegistries:
          type: object
          properties: {}
          description: Describes the credential parameters for accessing other custom registries. The key for the dictionary item will be the registry login server (myregistry.azurecr.io) and the value of the item will be the registry credentials for accessing the registry.
    SourceRegistryCredentials:
      type: object
      description: Describes the credential parameters for accessing the source registry.
      properties:
        identity:
          type: string
          description: The Entra identity used for source registry login. The value is `[system]` for system-assigned managed identity, `[caller]` for caller identity, and client ID for user-assigned managed identity.
        loginMode:
          $ref: "#/components/schemas/SourceRegistryLoginMode"
          description: 'The authentication mode which determines the source registry login scope. The credentials for the source registry will be generated using the given scope. These credentials will be used to login to the source registry during the run. Known values are: "None" and "Default".'
    SourceRegistryLoginMode:
      type: string
      enum:
        - None
        - Default
    TaskFlattened:
      type: object
      description: Flattened view of Task ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: Identity for the resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the task. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        creationDate:
          type: string
          format: date-time
          description: The creation date of task.
        status:
          $ref: "#/components/schemas/TaskStatus"
          description: 'The current status of task. Known values are: "Disabled" and "Enabled".'
        platform:
          $ref: "#/components/schemas/PlatformProperties"
          description: The platform properties against which the run has to happen.
        agentConfiguration:
          $ref: "#/components/schemas/AgentProperties"
          description: The machine configuration of the run agent.
        agentPoolName:
          type: string
          description: The dedicated agent pool for the task.
        timeout:
          type: integer
          description: Run timeout in seconds.
        step:
          $ref: "#/components/schemas/TaskStepProperties"
          description: The properties of a task step.
        trigger:
          $ref: "#/components/schemas/TriggerProperties"
          description: The properties that describe all triggers for the task.
        credentials:
          $ref: "#/components/schemas/Credentials"
          description: The properties that describes a set of credentials that will be used when this run is invoked.
        logTemplate:
          type: string
          description: The template that describes the repository and tag information for run log artifact.
        isSystemTask:
          type: boolean
          description: The value of this property indicates whether the task resource is system task or not.
    PagedTaskFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TaskFlattened"
        nextLink:
          type: string
    Task:
      type: object
      description: The task that has the ARM resource and task properties. The task will have all information to schedule a run against it.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/TaskProperties"
          description: The properties of a task.
        identity:
          $ref: "#/components/schemas/IdentityProperties"
          description: Identity for the resource.
      required:
        - location
    TaskPropertiesUpdateParameters:
      type: object
      description: The properties for updating a task.
      properties:
        status:
          $ref: "#/components/schemas/TaskStatus"
          description: 'The current status of task. Known values are: "Disabled" and "Enabled".'
        platform:
          $ref: "#/components/schemas/PlatformUpdateParameters"
          description: The platform properties against which the run has to happen.
        agentConfiguration:
          $ref: "#/components/schemas/AgentProperties"
          description: The machine configuration of the run agent.
        agentPoolName:
          type: string
          description: The dedicated agent pool for the task.
        timeout:
          type: integer
          description: Run timeout in seconds.
        step:
          $ref: "#/components/schemas/TaskStepUpdateParameters"
          description: The properties for updating a task step.
        trigger:
          $ref: "#/components/schemas/TriggerUpdateParameters"
          description: The properties for updating trigger properties.
        credentials:
          $ref: "#/components/schemas/Credentials"
          description: The parameters that describes a set of credentials that will be used when this run is invoked.
        logTemplate:
          type: string
          description: The template that describes the repository and tag information for run log artifact.
    PlatformUpdateParameters:
      type: object
      description: The properties for updating the platform configuration.
      properties:
        os:
          $ref: "#/components/schemas/OS"
          description: 'The operating system type required for the run. Known values are: "Windows" and "Linux".'
        architecture:
          $ref: "#/components/schemas/Architecture"
          description: 'The OS architecture. Known values are: "amd64", "x86", "386", "arm", and "arm64".'
        variant:
          $ref: "#/components/schemas/Variant"
          description: 'Variant of the CPU. Known values are: "v6", "v7", and "v8".'
    TaskStepUpdateParameters:
      type: object
      description: "Base properties for updating any task step. You probably want to use the sub-classes and not this class directly. Known sub-classes are: DockerBuildStepUpdateParameters, EncodedTaskStepUpdateParameters, FileTaskStepUpdateParameters"
      properties:
        type:
          type: string
          description: 'The type of the step. Required. Known values are: "Docker", "FileTask", and "EncodedTask".'
        contextPath:
          type: string
          description: The URL(absolute or relative) of the source context for the task step.
        contextAccessToken:
          type: string
          description: The token (git PAT or SAS token of storage account blob) associated with the context for a step.
      required:
        - type
    TriggerUpdateParameters:
      type: object
      description: The properties for updating triggers.
      properties:
        timerTriggers:
          type: array
          items:
            $ref: "#/components/schemas/TimerTriggerUpdateParameters"
          description: The collection of timer triggers.
        sourceTriggers:
          type: array
          items:
            $ref: "#/components/schemas/SourceTriggerUpdateParameters"
          description: The collection of triggers based on source code repository.
        baseImageTrigger:
          $ref: "#/components/schemas/BaseImageTriggerUpdateParameters"
          description: The trigger based on base image dependencies.
    TimerTriggerUpdateParameters:
      type: object
      description: The properties for updating a timer trigger.
      properties:
        schedule:
          type: string
          description: The CRON expression for the task schedule.
        status:
          $ref: "#/components/schemas/TriggerStatus"
          description: 'The current status of trigger. Known values are: "Disabled" and "Enabled".'
        name:
          type: string
          description: The name of the trigger. Required.
      required:
        - name
    SourceTriggerUpdateParameters:
      type: object
      description: The properties for updating a source based trigger.
      properties:
        sourceRepository:
          $ref: "#/components/schemas/SourceUpdateParameters"
          description: The properties that describes the source(code) for the task.
        sourceTriggerEvents:
          type: array
          items:
            $ref: "#/components/schemas/SourceTriggerEvent"
          description: The source event corresponding to the trigger.
        status:
          $ref: "#/components/schemas/TriggerStatus"
          description: 'The current status of trigger. Known values are: "Disabled" and "Enabled".'
        name:
          type: string
          description: The name of the trigger. Required.
      required:
        - name
    SourceUpdateParameters:
      type: object
      description: The properties for updating the source code repository.
      properties:
        sourceControlType:
          $ref: "#/components/schemas/SourceControlType"
          description: 'The type of source control service. Known values are: "Github" and "VisualStudioTeamService".'
        repositoryUrl:
          type: string
          description: The full URL to the source code repository.
        branch:
          type: string
          description: The branch name of the source code.
        sourceControlAuthProperties:
          $ref: "#/components/schemas/AuthInfoUpdateParameters"
          description: The authorization properties for accessing the source code repository and to set up webhooks for notifications.
    AuthInfoUpdateParameters:
      type: object
      description: The authorization properties for accessing the source code repository.
      properties:
        tokenType:
          $ref: "#/components/schemas/TokenType"
          description: 'The type of Auth token. Known values are: "PAT" and "OAuth".'
        token:
          type: string
          description: The access token used to access the source control provider.
        refreshToken:
          type: string
          description: The refresh token used to refresh the access token.
        scope:
          type: string
          description: The scope of the access token.
        expiresIn:
          type: integer
          description: Time in seconds that the token remains valid.
    BaseImageTriggerUpdateParameters:
      type: object
      description: The properties for updating base image dependency trigger.
      properties:
        baseImageTriggerType:
          $ref: "#/components/schemas/BaseImageTriggerType"
          description: 'The type of the auto trigger for base image dependency updates. Known values are: "All" and "Runtime".'
        updateTriggerEndpoint:
          type: string
          description: The endpoint URL for receiving update triggers.
        updateTriggerPayloadType:
          $ref: "#/components/schemas/UpdateTriggerPayloadType"
          description: 'Type of Payload body for Base image update triggers. Known values are: "Default" and "Token".'
        status:
          $ref: "#/components/schemas/TriggerStatus"
          description: 'The current status of trigger. Known values are: "Disabled" and "Enabled".'
        name:
          type: string
          description: The name of the trigger. Required.
      required:
        - name
  x-stackQL-resources:
    agent_pools:
      id: azure.containerregistrytasks.agent_pools
      name: agent_pools
      title: agent_pools
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1agentPools~1{agent_pool_name}?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1agentPools~1{agent_pool_name}?api-version=2025-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1agentPools~1{agent_pool_name}?api-version=2025-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1agentPools~1{agent_pool_name}?api-version=2025-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1agentPools?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_queue_status:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1agentPools~1{agent_pool_name}~1listQueueStatus?api-version=2025-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/agent_pools/methods/get"
          - $ref: "#/components/x-stackQL-resources/agent_pools/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/agent_pools/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/agent_pools/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/agent_pools/methods/delete"
    registries:
      id: azure.containerregistrytasks.registries
      name: registries
      title: registries
      methods:
        get_build_source_upload_url:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1listBuildSourceUploadUrl?api-version=2025-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        schedule_run:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1scheduleRun?api-version=2025-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
    runs:
      id: azure.containerregistrytasks.runs
      name: runs
      title: runs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1runs~1{run_id}?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1runs~1{run_id}?api-version=2025-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1runs?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        get_log_sas_url:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1runs~1{run_id}~1listLogSasUrl?api-version=2025-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        cancel:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1runs~1{run_id}~1cancel?api-version=2025-03-01-preview/post"
          response:
            openAPIDocKey: "200"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/runs/methods/get"
          - $ref: "#/components/x-stackQL-resources/runs/methods/list"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/runs/methods/update"
        delete: []
    task_runs:
      id: azure.containerregistrytasks.task_runs
      name: task_runs
      title: task_runs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1taskRuns~1{task_run_name}?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1taskRuns~1{task_run_name}?api-version=2025-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1taskRuns~1{task_run_name}?api-version=2025-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1taskRuns~1{task_run_name}?api-version=2025-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1taskRuns?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_details:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1taskRuns~1{task_run_name}~1listDetails?api-version=2025-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/task_runs/methods/get"
          - $ref: "#/components/x-stackQL-resources/task_runs/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/task_runs/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/task_runs/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/task_runs/methods/delete"
    tasks:
      id: azure.containerregistrytasks.tasks
      name: tasks
      title: tasks
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1tasks~1{task_name}?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1tasks~1{task_name}?api-version=2025-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1tasks~1{task_name}?api-version=2025-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1tasks~1{task_name}?api-version=2025-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1tasks?api-version=2025-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_details:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ContainerRegistry~1registries~1{registry_name}~1tasks~1{task_name}~1listDetails?api-version=2025-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/get"
          - $ref: "#/components/x-stackQL-resources/tasks/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/tasks/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/agentPools/{agent_pool_name}?api-version=2025-03-01-preview:
    get:
      operationId: AgentPools_get
      description: Gets the detailed information for a given agent pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: agent_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the agent pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentPoolFlattened"
    put:
      operationId: AgentPools_begin_create
      description: Creates an agent pool for a container registry with the specified parameters.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: agent_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the agent pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AgentPoolProperties"
                  description: The properties associated with the agent pool.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentPool"
    patch:
      operationId: AgentPools_begin_update
      description: Updates an agent pool with the specified parameters.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: agent_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the agent pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/AgentPoolPropertiesUpdateParameters"
                  description: The properties associated with the agent pool.
                tags:
                  type: object
                  properties: {}
                  description: The ARM resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentPool"
    delete:
      operationId: AgentPools_begin_delete
      description: Deletes a specified agent pool resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: agent_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the agent pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/agentPools?api-version=2025-03-01-preview:
    get:
      operationId: AgentPools_list
      description: Lists all the agent pools for a specified container registry.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAgentPoolFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/agentPools/{agent_pool_name}/listQueueStatus?api-version=2025-03-01-preview:
    post:
      operationId: AgentPools_get_queue_status
      description: Gets the count of queued runs for a given agent pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: agent_pool_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the agent pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentPoolQueueStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/listBuildSourceUploadUrl?api-version=2025-03-01-preview:
    post:
      operationId: Registries_get_build_source_upload_url
      description: Get the upload location for the user to be able to upload the source.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the container registry. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceUploadDefinition"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/scheduleRun?api-version=2025-03-01-preview:
    post:
      operationId: Registries_schedule_run
      description: Schedules a new run based on the request parameters and add it to the run queue.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the container registry. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: The type of the run request. Required. Default value is None.
                isArchiveEnabled:
                  type: boolean
                  description: The value that indicates whether archiving is enabled for the run or not.
                agentPoolName:
                  type: string
                  description: The dedicated agent pool for the run.
                logTemplate:
                  type: string
                  description: The template that describes the repository and tag information for run log artifact.
              required:
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Run"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/runs/{run_id}?api-version=2025-03-01-preview:
    get:
      operationId: Runs_get
      description: Gets the detailed information for a given run.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: run_id
          in: path
          required: true
          schema:
            type: string
          description: The run ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunFlattened"
    patch:
      operationId: Runs_update
      description: Patch the run properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: run_id
          in: path
          required: true
          schema:
            type: string
          description: The run ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                isArchiveEnabled:
                  type: boolean
                  description: The value that indicates whether archiving is enabled or not.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Run"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/runs?api-version=2025-03-01-preview:
    get:
      operationId: Runs_list
      description: Gets all the runs for a registry.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The runs filter to apply on the operation. Arithmetic operators are not supported. The allowed string function is 'contains'. All logical operators except 'Not', 'Has', 'All' are allowed. Default value is None.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: $top is supported for get list of runs, which limits the maximum number of runs to return. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRunFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/runs/{run_id}/listLogSasUrl?api-version=2025-03-01-preview:
    post:
      operationId: Runs_get_log_sas_url
      description: Gets a link to download the run logs.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: run_id
          in: path
          required: true
          schema:
            type: string
          description: The run ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunGetLogResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/runs/{run_id}/cancel?api-version=2025-03-01-preview:
    post:
      operationId: Runs_cancel
      description: Cancel an existing run.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: run_id
          in: path
          required: true
          schema:
            type: string
          description: The run ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/taskRuns/{task_run_name}?api-version=2025-03-01-preview:
    get:
      operationId: TaskRuns_get
      description: Gets the detailed information for a given task run.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_run_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the task run. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskRunFlattened"
    put:
      operationId: TaskRuns_begin_create
      description: Creates a task run for a container registry with the specified parameters.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_run_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the task run. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/TaskRunProperties"
                  description: The properties associated with the task run, i.e., request and result of the run.
                identity:
                  $ref: "#/components/schemas/IdentityProperties"
                  description: Identity for the resource.
                location:
                  type: string
                  description: The location of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskRun"
    patch:
      operationId: TaskRuns_begin_update
      description: Updates a task run with the specified parameters.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_run_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the task run. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/IdentityProperties"
                  description: Identity for the resource.
                properties:
                  $ref: "#/components/schemas/TaskRunPropertiesUpdateParameters"
                  description: The properties for updating a task run.
                location:
                  type: string
                  description: The location of the resource.
                tags:
                  type: object
                  properties: {}
                  description: The ARM resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskRun"
    delete:
      operationId: TaskRuns_delete
      description: Deletes a specified task run resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_run_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the task run. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/taskRuns?api-version=2025-03-01-preview:
    get:
      operationId: TaskRuns_list
      description: Lists all the task runs for a specified container registry.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTaskRunFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/taskRuns/{task_run_name}/listDetails?api-version=2025-03-01-preview:
    post:
      operationId: TaskRuns_get_details
      description: Gets the detailed information for a given task run that includes all secrets.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_run_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the task run. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskRun"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/tasks/{task_name}?api-version=2025-03-01-preview:
    get:
      operationId: Tasks_get
      description: Get the properties of a specified task.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the container registry task. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskFlattened"
    put:
      operationId: Tasks_create
      description: Creates a task for a container registry with the specified parameters.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the container registry task. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/TaskProperties"
                  description: The properties of a task.
                identity:
                  $ref: "#/components/schemas/IdentityProperties"
                  description: Identity for the resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
    patch:
      operationId: Tasks_update
      description: Updates a task with the specified parameters.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the container registry task. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/IdentityProperties"
                  description: Identity for the resource.
                properties:
                  $ref: "#/components/schemas/TaskPropertiesUpdateParameters"
                  description: The properties for updating a task.
                tags:
                  type: object
                  properties: {}
                  description: The ARM resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
    delete:
      operationId: Tasks_delete
      description: Deletes a specified task.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the container registry task. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/tasks?api-version=2025-03-01-preview:
    get:
      operationId: Tasks_list
      description: Lists all the tasks for a specified container registry.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTaskFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ContainerRegistry/registries/{registry_name}/tasks/{task_name}/listDetails?api-version=2025-03-01-preview:
    post:
      operationId: Tasks_get_details
      description: Returns a task with extended information that includes all secrets.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: registry_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Registry. Required.
        - name: task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the container registry task. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
