openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: _ S D K Client
  description: This is the Microsoft Azure Kubernetes Configuration Management Client Library.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-05-01-stackql-generated
  x-serviceAlias: kubernetesconfiguration
  x-sdkPackage: azure-mgmt-kubernetesconfiguration
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    Identity:
      type: object
      description: Identity for the resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        principalId:
          type: string
          description: The principal ID of resource identity.
        tenantId:
          type: string
          description: The tenant ID of resource.
        type:
          type: string
          description: The identity type. Default value is "SystemAssigned".
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    Plan:
      type: object
      description: Plan for the resource. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: A user defined name of the 3rd Party Artifact that is being procured. Required.
        publisher:
          type: string
          description: The publisher of the 3rd Party Artifact that is being bought. E.g. NewRelic. Required.
        product:
          type: string
          description: The 3rd Party artifact that is being procured. E.g. NewRelic. Product maps to the OfferID specified for the artifact at the time of Data Market onboarding. Required.
        promotionCode:
          type: string
          description: A publisher provided promotion code as provisioned in Data Market for the said product/artifact.
        version:
          type: string
          description: The version of the desired product/artifact.
      required:
        - name
        - publisher
        - product
    Scope:
      type: object
      description: Scope of the extension. It can be either Cluster or Namespace; but not both.
      properties:
        cluster:
          $ref: "#/components/schemas/ScopeCluster"
          description: Specifies that the scope of the extension is Cluster.
        namespace:
          $ref: "#/components/schemas/ScopeNamespace"
          description: Specifies that the scope of the extension is Namespace.
    ScopeCluster:
      type: object
      description: Specifies that the scope of the extension is Cluster.
      properties:
        releaseNamespace:
          type: string
          description: Namespace where the extension Release must be placed, for a Cluster scoped extension. If this namespace does not exist, it will be created.
    ScopeNamespace:
      type: object
      description: Specifies that the scope of the extension is Namespace.
      properties:
        targetNamespace:
          type: string
          description: Namespace where the extension will be created for an Namespace scoped extension. If this namespace does not exist, it will be created.
    ExtensionStatus:
      type: object
      description: Status from the extension.
      properties:
        code:
          type: string
          description: Status code provided by the Extension.
        displayStatus:
          type: string
          description: Short description of status of the extension.
        level:
          type: string
          description: 'Level of the status. Known values are: "Error", "Warning", and "Information".'
        message:
          type: string
          description: Detailed message of the status from the Extension.
        time:
          type: string
          description: DateLiteral (per ISO8601) noting the time of installation status.
    ErrorDetail:
      type: object
      description: The error detail. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        code:
          type: string
          description: The error code.
        message:
          type: string
          description: The error message.
        target:
          type: string
          description: The error target.
        details:
          type: array
          items:
            $ref: "#/components/schemas/ErrorDetail"
          description: The error details.
        additionalInfo:
          type: array
          items:
            $ref: "#/components/schemas/ErrorAdditionalInfo"
          description: The error additional info.
    ErrorAdditionalInfo:
      type: object
      description: The resource management error additional info. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        type:
          type: string
          description: The additional info type.
        info:
          type: object
          properties: {}
          description: The additional info.
    ExtensionPropertiesAksAssignedIdentity:
      type: object
      description: Identity of the Extension resource in an AKS cluster. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        principalId:
          type: string
          description: The principal ID of resource identity.
        tenantId:
          type: string
          description: The tenant ID of resource.
        type:
          type: string
          description: 'The identity type. Known values are: "SystemAssigned" and "UserAssigned".'
    ExtensionFlattened:
      type: object
      description: Flattened view of Extension ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        identity:
          $ref: "#/components/schemas/Identity"
          description: Identity of the Extension resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Top level metadata https://github.com/Azure/azure-resource-manager-rpc/blob/master/v1.0/common-api-contracts.md#system-metadata-for-all-azure-resources.
        plan:
          $ref: "#/components/schemas/Plan"
          description: The plan information.
        extensionType:
          type: string
          description: Type of the Extension, of which this resource is an instance of. It must be one of the Extension Types registered with Microsoft.KubernetesConfiguration by the Extension publisher.
        autoUpgradeMinorVersion:
          type: boolean
          description: Flag to note if this extension participates in auto upgrade of minor version, or not.
        releaseTrain:
          type: string
          description: ReleaseTrain this extension participates in for auto-upgrade (e.g. Stable, Preview, etc.) - only if autoUpgradeMinorVersion is 'true'.
        version:
          type: string
          description: User-specified version of the extension for this extension to 'pin'. To use 'version', autoUpgradeMinorVersion must be 'false'.
        scope:
          $ref: "#/components/schemas/Scope"
          description: Scope at which the extension is installed.
        configurationSettings:
          type: object
          properties: {}
          description: Configuration settings, as name-value pairs for configuring this extension.
        configurationProtectedSettings:
          type: object
          properties: {}
          description: Configuration settings that are sensitive, as name-value pairs for configuring this extension.
        currentVersion:
          type: string
          description: Currently installed version of the extension.
        provisioningState:
          type: string
          description: 'Status of installation of this extension. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Updating", and "Deleting".'
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ExtensionStatus"
          description: Status from this extension.
        errorInfo:
          $ref: "#/components/schemas/ErrorDetail"
          description: Error information from the Agent - e.g. errors during installation.
        customLocationSettings:
          type: object
          properties: {}
          description: Custom Location settings properties.
        packageUri:
          type: string
          description: Uri of the Helm package.
        aksAssignedIdentity:
          $ref: "#/components/schemas/ExtensionPropertiesAksAssignedIdentity"
          description: Identity of the Extension resource in an AKS cluster.
        isSystemExtension:
          type: boolean
          description: Flag to note if this extension is a system extension.
    PagedExtensionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ExtensionFlattened"
        nextLink:
          type: string
    Extension:
      type: object
      description: The Extension object. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        identity:
          $ref: "#/components/schemas/Identity"
          description: Identity of the Extension resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Top level metadata https://github.com/Azure/azure-resource-manager-rpc/blob/master/v1.0/common-api-contracts.md#system-metadata-for-all-azure-resources.
        plan:
          $ref: "#/components/schemas/Plan"
          description: The plan information.
        properties:
          type: object
          properties:
            extensionType:
              type: string
              description: Type of the Extension, of which this resource is an instance of. It must be one of the Extension Types registered with Microsoft.KubernetesConfiguration by the Extension publisher.
            autoUpgradeMinorVersion:
              type: boolean
              description: Flag to note if this extension participates in auto upgrade of minor version, or not.
            releaseTrain:
              type: string
              description: ReleaseTrain this extension participates in for auto-upgrade (e.g. Stable, Preview, etc.) - only if autoUpgradeMinorVersion is 'true'.
            version:
              type: string
              description: User-specified version of the extension for this extension to 'pin'. To use 'version', autoUpgradeMinorVersion must be 'false'.
            scope:
              $ref: "#/components/schemas/Scope"
              description: Scope at which the extension is installed.
            configurationSettings:
              type: object
              properties: {}
              description: Configuration settings, as name-value pairs for configuring this extension.
            configurationProtectedSettings:
              type: object
              properties: {}
              description: Configuration settings that are sensitive, as name-value pairs for configuring this extension.
            currentVersion:
              type: string
              description: Currently installed version of the extension.
            provisioningState:
              type: string
              description: 'Status of installation of this extension. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Updating", and "Deleting".'
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ExtensionStatus"
              description: Status from this extension.
            errorInfo:
              $ref: "#/components/schemas/ErrorDetail"
              description: Error information from the Agent - e.g. errors during installation.
            customLocationSettings:
              type: object
              properties: {}
              description: Custom Location settings properties.
            packageUri:
              type: string
              description: Uri of the Helm package.
            aksAssignedIdentity:
              $ref: "#/components/schemas/ExtensionPropertiesAksAssignedIdentity"
              description: Identity of the Extension resource in an AKS cluster.
            isSystemExtension:
              type: boolean
              description: Flag to note if this extension is a system extension.
    OperationStatusResult:
      type: object
      description: The current status of an async operation. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified ID for the async operation.
        name:
          type: string
          description: Name of the async operation.
        status:
          type: string
          description: Operation status. Required.
        properties:
          type: object
          properties: {}
          description: Additional information, if available.
        error:
          $ref: "#/components/schemas/ErrorDetail"
          description: If present, details of the operation error.
      required:
        - status
    GitRepositoryDefinition:
      type: object
      description: Parameters to reconcile to the GitRepository source kind type.
      properties:
        url:
          type: string
          description: The URL to sync for the flux configuration git repository.
        timeoutInSeconds:
          type: integer
          description: The maximum time to attempt to reconcile the cluster git repository source with the remote.
        syncIntervalInSeconds:
          type: integer
          description: The interval at which to re-reconcile the cluster git repository source with the remote.
        repositoryRef:
          $ref: "#/components/schemas/RepositoryRefDefinition"
          description: The source reference for the GitRepository object.
        sshKnownHosts:
          type: string
          description: Base64-encoded known_hosts value containing public SSH keys required to access private git repositories over SSH.
        httpsUser:
          type: string
          description: Plaintext HTTPS username used to access private git repositories over HTTPS.
        httpsCACert:
          type: string
          description: Base64-encoded HTTPS certificate authority contents used to access git private git repositories over HTTPS.
        localAuthRef:
          type: string
          description: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets.
    RepositoryRefDefinition:
      type: object
      description: The source reference for the GitRepository object.
      properties:
        branch:
          type: string
          description: The git repository branch name to checkout.
        tag:
          type: string
          description: The git repository tag name to checkout. This takes precedence over branch.
        semver:
          type: string
          description: The semver range used to match against git repository tags. This takes precedence over tag.
        commit:
          type: string
          description: The commit SHA to checkout. This value must be combined with the branch name to be valid. This takes precedence over semver.
    BucketDefinition:
      type: object
      description: Parameters to reconcile to the Bucket source kind type.
      properties:
        url:
          type: string
          description: The URL to sync for the flux configuration S3 bucket.
        bucketName:
          type: string
          description: The bucket name to sync from the url endpoint for the flux configuration.
        insecure:
          type: boolean
          description: Specify whether to use insecure communication when puling data from the S3 bucket.
        timeoutInSeconds:
          type: integer
          description: The maximum time to attempt to reconcile the cluster bucket source with the remote.
        syncIntervalInSeconds:
          type: integer
          description: The interval at which to re-reconcile the cluster bucket source with the remote.
        accessKey:
          type: string
          description: Plaintext access key used to securely access the S3 bucket.
        localAuthRef:
          type: string
          description: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets.
    AzureBlobDefinition:
      type: object
      description: Parameters to reconcile to the AzureBlob source kind type.
      properties:
        url:
          type: string
          description: The URL to sync for the flux configuration Azure Blob storage account.
        containerName:
          type: string
          description: The Azure Blob container name to sync from the url endpoint for the flux configuration.
        timeoutInSeconds:
          type: integer
          description: The maximum time to attempt to reconcile the cluster Azure Blob source with the remote.
        syncIntervalInSeconds:
          type: integer
          description: The interval at which to re-reconcile the cluster Azure Blob source with the remote.
        servicePrincipal:
          $ref: "#/components/schemas/ServicePrincipalDefinition"
          description: Parameters to authenticate using Service Principal.
        accountKey:
          type: string
          description: The account key (shared key) to access the storage account.
        sasToken:
          type: string
          description: The Shared Access token to access the storage container.
        managedIdentity:
          $ref: "#/components/schemas/ManagedIdentityDefinition"
          description: Parameters to authenticate using a Managed Identity.
        localAuthRef:
          type: string
          description: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets.
    ServicePrincipalDefinition:
      type: object
      description: Parameters to authenticate using Service Principal.
      properties:
        clientId:
          type: string
          description: The client Id for authenticating a Service Principal.
        tenantId:
          type: string
          description: The tenant Id for authenticating a Service Principal.
        clientSecret:
          type: string
          description: The client secret for authenticating a Service Principal.
        clientCertificate:
          type: string
          description: Base64-encoded certificate used to authenticate a Service Principal.
        clientCertificatePassword:
          type: string
          description: The password for the certificate used to authenticate a Service Principal.
        clientCertificateSendChain:
          type: boolean
          description: Specifies whether to include x5c header in client claims when acquiring a token to enable subject name / issuer based authentication for the Client Certificate.
    ManagedIdentityDefinition:
      type: object
      description: Parameters to authenticate using a Managed Identity.
      properties:
        clientId:
          type: string
          description: The client Id for authenticating a Managed Identity.
    ObjectStatusDefinition:
      type: object
      description: Statuses of objects deployed by the user-specified kustomizations from the git repository.
      properties:
        name:
          type: string
          description: Name of the applied object.
        namespace:
          type: string
          description: Namespace of the applied object.
        kind:
          type: string
          description: Kind of the applied object.
        complianceState:
          type: string
          description: 'Compliance state of the applied object showing whether the applied object has come into a ready state on the cluster. Known values are: "Compliant", "Non-Compliant", "Pending", "Suspended", and "Unknown".'
        appliedBy:
          $ref: "#/components/schemas/ObjectReferenceDefinition"
          description: Object reference to the Kustomization that applied this object.
        statusConditions:
          type: array
          items:
            $ref: "#/components/schemas/ObjectStatusConditionDefinition"
          description: List of Kubernetes object status conditions present on the cluster.
        helmReleaseProperties:
          $ref: "#/components/schemas/HelmReleasePropertiesDefinition"
          description: Additional properties that are provided from objects of the HelmRelease kind.
    ObjectReferenceDefinition:
      type: object
      description: Object reference to a Kubernetes object on a cluster.
      properties:
        name:
          type: string
          description: Name of the object.
        namespace:
          type: string
          description: Namespace of the object.
    ObjectStatusConditionDefinition:
      type: object
      description: Status condition of Kubernetes object.
      properties:
        lastTransitionTime:
          type: string
          format: date-time
          description: Last time this status condition has changed.
        message:
          type: string
          description: A more verbose description of the object status condition.
        reason:
          type: string
          description: Reason for the specified status condition type status.
        status:
          type: string
          description: Status of the Kubernetes object condition type.
        type:
          type: string
          description: Object status condition type for this object.
    HelmReleasePropertiesDefinition:
      type: object
      description: Properties for HelmRelease objects.
      properties:
        lastRevisionApplied:
          type: integer
          description: The revision number of the last released object change.
        helmChartRef:
          $ref: "#/components/schemas/ObjectReferenceDefinition"
          description: The reference to the HelmChart object used as the source to this HelmRelease.
        failureCount:
          type: integer
          description: Total number of times that the HelmRelease failed to install or upgrade.
        installFailureCount:
          type: integer
          description: Number of times that the HelmRelease failed to install.
        upgradeFailureCount:
          type: integer
          description: Number of times that the HelmRelease failed to upgrade.
    FluxConfigurationFlattened:
      type: object
      description: Flattened view of FluxConfiguration ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Top level metadata https://github.com/Azure/azure-resource-manager-rpc/blob/master/v1.0/common-api-contracts.md#system-metadata-for-all-azure-resources.
        scope:
          type: string
          description: 'Scope at which the operator will be installed. Known values are: "cluster" and "namespace".'
        namespace:
          type: string
          description: The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only.
        sourceKind:
          type: string
          description: 'Source Kind to pull the configuration data from. Known values are: "GitRepository", "Bucket", and "AzureBlob".'
        suspend:
          type: boolean
          description: Whether this configuration should suspend its reconciliation of its kustomizations and sources.
        gitRepository:
          $ref: "#/components/schemas/GitRepositoryDefinition"
          description: Parameters to reconcile to the GitRepository source kind type.
        bucket:
          $ref: "#/components/schemas/BucketDefinition"
          description: Parameters to reconcile to the Bucket source kind type.
        azureBlob:
          $ref: "#/components/schemas/AzureBlobDefinition"
          description: Parameters to reconcile to the AzureBlob source kind type.
        kustomizations:
          type: object
          properties: {}
          description: Array of kustomizations used to reconcile the artifact pulled by the source type on the cluster.
        configurationProtectedSettings:
          type: object
          properties: {}
          description: Key-value pairs of protected configuration settings for the configuration.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ObjectStatusDefinition"
          description: Statuses of the Flux Kubernetes resources created by the fluxConfiguration or created by the managed objects provisioned by the fluxConfiguration.
        repositoryPublicKey:
          type: string
          description: Public Key associated with this fluxConfiguration (either generated within the cluster or provided by the user).
        sourceSyncedCommitId:
          type: string
          description: Branch and/or SHA of the source commit synced with the cluster.
        sourceUpdatedAt:
          type: string
          format: date-time
          description: Datetime the fluxConfiguration synced its source on the cluster.
        statusUpdatedAt:
          type: string
          format: date-time
          description: Datetime the fluxConfiguration synced its status on the cluster with Azure.
        waitForReconciliation:
          type: boolean
          description: Whether flux configuration deployment should wait for cluster to reconcile the kustomizations.
        reconciliationWaitDuration:
          type: string
          description: Maximum duration to wait for flux configuration reconciliation. E.g PT1H, PT5M, P1D.
        complianceState:
          type: string
          description: 'Combined status of the Flux Kubernetes resources created by the fluxConfiguration or created by the managed objects. Known values are: "Compliant", "Non-Compliant", "Pending", "Suspended", and "Unknown".'
        provisioningState:
          type: string
          description: 'Status of the creation of the fluxConfiguration. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Updating", and "Deleting".'
        errorMessage:
          type: string
          description: Error message returned to the user in the case of provisioning failure.
    PagedFluxConfigurationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FluxConfigurationFlattened"
        nextLink:
          type: string
    FluxConfiguration:
      type: object
      description: The Flux Configuration object returned in Get & Put response. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Top level metadata https://github.com/Azure/azure-resource-manager-rpc/blob/master/v1.0/common-api-contracts.md#system-metadata-for-all-azure-resources.
        properties:
          type: object
          properties:
            scope:
              type: string
              description: 'Scope at which the operator will be installed. Known values are: "cluster" and "namespace".'
            namespace:
              type: string
              description: The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only.
            sourceKind:
              type: string
              description: 'Source Kind to pull the configuration data from. Known values are: "GitRepository", "Bucket", and "AzureBlob".'
            suspend:
              type: boolean
              description: Whether this configuration should suspend its reconciliation of its kustomizations and sources.
            gitRepository:
              $ref: "#/components/schemas/GitRepositoryDefinition"
              description: Parameters to reconcile to the GitRepository source kind type.
            bucket:
              $ref: "#/components/schemas/BucketDefinition"
              description: Parameters to reconcile to the Bucket source kind type.
            azureBlob:
              $ref: "#/components/schemas/AzureBlobDefinition"
              description: Parameters to reconcile to the AzureBlob source kind type.
            kustomizations:
              type: object
              properties: {}
              description: Array of kustomizations used to reconcile the artifact pulled by the source type on the cluster.
            configurationProtectedSettings:
              type: object
              properties: {}
              description: Key-value pairs of protected configuration settings for the configuration.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ObjectStatusDefinition"
              description: Statuses of the Flux Kubernetes resources created by the fluxConfiguration or created by the managed objects provisioned by the fluxConfiguration.
            repositoryPublicKey:
              type: string
              description: Public Key associated with this fluxConfiguration (either generated within the cluster or provided by the user).
            sourceSyncedCommitId:
              type: string
              description: Branch and/or SHA of the source commit synced with the cluster.
            sourceUpdatedAt:
              type: string
              format: date-time
              description: Datetime the fluxConfiguration synced its source on the cluster.
            statusUpdatedAt:
              type: string
              format: date-time
              description: Datetime the fluxConfiguration synced its status on the cluster with Azure.
            waitForReconciliation:
              type: boolean
              description: Whether flux configuration deployment should wait for cluster to reconcile the kustomizations.
            reconciliationWaitDuration:
              type: string
              description: Maximum duration to wait for flux configuration reconciliation. E.g PT1H, PT5M, P1D.
            complianceState:
              type: string
              description: 'Combined status of the Flux Kubernetes resources created by the fluxConfiguration or created by the managed objects. Known values are: "Compliant", "Non-Compliant", "Pending", "Suspended", and "Unknown".'
            provisioningState:
              type: string
              description: 'Status of the creation of the fluxConfiguration. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Updating", and "Deleting".'
            errorMessage:
              type: string
              description: Error message returned to the user in the case of provisioning failure.
    GitRepositoryPatchDefinition:
      type: object
      description: Parameters to reconcile to the GitRepository source kind type.
      properties:
        url:
          type: string
          description: The URL to sync for the flux configuration git repository.
        timeoutInSeconds:
          type: integer
          description: The maximum time to attempt to reconcile the cluster git repository source with the remote.
        syncIntervalInSeconds:
          type: integer
          description: The interval at which to re-reconcile the cluster git repository source with the remote.
        repositoryRef:
          $ref: "#/components/schemas/RepositoryRefDefinition"
          description: The source reference for the GitRepository object.
        sshKnownHosts:
          type: string
          description: Base64-encoded known_hosts value containing public SSH keys required to access private git repositories over SSH.
        httpsUser:
          type: string
          description: Plaintext HTTPS username used to access private git repositories over HTTPS.
        httpsCACert:
          type: string
          description: Base64-encoded HTTPS certificate authority contents used to access git private git repositories over HTTPS.
        localAuthRef:
          type: string
          description: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets.
    BucketPatchDefinition:
      type: object
      description: Parameters to reconcile to the Bucket source kind type.
      properties:
        url:
          type: string
          description: The URL to sync for the flux configuration S3 bucket.
        bucketName:
          type: string
          description: The bucket name to sync from the url endpoint for the flux configuration.
        insecure:
          type: boolean
          description: Specify whether to use insecure communication when puling data from the S3 bucket.
        timeoutInSeconds:
          type: integer
          description: The maximum time to attempt to reconcile the cluster bucket source with the remote.
        syncIntervalInSeconds:
          type: integer
          description: The interval at which to re-reconcile the cluster bucket source with the remote.
        accessKey:
          type: string
          description: Plaintext access key used to securely access the S3 bucket.
        localAuthRef:
          type: string
          description: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets.
    AzureBlobPatchDefinition:
      type: object
      description: Parameters to reconcile to the AzureBlob source kind type.
      properties:
        url:
          type: string
          description: The URL to sync for the flux configuration Azure Blob storage account.
        containerName:
          type: string
          description: The Azure Blob container name to sync from the url endpoint for the flux configuration.
        timeoutInSeconds:
          type: integer
          description: The maximum time to attempt to reconcile the cluster Azure Blob source with the remote.
        syncIntervalInSeconds:
          type: integer
          description: The interval at which to re-reconcile the cluster Azure Blob source with the remote.
        servicePrincipal:
          $ref: "#/components/schemas/ServicePrincipalPatchDefinition"
          description: Parameters to authenticate using Service Principal.
        accountKey:
          type: string
          description: The account key (shared key) to access the storage account.
        sasToken:
          type: string
          description: The Shared Access token to access the storage container.
        managedIdentity:
          $ref: "#/components/schemas/ManagedIdentityPatchDefinition"
          description: Parameters to authenticate using a Managed Identity.
        localAuthRef:
          type: string
          description: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the managed or user-provided configuration secrets.
    ServicePrincipalPatchDefinition:
      type: object
      description: Parameters to authenticate using Service Principal.
      properties:
        clientId:
          type: string
          description: The client Id for authenticating a Service Principal.
        tenantId:
          type: string
          description: The tenant Id for authenticating a Service Principal.
        clientSecret:
          type: string
          description: The client secret for authenticating a Service Principal.
        clientCertificate:
          type: string
          description: Base64-encoded certificate used to authenticate a Service Principal.
        clientCertificatePassword:
          type: string
          description: The password for the certificate used to authenticate a Service Principal.
        clientCertificateSendChain:
          type: boolean
          description: Specifies whether to include x5c header in client claims when acquiring a token to enable subject name / issuer based authentication for the Client Certificate.
    ManagedIdentityPatchDefinition:
      type: object
      description: Parameters to authenticate using a Managed Identity.
      properties:
        clientId:
          type: string
          description: The client Id for authenticating a Managed Identity.
    PagedOperationStatusResult:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OperationStatusResult"
        nextLink:
          type: string
    ResourceProviderOperationDisplay:
      type: object
      description: Display metadata associated with the operation.
      properties:
        provider:
          type: string
          description: "Resource provider: Microsoft KubernetesConfiguration."
        resource:
          type: string
          description: Resource on which the operation is performed.
        operation:
          type: string
          description: "Type of operation: get, read, delete, etc."
        description:
          type: string
          description: Description of this operation.
    ResourceProviderOperation:
      type: object
      description: Supported operation of this resource provider. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Operation name, in format of {provider}/{resource}/{operation}.
        display:
          $ref: "#/components/schemas/ResourceProviderOperationDisplay"
          description: Display metadata associated with the operation.
        isDataAction:
          type: boolean
          description: The flag that indicates whether the operation applies to data plane.
        origin:
          type: string
          description: Origin of the operation.
    PagedResourceProviderOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourceProviderOperation"
        nextLink:
          type: string
    HelmOperatorProperties:
      type: object
      description: Properties for Helm operator.
      properties:
        chartVersion:
          type: string
          description: Version of the operator Helm chart.
        chartValues:
          type: string
          description: Values override for the operator Helm chart.
    ComplianceStatus:
      type: object
      description: Compliance Status details. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        complianceState:
          type: string
          description: 'The compliance state of the configuration. Known values are: "Pending", "Compliant", "Noncompliant", "Installed", and "Failed".'
        lastConfigApplied:
          type: string
          format: date-time
          description: Datetime the configuration was last applied.
        message:
          type: string
          description: Message from when the configuration was applied.
        messageLevel:
          type: string
          description: 'Level of the message. Known values are: "Error", "Warning", and "Information".'
    SourceControlConfigurationFlattened:
      type: object
      description: Flattened view of SourceControlConfiguration ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Top level metadata https://github.com/Azure/azure-resource-manager-rpc/blob/master/v1.0/common-api-contracts.md#system-metadata-for-all-azure-resources.
        repositoryUrl:
          type: string
          description: Url of the SourceControl Repository.
        operatorNamespace:
          type: string
          description: The namespace to which this operator is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only.
        operatorInstanceName:
          type: string
          description: Instance name of the operator - identifying the specific configuration.
        operatorType:
          type: string
          description: Type of the operator. "Flux"
        operatorParams:
          type: string
          description: Any Parameters for the Operator instance in string format.
        configurationProtectedSettings:
          type: object
          properties: {}
          description: Name-value pairs of protected configuration settings for the configuration.
        operatorScope:
          type: string
          description: 'Scope at which the operator will be installed. Known values are: "cluster" and "namespace".'
        repositoryPublicKey:
          type: string
          description: Public Key associated with this SourceControl configuration (either generated within the cluster or provided by the user).
        sshKnownHostsContents:
          type: string
          description: Base64-encoded known_hosts contents containing public SSH keys required to access private Git instances.
        enableHelmOperator:
          type: boolean
          description: Option to enable Helm Operator for this git configuration.
        helmOperatorProperties:
          $ref: "#/components/schemas/HelmOperatorProperties"
          description: Properties for Helm operator.
        provisioningState:
          type: string
          description: 'The provisioning state of the resource provider. Known values are: "Accepted", "Deleting", "Running", "Succeeded", and "Failed".'
        complianceStatus:
          $ref: "#/components/schemas/ComplianceStatus"
          description: Compliance Status of the Configuration.
    PagedSourceControlConfigurationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SourceControlConfigurationFlattened"
        nextLink:
          type: string
    SourceControlConfiguration:
      type: object
      description: The SourceControl Configuration object returned in Get & Put response. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Top level metadata https://github.com/Azure/azure-resource-manager-rpc/blob/master/v1.0/common-api-contracts.md#system-metadata-for-all-azure-resources.
        properties:
          type: object
          properties:
            repositoryUrl:
              type: string
              description: Url of the SourceControl Repository.
            operatorNamespace:
              type: string
              description: The namespace to which this operator is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only.
            operatorInstanceName:
              type: string
              description: Instance name of the operator - identifying the specific configuration.
            operatorType:
              type: string
              description: Type of the operator. "Flux"
            operatorParams:
              type: string
              description: Any Parameters for the Operator instance in string format.
            configurationProtectedSettings:
              type: object
              properties: {}
              description: Name-value pairs of protected configuration settings for the configuration.
            operatorScope:
              type: string
              description: 'Scope at which the operator will be installed. Known values are: "cluster" and "namespace".'
            repositoryPublicKey:
              type: string
              description: Public Key associated with this SourceControl configuration (either generated within the cluster or provided by the user).
            sshKnownHostsContents:
              type: string
              description: Base64-encoded known_hosts contents containing public SSH keys required to access private Git instances.
            enableHelmOperator:
              type: boolean
              description: Option to enable Helm Operator for this git configuration.
            helmOperatorProperties:
              $ref: "#/components/schemas/HelmOperatorProperties"
              description: Properties for Helm operator.
            provisioningState:
              type: string
              description: 'The provisioning state of the resource provider. Known values are: "Accepted", "Deleting", "Running", "Succeeded", and "Failed".'
            complianceStatus:
              $ref: "#/components/schemas/ComplianceStatus"
              description: Compliance Status of the Configuration.
  x-stackQL-resources:
    extensions:
      id: azure.kubernetesconfiguration.extensions
      name: extensions
      title: extensions
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1extensions~1{extension_name}?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1extensions~1{extension_name}?api-version=2023-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1extensions~1{extension_name}?api-version=2023-05-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1extensions~1{extension_name}?api-version=2023-05-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1extensions?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/extensions/methods/get"
          - $ref: "#/components/x-stackQL-resources/extensions/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/extensions/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/extensions/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/extensions/methods/delete"
    flux_config_operation_status:
      id: azure.kubernetesconfiguration.flux_config_operation_status
      name: flux_config_operation_status
      title: flux_config_operation_status
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1fluxConfigurations~1{flux_configuration_name}~1operations~1{operation_id}?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/flux_config_operation_status/methods/get"
        insert: []
        update: []
        delete: []
    flux_configurations:
      id: azure.kubernetesconfiguration.flux_configurations
      name: flux_configurations
      title: flux_configurations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1fluxConfigurations~1{flux_configuration_name}?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1fluxConfigurations~1{flux_configuration_name}?api-version=2023-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1fluxConfigurations~1{flux_configuration_name}?api-version=2023-05-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1fluxConfigurations~1{flux_configuration_name}?api-version=2023-05-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1fluxConfigurations?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/flux_configurations/methods/get"
          - $ref: "#/components/x-stackQL-resources/flux_configurations/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/flux_configurations/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/flux_configurations/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/flux_configurations/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/flux_configurations/methods/delete"
    operation_status:
      id: azure.kubernetesconfiguration.operation_status
      name: operation_status
      title: operation_status
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1extensions~1{extension_name}~1operations~1{operation_id}?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1operations?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operation_status/methods/get"
          - $ref: "#/components/x-stackQL-resources/operation_status/methods/list"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.kubernetesconfiguration.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.KubernetesConfiguration~1operations?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    source_control_configurations:
      id: azure.kubernetesconfiguration.source_control_configurations
      name: source_control_configurations
      title: source_control_configurations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1sourceControlConfigurations~1{source_control_configuration_name}?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1sourceControlConfigurations~1{source_control_configuration_name}?api-version=2023-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1sourceControlConfigurations~1{source_control_configuration_name}?api-version=2023-05-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{cluster_rp}~1{cluster_resource_name}~1{cluster_name}~1providers~1Microsoft.KubernetesConfiguration~1sourceControlConfigurations?api-version=2023-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/source_control_configurations/methods/get"
          - $ref: "#/components/x-stackQL-resources/source_control_configurations/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/source_control_configurations/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/source_control_configurations/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/source_control_configurations/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/extensions/{extension_name}?api-version=2023-05-01:
    get:
      operationId: Extensions_get
      description: Gets Kubernetes Cluster Extension.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: extension_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Extension. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExtensionFlattened"
    put:
      operationId: Extensions_begin_create
      description: Create a new Kubernetes Cluster Extension.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: extension_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Extension. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: Identity of the Extension resource.
                plan:
                  $ref: "#/components/schemas/Plan"
                  description: The plan information.
                properties:
                  type: object
                  properties:
                    extensionType:
                      type: string
                      description: Type of the Extension, of which this resource is an instance of. It must be one of the Extension Types registered with Microsoft.KubernetesConfiguration by the Extension publisher.
                    autoUpgradeMinorVersion:
                      type: boolean
                      description: Flag to note if this extension participates in auto upgrade of minor version, or not.
                    releaseTrain:
                      type: string
                      description: ReleaseTrain this extension participates in for auto-upgrade (e.g. Stable, Preview, etc.) - only if autoUpgradeMinorVersion is 'true'.
                    version:
                      type: string
                      description: User-specified version of the extension for this extension to 'pin'. To use 'version', autoUpgradeMinorVersion must be 'false'.
                    scope:
                      $ref: "#/components/schemas/Scope"
                      description: Scope at which the extension is installed.
                    configurationSettings:
                      type: object
                      properties: {}
                      description: Configuration settings, as name-value pairs for configuring this extension.
                    configurationProtectedSettings:
                      type: object
                      properties: {}
                      description: Configuration settings that are sensitive, as name-value pairs for configuring this extension.
                    statuses:
                      type: array
                      items:
                        $ref: "#/components/schemas/ExtensionStatus"
                      description: Status from this extension.
                    aksAssignedIdentity:
                      $ref: "#/components/schemas/ExtensionPropertiesAksAssignedIdentity"
                      description: Identity of the Extension resource in an AKS cluster.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Extension"
    delete:
      operationId: Extensions_begin_delete
      description: Delete a Kubernetes Cluster Extension. This will cause the Agent to Uninstall the extension from the cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: extension_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Extension. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: forceDelete
          in: query
          required: false
          schema:
            type: boolean
          description: Delete the extension resource in Azure - not the normal asynchronous delete. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Extensions_begin_update
      description: Patch an existing Kubernetes Cluster Extension.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: extension_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Extension. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    autoUpgradeMinorVersion:
                      type: boolean
                      description: Flag to note if this extension participates in auto upgrade of minor version, or not.
                    releaseTrain:
                      type: string
                      description: ReleaseTrain this extension participates in for auto-upgrade (e.g. Stable, Preview, etc.) - only if autoUpgradeMinorVersion is 'true'.
                    version:
                      type: string
                      description: Version of the extension for this extension, if it is 'pinned' to a specific version. autoUpgradeMinorVersion must be 'false'.
                    configurationSettings:
                      type: object
                      properties: {}
                      description: Configuration settings, as name-value pairs for configuring this extension.
                    configurationProtectedSettings:
                      type: object
                      properties: {}
                      description: Configuration settings that are sensitive, as name-value pairs for configuring this extension.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Extension"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/extensions?api-version=2023-05-01:
    get:
      operationId: Extensions_list
      description: List all Extensions in the cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedExtensionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/fluxConfigurations/{flux_configuration_name}/operations/{operation_id}?api-version=2023-05-01:
    get:
      operationId: FluxConfigOperationStatus_get
      description: Get Async Operation status.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: flux_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Flux Configuration. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: operation Id. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatusResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/fluxConfigurations/{flux_configuration_name}?api-version=2023-05-01:
    get:
      operationId: FluxConfigurations_get
      description: Gets details of the Flux Configuration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: flux_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Flux Configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FluxConfigurationFlattened"
    put:
      operationId: FluxConfigurations_begin_create_or_update
      description: Create a new Kubernetes Flux Configuration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: flux_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Flux Configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    scope:
                      type: string
                      description: 'Scope at which the operator will be installed. Known values are: "cluster" and "namespace".'
                    namespace:
                      type: string
                      description: The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only.
                    sourceKind:
                      type: string
                      description: 'Source Kind to pull the configuration data from. Known values are: "GitRepository", "Bucket", and "AzureBlob".'
                    suspend:
                      type: boolean
                      description: Whether this configuration should suspend its reconciliation of its kustomizations and sources.
                    gitRepository:
                      $ref: "#/components/schemas/GitRepositoryDefinition"
                      description: Parameters to reconcile to the GitRepository source kind type.
                    bucket:
                      $ref: "#/components/schemas/BucketDefinition"
                      description: Parameters to reconcile to the Bucket source kind type.
                    azureBlob:
                      $ref: "#/components/schemas/AzureBlobDefinition"
                      description: Parameters to reconcile to the AzureBlob source kind type.
                    kustomizations:
                      type: object
                      properties: {}
                      description: Array of kustomizations used to reconcile the artifact pulled by the source type on the cluster.
                    configurationProtectedSettings:
                      type: object
                      properties: {}
                      description: Key-value pairs of protected configuration settings for the configuration.
                    waitForReconciliation:
                      type: boolean
                      description: Whether flux configuration deployment should wait for cluster to reconcile the kustomizations.
                    reconciliationWaitDuration:
                      type: string
                      description: Maximum duration to wait for flux configuration reconciliation. E.g PT1H, PT5M, P1D.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FluxConfiguration"
    patch:
      operationId: FluxConfigurations_begin_update
      description: Update an existing Kubernetes Flux Configuration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: flux_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Flux Configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    sourceKind:
                      type: string
                      description: 'Source Kind to pull the configuration data from. Known values are: "GitRepository", "Bucket", and "AzureBlob".'
                    suspend:
                      type: boolean
                      description: Whether this configuration should suspend its reconciliation of its kustomizations and sources.
                    gitRepository:
                      $ref: "#/components/schemas/GitRepositoryPatchDefinition"
                      description: Parameters to reconcile to the GitRepository source kind type.
                    bucket:
                      $ref: "#/components/schemas/BucketPatchDefinition"
                      description: Parameters to reconcile to the Bucket source kind type.
                    azureBlob:
                      $ref: "#/components/schemas/AzureBlobPatchDefinition"
                      description: Parameters to reconcile to the AzureBlob source kind type.
                    kustomizations:
                      type: object
                      properties: {}
                      description: Array of kustomizations used to reconcile the artifact pulled by the source type on the cluster.
                    configurationProtectedSettings:
                      type: object
                      properties: {}
                      description: Key-value pairs of protected configuration settings for the configuration.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FluxConfiguration"
    delete:
      operationId: FluxConfigurations_begin_delete
      description: This will delete the YAML file used to set up the Flux Configuration, thus stopping future sync from the source repo.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: flux_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Flux Configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: forceDelete
          in: query
          required: false
          schema:
            type: boolean
          description: Delete the extension resource in Azure - not the normal asynchronous delete. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/fluxConfigurations?api-version=2023-05-01:
    get:
      operationId: FluxConfigurations_list
      description: List all Flux Configurations.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFluxConfigurationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/extensions/{extension_name}/operations/{operation_id}?api-version=2023-05-01:
    get:
      operationId: OperationStatus_get
      description: Get Async Operation status.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: extension_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Extension. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: operation Id. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatusResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/operations?api-version=2023-05-01:
    get:
      operationId: OperationStatus_list
      description: List Async Operations, currently in progress, in a cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperationStatusResult"
  /providers/Microsoft.KubernetesConfiguration/operations?api-version=2023-05-01:
    get:
      operationId: kubernetesconfiguration_list
      description: List all the available operations the KubernetesConfiguration resource provider supports.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourceProviderOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/sourceControlConfigurations/{source_control_configuration_name}?api-version=2023-05-01:
    get:
      operationId: SourceControlConfigurations_get
      description: Gets details of the Source Control Configuration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: source_control_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Source Control Configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceControlConfigurationFlattened"
    put:
      operationId: SourceControlConfigurations_create_or_update
      description: Create a new Kubernetes Source Control Configuration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: source_control_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Source Control Configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    repositoryUrl:
                      type: string
                      description: Url of the SourceControl Repository.
                    operatorNamespace:
                      type: string
                      description: The namespace to which this operator is installed to. Maximum of 253 lower case alphanumeric characters, hyphen and period only.
                    operatorInstanceName:
                      type: string
                      description: Instance name of the operator - identifying the specific configuration.
                    operatorType:
                      type: string
                      description: Type of the operator. "Flux"
                    operatorParams:
                      type: string
                      description: Any Parameters for the Operator instance in string format.
                    configurationProtectedSettings:
                      type: object
                      properties: {}
                      description: Name-value pairs of protected configuration settings for the configuration.
                    operatorScope:
                      type: string
                      description: 'Scope at which the operator will be installed. Known values are: "cluster" and "namespace".'
                    sshKnownHostsContents:
                      type: string
                      description: Base64-encoded known_hosts contents containing public SSH keys required to access private Git instances.
                    enableHelmOperator:
                      type: boolean
                      description: Option to enable Helm Operator for this git configuration.
                    helmOperatorProperties:
                      $ref: "#/components/schemas/HelmOperatorProperties"
                      description: Properties for Helm operator.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceControlConfiguration"
    delete:
      operationId: SourceControlConfigurations_begin_delete
      description: This will delete the YAML file used to set up the Source control configuration, thus stopping future sync from the source repo.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: source_control_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Source Control Configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{cluster_rp}/{cluster_resource_name}/{cluster_name}/providers/Microsoft.KubernetesConfiguration/sourceControlConfigurations?api-version=2023-05-01:
    get:
      operationId: SourceControlConfigurations_list
      description: List all Source Control Configurations.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_rp
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster RP - i.e. Microsoft.ContainerService, Microsoft.Kubernetes, Microsoft.HybridContainerService. Required.
        - name: cluster_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Kubernetes cluster resource name - i.e. managedClusters, connectedClusters, provisionedClusters. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the kubernetes cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSourceControlConfigurationFlattened"
