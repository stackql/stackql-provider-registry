openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Managed Ops Mgmt Client
  description: Managed Operations API.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-07-28-preview-stackql-generated
  x-serviceAlias: managedops
  x-sdkPackage: azure-mgmt-managedops
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    ManagedOpsProperties:
      type: object
      description: Properties of the ManagedOps resource.
      properties:
        sku:
          $ref: "#/components/schemas/Sku"
          description: Product plan details of this resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", and "Deleting".'
        desiredConfiguration:
          $ref: "#/components/schemas/DesiredConfiguration"
          description: Desired configuration input by the user. Required.
        services:
          $ref: "#/components/schemas/ServiceInformation"
          description: Services provisioned by this resource.
        policyAssignmentProperties:
          $ref: "#/components/schemas/PolicyAssignmentProperties"
          description: Policy assignments created for managing services.
      required:
        - desiredConfiguration
    Sku:
      type: object
      description: Specifies the service plan for this resource.
      properties:
        name:
          type: string
          description: Name of the SKU. Required.
        tier:
          type: string
          description: Pricing tier of the SKU. Required.
      required:
        - name
        - tier
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Deleting
    DesiredConfiguration:
      type: object
      description: Desired configuration input by the user.
      properties:
        changeTrackingAndInventory:
          $ref: "#/components/schemas/ChangeTrackingConfiguration"
          description: Configuration for the Change Tracking and Inventory service. Required.
        azureMonitorInsights:
          $ref: "#/components/schemas/AzureMonitorConfiguration"
          description: Configuration for the Azure Monitor Insights service. Required.
        userAssignedManagedIdentityId:
          type: string
          description: User assigned Managed Identity used to perform operations on machines managed by Ops360. Required.
        defenderForServers:
          $ref: "#/components/schemas/DesiredEnablementState"
          description: 'Desired enablement state of the Defender For Servers service. Known values are: "Enable" and "Disable".'
        defenderCspm:
          $ref: "#/components/schemas/DesiredEnablementState"
          description: 'Desired enablement state of the Defender Cloud Security Posture Management (CSPM) service. Known values are: "Enable" and "Disable".'
      required:
        - changeTrackingAndInventory
        - azureMonitorInsights
        - userAssignedManagedIdentityId
    ChangeTrackingConfiguration:
      type: object
      description: Configuration for the Change Tracking and Inventory service.
      properties:
        logAnalyticsWorkspaceId:
          type: string
          description: Log analytics workspace resource ID used by the service. Required.
      required:
        - logAnalyticsWorkspaceId
    AzureMonitorConfiguration:
      type: object
      description: Configuration for the Azure Monitor Insights service.
      properties:
        azureMonitorWorkspaceId:
          type: string
          description: Azure monitor workspace resource ID used by the service. Required.
      required:
        - azureMonitorWorkspaceId
    DesiredEnablementState:
      type: string
      enum:
        - Enable
        - Disable
    ServiceInformation:
      type: object
      description: Services provisioned by this resource.
      properties:
        changeTrackingAndInventory:
          $ref: "#/components/schemas/ChangeTrackingInformation"
          description: Change Tracking and Inventory service information.
        azureMonitorInsights:
          $ref: "#/components/schemas/AzureMonitorInformation"
          description: Azure Monitor Insights service information.
        azureUpdateManager:
          $ref: "#/components/schemas/UpdateManagerInformation"
          description: Azure Update Manager service information.
        azurePolicyAndMachineConfiguration:
          $ref: "#/components/schemas/GuestConfigurationInformation"
          description: Azure Policy and Machine Configuration service information.
        defenderForServers:
          $ref: "#/components/schemas/DefenderForServersInformation"
          description: Defender for Servers service information.
        defenderCspm:
          $ref: "#/components/schemas/DefenderCspmInformation"
          description: Defender for Cloud's Cloud security posture management (CSPM) service information.
    ChangeTrackingInformation:
      type: object
      description: Change Tracking and Inventory service information.
      properties:
        dcrId:
          type: string
          description: ID of Data Collection Rule (DCR) associated with this service. Required.
        enablementStatus:
          $ref: "#/components/schemas/EnablementState"
          description: 'Indicates whether the service is enabled. Required. Known values are: "Enabled", "InProgress", "Failed", and "Disabled".'
      required:
        - dcrId
        - enablementStatus
    EnablementState:
      type: string
      enum:
        - Enabled
        - InProgress
        - Failed
        - Disabled
    AzureMonitorInformation:
      type: object
      description: Azure Monitor Insights service information.
      properties:
        dcrId:
          type: string
          description: ID of Data Collection Rule (DCR) associated with this service. Required.
        enablementStatus:
          $ref: "#/components/schemas/EnablementState"
          description: 'Indicates whether the service is enabled. Required. Known values are: "Enabled", "InProgress", "Failed", and "Disabled".'
      required:
        - dcrId
        - enablementStatus
    UpdateManagerInformation:
      type: object
      description: Azure Update Manager service information.
      properties:
        enablementStatus:
          $ref: "#/components/schemas/EnablementState"
          description: 'Indicates whether the service is enabled. Required. Known values are: "Enabled", "InProgress", "Failed", and "Disabled".'
      required:
        - enablementStatus
    GuestConfigurationInformation:
      type: object
      description: Azure Policy and Machine Configuration service information.
      properties:
        enablementStatus:
          $ref: "#/components/schemas/EnablementState"
          description: 'Indicates whether the service is enabled. Required. Known values are: "Enabled", "InProgress", "Failed", and "Disabled".'
      required:
        - enablementStatus
    DefenderForServersInformation:
      type: object
      description: Defender for Servers service information.
      properties:
        enablementStatus:
          $ref: "#/components/schemas/EnablementState"
          description: 'Indicates whether the service is enabled. Required. Known values are: "Enabled", "InProgress", "Failed", and "Disabled".'
      required:
        - enablementStatus
    DefenderCspmInformation:
      type: object
      description: Defender Cloud Security Posture Management (CSPM) service information.
      properties:
        enablementStatus:
          $ref: "#/components/schemas/EnablementState"
          description: 'Indicates whether the service is enabled. Required. Known values are: "Enabled", "InProgress", "Failed", and "Disabled".'
      required:
        - enablementStatus
    PolicyAssignmentProperties:
      type: object
      description: Policy assignments created for managing services.
      properties:
        policyInitiativeAssignmentId:
          type: string
          description: Policy initiative assignment ID. Required.
      required:
        - policyInitiativeAssignmentId
    ManagedOpFlattened:
      type: object
      description: Flattened view of ManagedOp ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        sku:
          $ref: "#/components/schemas/Sku"
          description: Product plan details of this resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", and "Deleting".'
        desiredConfiguration:
          $ref: "#/components/schemas/DesiredConfiguration"
          description: Desired configuration input by the user. Required.
        services:
          $ref: "#/components/schemas/ServiceInformation"
          description: Services provisioned by this resource.
        policyAssignmentProperties:
          $ref: "#/components/schemas/PolicyAssignmentProperties"
          description: Policy assignments created for managing services.
    PagedManagedOpFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ManagedOpFlattened"
        nextLink:
          type: string
    ManagedOp:
      type: object
      description: The Managed Operations resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ManagedOpsProperties"
          description: The resource-specific properties for this resource.
    ManagedOpUpdateProperties:
      type: object
      description: Updatable properties in the ManagedOps resource.
      properties:
        desiredConfiguration:
          $ref: "#/components/schemas/DesiredConfigurationUpdate"
          description: Desired configuration input by the user.
    DesiredConfigurationUpdate:
      type: object
      description: Updatable parameters in the Desired configuration input.
      properties:
        defenderForServers:
          $ref: "#/components/schemas/DesiredEnablementState"
          description: 'Desired enablement state of the Defender For Servers service. Known values are: "Enable" and "Disable".'
        defenderCspm:
          $ref: "#/components/schemas/DesiredEnablementState"
          description: 'Desired enablement state of the Defender Cloud Security Posture Management (CSPM) service. Known values are: "Enable" and "Disable".'
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    managed_ops:
      id: azure.managedops.managed_ops
      name: managed_ops
      title: managed_ops
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ManagedOps~1managedOps~1{managed_ops_name}?api-version=2025-07-28-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ManagedOps~1managedOps~1{managed_ops_name}?api-version=2025-07-28-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ManagedOps~1managedOps~1{managed_ops_name}?api-version=2025-07-28-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ManagedOps~1managedOps~1{managed_ops_name}?api-version=2025-07-28-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ManagedOps~1managedOps?api-version=2025-07-28-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/managed_ops/methods/get"
          - $ref: "#/components/x-stackQL-resources/managed_ops/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/managed_ops/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/managed_ops/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/managed_ops/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/managed_ops/methods/delete"
    operations:
      id: azure.managedops.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ManagedOps~1operations?api-version=2025-07-28-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.ManagedOps/managedOps/{managed_ops_name}?api-version=2025-07-28-preview:
    get:
      operationId: ManagedOps_get
      description: Gets the information of the ManagedOps instance.
      parameters:
        - name: managed_ops_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedOpFlattened"
    put:
      operationId: ManagedOps_begin_create_or_update
      description: Creates or updates the ManagedOps instance.
      parameters:
        - name: managed_ops_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ManagedOpsProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedOp"
    patch:
      operationId: ManagedOps_begin_update
      description: Updates the ManagedOps instance with the supplied fields.
      parameters:
        - name: managed_ops_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ManagedOpUpdateProperties"
                  description: Updatable properties in the ManagedOps resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedOp"
    delete:
      operationId: ManagedOps_begin_delete
      description: Deletes the ManagedOps instance.
      parameters:
        - name: managed_ops_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ManagedOps/managedOps?api-version=2025-07-28-preview:
    get:
      operationId: ManagedOps_list
      description: List all ManagedOps instances in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedManagedOpFlattened"
  /providers/Microsoft.ManagedOps/operations?api-version=2025-07-28-preview:
    get:
      operationId: managedops_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
