openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Billing Benefits
  description: The Billing Benefits API gives you access to billing benefits (savings plan) operations.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-09-27-stackql-generated
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  parameters:
    SavingsPlanOrderAliasNameParameter:
      name: savingsPlanOrderAliasName
      in: path
      description: Name of the savings plan order alias
      required: true
      x-ms-parameter-location: method
      schema:
        pattern: ^[a-zA-Z0-9_\-\.]+$
        type: string
    ApiVersionParameter:
      name: api-version
      in: query
      description: The API version to use for this operation.
      required: true
      schema:
        minLength: 1
        type: string
    ExpandParameter:
      name: $expand
      in: query
      description: May be used to expand the detail information of some properties.
      required: false
      x-ms-parameter-location: client
      schema:
        type: string
    SavingsPlanOrderIdParameter:
      name: savingsPlanOrderId
      in: path
      description: Order ID of the savings plan
      required: true
      x-ms-parameter-location: method
      schema:
        type: string
    SavingsPlanIdParameter:
      name: savingsPlanId
      in: path
      description: ID of the savings plan
      required: true
      x-ms-parameter-location: method
      schema:
        type: string
    ReservationOrderAliasNameParameter:
      name: reservationOrderAliasName
      in: path
      description: Name of the reservation order alias
      required: true
      x-ms-parameter-location: method
      schema:
        pattern: ^[a-zA-Z0-9_\-\.]+$
        type: string
    SubscriptionIdParameter:
      name: subscriptionId
      in: path
      description: The ID of the target subscription.
      required: true
      schema:
        minLength: 1
        type: string
    ResourceGroupNameParameter:
      name: resourceGroupName
      in: path
      description: The name of the resource group. The name is case insensitive.
      required: true
      x-ms-parameter-location: method
      schema:
        minLength: 1
        maxLength: 90
        type: string
    OperationIdParameter:
      name: operationId
      in: path
      description: The ID of an ongoing async operation.
      required: true
      x-ms-parameter-location: method
      schema:
        minLength: 1
        type: string
    LocationParameter:
      name: location
      in: path
      description: The name of Azure region.
      required: true
      x-ms-parameter-location: method
      schema:
        minLength: 1
        type: string
  schemas:
    Sku:
      description: The resource model definition representing SKU
      type: object
      properties:
        name:
          type: string
          description: The name of the SKU. Ex - P3. It is typically a letter+number code
        tier:
          $ref: '#/components/schemas/SkuTier'
        size:
          type: string
          description: 'The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. '
        family:
          type: string
          description: If the service has different generations of hardware, for the same SKU, then that can be captured here.
        capacity:
          type: integer
          format: int32
          description: If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted.
      required:
        - name
    SavingsPlanOrderAliasModel:
      description: Savings plan order alias
      required:
        - sku
      x-ms-azure-resource: true
      properties:
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
        sku:
          description: Savings plan SKU
          $ref: '#/components/schemas/Sku'
        kind:
          type: string
          description: Resource provider kind
        properties:
          description: Savings plan order alias properties
          x-ms-client-flatten: true
          $ref: '#/components/schemas/SavingsPlanOrderAliasProperties'
      type: object
    SavingsPlanOrderAliasProperties:
      type: object
      description: Savings plan properties
      properties:
        displayName:
          $ref: '#/components/schemas/DisplayName'
        savingsPlanOrderId:
          type: string
          description: Identifier of the savings plan created
          readOnly: true
        provisioningState:
          $ref: '#/components/schemas/ProvisioningState'
        billingScopeId:
          $ref: '#/components/schemas/BillingScopeId'
        term:
          $ref: '#/components/schemas/Term'
        billingPlan:
          $ref: '#/components/schemas/BillingPlan'
        appliedScopeType:
          $ref: '#/components/schemas/AppliedScopeType'
        appliedScopeProperties:
          $ref: '#/components/schemas/AppliedScopeProperties'
        commitment:
          $ref: '#/components/schemas/Commitment'
        renew:
          $ref: '#/components/schemas/Renew'
    BillingScopeId:
      type: string
      description: Subscription that will be charged for purchasing the benefit
    BillingProfileId:
      type: string
      readOnly: true
      description: Fully-qualified identifier of the billing profile where the savings plan is applied. Present only for Field-led or Customer-led customers.
    CustomerId:
      type: string
      readOnly: true
      description: Fully-qualified identifier of the customer where the savings plan is applied. Present only for Partner-led customers.
    BillingAccountId:
      type: string
      readOnly: true
      description: Fully-qualified identifier of the billing account where the savings plan is applied. Present only for Enterprise Agreement customers.
    ReservedResourceType:
      type: string
      description: The type of the resource that is being reserved.
      enum:
        - VirtualMachines
        - SqlDatabases
        - SuseLinux
        - CosmosDb
        - RedHat
        - SqlDataWarehouse
        - VMwareCloudSimple
        - RedHatOsa
        - Databricks
        - AppService
        - ManagedDisk
        - BlockBlob
        - RedisCache
        - AzureDataExplorer
        - MySql
        - MariaDb
        - PostgreSql
        - DedicatedHost
        - SapHana
        - SqlAzureHybridBenefit
        - AVS
        - DataFactory
        - NetAppStorage
        - AzureFiles
        - SqlEdge
        - VirtualMachineSoftware
      x-ms-enum:
        name: ReservedResourceType
        modelAsString: true
    ProvisioningState:
      type: string
      description: Provisioning state
      readOnly: true
      enum:
        - Creating
        - PendingBilling
        - ConfirmedBilling
        - Created
        - Succeeded
        - Cancelled
        - Expired
        - Failed
      x-ms-enum:
        name: ProvisioningState
        modelAsString: true
    BillingPlan:
      type: string
      description: Represents the billing plan in ISO 8601 format. Required only for monthly billing plans.
      enum:
        - P1M
      x-ms-enum:
        name: BillingPlan
        modelAsString: true
    Term:
      type: string
      description: Represent benefit term in ISO 8601 format.
      enum:
        - P1Y
        - P3Y
        - P5Y
      x-ms-enum:
        name: Term
        modelAsString: true
    DisplayName:
      type: string
      description: Display name
    AppliedScopeType:
      type: string
      description: Type of the Applied Scope.
      enum:
        - Single
        - Shared
        - ManagementGroup
      x-ms-enum:
        name: AppliedScopeType
        modelAsString: true
    AppliedScopeProperties:
      type: object
      description: Properties specific to applied scope type. Not required if not applicable.
      properties:
        tenantId:
          $ref: '#/components/schemas/TenantId'
        managementGroupId:
          $ref: '#/components/schemas/ManagementGroupId'
        subscriptionId:
          $ref: '#/components/schemas/SubscriptionId'
        resourceGroupId:
          $ref: '#/components/schemas/ResourceGroupId'
        displayName:
          type: string
          description: Display name
    TenantId:
      type: string
      description: Tenant ID where the benefit is applied.
    ManagementGroupId:
      type: string
      description: Fully-qualified identifier of the management group where the benefit must be applied.
    SubscriptionId:
      type: string
      description: Fully-qualified identifier of the subscription.
    ResourceGroupId:
      type: string
      description: Fully-qualified identifier of the resource group.
    Renew:
      type: boolean
      default: false
      description: Setting this to true will automatically purchase a new benefit on the expiration date time.
    Commitment:
      description: Commitment towards the benefit.
      properties:
        currencyCode:
          type: string
          description: The ISO 4217 3-letter currency code for the currency used by this purchase record.
        amount:
          type: number
          format: double
        grain:
          type: string
          description: Commitment grain.
          enum:
            - Hourly
          x-ms-enum:
            name: CommitmentGrain
            modelAsString: true
      type: object
    Utilization:
      readOnly: true
      type: object
      description: Savings plan utilization
      properties:
        trend:
          description: The number of days trend for a savings plan
          readOnly: true
          type: string
        aggregates:
          description: The array of aggregates of a savings plan's utilization
          type: array
          items:
            $ref: '#/components/schemas/UtilizationAggregates'
          x-ms-identifiers: []
    UtilizationAggregates:
      description: The aggregate values of savings plan utilization
      type: object
      properties:
        grain:
          description: The grain of the aggregate
          readOnly: true
          type: number
        grainUnit:
          description: The grain unit of the aggregate
          readOnly: true
          type: string
        value:
          description: The aggregate value
          readOnly: true
          type: number
        valueUnit:
          description: The aggregate value unit
          readOnly: true
          type: string
    SavingsPlanId:
      type: string
      description: Identifier of the savings plan
      readOnly: true
    EffectiveDateTime:
      type: string
      format: date-time
      description: DateTime of the savings plan starts providing benefit from.
      readOnly: true
    ExpiryDateTime:
      type: string
      description: Expiry date time
      readOnly: true
      format: date-time
    PurchaseDateTime:
      type: string
      description: Date time when the savings plan was purchased
      readOnly: true
      format: date-time
    BenefitStartTime:
      type: string
      format: date-time
      description: This is the DateTime when the savings plan benefit started.
    SavingsPlanModelListResult:
      type: object
      properties:
        value:
          description: The list of savings plans.
          type: array
          readOnly: true
          items:
            $ref: '#/components/schemas/SavingsPlanModel'
        nextLink:
          type: string
          readOnly: true
          description: Url to get the next page.
        additionalProperties:
          description: The roll out count summary of the savings plans
          type: array
          readOnly: true
          items:
            $ref: '#/components/schemas/SavingsPlanSummary'
          x-ms-identifiers: []
    SavingsPlanModelList:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: '#/components/schemas/SavingsPlanModel'
          x-ms-identifiers: []
        nextLink:
          type: string
          description: Url to get the next page.
    SavingsPlanModel:
      description: Savings plan
      required:
        - sku
      properties:
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
        sku:
          description: Savings plan SKU
          $ref: '#/components/schemas/Sku'
        properties:
          description: Savings plan properties
          x-ms-client-flatten: true
          $ref: '#/components/schemas/SavingsPlanModelProperties'
      type: object
    SavingsPlanModelProperties:
      type: object
      description: Savings plan properties
      properties:
        displayName:
          $ref: '#/components/schemas/DisplayName'
        provisioningState:
          $ref: '#/components/schemas/ProvisioningState'
        displayProvisioningState:
          description: The provisioning state of the savings plan for display, e.g. Succeeded
          type: string
          readOnly: true
        billingScopeId:
          $ref: '#/components/schemas/BillingScopeId'
        billingProfileId:
          $ref: '#/components/schemas/BillingProfileId'
        customerId:
          $ref: '#/components/schemas/CustomerId'
        billingAccountId:
          $ref: '#/components/schemas/BillingAccountId'
        term:
          $ref: '#/components/schemas/Term'
        billingPlan:
          $ref: '#/components/schemas/BillingPlan'
        appliedScopeType:
          $ref: '#/components/schemas/AppliedScopeType'
        userFriendlyAppliedScopeType:
          description: The applied scope type of the savings plan for display, e.g. Shared
          type: string
          readOnly: true
        appliedScopeProperties:
          $ref: '#/components/schemas/AppliedScopeProperties'
        commitment:
          $ref: '#/components/schemas/Commitment'
        effectiveDateTime:
          $ref: '#/components/schemas/EffectiveDateTime'
        expiryDateTime:
          $ref: '#/components/schemas/ExpiryDateTime'
        purchaseDateTime:
          $ref: '#/components/schemas/PurchaseDateTime'
        benefitStartTime:
          $ref: '#/components/schemas/BenefitStartTime'
        extendedStatusInfo:
          $ref: '#/components/schemas/ExtendedStatusInfo'
        renew:
          $ref: '#/components/schemas/Renew'
        utilization:
          $ref: '#/components/schemas/Utilization'
        renewSource:
          $ref: '#/components/schemas/RenewSource'
        renewDestination:
          $ref: '#/components/schemas/RenewDestination'
        renewProperties:
          $ref: '#/components/schemas/RenewProperties'
    SavingsPlanOrderModelList:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: '#/components/schemas/SavingsPlanOrderModel'
        nextLink:
          type: string
          description: Url to get the next page.
    SavingsPlanOrderModel:
      description: Savings plan order
      required:
        - sku
      properties:
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
        sku:
          description: Savings plan SKU
          $ref: '#/components/schemas/Sku'
        properties:
          description: Savings plan order properties
          x-ms-client-flatten: true
          $ref: '#/components/schemas/SavingsPlanOrderModelProperties'
      type: object
    SavingsPlanOrderModelProperties:
      type: object
      description: Savings plan order properties
      properties:
        displayName:
          $ref: '#/components/schemas/DisplayName'
        provisioningState:
          $ref: '#/components/schemas/ProvisioningState'
        billingScopeId:
          $ref: '#/components/schemas/BillingScopeId'
        billingProfileId:
          $ref: '#/components/schemas/BillingProfileId'
        customerId:
          $ref: '#/components/schemas/CustomerId'
        billingAccountId:
          $ref: '#/components/schemas/BillingAccountId'
        term:
          $ref: '#/components/schemas/Term'
        billingPlan:
          $ref: '#/components/schemas/BillingPlan'
        expiryDateTime:
          $ref: '#/components/schemas/ExpiryDateTime'
        benefitStartTime:
          $ref: '#/components/schemas/BenefitStartTime'
        planInformation:
          $ref: '#/components/schemas/BillingPlanInformation'
        savingsPlans:
          type: array
          items:
            $ref: '#/components/schemas/SavingsPlanId'
        extendedStatusInfo:
          $ref: '#/components/schemas/ExtendedStatusInfo'
    SavingsPlanUpdateRequest:
      description: Savings plan patch request
      properties:
        properties:
          $ref: '#/components/schemas/SavingsPlanUpdateRequestProperties'
      type: object
    SavingsPlanUpdateRequestProperties:
      type: object
      description: Savings plan patch request
      properties:
        displayName:
          $ref: '#/components/schemas/DisplayName'
        appliedScopeType:
          $ref: '#/components/schemas/AppliedScopeType'
        appliedScopeProperties:
          $ref: '#/components/schemas/AppliedScopeProperties'
        renew:
          $ref: '#/components/schemas/Renew'
        renewProperties:
          $ref: '#/components/schemas/RenewProperties'
    Price:
      type: object
      properties:
        currencyCode:
          type: string
          description: The ISO 4217 3-letter currency code for the currency used by this purchase record.
        amount:
          type: number
          format: double
    BillingPlanInformation:
      type: object
      description: Information describing the type of billing plan for this savings plan.
      properties:
        pricingCurrencyTotal:
          $ref: '#/components/schemas/Price'
          description: Amount of money to be paid for the Order. Tax is not included.
        startDate:
          type: string
          format: date
          description: Date when the billing plan has started.
        nextPaymentDueDate:
          type: string
          format: date
          description: For recurring billing plans, indicates the date when next payment will be processed. Null when total is paid off.
        transactions:
          type: array
          items:
            $ref: '#/components/schemas/PaymentDetail'
          x-ms-identifiers: []
    PaymentDetail:
      type: object
      description: Information about payment related to a savings plan order.
      properties:
        dueDate:
          type: string
          format: date
          description: Date when the payment needs to be done.
        paymentDate:
          type: string
          format: date
          description: Date when the transaction is completed. Is null when it is scheduled.
        pricingCurrencyTotal:
          $ref: '#/components/schemas/Price'
          description: Amount in pricing currency. Tax not included.
        billingCurrencyTotal:
          $ref: '#/components/schemas/Price'
          description: Amount charged in Billing currency. Tax not included. Is null for future payments
        status:
          $ref: '#/components/schemas/PaymentStatus'
        extendedStatusInfo:
          $ref: '#/components/schemas/ExtendedStatusInfo'
        billingAccount:
          type: string
          description: Billing account
    PaymentStatus:
      type: string
      description: Describes whether the payment is completed, failed, cancelled or scheduled in the future.
      enum:
        - Succeeded
        - Failed
        - Scheduled
        - Cancelled
      x-ms-enum:
        name: PaymentStatus
        modelAsString: true
    ExtendedStatusInfo:
      type: object
      readOnly: true
      properties:
        statusCode:
          type: string
          description: Status code providing additional information.
        message:
          type: string
          description: The message giving detailed information about the status code.
    RenewSource:
      type: string
      description: SavingsPlan Id of the SavingsPlan from which this SavingsPlan is renewed.
    RenewDestination:
      type: string
      description: SavingsPlan Id of the SavingsPlan which is purchased because of renew.
    RenewProperties:
      type: object
      properties:
        purchaseProperties:
          $ref: '#/components/schemas/PurchaseRequest'
    SavingsPlanPurchaseValidateRequest:
      type: object
      properties:
        benefits:
          type: array
          items:
            $ref: '#/components/schemas/SavingsPlanOrderAliasModel'
    SavingsPlanUpdateValidateRequest:
      type: object
      properties:
        benefits:
          type: array
          items:
            $ref: '#/components/schemas/SavingsPlanUpdateRequestProperties'
          x-ms-identifiers: []
    SavingsPlanValidateResponse:
      type: object
      properties:
        benefits:
          type: array
          items:
            $ref: '#/components/schemas/SavingsPlanValidResponseProperty'
          x-ms-identifiers: []
        nextLink:
          type: string
          description: Url to get the next page.
    SavingsPlanValidResponseProperty:
      type: object
      description: Benefit scope response property
      properties:
        valid:
          type: boolean
          description: Indicates if the provided input was valid
        reasonCode:
          type: string
          description: Failure reason code if the provided input was invalid
        reason:
          type: string
          description: Failure reason if the provided input was invalid
    PricingCurrencyTotal:
      properties:
        currencyCode:
          type: string
          description: The ISO 4217 3-letter currency code for the currency used by this purchase record.
        amount:
          type: number
          format: double
        duration:
          $ref: '#/components/schemas/PricingCurrencyDuration'
      type: object
    PricingCurrencyDuration:
      type: string
      description: Represents either billing plan or savings plan term in ISO 8601 format.
      enum:
        - P1M
        - P1Y
        - P3Y
      x-ms-enum:
        name: PricingCurrencyDuration
        modelAsString: true
    PurchaseRequest:
      properties:
        sku:
          $ref: '#/components/schemas/Sku'
        properties:
          x-ms-client-flatten: true
          $ref: '#/components/schemas/PurchaseRequestProperties'
      type: object
    PurchaseRequestProperties:
      type: object
      properties:
        displayName:
          type: string
          description: Friendly name of the savings plan
        billingScopeId:
          $ref: '#/components/schemas/BillingScopeId'
        term:
          $ref: '#/components/schemas/Term'
        billingPlan:
          $ref: '#/components/schemas/BillingPlan'
        appliedScopeType:
          $ref: '#/components/schemas/AppliedScopeType'
        commitment:
          $ref: '#/components/schemas/Commitment'
        effectiveDateTime:
          $ref: '#/components/schemas/EffectiveDateTime'
        renew:
          $ref: '#/components/schemas/Renew'
        appliedScopeProperties:
          $ref: '#/components/schemas/AppliedScopeProperties'
    OperationResultError:
      type: object
      description: Required if status == failed or status == canceled.
      properties:
        code:
          type: string
          description: Required if status == failed or status == cancelled. If status == failed, provide an invariant error code used for error troubleshooting, aggregation, and analysis.
        message:
          type: string
          description: Required if status == failed. Localized. If status == failed, provide an actionable error message indicating what error occurred, and what the user can do to address the issue.
    BillingInformation:
      type: object
      description: billing information
      properties:
        billingCurrencyTotalPaidAmount:
          $ref: '#/components/schemas/Price'
        billingCurrencyProratedAmount:
          $ref: '#/components/schemas/Price'
        billingCurrencyRemainingCommitmentAmount:
          $ref: '#/components/schemas/Price'
    SavingsPlanSummary:
      type: object
      description: Savings plans list summary
      properties:
        name:
          description: This property has value 'summary'
          readOnly: true
          type: string
        value:
          $ref: '#/components/schemas/SavingsPlanSummaryCount'
    SavingsPlanSummaryCount:
      description: The roll up count summary of savings plans in each state
      type: object
      properties:
        succeededCount:
          description: The number of savings plans in Succeeded state
          readOnly: true
          type: number
        failedCount:
          description: The number of savings plans in Failed state
          readOnly: true
          type: number
        expiringCount:
          description: The number of savings plans in Expiring state
          readOnly: true
          type: number
        expiredCount:
          description: The number of savings plans in Expired state
          readOnly: true
          type: number
        pendingCount:
          description: The number of savings plans in Pending state
          readOnly: true
          type: number
        cancelledCount:
          description: The number of savings plans in Cancelled state
          readOnly: true
          type: number
        processingCount:
          description: The number of savings plans in Processing state
          readOnly: true
          type: number
        noBenefitCount:
          description: The number of savings plans in No Benefit state
          readOnly: true
          type: number
        warningCount:
          description: The number of savings plans in Warning state
          readOnly: true
          type: number
    ReservationOrderAliasRequest:
      description: Reservation order alias
      required:
        - sku
      x-ms-azure-resource: true
      properties:
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
        sku:
          description: Reservation order SKU
          $ref: '#/components/schemas/Sku'
        location:
          type: string
          description: The Azure Region where the reservation benefits are applied to.
        properties:
          description: Reservation order alias request properties
          x-ms-client-flatten: true
          $ref: '#/components/schemas/ReservationOrderAliasRequestProperties'
      type: object
    ReservationOrderAliasRequestProperties:
      type: object
      description: Reservation properties
      properties:
        displayName:
          $ref: '#/components/schemas/DisplayName'
        billingScopeId:
          $ref: '#/components/schemas/BillingScopeId'
        term:
          $ref: '#/components/schemas/Term'
        billingPlan:
          $ref: '#/components/schemas/BillingPlan'
        appliedScopeType:
          $ref: '#/components/schemas/AppliedScopeType'
        appliedScopeProperties:
          $ref: '#/components/schemas/AppliedScopeProperties'
        quantity:
          type: integer
          format: int32
          description: Total Quantity of the SKUs purchased in the Reservation.
          minimum: 1
        renew:
          $ref: '#/components/schemas/Renew'
        reservedResourceType:
          description: The type of the resource that is being reserved.
          $ref: '#/components/schemas/ReservedResourceType'
        reviewDateTime:
          type: string
          format: date-time
          description: This is the date-time when the Azure Hybrid Benefit needs to be reviewed.
        reservedResourceProperties:
          type: object
          description: Properties specific to each reserved resource type. Not required if not applicable.
          properties:
            instanceFlexibility:
              $ref: '#/components/schemas/InstanceFlexibility'
    ReservationOrderAliasResponse:
      description: Reservation order alias
      required:
        - sku
      x-ms-azure-resource: true
      properties:
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
        sku:
          description: Reservation order SKU
          $ref: '#/components/schemas/Sku'
        location:
          type: string
          description: The Azure Region where the reserved resource lives.
        properties:
          description: Reservation order alias response properties
          x-ms-client-flatten: true
          $ref: '#/components/schemas/ReservationOrderAliasResponseProperties'
      type: object
    ReservationOrderAliasResponseProperties:
      type: object
      description: Reservation properties
      properties:
        displayName:
          $ref: '#/components/schemas/DisplayName'
        reservationOrderId:
          type: string
          description: Identifier of the reservation order created
          readOnly: true
        provisioningState:
          $ref: '#/components/schemas/ProvisioningState'
        billingScopeId:
          $ref: '#/components/schemas/BillingScopeId'
        term:
          $ref: '#/components/schemas/Term'
        billingPlan:
          $ref: '#/components/schemas/BillingPlan'
        appliedScopeType:
          $ref: '#/components/schemas/AppliedScopeType'
        appliedScopeProperties:
          $ref: '#/components/schemas/AppliedScopeProperties'
        quantity:
          type: integer
          format: int32
          description: Total Quantity of the SKUs purchased in the Reservation.
        renew:
          $ref: '#/components/schemas/Renew'
        reservedResourceType:
          description: The type of the resource that is being reserved.
          $ref: '#/components/schemas/ReservedResourceType'
        reviewDateTime:
          type: string
          format: date-time
          description: This is the date-time when the Reservation needs to be reviewed.
        reservedResourceProperties:
          type: object
          description: Properties specific to each reserved resource type. Not required if not applicable.
          properties:
            instanceFlexibility:
              $ref: '#/components/schemas/InstanceFlexibility'
    InstanceFlexibility:
      type: string
      description: Turning this on will apply the reservation discount to other VMs in the same VM size group.
      enum:
        - 'On'
        - 'Off'
      x-ms-enum:
        name: InstanceFlexibility
        modelAsString: true
    RoleAssignmentEntity:
      description: Role assignment entity
      properties:
        id:
          description: Role assignment entity id
          type: string
        name:
          description: Role assignment entity name
          type: string
        properties:
          description: Role assignment entity properties
          x-ms-client-flatten: true
          $ref: '#/components/schemas/RoleAssignmentEntityProperties'
      type: object
    RoleAssignmentEntityProperties:
      type: object
      description: Role assignment entity properties
      properties:
        principalId:
          description: Principal Id
          type: string
        roleDefinitionId:
          description: Role definition id
          type: string
        scope:
          description: Scope of the role assignment entity
          type: string
    Resource:
      title: Resource
      description: Common fields that are returned in the response for all Azure Resource Manager resources
      type: object
      properties:
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
      x-ms-azure-resource: true
    AzureEntityResource:
      x-ms-client-name: AzureEntityResource
      title: Entity Resource
      description: The resource model definition for an Azure Resource Manager resource with an etag.
      properties:
        etag:
          type: string
          readOnly: true
          description: Resource Etag.
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
      type: object
    TrackedResource:
      title: Tracked Resource
      description: The resource model definition for an Azure Resource Manager tracked top level resource which has 'tags' and a 'location'
      required:
        - location
      properties:
        tags:
          type: object
          additionalProperties:
            type: string
          x-ms-mutability:
            - read
            - create
            - update
          description: Resource tags.
        location:
          type: string
          x-ms-mutability:
            - read
            - create
          description: The geo-location where the resource lives
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
      type: object
    ProxyResource:
      title: Proxy Resource
      description: The resource model definition for a Azure Resource Manager proxy resource. It will not have tags and a location
      properties:
        id:
          readOnly: true
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
        name:
          readOnly: true
          type: string
          description: The name of the resource
        type:
          readOnly: true
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
        systemData:
          readOnly: true
          type: object
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
          $ref: '#/components/schemas/systemData'
      type: object
    ResourceModelWithAllowedPropertySet:
      description: The resource model definition containing the full set of allowed properties for a resource. Except properties bag, there cannot be a top level property outside of this set.
      x-ms-azure-resource: true
      properties:
        managedBy:
          type: string
          x-ms-mutability:
            - read
            - create
            - update
          description: The fully qualified resource ID of the resource that manages this resource. Indicates if this resource is managed by another Azure resource. If this is present, complete mode deployment will not delete the resource if it is removed from the template since it is managed by another resource.
        kind:
          type: string
          x-ms-mutability:
            - read
            - create
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type.  If supported, the resource provider must validate and persist this value.
          pattern: ^[-\w\._,\(\)]+$
        etag:
          readOnly: true
          type: string
          description: 'The etag field is *not* required. If it is provided in the response body, it must also be provided as a header per the normal etag convention.  Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields. '
        identity:
          allOf:
            - $ref: '#/components/schemas/Identity'
        sku:
          allOf:
            - $ref: '#/components/schemas/Sku'
        plan:
          allOf:
            - $ref: '#/components/schemas/Plan'
        tags:
          type: object
          additionalProperties:
            type: string
          x-ms-mutability:
            - read
            - create
            - update
          description: Resource tags.
        location:
          type: string
          x-ms-mutability:
            - read
            - create
          description: The geo-location where the resource lives
      type: object
    SkuTier:
      type: string
      enum:
        - Free
        - Basic
        - Standard
        - Premium
      x-ms-enum:
        name: SkuTier
        modelAsString: false
      description: This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT.
    Identity:
      description: Identity for the resource.
      type: object
      properties:
        principalId:
          readOnly: true
          type: string
          description: The principal ID of resource identity.
        tenantId:
          readOnly: true
          type: string
          description: The tenant ID of resource.
        type:
          type: string
          description: The identity type.
          enum:
            - SystemAssigned
          x-ms-enum:
            name: ResourceIdentityType
            modelAsString: false
    Plan:
      type: object
      properties:
        name:
          type: string
          description: A user defined name of the 3rd Party Artifact that is being procured.
        publisher:
          type: string
          description: The publisher of the 3rd Party Artifact that is being bought. E.g. NewRelic
        product:
          type: string
          description: 'The 3rd Party artifact that is being procured. E.g. NewRelic. Product maps to the OfferID specified for the artifact at the time of Data Market onboarding. '
        promotionCode:
          type: string
          description: A publisher provided promotion code as provisioned in Data Market for the said product/artifact.
        version:
          type: string
          description: The version of the desired product/artifact.
      description: Plan for the resource.
      required:
        - name
        - publisher
        - product
    ErrorDetail:
      description: The error detail.
      type: object
      properties:
        code:
          readOnly: true
          type: string
          description: The error code.
        message:
          readOnly: true
          type: string
          description: The error message.
        target:
          readOnly: true
          type: string
          description: The error target.
        details:
          readOnly: true
          type: array
          items:
            $ref: '#/components/schemas/ErrorDetail'
          x-ms-identifiers:
            - message
            - target
          description: The error details.
        additionalInfo:
          readOnly: true
          type: array
          items:
            $ref: '#/components/schemas/ErrorAdditionalInfo'
          x-ms-identifiers: []
          description: The error additional info.
    ErrorResponse:
      title: Error response
      description: Common error response for all Azure Resource Manager APIs to return error details for failed operations. (This also follows the OData error response format.).
      type: object
      properties:
        error:
          description: The error object.
          $ref: '#/components/schemas/ErrorDetail'
    ErrorAdditionalInfo:
      type: object
      properties:
        type:
          readOnly: true
          type: string
          description: The additional info type.
        info:
          readOnly: true
          type: object
          description: The additional info.
      description: The resource management error additional info.
    Operation:
      title: REST API Operation
      description: Details of a REST API operation, returned from the Resource Provider Operations API
      type: object
      properties:
        name:
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action"'
          type: string
          readOnly: true
        isDataAction:
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
          type: boolean
          readOnly: true
        display:
          description: Localized display information for this particular operation.
          type: object
          properties:
            provider:
              description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
              type: string
              readOnly: true
            resource:
              description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
              type: string
              readOnly: true
            operation:
              description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
              type: string
              readOnly: true
            description:
              description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
              type: string
              readOnly: true
        origin:
          description: The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system"
          type: string
          readOnly: true
          enum:
            - user
            - system
            - user,system
          x-ms-enum:
            name: Origin
            modelAsString: true
        actionType:
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs.
          type: string
          readOnly: true
          enum:
            - Internal
          x-ms-enum:
            name: ActionType
            modelAsString: true
    OperationListResult:
      description: A list of REST API operations supported by an Azure Resource Provider. It contains an URL link to get the next set of results.
      type: object
      properties:
        value:
          type: array
          items:
            $ref: '#/components/schemas/Operation'
          x-ms-identifiers:
            - name
          description: List of operations supported by the resource provider
          readOnly: true
        nextLink:
          type: string
          description: URL to get the next set of operation list results (if there are any).
          readOnly: true
    OperationStatusResult:
      description: The current status of an async operation.
      type: object
      required:
        - status
      properties:
        id:
          description: Fully qualified ID for the async operation.
          type: string
        name:
          description: Name of the async operation.
          type: string
        status:
          description: Operation status.
          type: string
        percentComplete:
          description: Percent of the operation that is complete.
          type: number
          minimum: 0
          maximum: 100
        startTime:
          description: The start time of the operation.
          type: string
          format: date-time
        endTime:
          description: The end time of the operation.
          type: string
          format: date-time
        operations:
          description: The operations list.
          type: array
          items:
            $ref: '#/components/schemas/OperationStatusResult'
        error:
          description: If present, details of the operation error.
          $ref: '#/components/schemas/ErrorDetail'
    locationData:
      description: Metadata pertaining to the geographic location of the resource.
      type: object
      properties:
        name:
          type: string
          maxLength: 256
          description: A canonical name for the geographic or physical location.
        city:
          type: string
          description: The city or locality where the resource is located.
        district:
          type: string
          description: The district, state, or province where the resource is located.
        countryOrRegion:
          type: string
          description: The country or region where the resource is located
      required:
        - name
    systemData:
      description: Metadata pertaining to creation and last modification of the resource.
      type: object
      readOnly: true
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: The type of identity that created the resource.
          enum:
            - User
            - Application
            - ManagedIdentity
            - Key
          x-ms-enum:
            name: createdByType
            modelAsString: true
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: The type of identity that last modified the resource.
          enum:
            - User
            - Application
            - ManagedIdentity
            - Key
          x-ms-enum:
            name: createdByType
            modelAsString: true
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC)
    encryptionProperties:
      description: Configuration of key for data encryption
      type: object
      properties:
        status:
          description: Indicates whether or not the encryption is enabled for container registry.
          enum:
            - enabled
            - disabled
          type: string
          x-ms-enum:
            name: EncryptionStatus
            modelAsString: true
        keyVaultProperties:
          $ref: '#/components/schemas/KeyVaultProperties'
          description: Key vault properties.
    KeyVaultProperties:
      type: object
      properties:
        keyIdentifier:
          description: Key vault uri to access the encryption key.
          type: string
        identity:
          description: The client ID of the identity which will be used to access key vault.
          type: string
    CheckNameAvailabilityRequest:
      description: The check availability request body.
      type: object
      properties:
        name:
          description: The name of the resource for which availability needs to be checked.
          type: string
        type:
          description: The resource type.
          type: string
    CheckNameAvailabilityResponse:
      description: The check availability result.
      type: object
      properties:
        nameAvailable:
          description: Indicates if the resource name is available.
          type: boolean
        reason:
          description: The reason why the given name is not available.
          type: string
          enum:
            - Invalid
            - AlreadyExists
          x-ms-enum:
            name: CheckNameAvailabilityReason
            modelAsString: true
        message:
          description: Detailed reason why the given name is available.
          type: string
  x-stackQL-resources:
    operations:
      id: azure.billing_benefits.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1operations~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/operations/methods/list'
        insert: []
        update: []
        replace: []
        delete: []
    savings_plan_order_alias:
      id: azure.billing_benefits.savings_plan_order_alias
      name: savings_plan_order_alias
      title: savings_plan_order_alias
      methods:
        create:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrderAliases~1{savingsPlanOrderAliasName}~1?api-version=2022-11-01/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrderAliases~1{savingsPlanOrderAliasName}~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/savings_plan_order_alias/methods/get'
        insert:
          - $ref: '#/components/x-stackQL-resources/savings_plan_order_alias/methods/create'
        update: []
        replace: []
        delete: []
    savings_plan_order:
      id: azure.billing_benefits.savings_plan_order
      name: savings_plan_order
      title: savings_plan_order
      methods:
        get:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrders~1{savingsPlanOrderId}~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        elevate:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrders~1{savingsPlanOrderId}~1elevate~1?api-version=2022-11-01/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrders~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/savings_plan_order/methods/get'
          - $ref: '#/components/x-stackQL-resources/savings_plan_order/methods/list'
        insert: []
        update: []
        replace: []
        delete: []
    savings_plan:
      id: azure.billing_benefits.savings_plan
      name: savings_plan
      title: savings_plan
      methods:
        list:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrders~1{savingsPlanOrderId}~1savingsPlans~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.value
        get:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrders~1{savingsPlanOrderId}~1savingsPlans~1{savingsPlanId}~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrders~1{savingsPlanOrderId}~1savingsPlans~1{savingsPlanId}~1?api-version=2022-11-01/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        validate_update:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlanOrders~1{savingsPlanOrderId}~1savingsPlans~1{savingsPlanId}~1validate~1?api-version=2022-11-01/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/savings_plan/methods/get'
          - $ref: '#/components/x-stackQL-resources/savings_plan/methods/list'
        insert: []
        update: []
        replace: []
        delete: []
    savings_plan_all:
      id: azure.billing_benefits.savings_plan_all
      name: savings_plan_all
      title: savings_plan_all
      methods:
        list:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1savingsPlans~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/savings_plan_all/methods/list'
        insert: []
        update: []
        replace: []
        delete: []
    savingsplan:
      id: azure.billing_benefits.savingsplan
      name: savingsplan
      title: savingsplan
      methods:
        savingsplan:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1validate~1?api-version=2022-11-01/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select: []
        insert: []
        update: []
        replace: []
        delete: []
    reservation_order_alias:
      id: azure.billing_benefits.reservation_order_alias
      name: reservation_order_alias
      title: reservation_order_alias
      methods:
        create:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1reservationOrderAliases~1{reservationOrderAliasName}~1?api-version=2022-11-01/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1providers~1Microsoft.BillingBenefits~1reservationOrderAliases~1{reservationOrderAliasName}~1?api-version=2022-11-01/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/reservation_order_alias/methods/get'
        insert:
          - $ref: '#/components/x-stackQL-resources/reservation_order_alias/methods/create'
        update: []
        replace: []
        delete: []
    vw_savings_plan_order_alias:
      id: azure.billing_benefits.vw_savings_plan_order_alias
      name: vw_savings_plan_order_alias
      config:
        views:
          select:
            predicate: sqlDialect == "sqlite3"
            ddl: |-
              SELECT
              id as id,
              name as name,
              type as type,
              systemData as system_data,
              sku as sku,
              kind as kind,
              JSON_EXTRACT(properties, '$.displayName') as display_name,
              JSON_EXTRACT(properties, '$.savingsPlanOrderId') as savings_plan_order_id,
              JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
              JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
              JSON_EXTRACT(properties, '$.term') as term,
              JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
              JSON_EXTRACT(properties, '$.appliedScopeType') as applied_scope_type,
              JSON_EXTRACT(properties, '$.appliedScopeProperties') as applied_scope_properties,
              JSON_EXTRACT(properties, '$.commitment') as commitment,
              JSON_EXTRACT(properties, '$.renew') as renew,
              savingsPlanOrderAliasName
              FROM azure.billing_benefits.savings_plan_order_alias
              WHERE savingsPlanOrderAliasName = 'replace-me';
            fallback:
              predicate: sqlDialect == "postgres"
              ddl: |-
                SELECT
                id as id,
                name as name,
                type as type,
                systemData as system_data,
                sku as sku,
                kind as kind,
                json_extract_path_text(properties, '$.displayName') as display_name,
                JSON_EXTRACT(properties, '$.savingsPlanOrderId') as savings_plan_order_id,
                JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
                JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
                JSON_EXTRACT(properties, '$.term') as term,
                JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
                JSON_EXTRACT(properties, '$.appliedScopeType') as applied_scope_type,
                JSON_EXTRACT(properties, '$.appliedScopeProperties') as applied_scope_properties,
                JSON_EXTRACT(properties, '$.commitment') as commitment,
                JSON_EXTRACT(properties, '$.renew') as renew,
                savingsPlanOrderAliasName
                FROM azure.billing_benefits.savings_plan_order_alias
                WHERE savingsPlanOrderAliasName = 'replace-me';
    vw_savings_plan_order:
      id: azure.billing_benefits.vw_savings_plan_order
      name: vw_savings_plan_order
      config:
        views:
          select:
            predicate: sqlDialect == "sqlite3"
            ddl: |-
              SELECT
              id as id,
              name as name,
              type as type,
              systemData as system_data,
              sku as sku,
              JSON_EXTRACT(properties, '$.displayName') as display_name,
              JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
              JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
              JSON_EXTRACT(properties, '$.billingProfileId') as billing_profile_id,
              JSON_EXTRACT(properties, '$.customerId') as customer_id,
              JSON_EXTRACT(properties, '$.billingAccountId') as billing_account_id,
              JSON_EXTRACT(properties, '$.term') as term,
              JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
              JSON_EXTRACT(properties, '$.expiryDateTime') as expiry_date_time,
              JSON_EXTRACT(properties, '$.benefitStartTime') as benefit_start_time,
              JSON_EXTRACT(properties, '$.planInformation') as plan_information,
              JSON_EXTRACT(properties, '$.savingsPlans') as savings_plans,
              JSON_EXTRACT(properties, '$.extendedStatusInfo') as extended_status_info,
              savingsPlanOrderId
              FROM azure.billing_benefits.savings_plan_order
              WHERE ;
            fallback:
              predicate: sqlDialect == "postgres"
              ddl: |-
                SELECT
                id as id,
                name as name,
                type as type,
                systemData as system_data,
                sku as sku,
                json_extract_path_text(properties, '$.displayName') as display_name,
                JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
                JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
                JSON_EXTRACT(properties, '$.billingProfileId') as billing_profile_id,
                JSON_EXTRACT(properties, '$.customerId') as customer_id,
                JSON_EXTRACT(properties, '$.billingAccountId') as billing_account_id,
                JSON_EXTRACT(properties, '$.term') as term,
                JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
                JSON_EXTRACT(properties, '$.expiryDateTime') as expiry_date_time,
                JSON_EXTRACT(properties, '$.benefitStartTime') as benefit_start_time,
                JSON_EXTRACT(properties, '$.planInformation') as plan_information,
                JSON_EXTRACT(properties, '$.savingsPlans') as savings_plans,
                JSON_EXTRACT(properties, '$.extendedStatusInfo') as extended_status_info,
                savingsPlanOrderId
                FROM azure.billing_benefits.savings_plan_order
                WHERE ;
    vw_savings_plan:
      id: azure.billing_benefits.vw_savings_plan
      name: vw_savings_plan
      config:
        views:
          select:
            predicate: sqlDialect == "sqlite3"
            ddl: |-
              SELECT
              id as id,
              name as name,
              type as type,
              systemData as system_data,
              sku as sku,
              JSON_EXTRACT(properties, '$.displayName') as display_name,
              JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
              JSON_EXTRACT(properties, '$.displayProvisioningState') as display_provisioning_state,
              JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
              JSON_EXTRACT(properties, '$.billingProfileId') as billing_profile_id,
              JSON_EXTRACT(properties, '$.customerId') as customer_id,
              JSON_EXTRACT(properties, '$.billingAccountId') as billing_account_id,
              JSON_EXTRACT(properties, '$.term') as term,
              JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
              JSON_EXTRACT(properties, '$.appliedScopeType') as applied_scope_type,
              JSON_EXTRACT(properties, '$.userFriendlyAppliedScopeType') as user_friendly_applied_scope_type,
              JSON_EXTRACT(properties, '$.appliedScopeProperties') as applied_scope_properties,
              JSON_EXTRACT(properties, '$.commitment') as commitment,
              JSON_EXTRACT(properties, '$.effectiveDateTime') as effective_date_time,
              JSON_EXTRACT(properties, '$.expiryDateTime') as expiry_date_time,
              JSON_EXTRACT(properties, '$.purchaseDateTime') as purchase_date_time,
              JSON_EXTRACT(properties, '$.benefitStartTime') as benefit_start_time,
              JSON_EXTRACT(properties, '$.extendedStatusInfo') as extended_status_info,
              JSON_EXTRACT(properties, '$.renew') as renew,
              JSON_EXTRACT(properties, '$.utilization') as utilization,
              JSON_EXTRACT(properties, '$.renewSource') as renew_source,
              JSON_EXTRACT(properties, '$.renewDestination') as renew_destination,
              JSON_EXTRACT(properties, '$.renewProperties') as renew_properties,
              savingsPlanOrderId,
              savingsPlanId
              FROM azure.billing_benefits.savings_plan
              WHERE savingsPlanOrderId = 'replace-me';
            fallback:
              predicate: sqlDialect == "postgres"
              ddl: |-
                SELECT
                id as id,
                name as name,
                type as type,
                systemData as system_data,
                sku as sku,
                json_extract_path_text(properties, '$.displayName') as display_name,
                JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
                JSON_EXTRACT(properties, '$.displayProvisioningState') as display_provisioning_state,
                JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
                JSON_EXTRACT(properties, '$.billingProfileId') as billing_profile_id,
                JSON_EXTRACT(properties, '$.customerId') as customer_id,
                JSON_EXTRACT(properties, '$.billingAccountId') as billing_account_id,
                JSON_EXTRACT(properties, '$.term') as term,
                JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
                JSON_EXTRACT(properties, '$.appliedScopeType') as applied_scope_type,
                JSON_EXTRACT(properties, '$.userFriendlyAppliedScopeType') as user_friendly_applied_scope_type,
                JSON_EXTRACT(properties, '$.appliedScopeProperties') as applied_scope_properties,
                JSON_EXTRACT(properties, '$.commitment') as commitment,
                JSON_EXTRACT(properties, '$.effectiveDateTime') as effective_date_time,
                JSON_EXTRACT(properties, '$.expiryDateTime') as expiry_date_time,
                JSON_EXTRACT(properties, '$.purchaseDateTime') as purchase_date_time,
                JSON_EXTRACT(properties, '$.benefitStartTime') as benefit_start_time,
                JSON_EXTRACT(properties, '$.extendedStatusInfo') as extended_status_info,
                JSON_EXTRACT(properties, '$.renew') as renew,
                JSON_EXTRACT(properties, '$.utilization') as utilization,
                JSON_EXTRACT(properties, '$.renewSource') as renew_source,
                JSON_EXTRACT(properties, '$.renewDestination') as renew_destination,
                JSON_EXTRACT(properties, '$.renewProperties') as renew_properties,
                savingsPlanOrderId,
                savingsPlanId
                FROM azure.billing_benefits.savings_plan
                WHERE savingsPlanOrderId = 'replace-me';
    vw_reservation_order_alias:
      id: azure.billing_benefits.vw_reservation_order_alias
      name: vw_reservation_order_alias
      config:
        views:
          select:
            predicate: sqlDialect == "sqlite3"
            ddl: |-
              SELECT
              id as id,
              name as name,
              location as location,
              type as type,
              systemData as system_data,
              sku as sku,
              JSON_EXTRACT(properties, '$.displayName') as display_name,
              JSON_EXTRACT(properties, '$.reservationOrderId') as reservation_order_id,
              JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
              JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
              JSON_EXTRACT(properties, '$.term') as term,
              JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
              JSON_EXTRACT(properties, '$.appliedScopeType') as applied_scope_type,
              JSON_EXTRACT(properties, '$.appliedScopeProperties') as applied_scope_properties,
              JSON_EXTRACT(properties, '$.quantity') as quantity,
              JSON_EXTRACT(properties, '$.renew') as renew,
              JSON_EXTRACT(properties, '$.reservedResourceType') as reserved_resource_type,
              JSON_EXTRACT(properties, '$.reviewDateTime') as review_date_time,
              JSON_EXTRACT(properties, '$.reservedResourceProperties') as reserved_resource_properties,
              reservationOrderAliasName
              FROM azure.billing_benefits.reservation_order_alias
              WHERE reservationOrderAliasName = 'replace-me';
            fallback:
              predicate: sqlDialect == "postgres"
              ddl: |-
                SELECT
                id as id,
                name as name,
                location as location,
                type as type,
                systemData as system_data,
                sku as sku,
                json_extract_path_text(properties, '$.displayName') as display_name,
                JSON_EXTRACT(properties, '$.reservationOrderId') as reservation_order_id,
                JSON_EXTRACT(properties, '$.provisioningState') as provisioning_state,
                JSON_EXTRACT(properties, '$.billingScopeId') as billing_scope_id,
                JSON_EXTRACT(properties, '$.term') as term,
                JSON_EXTRACT(properties, '$.billingPlan') as billing_plan,
                JSON_EXTRACT(properties, '$.appliedScopeType') as applied_scope_type,
                JSON_EXTRACT(properties, '$.appliedScopeProperties') as applied_scope_properties,
                JSON_EXTRACT(properties, '$.quantity') as quantity,
                JSON_EXTRACT(properties, '$.renew') as renew,
                JSON_EXTRACT(properties, '$.reservedResourceType') as reserved_resource_type,
                JSON_EXTRACT(properties, '$.reviewDateTime') as review_date_time,
                JSON_EXTRACT(properties, '$.reservedResourceProperties') as reserved_resource_properties,
                reservationOrderAliasName
                FROM azure.billing_benefits.reservation_order_alias
                WHERE reservationOrderAliasName = 'replace-me';
paths:
  /providers/Microsoft.BillingBenefits/operations/?api-version=2022-11-01:
    get:
      summary: Get operations.
      description: List all the operations.
      operationId: Operations_List
      x-ms-pageable:
        nextLinkName: nextLink
      parameters: []
      tags:
        - Benefits operations
      responses:
        '200':
          description: List all the operations.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationListResult'
        default:
          description: Error response describing why the operation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlanOrderAliases/{savingsPlanOrderAliasName}/?api-version=2022-11-01:
    put:
      description: Create a savings plan. Learn more about permissions needed at https://go.microsoft.com/fwlink/?linkid=2215851
      operationId: SavingsPlanOrderAlias_Create
      externalDocs:
        url: https://go.microsoft.com/fwlink/?linkid=2215851/
      tags:
        - Savings plan order aliases
      x-ms-long-running-operation: true
      x-ms-long-running-operation-options:
        final-state-via: azure-async-operation
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderAliasNameParameter'
      requestBody:
        description: Request body for creating a savings plan order alias
        required: true
        x-ms-requestBody-name: body
        x-ms-parameter-location: method
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavingsPlanOrderAliasModel'
      responses:
        '200':
          description: The resource has been created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanOrderAliasModel'
        '201':
          description: The request is being processed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanOrderAliasModel'
          headers:
            Azure-AsyncOperation:
              schema:
                type: string
              description: URL for checking the ongoing status of the operation.
            Retry-After:
              schema:
                format: int32
                type: integer
              description: Clients should wait for the Retry-After interval before polling again
        default:
          description: Error response describing why the operation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      description: Get a savings plan.
      operationId: SavingsPlanOrderAlias_Get
      tags:
        - Savings plan order aliases
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderAliasNameParameter'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanOrderAliasModel'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlanOrders/{savingsPlanOrderId}/?api-version=2022-11-01:
    get:
      description: Get a savings plan order.
      operationId: SavingsPlanOrder_Get
      tags:
        - Savings plan order
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderIdParameter'
        - $ref: '#/components/parameters/ExpandParameter'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanOrderModel'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlanOrders/{savingsPlanOrderId}/elevate/?api-version=2022-11-01:
    post:
      description: Elevate as owner on savings plan order based on billing permissions.
      operationId: SavingsPlanOrder_Elevate
      tags:
        - Savings plan order
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderIdParameter'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleAssignmentEntity'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlanOrders/?api-version=2022-11-01:
    get:
      description: List all Savings plan orders.
      operationId: SavingsPlanOrder_List
      tags:
        - Savings plan order
      x-ms-pageable:
        nextLinkName: nextLink
      parameters: []
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanOrderModelList'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlanOrders/{savingsPlanOrderId}/savingsPlans/?api-version=2022-11-01:
    get:
      description: List savings plans in an order.
      operationId: SavingsPlan_List
      tags:
        - Savings plan
      x-ms-pageable:
        nextLinkName: nextLink
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderIdParameter'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanModelList'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlans/?api-version=2022-11-01:
    get:
      description: List savings plans.
      operationId: SavingsPlan_ListAll
      tags:
        - Savings plan
      x-ms-pageable:
        nextLinkName: nextLink
      parameters:
        - name: $filter
          in: query
          description: May be used to filter by reservation properties. The filter supports 'eq', 'or', and 'and'. It does not currently support 'ne', 'gt', 'le', 'ge', or 'not'. Reservation properties include sku/name, properties/{appliedScopeType, archived, displayName, displayProvisioningState, effectiveDateTime, expiryDate, provisioningState, quantity, renew, reservedResourceType, term, userFriendlyAppliedScopeType, userFriendlyRenewState}
          required: false
          schema:
            type: string
        - name: $orderby
          in: query
          description: May be used to sort order by reservation properties.
          required: false
          schema:
            type: string
        - name: refreshSummary
          in: query
          description: To indicate whether to refresh the roll up counts of the savings plans group by provisioning states
          required: false
          schema:
            type: string
        - name: $skiptoken
          in: query
          description: The number of savings plans to skip from the list before returning results
          required: false
          schema:
            type: number
        - name: selectedState
          in: query
          description: The selected provisioning state
          required: false
          schema:
            type: string
        - name: take
          in: query
          description: To number of savings plans to return
          required: false
          schema:
            type: number
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanModelListResult'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlanOrders/{savingsPlanOrderId}/savingsPlans/{savingsPlanId}/?api-version=2022-11-01:
    get:
      description: Get savings plan.
      operationId: SavingsPlan_Get
      tags:
        - Savings plan
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderIdParameter'
        - $ref: '#/components/parameters/SavingsPlanIdParameter'
        - $ref: '#/components/parameters/ExpandParameter'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanModel'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      description: Update savings plan.
      operationId: SavingsPlan_Update
      tags:
        - Savings plan
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderIdParameter'
        - $ref: '#/components/parameters/SavingsPlanIdParameter'
      requestBody:
        description: Request body for patching a savings plan order alias
        required: true
        x-ms-requestBody-name: body
        x-ms-parameter-location: method
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavingsPlanUpdateRequest'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanModel'
        '202':
          description: The request is accepted.
          headers:
            Location:
              schema:
                type: string
        '404':
          description: Not found.
          x-ms-error-response: true
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/savingsPlanOrders/{savingsPlanOrderId}/savingsPlans/{savingsPlanId}/validate/?api-version=2022-11-01:
    post:
      description: Validate savings plan patch.
      operationId: SavingsPlan_ValidateUpdate
      tags:
        - Savings plan
      parameters:
        - $ref: '#/components/parameters/SavingsPlanOrderIdParameter'
        - $ref: '#/components/parameters/SavingsPlanIdParameter'
      requestBody:
        description: Request body for validating a savings plan patch request
        required: true
        x-ms-requestBody-name: body
        x-ms-parameter-location: method
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavingsPlanUpdateValidateRequest'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanValidateResponse'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/validate/?api-version=2022-11-01:
    post:
      description: Validate savings plan purchase.
      operationId: ValidatePurchase
      tags:
        - Savings plan
      parameters: []
      requestBody:
        description: Request body for validating the purchase of a savings plan
        required: true
        x-ms-requestBody-name: body
        x-ms-parameter-location: method
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavingsPlanPurchaseValidateRequest'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavingsPlanValidateResponse'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /providers/Microsoft.BillingBenefits/reservationOrderAliases/{reservationOrderAliasName}/?api-version=2022-11-01:
    put:
      description: Create a reservation order alias.
      operationId: ReservationOrderAlias_Create
      tags:
        - Reservation order aliases
      x-ms-long-running-operation: true
      x-ms-long-running-operation-options:
        final-state-via: azure-async-operation
      parameters:
        - $ref: '#/components/parameters/ReservationOrderAliasNameParameter'
      requestBody:
        description: Request body for creating a reservation order alias
        required: true
        x-ms-requestBody-name: body
        x-ms-parameter-location: method
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReservationOrderAliasRequest'
      responses:
        '200':
          description: The resource has been created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReservationOrderAliasResponse'
        '201':
          description: The request is being processed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReservationOrderAliasResponse'
          headers:
            Azure-AsyncOperation:
              schema:
                type: string
              description: URL for checking the ongoing status of the operation.
            Retry-After:
              schema:
                format: int32
                type: integer
              description: Clients should wait for the Retry-After interval before polling again
        default:
          description: Error response describing why the operation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      description: Get a reservation order alias.
      operationId: ReservationOrderAlias_Get
      tags:
        - Reservation order aliases
      parameters:
        - $ref: '#/components/parameters/ReservationOrderAliasNameParameter'
      responses:
        '200':
          description: The request is completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReservationOrderAliasResponse'
        default:
          description: Unexpected error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
