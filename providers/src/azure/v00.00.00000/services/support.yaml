openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Support
  description: Support tickets, communications, file attachments and problem classifications.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-04-01-stackql-generated
  x-serviceAlias: support
  x-sdkPackage: azure-mgmt-support
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    MessageProperties:
      type: object
      description: Describes the properties of a Message Details resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        contentType:
          type: string
          description: Content type.
        communicationDirection:
          type: string
          description: 'Direction of communication. Known values are: "inbound" and "outbound".'
        sender:
          type: string
          description: Name of the sender.
        body:
          type: string
          description: Body of the communication.
        createdDate:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the communication was created.
    ChatTranscriptDetailsFlattened:
      type: object
      description: Flattened view of ChatTranscriptDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        messages:
          type: array
          items:
            $ref: "#/components/schemas/MessageProperties"
          description: List of chat transcript communication resources.
        startTime:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the chat began.
    PagedChatTranscriptDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ChatTranscriptDetailsFlattened"
        nextLink:
          type: string
    CommunicationDetailsFlattened:
      type: object
      description: Flattened view of CommunicationDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Id of the resource.
        name:
          type: string
          description: Name of the resource.
        type:
          type: string
          description: Type of the resource 'Microsoft.Support/communications'.
        communicationType:
          type: string
          description: 'Communication type. Known values are: "web" and "phone".'
        communicationDirection:
          type: string
          description: 'Direction of communication. Known values are: "inbound" and "outbound".'
        sender:
          type: string
          description: Email address of the sender. This property is required if called by a service principal.
        subject:
          type: string
          description: Subject of the communication. Required.
        body:
          type: string
          description: Body of the communication. Required.
        createdDate:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the communication was created.
    PagedCommunicationDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CommunicationDetailsFlattened"
        nextLink:
          type: string
    CheckNameAvailabilityOutput:
      type: object
      description: Output of check name availability API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        nameAvailable:
          type: boolean
          description: Indicates whether the name is available.
        reason:
          type: string
          description: The reason why the name is not available.
        message:
          type: string
          description: The detailed error message describing why the name is not available.
    CommunicationDetails:
      type: object
      description: Object that represents a Communication resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Id of the resource.
        name:
          type: string
          description: Name of the resource.
        type:
          type: string
          description: Type of the resource 'Microsoft.Support/communications'.
        properties:
          type: object
          properties:
            communicationType:
              type: string
              description: 'Communication type. Known values are: "web" and "phone".'
            communicationDirection:
              type: string
              description: 'Direction of communication. Known values are: "inbound" and "outbound".'
            sender:
              type: string
              description: Email address of the sender. This property is required if called by a service principal.
            subject:
              type: string
              description: Subject of the communication. Required.
            body:
              type: string
              description: Body of the communication. Required.
            createdDate:
              type: string
              format: date-time
              description: Time in UTC (ISO 8601 format) when the communication was created.
      required:
        - properties
    FileWorkspaceDetailsFlattened:
      type: object
      description: Flattened view of FileWorkspaceDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        createdOn:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when file workspace was created.
        expirationTime:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when file workspace is going to expire.
    FileWorkspaceDetails:
      type: object
      description: Object that represents FileWorkspaceDetails resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            createdOn:
              type: string
              format: date-time
              description: Time in UTC (ISO 8601 format) when file workspace was created.
            expirationTime:
              type: string
              format: date-time
              description: Time in UTC (ISO 8601 format) when file workspace is going to expire.
    FileDetailsFlattened:
      type: object
      description: Flattened view of FileDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        createdOn:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when file workspace was created.
        chunkSize:
          type: integer
          description: Size of each chunk. The size of each chunk should be provided in bytes and must not exceed 2.5 megabytes (MB).
        fileSize:
          type: integer
          description: Size of the file to be uploaded. The file size must not exceed 5 MB and should be provided in bytes.
        numberOfChunks:
          type: integer
          description: Number of chunks to be uploaded. The maximum number of allowed chunks is 2.
    PagedFileDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FileDetailsFlattened"
        nextLink:
          type: string
    FileDetails:
      type: object
      description: Object that represents File Details resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            createdOn:
              type: string
              format: date-time
              description: Time in UTC (ISO 8601 format) when file workspace was created.
            chunkSize:
              type: integer
              description: Size of each chunk. The size of each chunk should be provided in bytes and must not exceed 2.5 megabytes (MB).
            fileSize:
              type: integer
              description: Size of the file to be uploaded. The file size must not exceed 5 MB and should be provided in bytes.
            numberOfChunks:
              type: integer
              description: Number of chunks to be uploaded. The maximum number of allowed chunks is 2.
    OperationDisplay:
      type: object
      description: The object that describes the operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        description:
          type: string
          description: The description of the operation.
        operation:
          type: string
          description: The action that users can perform, based on their permission level.
        provider:
          type: string
          description: "Service provider: Microsoft Support."
        resource:
          type: string
          description: Resource on which the operation is performed.
    Operation:
      type: object
      description: The operation supported by Microsoft Support resource provider. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: "Operation name: {provider}/{resource}/{operation}."
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that describes the operation.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    SecondaryConsentEnabled:
      type: object
      description: This property indicates whether secondary consent is present for problem classification.
      properties:
        description:
          type: string
          description: User consent description.
        type:
          type: string
          description: The Azure service for which secondary consent is needed for case creation.
    ProblemClassificationFlattened:
      type: object
      description: Flattened view of ProblemClassification ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Id of the resource.
        name:
          type: string
          description: Name of the resource.
        type:
          type: string
          description: Type of the resource 'Microsoft.Support/problemClassification'.
        displayName:
          type: string
          description: Localized name of problem classification.
        secondaryConsentEnabled:
          type: array
          items:
            $ref: "#/components/schemas/SecondaryConsentEnabled"
          description: This property indicates whether secondary consent is present for problem classification.
    PagedProblemClassificationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ProblemClassificationFlattened"
        nextLink:
          type: string
    ServiceFlattened:
      type: object
      description: Flattened view of Service ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Id of the resource.
        name:
          type: string
          description: Name of the resource.
        type:
          type: string
          description: Type of the resource 'Microsoft.Support/services'.
        displayName:
          type: string
          description: Localized name of the Azure service.
        resourceTypes:
          type: array
          items:
            type: string
          description: ARM Resource types.
    PagedServiceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ServiceFlattened"
        nextLink:
          type: string
    ContactProfile:
      type: object
      description: Contact information associated with the support ticket. All required parameters must be populated in order to send to server.
      properties:
        firstName:
          type: string
          description: First name. Required.
        lastName:
          type: string
          description: Last name. Required.
        preferredContactMethod:
          type: string
          description: 'Preferred contact method. Required. Known values are: "email" and "phone".'
        primaryEmailAddress:
          type: string
          description: Primary email address. Required.
        additionalEmailAddresses:
          type: array
          items:
            type: string
          description: Additional email addresses listed will be copied on any correspondence about the support ticket.
        phoneNumber:
          type: string
          description: Phone number. This is required if preferred contact method is phone.
        preferredTimeZone:
          type: string
          description: Time zone of the user. This is the name of the time zone from `Microsoft Time Zone Index Values `_. Required.
        country:
          type: string
          description: Country of the user. This is the ISO 3166-1 alpha-3 code. Required.
        preferredSupportLanguage:
          type: string
          description: Preferred language of support from Azure. Support languages vary based on the severity you choose for your support ticket. Learn more at `Azure Severity and responsiveness `_. Use the standard language-country code. Valid values are 'en-us' for English, 'zh-hans' for Chinese, 'es-es' for Spanish, 'fr-fr' for French, 'ja-jp' for Japanese, 'ko-kr' for Korean, 'ru-ru' for Russian, 'pt-br' for Portuguese, 'it-it' for Italian, 'zh-tw' for Chinese and 'de-de' for German. Required.
      required:
        - firstName
        - lastName
        - preferredContactMethod
        - primaryEmailAddress
        - preferredTimeZone
        - country
        - preferredSupportLanguage
    ServiceLevelAgreement:
      type: object
      description: Service Level Agreement details for a support ticket. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        startTime:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the service level agreement starts.
        expirationTime:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the service level agreement expires.
        slaMinutes:
          type: integer
          description: Service Level Agreement in minutes.
    SupportEngineer:
      type: object
      description: Support engineer information. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        emailAddress:
          type: string
          description: Email address of the Azure Support engineer assigned to the support ticket.
    TechnicalTicketDetails:
      type: object
      description: Additional information for technical support ticket.
      properties:
        resourceId:
          type: string
          description: "This is the resource Id of the Azure service resource (For example: A virtual machine resource or an HDInsight resource) for which the support ticket is created."
    QuotaTicketDetails:
      type: object
      description: "Additional set of information required for quota increase support ticket for certain quota types, e.g.: Virtual machine cores. Get complete details about Quota payload support request along with examples at `Support quota request `_."
      properties:
        quotaChangeRequestSubType:
          type: string
          description: Required for certain quota types when there is a sub type, such as Batch, for which you are requesting a quota increase.
        quotaChangeRequestVersion:
          type: string
          description: Quota change request version.
        quotaChangeRequests:
          type: array
          items:
            $ref: "#/components/schemas/QuotaChangeRequest"
          description: This property is required for providing the region and new quota limits.
    QuotaChangeRequest:
      type: object
      description: This property is required for providing the region and new quota limits.
      properties:
        region:
          type: string
          description: Region for which the quota increase request is being made.
        payload:
          type: string
          description: Payload of the quota increase request.
    SecondaryConsent:
      type: object
      description: This property indicates secondary consent for the support ticket.
      properties:
        userConsent:
          type: string
          description: 'User consent value provided. Known values are: "Yes" and "No".'
        type:
          type: string
          description: The service name for which the secondary consent is being provided. The value needs to be retrieved from the Problem Classification API response.
    SupportTicketDetailsFlattened:
      type: object
      description: Flattened view of SupportTicketDetails ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Id of the resource.
        name:
          type: string
          description: Name of the resource.
        type:
          type: string
          description: Type of the resource 'Microsoft.Support/supportTickets'.
        supportTicketId:
          type: string
          description: System generated support ticket Id that is unique.
        description:
          type: string
          description: Detailed description of the question or issue. Required.
        problemClassificationId:
          type: string
          description: Each Azure service has its own set of issue categories, also known as problem classification. This parameter is the unique Id for the type of problem you are experiencing. Required.
        problemClassificationDisplayName:
          type: string
          description: Localized name of problem classification.
        severity:
          type: string
          description: "A value that indicates the urgency of the case, which in turn determines the response time according to the service level agreement of the technical support plan you have with Azure. Note: 'Highest critical impact', also known as the 'Emergency - Severe impact' level in the Azure portal is reserved only for our Premium customers. Required. Known values are: \"minimal\", \"moderate\", \"critical\", and \"highestcriticalimpact\"."
        enrollmentId:
          type: string
          description: Enrollment Id associated with the support ticket.
        require24X7Response:
          type: boolean
          description: Indicates if this requires a 24x7 response from Azure.
        advancedDiagnosticConsent:
          type: string
          description: 'Advanced diagnostic consent to be updated on the support ticket. Required. Known values are: "Yes" and "No".'
        problemScopingQuestions:
          type: string
          description: Problem scoping questions associated with the support ticket.
        supportPlanId:
          type: string
          description: Support plan id associated with the support ticket.
        contactDetails:
          $ref: "#/components/schemas/ContactProfile"
          description: Contact information of the user requesting to create a support ticket. Required.
        serviceLevelAgreement:
          $ref: "#/components/schemas/ServiceLevelAgreement"
          description: Service Level Agreement information for this support ticket.
        supportEngineer:
          $ref: "#/components/schemas/SupportEngineer"
          description: Information about the support engineer working on this support ticket.
        supportPlanType:
          type: string
          description: Support plan type associated with the support ticket.
        supportPlanDisplayName:
          type: string
          description: Support plan type associated with the support ticket.
        title:
          type: string
          description: Title of the support ticket. Required.
        problemStartTime:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the problem started.
        serviceId:
          type: string
          description: This is the resource Id of the Azure service resource associated with the support ticket. Required.
        serviceDisplayName:
          type: string
          description: Localized name of the Azure service.
        status:
          type: string
          description: Status of the support ticket.
        createdDate:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the support ticket was created.
        modifiedDate:
          type: string
          format: date-time
          description: Time in UTC (ISO 8601 format) when the support ticket was last modified.
        fileWorkspaceName:
          type: string
          description: File workspace name.
        isTemporaryTicket:
          type: string
          description: 'This property indicates if support ticket is a temporary ticket. Known values are: "Yes" and "No".'
        technicalTicketDetails:
          $ref: "#/components/schemas/TechnicalTicketDetails"
          description: Additional ticket details associated with a technical support ticket request.
        quotaTicketDetails:
          $ref: "#/components/schemas/QuotaTicketDetails"
          description: Additional ticket details associated with a quota support ticket request.
        secondaryConsent:
          type: array
          items:
            $ref: "#/components/schemas/SecondaryConsent"
          description: This property indicates secondary consents for the support ticket.
    PagedSupportTicketDetailsFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SupportTicketDetailsFlattened"
        nextLink:
          type: string
    UpdateContactProfile:
      type: object
      description: Contact information associated with the support ticket.
      properties:
        firstName:
          type: string
          description: First name.
        lastName:
          type: string
          description: Last name.
        preferredContactMethod:
          type: string
          description: 'Preferred contact method. Known values are: "email" and "phone".'
        primaryEmailAddress:
          type: string
          description: Primary email address.
        additionalEmailAddresses:
          type: array
          items:
            type: string
          description: Email addresses listed will be copied on any correspondence about the support ticket.
        phoneNumber:
          type: string
          description: Phone number. This is required if preferred contact method is phone.
        preferredTimeZone:
          type: string
          description: Time zone of the user. This is the name of the time zone from `Microsoft Time Zone Index Values `_.
        country:
          type: string
          description: Country of the user. This is the ISO 3166-1 alpha-3 code.
        preferredSupportLanguage:
          type: string
          description: Preferred language of support from Azure. Support languages vary based on the severity you choose for your support ticket. Learn more at `Azure Severity and responsiveness `_. Use the standard language-country code. Valid values are 'en-us' for English, 'zh-hans' for Chinese, 'es-es' for Spanish, 'fr-fr' for French, 'ja-jp' for Japanese, 'ko-kr' for Korean, 'ru-ru' for Russian, 'pt-br' for Portuguese, 'it-it' for Italian, 'zh-tw' for Chinese and 'de-de' for German.
    SupportTicketDetails:
      type: object
      description: Object that represents SupportTicketDetails resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Id of the resource.
        name:
          type: string
          description: Name of the resource.
        type:
          type: string
          description: Type of the resource 'Microsoft.Support/supportTickets'.
        properties:
          type: object
          properties:
            supportTicketId:
              type: string
              description: System generated support ticket Id that is unique.
            description:
              type: string
              description: Detailed description of the question or issue. Required.
            problemClassificationId:
              type: string
              description: Each Azure service has its own set of issue categories, also known as problem classification. This parameter is the unique Id for the type of problem you are experiencing. Required.
            problemClassificationDisplayName:
              type: string
              description: Localized name of problem classification.
            severity:
              type: string
              description: "A value that indicates the urgency of the case, which in turn determines the response time according to the service level agreement of the technical support plan you have with Azure. Note: 'Highest critical impact', also known as the 'Emergency - Severe impact' level in the Azure portal is reserved only for our Premium customers. Required. Known values are: \"minimal\", \"moderate\", \"critical\", and \"highestcriticalimpact\"."
            enrollmentId:
              type: string
              description: Enrollment Id associated with the support ticket.
            require24X7Response:
              type: boolean
              description: Indicates if this requires a 24x7 response from Azure.
            advancedDiagnosticConsent:
              type: string
              description: 'Advanced diagnostic consent to be updated on the support ticket. Required. Known values are: "Yes" and "No".'
            problemScopingQuestions:
              type: string
              description: Problem scoping questions associated with the support ticket.
            supportPlanId:
              type: string
              description: Support plan id associated with the support ticket.
            contactDetails:
              $ref: "#/components/schemas/ContactProfile"
              description: Contact information of the user requesting to create a support ticket. Required.
            serviceLevelAgreement:
              $ref: "#/components/schemas/ServiceLevelAgreement"
              description: Service Level Agreement information for this support ticket.
            supportEngineer:
              $ref: "#/components/schemas/SupportEngineer"
              description: Information about the support engineer working on this support ticket.
            supportPlanType:
              type: string
              description: Support plan type associated with the support ticket.
            supportPlanDisplayName:
              type: string
              description: Support plan type associated with the support ticket.
            title:
              type: string
              description: Title of the support ticket. Required.
            problemStartTime:
              type: string
              format: date-time
              description: Time in UTC (ISO 8601 format) when the problem started.
            serviceId:
              type: string
              description: This is the resource Id of the Azure service resource associated with the support ticket. Required.
            serviceDisplayName:
              type: string
              description: Localized name of the Azure service.
            status:
              type: string
              description: Status of the support ticket.
            createdDate:
              type: string
              format: date-time
              description: Time in UTC (ISO 8601 format) when the support ticket was created.
            modifiedDate:
              type: string
              format: date-time
              description: Time in UTC (ISO 8601 format) when the support ticket was last modified.
            fileWorkspaceName:
              type: string
              description: File workspace name.
            isTemporaryTicket:
              type: string
              description: 'This property indicates if support ticket is a temporary ticket. Known values are: "Yes" and "No".'
            technicalTicketDetails:
              $ref: "#/components/schemas/TechnicalTicketDetails"
              description: Additional ticket details associated with a technical support ticket request.
            quotaTicketDetails:
              $ref: "#/components/schemas/QuotaTicketDetails"
              description: Additional ticket details associated with a quota support ticket request.
            secondaryConsent:
              type: array
              items:
                $ref: "#/components/schemas/SecondaryConsent"
              description: This property indicates secondary consents for the support ticket.
      required:
        - properties
  x-stackQL-resources:
    chat_transcripts_no_subscription:
      id: azure.support.chat_transcripts_no_subscription
      name: chat_transcripts_no_subscription
      title: chat_transcripts_no_subscription
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1chatTranscripts~1{chat_transcript_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1chatTranscripts?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/chat_transcripts_no_subscription/methods/get"
          - $ref: "#/components/x-stackQL-resources/chat_transcripts_no_subscription/methods/list"
        insert: []
        update: []
        delete: []
    chat_transcripts:
      id: azure.support.chat_transcripts
      name: chat_transcripts
      title: chat_transcripts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1chatTranscripts~1{chat_transcript_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1chatTranscripts?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/chat_transcripts/methods/get"
          - $ref: "#/components/x-stackQL-resources/chat_transcripts/methods/list"
        insert: []
        update: []
        delete: []
    communications_no_subscription:
      id: azure.support.communications_no_subscription
      name: communications_no_subscription
      title: communications_no_subscription
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1communications~1{communication_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1communications~1{communication_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1communications?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        check_name_availability:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1checkNameAvailability?api-version=2024-04-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/communications_no_subscription/methods/check_name_availability"
          - $ref: "#/components/x-stackQL-resources/communications_no_subscription/methods/get"
          - $ref: "#/components/x-stackQL-resources/communications_no_subscription/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/communications_no_subscription/methods/create"
        update: []
        delete: []
    communications:
      id: azure.support.communications
      name: communications
      title: communications
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1communications~1{communication_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1communications~1{communication_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1communications?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}~1checkNameAvailability?api-version=2024-04-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/communications/methods/check_name_availability"
          - $ref: "#/components/x-stackQL-resources/communications/methods/get"
          - $ref: "#/components/x-stackQL-resources/communications/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/communications/methods/create"
        update: []
        delete: []
    file_workspaces_no_subscription:
      id: azure.support.file_workspaces_no_subscription
      name: file_workspaces_no_subscription
      title: file_workspaces_no_subscription
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/file_workspaces_no_subscription/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/file_workspaces_no_subscription/methods/create"
        update: []
        delete: []
    file_workspaces:
      id: azure.support.file_workspaces
      name: file_workspaces
      title: file_workspaces
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/file_workspaces/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/file_workspaces/methods/create"
        update: []
        delete: []
    files_no_subscription:
      id: azure.support.files_no_subscription
      name: files_no_subscription
      title: files_no_subscription
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files~1{file_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files~1{file_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        upload:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files~1{file_name}~1upload?api-version=2024-04-01/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/files_no_subscription/methods/get"
          - $ref: "#/components/x-stackQL-resources/files_no_subscription/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/files_no_subscription/methods/create"
        update: []
        delete: []
    files:
      id: azure.support.files
      name: files
      title: files
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files~1{file_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files~1{file_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        upload:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1fileWorkspaces~1{file_workspace_name}~1files~1{file_name}~1upload?api-version=2024-04-01/post"
          response:
            openAPIDocKey: "200"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/files/methods/get"
          - $ref: "#/components/x-stackQL-resources/files/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/files/methods/create"
        update: []
        delete: []
    operations:
      id: azure.support.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1operations?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    problem_classifications:
      id: azure.support.problem_classifications
      name: problem_classifications
      title: problem_classifications
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1services~1{service_name}~1problemClassifications~1{problem_classification_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1services~1{service_name}~1problemClassifications?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/problem_classifications/methods/get"
          - $ref: "#/components/x-stackQL-resources/problem_classifications/methods/list"
        insert: []
        update: []
        delete: []
    services:
      id: azure.support.services
      name: services
      title: services
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1services~1{service_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1services?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/services/methods/get"
          - $ref: "#/components/x-stackQL-resources/services/methods/list"
        insert: []
        update: []
        delete: []
    support_tickets_no_subscription:
      id: azure.support.support_tickets_no_subscription
      name: support_tickets_no_subscription
      title: support_tickets_no_subscription
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}?api-version=2024-04-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        create:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1supportTickets?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        check_name_availability:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Support~1checkNameAvailability?api-version=2024-04-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/support_tickets_no_subscription/methods/check_name_availability"
          - $ref: "#/components/x-stackQL-resources/support_tickets_no_subscription/methods/get"
          - $ref: "#/components/x-stackQL-resources/support_tickets_no_subscription/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/support_tickets_no_subscription/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/support_tickets_no_subscription/methods/update"
        delete: []
    support_tickets:
      id: azure.support.support_tickets
      name: support_tickets
      title: support_tickets
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}?api-version=2024-04-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets~1{support_ticket_name}?api-version=2024-04-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1supportTickets?api-version=2024-04-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Support~1checkNameAvailability?api-version=2024-04-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/support_tickets/methods/check_name_availability"
          - $ref: "#/components/x-stackQL-resources/support_tickets/methods/get"
          - $ref: "#/components/x-stackQL-resources/support_tickets/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/support_tickets/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/support_tickets/methods/update"
        delete: []
paths:
  /providers/Microsoft.Support/supportTickets/{support_ticket_name}/chatTranscripts/{chat_transcript_name}?api-version=2024-04-01:
    get:
      operationId: ChatTranscriptsNoSubscription_get
      description: Returns chatTranscript details for a no subscription support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: chat_transcript_name
          in: path
          required: true
          schema:
            type: string
          description: ChatTranscript name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatTranscriptDetailsFlattened"
  /providers/Microsoft.Support/supportTickets/{support_ticket_name}/chatTranscripts?api-version=2024-04-01:
    get:
      operationId: ChatTranscriptsNoSubscription_list
      description: Lists all chat transcripts for a support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedChatTranscriptDetailsFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/supportTickets/{support_ticket_name}/chatTranscripts/{chat_transcript_name}?api-version=2024-04-01:
    get:
      operationId: ChatTranscripts_get
      description: Returns chatTranscript details for a support ticket under a subscription.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: chat_transcript_name
          in: path
          required: true
          schema:
            type: string
          description: ChatTranscript name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatTranscriptDetailsFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/supportTickets/{support_ticket_name}/chatTranscripts?api-version=2024-04-01:
    get:
      operationId: ChatTranscripts_list
      description: Lists all chat transcripts for a support ticket under subscription.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedChatTranscriptDetailsFlattened"
  /providers/Microsoft.Support/supportTickets/{support_ticket_name}/communications/{communication_name}?api-version=2024-04-01:
    get:
      operationId: CommunicationsNoSubscription_get
      description: Returns communication details for a support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: communication_name
          in: path
          required: true
          schema:
            type: string
          description: Communication name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationDetailsFlattened"
    put:
      operationId: CommunicationsNoSubscription_begin_create
      description: Adds a new customer communication to an Azure support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: communication_name
          in: path
          required: true
          schema:
            type: string
          description: Communication name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    sender:
                      type: string
                      description: Email address of the sender. This property is required if called by a service principal.
                    subject:
                      type: string
                      description: Subject of the communication. Required.
                    body:
                      type: string
                      description: Body of the communication. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationDetails"
  /providers/Microsoft.Support/supportTickets/{support_ticket_name}/communications?api-version=2024-04-01:
    get:
      operationId: CommunicationsNoSubscription_list
      description: Lists all communications (attachments not included) for a support ticket. You can also filter support ticket communications by *CreatedDate* or *CommunicationType* using the $filter parameter. The only type of communication supported today is *Web*. Output will be a paged result with *nextLink*\ , using which you can retrieve the next set of Communication results. Support ticket data is available for 18 months after ticket creation. If a ticket was created more than 18 months ago, a request for data might cause an error.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The number of values to return in the collection. Default is 10 and max is 10. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. You can filter by communicationType and createdDate properties. CommunicationType supports Equals ('eq') operator and createdDate supports Greater Than ('gt') and Greater Than or Equals ('ge') operators. You may combine the CommunicationType and CreatedDate filters by Logical And ('and') operator. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCommunicationDetailsFlattened"
  /providers/Microsoft.Support/supportTickets/{support_ticket_name}/checkNameAvailability?api-version=2024-04-01:
    post:
      operationId: CommunicationsNoSubscription_check_name_availability
      description: Check the availability of a resource name. This API should be used to check the uniqueness of the name for adding a new communication to the support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The resource name to validate. Required.
                type:
                  type: string
                  description: 'The type of resource. Required. Known values are: "Microsoft.Support/supportTickets" and "Microsoft.Support/communications".'
              required:
                - name
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityOutput"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/supportTickets/{support_ticket_name}/communications/{communication_name}?api-version=2024-04-01:
    get:
      operationId: Communications_get
      description: Returns communication details for a support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: communication_name
          in: path
          required: true
          schema:
            type: string
          description: Communication name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationDetailsFlattened"
    put:
      operationId: Communications_begin_create
      description: Adds a new customer communication to an Azure support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: communication_name
          in: path
          required: true
          schema:
            type: string
          description: Communication name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    sender:
                      type: string
                      description: Email address of the sender. This property is required if called by a service principal.
                    subject:
                      type: string
                      description: Subject of the communication. Required.
                    body:
                      type: string
                      description: Body of the communication. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationDetails"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/supportTickets/{support_ticket_name}/communications?api-version=2024-04-01:
    get:
      operationId: Communications_list
      description: Lists all communications (attachments not included) for a support ticket. You can also filter support ticket communications by *CreatedDate* or *CommunicationType* using the $filter parameter. The only type of communication supported today is *Web*. Output will be a paged result with *nextLink*\ , using which you can retrieve the next set of Communication results. Support ticket data is available for 18 months after ticket creation. If a ticket was created more than 18 months ago, a request for data might cause an error.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The number of values to return in the collection. Default is 10 and max is 10. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. You can filter by communicationType and createdDate properties. CommunicationType supports Equals ('eq') operator and createdDate supports Greater Than ('gt') and Greater Than or Equals ('ge') operators. You may combine the CommunicationType and CreatedDate filters by Logical And ('and') operator. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCommunicationDetailsFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/supportTickets/{support_ticket_name}/checkNameAvailability?api-version=2024-04-01:
    post:
      operationId: Communications_check_name_availability
      description: Check the availability of a resource name. This API should be used to check the uniqueness of the name for adding a new communication to the support ticket.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The resource name to validate. Required.
                type:
                  type: string
                  description: 'The type of resource. Required. Known values are: "Microsoft.Support/supportTickets" and "Microsoft.Support/communications".'
              required:
                - name
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityOutput"
  /providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}?api-version=2024-04-01:
    get:
      operationId: FileWorkspacesNoSubscription_get
      description: Gets details for a specific file workspace.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File Workspace Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileWorkspaceDetailsFlattened"
    put:
      operationId: FileWorkspacesNoSubscription_create
      description: Creates a new file workspace.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File workspace name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileWorkspaceDetails"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}?api-version=2024-04-01:
    get:
      operationId: FileWorkspaces_get
      description: Gets details for a specific file workspace in an Azure subscription.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File Workspace Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileWorkspaceDetailsFlattened"
    put:
      operationId: FileWorkspaces_create
      description: Creates a new file workspace for the specified subscription.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File workspace name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileWorkspaceDetails"
  /providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}/files/{file_name}?api-version=2024-04-01:
    get:
      operationId: FilesNoSubscription_get
      description: Returns details of a specific file in a work space.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File Workspace Name. Required.
        - name: file_name
          in: path
          required: true
          schema:
            type: string
          description: File Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileDetailsFlattened"
    put:
      operationId: FilesNoSubscription_create
      description: Creates a new file under a workspace.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File workspace name. Required.
        - name: file_name
          in: path
          required: true
          schema:
            type: string
          description: File name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    chunkSize:
                      type: integer
                      description: Size of each chunk. The size of each chunk should be provided in bytes and must not exceed 2.5 megabytes (MB).
                    fileSize:
                      type: integer
                      description: Size of the file to be uploaded. The file size must not exceed 5 MB and should be provided in bytes.
                    numberOfChunks:
                      type: integer
                      description: Number of chunks to be uploaded. The maximum number of allowed chunks is 2.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileDetails"
  /providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}/files?api-version=2024-04-01:
    get:
      operationId: FilesNoSubscription_list
      description: Lists all the Files information under a workspace for an Azure subscription.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File Workspace Name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFileDetailsFlattened"
  /providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}/files/{file_name}/upload?api-version=2024-04-01:
    post:
      operationId: FilesNoSubscription_upload
      description: This API allows you to upload content to a file.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File WorkspaceName. Required.
        - name: file_name
          in: path
          required: true
          schema:
            type: string
          description: File Name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                content:
                  type: string
                  description: File Content in base64 encoded format.
                chunkIndex:
                  type: integer
                  description: Index of the uploaded chunk (Index starts at 0).
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}/files/{file_name}?api-version=2024-04-01:
    get:
      operationId: Files_get
      description: Returns details of a specific file in a work space.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File Workspace Name. Required.
        - name: file_name
          in: path
          required: true
          schema:
            type: string
          description: File Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileDetailsFlattened"
    put:
      operationId: Files_create
      description: Creates a new file under a workspace for the specified subscription.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File workspace name. Required.
        - name: file_name
          in: path
          required: true
          schema:
            type: string
          description: File name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    chunkSize:
                      type: integer
                      description: Size of each chunk. The size of each chunk should be provided in bytes and must not exceed 2.5 megabytes (MB).
                    fileSize:
                      type: integer
                      description: Size of the file to be uploaded. The file size must not exceed 5 MB and should be provided in bytes.
                    numberOfChunks:
                      type: integer
                      description: Number of chunks to be uploaded. The maximum number of allowed chunks is 2.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileDetails"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}/files?api-version=2024-04-01:
    get:
      operationId: Files_list
      description: Lists all the Files information under a workspace for an Azure subscription.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File Workspace Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFileDetailsFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/fileWorkspaces/{file_workspace_name}/files/{file_name}/upload?api-version=2024-04-01:
    post:
      operationId: Files_upload
      description: This API allows you to upload content to a file.
      parameters:
        - name: file_workspace_name
          in: path
          required: true
          schema:
            type: string
          description: File WorkspaceName. Required.
        - name: file_name
          in: path
          required: true
          schema:
            type: string
          description: File Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                content:
                  type: string
                  description: File Content in base64 encoded format.
                chunkIndex:
                  type: integer
                  description: Index of the uploaded chunk (Index starts at 0).
      responses:
        "200":
          description: Success
  /providers/Microsoft.Support/operations?api-version=2024-04-01:
    get:
      operationId: support_list
      description: This lists all the available Microsoft Support REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /providers/Microsoft.Support/services/{service_name}/problemClassifications/{problem_classification_name}?api-version=2024-04-01:
    get:
      operationId: ProblemClassifications_get
      description: Get problem classification details for a specific Azure service.
      parameters:
        - name: service_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Azure service available for support. Required.
        - name: problem_classification_name
          in: path
          required: true
          schema:
            type: string
          description: Name of problem classification. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemClassificationFlattened"
  /providers/Microsoft.Support/services/{service_name}/problemClassifications?api-version=2024-04-01:
    get:
      operationId: ProblemClassifications_list
      description: Lists all the problem classifications (categories) available for a specific Azure service. Always use the service and problem classifications obtained programmatically. This practice ensures that you always have the most recent set of service and problem classification Ids.
      parameters:
        - name: service_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Azure service for which the problem classifications need to be retrieved. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedProblemClassificationFlattened"
  /providers/Microsoft.Support/services/{service_name}?api-version=2024-04-01:
    get:
      operationId: Services_get
      description: Gets a specific Azure service for support ticket creation.
      parameters:
        - name: service_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Azure service. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceFlattened"
  /providers/Microsoft.Support/services?api-version=2024-04-01:
    get:
      operationId: Services_list
      description: Lists all the Azure services available for support ticket creation. For **Technical** issues, select the Service Id that maps to the Azure service/product as displayed in the **Services** drop-down list on the Azure portal's `New support request `_ page. Always use the service and its corresponding problem classification(s) obtained programmatically for support ticket creation. This practice ensures that you always have the most recent set of service and problem classification Ids.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedServiceFlattened"
  /providers/Microsoft.Support/supportTickets/{support_ticket_name}?api-version=2024-04-01:
    get:
      operationId: SupportTicketsNoSubscription_get
      description: Gets details for a specific support ticket. Support ticket data is available for 18 months after ticket creation. If a ticket was created more than 18 months ago, a request for data might cause an error.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketDetailsFlattened"
    patch:
      operationId: SupportTicketsNoSubscription_update
      description: "This API allows you to update the severity level, ticket status, and your contact information in the support ticket.Note: The severity levels cannot be changed if a support ticket is actively being worked upon by an Azure support engineer. In such a case, contact your support engineer to request severity update by adding a new communication using the Communications API."
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                severity:
                  type: string
                  description: 'Severity level. Known values are: "minimal", "moderate", "critical", and "highestcriticalimpact".'
                status:
                  type: string
                  description: 'Status to be updated on the ticket. Known values are: "open" and "closed".'
                contactDetails:
                  $ref: "#/components/schemas/UpdateContactProfile"
                  description: Contact details to be updated on the support ticket.
                advancedDiagnosticConsent:
                  type: string
                  description: 'Advanced diagnostic consent to be updated on the support ticket. Known values are: "Yes" and "No".'
                secondaryConsent:
                  type: array
                  items:
                    $ref: "#/components/schemas/SecondaryConsent"
                  description: This property indicates secondary consents for the support ticket.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketDetails"
    put:
      operationId: SupportTicketsNoSubscription_begin_create
      description: Creates a new support ticket for Billing, and Subscription Management issues. Learn the `prerequisites `_ required to create a support ticket.Always call the Services and ProblemClassifications API to get the most recent set of services and problem categories required for support ticket creation.Adding attachments is not currently supported via the API. To add a file to an existing support ticket, visit the `Manage support ticket `_ page in the Azure portal, select the support ticket, and use the file upload control to add a new file.Providing consent to share diagnostic information with Azure support is currently not supported via the API. The Azure support engineer working on your ticket will reach out to you for consent if your issue requires gathering diagnostic information from your Azure resources..
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    supportTicketId:
                      type: string
                      description: System generated support ticket Id that is unique.
                    description:
                      type: string
                      description: Detailed description of the question or issue. Required.
                    problemClassificationId:
                      type: string
                      description: Each Azure service has its own set of issue categories, also known as problem classification. This parameter is the unique Id for the type of problem you are experiencing. Required.
                    severity:
                      type: string
                      description: "A value that indicates the urgency of the case, which in turn determines the response time according to the service level agreement of the technical support plan you have with Azure. Note: 'Highest critical impact', also known as the 'Emergency - Severe impact' level in the Azure portal is reserved only for our Premium customers. Required. Known values are: \"minimal\", \"moderate\", \"critical\", and \"highestcriticalimpact\"."
                    enrollmentId:
                      type: string
                      description: Enrollment Id associated with the support ticket.
                    require24X7Response:
                      type: boolean
                      description: Indicates if this requires a 24x7 response from Azure.
                    advancedDiagnosticConsent:
                      type: string
                      description: 'Advanced diagnostic consent to be updated on the support ticket. Required. Known values are: "Yes" and "No".'
                    problemScopingQuestions:
                      type: string
                      description: Problem scoping questions associated with the support ticket.
                    supportPlanId:
                      type: string
                      description: Support plan id associated with the support ticket.
                    contactDetails:
                      $ref: "#/components/schemas/ContactProfile"
                      description: Contact information of the user requesting to create a support ticket. Required.
                    serviceLevelAgreement:
                      $ref: "#/components/schemas/ServiceLevelAgreement"
                      description: Service Level Agreement information for this support ticket.
                    supportEngineer:
                      $ref: "#/components/schemas/SupportEngineer"
                      description: Information about the support engineer working on this support ticket.
                    title:
                      type: string
                      description: Title of the support ticket. Required.
                    problemStartTime:
                      type: string
                      format: date-time
                      description: Time in UTC (ISO 8601 format) when the problem started.
                    serviceId:
                      type: string
                      description: This is the resource Id of the Azure service resource associated with the support ticket. Required.
                    fileWorkspaceName:
                      type: string
                      description: File workspace name.
                    technicalTicketDetails:
                      $ref: "#/components/schemas/TechnicalTicketDetails"
                      description: Additional ticket details associated with a technical support ticket request.
                    quotaTicketDetails:
                      $ref: "#/components/schemas/QuotaTicketDetails"
                      description: Additional ticket details associated with a quota support ticket request.
                    secondaryConsent:
                      type: array
                      items:
                        $ref: "#/components/schemas/SecondaryConsent"
                      description: This property indicates secondary consents for the support ticket.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketDetails"
  /providers/Microsoft.Support/supportTickets?api-version=2024-04-01:
    get:
      operationId: SupportTicketsNoSubscription_list
      description: Lists all the support tickets. You can also filter the support tickets by Status, CreatedDate, , ServiceId, and ProblemClassificationId using the $filter parameter. Output will be a paged result with nextLink, using which you can retrieve the next set of support tickets. Support ticket data is available for 18 months after ticket creation. If a ticket was created more than 18 months ago, a request for data might cause an error.
      parameters:
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The number of values to return in the collection. Default is 25 and max is 100. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. We support 'odata v4.0' filter semantics.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSupportTicketDetailsFlattened"
  /providers/Microsoft.Support/checkNameAvailability?api-version=2024-04-01:
    post:
      operationId: SupportTicketsNoSubscription_check_name_availability
      description: Check the availability of a resource name. This API should be used to check the uniqueness of the name for support ticket creation for the selected subscription.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The resource name to validate. Required.
                type:
                  type: string
                  description: 'The type of resource. Required. Known values are: "Microsoft.Support/supportTickets" and "Microsoft.Support/communications".'
              required:
                - name
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityOutput"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/supportTickets/{support_ticket_name}?api-version=2024-04-01:
    get:
      operationId: SupportTickets_get
      description: Get ticket details for an Azure subscription. Support ticket data is available for 18 months after ticket creation. If a ticket was created more than 18 months ago, a request for data might cause an error.
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketDetailsFlattened"
    patch:
      operationId: SupportTickets_update
      description: "This API allows you to update the severity level, ticket status, advanced diagnostic consent and your contact information in the support ticket.Note: The severity levels cannot be changed if a support ticket is actively being worked upon by an Azure support engineer. In such a case, contact your support engineer to request severity update by adding a new communication using the Communications API."
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                severity:
                  type: string
                  description: 'Severity level. Known values are: "minimal", "moderate", "critical", and "highestcriticalimpact".'
                status:
                  type: string
                  description: 'Status to be updated on the ticket. Known values are: "open" and "closed".'
                contactDetails:
                  $ref: "#/components/schemas/UpdateContactProfile"
                  description: Contact details to be updated on the support ticket.
                advancedDiagnosticConsent:
                  type: string
                  description: 'Advanced diagnostic consent to be updated on the support ticket. Known values are: "Yes" and "No".'
                secondaryConsent:
                  type: array
                  items:
                    $ref: "#/components/schemas/SecondaryConsent"
                  description: This property indicates secondary consents for the support ticket.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketDetails"
    put:
      operationId: SupportTickets_begin_create
      description: "Creates a new support ticket for Subscription and Service limits (Quota), Technical, Billing, and Subscription Management issues for the specified subscription. Learn the `prerequisites `_ required to create a support ticket.Always call the Services and ProblemClassifications API to get the most recent set of services and problem categories required for support ticket creation.Adding attachments is not currently supported via the API. To add a file to an existing support ticket, visit the `Manage support ticket `_ page in the Azure portal, select the support ticket, and use the file upload control to add a new file.Providing consent to share diagnostic information with Azure support is currently not supported via the API. The Azure support engineer working on your ticket will reach out to you for consent if your issue requires gathering diagnostic information from your Azure resources.\\ **Creating a support ticket for on-behalf-of**\\ : Include *x-ms-authorization-auxiliary* header to provide an auxiliary token as per `documentation `_. The primary token will be from the tenant for whom a support ticket is being raised against the subscription, i.e. Cloud solution provider (CSP) customer tenant. The auxiliary token will be from the Cloud solution provider (CSP) partner tenant."
      parameters:
        - name: support_ticket_name
          in: path
          required: true
          schema:
            type: string
          description: Support ticket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    supportTicketId:
                      type: string
                      description: System generated support ticket Id that is unique.
                    description:
                      type: string
                      description: Detailed description of the question or issue. Required.
                    problemClassificationId:
                      type: string
                      description: Each Azure service has its own set of issue categories, also known as problem classification. This parameter is the unique Id for the type of problem you are experiencing. Required.
                    severity:
                      type: string
                      description: "A value that indicates the urgency of the case, which in turn determines the response time according to the service level agreement of the technical support plan you have with Azure. Note: 'Highest critical impact', also known as the 'Emergency - Severe impact' level in the Azure portal is reserved only for our Premium customers. Required. Known values are: \"minimal\", \"moderate\", \"critical\", and \"highestcriticalimpact\"."
                    enrollmentId:
                      type: string
                      description: Enrollment Id associated with the support ticket.
                    require24X7Response:
                      type: boolean
                      description: Indicates if this requires a 24x7 response from Azure.
                    advancedDiagnosticConsent:
                      type: string
                      description: 'Advanced diagnostic consent to be updated on the support ticket. Required. Known values are: "Yes" and "No".'
                    problemScopingQuestions:
                      type: string
                      description: Problem scoping questions associated with the support ticket.
                    supportPlanId:
                      type: string
                      description: Support plan id associated with the support ticket.
                    contactDetails:
                      $ref: "#/components/schemas/ContactProfile"
                      description: Contact information of the user requesting to create a support ticket. Required.
                    serviceLevelAgreement:
                      $ref: "#/components/schemas/ServiceLevelAgreement"
                      description: Service Level Agreement information for this support ticket.
                    supportEngineer:
                      $ref: "#/components/schemas/SupportEngineer"
                      description: Information about the support engineer working on this support ticket.
                    title:
                      type: string
                      description: Title of the support ticket. Required.
                    problemStartTime:
                      type: string
                      format: date-time
                      description: Time in UTC (ISO 8601 format) when the problem started.
                    serviceId:
                      type: string
                      description: This is the resource Id of the Azure service resource associated with the support ticket. Required.
                    fileWorkspaceName:
                      type: string
                      description: File workspace name.
                    technicalTicketDetails:
                      $ref: "#/components/schemas/TechnicalTicketDetails"
                      description: Additional ticket details associated with a technical support ticket request.
                    quotaTicketDetails:
                      $ref: "#/components/schemas/QuotaTicketDetails"
                      description: Additional ticket details associated with a quota support ticket request.
                    secondaryConsent:
                      type: array
                      items:
                        $ref: "#/components/schemas/SecondaryConsent"
                      description: This property indicates secondary consents for the support ticket.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketDetails"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/supportTickets?api-version=2024-04-01:
    get:
      operationId: SupportTickets_list
      description: Lists all the support tickets for an Azure subscription. You can also filter the support tickets by *Status*\ , *CreatedDate*\ , *ServiceId*\ , and *ProblemClassificationId* using the $filter parameter. Output will be a paged result with *nextLink*\ , using which you can retrieve the next set of support tickets. Support ticket data is available for 18 months after ticket creation. If a ticket was created more than 18 months ago, a request for data might cause an error.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The number of values to return in the collection. Default is 25 and max is 100. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. We support 'odata v4.0' filter semantics. `Learn more `_. *Status*\ , *ServiceId*\ , and *ProblemClassificationId* filters can only be used with Equals ('eq') operator. For *CreatedDate* filter, the supported operators are Greater Than ('gt') and Greater Than or Equals ('ge'). When using both filters, combine them using the logical 'AND'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSupportTicketDetailsFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Support/checkNameAvailability?api-version=2024-04-01:
    post:
      operationId: SupportTickets_check_name_availability
      description: Check the availability of a resource name. This API should be used to check the uniqueness of the name for support ticket creation for the selected subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The resource name to validate. Required.
                type:
                  type: string
                  description: 'The type of resource. Required. Known values are: "Microsoft.Support/supportTickets" and "Microsoft.Support/communications".'
              required:
                - name
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityOutput"
