openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Online Experimentation Mgmt Client
  description: Microsoft.OnlineExperimentation Resource Provider management API.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-05-31-preview-stackql-generated
  x-serviceAlias: onlineexperimentation
  x-sdkPackage: azure-mgmt-onlineexperimentation
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    OnlineExperimentationWorkspaceProperties:
      type: object
      description: The properties of an online experimentation workspace.
      properties:
        workspaceId:
          type: string
          description: The Id of the workspace.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state for the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
        logAnalyticsWorkspaceResourceId:
          type: string
          description: The resource identifier of the Log Analytics workspace which online experimentation workspace uses for generating experiment analysis results. Required.
        logsExporterStorageAccountResourceId:
          type: string
          description: The resource identifier of storage account where logs are exported from Log Analytics workspace. online experimentation workspace uses it generating experiment analysis results. Required.
        appConfigurationResourceId:
          type: string
          description: The resource identifier of App Configuration with which this online experimentation workspace is tied for experimentation. This is a required field for creating an online experimentation workspace. Required.
        encryption:
          $ref: "#/components/schemas/ResourceEncryptionConfiguration"
          description: The encryption configuration for the online experimentation workspace resource.
        endpoint:
          type: string
          description: The data plane endpoint for the online experimentation workspace resource.
      required:
        - logAnalyticsWorkspaceResourceId
        - logsExporterStorageAccountResourceId
        - appConfigurationResourceId
    ResourceProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
    ResourceEncryptionConfiguration:
      type: object
      description: The encryption configuration for the online experimentation workspace resource.
      properties:
        customerManagedKeyEncryption:
          $ref: "#/components/schemas/CustomerManagedKeyEncryption"
          description: All Customer-managed key encryption properties for the resource.
    CustomerManagedKeyEncryption:
      type: object
      description: Customer-managed key encryption properties for the resource.
      properties:
        keyEncryptionKeyIdentity:
          $ref: "#/components/schemas/KeyEncryptionKeyIdentity"
          description: All identity configuration for Customer-managed key settings defining which identity should be used to auth to Key Vault.
        keyEncryptionKeyUrl:
          type: string
          description: "key encryption key Url, versioned or non-versioned. Ex: `https://contosovault.vault.azure.net/keys/contosokek/562a4bb76b524a1493a6afe8e536ee78 `_ or `https://contosovault.vault.azure.net/keys/contosokek `_."
    KeyEncryptionKeyIdentity:
      type: object
      description: All identity configuration for Customer-managed key settings defining which identity should be used to auth to Key Vault.
      properties:
        identityType:
          $ref: "#/components/schemas/KeyEncryptionKeyIdentityType"
          description: 'The type of identity to use. Values can be systemAssignedIdentity, userAssignedIdentity, or delegatedResourceIdentity. Known values are: "SystemAssignedIdentity" and "UserAssignedIdentity".'
        userAssignedIdentityResourceId:
          type: string
          description: "User assigned identity to use for accessing key encryption key Url. Ex: /subscriptions/fa5fc227-a624-475e-b696-cdd604c735bc/resourceGroups//providers/Microsoft.ManagedIdentity/userAssignedIdentities/myId. Mutually exclusive with identityType systemAssignedIdentity."
        federatedClientId:
          type: string
          description: "application client identity to use for accessing key encryption key Url in a different tenant. Ex: f83c6b1b-4d34-47e4-bb34-9d83df58b540."
    KeyEncryptionKeyIdentityType:
      type: string
      enum:
        - SystemAssignedIdentity
        - UserAssignedIdentity
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    OnlineExperimentationWorkspaceSku:
      type: object
      description: The SKU (Stock Keeping Unit) assigned to this resource.
      properties:
        name:
          $ref: "#/components/schemas/OnlineExperimentationWorkspaceSkuName"
          description: 'The name of the SKU. Ex - F0, P0. It is typically a letter+number code. Required. Known values are: "F0", "S0", "P0", and "D0".'
        tier:
          $ref: "#/components/schemas/OnlineExperimentationWorkspaceSkuTier"
          description: 'The name of the SKU tier. Known values are: "Free", "Standard", "Premium", and "Developer".'
      required:
        - name
    OnlineExperimentationWorkspaceSkuName:
      type: string
      enum:
        - F0
        - S0
        - P0
        - D0
    OnlineExperimentationWorkspaceSkuTier:
      type: string
      enum:
        - Free
        - Standard
        - Premium
        - Developer
    OnlineExperimentationWorkspaceFlattened:
      type: object
      description: Flattened view of OnlineExperimentationWorkspace ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        sku:
          $ref: "#/components/schemas/OnlineExperimentationWorkspaceSku"
          description: The SKU (Stock Keeping Unit) assigned to this resource.
        workspaceId:
          type: string
          description: The Id of the workspace.
        provisioningState:
          $ref: "#/components/schemas/ResourceProvisioningState"
          description: 'The provisioning state for the resource. Known values are: "Succeeded", "Failed", and "Canceled".'
        logAnalyticsWorkspaceResourceId:
          type: string
          description: The resource identifier of the Log Analytics workspace which online experimentation workspace uses for generating experiment analysis results. Required.
        logsExporterStorageAccountResourceId:
          type: string
          description: The resource identifier of storage account where logs are exported from Log Analytics workspace. online experimentation workspace uses it generating experiment analysis results. Required.
        appConfigurationResourceId:
          type: string
          description: The resource identifier of App Configuration with which this online experimentation workspace is tied for experimentation. This is a required field for creating an online experimentation workspace. Required.
        encryption:
          $ref: "#/components/schemas/ResourceEncryptionConfiguration"
          description: The encryption configuration for the online experimentation workspace resource.
        endpoint:
          type: string
          description: The data plane endpoint for the online experimentation workspace resource.
    PagedOnlineExperimentationWorkspaceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OnlineExperimentationWorkspaceFlattened"
        nextLink:
          type: string
    OnlineExperimentationWorkspace:
      type: object
      description: An online experimentation workspace resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/OnlineExperimentationWorkspaceProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        sku:
          $ref: "#/components/schemas/OnlineExperimentationWorkspaceSku"
          description: The SKU (Stock Keeping Unit) assigned to this resource.
      required:
        - location
    OnlineExperimentationWorkspacePatchProperties:
      type: object
      description: OnlineExperimentationWorkspacePatchProperties.
      properties:
        logAnalyticsWorkspaceResourceId:
          type: string
          description: The resource identifier of the Log Analytics workspace which online experimentation workspace uses for generating experiment analysis results.
        logsExporterStorageAccountResourceId:
          type: string
          description: The resource identifier of storage account where logs are exported from Log Analytics workspace. online experimentation workspace uses it generating experiment analysis results.
        encryption:
          $ref: "#/components/schemas/ResourceEncryptionConfiguration"
          description: The encryption configuration for the online experimentation workspace resource.
    OperationDisplay:
      type: object
      description: Localized display information for and operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    online_experimentation_workspaces:
      id: azure.onlineexperimentation.online_experimentation_workspaces
      name: online_experimentation_workspaces
      title: online_experimentation_workspaces
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.OnlineExperimentation~1workspaces~1{workspace_name}?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.OnlineExperimentation~1workspaces~1{workspace_name}?api-version=2025-05-31-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.OnlineExperimentation~1workspaces~1{workspace_name}?api-version=2025-05-31-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.OnlineExperimentation~1workspaces~1{workspace_name}?api-version=2025-05-31-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.OnlineExperimentation~1workspaces?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.OnlineExperimentation~1workspaces?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/online_experimentation_workspaces/methods/get"
          - $ref: "#/components/x-stackQL-resources/online_experimentation_workspaces/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/online_experimentation_workspaces/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/online_experimentation_workspaces/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/online_experimentation_workspaces/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/online_experimentation_workspaces/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/online_experimentation_workspaces/methods/delete"
    operations:
      id: azure.onlineexperimentation.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.OnlineExperimentation~1operations?api-version=2025-05-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.OnlineExperimentation/workspaces/{workspace_name}?api-version=2025-05-31-preview:
    get:
      operationId: OnlineExperimentationWorkspaces_get
      description: Gets an online experimentation workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OnlineExperimentationWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnlineExperimentationWorkspaceFlattened"
    put:
      operationId: OnlineExperimentationWorkspaces_begin_create_or_update
      description: Create an online experimentation workspace, or update an existing workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OnlineExperimentationWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/OnlineExperimentationWorkspaceProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                sku:
                  $ref: "#/components/schemas/OnlineExperimentationWorkspaceSku"
                  description: The SKU (Stock Keeping Unit) assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnlineExperimentationWorkspace"
    patch:
      operationId: OnlineExperimentationWorkspaces_begin_update
      description: Patch an online experimentation workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OnlineExperimentationWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                sku:
                  $ref: "#/components/schemas/OnlineExperimentationWorkspaceSku"
                  description: The SKU (Stock Keeping Unit) assigned to this resource.
                properties:
                  $ref: "#/components/schemas/OnlineExperimentationWorkspacePatchProperties"
                  description: Updatable properties of the online experimentation workspace resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnlineExperimentationWorkspace"
    delete:
      operationId: OnlineExperimentationWorkspaces_begin_delete
      description: Deletes an online experimentation workspace.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: workspace_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the OnlineExperimentationWorkspace. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.OnlineExperimentation/workspaces?api-version=2025-05-31-preview:
    get:
      operationId: OnlineExperimentationWorkspaces_list_by_resource_group
      description: Gets all online experimentation workspaces in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOnlineExperimentationWorkspaceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.OnlineExperimentation/workspaces?api-version=2025-05-31-preview:
    get:
      operationId: OnlineExperimentationWorkspaces_list_by_subscription
      description: Gets all online experimentation workspaces in the specified subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOnlineExperimentationWorkspaceFlattened"
  /providers/Microsoft.OnlineExperimentation/operations?api-version=2025-05-31-preview:
    get:
      operationId: onlineexperimentation_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
