openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Resource Relationships
  description: Dependency and connection relationships between resources (Microsoft.Relationships).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-09-01-preview-stackql-generated
  x-serviceAlias: relationships
  x-sdkPackage: azure-mgmt-relationships
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    DependencyOfRelationshipProperties:
      type: object
      description: dependencyOf relationship properties.
      properties:
        sourceId:
          type: string
          description: The relationship source resource id. Required.
        targetId:
          type: string
          description: The relationship target resource id. Required.
        targetTenant:
          type: string
          description: The relationship target tenant id.
        originInformation:
          $ref: "#/components/schemas/RelationshipOriginInformation"
          description: Information about the origin of the relationship. Required.
        metadata:
          $ref: "#/components/schemas/RelationshipMetadata"
          description: Metadata about the relationship. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the relationship. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - targetId
    RelationshipOriginInformation:
      type: object
      description: Provides information about the origin of a relationship.
      properties:
        relationshipOriginType:
          $ref: "#/components/schemas/RelationshipOrigins"
          description: 'Identifies the origin type of the relationship. Required. Known values are: "ServiceExplicitlyCreated", "SystemDiscoveredByRule", "UserExplicitlyCreated", and "UserDiscoveredByRule".'
        discoveryEngine:
          type: string
          description: The name of the discovery engine that created the relationship.
    RelationshipOrigins:
      type: string
      enum:
        - ServiceExplicitlyCreated
        - SystemDiscoveredByRule
        - UserExplicitlyCreated
        - UserDiscoveredByRule
    RelationshipMetadata:
      type: object
      description: Provides information about the relationship properties.
      properties:
        sourceType:
          type: string
          description: The type of the relationship source resource. Required.
        targetType:
          type: string
          description: The type of the relationship target resource. Required.
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
        - Accepted
    DependencyOfRelationshipFlattened:
      type: object
      description: Flattened view of DependencyOfRelationship ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        sourceId:
          type: string
          description: The relationship source resource id. Required.
        targetId:
          type: string
          description: The relationship target resource id. Required.
        targetTenant:
          type: string
          description: The relationship target tenant id.
        originInformation:
          $ref: "#/components/schemas/RelationshipOriginInformation"
          description: Information about the origin of the relationship. Required.
        metadata:
          $ref: "#/components/schemas/RelationshipMetadata"
          description: Metadata about the relationship. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the relationship. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    DependencyOfRelationship:
      type: object
      description: Defines a dependencyOf relationship resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/DependencyOfRelationshipProperties"
          description: The resource-specific properties for this resource.
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    ServiceGroupMemberRelationshipProperties:
      type: object
      description: ServiceGroupMember relationship properties.
      properties:
        sourceId:
          type: string
          description: The relationship source resource id. Required.
        targetId:
          type: string
          description: The relationship target resource id. Required.
        targetTenant:
          type: string
          description: The relationship target tenant id.
        originInformation:
          $ref: "#/components/schemas/RelationshipOriginInformation"
          description: Information about the origin of the relationship. Required.
        metadata:
          $ref: "#/components/schemas/RelationshipMetadata"
          description: Metadata about the relationship. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the relationship. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - targetId
    ServiceGroupMemberRelationshipFlattened:
      type: object
      description: Flattened view of ServiceGroupMemberRelationship ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        sourceId:
          type: string
          description: The relationship source resource id. Required.
        targetId:
          type: string
          description: The relationship target resource id. Required.
        targetTenant:
          type: string
          description: The relationship target tenant id.
        originInformation:
          $ref: "#/components/schemas/RelationshipOriginInformation"
          description: Information about the origin of the relationship. Required.
        metadata:
          $ref: "#/components/schemas/RelationshipMetadata"
          description: Metadata about the relationship. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the relationship. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    ServiceGroupMemberRelationship:
      type: object
      description: Defines a ServiceGroupMember relationship resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ServiceGroupMemberRelationshipProperties"
          description: The resource-specific properties for this resource.
  x-stackQL-resources:
    dependency_of_relationships:
      id: azure.relationships.dependency_of_relationships
      name: dependency_of_relationships
      title: dependency_of_relationships
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.Relationships~1dependencyOf~1{name}?api-version=2023-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.Relationships~1dependencyOf~1{name}?api-version=2023-09-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.Relationships~1dependencyOf~1{name}?api-version=2023-09-01-preview/delete"
          response:
            openAPIDocKey: "204"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/dependency_of_relationships/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/dependency_of_relationships/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/dependency_of_relationships/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/dependency_of_relationships/methods/delete"
    operations:
      id: azure.relationships.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Relationships~1operations?api-version=2023-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    service_group_member_relationships:
      id: azure.relationships.service_group_member_relationships
      name: service_group_member_relationships
      title: service_group_member_relationships
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.Relationships~1serviceGroupMember~1{name}?api-version=2023-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.Relationships~1serviceGroupMember~1{name}?api-version=2023-09-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.Relationships~1serviceGroupMember~1{name}?api-version=2023-09-01-preview/delete"
          response:
            openAPIDocKey: "204"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/service_group_member_relationships/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/service_group_member_relationships/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/service_group_member_relationships/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/service_group_member_relationships/methods/delete"
paths:
  /{resource_uri}/providers/Microsoft.Relationships/dependencyOf/{name}?api-version=2023-09-01-preview:
    get:
      operationId: DependencyOfRelationships_get
      description: Get a DependencyOfRelationship.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of dependencyOf relationship. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DependencyOfRelationshipFlattened"
    put:
      operationId: DependencyOfRelationships_begin_create_or_update
      description: Create a DependencyOfRelationship.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of dependencyOf relationship. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/DependencyOfRelationshipProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DependencyOfRelationship"
    delete:
      operationId: DependencyOfRelationships_begin_delete
      description: Delete a DependencyOfRelationship.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of dependencyOf relationship. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Relationships/operations?api-version=2023-09-01-preview:
    get:
      operationId: relationships_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /{resource_uri}/providers/Microsoft.Relationships/serviceGroupMember/{name}?api-version=2023-09-01-preview:
    get:
      operationId: ServiceGroupMemberRelationships_get
      description: Get a ServiceGroupMemberRelationship.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of ServiceGroupMember relationship. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceGroupMemberRelationshipFlattened"
    put:
      operationId: ServiceGroupMemberRelationships_begin_create_or_update
      description: Create a ServiceGroupMemberRelationship.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of ServiceGroupMember relationship. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ServiceGroupMemberRelationshipProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceGroupMemberRelationship"
    delete:
      operationId: ServiceGroupMemberRelationships_begin_delete
      description: Delete a ServiceGroupMemberRelationship.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of ServiceGroupMember relationship. Required.
      responses:
        "204":
          description: Success (no content / accepted)
