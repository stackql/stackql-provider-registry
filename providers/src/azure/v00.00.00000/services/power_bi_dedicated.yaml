openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Power BI Embedded - Dedicated Capacities
  description: Power BI dedicated capacities and auto-scale v-cores (Microsoft.PowerBIDedicated).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-01-01-stackql-generated
  x-serviceAlias: power_bi_dedicated
  x-sdkPackage: azure-mgmt-powerbidedicated
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: An identifier for the identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: An identifier for the identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    AutoScaleVCoreSku:
      type: object
      description: Represents the SKU name and Azure pricing tier for auto scale v-core resource. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: Name of the SKU level. Required.
        tier:
          type: string
          description: The name of the Azure pricing tier to which the SKU applies. "AutoScale"
        capacity:
          type: integer
          description: The capacity of an auto scale v-core resource.
      required:
        - name
    AutoScaleVCoreFlattened:
      type: object
      description: Flattened view of AutoScaleVCore ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: An identifier that represents the PowerBI Dedicated resource.
        name:
          type: string
          description: The name of the PowerBI Dedicated resource.
        type:
          type: string
          description: The type of the PowerBI Dedicated resource.
        location:
          type: string
          description: Location of the PowerBI Dedicated resource. Required.
        tags:
          type: object
          properties: {}
          description: Key-value pairs of additional resource provisioning properties.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        sku:
          $ref: "#/components/schemas/AutoScaleVCoreSku"
          description: The SKU of the auto scale v-core resource. Required.
        capacityLimit:
          type: integer
          description: The maximum capacity of an auto scale v-core resource.
        capacityObjectId:
          type: string
          description: The object ID of the capacity resource associated with the auto scale v-core resource.
        provisioningState:
          type: string
          description: The current deployment state of an auto scale v-core resource. The provisioningState is to indicate states for resource provisioning. "Succeeded"
    PagedAutoScaleVCoreFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AutoScaleVCoreFlattened"
        nextLink:
          type: string
    AutoScaleVCore:
      type: object
      description: Represents an instance of an auto scale v-core resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: An identifier that represents the PowerBI Dedicated resource.
        name:
          type: string
          description: The name of the PowerBI Dedicated resource.
        type:
          type: string
          description: The type of the PowerBI Dedicated resource.
        location:
          type: string
          description: Location of the PowerBI Dedicated resource. Required.
        tags:
          type: object
          properties: {}
          description: Key-value pairs of additional resource provisioning properties.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        sku:
          $ref: "#/components/schemas/AutoScaleVCoreSku"
          description: The SKU of the auto scale v-core resource. Required.
        properties:
          type: object
          properties:
            capacityLimit:
              type: integer
              description: The maximum capacity of an auto scale v-core resource.
            capacityObjectId:
              type: string
              description: The object ID of the capacity resource associated with the auto scale v-core resource.
            provisioningState:
              type: string
              description: The current deployment state of an auto scale v-core resource. The provisioningState is to indicate states for resource provisioning. "Succeeded"
      required:
        - location
        - sku
    CapacitySku:
      type: object
      description: Represents the SKU name and Azure pricing tier for PowerBI Dedicated capacity resource. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: Name of the SKU level. Required.
        tier:
          type: string
          description: 'The name of the Azure pricing tier to which the SKU applies. Known values are: "PBIE_Azure", "Premium", and "AutoPremiumHost".'
        capacity:
          type: integer
          description: The capacity of the SKU.
      required:
        - name
    DedicatedCapacityAdministrators:
      type: object
      description: An array of administrator user identities.
      properties:
        members:
          type: array
          items:
            type: string
          description: An array of administrator user identities.
    DedicatedCapacityFlattened:
      type: object
      description: Flattened view of DedicatedCapacity ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: An identifier that represents the PowerBI Dedicated resource.
        name:
          type: string
          description: The name of the PowerBI Dedicated resource.
        type:
          type: string
          description: The type of the PowerBI Dedicated resource.
        location:
          type: string
          description: Location of the PowerBI Dedicated resource. Required.
        tags:
          type: object
          properties: {}
          description: Key-value pairs of additional resource provisioning properties.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        sku:
          $ref: "#/components/schemas/CapacitySku"
          description: The SKU of the PowerBI Dedicated capacity resource. Required.
        administration:
          $ref: "#/components/schemas/DedicatedCapacityAdministrators"
          description: A collection of Dedicated capacity administrators.
        mode:
          type: string
          description: "Specifies the generation of the Power BI Embedded capacity. If no value is specified, the default value 'Gen2' is used. `Learn More `_. Known values are: \"Gen1\" and \"Gen2\"."
        tenantId:
          type: string
          description: Tenant ID for the capacity. Used for creating Pro Plus capacity.
        friendlyName:
          type: string
          description: Capacity name.
        state:
          type: string
          description: 'The current state of PowerBI Dedicated resource. The state is to indicate more states outside of resource provisioning. Known values are: "Deleting", "Succeeded", "Failed", "Paused", "Suspended", "Provisioning", "Updating", "Suspending", "Pausing", "Resuming", "Preparing", and "Scaling".'
        provisioningState:
          type: string
          description: 'The current deployment state of PowerBI Dedicated resource. The provisioningState is to indicate states for resource provisioning. Known values are: "Deleting", "Succeeded", "Failed", "Paused", "Suspended", "Provisioning", "Updating", "Suspending", "Pausing", "Resuming", "Preparing", and "Scaling".'
    PagedDedicatedCapacityFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DedicatedCapacityFlattened"
        nextLink:
          type: string
    SkuEnumerationForNewResourceResult:
      type: object
      description: An object that represents enumerating SKUs for new resources.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CapacitySku"
          description: The collection of available SKUs for new resources.
    SkuDetailsForExistingResource:
      type: object
      description: An object that represents SKU details for existing resources.
      properties:
        resourceType:
          type: string
          description: The resource type.
        sku:
          $ref: "#/components/schemas/CapacitySku"
          description: The SKU in SKU details for existing resources.
    SkuEnumerationForExistingResourceResult:
      type: object
      description: An object that represents enumerating SKUs for existing resources.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SkuDetailsForExistingResource"
          description: The collection of available SKUs for existing resources.
    DedicatedCapacity:
      type: object
      description: Represents an instance of a Dedicated Capacity resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: An identifier that represents the PowerBI Dedicated resource.
        name:
          type: string
          description: The name of the PowerBI Dedicated resource.
        type:
          type: string
          description: The type of the PowerBI Dedicated resource.
        location:
          type: string
          description: Location of the PowerBI Dedicated resource. Required.
        tags:
          type: object
          properties: {}
          description: Key-value pairs of additional resource provisioning properties.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        sku:
          $ref: "#/components/schemas/CapacitySku"
          description: The SKU of the PowerBI Dedicated capacity resource. Required.
        properties:
          type: object
          properties:
            administration:
              $ref: "#/components/schemas/DedicatedCapacityAdministrators"
              description: A collection of Dedicated capacity administrators.
            mode:
              type: string
              description: "Specifies the generation of the Power BI Embedded capacity. If no value is specified, the default value 'Gen2' is used. `Learn More `_. Known values are: \"Gen1\" and \"Gen2\"."
            tenantId:
              type: string
              description: Tenant ID for the capacity. Used for creating Pro Plus capacity.
            friendlyName:
              type: string
              description: Capacity name.
            state:
              type: string
              description: 'The current state of PowerBI Dedicated resource. The state is to indicate more states outside of resource provisioning. Known values are: "Deleting", "Succeeded", "Failed", "Paused", "Suspended", "Provisioning", "Updating", "Suspending", "Pausing", "Resuming", "Preparing", and "Scaling".'
            provisioningState:
              type: string
              description: 'The current deployment state of PowerBI Dedicated resource. The provisioningState is to indicate states for resource provisioning. Known values are: "Deleting", "Succeeded", "Failed", "Paused", "Suspended", "Provisioning", "Updating", "Suspending", "Pausing", "Resuming", "Preparing", and "Scaling".'
      required:
        - location
        - sku
    CheckCapacityNameAvailabilityResult:
      type: object
      description: The checking result of capacity name availability.
      properties:
        nameAvailable:
          type: boolean
          description: Indicator of availability of the capacity name.
        reason:
          type: string
          description: The reason of unavailability.
        message:
          type: string
          description: The detailed message of the request unavailability.
    OperationDisplay:
      type: object
      description: The object that represents the operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: "Service provider: Microsoft.PowerBIDedicated."
        resource:
          type: string
          description: "Resource on which the operation is performed: capacity, etc."
        operation:
          type: string
          description: "Operation type: create, update, delete, etc."
        description:
          type: string
          description: Localized description of the operation.
    OperationProperties:
      type: object
      description: Additional properties to expose performance metrics to shoebox.
      properties:
        serviceSpecification:
          $ref: "#/components/schemas/ServiceSpecification"
          description: Service specification for exposing performance metrics to shoebox.
    ServiceSpecification:
      type: object
      description: Service specification for exposing performance metrics to shoebox.
      properties:
        metricSpecifications:
          type: array
          items:
            $ref: "#/components/schemas/MetricSpecification"
          description: Metric specifications for exposing performance metrics to shoebox.
        logSpecifications:
          type: array
          items:
            $ref: "#/components/schemas/LogSpecification"
          description: Log specifications for exposing diagnostic logs to shoebox.
    MetricSpecification:
      type: object
      description: Metric specification for exposing performance metrics to shoebox. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Metric name.
        displayName:
          type: string
          description: Localizable metric name.
        displayDescription:
          type: string
          description: Localizable description of metric.
        unit:
          type: string
          description: Unit for the metric.
        aggregationType:
          type: string
          description: Aggregation type for the metric.
        metricFilterPattern:
          type: string
          description: Pattern used to filter the metric.
        dimensions:
          type: array
          items:
            $ref: "#/components/schemas/MetricSpecificationDimensionsItem"
          description: For describing multi dimensional metrics.
    MetricSpecificationDimensionsItem:
      type: object
      description: MetricSpecificationDimensionsItem. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Dimension of the metric.
        displayName:
          type: string
          description: Localizable dimension of the metric.
    LogSpecification:
      type: object
      description: Log specification for exposing diagnostic logs to shoebox. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Name of the log.
        displayName:
          type: string
          description: Localizable name of the log.
        blobDuration:
          type: string
          description: Blob duration for the log.
    OperationFlattened:
      type: object
      description: Flattened view of Operation ('properties' hoisted to the top level).
      properties:
        name:
          type: string
          description: "Operation name: {provider}/{resource}/{operation}."
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that represents the operation.
        origin:
          type: string
          description: Origin of the operation.
        serviceSpecification:
          $ref: "#/components/schemas/ServiceSpecification"
          description: Service specification for exposing performance metrics to shoebox.
    PagedOperationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OperationFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    auto_scale_vcores:
      id: azure.power_bi_dedicated.auto_scale_vcores
      name: auto_scale_vcores
      title: auto_scale_vcores
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1autoScaleVCores~1{vcore_name}?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1autoScaleVCores~1{vcore_name}?api-version=2021-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1autoScaleVCores~1{vcore_name}?api-version=2021-01-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1autoScaleVCores~1{vcore_name}?api-version=2021-01-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1autoScaleVCores?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.PowerBIDedicated~1autoScaleVCores?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/auto_scale_vcores/methods/get"
          - $ref: "#/components/x-stackQL-resources/auto_scale_vcores/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/auto_scale_vcores/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/auto_scale_vcores/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/auto_scale_vcores/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/auto_scale_vcores/methods/delete"
    capacities:
      id: azure.power_bi_dedicated.capacities
      name: capacities
      title: capacities
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.PowerBIDedicated~1capacities?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_skus:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.PowerBIDedicated~1skus?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_skus_for_capacity:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities~1{dedicated_capacity_name}~1skus?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_details:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities~1{dedicated_capacity_name}?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities~1{dedicated_capacity_name}?api-version=2021-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities~1{dedicated_capacity_name}?api-version=2021-01-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities~1{dedicated_capacity_name}?api-version=2021-01-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        suspend:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities~1{dedicated_capacity_name}~1suspend?api-version=2021-01-01/post"
          response:
            openAPIDocKey: "204"
        resume:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.PowerBIDedicated~1capacities~1{dedicated_capacity_name}~1resume?api-version=2021-01-01/post"
          response:
            openAPIDocKey: "204"
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.PowerBIDedicated~1locations~1{location}~1checkNameAvailability?api-version=2021-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/capacities/methods/list_skus_for_capacity"
          - $ref: "#/components/x-stackQL-resources/capacities/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/capacities/methods/check_name_availability"
          - $ref: "#/components/x-stackQL-resources/capacities/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/capacities/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/capacities/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/capacities/methods/delete"
    operations:
      id: azure.power_bi_dedicated.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.PowerBIDedicated~1operations?api-version=2021-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerBIDedicated/autoScaleVCores/{vcore_name}?api-version=2021-01-01:
    get:
      operationId: AutoScaleVCores_get
      description: Gets details about the specified auto scale v-core.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: vcore_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the auto scale v-core. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoScaleVCoreFlattened"
    put:
      operationId: AutoScaleVCores_create
      description: Provisions the specified auto scale v-core based on the configuration specified in the request.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: vcore_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the auto scale v-core. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Location of the PowerBI Dedicated resource. Required.
                tags:
                  type: object
                  properties: {}
                  description: Key-value pairs of additional resource provisioning properties.
                systemData:
                  $ref: "#/components/schemas/SystemData"
                  description: Metadata pertaining to creation and last modification of the resource.
                sku:
                  $ref: "#/components/schemas/AutoScaleVCoreSku"
                  description: The SKU of the auto scale v-core resource. Required.
                properties:
                  type: object
                  properties:
                    capacityLimit:
                      type: integer
                      description: The maximum capacity of an auto scale v-core resource.
                    capacityObjectId:
                      type: string
                      description: The object ID of the capacity resource associated with the auto scale v-core resource.
              required:
                - location
                - sku
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoScaleVCore"
    delete:
      operationId: AutoScaleVCores_delete
      description: Deletes the specified auto scale v-core.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: vcore_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the auto scale v-core. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: AutoScaleVCores_update
      description: Updates the current state of the specified auto scale v-core.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: vcore_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the auto scale v-core. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                sku:
                  $ref: "#/components/schemas/AutoScaleVCoreSku"
                  description: The SKU of the auto scale v-core resource.
                tags:
                  type: object
                  properties: {}
                  description: Key-value pairs of additional provisioning properties.
                properties:
                  type: object
                  properties:
                    capacityLimit:
                      type: integer
                      description: The maximum capacity of an auto scale v-core resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoScaleVCore"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerBIDedicated/autoScaleVCores?api-version=2021-01-01:
    get:
      operationId: AutoScaleVCores_list_by_resource_group
      description: Gets all the auto scale v-cores for the given resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAutoScaleVCoreFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.PowerBIDedicated/autoScaleVCores?api-version=2021-01-01:
    get:
      operationId: AutoScaleVCores_list_by_subscription
      description: Lists all the auto scale v-cores for the given subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAutoScaleVCoreFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.PowerBIDedicated/capacities?api-version=2021-01-01:
    get:
      operationId: Capacities_list
      description: Lists all the Dedicated capacities for the given subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDedicatedCapacityFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerBIDedicated/capacities?api-version=2021-01-01:
    get:
      operationId: Capacities_list_by_resource_group
      description: Gets all the Dedicated capacities for the given resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDedicatedCapacityFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.PowerBIDedicated/skus?api-version=2021-01-01:
    get:
      operationId: Capacities_list_skus
      description: Lists eligible SKUs for PowerBI Dedicated resource provider.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkuEnumerationForNewResourceResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerBIDedicated/capacities/{dedicated_capacity_name}/skus?api-version=2021-01-01:
    get:
      operationId: Capacities_list_skus_for_capacity
      description: Lists eligible SKUs for a PowerBI Dedicated resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: dedicated_capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Dedicated capacity. It must be at least 3 characters in length, and no more than 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkuEnumerationForExistingResourceResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerBIDedicated/capacities/{dedicated_capacity_name}?api-version=2021-01-01:
    get:
      operationId: Capacities_get_details
      description: Gets details about the specified dedicated capacity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: dedicated_capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the dedicated capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCapacityFlattened"
    put:
      operationId: Capacities_begin_create
      description: Provisions the specified Dedicated capacity based on the configuration specified in the request.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: dedicated_capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Dedicated capacity. It must be a minimum of 3 characters, and a maximum of 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Location of the PowerBI Dedicated resource. Required.
                tags:
                  type: object
                  properties: {}
                  description: Key-value pairs of additional resource provisioning properties.
                systemData:
                  $ref: "#/components/schemas/SystemData"
                  description: Metadata pertaining to creation and last modification of the resource.
                sku:
                  $ref: "#/components/schemas/CapacitySku"
                  description: The SKU of the PowerBI Dedicated capacity resource. Required.
                properties:
                  type: object
                  properties:
                    administration:
                      $ref: "#/components/schemas/DedicatedCapacityAdministrators"
                      description: A collection of Dedicated capacity administrators.
                    mode:
                      type: string
                      description: "Specifies the generation of the Power BI Embedded capacity. If no value is specified, the default value 'Gen2' is used. `Learn More `_. Known values are: \"Gen1\" and \"Gen2\"."
              required:
                - location
                - sku
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCapacity"
    delete:
      operationId: Capacities_begin_delete
      description: Deletes the specified Dedicated capacity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: dedicated_capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Dedicated capacity. It must be at least 3 characters in length, and no more than 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Capacities_begin_update
      description: Updates the current state of the specified Dedicated capacity.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: dedicated_capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Dedicated capacity. It must be at least 3 characters in length, and no more than 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                sku:
                  $ref: "#/components/schemas/CapacitySku"
                  description: The SKU of the Dedicated capacity resource.
                tags:
                  type: object
                  properties: {}
                  description: Key-value pairs of additional provisioning properties.
                properties:
                  type: object
                  properties:
                    administration:
                      $ref: "#/components/schemas/DedicatedCapacityAdministrators"
                      description: A collection of Dedicated capacity administrators.
                    mode:
                      type: string
                      description: "Specifies the generation of the Power BI Embedded capacity. If no value is specified, the default value 'Gen2' is used. `Learn More `_. Known values are: \"Gen1\" and \"Gen2\"."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DedicatedCapacity"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerBIDedicated/capacities/{dedicated_capacity_name}/suspend?api-version=2021-01-01:
    post:
      operationId: Capacities_begin_suspend
      description: Suspends operation of the specified dedicated capacity instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: dedicated_capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Dedicated capacity. It must be at least 3 characters in length, and no more than 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.PowerBIDedicated/capacities/{dedicated_capacity_name}/resume?api-version=2021-01-01:
    post:
      operationId: Capacities_begin_resume
      description: Resumes operation of the specified Dedicated capacity instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure Resource group of which a given PowerBIDedicated capacity is part. This name must be at least 1 character in length, and no more than 90. Required.
        - name: dedicated_capacity_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Dedicated capacity. It must be at least 3 characters in length, and no more than 63. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.PowerBIDedicated/locations/{location}/checkNameAvailability?api-version=2021-01-01:
    post:
      operationId: Capacities_check_name_availability
      description: Check the name availability in the target location.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The region name which the operation will lookup into. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name for checking availability.
                type:
                  type: string
                  description: The resource type of PowerBI dedicated.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckCapacityNameAvailabilityResult"
  /providers/Microsoft.PowerBIDedicated/operations?api-version=2021-01-01:
    get:
      operationId: powerbidedicated_list
      description: Lists all of the available PowerBIDedicated REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperationFlattened"
