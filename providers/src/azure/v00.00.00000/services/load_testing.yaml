openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Load Testing
  description: Management plane for load testing resources and quotas.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2022-12-01-stackql-generated
  x-serviceAlias: load_testing
  x-sdkPackage: azure-mgmt-loadtesting
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities). Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          type: string
          description: 'Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: "The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}. The dictionary values can be empty objects ({}) in requests."
      required:
        - type
    EncryptionProperties:
      type: object
      description: Key and identity details for Customer Managed Key encryption of load test resource.
      properties:
        identity:
          $ref: "#/components/schemas/EncryptionPropertiesIdentity"
          description: All identity configuration for Customer-managed key settings defining which identity should be used to auth to Key Vault.
        keyUrl:
          type: string
          description: "key encryption key Url, versioned. Ex: https://contosovault.vault.azure.net/keys/contosokek/562a4bb76b524a1493a6afe8e536ee78 or https://contosovault.vault.azure.net/keys/contosokek."
    EncryptionPropertiesIdentity:
      type: object
      description: All identity configuration for Customer-managed key settings defining which identity should be used to auth to Key Vault.
      properties:
        type:
          type: string
          description: 'Managed identity type to use for accessing encryption key Url. Known values are: "SystemAssigned" and "UserAssigned".'
        resourceId:
          type: string
          description: "user assigned identity to use for accessing key encryption key Url. Ex: /subscriptions/fa5fc227-a624-475e-b696-cdd604c735bc/resourceGroups//providers/Microsoft.ManagedIdentity/userAssignedIdentities/myId."
    LoadTestResourceFlattened:
      type: object
      description: Flattened view of LoadTestResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The type of identity used for the resource.
        description:
          type: string
          description: Description of the resource.
        provisioningState:
          type: string
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", "Canceled", and "Deleted".'
        dataPlaneURI:
          type: string
          description: Resource data plane URI.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: CMK Encryption property.
    PagedLoadTestResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/LoadTestResourceFlattened"
        nextLink:
          type: string
    LoadTestResource:
      type: object
      description: LoadTest details. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The type of identity used for the resource.
        properties:
          type: object
          properties:
            description:
              type: string
              description: Description of the resource.
            provisioningState:
              type: string
              description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", "Canceled", and "Deleted".'
            dataPlaneURI:
              type: string
              description: Resource data plane URI.
            encryption:
              $ref: "#/components/schemas/EncryptionProperties"
              description: CMK Encryption property.
      required:
        - location
    EndpointDependency:
      type: object
      description: A domain name and connection details used to access a dependency. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        domainName:
          type: string
          description: The domain name of the dependency. Domain names may be fully qualified or may contain a * wildcard.
        description:
          type: string
          description: Human-readable supplemental information about the dependency and when it is applicable.
        endpointDetails:
          type: array
          items:
            $ref: "#/components/schemas/EndpointDetail"
          description: The list of connection details for this endpoint.
    EndpointDetail:
      type: object
      description: Details about the connection between the Batch service and the endpoint. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        port:
          type: integer
          description: The port an endpoint is connected to.
    OutboundEnvironmentEndpoint:
      type: object
      description: A collection of related endpoints from the same service for which the Batch service requires outbound access. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        category:
          type: string
          description: The type of service that Azure Load Testing connects to.
        endpoints:
          type: array
          items:
            $ref: "#/components/schemas/EndpointDependency"
          description: The endpoints for this service to which the Batch service makes outbound calls.
    PagedOutboundEnvironmentEndpoint:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OutboundEnvironmentEndpoint"
        nextLink:
          type: string
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    QuotaResourceFlattened:
      type: object
      description: Flattened view of QuotaResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        limit:
          type: integer
          description: Current quota limit of the quota bucket.
        usage:
          type: integer
          description: Current quota usage of the quota bucket.
        provisioningState:
          type: string
          description: 'Resource provisioning state. Known values are: "Succeeded", "Failed", "Canceled", and "Deleted".'
    PagedQuotaResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/QuotaResourceFlattened"
        nextLink:
          type: string
    QuotaBucketRequestPropertiesDimensions:
      type: object
      description: Dimensions for new quota request.
      properties:
        subscriptionId:
          type: string
          description: Subscription Id dimension for new quota request of the quota bucket.
        location:
          type: string
          description: Location dimension for new quota request of the quota bucket.
    CheckQuotaAvailabilityResponseFlattened:
      type: object
      description: Flattened view of CheckQuotaAvailabilityResponse ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        isAvailable:
          type: boolean
          description: True/False indicating whether the quota request be granted based on availability.
        availabilityStatus:
          type: string
          description: Message indicating additional details to add to quota support request.
  x-stackQL-resources:
    load_tests:
      id: azure.load_testing.load_tests
      name: load_tests
      title: load_tests
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.LoadTestService~1loadTests~1{load_test_name}?api-version=2022-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.LoadTestService~1loadTests~1{load_test_name}?api-version=2022-12-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.LoadTestService~1loadTests~1{load_test_name}?api-version=2022-12-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.LoadTestService~1loadTests~1{load_test_name}?api-version=2022-12-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.LoadTestService~1loadTests?api-version=2022-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.LoadTestService~1loadTests?api-version=2022-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_outbound_network_dependencies_endpoints:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.LoadTestService~1loadTests~1{load_test_name}~1outboundNetworkDependenciesEndpoints?api-version=2022-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/load_tests/methods/get"
          - $ref: "#/components/x-stackQL-resources/load_tests/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/load_tests/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/load_tests/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/load_tests/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/load_tests/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/load_tests/methods/delete"
    operations:
      id: azure.load_testing.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.LoadTestService~1operations?api-version=2022-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    quotas:
      id: azure.load_testing.quotas
      name: quotas
      title: quotas
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.LoadTestService~1locations~1{location}~1quotas~1{quota_bucket_name}?api-version=2022-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.LoadTestService~1locations~1{location}~1quotas?api-version=2022-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        check_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.LoadTestService~1locations~1{location}~1quotas~1{quota_bucket_name}~1checkAvailability?api-version=2022-12-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/quotas/methods/get"
          - $ref: "#/components/x-stackQL-resources/quotas/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.LoadTestService/loadTests/{load_test_name}?api-version=2022-12-01:
    get:
      operationId: LoadTests_get
      description: Get a LoadTest resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: load_test_name
          in: path
          required: true
          schema:
            type: string
          description: Load Test name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoadTestResourceFlattened"
    put:
      operationId: LoadTests_begin_create_or_update
      description: Create or update LoadTest resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: load_test_name
          in: path
          required: true
          schema:
            type: string
          description: Load Test name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The type of identity used for the resource.
                properties:
                  type: object
                  properties:
                    description:
                      type: string
                      description: Description of the resource.
                    encryption:
                      $ref: "#/components/schemas/EncryptionProperties"
                      description: CMK Encryption property.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoadTestResource"
    patch:
      operationId: LoadTests_begin_update
      description: Update a loadtest resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: load_test_name
          in: path
          required: true
          schema:
            type: string
          description: Load Test name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The type of identity used for the resource.
                properties:
                  type: object
                  properties:
                    description:
                      type: string
                      description: Description of the resource.
                    encryption:
                      $ref: "#/components/schemas/EncryptionProperties"
                      description: CMK Encryption property.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoadTestResource"
    delete:
      operationId: LoadTests_begin_delete
      description: Delete a LoadTest resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: load_test_name
          in: path
          required: true
          schema:
            type: string
          description: Load Test name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.LoadTestService/loadTests?api-version=2022-12-01:
    get:
      operationId: LoadTests_list_by_subscription
      description: Lists loadtests resources in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLoadTestResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.LoadTestService/loadTests?api-version=2022-12-01:
    get:
      operationId: LoadTests_list_by_resource_group
      description: Lists loadtest resources in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLoadTestResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.LoadTestService/loadTests/{load_test_name}/outboundNetworkDependenciesEndpoints?api-version=2022-12-01:
    get:
      operationId: LoadTests_list_outbound_network_dependencies_endpoints
      description: Lists the endpoints that agents may call as part of load testing.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: load_test_name
          in: path
          required: true
          schema:
            type: string
          description: Load Test name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOutboundEnvironmentEndpoint"
  /providers/Microsoft.LoadTestService/operations?api-version=2022-12-01:
    get:
      operationId: loadtesting_list
      description: Lists all the available API operations for Load Test Resource.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/providers/Microsoft.LoadTestService/locations/{location}/quotas/{quota_bucket_name}?api-version=2022-12-01:
    get:
      operationId: Quotas_get
      description: Get the available quota for a quota bucket per region per subscription.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: quota_bucket_name
          in: path
          required: true
          schema:
            type: string
          description: Quota Bucket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.LoadTestService/locations/{location}/quotas?api-version=2022-12-01:
    get:
      operationId: Quotas_list
      description: Lists all the available quota per region per subscription.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedQuotaResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.LoadTestService/locations/{location}/quotas/{quota_bucket_name}/checkAvailability?api-version=2022-12-01:
    post:
      operationId: Quotas_check_availability
      description: Check Quota Availability on quota bucket per region per subscription.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: quota_bucket_name
          in: path
          required: true
          schema:
            type: string
          description: Quota Bucket name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    currentUsage:
                      type: integer
                      description: Current quota usage of the quota bucket.
                    currentQuota:
                      type: integer
                      description: Current quota limit of the quota bucket.
                    newQuota:
                      type: integer
                      description: New quota limit of the quota bucket.
                    dimensions:
                      $ref: "#/components/schemas/QuotaBucketRequestPropertiesDimensions"
                      description: Dimensions for new quota request.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckQuotaAvailabilityResponseFlattened"
