openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Kubernetes Runtime
  description: "Arc-connected Kubernetes runtime services: load balancers, BGP peers and storage classes (Microsoft.KubernetesRuntime)."
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-03-01-stackql-generated
  x-serviceAlias: container_orchestrator_runtime
  x-sdkPackage: azure-mgmt-containerorchestratorruntime
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    BgpPeerProperties:
      type: object
      description: Details of the BgpPeer. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        myAsn:
          type: integer
          description: My ASN. Required.
        peerAsn:
          type: integer
          description: Peer ASN. Required.
        peerAddress:
          type: string
          description: Peer Address. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - myAsn
        - peerAsn
        - peerAddress
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
        - Accepted
    BgpPeerFlattened:
      type: object
      description: Flattened view of BgpPeer ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        myAsn:
          type: integer
          description: My ASN. Required.
        peerAsn:
          type: integer
          description: Peer ASN. Required.
        peerAddress:
          type: string
          description: Peer Address. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedBgpPeerFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/BgpPeerFlattened"
        nextLink:
          type: string
    BgpPeer:
      type: object
      description: A BgpPeer resource for an Arc connected cluster (Microsoft.Kubernetes/connectedClusters). Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/BgpPeerProperties"
          description: The resource-specific properties for this resource.
    LoadBalancerProperties:
      type: object
      description: Details of the LoadBalancer. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        addresses:
          type: array
          items:
            type: string
          description: IP Range. Required.
        serviceSelector:
          type: object
          properties: {}
          description: 'A dynamic label mapping to select related services. For instance, if you want to create a load balancer only for services with label "a=b", then please specify {"a": "b"} in the field.'
        advertiseMode:
          $ref: "#/components/schemas/AdvertiseMode"
          description: 'Advertise Mode. Required. Known values are: "ARP", "BGP", and "Both".'
        bgpPeers:
          type: array
          items:
            type: string
          description: The list of BGP peers it should advertise to. Null or empty means to advertise to all peers.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - addresses
        - advertiseMode
    AdvertiseMode:
      type: string
      enum:
        - ARP
        - BGP
        - Both
    LoadBalancerFlattened:
      type: object
      description: Flattened view of LoadBalancer ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        addresses:
          type: array
          items:
            type: string
          description: IP Range. Required.
        serviceSelector:
          type: object
          properties: {}
          description: 'A dynamic label mapping to select related services. For instance, if you want to create a load balancer only for services with label "a=b", then please specify {"a": "b"} in the field.'
        advertiseMode:
          $ref: "#/components/schemas/AdvertiseMode"
          description: 'Advertise Mode. Required. Known values are: "ARP", "BGP", and "Both".'
        bgpPeers:
          type: array
          items:
            type: string
          description: The list of BGP peers it should advertise to. Null or empty means to advertise to all peers.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedLoadBalancerFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/LoadBalancerFlattened"
        nextLink:
          type: string
    LoadBalancer:
      type: object
      description: A LoadBalancer resource for an Arc connected cluster (Microsoft.Kubernetes/connectedClusters). Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/LoadBalancerProperties"
          description: The resource-specific properties for this resource.
    OperationDisplay:
      type: object
      description: Localized display information for and operation. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    ServiceProperties:
      type: object
      description: Properties for the service resource. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        rpObjectId:
          type: string
          description: The object id of the service principal of the RP provisioned in the tenant.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    ServiceResourceFlattened:
      type: object
      description: Flattened view of ServiceResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        rpObjectId:
          type: string
          description: The object id of the service principal of the RP provisioned in the tenant.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedServiceResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ServiceResourceFlattened"
        nextLink:
          type: string
    ServiceResource:
      type: object
      description: A Service resource for an Arc connected cluster (Microsoft.Kubernetes/connectedClusters). Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ServiceProperties"
          description: The resource-specific properties for this resource.
    StorageClassProperties:
      type: object
      description: Details of the StorageClass StorageClass. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        allowVolumeExpansion:
          $ref: "#/components/schemas/VolumeExpansion"
          description: 'Volume can be expanded or not. Known values are: "Allow" and "Disallow".'
        mountOptions:
          type: array
          items:
            type: string
          description: Additional mount options.
        provisioner:
          type: string
          description: Provisioner name.
        volumeBindingMode:
          $ref: "#/components/schemas/VolumeBindingMode"
          description: 'Binding mode of volumes: Immediate, WaitForFirstConsumer. Known values are: "Immediate" and "WaitForFirstConsumer".'
        accessModes:
          type: array
          items:
            $ref: "#/components/schemas/AccessMode"
          description: "The access mode: [ReadWriteOnce, ReadWriteMany] or [ReadWriteOnce]."
        dataResilience:
          $ref: "#/components/schemas/DataResilienceTier"
          description: 'Allow single data node failure. Known values are: "NotDataResilient" and "DataResilient".'
        failoverSpeed:
          $ref: "#/components/schemas/FailoverTier"
          description: 'Failover speed: NA, Slow, Fast. Known values are: "NotAvailable", "Slow", "Fast", and "Super".'
        limitations:
          type: array
          items:
            type: string
          description: Limitations of the storage class.
        performance:
          $ref: "#/components/schemas/PerformanceTier"
          description: 'Performance tier. Known values are: "Undefined", "Basic", "Standard", "Premium", and "Ultra".'
        priority:
          type: integer
          description: "Selection priority when multiple storage classes meet the criteria. 0: Highest, -1: Never use."
        typeProperties:
          $ref: "#/components/schemas/StorageClassTypeProperties"
          description: Properties of the StorageClass. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - typeProperties
    VolumeExpansion:
      type: string
      enum:
        - Allow
        - Disallow
    VolumeBindingMode:
      type: string
      enum:
        - Immediate
        - WaitForFirstConsumer
    AccessMode:
      type: string
      enum:
        - ReadWriteOnce
        - ReadWriteMany
    DataResilienceTier:
      type: string
      enum:
        - NotDataResilient
        - DataResilient
    FailoverTier:
      type: string
      enum:
        - NotAvailable
        - Slow
        - Fast
        - Super
    PerformanceTier:
      type: string
      enum:
        - Undefined
        - Basic
        - Standard
        - Premium
        - Ultra
    StorageClassTypeProperties:
      type: object
      description: "The properties of storage class of the StorageClass. You probably want to use the sub-classes and not this class directly. Known sub-classes are: BlobStorageClassTypeProperties, NfsStorageClassTypeProperties, NativeStorageClassTypeProperties, RwxStorageClassTypeProperties, SmbStorageClassTypeProperties"
      properties:
        type:
          type: string
          description: 'Type of the storage class. Required. Known values are: "Native", "RWX", "Blob", "NFS", and "SMB".'
      required:
        - type
    StorageClassResourceFlattened:
      type: object
      description: Flattened view of StorageClassResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        allowVolumeExpansion:
          $ref: "#/components/schemas/VolumeExpansion"
          description: 'Volume can be expanded or not. Known values are: "Allow" and "Disallow".'
        mountOptions:
          type: array
          items:
            type: string
          description: Additional mount options.
        provisioner:
          type: string
          description: Provisioner name.
        volumeBindingMode:
          $ref: "#/components/schemas/VolumeBindingMode"
          description: 'Binding mode of volumes: Immediate, WaitForFirstConsumer. Known values are: "Immediate" and "WaitForFirstConsumer".'
        accessModes:
          type: array
          items:
            $ref: "#/components/schemas/AccessMode"
          description: "The access mode: [ReadWriteOnce, ReadWriteMany] or [ReadWriteOnce]."
        dataResilience:
          $ref: "#/components/schemas/DataResilienceTier"
          description: 'Allow single data node failure. Known values are: "NotDataResilient" and "DataResilient".'
        failoverSpeed:
          $ref: "#/components/schemas/FailoverTier"
          description: 'Failover speed: NA, Slow, Fast. Known values are: "NotAvailable", "Slow", "Fast", and "Super".'
        limitations:
          type: array
          items:
            type: string
          description: Limitations of the storage class.
        performance:
          $ref: "#/components/schemas/PerformanceTier"
          description: 'Performance tier. Known values are: "Undefined", "Basic", "Standard", "Premium", and "Ultra".'
        priority:
          type: integer
          description: "Selection priority when multiple storage classes meet the criteria. 0: Highest, -1: Never use."
        typeProperties:
          $ref: "#/components/schemas/StorageClassTypeProperties"
          description: Properties of the StorageClass. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Resource provision state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedStorageClassResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StorageClassResourceFlattened"
        nextLink:
          type: string
    StorageClassResource:
      type: object
      description: A StorageClass resource for an Arc connected cluster (Microsoft.Kubernetes/connectedClusters). Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/StorageClassProperties"
          description: The resource-specific properties for this resource.
    StorageClassPropertiesUpdate:
      type: object
      description: The model for updating storageClass properties.
      properties:
        allowVolumeExpansion:
          $ref: "#/components/schemas/VolumeExpansion"
          description: 'Volume can be expanded or not. Known values are: "Allow" and "Disallow".'
        mountOptions:
          type: array
          items:
            type: string
          description: Additional mount options.
        accessModes:
          type: array
          items:
            $ref: "#/components/schemas/AccessMode"
          description: "The access mode: [ReadWriteOnce, ReadWriteMany] or [ReadWriteOnce]."
        dataResilience:
          $ref: "#/components/schemas/DataResilienceTier"
          description: 'Allow single data node failure. Known values are: "NotDataResilient" and "DataResilient".'
        failoverSpeed:
          $ref: "#/components/schemas/FailoverTier"
          description: 'Failover speed: NA, Slow, Fast. Known values are: "NotAvailable", "Slow", "Fast", and "Super".'
        limitations:
          type: array
          items:
            type: string
          description: Limitations of the storage class.
        performance:
          $ref: "#/components/schemas/PerformanceTier"
          description: 'Performance tier. Known values are: "Undefined", "Basic", "Standard", "Premium", and "Ultra".'
        priority:
          type: integer
          description: "Selection priority when multiple storage classes meet the criteria. 0: Highest, -1: Never use."
        typeProperties:
          $ref: "#/components/schemas/StorageClassTypePropertiesUpdate"
          description: New storage class type of storageClass.
    StorageClassTypePropertiesUpdate:
      type: object
      description: The model for update a storageClass.
      properties:
        backingStorageClassName:
          type: string
          description: The backing storageclass used to create new storageclass.
        azureStorageAccountName:
          type: string
          description: Azure Storage Account Name.
        azureStorageAccountKey:
          type: string
          description: Azure Storage Account Key.
        server:
          type: string
          description: NFS Server.
        share:
          type: string
          description: NFS share.
        subDir:
          type: string
          description: Sub directory under share. If the sub directory doesn't exist, driver will create it.
        mountPermissions:
          type: string
          description: Mounted folder permissions. Default is 0. If set as non-zero, driver will perform `chmod` after mount.
        onDelete:
          $ref: "#/components/schemas/NfsDirectoryActionOnVolumeDeletion"
          description: 'The action to take when a NFS volume is deleted. Default is Delete. Known values are: "Delete" and "Retain".'
        source:
          type: string
          description: SMB Source.
        username:
          type: string
          description: Server username.
        password:
          type: string
          description: Server password.
        domain:
          type: string
          description: Server domain.
    NfsDirectoryActionOnVolumeDeletion:
      type: string
      enum:
        - Delete
        - Retain
  x-stackQL-resources:
    bgp_peers:
      id: azure.container_orchestrator_runtime.bgp_peers
      name: bgp_peers
      title: bgp_peers
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1bgpPeers~1{bgp_peer_name}?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1bgpPeers~1{bgp_peer_name}?api-version=2024-03-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1bgpPeers~1{bgp_peer_name}?api-version=2024-03-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1bgpPeers?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/bgp_peers/methods/get"
          - $ref: "#/components/x-stackQL-resources/bgp_peers/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/bgp_peers/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/bgp_peers/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/bgp_peers/methods/delete"
    load_balancers:
      id: azure.container_orchestrator_runtime.load_balancers
      name: load_balancers
      title: load_balancers
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1loadBalancers~1{load_balancer_name}?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1loadBalancers~1{load_balancer_name}?api-version=2024-03-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1loadBalancers~1{load_balancer_name}?api-version=2024-03-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1loadBalancers?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/load_balancers/methods/get"
          - $ref: "#/components/x-stackQL-resources/load_balancers/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/load_balancers/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/load_balancers/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/load_balancers/methods/delete"
    operations:
      id: azure.container_orchestrator_runtime.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.KubernetesRuntime~1operations?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    services:
      id: azure.container_orchestrator_runtime.services
      name: services
      title: services
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1services~1{service_name}?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1services~1{service_name}?api-version=2024-03-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1services~1{service_name}?api-version=2024-03-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1services?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/services/methods/get"
          - $ref: "#/components/x-stackQL-resources/services/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/services/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/services/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/services/methods/delete"
    storage_class:
      id: azure.container_orchestrator_runtime.storage_class
      name: storage_class
      title: storage_class
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1storageClasses~1{storage_class_name}?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1storageClasses~1{storage_class_name}?api-version=2024-03-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1storageClasses~1{storage_class_name}?api-version=2024-03-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1storageClasses~1{storage_class_name}?api-version=2024-03-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.KubernetesRuntime~1storageClasses?api-version=2024-03-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/storage_class/methods/get"
          - $ref: "#/components/x-stackQL-resources/storage_class/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/storage_class/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/storage_class/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/storage_class/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/storage_class/methods/delete"
paths:
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/bgpPeers/{bgp_peer_name}?api-version=2024-03-01:
    get:
      operationId: BgpPeers_get
      description: Get a BgpPeer.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: bgp_peer_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the BgpPeer. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BgpPeerFlattened"
    put:
      operationId: BgpPeers_begin_create_or_update
      description: Create a BgpPeer.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: bgp_peer_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the BgpPeer. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/BgpPeerProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BgpPeer"
    delete:
      operationId: BgpPeers_delete
      description: Delete a BgpPeer.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: bgp_peer_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the BgpPeer. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/bgpPeers?api-version=2024-03-01:
    get:
      operationId: BgpPeers_list
      description: List BgpPeer resources by parent.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedBgpPeerFlattened"
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/loadBalancers/{load_balancer_name}?api-version=2024-03-01:
    get:
      operationId: LoadBalancers_get
      description: Get a LoadBalancer.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: load_balancer_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the LoadBalancer. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoadBalancerFlattened"
    put:
      operationId: LoadBalancers_begin_create_or_update
      description: Create a LoadBalancer.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: load_balancer_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the LoadBalancer. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/LoadBalancerProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoadBalancer"
    delete:
      operationId: LoadBalancers_delete
      description: Delete a LoadBalancer.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: load_balancer_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the LoadBalancer. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/loadBalancers?api-version=2024-03-01:
    get:
      operationId: LoadBalancers_list
      description: List LoadBalancer resources by parent.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLoadBalancerFlattened"
  /providers/Microsoft.KubernetesRuntime/operations?api-version=2024-03-01:
    get:
      operationId: containerorchestratorruntime_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/services/{service_name}?api-version=2024-03-01:
    get:
      operationId: Services_get
      description: Get a ServiceResource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: service_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the the service. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceResourceFlattened"
    put:
      operationId: Services_create_or_update
      description: Create a ServiceResource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: service_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the the service. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ServiceProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceResource"
    delete:
      operationId: Services_delete
      description: Delete a ServiceResource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: service_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the the service. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/services?api-version=2024-03-01:
    get:
      operationId: Services_list
      description: List ServiceResource resources by parent.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedServiceResourceFlattened"
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/storageClasses/{storage_class_name}?api-version=2024-03-01:
    get:
      operationId: StorageClass_get
      description: Get a StorageClassResource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: storage_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the the storage class. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageClassResourceFlattened"
    put:
      operationId: StorageClass_begin_create_or_update
      description: Create a StorageClassResource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: storage_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the the storage class. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/StorageClassProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageClassResource"
    patch:
      operationId: StorageClass_begin_update
      description: Update a StorageClassResource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: storage_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the the storage class. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/StorageClassPropertiesUpdate"
                  description: The properties of StorageClass.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageClassResource"
    delete:
      operationId: StorageClass_begin_delete
      description: Delete a StorageClassResource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: storage_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the the storage class. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.KubernetesRuntime/storageClasses?api-version=2024-03-01:
    get:
      operationId: StorageClass_list
      description: List StorageClassResource resources by parent.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStorageClassResourceFlattened"
