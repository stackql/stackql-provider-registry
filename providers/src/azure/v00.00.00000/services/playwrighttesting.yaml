openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Playwright Testing Mgmt Client
  description: Microsoft.AzurePlaywrightService Resource Provider Management API.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-12-01-stackql-generated
  x-serviceAlias: playwrighttesting
  x-sdkPackage: azure-mgmt-playwrighttesting
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    AccountQuotaProperties:
      type: object
      description: The Playwright service account quota resource properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        freeTrial:
          $ref: "#/components/schemas/AccountFreeTrialProperties"
          description: The Playwright service account quota resource free-trial properties.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Accepted".'
    AccountFreeTrialProperties:
      type: object
      description: The Playwright service account quota resource free-trial properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        createdAt:
          type: string
          format: date-time
          description: The free-trial createdAt utcDateTime. Required.
        expiryAt:
          type: string
          format: date-time
          description: The free-trial expiryAt utcDateTime. Required.
        allocatedValue:
          type: integer
          description: The free-trial allocated limit value eg. allocated free minutes. Required.
        usedValue:
          type: integer
          description: The free-trial used value eg. used free minutes. Required.
        percentageUsed:
          type: number
          description: The free-trial percentage used. Required.
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Creating
        - Deleting
        - Accepted
    AccountQuotaFlattened:
      type: object
      description: Flattened view of AccountQuota ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        freeTrial:
          $ref: "#/components/schemas/AccountFreeTrialProperties"
          description: The Playwright service account quota resource free-trial properties.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Accepted".'
    PagedAccountQuotaFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AccountQuotaFlattened"
        nextLink:
          type: string
    AccountProperties:
      type: object
      description: Account resource properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        dashboardUri:
          type: string
          description: The Playwright testing dashboard URI for the account resource.
        regionalAffinity:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'This property sets the connection region for Playwright client workers to cloud-hosted browsers. If enabled, workers connect to browsers in the closest Azure region, ensuring lower latency. If disabled, workers connect to browsers in the Azure region in which the workspace was initially created. Known values are: "Enabled" and "Disabled".'
        scalableExecution:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, Playwright client workers can connect to cloud-hosted browsers. This can increase the number of parallel workers for a test run, significantly minimizing test completion durations. Known values are: "Enabled" and "Disabled".'
        reporting:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, this feature allows the workspace to upload and display test results, including artifacts like traces and screenshots, in the Playwright portal. This enables faster and more efficient troubleshooting. Known values are: "Enabled" and "Disabled".'
        localAuth:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, this feature allows the workspace to use local auth(through access key) for authentication of test runs. Known values are: "Enabled" and "Disabled".'
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Accepted".'
    EnablementStatus:
      type: string
      enum:
        - Enabled
        - Disabled
    AccountFlattened:
      type: object
      description: Flattened view of Account ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        dashboardUri:
          type: string
          description: The Playwright testing dashboard URI for the account resource.
        regionalAffinity:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'This property sets the connection region for Playwright client workers to cloud-hosted browsers. If enabled, workers connect to browsers in the closest Azure region, ensuring lower latency. If disabled, workers connect to browsers in the Azure region in which the workspace was initially created. Known values are: "Enabled" and "Disabled".'
        scalableExecution:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, Playwright client workers can connect to cloud-hosted browsers. This can increase the number of parallel workers for a test run, significantly minimizing test completion durations. Known values are: "Enabled" and "Disabled".'
        reporting:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, this feature allows the workspace to upload and display test results, including artifacts like traces and screenshots, in the Playwright portal. This enables faster and more efficient troubleshooting. Known values are: "Enabled" and "Disabled".'
        localAuth:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, this feature allows the workspace to use local auth(through access key) for authentication of test runs. Known values are: "Enabled" and "Disabled".'
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Accepted".'
    PagedAccountFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AccountFlattened"
        nextLink:
          type: string
    Account:
      type: object
      description: A Playwright service account resource. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AccountProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    AccountUpdateProperties:
      type: object
      description: The updatable properties of the Account.
      properties:
        regionalAffinity:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'This property sets the connection region for Playwright client workers to cloud-hosted browsers. If enabled, workers connect to browsers in the closest Azure region, ensuring lower latency. If disabled, workers connect to browsers in the Azure region in which the workspace was initially created. Known values are: "Enabled" and "Disabled".'
        scalableExecution:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, Playwright client workers can connect to cloud-hosted browsers. This can increase the number of parallel workers for a test run, significantly minimizing test completion durations. Known values are: "Enabled" and "Disabled".'
        reporting:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, this feature allows the workspace to upload and display test results, including artifacts like traces and screenshots, in the Playwright portal. This enables faster and more efficient troubleshooting. Known values are: "Enabled" and "Disabled".'
        localAuth:
          $ref: "#/components/schemas/EnablementStatus"
          description: 'When enabled, this feature allows the workspace to use local auth(through access key) for authentication of test runs. Known values are: "Enabled" and "Disabled".'
    CheckNameAvailabilityResponse:
      type: object
      description: The check availability result.
      properties:
        nameAvailable:
          type: boolean
          description: Indicates if the resource name is available.
        reason:
          $ref: "#/components/schemas/CheckNameAvailabilityReason"
          description: 'The reason why the given name is not available. Known values are: "Invalid" and "AlreadyExists".'
        message:
          type: string
          description: Detailed reason why the given name is not available.
    CheckNameAvailabilityReason:
      type: string
      enum:
        - Invalid
        - AlreadyExists
    OperationDisplay:
      type: object
      description: Localized display information for and operation. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    QuotaProperties:
      type: object
      description: The subscription quota resource properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        freeTrial:
          $ref: "#/components/schemas/FreeTrialProperties"
          description: The subscription quota resource free-trial properties.
        offeringType:
          $ref: "#/components/schemas/OfferingType"
          description: 'Indicates the offering type for the subscription. Known values are: "NotApplicable", "PrivatePreview", "PublicPreview", and "GeneralAvailability".'
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Accepted".'
    FreeTrialProperties:
      type: object
      description: The subscription quota resource free-trial properties. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        accountId:
          type: string
          description: The Playwright service account id. Required.
        state:
          $ref: "#/components/schemas/FreeTrialState"
          description: 'The free-trial state. Required. Known values are: "Active", "Expired", "NotEligible", and "NotRegistered".'
    FreeTrialState:
      type: string
      enum:
        - Active
        - Expired
        - NotEligible
        - NotRegistered
    OfferingType:
      type: string
      enum:
        - NotApplicable
        - PrivatePreview
        - PublicPreview
        - GeneralAvailability
    QuotaFlattened:
      type: object
      description: Flattened view of Quota ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        freeTrial:
          $ref: "#/components/schemas/FreeTrialProperties"
          description: The subscription quota resource free-trial properties.
        offeringType:
          $ref: "#/components/schemas/OfferingType"
          description: 'Indicates the offering type for the subscription. Known values are: "NotApplicable", "PrivatePreview", "PublicPreview", and "GeneralAvailability".'
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", "Creating", "Deleting", and "Accepted".'
    PagedQuotaFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/QuotaFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    account_quotas:
      id: azure.playwrighttesting.account_quotas
      name: account_quotas
      title: account_quotas
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzurePlaywrightService~1accounts~1{account_name}~1quotas~1{quota_name}?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_account:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzurePlaywrightService~1accounts~1{account_name}~1quotas?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/account_quotas/methods/get"
          - $ref: "#/components/x-stackQL-resources/account_quotas/methods/list_by_account"
        insert: []
        update: []
        delete: []
    accounts:
      id: azure.playwrighttesting.accounts
      name: accounts
      title: accounts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzurePlaywrightService~1accounts~1{account_name}?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzurePlaywrightService~1accounts~1{account_name}?api-version=2024-12-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzurePlaywrightService~1accounts~1{account_name}?api-version=2024-12-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzurePlaywrightService~1accounts~1{account_name}?api-version=2024-12-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzurePlaywrightService~1accounts?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzurePlaywrightService~1accounts?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzurePlaywrightService~1checkNameAvailability?api-version=2024-12-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/get"
          - $ref: "#/components/x-stackQL-resources/accounts/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/accounts/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/accounts/methods/delete"
    operations:
      id: azure.playwrighttesting.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AzurePlaywrightService~1operations?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    quotas:
      id: azure.playwrighttesting.quotas
      name: quotas
      title: quotas
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzurePlaywrightService~1locations~1{location}~1quotas~1{quota_name}?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzurePlaywrightService~1locations~1{location}~1quotas?api-version=2024-12-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/quotas/methods/get"
          - $ref: "#/components/x-stackQL-resources/quotas/methods/list_by_subscription"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzurePlaywrightService/accounts/{account_name}/quotas/{quota_name}?api-version=2024-12-01:
    get:
      operationId: AccountQuotas_get
      description: Get quota by name for an account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of account. Required.
        - name: quota_name
          in: path
          required: true
          schema:
            type: string
          description: 'The Playwright service account quota name. Known values are: "ScalableExecution" and "Reporting". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountQuotaFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzurePlaywrightService/accounts/{account_name}/quotas?api-version=2024-12-01:
    get:
      operationId: AccountQuotas_list_by_account
      description: List quotas for a given account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAccountQuotaFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzurePlaywrightService/accounts/{account_name}?api-version=2024-12-01:
    get:
      operationId: Accounts_get
      description: Get a Account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountFlattened"
    put:
      operationId: Accounts_begin_create_or_update
      description: Create a Account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AccountProperties"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
    patch:
      operationId: Accounts_update
      description: Update a Account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/AccountUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
    delete:
      operationId: Accounts_begin_delete
      description: Delete a Account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: Name of account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzurePlaywrightService/accounts?api-version=2024-12-01:
    get:
      operationId: Accounts_list_by_resource_group
      description: List Account resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAccountFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.AzurePlaywrightService/accounts?api-version=2024-12-01:
    get:
      operationId: Accounts_list_by_subscription
      description: List Account resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAccountFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.AzurePlaywrightService/checkNameAvailability?api-version=2024-12-01:
    post:
      operationId: Accounts_check_name_availability
      description: Adds check global name availability operation, normally used if a resource name must be globally unique.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The name of the resource for which availability needs to be checked.
                type:
                  type: string
                  description: The resource type.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResponse"
  /providers/Microsoft.AzurePlaywrightService/operations?api-version=2024-12-01:
    get:
      operationId: playwrighttesting_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/providers/Microsoft.AzurePlaywrightService/locations/{location}/quotas/{quota_name}?api-version=2024-12-01:
    get:
      operationId: Quotas_get
      description: Get subscription quota by name.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The location of quota in ARM Normalized format like eastus, southeastasia etc. Required.
        - name: quota_name
          in: path
          required: true
          schema:
            type: string
          description: 'The quota name. Known values are: "ScalableExecution" and "Reporting". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuotaFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.AzurePlaywrightService/locations/{location}/quotas?api-version=2024-12-01:
    get:
      operationId: Quotas_list_by_subscription
      description: List quotas for a given subscription Id.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The location of quota in ARM Normalized format like eastus, southeastasia etc. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedQuotaFlattened"
