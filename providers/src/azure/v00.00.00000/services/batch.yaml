openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Batch Management Client
  description: The Batch Management Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-06-01-stackql-generated
  x-serviceAlias: batch
  x-sdkPackage: azure-mgmt-batch
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    ApplicationProperties:
      type: object
      description: The properties associated with the Application.
      properties:
        displayName:
          type: string
          description: The display name for the application.
        allowUpdates:
          type: boolean
          description: A value indicating whether packages within the application may be overwritten using the same version string.
        defaultVersion:
          type: string
          description: The package to use if a client requests the application but does not specify a version. This property can only be set to the name of an existing package.
    ApplicationFlattened:
      type: object
      description: Flattened view of Application ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
        displayName:
          type: string
          description: The display name for the application.
        allowUpdates:
          type: boolean
          description: A value indicating whether packages within the application may be overwritten using the same version string.
        defaultVersion:
          type: string
          description: The package to use if a client requests the application but does not specify a version. This property can only be set to the name of an existing package.
    PagedApplicationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationFlattened"
        nextLink:
          type: string
    Application:
      type: object
      description: Contains information about an application in a Batch account.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ApplicationProperties"
          description: The properties associated with the Application.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
    ApplicationPackageProperties:
      type: object
      description: Properties of an application package.
      properties:
        state:
          $ref: "#/components/schemas/PackageState"
          description: 'The current state of the application package. Known values are: "Pending" and "Active".'
        format:
          type: string
          description: The format of the application package, if the package is active.
        storageUrl:
          type: string
          description: The URL for the application package in Azure Storage.
        storageUrlExpiry:
          type: string
          format: date-time
          description: The UTC time at which the Azure Storage URL will expire.
        lastActivationTime:
          type: string
          format: date-time
          description: The time at which the package was last activated, if the package is active.
    PackageState:
      type: string
      enum:
        - Pending
        - Active
    ApplicationPackageFlattened:
      type: object
      description: Flattened view of ApplicationPackage ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
        state:
          $ref: "#/components/schemas/PackageState"
          description: 'The current state of the application package. Known values are: "Pending" and "Active".'
        format:
          type: string
          description: The format of the application package, if the package is active.
        storageUrl:
          type: string
          description: The URL for the application package in Azure Storage.
        storageUrlExpiry:
          type: string
          format: date-time
          description: The UTC time at which the Azure Storage URL will expire.
        lastActivationTime:
          type: string
          format: date-time
          description: The time at which the package was last activated, if the package is active.
    PagedApplicationPackageFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationPackageFlattened"
        nextLink:
          type: string
    ApplicationPackage:
      type: object
      description: An application package which represents a particular version of an application.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ApplicationPackageProperties"
          description: The properties associated with the Application Package.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
    BatchAccountProperties:
      type: object
      description: Account specific properties.
      properties:
        accountEndpoint:
          type: string
          description: The account endpoint used to interact with the Batch service.
        nodeManagementEndpoint:
          type: string
          description: The endpoint used by compute node to connect to the Batch node management service.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioned state of the resource. Known values are: "Invalid", "Creating", "Deleting", "Succeeded", "Failed", and "Cancelled".'
        poolAllocationMode:
          $ref: "#/components/schemas/PoolAllocationMode"
          description: 'The allocation mode to use for creating pools in the Batch account. Known values are: "BatchService" and "UserSubscription".'
        keyVaultReference:
          $ref: "#/components/schemas/KeyVaultReference"
          description: A reference to the Azure key vault associated with the Batch account.
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccessType"
          description: 'The network interface type for accessing Azure Batch service and Batch account operations. Known values are: "Enabled", "Disabled", and "SecuredByPerimeter".'
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Network profile for Batch account, which contains network rule settings for each endpoint.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connections associated with the Batch account.
        autoStorage:
          $ref: "#/components/schemas/AutoStorageProperties"
          description: The properties and status of any auto-storage account associated with the Batch account.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the Batch account.
        dedicatedCoreQuota:
          type: integer
          description: The dedicated core quota for the Batch account.
        lowPriorityCoreQuota:
          type: integer
          description: The Spot/low-priority core quota for the Batch account.
        dedicatedCoreQuotaPerVMFamily:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineFamilyCoreQuota"
          description: A list of the dedicated core quota per Virtual Machine family for the Batch account. For accounts with PoolAllocationMode set to UserSubscription, quota is managed on the subscription so this value is not returned.
        dedicatedCoreQuotaPerVMFamilyEnforced:
          type: boolean
          description: A value indicating whether core quotas per Virtual Machine family are enforced for this account.
        poolQuota:
          type: integer
          description: The pool quota for the Batch account.
        activeJobAndJobScheduleQuota:
          type: integer
          description: The active job and job schedule quota for the Batch account.
        allowedAuthenticationModes:
          type: array
          items:
            $ref: "#/components/schemas/AuthenticationMode"
          description: List of allowed authentication modes for the Batch account that can be used to authenticate with the data plane. This does not affect authentication with the control plane.
    ProvisioningState:
      type: string
      enum:
        - Invalid
        - Creating
        - Deleting
        - Succeeded
        - Failed
        - Cancelled
    PoolAllocationMode:
      type: string
      enum:
        - BatchService
        - UserSubscription
    KeyVaultReference:
      type: object
      description: Identifies the Azure key vault associated with a Batch account.
      properties:
        id:
          type: string
          description: The resource ID of the Azure key vault associated with the Batch account. Required.
        url:
          type: string
          description: The URL of the Azure key vault associated with the Batch account. Required.
      required:
        - id
        - url
    PublicNetworkAccessType:
      type: string
      enum:
        - Enabled
        - Disabled
        - SecuredByPerimeter
    NetworkProfile:
      type: object
      description: Network profile for Batch account, which contains network rule settings for each endpoint.
      properties:
        accountAccess:
          $ref: "#/components/schemas/EndpointAccessProfile"
          description: Network access profile for batchAccount endpoint (Batch account data plane API).
        nodeManagementAccess:
          $ref: "#/components/schemas/EndpointAccessProfile"
          description: Network access profile for nodeManagement endpoint (Batch service managing compute nodes for Batch pools).
    EndpointAccessProfile:
      type: object
      description: Network access profile for Batch endpoint.
      properties:
        defaultAction:
          $ref: "#/components/schemas/EndpointAccessDefaultAction"
          description: 'The default action when there is no IPRule matched. Required. Known values are: "Allow" and "Deny".'
        ipRules:
          type: array
          items:
            $ref: "#/components/schemas/IPRule"
          description: Array of IP ranges to filter client IP address.
      required:
        - defaultAction
    EndpointAccessDefaultAction:
      type: string
      enum:
        - Allow
        - Deny
    IPRule:
      type: object
      description: Rule to filter client IP address.
      properties:
        action:
          $ref: "#/components/schemas/IPRuleAction"
          description: Action when client IP address is matched. Required. "Allow"
        value:
          type: string
          description: The IP address or IP address range to filter. Required.
      required:
        - action
        - value
    IPRuleAction:
      type: string
      enum:
        - Allow
    PrivateEndpointConnection:
      type: object
      description: Contains information about a private link resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
          description: The properties associated with the private endpoint connection.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
    PrivateEndpointConnectionProperties:
      type: object
      description: Private endpoint connection properties.
      properties:
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Cancelled".'
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The ARM resource identifier of the private endpoint.
        groupIds:
          type: array
          items:
            type: string
          description: The group id of the private endpoint connection.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: The private link service connection state of the private endpoint connection.
    PrivateEndpointConnectionProvisioningState:
      type: string
      enum:
        - Creating
        - Updating
        - Deleting
        - Succeeded
        - Failed
        - Cancelled
    PrivateEndpoint:
      type: object
      description: The private endpoint of the private endpoint connection.
      properties:
        id:
          type: string
          description: The ARM resource identifier of the private endpoint. This is of the form /subscriptions/{subscription}/resourceGroups/{group}/providers/Microsoft.Network/privateEndpoints/{privateEndpoint}.
    PrivateLinkServiceConnectionState:
      type: object
      description: The private link service connection state of the private endpoint connection.
      properties:
        status:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionStatus"
          description: 'The status of the Batch private endpoint connection. Required. Known values are: "Approved", "Pending", "Rejected", and "Disconnected".'
        description:
          type: string
          description: Description of the private Connection state.
        actionsRequired:
          type: string
          description: Action required on the private connection state.
      required:
        - status
    PrivateLinkServiceConnectionStatus:
      type: string
      enum:
        - Approved
        - Pending
        - Rejected
        - Disconnected
    AutoStorageProperties:
      type: object
      description: Contains information about the auto-storage account associated with a Batch account.
      properties:
        storageAccountId:
          type: string
          description: The resource ID of the storage account to be used for auto-storage account. Required.
        authenticationMode:
          $ref: "#/components/schemas/AutoStorageAuthenticationMode"
          description: 'The authentication mode which the Batch service will use to manage the auto-storage account. Known values are: "StorageKeys" and "BatchAccountManagedIdentity".'
        nodeIdentityReference:
          $ref: "#/components/schemas/ComputeNodeIdentityReference"
          description: The reference to the user assigned identity which compute nodes will use to access auto-storage.
        lastKeySync:
          type: string
          format: date-time
          description: The UTC time at which storage keys were last synchronized with the Batch account. Required.
      required:
        - storageAccountId
        - lastKeySync
    AutoStorageAuthenticationMode:
      type: string
      enum:
        - StorageKeys
        - BatchAccountManagedIdentity
    ComputeNodeIdentityReference:
      type: object
      description: The reference to a user assigned identity associated with the Batch pool which a compute node will use.
      properties:
        resourceId:
          type: string
          description: The ARM resource id of the user assigned identity.
    EncryptionProperties:
      type: object
      description: Configures how customer data is encrypted inside the Batch account. By default, accounts are encrypted using a Microsoft managed key. For additional control, a customer-managed key can be used instead.
      properties:
        keySource:
          $ref: "#/components/schemas/KeySource"
          description: 'Type of the key source. Known values are: "Microsoft.Batch" and "Microsoft.KeyVault".'
        keyVaultProperties:
          $ref: "#/components/schemas/KeyVaultProperties"
          description: Additional details when using Microsoft.KeyVault.
    KeySource:
      type: string
      enum:
        - Microsoft.Batch
        - Microsoft.KeyVault
    KeyVaultProperties:
      type: object
      description: KeyVault configuration when using an encryption KeySource of Microsoft.KeyVault.
      properties:
        keyIdentifier:
          type: string
          description: "Full path to the secret with or without version. Example `https://mykeyvault.vault.azure.net/keys/testkey/6e34a81fef704045975661e297a4c053 `_. or `https://mykeyvault.vault.azure.net/keys/testkey `_. To be usable the following prerequisites must be met: The Batch Account has a System Assigned identity The account identity has been granted Key/Get, Key/Unwrap and Key/Wrap permissions The KeyVault has soft-delete and purge protection enabled."
    VirtualMachineFamilyCoreQuota:
      type: object
      description: A VM Family and its associated core quota for the Batch account.
      properties:
        name:
          type: string
          description: The Virtual Machine family name.
        coreQuota:
          type: integer
          description: The core quota for the VM family for the Batch account.
    AuthenticationMode:
      type: string
      enum:
        - SharedKey
        - AAD
        - TaskAuthenticationToken
    BatchAccountIdentity:
      type: object
      description: The identity of the Batch account, if configured. This is used when the user specifies 'Microsoft.KeyVault' as their Batch account encryption configuration or when `ManagedIdentity` is selected as the auto-storage authentication mode.
      properties:
        principalId:
          type: string
          description: The principal id of the Batch account. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant id associated with the Batch account. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ResourceIdentityType"
          description: 'The type of identity used for the Batch account. Required. Known values are: "SystemAssigned", "UserAssigned", and "None".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The list of user identities associated with the Batch account.
      required:
        - type
    ResourceIdentityType:
      type: string
      enum:
        - SystemAssigned
        - UserAssigned
        - None
    BatchAccountFlattened:
      type: object
      description: Flattened view of BatchAccount ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/BatchAccountIdentity"
          description: The identity of the Batch account.
        accountEndpoint:
          type: string
          description: The account endpoint used to interact with the Batch service.
        nodeManagementEndpoint:
          type: string
          description: The endpoint used by compute node to connect to the Batch node management service.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioned state of the resource. Known values are: "Invalid", "Creating", "Deleting", "Succeeded", "Failed", and "Cancelled".'
        poolAllocationMode:
          $ref: "#/components/schemas/PoolAllocationMode"
          description: 'The allocation mode to use for creating pools in the Batch account. Known values are: "BatchService" and "UserSubscription".'
        keyVaultReference:
          $ref: "#/components/schemas/KeyVaultReference"
          description: A reference to the Azure key vault associated with the Batch account.
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccessType"
          description: 'The network interface type for accessing Azure Batch service and Batch account operations. Known values are: "Enabled", "Disabled", and "SecuredByPerimeter".'
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Network profile for Batch account, which contains network rule settings for each endpoint.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnection"
          description: List of private endpoint connections associated with the Batch account.
        autoStorage:
          $ref: "#/components/schemas/AutoStorageProperties"
          description: The properties and status of any auto-storage account associated with the Batch account.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the Batch account.
        dedicatedCoreQuota:
          type: integer
          description: The dedicated core quota for the Batch account.
        lowPriorityCoreQuota:
          type: integer
          description: The Spot/low-priority core quota for the Batch account.
        dedicatedCoreQuotaPerVMFamily:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineFamilyCoreQuota"
          description: A list of the dedicated core quota per Virtual Machine family for the Batch account. For accounts with PoolAllocationMode set to UserSubscription, quota is managed on the subscription so this value is not returned.
        dedicatedCoreQuotaPerVMFamilyEnforced:
          type: boolean
          description: A value indicating whether core quotas per Virtual Machine family are enforced for this account.
        poolQuota:
          type: integer
          description: The pool quota for the Batch account.
        activeJobAndJobScheduleQuota:
          type: integer
          description: The active job and job schedule quota for the Batch account.
        allowedAuthenticationModes:
          type: array
          items:
            $ref: "#/components/schemas/AuthenticationMode"
          description: List of allowed authentication modes for the Batch account that can be used to authenticate with the data plane. This does not affect authentication with the control plane.
    PagedBatchAccountFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/BatchAccountFlattened"
        nextLink:
          type: string
    EndpointDependency:
      type: object
      description: A domain name and connection details used to access a dependency.
      properties:
        domainName:
          type: string
          description: The domain name of the dependency. Domain names may be fully qualified or may contain a * wildcard.
        description:
          type: string
          description: Human-readable supplemental information about the dependency and when it is applicable.
        endpointDetails:
          type: array
          items:
            $ref: "#/components/schemas/EndpointDetail"
          description: The list of connection details for this endpoint.
    EndpointDetail:
      type: object
      description: Details about the connection between the Batch service and the endpoint.
      properties:
        port:
          type: integer
          description: The port an endpoint is connected to.
    OutboundEnvironmentEndpoint:
      type: object
      description: A collection of related endpoints from the same service for which the Batch service requires outbound access.
      properties:
        category:
          type: string
          description: The type of service that the Batch service connects to.
        endpoints:
          type: array
          items:
            $ref: "#/components/schemas/EndpointDependency"
          description: The endpoints for this service to which the Batch service makes outbound calls.
    PagedOutboundEnvironmentEndpoint:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OutboundEnvironmentEndpoint"
        nextLink:
          type: string
    DetectorResponseProperties:
      type: object
      description: Detector response properties.
      properties:
        value:
          type: string
          description: A base64 encoded string that represents the content of a detector.
    DetectorResponseFlattened:
      type: object
      description: Flattened view of DetectorResponse ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
        value:
          type: string
          description: A base64 encoded string that represents the content of a detector.
    PagedDetectorResponseFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DetectorResponseFlattened"
        nextLink:
          type: string
    BatchAccountKeys:
      type: object
      description: A set of Azure Batch account keys.
      properties:
        accountName:
          type: string
          description: The Batch account name.
        primary:
          type: string
          description: The primary key associated with the account.
        secondary:
          type: string
          description: The secondary key associated with the account.
    BatchAccountCreateProperties:
      type: object
      description: The properties of a Batch account.
      properties:
        autoStorage:
          $ref: "#/components/schemas/AutoStorageBaseProperties"
          description: The properties related to the auto-storage account.
        poolAllocationMode:
          $ref: "#/components/schemas/PoolAllocationMode"
          description: 'The allocation mode to use for creating pools in the Batch account. Known values are: "BatchService" and "UserSubscription".'
        keyVaultReference:
          $ref: "#/components/schemas/KeyVaultReference"
          description: A reference to the Azure key vault associated with the Batch account.
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccessType"
          description: 'The network access type for accessing Azure Batch account. Known values are: "Enabled", "Disabled", and "SecuredByPerimeter".'
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Network profile for Batch account, which contains network rule settings for each endpoint.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the Batch account.
        allowedAuthenticationModes:
          type: array
          items:
            $ref: "#/components/schemas/AuthenticationMode"
          description: List of allowed authentication modes for the Batch account that can be used to authenticate with the data plane. This does not affect authentication with the control plane.
    AutoStorageBaseProperties:
      type: object
      description: The properties related to the auto-storage account.
      properties:
        storageAccountId:
          type: string
          description: The resource ID of the storage account to be used for auto-storage account. Required.
        authenticationMode:
          $ref: "#/components/schemas/AutoStorageAuthenticationMode"
          description: 'The authentication mode which the Batch service will use to manage the auto-storage account. Known values are: "StorageKeys" and "BatchAccountManagedIdentity".'
        nodeIdentityReference:
          $ref: "#/components/schemas/ComputeNodeIdentityReference"
          description: The reference to the user assigned identity which compute nodes will use to access auto-storage.
      required:
        - storageAccountId
    BatchAccount:
      type: object
      description: Contains information about an Azure Batch account.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/BatchAccountProperties"
          description: The properties associated with the account.
        identity:
          $ref: "#/components/schemas/BatchAccountIdentity"
          description: The identity of the Batch account.
      required:
        - location
    BatchAccountUpdateProperties:
      type: object
      description: The properties of a Batch account.
      properties:
        autoStorage:
          $ref: "#/components/schemas/AutoStorageBaseProperties"
          description: The properties related to the auto-storage account.
        encryption:
          $ref: "#/components/schemas/EncryptionProperties"
          description: The encryption configuration for the Batch account.
        allowedAuthenticationModes:
          type: array
          items:
            $ref: "#/components/schemas/AuthenticationMode"
          description: List of allowed authentication modes for the Batch account that can be used to authenticate with the data plane. This does not affect authentication with the control plane.
        publicNetworkAccess:
          $ref: "#/components/schemas/PublicNetworkAccessType"
          description: 'The network access type for accessing Azure Batch account. Known values are: "Enabled", "Disabled", and "SecuredByPerimeter".'
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Network profile for Batch account, which contains network rule settings for each endpoint.
    AccountKeyType:
      type: string
      enum:
        - Primary
        - Secondary
    SkuCapability:
      type: object
      description: A SKU capability, such as the number of cores.
      properties:
        name:
          type: string
          description: The name of the feature.
        value:
          type: string
          description: The value of the feature.
    SupportedSku:
      type: object
      description: Describes a Batch supported SKU.
      properties:
        name:
          type: string
          description: The name of the SKU.
        familyName:
          type: string
          description: The family name of the SKU.
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/SkuCapability"
          description: A collection of capabilities which this SKU supports.
        batchSupportEndOfLife:
          type: string
          format: date-time
          description: The time when Azure Batch service will retire this SKU.
    PagedSupportedSku:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SupportedSku"
        nextLink:
          type: string
    BatchLocationQuota:
      type: object
      description: Quotas associated with a Batch region for a particular subscription.
      properties:
        accountQuota:
          type: integer
          description: The number of Batch accounts that may be created under the subscription in the specified region.
    ResourceType:
      type: string
      enum:
        - Microsoft.Batch/batchAccounts
    CheckNameAvailabilityResult:
      type: object
      description: The CheckNameAvailability operation response.
      properties:
        nameAvailable:
          type: boolean
          description: Gets a boolean value that indicates whether the name is available for you to use. If true, the name is available. If false, the name has already been taken or invalid and cannot be used.
        reason:
          $ref: "#/components/schemas/NameAvailabilityReason"
          description: 'Gets the reason that a Batch account name could not be used. The Reason element is only returned if NameAvailable is false. Known values are: "Invalid" and "AlreadyExists".'
        message:
          type: string
          description: Gets an error message explaining the Reason value in more detail.
    NameAvailabilityReason:
      type: string
      enum:
        - Invalid
        - AlreadyExists
    NetworkSecurityPerimeterConfigurationProperties:
      type: object
      description: Network security configuration properties.
      properties:
        provisioningState:
          $ref: "#/components/schemas/NetworkSecurityPerimeterConfigurationProvisioningState"
          description: 'Known values are: "Succeeded", "Creating", "Updating", "Deleting", "Accepted", "Failed", and "Canceled".'
        provisioningIssues:
          type: array
          items:
            $ref: "#/components/schemas/ProvisioningIssue"
          description: List of provisioning issues, if any.
        networkSecurityPerimeter:
          $ref: "#/components/schemas/NetworkSecurityPerimeter"
          description: ":vartype network_security_perimeter: ~azure.mgmt.batch.models.NetworkSecurityPerimeter"
        resourceAssociation:
          $ref: "#/components/schemas/ResourceAssociation"
          description: ":vartype resource_association: ~azure.mgmt.batch.models.ResourceAssociation"
        profile:
          $ref: "#/components/schemas/NetworkSecurityProfile"
          description: ":vartype profile: ~azure.mgmt.batch.models.NetworkSecurityProfile"
    NetworkSecurityPerimeterConfigurationProvisioningState:
      type: string
      enum:
        - Succeeded
        - Creating
        - Updating
        - Deleting
        - Accepted
        - Failed
        - Canceled
    ProvisioningIssue:
      type: object
      description: Describes a provisioning issue for a network security perimeter configuration.
      properties:
        name:
          type: string
          description: Name of the issue.
        properties:
          $ref: "#/components/schemas/ProvisioningIssueProperties"
          description: ":vartype properties: ~azure.mgmt.batch.models.ProvisioningIssueProperties"
    ProvisioningIssueProperties:
      type: object
      description: Details of a provisioning issue for a network security perimeter (NSP) configuration. Resource providers should generate separate provisioning issue elements for each separate issue detected, and include a meaningful and distinctive description, as well as any appropriate suggestedResourceIds and suggestedAccessRules.
      properties:
        issueType:
          $ref: "#/components/schemas/IssueType"
          description: 'Type of issue. Known values are: "Unknown", "ConfigurationPropagationFailure", "MissingPerimeterConfiguration", and "MissingIdentityConfiguration".'
        severity:
          $ref: "#/components/schemas/Severity"
          description: 'Severity of the issue. Known values are: "Warning" and "Error".'
        description:
          type: string
          description: Description of the issue.
        suggestedResourceIds:
          type: array
          items:
            type: string
          description: Fully qualified resource IDs of suggested resources that can be associated to the network security perimeter (NSP) to remediate the issue.
        suggestedAccessRules:
          type: array
          items:
            $ref: "#/components/schemas/AccessRule"
          description: Access rules that can be added to the network security profile (NSP) to remediate the issue.
    IssueType:
      type: string
      enum:
        - Unknown
        - ConfigurationPropagationFailure
        - MissingPerimeterConfiguration
        - MissingIdentityConfiguration
    Severity:
      type: string
      enum:
        - Warning
        - Error
    AccessRule:
      type: object
      description: Access rule in a network security perimeter configuration profile.
      properties:
        name:
          type: string
          description: Name of the access rule.
        properties:
          $ref: "#/components/schemas/AccessRuleProperties"
          description: ":vartype properties: ~azure.mgmt.batch.models.AccessRuleProperties"
    AccessRuleProperties:
      type: object
      description: Properties of Access Rule.
      properties:
        direction:
          $ref: "#/components/schemas/AccessRuleDirection"
          description: 'Known values are: "Inbound" and "Outbound".'
        addressPrefixes:
          type: array
          items:
            type: string
          description: Address prefixes in the CIDR format for inbound rules.
        subscriptions:
          type: array
          items:
            $ref: "#/components/schemas/AccessRulePropertiesSubscription"
          description: Subscriptions for inbound rules.
        networkSecurityPerimeters:
          type: array
          items:
            $ref: "#/components/schemas/NetworkSecurityPerimeter"
          description: Network security perimeters for inbound rules.
        fullyQualifiedDomainNames:
          type: array
          items:
            type: string
          description: Fully qualified domain names (FQDN) for outbound rules.
        emailAddresses:
          type: array
          items:
            type: string
          description: Email addresses for outbound rules.
        phoneNumbers:
          type: array
          items:
            type: string
          description: Phone numbers for outbound rules.
    AccessRuleDirection:
      type: string
      enum:
        - Inbound
        - Outbound
    AccessRulePropertiesSubscription:
      type: object
      description: AccessRulePropertiesSubscription.
      properties:
        id:
          type: string
          description: The fully qualified Azure resource ID of the subscription e.g. ('/subscriptions/00000000-0000-0000-0000-000000000000').
    NetworkSecurityPerimeter:
      type: object
      description: Information about a network security perimeter (NSP).
      properties:
        id:
          type: string
          description: Fully qualified Azure resource ID of the NSP resource.
        perimeterGuid:
          type: string
          description: Universal unique ID (UUID) of the network security perimeter.
        location:
          type: string
          description: Location of the network security perimeter.
    ResourceAssociation:
      type: object
      description: Information about resource association.
      properties:
        name:
          type: string
          description: Name of the resource association.
        accessMode:
          $ref: "#/components/schemas/ResourceAssociationAccessMode"
          description: 'Known values are: "Enforced", "Learning", and "Audit".'
    ResourceAssociationAccessMode:
      type: string
      enum:
        - Enforced
        - Learning
        - Audit
    NetworkSecurityProfile:
      type: object
      description: Network security perimeter configuration profile.
      properties:
        name:
          type: string
          description: Name of the profile.
        accessRulesVersion:
          type: integer
          description: Current access rules version.
        accessRules:
          type: array
          items:
            $ref: "#/components/schemas/AccessRule"
          description: List of Access Rules.
        diagnosticSettingsVersion:
          type: integer
          description: Current diagnostic settings version.
        enabledLogCategories:
          type: array
          items:
            type: string
          description: List of log categories that are enabled.
    NetworkSecurityPerimeterConfigurationFlattened:
      type: object
      description: Flattened view of NetworkSecurityPerimeterConfiguration ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          $ref: "#/components/schemas/NetworkSecurityPerimeterConfigurationProvisioningState"
          description: 'Known values are: "Succeeded", "Creating", "Updating", "Deleting", "Accepted", "Failed", and "Canceled".'
        provisioningIssues:
          type: array
          items:
            $ref: "#/components/schemas/ProvisioningIssue"
          description: List of provisioning issues, if any.
        networkSecurityPerimeter:
          $ref: "#/components/schemas/NetworkSecurityPerimeter"
          description: ":vartype network_security_perimeter: ~azure.mgmt.batch.models.NetworkSecurityPerimeter"
        resourceAssociation:
          $ref: "#/components/schemas/ResourceAssociation"
          description: ":vartype resource_association: ~azure.mgmt.batch.models.ResourceAssociation"
        profile:
          $ref: "#/components/schemas/NetworkSecurityProfile"
          description: ":vartype profile: ~azure.mgmt.batch.models.NetworkSecurityProfile"
    PagedNetworkSecurityPerimeterConfigurationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/NetworkSecurityPerimeterConfigurationFlattened"
        nextLink:
          type: string
    OperationDisplay:
      type: object
      description: The object that describes the operation.
      properties:
        provider:
          type: string
          description: Friendly name of the resource provider.
        operation:
          type: string
          description: The operation type.
        resource:
          type: string
          description: The resource type on which the operation is performed.
        description:
          type: string
          description: The friendly name of the operation.
    Operation:
      type: object
      description: A REST API operation.
      properties:
        name:
          type: string
          description: The operation name.
        isDataAction:
          type: boolean
          description: Indicates whether the operation is a data action.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The object that describes the operation.
        origin:
          type: string
          description: The intended executor of the operation.
        properties:
          type: object
          properties: {}
          description: Properties of the operation.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    PoolProperties:
      type: object
      description: Pool properties.
      properties:
        displayName:
          type: string
          description: The display name for the pool.
        lastModified:
          type: string
          format: date-time
          description: The last modified time of the pool.
        creationTime:
          type: string
          format: date-time
          description: The creation time of the pool.
        provisioningState:
          $ref: "#/components/schemas/PoolProvisioningState"
          description: 'The current state of the pool. Known values are: "Succeeded" and "Deleting".'
        provisioningStateTransitionTime:
          type: string
          format: date-time
          description: The time at which the pool entered its current state.
        allocationState:
          $ref: "#/components/schemas/AllocationState"
          description: 'Whether the pool is resizing. Known values are: "Steady", "Resizing", and "Stopping".'
        allocationStateTransitionTime:
          type: string
          format: date-time
          description: The time at which the pool entered its current allocation state.
        vmSize:
          type: string
          description: The size of virtual machines in the pool. All VMs in a pool are the same size.
        deploymentConfiguration:
          $ref: "#/components/schemas/DeploymentConfiguration"
          description: This property describes the virtual machines that the pool nodes will be deployed on.
        currentDedicatedNodes:
          type: integer
          description: The number of dedicated compute nodes currently in the pool.
        currentLowPriorityNodes:
          type: integer
          description: The number of Spot/low-priority compute nodes currently in the pool.
        scaleSettings:
          $ref: "#/components/schemas/ScaleSettings"
          description: Settings which configure the number of nodes in the pool.
        autoScaleRun:
          $ref: "#/components/schemas/AutoScaleRun"
          description: The results and errors from the last execution of the autoscale formula.
        interNodeCommunication:
          $ref: "#/components/schemas/InterNodeCommunicationState"
          description: 'Whether the pool permits direct communication between nodes. Known values are: "Enabled" and "Disabled".'
        networkConfiguration:
          $ref: "#/components/schemas/NetworkConfiguration"
          description: The network configuration for the pool.
        taskSlotsPerNode:
          type: integer
          description: The number of task slots that can be used to run concurrent tasks on a single compute node in the pool.
        taskSchedulingPolicy:
          $ref: "#/components/schemas/TaskSchedulingPolicy"
          description: How tasks are distributed across compute nodes in a pool.
        userAccounts:
          type: array
          items:
            $ref: "#/components/schemas/UserAccount"
          description: The list of user accounts to be created on each node in the pool.
        metadata:
          type: array
          items:
            $ref: "#/components/schemas/MetadataItem"
          description: A list of name-value pairs associated with the pool as metadata.
        startTask:
          $ref: "#/components/schemas/StartTask"
          description: A task specified to run on each compute node as it joins the pool.
        applicationPackages:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationPackageReference"
          description: The list of application packages to be installed on each compute node in the pool.
        resizeOperationStatus:
          $ref: "#/components/schemas/ResizeOperationStatus"
          description: Contains details about the current or last completed resize operation.
        mountConfiguration:
          type: array
          items:
            $ref: "#/components/schemas/MountConfiguration"
          description: A list of file systems to mount on each node in the pool.
        upgradePolicy:
          $ref: "#/components/schemas/UpgradePolicy"
          description: The upgrade policy for the pool.
    PoolProvisioningState:
      type: string
      enum:
        - Succeeded
        - Deleting
    AllocationState:
      type: string
      enum:
        - Steady
        - Resizing
        - Stopping
    DeploymentConfiguration:
      type: object
      description: Deployment configuration properties.
      properties:
        virtualMachineConfiguration:
          $ref: "#/components/schemas/VirtualMachineConfiguration"
          description: The virtual machine configuration for the pool.
    VirtualMachineConfiguration:
      type: object
      description: The configuration for compute nodes in a pool based on the Azure Virtual Machines infrastructure.
      properties:
        imageReference:
          $ref: "#/components/schemas/ImageReference"
          description: A reference to the Azure Virtual Machines Marketplace Image or the custom Virtual Machine Image to use. Required.
        nodeAgentSkuId:
          type: string
          description: The SKU of the Batch node agent to be provisioned on compute nodes in the pool. Required.
        windowsConfiguration:
          $ref: "#/components/schemas/WindowsConfiguration"
          description: Windows operating system settings on the virtual machine.
        dataDisks:
          type: array
          items:
            $ref: "#/components/schemas/DataDisk"
          description: The configuration for data disks attached to the compute nodes in the pool.
        licenseType:
          type: string
          description: The type of on-premises license to be used when deploying the operating system.
        containerConfiguration:
          $ref: "#/components/schemas/ContainerConfiguration"
          description: The container configuration for the pool.
        diskEncryptionConfiguration:
          $ref: "#/components/schemas/DiskEncryptionConfiguration"
          description: The disk encryption configuration for the pool.
        nodePlacementConfiguration:
          $ref: "#/components/schemas/NodePlacementConfiguration"
          description: The node placement configuration for the pool.
        extensions:
          type: array
          items:
            $ref: "#/components/schemas/VMExtension"
          description: The virtual machine extension for the pool.
        osDisk:
          $ref: "#/components/schemas/OSDisk"
          description: Settings for the operating system disk of the Virtual Machine.
        securityProfile:
          $ref: "#/components/schemas/SecurityProfile"
          description: Specifies the security profile settings for the virtual machine or virtual machine scale set.
        serviceArtifactReference:
          $ref: "#/components/schemas/ServiceArtifactReference"
          description: Specifies the service artifact reference id used to set same image version for all virtual machines in the scale set when using 'latest' image version.
      required:
        - imageReference
        - nodeAgentSkuId
    ImageReference:
      type: object
      description: A reference to an Azure Virtual Machines Marketplace image or the Azure Image resource of a custom Virtual Machine. To get the list of all imageReferences verified by Azure Batch, see the 'List supported node agent SKUs' operation.
      properties:
        publisher:
          type: string
          description: The publisher of the Azure Virtual Machines Marketplace image.
        offer:
          type: string
          description: The offer type of the Azure Virtual Machines Marketplace image.
        sku:
          type: string
          description: The SKU of the Azure Virtual Machines Marketplace image.
        version:
          type: string
          description: The version of the Azure Virtual Machines Marketplace image.
        id:
          type: string
          description: The ARM resource identifier of the Azure Compute Gallery Image. Compute Nodes in the Pool will be created using this Image Id. This is of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.Compute/galleries/{galleryName}/images/{imageDefinitionName}/versions/{versionId}.
        sharedGalleryImageId:
          type: string
          description: The shared gallery image unique identifier.
        communityGalleryImageId:
          type: string
          description: The community gallery image unique identifier.
    WindowsConfiguration:
      type: object
      description: Windows operating system settings to apply to the virtual machine.
      properties:
        enableAutomaticUpdates:
          type: boolean
          description: Whether automatic updates are enabled on the virtual machine.
    DataDisk:
      type: object
      description: Settings which will be used by the data disks associated to Compute Nodes in the Pool. When using attached data disks, you need to mount and format the disks from within a VM to use them.
      properties:
        lun:
          type: integer
          description: The logical unit number. Required.
        caching:
          $ref: "#/components/schemas/CachingType"
          description: 'The type of caching to enable for the disk. Known values are: "None", "ReadOnly", and "ReadWrite".'
        diskSizeGB:
          type: integer
          description: The initial disk size in GB when creating new data disk. Required.
        managedDisk:
          $ref: "#/components/schemas/ManagedDisk"
          description: The managed disk parameters.
      required:
        - lun
        - diskSizeGB
    CachingType:
      type: string
      enum:
        - None
        - ReadOnly
        - ReadWrite
    ManagedDisk:
      type: object
      description: The managed disk parameters.
      properties:
        storageAccountType:
          $ref: "#/components/schemas/StorageAccountType"
          description: 'The storage account type for use in creating data disks or OS disk. Known values are: "Standard_LRS", "Premium_LRS", and "StandardSSD_LRS".'
        securityProfile:
          $ref: "#/components/schemas/VMDiskSecurityProfile"
          description: Specifies the security profile for the managed disk.
        diskEncryptionSet:
          $ref: "#/components/schemas/DiskEncryptionSetParameters"
          description: Specifies the customer managed disk encryption set resource id for the managed disk. It can be set only in UserSubscription mode.
    StorageAccountType:
      type: string
      enum:
        - Standard_LRS
        - Premium_LRS
        - StandardSSD_LRS
    VMDiskSecurityProfile:
      type: object
      description: "Specifies the security profile settings for the managed disk. **Note**: It can only be set for Confidential VMs and is required when using Confidential VMs."
      properties:
        securityEncryptionType:
          $ref: "#/components/schemas/SecurityEncryptionTypes"
          description: 'Specifies the EncryptionType of the managed disk. It is set to VMGuestStateOnly for encryption of just the VMGuestState blob, and NonPersistedTPM for not persisting firmware state in the VMGuestState blob. **Note**: It can be set for only Confidential VMs and required when using Confidential VMs. Known values are: "NonPersistedTPM", "VMGuestStateOnly", and "DiskWithVMGuestState".'
        diskEncryptionSet:
          $ref: "#/components/schemas/DiskEncryptionSetParameters"
          description: Specifies the customer managed disk encryption set resource id for the managed disk that is used for Customer Managed Key encrypted ConfidentialVM OS Disk and VMGuest blob. It can be set only in UserSubscription mode.
    SecurityEncryptionTypes:
      type: string
      enum:
        - NonPersistedTPM
        - VMGuestStateOnly
        - DiskWithVMGuestState
    DiskEncryptionSetParameters:
      type: object
      description: The ARM resource id of the disk encryption set.
      properties:
        id:
          type: string
          description: The resource id of the disk encryption set. Required.
      required:
        - id
    ContainerConfiguration:
      type: object
      description: The configuration for container-enabled pools.
      properties:
        type:
          $ref: "#/components/schemas/ContainerType"
          description: 'The container technology to be used. Required. Known values are: "DockerCompatible" and "CriCompatible".'
        containerImageNames:
          type: array
          items:
            type: string
          description: The collection of container image names.
        containerRegistries:
          type: array
          items:
            $ref: "#/components/schemas/ContainerRegistry"
          description: Additional private registries from which containers can be pulled.
      required:
        - type
    ContainerType:
      type: string
      enum:
        - DockerCompatible
        - CriCompatible
    ContainerRegistry:
      type: object
      description: A private container registry.
      properties:
        username:
          type: string
          description: The user name to log into the registry server.
        password:
          type: string
          description: The password to log into the registry server.
        registryServer:
          type: string
          description: The registry URL.
        identityReference:
          $ref: "#/components/schemas/ComputeNodeIdentityReference"
          description: The reference to the user assigned identity to use to access an Azure Container Registry instead of username and password.
    DiskEncryptionConfiguration:
      type: object
      description: The disk encryption configuration applied on compute nodes in the pool. Disk encryption configuration is not supported on Linux pool created with Virtual Machine Image or Azure Compute Gallery Image.
      properties:
        targets:
          type: array
          items:
            $ref: "#/components/schemas/DiskEncryptionTarget"
          description: The list of disk targets Batch Service will encrypt on the compute node.
        customerManagedKey:
          $ref: "#/components/schemas/DiskCustomerManagedKey"
          description: The Customer Managed Key reference to encrypt the OS Disk.
    DiskEncryptionTarget:
      type: string
      enum:
        - OsDisk
        - TemporaryDisk
    DiskCustomerManagedKey:
      type: object
      description: The Customer Managed Key reference to encrypt the Disk.
      properties:
        keyUrl:
          type: string
          description: Fully versioned Key Url pointing to a key in KeyVault. Version segment of the Url is required regardless of rotationToLatestKeyVersionEnabled value.
        rotationToLatestKeyVersionEnabled:
          type: boolean
          description: Set this flag to true to enable auto-updating of the Disk Encryption to the latest key version. Default is false.
        identityReference:
          $ref: "#/components/schemas/ComputeNodeIdentityReference"
          description: The reference of one of the pool identities to encrypt Disk. This identity will be used to access the KeyVault.
    NodePlacementConfiguration:
      type: object
      description: Node placement configuration for Batch pools.
      properties:
        policy:
          $ref: "#/components/schemas/NodePlacementPolicyType"
          description: 'Node placement Policy type on Batch pools. Known values are: "Regional" and "Zonal".'
    NodePlacementPolicyType:
      type: string
      enum:
        - Regional
        - Zonal
    VMExtension:
      type: object
      description: The configuration for virtual machine extensions.
      properties:
        name:
          type: string
          description: The name of the virtual machine extension. Required.
        publisher:
          type: string
          description: The name of the extension handler publisher. Required.
        type:
          type: string
          description: The type of the extensions. Required.
        typeHandlerVersion:
          type: string
          description: The version of script handler.
        autoUpgradeMinorVersion:
          type: boolean
          description: Indicates whether the extension should use a newer minor version if one is available at deployment time. Once deployed, however, the extension will not upgrade minor versions unless redeployed, even with this property set to true.
        enableAutomaticUpgrade:
          type: boolean
          description: Indicates whether the extension should be automatically upgraded by the platform if there is a newer version of the extension available.
        settings:
          type: object
          properties: {}
          description: JSON formatted public settings for the extension.
        protectedSettings:
          type: object
          properties: {}
          description: The extension can contain either protectedSettings or protectedSettingsFromKeyVault or no protected settings at all.
        provisionAfterExtensions:
          type: array
          items:
            type: string
          description: The collection of extension names.
      required:
        - name
        - publisher
        - type
    OSDisk:
      type: object
      description: Settings for the operating system disk of the virtual machine.
      properties:
        ephemeralOSDiskSettings:
          $ref: "#/components/schemas/DiffDiskSettings"
          description: Specifies the ephemeral Disk Settings for the operating system disk used by the virtual machine.
        caching:
          $ref: "#/components/schemas/CachingType"
          description: 'The type of caching to enable for the disk. Known values are: "None", "ReadOnly", and "ReadWrite".'
        managedDisk:
          $ref: "#/components/schemas/ManagedDisk"
          description: The managed disk parameters.
        diskSizeGB:
          type: integer
          description: The initial disk size in GB when creating new OS disk.
        writeAcceleratorEnabled:
          type: boolean
          description: Specifies whether writeAccelerator should be enabled or disabled on the disk.
    DiffDiskSettings:
      type: object
      description: Specifies the ephemeral Disk Settings for the operating system disk used by the virtual machine.
      properties:
        placement:
          $ref: "#/components/schemas/DiffDiskPlacement"
          description: Specifies the ephemeral disk placement for operating system disk for all VMs in the pool. "CacheDisk"
    DiffDiskPlacement:
      type: string
      enum:
        - CacheDisk
    SecurityProfile:
      type: object
      description: Specifies the security profile settings for the virtual machine or virtual machine scale set.
      properties:
        securityType:
          $ref: "#/components/schemas/SecurityTypes"
          description: 'Specifies the SecurityType of the virtual machine. It has to be set to any specified value to enable UefiSettings. Known values are: "trustedLaunch" and "confidentialVM".'
        encryptionAtHost:
          type: boolean
          description: This property can be used by user in the request to enable or disable the Host Encryption for the virtual machine or virtual machine scale set. This will enable the encryption for all the disks including Resource/Temp disk at host itself.
        uefiSettings:
          $ref: "#/components/schemas/UefiSettings"
          description: Specifies the security settings like secure boot and vTPM used while creating the virtual machine.
        proxyAgentSettings:
          $ref: "#/components/schemas/ProxyAgentSettings"
          description: Specifies ProxyAgent settings while creating the virtual machine.
    SecurityTypes:
      type: string
      enum:
        - trustedLaunch
        - confidentialVM
    UefiSettings:
      type: object
      description: Specifies the security settings like secure boot and vTPM used while creating the virtual machine.
      properties:
        secureBootEnabled:
          type: boolean
          description: Specifies whether secure boot should be enabled on the virtual machine.
        vTpmEnabled:
          type: boolean
          description: Specifies whether vTPM should be enabled on the virtual machine.
    ProxyAgentSettings:
      type: object
      description: Specifies ProxyAgent settings while creating the virtual machine.
      properties:
        enabled:
          type: boolean
          description: Specifies whether Metadata Security Protocol feature should be enabled on the virtual machine or virtual machine scale set. Default is False.
        imds:
          $ref: "#/components/schemas/HostEndpointSettings"
          description: Settings for the IMDS endpoint.
        wireServer:
          $ref: "#/components/schemas/HostEndpointSettings"
          description: Settings for the WireServer endpoint.
    HostEndpointSettings:
      type: object
      description: Specifies particular host endpoint settings.
      properties:
        mode:
          $ref: "#/components/schemas/HostEndpointSettingsModeTypes"
          description: 'Specifies the access control policy execution mode. Known values are: "Audit" and "Enforce".'
        inVMAccessControlProfileReferenceId:
          type: string
          description: Specifies the reference to the InVMAccessControlProfileVersion resource id in the form of /subscriptions/{SubscriptionId}/resourceGroups/{ResourceGroupName}/providers/Microsoft.Compute/galleries/{galleryName}/inVMAccessControlProfiles/{profile}/versions/{version}.
    HostEndpointSettingsModeTypes:
      type: string
      enum:
        - Audit
        - Enforce
    ServiceArtifactReference:
      type: object
      description: Specifies the service artifact reference id used to set same image version for all virtual machines in the scale set when using 'latest' image version.
      properties:
        id:
          type: string
          description: The service artifact reference id of ServiceArtifactReference. Required.
      required:
        - id
    ScaleSettings:
      type: object
      description: Scale settings for the pool.
      properties:
        fixedScale:
          $ref: "#/components/schemas/FixedScaleSettings"
          description: Fixed scale settings for the pool.
        autoScale:
          $ref: "#/components/schemas/AutoScaleSettings"
          description: AutoScale settings for the pool.
    FixedScaleSettings:
      type: object
      description: Fixed scale settings for the pool.
      properties:
        resizeTimeout:
          type: string
          description: The timeout for allocation of compute nodes to the pool.
        targetDedicatedNodes:
          type: integer
          description: The desired number of dedicated compute nodes in the pool.
        targetLowPriorityNodes:
          type: integer
          description: The desired number of Spot/low-priority compute nodes in the pool.
        nodeDeallocationOption:
          $ref: "#/components/schemas/ComputeNodeDeallocationOption"
          description: 'Determines what to do with a node and its running task(s) after it has been selected for deallocation. Known values are: "Requeue", "Terminate", "TaskCompletion", and "RetainedData".'
    ComputeNodeDeallocationOption:
      type: string
      enum:
        - Requeue
        - Terminate
        - TaskCompletion
        - RetainedData
    AutoScaleSettings:
      type: object
      description: AutoScale settings for the pool.
      properties:
        formula:
          type: string
          description: A formula for the desired number of compute nodes in the pool. Required.
        evaluationInterval:
          type: string
          description: The time interval at which to automatically adjust the pool size according to the autoscale formula.
      required:
        - formula
    AutoScaleRun:
      type: object
      description: The results and errors from an execution of a pool autoscale formula.
      properties:
        evaluationTime:
          type: string
          format: date-time
          description: The time at which the autoscale formula was last evaluated. Required.
        results:
          type: string
          description: The final values of all variables used in the evaluation of the autoscale formula.
        error:
          $ref: "#/components/schemas/AutoScaleRunError"
          description: Details of the error encountered evaluating the autoscale formula on the pool, if the evaluation was unsuccessful.
      required:
        - evaluationTime
    AutoScaleRunError:
      type: object
      description: An error that occurred when autoscaling a pool.
      properties:
        code:
          type: string
          description: An identifier for the error. Codes are invariant and are intended to be consumed programmatically. Required.
        message:
          type: string
          description: A message describing the error, intended to be suitable for display in a user interface. Required.
        details:
          type: array
          items:
            $ref: "#/components/schemas/AutoScaleRunError"
          description: Additional details about the error.
      required:
        - code
        - message
    InterNodeCommunicationState:
      type: string
      enum:
        - Enabled
        - Disabled
    NetworkConfiguration:
      type: object
      description: The network configuration for a pool.
      properties:
        subnetId:
          type: string
          description: The ARM resource identifier of the virtual network subnet which the compute nodes of the pool will join. This is of the form /subscriptions/{subscription}/resourceGroups/{group}/providers/{provider}/virtualNetworks/{network}/subnets/{subnet}.
        dynamicVnetAssignmentScope:
          $ref: "#/components/schemas/DynamicVNetAssignmentScope"
          description: 'The scope of dynamic vnet assignment. Known values are: "none" and "job".'
        endpointConfiguration:
          $ref: "#/components/schemas/PoolEndpointConfiguration"
          description: The configuration for endpoints on compute nodes in the Batch pool.
        publicIPAddressConfiguration:
          $ref: "#/components/schemas/PublicIPAddressConfiguration"
          description: The Public IPAddress configuration for Compute Nodes in the Batch Pool.
        enableAcceleratedNetworking:
          type: boolean
          description: Whether this pool should enable accelerated networking.
    DynamicVNetAssignmentScope:
      type: string
      enum:
        - none
        - job
    PoolEndpointConfiguration:
      type: object
      description: The endpoint configuration for a pool.
      properties:
        inboundNatPools:
          type: array
          items:
            $ref: "#/components/schemas/InboundNatPool"
          description: A list of inbound NAT pools that can be used to address specific ports on an individual compute node externally. Required.
      required:
        - inboundNatPools
    InboundNatPool:
      type: object
      description: A inbound NAT pool that can be used to address specific ports on compute nodes in a Batch pool externally.
      properties:
        name:
          type: string
          description: The name of the endpoint. Required.
        protocol:
          $ref: "#/components/schemas/InboundEndpointProtocol"
          description: 'The protocol of the endpoint. Required. Known values are: "TCP" and "UDP".'
        backendPort:
          type: integer
          description: The port number on the compute node. Required.
        frontendPortRangeStart:
          type: integer
          description: The first port number in the range of external ports that will be used to provide inbound access to the backendPort on individual compute nodes. Required.
        frontendPortRangeEnd:
          type: integer
          description: The last port number in the range of external ports that will be used to provide inbound access to the backendPort on individual compute nodes. Required.
        networkSecurityGroupRules:
          type: array
          items:
            $ref: "#/components/schemas/NetworkSecurityGroupRule"
          description: A list of network security group rules that will be applied to the endpoint.
      required:
        - name
        - protocol
        - backendPort
        - frontendPortRangeStart
        - frontendPortRangeEnd
    InboundEndpointProtocol:
      type: string
      enum:
        - TCP
        - UDP
    NetworkSecurityGroupRule:
      type: object
      description: A network security group rule to apply to an inbound endpoint.
      properties:
        priority:
          type: integer
          description: The priority for this rule. Required.
        access:
          $ref: "#/components/schemas/NetworkSecurityGroupRuleAccess"
          description: 'The action that should be taken for a specified IP address, subnet range or tag. Required. Known values are: "Allow" and "Deny".'
        sourceAddressPrefix:
          type: string
          description: The source address prefix or tag to match for the rule. Required.
        sourcePortRanges:
          type: array
          items:
            type: string
          description: The source port ranges to match for the rule.
      required:
        - priority
        - access
        - sourceAddressPrefix
    NetworkSecurityGroupRuleAccess:
      type: string
      enum:
        - Allow
        - Deny
    PublicIPAddressConfiguration:
      type: object
      description: The public IP Address configuration of the networking configuration of a Pool.
      properties:
        provision:
          $ref: "#/components/schemas/IPAddressProvisioningType"
          description: 'The provisioning type for Public IP Addresses for the Batch Pool. Known values are: "BatchManaged", "UserManaged", and "NoPublicIPAddresses".'
        ipAddressIds:
          type: array
          items:
            type: string
          description: The list of public IPs which the Batch service will use when provisioning Compute Nodes.
        ipFamilies:
          type: array
          items:
            $ref: "#/components/schemas/IPFamily"
          description: The IP families used to specify IP versions available to the pool.
        ipTags:
          type: array
          items:
            $ref: "#/components/schemas/IPTag"
          description: IP Tags that will applied to new Public IPs that Batch creates.
    IPAddressProvisioningType:
      type: string
      enum:
        - BatchManaged
        - UserManaged
        - NoPublicIPAddresses
    IPFamily:
      type: string
      enum:
        - IPv4
        - IPv6
    IPTag:
      type: object
      description: Specifies how tasks should be distributed across compute nodes.
      properties:
        ipTagType:
          type: string
          description: The IP Tag type.
        tag:
          type: string
          description: The value of the IP tag associated with the public IP.
    TaskSchedulingPolicy:
      type: object
      description: Specifies how tasks should be distributed across compute nodes.
      properties:
        jobDefaultOrder:
          $ref: "#/components/schemas/JobDefaultOrder"
          description: 'If not specified, the default is none. Known values are: "None" and "CreationTime".'
        nodeFillType:
          $ref: "#/components/schemas/ComputeNodeFillType"
          description: 'How tasks should be distributed across compute nodes. Required. Known values are: "Spread" and "Pack".'
      required:
        - nodeFillType
    JobDefaultOrder:
      type: string
      enum:
        - None
        - CreationTime
    ComputeNodeFillType:
      type: string
      enum:
        - Spread
        - Pack
    UserAccount:
      type: object
      description: Properties used to create a user on an Azure Batch node.
      properties:
        name:
          type: string
          description: The name of the user account. Names can contain any Unicode characters up to a maximum length of 20. Required.
        password:
          type: string
          description: The password for the user account. Required.
        elevationLevel:
          $ref: "#/components/schemas/ElevationLevel"
          description: 'The elevation level of the user. Known values are: "NonAdmin" and "Admin".'
        linuxUserConfiguration:
          $ref: "#/components/schemas/LinuxUserConfiguration"
          description: The Linux-specific user configuration for the user account.
        windowsUserConfiguration:
          $ref: "#/components/schemas/WindowsUserConfiguration"
          description: The Windows-specific user configuration for the user account.
      required:
        - name
        - password
    ElevationLevel:
      type: string
      enum:
        - NonAdmin
        - Admin
    LinuxUserConfiguration:
      type: object
      description: Properties used to create a user account on a Linux node.
      properties:
        uid:
          type: integer
          description: The user ID of the user account.
        gid:
          type: integer
          description: The group ID for the user account.
        sshPrivateKey:
          type: string
          description: The SSH private key for the user account.
    WindowsUserConfiguration:
      type: object
      description: Properties used to create a user account on a Windows node.
      properties:
        loginMode:
          $ref: "#/components/schemas/LoginMode"
          description: 'Login mode for user. Known values are: "Batch" and "Interactive".'
    LoginMode:
      type: string
      enum:
        - Batch
        - Interactive
    MetadataItem:
      type: object
      description: A name-value pair associated with a Batch service resource.
      properties:
        name:
          type: string
          description: The name of the metadata item. Required.
        value:
          type: string
          description: The value of the metadata item. Required.
      required:
        - name
        - value
    StartTask:
      type: object
      description: A task which is run when a compute node joins a pool in the Azure Batch service, or when the compute node is rebooted or reimaged.
      properties:
        commandLine:
          type: string
          description: The command line of the start task.
        resourceFiles:
          type: array
          items:
            $ref: "#/components/schemas/ResourceFile"
          description: A list of files that the Batch service will download to the compute node before running the command line.
        environmentSettings:
          type: array
          items:
            $ref: "#/components/schemas/EnvironmentSetting"
          description: A list of environment variable settings for the start task.
        userIdentity:
          $ref: "#/components/schemas/UserIdentity"
          description: The user identity under which the start task runs.
        maxTaskRetryCount:
          type: integer
          description: The maximum number of times the task may be retried.
        waitForSuccess:
          type: boolean
          description: Whether the Batch service should wait for the start task to complete successfully (that is, to exit with exit code 0) before scheduling any tasks on the compute node.
        containerSettings:
          $ref: "#/components/schemas/TaskContainerSettings"
          description: The settings for the container under which the start task runs.
    ResourceFile:
      type: object
      description: A single file or multiple files to be downloaded to a compute node.
      properties:
        autoStorageContainerName:
          type: string
          description: The storage container name in the auto storage account.
        storageContainerUrl:
          type: string
          description: The URL of the blob container within Azure Blob Storage.
        httpUrl:
          type: string
          description: The URL of the file to download.
        blobPrefix:
          type: string
          description: The blob prefix to use when downloading blobs from an Azure Storage container. Only the blobs whose names begin with the specified prefix will be downloaded.
        filePath:
          type: string
          description: The location on the compute node to which to download the file, relative to the task's working directory.
        fileMode:
          type: string
          description: The file permission mode attribute in octal format.
        identityReference:
          $ref: "#/components/schemas/ComputeNodeIdentityReference"
          description: The reference to the user assigned identity to use to access Azure Blob Storage specified by storageContainerUrl or httpUrl.
    EnvironmentSetting:
      type: object
      description: An environment variable to be set on a task process.
      properties:
        name:
          type: string
          description: The name of the environment variable. Required.
        value:
          type: string
          description: The value of the environment variable.
      required:
        - name
    UserIdentity:
      type: object
      description: The definition of the user identity under which the task is run.
      properties:
        userName:
          type: string
          description: The name of the user identity under which the task is run.
        autoUser:
          $ref: "#/components/schemas/AutoUserSpecification"
          description: The auto user under which the task is run.
    AutoUserSpecification:
      type: object
      description: Specifies the parameters for the auto user that runs a task on the Batch service.
      properties:
        scope:
          $ref: "#/components/schemas/AutoUserScope"
          description: 'The scope for the auto user. Known values are: "Task" and "Pool".'
        elevationLevel:
          $ref: "#/components/schemas/ElevationLevel"
          description: 'The elevation level of the user. Known values are: "NonAdmin" and "Admin".'
    AutoUserScope:
      type: string
      enum:
        - Task
        - Pool
    TaskContainerSettings:
      type: object
      description: The container settings for a task.
      properties:
        containerRunOptions:
          type: string
          description: Additional options to the container create command.
        imageName:
          type: string
          description: The image to use to create the container in which the task will run. Required.
        registry:
          $ref: "#/components/schemas/ContainerRegistry"
          description: The private registry which contains the container image.
        workingDirectory:
          $ref: "#/components/schemas/ContainerWorkingDirectory"
          description: "A flag to indicate where the container task working directory is. The default is 'taskWorkingDirectory'. Known values are: \"TaskWorkingDirectory\" and \"ContainerImageDefault\"."
        containerHostBatchBindMounts:
          type: array
          items:
            $ref: "#/components/schemas/ContainerHostBatchBindMountEntry"
          description: The paths you want to mounted to container task.
      required:
        - imageName
    ContainerWorkingDirectory:
      type: string
      enum:
        - TaskWorkingDirectory
        - ContainerImageDefault
    ContainerHostBatchBindMountEntry:
      type: object
      description: The entry of path and mount mode you want to mount into task container.
      properties:
        source:
          $ref: "#/components/schemas/ContainerHostDataPath"
          description: "The paths which will be mounted to container task's container. Known values are: \"Shared\", \"Startup\", \"VfsMounts\", \"Task\", \"JobPrep\", and \"Applications\"."
        isReadOnly:
          type: boolean
          description: Mount this source path as read-only mode or not. Default value is false (read/write mode).
    ContainerHostDataPath:
      type: string
      enum:
        - Shared
        - Startup
        - VfsMounts
        - Task
        - JobPrep
        - Applications
    ApplicationPackageReference:
      type: object
      description: Link to an application package inside the batch account.
      properties:
        id:
          type: string
          description: The ID of the application package to install. This must be inside the same Batch account as the pool. This can either be a reference to a specific version or the default version if one exists. Required.
        version:
          type: string
          description: The version of the application to deploy. If omitted, the default version is deployed.
      required:
        - id
    ResizeOperationStatus:
      type: object
      description: Details about the current or last completed resize operation.
      properties:
        targetDedicatedNodes:
          type: integer
          description: The desired number of dedicated compute nodes in the pool.
        targetLowPriorityNodes:
          type: integer
          description: The desired number of Spot/low-priority compute nodes in the pool.
        resizeTimeout:
          type: string
          description: The timeout for allocation of compute nodes to the pool or removal of compute nodes from the pool.
        nodeDeallocationOption:
          $ref: "#/components/schemas/ComputeNodeDeallocationOption"
          description: 'Determines what to do with a node and its running task(s) after it has been selected for deallocation. Known values are: "Requeue", "Terminate", "TaskCompletion", and "RetainedData".'
        startTime:
          type: string
          format: date-time
          description: The time when this resize operation was started.
        errors:
          type: array
          items:
            $ref: "#/components/schemas/ResizeError"
          description: Details of any errors encountered while performing the last resize on the pool.
    ResizeError:
      type: object
      description: An error that occurred when resizing a pool.
      properties:
        code:
          type: string
          description: An identifier for the error. Codes are invariant and are intended to be consumed programmatically. Required.
        message:
          type: string
          description: A message describing the error, intended to be suitable for display in a user interface. Required.
        details:
          type: array
          items:
            $ref: "#/components/schemas/ResizeError"
          description: Additional details about the error.
      required:
        - code
        - message
    MountConfiguration:
      type: object
      description: The file system to mount on each node.
      properties:
        azureBlobFileSystemConfiguration:
          $ref: "#/components/schemas/AzureBlobFileSystemConfiguration"
          description: The Azure Storage Container to mount using blob FUSE on each node.
        nfsMountConfiguration:
          $ref: "#/components/schemas/NFSMountConfiguration"
          description: The NFS file system to mount on each node.
        cifsMountConfiguration:
          $ref: "#/components/schemas/CIFSMountConfiguration"
          description: The CIFS/SMB file system to mount on each node.
        azureFileShareConfiguration:
          $ref: "#/components/schemas/AzureFileShareConfiguration"
          description: The Azure File Share to mount on each node.
    AzureBlobFileSystemConfiguration:
      type: object
      description: Information used to connect to an Azure Storage Container using Blobfuse.
      properties:
        accountName:
          type: string
          description: The Azure Storage Account name. Required.
        containerName:
          type: string
          description: The Azure Blob Storage Container name. Required.
        accountKey:
          type: string
          description: The Azure Storage Account key.
        sasKey:
          type: string
          description: The Azure Storage SAS token.
        blobfuseOptions:
          type: string
          description: Additional command line options to pass to the mount command.
        relativeMountPath:
          type: string
          description: The relative path on the compute node where the file system will be mounted. Required.
        identityReference:
          $ref: "#/components/schemas/ComputeNodeIdentityReference"
          description: The reference to the user assigned identity to use to access containerName.
      required:
        - accountName
        - containerName
        - relativeMountPath
    NFSMountConfiguration:
      type: object
      description: Information used to connect to an NFS file system.
      properties:
        source:
          type: string
          description: The URI of the file system to mount. Required.
        relativeMountPath:
          type: string
          description: The relative path on the compute node where the file system will be mounted. Required.
        mountOptions:
          type: string
          description: Additional command line options to pass to the mount command.
      required:
        - source
        - relativeMountPath
    CIFSMountConfiguration:
      type: object
      description: Information used to connect to a CIFS file system.
      properties:
        userName:
          type: string
          description: The user to use for authentication against the CIFS file system. Required.
        source:
          type: string
          description: The URI of the file system to mount. Required.
        relativeMountPath:
          type: string
          description: The relative path on the compute node where the file system will be mounted. Required.
        mountOptions:
          type: string
          description: Additional command line options to pass to the mount command.
        password:
          type: string
          description: The password to use for authentication against the CIFS file system. Required.
      required:
        - userName
        - source
        - relativeMountPath
        - password
    AzureFileShareConfiguration:
      type: object
      description: Information used to connect to an Azure Fileshare.
      properties:
        accountName:
          type: string
          description: The Azure Storage account name. Required.
        azureFileUrl:
          type: string
          description: The Azure Files URL. Required.
        accountKey:
          type: string
          description: The Azure Storage account key. Required.
        relativeMountPath:
          type: string
          description: The relative path on the compute node where the file system will be mounted. Required.
        mountOptions:
          type: string
          description: Additional command line options to pass to the mount command.
      required:
        - accountName
        - azureFileUrl
        - accountKey
        - relativeMountPath
    UpgradePolicy:
      type: object
      description: Describes an upgrade policy - automatic, manual, or rolling.
      properties:
        mode:
          $ref: "#/components/schemas/UpgradeMode"
          description: 'Specifies the mode of an upgrade to virtual machines in the scale set. Possible values are: **Manual** - You control the application of updates to virtual machines in the scale set. You do this by using the manualUpgrade action. **Automatic** - All virtual machines in the scale set are automatically updated at the same time. **Rolling** - Scale set performs updates in batches with an optional pause time in between. Required. Known values are: "automatic", "manual", and "rolling".'
        automaticOSUpgradePolicy:
          $ref: "#/components/schemas/AutomaticOSUpgradePolicy"
          description: Configuration parameters used for performing automatic OS Upgrade.
        rollingUpgradePolicy:
          $ref: "#/components/schemas/RollingUpgradePolicy"
          description: The configuration parameters used while performing a rolling upgrade.
      required:
        - mode
    UpgradeMode:
      type: string
      enum:
        - automatic
        - manual
        - rolling
    AutomaticOSUpgradePolicy:
      type: object
      description: The configuration parameters used for performing automatic OS upgrade.
      properties:
        disableAutomaticRollback:
          type: boolean
          description: Whether OS image rollback feature should be disabled.
        enableAutomaticOSUpgrade:
          type: boolean
          description: Indicates whether OS upgrades should automatically be applied to scale set instances in a rolling fashion when a newer version of the OS image becomes available. If this is set to true for Windows based pools, `WindowsConfiguration.enableAutomaticUpdates `_ cannot be set to true.
        useRollingUpgradePolicy:
          type: boolean
          description: Indicates whether rolling upgrade policy should be used during Auto OS Upgrade. Auto OS Upgrade will fallback to the default policy if no policy is defined on the VMSS.
        osRollingUpgradeDeferral:
          type: boolean
          description: Defer OS upgrades on the TVMs if they are running tasks.
    RollingUpgradePolicy:
      type: object
      description: The configuration parameters used while performing a rolling upgrade.
      properties:
        enableCrossZoneUpgrade:
          type: boolean
          description: Allow VMSS to ignore AZ boundaries when constructing upgrade batches. Take into consideration the Update Domain and maxBatchInstancePercent to determine the batch size. If this field is not set, Azure Azure Batch will not set its default value. The value of enableCrossZoneUpgrade on the created VirtualMachineScaleSet will be decided by the default configurations on VirtualMachineScaleSet. This field is able to be set to true or false only when using NodePlacementConfiguration as Zonal.
        maxBatchInstancePercent:
          type: integer
          description: The maximum percent of total virtual machine instances that will be upgraded simultaneously by the rolling upgrade in one batch. As this is a maximum, unhealthy instances in previous or future batches can cause the percentage of instances in a batch to decrease to ensure higher reliability. The value of this field should be between 5 and 100, inclusive. If both maxBatchInstancePercent and maxUnhealthyInstancePercent are assigned with value, the value of maxBatchInstancePercent should not be more than maxUnhealthyInstancePercent.
        maxUnhealthyInstancePercent:
          type: integer
          description: The maximum percentage of the total virtual machine instances in the scale set that can be simultaneously unhealthy, either as a result of being upgraded, or by being found in an unhealthy state by the virtual machine health checks before the rolling upgrade aborts. This constraint will be checked prior to starting any batch. The value of this field should be between 5 and 100, inclusive. If both maxBatchInstancePercent and maxUnhealthyInstancePercent are assigned with value, the value of maxBatchInstancePercent should not be more than maxUnhealthyInstancePercent.
        maxUnhealthyUpgradedInstancePercent:
          type: integer
          description: The maximum percentage of upgraded virtual machine instances that can be found to be in an unhealthy state. This check will happen after each batch is upgraded. If this percentage is ever exceeded, the rolling update aborts. The value of this field should be between 0 and 100, inclusive.
        pauseTimeBetweenBatches:
          type: string
          description: The wait time between completing the update for all virtual machines in one batch and starting the next batch. The time duration should be specified in ISO 8601 format.
        prioritizeUnhealthyInstances:
          type: boolean
          description: Upgrade all unhealthy instances in a scale set before any healthy instances.
        rollbackFailedInstancesOnPolicyBreach:
          type: boolean
          description: Rollback failed instances to previous model if the Rolling Upgrade policy is violated.
    BatchPoolIdentity:
      type: object
      description: The identity of the Batch pool, if configured. If the pool identity is updated during update an existing pool, only the new vms which are created after the pool shrinks to 0 will have the updated identities.
      properties:
        type:
          $ref: "#/components/schemas/PoolIdentityType"
          description: 'The type of identity used for the Batch Pool. Required. Known values are: "UserAssigned" and "None".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The list of user identities associated with the Batch pool.
      required:
        - type
    PoolIdentityType:
      type: string
      enum:
        - UserAssigned
        - None
    PoolFlattened:
      type: object
      description: Flattened view of Pool ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        identity:
          $ref: "#/components/schemas/BatchPoolIdentity"
          description: The type of identity used for the Batch Pool.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
        displayName:
          type: string
          description: The display name for the pool.
        lastModified:
          type: string
          format: date-time
          description: The last modified time of the pool.
        creationTime:
          type: string
          format: date-time
          description: The creation time of the pool.
        provisioningState:
          $ref: "#/components/schemas/PoolProvisioningState"
          description: 'The current state of the pool. Known values are: "Succeeded" and "Deleting".'
        provisioningStateTransitionTime:
          type: string
          format: date-time
          description: The time at which the pool entered its current state.
        allocationState:
          $ref: "#/components/schemas/AllocationState"
          description: 'Whether the pool is resizing. Known values are: "Steady", "Resizing", and "Stopping".'
        allocationStateTransitionTime:
          type: string
          format: date-time
          description: The time at which the pool entered its current allocation state.
        vmSize:
          type: string
          description: The size of virtual machines in the pool. All VMs in a pool are the same size.
        deploymentConfiguration:
          $ref: "#/components/schemas/DeploymentConfiguration"
          description: This property describes the virtual machines that the pool nodes will be deployed on.
        currentDedicatedNodes:
          type: integer
          description: The number of dedicated compute nodes currently in the pool.
        currentLowPriorityNodes:
          type: integer
          description: The number of Spot/low-priority compute nodes currently in the pool.
        scaleSettings:
          $ref: "#/components/schemas/ScaleSettings"
          description: Settings which configure the number of nodes in the pool.
        autoScaleRun:
          $ref: "#/components/schemas/AutoScaleRun"
          description: The results and errors from the last execution of the autoscale formula.
        interNodeCommunication:
          $ref: "#/components/schemas/InterNodeCommunicationState"
          description: 'Whether the pool permits direct communication between nodes. Known values are: "Enabled" and "Disabled".'
        networkConfiguration:
          $ref: "#/components/schemas/NetworkConfiguration"
          description: The network configuration for the pool.
        taskSlotsPerNode:
          type: integer
          description: The number of task slots that can be used to run concurrent tasks on a single compute node in the pool.
        taskSchedulingPolicy:
          $ref: "#/components/schemas/TaskSchedulingPolicy"
          description: How tasks are distributed across compute nodes in a pool.
        userAccounts:
          type: array
          items:
            $ref: "#/components/schemas/UserAccount"
          description: The list of user accounts to be created on each node in the pool.
        metadata:
          type: array
          items:
            $ref: "#/components/schemas/MetadataItem"
          description: A list of name-value pairs associated with the pool as metadata.
        startTask:
          $ref: "#/components/schemas/StartTask"
          description: A task specified to run on each compute node as it joins the pool.
        applicationPackages:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationPackageReference"
          description: The list of application packages to be installed on each compute node in the pool.
        resizeOperationStatus:
          $ref: "#/components/schemas/ResizeOperationStatus"
          description: Contains details about the current or last completed resize operation.
        mountConfiguration:
          type: array
          items:
            $ref: "#/components/schemas/MountConfiguration"
          description: A list of file systems to mount on each node in the pool.
        upgradePolicy:
          $ref: "#/components/schemas/UpgradePolicy"
          description: The upgrade policy for the pool.
    PagedPoolFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PoolFlattened"
        nextLink:
          type: string
    Pool:
      type: object
      description: Contains information about a pool.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/PoolProperties"
          description: The properties associated with the pool.
        identity:
          $ref: "#/components/schemas/BatchPoolIdentity"
          description: The type of identity used for the Batch Pool.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
    PrivateEndpointConnectionFlattened:
      type: object
      description: Flattened view of PrivateEndpointConnection ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
        provisioningState:
          $ref: "#/components/schemas/PrivateEndpointConnectionProvisioningState"
          description: 'The provisioning state of the private endpoint connection. Known values are: "Creating", "Updating", "Deleting", "Succeeded", "Failed", and "Cancelled".'
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The ARM resource identifier of the private endpoint.
        groupIds:
          type: array
          items:
            type: string
          description: The group id of the private endpoint connection.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: The private link service connection state of the private endpoint connection.
    PagedPrivateEndpointConnectionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
        nextLink:
          type: string
    PrivateLinkResourceProperties:
      type: object
      description: Private link resource properties.
      properties:
        groupId:
          type: string
          description: The group id of the private link resource.
        requiredMembers:
          type: array
          items:
            type: string
          description: The list of required members that are used to establish the private link connection.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The list of required zone names for the private DNS resource name.
    PrivateLinkResourceFlattened:
      type: object
      description: Flattened view of PrivateLinkResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        etag:
          type: string
          description: The ETag of the resource, used for concurrency statements.
        tags:
          type: object
          properties: {}
          description: The tags of the resource.
        groupId:
          type: string
          description: The group id of the private link resource.
        requiredMembers:
          type: array
          items:
            type: string
          description: The list of required members that are used to establish the private link connection.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The list of required zone names for the private DNS resource name.
    PagedPrivateLinkResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateLinkResourceFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    application:
      id: azure.batch.application
      name: application
      title: application
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}?api-version=2025-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}?api-version=2025-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}?api-version=2025-06-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/application/methods/get"
          - $ref: "#/components/x-stackQL-resources/application/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/application/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/application/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/application/methods/delete"
    application_package:
      id: azure.batch.application_package
      name: application_package
      title: application_package
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}~1versions~1{version_name}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}~1versions~1{version_name}?api-version=2025-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}~1versions~1{version_name}?api-version=2025-06-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}~1versions?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        activate:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1applications~1{application_name}~1versions~1{version_name}~1activate?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/application_package/methods/get"
          - $ref: "#/components/x-stackQL-resources/application_package/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/application_package/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/application_package/methods/delete"
    batch_account:
      id: azure.batch.batch_account
      name: batch_account
      title: batch_account
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}?api-version=2025-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}?api-version=2025-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}?api-version=2025-06-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Batch~1batchAccounts?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_outbound_network_dependencies_endpoints:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1outboundNetworkDependenciesEndpoints?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_detectors:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1detectors?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_keys:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1listKeys?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        get_detector:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1detectors~1{detector_id}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        synchronize_auto_storage_keys:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1syncAutoStorageKeys?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "200"
        regenerate_key:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1regenerateKeys?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/batch_account/methods/get_detector"
          - $ref: "#/components/x-stackQL-resources/batch_account/methods/get"
          - $ref: "#/components/x-stackQL-resources/batch_account/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/batch_account/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/batch_account/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/batch_account/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/batch_account/methods/delete"
    location:
      id: azure.batch.location
      name: location
      title: location
      methods:
        list_supported_virtual_machine_skus:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Batch~1locations~1{location_name}~1virtualMachineSkus?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
        get_quotas:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Batch~1locations~1{location_name}~1quotas?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Batch~1locations~1{location_name}~1checkNameAvailability?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/location/methods/list_supported_virtual_machine_skus"
        insert: []
        update: []
        delete: []
    network_security_perimeter:
      id: azure.batch.network_security_perimeter
      name: network_security_perimeter
      title: network_security_perimeter
      methods:
        list_configurations:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1networkSecurityPerimeterConfigurations?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_configuration:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1networkSecurityPerimeterConfigurations~1{network_security_perimeter_configuration_name}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        reconcile_configuration:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1networkSecurityPerimeterConfigurations~1{network_security_perimeter_configuration_name}~1reconcile?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "204"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/network_security_perimeter/methods/get_configuration"
          - $ref: "#/components/x-stackQL-resources/network_security_perimeter/methods/list_configurations"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.batch.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Batch~1operations?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    pool:
      id: azure.batch.pool
      name: pool
      title: pool
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1pools~1{pool_name}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1pools~1{pool_name}?api-version=2025-06-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1pools~1{pool_name}?api-version=2025-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1pools~1{pool_name}?api-version=2025-06-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_batch_account:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1pools?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        disable_auto_scale:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1pools~1{pool_name}~1disableAutoScale?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        stop_resize:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1pools~1{pool_name}~1stopResize?api-version=2025-06-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/pool/methods/get"
          - $ref: "#/components/x-stackQL-resources/pool/methods/list_by_batch_account"
        insert:
          - $ref: "#/components/x-stackQL-resources/pool/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/pool/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/pool/methods/delete"
    private_endpoint_connection:
      id: azure.batch.private_endpoint_connection
      name: private_endpoint_connection
      title: private_endpoint_connection
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2025-06-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2025-06-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_batch_account:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1privateEndpointConnections?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connection/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connection/methods/list_by_batch_account"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connection/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connection/methods/delete"
    private_link_resource:
      id: azure.batch.private_link_resource
      name: private_link_resource
      title: private_link_resource
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1privateLinkResources~1{private_link_resource_name}?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_batch_account:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Batch~1batchAccounts~1{account_name}~1privateLinkResources?api-version=2025-06-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_link_resource/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_link_resource/methods/list_by_batch_account"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/applications/{application_name}?api-version=2025-06-01:
    get:
      operationId: Application_get
      description: Gets information about the specified application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationFlattened"
    put:
      operationId: Application_create
      description: Adds an application to the specified Batch account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ApplicationProperties"
                  description: The properties associated with the Application.
                tags:
                  type: object
                  properties: {}
                  description: The tags of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Application"
    patch:
      operationId: Application_update
      description: Updates settings for the specified application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ApplicationProperties"
                  description: The properties associated with the Application.
                tags:
                  type: object
                  properties: {}
                  description: The tags of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Application"
    delete:
      operationId: Application_delete
      description: Deletes an application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/applications?api-version=2025-06-01:
    get:
      operationId: Application_list
      description: Lists all of the applications in the specified account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: maxresults
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of items to return in the response. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedApplicationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/applications/{application_name}/versions/{version_name}?api-version=2025-06-01:
    get:
      operationId: ApplicationPackage_get
      description: Gets information about the specified application package.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version of the application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationPackageFlattened"
    put:
      operationId: ApplicationPackage_create
      description: Creates an application package record. The record contains a storageUrl where the package should be uploaded to. Once it is uploaded the `ApplicationPackage` needs to be activated using `ApplicationPackageActive` before it can be used. If the auto storage account was configured to use storage keys, the URL returned will contain a SAS.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version of the application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ApplicationPackageProperties"
                  description: The properties associated with the Application Package.
                tags:
                  type: object
                  properties: {}
                  description: The tags of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationPackage"
    delete:
      operationId: ApplicationPackage_delete
      description: Deletes an application package record and its associated binary file.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version of the application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/applications/{application_name}/versions?api-version=2025-06-01:
    get:
      operationId: ApplicationPackage_list
      description: Lists all of the application packages in the specified application.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: maxresults
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of items to return in the response. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedApplicationPackageFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/applications/{application_name}/versions/{version_name}/activate?api-version=2025-06-01:
    post:
      operationId: ApplicationPackage_activate
      description: Activates the specified application package. This should be done after the `ApplicationPackage` was created and uploaded. This needs to be done before an `ApplicationPackage` can be used on Pools or Tasks.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: application_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the application. This must be unique within the account. Required.
        - name: version_name
          in: path
          required: true
          schema:
            type: string
          description: The version of the application. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                format:
                  type: string
                  description: The format of the application package binary file. Required.
              required:
                - format
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationPackage"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}?api-version=2025-06-01:
    get:
      operationId: BatchAccount_get
      description: Gets information about the specified Batch account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchAccountFlattened"
    put:
      operationId: BatchAccount_begin_create
      description: Creates a new Batch account with the specified parameters. Existing accounts cannot be updated with this API and should instead be updated with the Update Batch Account API.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: The region in which to create the account. Required.
                tags:
                  type: object
                  properties: {}
                  description: The user-specified tags associated with the account.
                properties:
                  $ref: "#/components/schemas/BatchAccountCreateProperties"
                  description: The properties of the Batch account.
                identity:
                  $ref: "#/components/schemas/BatchAccountIdentity"
                  description: The identity of the Batch account.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchAccount"
    patch:
      operationId: BatchAccount_update
      description: Updates the properties of an existing Batch account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: The user-specified tags associated with the account.
                properties:
                  $ref: "#/components/schemas/BatchAccountUpdateProperties"
                  description: The properties of the account.
                identity:
                  $ref: "#/components/schemas/BatchAccountIdentity"
                  description: The identity of the Batch account.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchAccount"
    delete:
      operationId: BatchAccount_begin_delete
      description: Deletes the specified Batch account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Batch/batchAccounts?api-version=2025-06-01:
    get:
      operationId: BatchAccount_list
      description: Gets information about the Batch accounts associated with the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedBatchAccountFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts?api-version=2025-06-01:
    get:
      operationId: BatchAccount_list_by_resource_group
      description: Gets information about the Batch accounts associated with the specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedBatchAccountFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/outboundNetworkDependenciesEndpoints?api-version=2025-06-01:
    get:
      operationId: BatchAccount_list_outbound_network_dependencies_endpoints
      description: Lists the endpoints that a Batch Compute Node under this Batch Account may call as part of Batch service administration. If you are deploying a Pool inside of a virtual network that you specify, you must make sure your network allows outbound access to these endpoints. Failure to allow access to these endpoints may cause Batch to mark the affected nodes as unusable. For more information about creating a pool inside of a virtual network, see `https://learn.microsoft.com/azure/batch/batch-virtual-network `_.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOutboundEnvironmentEndpoint"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/detectors?api-version=2025-06-01:
    get:
      operationId: BatchAccount_list_detectors
      description: Gets information about the detectors available for a given Batch account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDetectorResponseFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/listKeys?api-version=2025-06-01:
    post:
      operationId: BatchAccount_get_keys
      description: Gets the account keys for the specified Batch account. This operation applies only to Batch accounts with allowedAuthenticationModes containing 'SharedKey'. If the Batch account doesn't contain 'SharedKey' in its allowedAuthenticationMode, clients cannot use shared keys to authenticate, and must use another allowedAuthenticationModes instead. In this case, getting the keys will fail.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchAccountKeys"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/detectors/{detector_id}?api-version=2025-06-01:
    get:
      operationId: BatchAccount_get_detector
      description: Gets information about the given detector for a given Batch account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: detector_id
          in: path
          required: true
          schema:
            type: string
          description: The name of the detector. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DetectorResponseFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/syncAutoStorageKeys?api-version=2025-06-01:
    post:
      operationId: BatchAccount_synchronize_auto_storage_keys
      description: Synchronizes access keys for the auto-storage account configured for the specified Batch account, only if storage key authentication is being used.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/regenerateKeys?api-version=2025-06-01:
    post:
      operationId: BatchAccount_regenerate_key
      description: Regenerates the specified account key for the Batch account. This operation applies only to Batch accounts with allowedAuthenticationModes containing 'SharedKey'. If the Batch account doesn't contain 'SharedKey' in its allowedAuthenticationMode, clients cannot use shared keys to authenticate, and must use another allowedAuthenticationModes instead. In this case, regenerating the keys will fail.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                keyName:
                  $ref: "#/components/schemas/AccountKeyType"
                  description: 'The type of account key to regenerate. Required. Known values are: "Primary" and "Secondary".'
              required:
                - keyName
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchAccountKeys"
  /subscriptions/{subscription_id}/providers/Microsoft.Batch/locations/{location_name}/virtualMachineSkus?api-version=2025-06-01:
    get:
      operationId: Location_list_supported_virtual_machine_skus
      description: Gets the list of Batch supported Virtual Machine VM sizes available at the given location.
      parameters:
        - name: location_name
          in: path
          required: true
          schema:
            type: string
          description: The region for which to retrieve Batch service supported SKUs. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: maxresults
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of items to return in the response. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: OData filter expression. Valid properties for filtering are "familyName". Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSupportedSku"
  /subscriptions/{subscription_id}/providers/Microsoft.Batch/locations/{location_name}/quotas?api-version=2025-06-01:
    get:
      operationId: Location_get_quotas
      description: Gets the Batch service quotas for the specified subscription at the given location.
      parameters:
        - name: location_name
          in: path
          required: true
          schema:
            type: string
          description: The region for which to retrieve Batch service quotas. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchLocationQuota"
  /subscriptions/{subscription_id}/providers/Microsoft.Batch/locations/{location_name}/checkNameAvailability?api-version=2025-06-01:
    post:
      operationId: Location_check_name_availability
      description: Checks whether the Batch account name is available in the specified region.
      parameters:
        - name: location_name
          in: path
          required: true
          schema:
            type: string
          description: The desired region for the name check. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The name to check for availability. Required.
                type:
                  $ref: "#/components/schemas/ResourceType"
                  description: The resource type. Required. "Microsoft.Batch/batchAccounts"
              required:
                - name
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckNameAvailabilityResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/networkSecurityPerimeterConfigurations?api-version=2025-06-01:
    get:
      operationId: NetworkSecurityPerimeter_list_configurations
      description: Lists all of the NSP configurations in the specified account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedNetworkSecurityPerimeterConfigurationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/networkSecurityPerimeterConfigurations/{network_security_perimeter_configuration_name}?api-version=2025-06-01:
    get:
      operationId: NetworkSecurityPerimeter_get_configuration
      description: Gets information about the specified NSP configuration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: network_security_perimeter_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name for a network security perimeter configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkSecurityPerimeterConfigurationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/networkSecurityPerimeterConfigurations/{network_security_perimeter_configuration_name}/reconcile?api-version=2025-06-01:
    post:
      operationId: NetworkSecurityPerimeter_begin_reconcile_configuration
      description: Reconciles the specified NSP configuration.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: network_security_perimeter_configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name for a network security perimeter configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Batch/operations?api-version=2025-06-01:
    get:
      operationId: batch_list
      description: Lists available operations for the Microsoft.Batch provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/pools/{pool_name}?api-version=2025-06-01:
    get:
      operationId: Pool_get
      description: Gets information about the specified pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: The pool name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PoolFlattened"
    put:
      operationId: Pool_create
      description: Creates a new pool inside the specified account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: The pool name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PoolProperties"
                  description: The properties associated with the pool.
                identity:
                  $ref: "#/components/schemas/BatchPoolIdentity"
                  description: The type of identity used for the Batch Pool.
                tags:
                  type: object
                  properties: {}
                  description: The tags of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pool"
    patch:
      operationId: Pool_update
      description: Updates the properties of an existing pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: The pool name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PoolProperties"
                  description: The properties associated with the pool.
                identity:
                  $ref: "#/components/schemas/BatchPoolIdentity"
                  description: The type of identity used for the Batch Pool.
                tags:
                  type: object
                  properties: {}
                  description: The tags of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pool"
    delete:
      operationId: Pool_begin_delete
      description: Deletes the specified pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: The pool name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/pools?api-version=2025-06-01:
    get:
      operationId: Pool_list_by_batch_account
      description: Lists all of the pools in the specified account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: maxresults
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of items to return in the response. Default value is None.
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: Comma separated list of properties that should be returned. e.g. "properties/provisioningState". Only top level properties under properties/ are valid for selection. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: "OData filter expression. Valid properties for filtering are: name properties/allocationState properties/allocationStateTransitionTime properties/creationTime properties/provisioningState properties/provisioningStateTransitionTime properties/lastModified properties/vmSize properties/interNodeCommunication properties/scaleSettings/autoScale properties/scaleSettings/fixedScale. Default value is None."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPoolFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/pools/{pool_name}/disableAutoScale?api-version=2025-06-01:
    post:
      operationId: Pool_disable_auto_scale
      description: Disables automatic scaling for a pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: The pool name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pool"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/pools/{pool_name}/stopResize?api-version=2025-06-01:
    post:
      operationId: Pool_stop_resize
      description: "Stops an ongoing resize operation on the pool. This does not restore the pool to its previous state before the resize operation: it only stops any further changes being made, and the pool maintains its current state. After stopping, the pool stabilizes at the number of nodes it was at when the stop operation was done. During the stop operation, the pool allocation state changes first to stopping and then to steady. A resize operation need not be an explicit resize pool request; this API can also be used to halt the initial sizing of the pool when it is created."
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: The pool name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pool"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/privateEndpointConnections/{private_endpoint_connection_name}?api-version=2025-06-01:
    get:
      operationId: PrivateEndpointConnection_get
      description: Gets information about the specified private endpoint connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The private endpoint connection name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
    patch:
      operationId: PrivateEndpointConnection_begin_update
      description: Updates the properties of an existing private endpoint connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The private endpoint connection name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PrivateEndpointConnectionProperties"
                  description: The properties associated with the private endpoint connection.
                tags:
                  type: object
                  properties: {}
                  description: The tags of the resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnection"
    delete:
      operationId: PrivateEndpointConnection_begin_delete
      description: Deletes the specified private endpoint connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The private endpoint connection name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/privateEndpointConnections?api-version=2025-06-01:
    get:
      operationId: PrivateEndpointConnection_list_by_batch_account
      description: Lists all of the private endpoint connections in the specified account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: maxresults
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of items to return in the response. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateEndpointConnectionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/privateLinkResources/{private_link_resource_name}?api-version=2025-06-01:
    get:
      operationId: PrivateLinkResource_get
      description: Gets information about the specified private link resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: private_link_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The private link resource name. This must be unique within the account. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateLinkResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Batch/batchAccounts/{account_name}/privateLinkResources?api-version=2025-06-01:
    get:
      operationId: PrivateLinkResource_list_by_batch_account
      description: Lists all of the private link resources in the specified account.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: account_name
          in: path
          required: true
          schema:
            type: string
          description: "A name for the Batch account which must be unique within the region. Batch account names must be between 3 and 24 characters in length and must use only numbers and lowercase letters. This name is used as part of the DNS name that is used to access the Batch service in the region in which the account is created. For example: `http://accountname.region.batch.azure.com/ `_. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: maxresults
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of items to return in the response. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateLinkResourceFlattened"
