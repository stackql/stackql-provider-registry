openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Subscription Client
  description: SubscriptionClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-11-01-preview-stackql-generated
  x-serviceAlias: subscription
  x-sdkPackage: azure-mgmt-subscription
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    SubscriptionAliasResponseProperties:
      type: object
      description: Put subscription creation result properties.
      properties:
        subscriptionId:
          type: string
          description: Newly created subscription Id.
        displayName:
          type: string
          description: The display name of the subscription.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the resource. Known values are: "Accepted", "Succeeded", and "Failed".'
        acceptOwnershipUrl:
          type: string
          description: Url to accept ownership of the subscription.
        acceptOwnershipState:
          $ref: "#/components/schemas/AcceptOwnership"
          description: 'The accept ownership state of the resource. Known values are: "Pending", "Completed", and "Expired".'
        billingScope:
          type: string
          description: Billing scope of the subscription. For CustomerLed and FieldLed - /billingAccounts/{billingAccountName}/billingProfiles/{billingProfileName}/invoiceSections/{invoiceSectionName} For PartnerLed - /billingAccounts/{billingAccountName}/customers/{customerName} For Legacy EA - /billingAccounts/{billingAccountName}/enrollmentAccounts/{enrollmentAccountName}.
        workload:
          $ref: "#/components/schemas/Workload"
          description: 'The workload type of the subscription. It can be either Production or DevTest. Known values are: "Production" and "DevTest".'
        resellerId:
          type: string
          description: Reseller Id.
        subscriptionOwnerId:
          type: string
          description: Owner Id of the subscription.
        managementGroupId:
          type: string
          description: The Management Group Id.
        createdTime:
          type: string
          description: Created Time.
        tags:
          type: object
          properties: {}
          description: Tags for the subscription.
    ProvisioningState:
      type: string
      enum:
        - Accepted
        - Succeeded
        - Failed
    AcceptOwnership:
      type: string
      enum:
        - Pending
        - Completed
        - Expired
    Workload:
      type: string
      enum:
        - Production
        - DevTest
    SubscriptionAliasResponseFlattened:
      type: object
      description: Flattened view of SubscriptionAliasResponse ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        subscriptionId:
          type: string
          description: Newly created subscription Id.
        displayName:
          type: string
          description: The display name of the subscription.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The provisioning state of the resource. Known values are: "Accepted", "Succeeded", and "Failed".'
        acceptOwnershipUrl:
          type: string
          description: Url to accept ownership of the subscription.
        acceptOwnershipState:
          $ref: "#/components/schemas/AcceptOwnership"
          description: 'The accept ownership state of the resource. Known values are: "Pending", "Completed", and "Expired".'
        billingScope:
          type: string
          description: Billing scope of the subscription. For CustomerLed and FieldLed - /billingAccounts/{billingAccountName}/billingProfiles/{billingProfileName}/invoiceSections/{invoiceSectionName} For PartnerLed - /billingAccounts/{billingAccountName}/customers/{customerName} For Legacy EA - /billingAccounts/{billingAccountName}/enrollmentAccounts/{enrollmentAccountName}.
        workload:
          $ref: "#/components/schemas/Workload"
          description: 'The workload type of the subscription. It can be either Production or DevTest. Known values are: "Production" and "DevTest".'
        resellerId:
          type: string
          description: Reseller Id.
        subscriptionOwnerId:
          type: string
          description: Owner Id of the subscription.
        managementGroupId:
          type: string
          description: The Management Group Id.
        createdTime:
          type: string
          description: Created Time.
        tags:
          type: object
          properties: {}
          description: Tags for the subscription.
    PagedSubscriptionAliasResponseFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SubscriptionAliasResponseFlattened"
        nextLink:
          type: string
    PutAliasRequestProperties:
      type: object
      description: Put subscription properties.
      properties:
        displayName:
          type: string
          description: The friendly name of the subscription.
        workload:
          $ref: "#/components/schemas/Workload"
          description: 'The workload type of the subscription. It can be either Production or DevTest. Known values are: "Production" and "DevTest".'
        billingScope:
          type: string
          description: Billing scope of the subscription. For CustomerLed and FieldLed - /billingAccounts/{billingAccountName}/billingProfiles/{billingProfileName}/invoiceSections/{invoiceSectionName} For PartnerLed - /billingAccounts/{billingAccountName}/customers/{customerName} For Legacy EA - /billingAccounts/{billingAccountName}/enrollmentAccounts/{enrollmentAccountName}.
        subscriptionId:
          type: string
          description: This parameter can be used to create alias for existing subscription Id.
        resellerId:
          type: string
          description: Reseller Id.
        additionalProperties:
          $ref: "#/components/schemas/PutAliasRequestAdditionalProperties"
          description: Put alias request additional properties.
    PutAliasRequestAdditionalProperties:
      type: object
      description: Put subscription additional properties.
      properties:
        managementGroupId:
          type: string
          description: Management group Id for the subscription.
        subscriptionTenantId:
          type: string
          description: Tenant Id of the subscription.
        subscriptionOwnerId:
          type: string
          description: Owner Id of the subscription.
        tags:
          type: object
          properties: {}
          description: Tags for the subscription.
    SubscriptionAliasResponse:
      type: object
      description: Subscription Information with the alias.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/SubscriptionAliasResponseProperties"
          description: Subscription Alias response properties.
    BillingAccountPoliciesResponseProperties:
      type: object
      description: Put billing account policies response properties.
      properties:
        serviceTenants:
          type: array
          items:
            $ref: "#/components/schemas/ServiceTenantResponse"
          description: Service tenant for the billing account.
        allowTransfers:
          type: boolean
          description: Determine if the transfers are allowed for the billing account.
    ServiceTenantResponse:
      type: object
      description: Billing account service tenant.
      properties:
        tenantId:
          type: string
          description: Service tenant id.
        tenantName:
          type: string
          description: Service tenant name.
    BillingAccountPoliciesResponseFlattened:
      type: object
      description: Flattened view of BillingAccountPoliciesResponse ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        serviceTenants:
          type: array
          items:
            $ref: "#/components/schemas/ServiceTenantResponse"
          description: Service tenant for the billing account.
        allowTransfers:
          type: boolean
          description: Determine if the transfers are allowed for the billing account.
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    SubscriptionCreationResult:
      type: object
      description: The created subscription object.
      properties:
        subscriptionLink:
          type: string
          description: The link to the new subscription. Use this link to check the status of subscription creation operation.
    CanceledSubscriptionId:
      type: object
      description: The ID of the canceled subscription.
      properties:
        subscriptionId:
          type: string
          description: The ID of the canceled subscription.
    RenamedSubscriptionId:
      type: object
      description: The ID of the subscriptions that is being renamed.
      properties:
        subscriptionId:
          type: string
          description: The ID of the subscriptions that is being renamed.
    EnabledSubscriptionId:
      type: object
      description: The ID of the subscriptions that is being enabled.
      properties:
        subscriptionId:
          type: string
          description: The ID of the subscriptions that is being enabled.
    AcceptOwnershipRequestProperties:
      type: object
      description: Accept subscription ownership request properties.
      properties:
        displayName:
          type: string
          description: The friendly name of the subscription. Required.
        managementGroupId:
          type: string
          description: Management group Id for the subscription.
        tags:
          type: object
          properties: {}
          description: Tags for the subscription.
      required:
        - displayName
    AcceptOwnershipStatusResponse:
      type: object
      description: Subscription Accept Ownership Response.
      properties:
        subscriptionId:
          type: string
          description: Newly created subscription Id.
        acceptOwnershipState:
          $ref: "#/components/schemas/AcceptOwnership"
          description: 'The accept ownership state of the resource. Known values are: "Pending", "Completed", and "Expired".'
        provisioningState:
          $ref: "#/components/schemas/Provisioning"
          description: 'The provisioning state of the resource. Known values are: "Pending", "Accepted", and "Succeeded".'
        billingOwner:
          type: string
          description: UPN of the billing owner.
        subscriptionTenantId:
          type: string
          description: Tenant Id of the subscription.
        displayName:
          type: string
          description: The display name of the subscription.
        tags:
          type: object
          properties: {}
          description: Tags for the subscription.
    Provisioning:
      type: string
      enum:
        - Pending
        - Accepted
        - Succeeded
    TenantPolicy:
      type: object
      description: Tenant policy.
      properties:
        policyId:
          type: string
          description: Policy Id.
        blockSubscriptionsLeavingTenant:
          type: boolean
          description: Blocks the leaving of subscriptions from user's tenant.
        blockSubscriptionsIntoTenant:
          type: boolean
          description: Blocks the entering of subscriptions into user's tenant.
        exemptedPrincipals:
          type: array
          items:
            type: string
          description: List of user objectIds that are exempted from the set subscription tenant policies for the user's tenant.
    GetTenantPolicyResponseFlattened:
      type: object
      description: Flattened view of GetTenantPolicyResponse ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        policyId:
          type: string
          description: Policy Id.
        blockSubscriptionsLeavingTenant:
          type: boolean
          description: Blocks the leaving of subscriptions from user's tenant.
        blockSubscriptionsIntoTenant:
          type: boolean
          description: Blocks the entering of subscriptions into user's tenant.
        exemptedPrincipals:
          type: array
          items:
            type: string
          description: List of user objectIds that are exempted from the set subscription tenant policies for the user's tenant.
    PagedGetTenantPolicyResponseFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/GetTenantPolicyResponseFlattened"
        nextLink:
          type: string
    GetTenantPolicyResponse:
      type: object
      description: Tenant policy Information.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/TenantPolicy"
          description: Tenant policy properties.
    TargetDirectoryResultProperties:
      type: object
      description: Properties of subscription Response for Changed Target Directory.
      properties:
        destinationTenantId:
          type: string
          description: Destination Tenant Id where Subscription will be accepted.
        destinationOwnerId:
          type: string
          description: Destination Owner Id where Subscription will be accepted.
        subscriptionId:
          type: string
          description: The id of the subscription being transferred.
        createdDate:
          type: string
          format: date-time
          description: The UTC date and time when the transfer request was created.
        acceptedDate:
          type: string
          format: date-time
          description: The UTC date and time when the transfer request was accepted.
        sourceOwnerEmail:
          type: string
          description: The email address of the user who initiated the transfer request. If the request was generated by a Service Principal, this field may be null.
        sourceOwnerId:
          type: string
          description: The object id of the user who initiated the transfer request.
        sourceTenantId:
          type: string
          description: The id of the tenant where the subscription originally resided.
        status:
          $ref: "#/components/schemas/ChangeDirectoryOperationStatus"
          description: 'Status of the subscription transfer operation. Known values are: "Initialized", "InProgress", and "Completed".'
        expiresOn:
          type: string
          format: date-time
          description: Subscription Initiate Request Expiry time.
    ChangeDirectoryOperationStatus:
      type: string
      enum:
        - Initialized
        - InProgress
        - Completed
    TargetDirectoryResultFlattened:
      type: object
      description: Flattened view of TargetDirectoryResult ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        destinationTenantId:
          type: string
          description: Destination Tenant Id where Subscription will be accepted.
        destinationOwnerId:
          type: string
          description: Destination Owner Id where Subscription will be accepted.
        subscriptionId:
          type: string
          description: The id of the subscription being transferred.
        createdDate:
          type: string
          format: date-time
          description: The UTC date and time when the transfer request was created.
        acceptedDate:
          type: string
          format: date-time
          description: The UTC date and time when the transfer request was accepted.
        sourceOwnerEmail:
          type: string
          description: The email address of the user who initiated the transfer request. If the request was generated by a Service Principal, this field may be null.
        sourceOwnerId:
          type: string
          description: The object id of the user who initiated the transfer request.
        sourceTenantId:
          type: string
          description: The id of the tenant where the subscription originally resided.
        status:
          $ref: "#/components/schemas/ChangeDirectoryOperationStatus"
          description: 'Status of the subscription transfer operation. Known values are: "Initialized", "InProgress", and "Completed".'
        expiresOn:
          type: string
          format: date-time
          description: Subscription Initiate Request Expiry time.
    PagedTargetDirectoryResultFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/TargetDirectoryResultFlattened"
        nextLink:
          type: string
    TargetDirectoryRequestProperties:
      type: object
      description: Properties of subscription Request for Changed Target Directory.
      properties:
        destinationOwnerId:
          type: string
          description: The destination OwnerId, can be object id or email address.
        destinationTenantId:
          type: string
          description: The destination Tenant id where subscription needs to be accepted.
    TargetDirectoryResult:
      type: object
      description: Subscription Response for Changed Target Directory.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/TargetDirectoryResultProperties"
          description: Subscription Changed Target Directory response properties.
  x-stackQL-resources:
    alias:
      id: azure.subscription.alias
      name: alias
      title: alias
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1aliases~1{alias_name}?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1aliases~1{alias_name}?api-version=2025-11-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1aliases~1{alias_name}?api-version=2025-11-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1aliases?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/alias/methods/get"
          - $ref: "#/components/x-stackQL-resources/alias/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/alias/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/alias/methods/delete"
    billing_account:
      id: azure.subscription.billing_account
      name: billing_account
      title: billing_account
      methods:
        get_policy:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Billing~1billingAccounts~1{billing_account_id}~1providers~1Microsoft.Subscription~1policies~1default?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/billing_account/methods/get_policy"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.subscription.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1operations?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    subscription_operation:
      id: azure.subscription.subscription_operation
      name: subscription_operation
      title: subscription_operation
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1subscriptionOperations~1{operation_id}?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/subscription_operation/methods/get"
        insert: []
        update: []
        delete: []
    subscription:
      id: azure.subscription.subscription
      name: subscription
      title: subscription
      methods:
        cancel:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Subscription~1cancel?api-version=2025-11-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        rename:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Subscription~1rename?api-version=2025-11-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        enable:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Subscription~1enable?api-version=2025-11-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        accept_ownership:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1subscriptions~1{subscription_id}~1acceptOwnership?api-version=2025-11-01-preview/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        accept_ownership_status:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1subscriptions~1{subscription_id}~1acceptOwnershipStatus?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
    subscription_policy:
      id: azure.subscription.subscription_policy
      name: subscription_policy
      title: subscription_policy
      methods:
        list_policy_for_tenant:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1policies?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_policy_for_tenant:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1policies~1default?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        add_update_policy_for_tenant:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1policies~1default?api-version=2025-11-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/subscription_policy/methods/list_policy_for_tenant"
        insert: []
        update: []
        delete: []
    subscriptions:
      id: azure.subscription.subscriptions
      name: subscriptions
      title: subscriptions
      methods:
        list_target_directory:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Subscription~1changeTenantRequest?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_target_directory:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Subscription~1changeTenantRequest~1default?api-version=2025-11-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        put_target_directory:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Subscription~1changeTenantRequest~1default?api-version=2025-11-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_target_directory:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Subscription~1changeTenantRequest~1default?api-version=2025-11-01-preview/delete"
          response:
            openAPIDocKey: "204"
        accept_target_directory:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1subscriptions~1{subscription_id}~1acceptChangeTenant?api-version=2025-11-01-preview/post"
          response:
            openAPIDocKey: "200"
        target_directory_status:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Subscription~1subscriptions~1{subscription_id}~1changeTenantStatus?api-version=2025-11-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/subscriptions/methods/list_target_directory"
        insert: []
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/subscriptions/methods/delete_target_directory"
paths:
  /providers/Microsoft.Subscription/aliases/{alias_name}?api-version=2025-11-01-preview:
    get:
      operationId: Alias_get
      description: Get Alias Subscription.
      parameters:
        - name: alias_name
          in: path
          required: true
          schema:
            type: string
          description: AliasName is the name for the subscription creation request. Note that this is not the same as subscription name and this doesn’t have any other lifecycle need beyond the request for subscription creation. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionAliasResponseFlattened"
    put:
      operationId: Alias_begin_create
      description: Create Alias Subscription.
      parameters:
        - name: alias_name
          in: path
          required: true
          schema:
            type: string
          description: AliasName is the name for the subscription creation request. Note that this is not the same as subscription name and this doesn’t have any other lifecycle need beyond the request for subscription creation. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PutAliasRequestProperties"
                  description: Put alias request properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionAliasResponse"
    delete:
      operationId: Alias_delete
      description: Delete Alias.
      parameters:
        - name: alias_name
          in: path
          required: true
          schema:
            type: string
          description: AliasName is the name for the subscription creation request. Note that this is not the same as subscription name and this doesn’t have any other lifecycle need beyond the request for subscription creation. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Subscription/aliases?api-version=2025-11-01-preview:
    get:
      operationId: Alias_list
      description: List Alias Subscription.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSubscriptionAliasResponseFlattened"
  /providers/Microsoft.Billing/billingAccounts/{billing_account_id}/providers/Microsoft.Subscription/policies/default?api-version=2025-11-01-preview:
    get:
      operationId: BillingAccount_get_policy
      description: Get Billing Account Policy.
      parameters:
        - name: billing_account_id
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingAccountPoliciesResponseFlattened"
  /providers/Microsoft.Subscription/operations?api-version=2025-11-01-preview:
    get:
      operationId: subscription_list
      description: Lists all of the available Microsoft.Subscription API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /providers/Microsoft.Subscription/subscriptionOperations/{operation_id}?api-version=2025-11-01-preview:
    get:
      operationId: SubscriptionOperation_get
      description: Get the status of the pending Microsoft.Subscription API operations.
      parameters:
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: The operation ID, which can be found from the Location field in the generate recommendation response header. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionCreationResult"
  /subscriptions/{subscription_id}/providers/Microsoft.Subscription/cancel?api-version=2025-11-01-preview:
    post:
      operationId: Subscription_cancel
      description: The operation to cancel a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. The value must be an UUID. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CanceledSubscriptionId"
  /subscriptions/{subscription_id}/providers/Microsoft.Subscription/rename?api-version=2025-11-01-preview:
    post:
      operationId: Subscription_rename
      description: The operation to rename a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. The value must be an UUID. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subscriptionName:
                  type: string
                  description: New subscription name.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RenamedSubscriptionId"
  /subscriptions/{subscription_id}/providers/Microsoft.Subscription/enable?api-version=2025-11-01-preview:
    post:
      operationId: Subscription_enable
      description: The operation to enable a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. The value must be an UUID. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnabledSubscriptionId"
  /providers/Microsoft.Subscription/subscriptions/{subscription_id}/acceptOwnership?api-version=2025-11-01-preview:
    post:
      operationId: Subscription_begin_accept_ownership
      description: Accept subscription ownership.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: Subscription Id. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/AcceptOwnershipRequestProperties"
                  description: Accept subscription ownership request properties.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Subscription/subscriptions/{subscription_id}/acceptOwnershipStatus?api-version=2025-11-01-preview:
    get:
      operationId: Subscription_accept_ownership_status
      description: Accept subscription ownership status.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: Subscription Id. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcceptOwnershipStatusResponse"
  /providers/Microsoft.Subscription/policies?api-version=2025-11-01-preview:
    get:
      operationId: SubscriptionPolicy_list_policy_for_tenant
      description: Get the subscription tenant policy for the user's tenant.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGetTenantPolicyResponseFlattened"
  /providers/Microsoft.Subscription/policies/default?api-version=2025-11-01-preview:
    get:
      operationId: SubscriptionPolicy_get_policy_for_tenant
      description: Get the subscription tenant policy for the user's tenant.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetTenantPolicyResponseFlattened"
    put:
      operationId: SubscriptionPolicy_add_update_policy_for_tenant
      description: Create or Update Subscription tenant policy for user's tenant.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                blockSubscriptionsLeavingTenant:
                  type: boolean
                  description: Blocks the leaving of subscriptions from user's tenant.
                blockSubscriptionsIntoTenant:
                  type: boolean
                  description: Blocks the entering of subscriptions into user's tenant.
                exemptedPrincipals:
                  type: array
                  items:
                    type: string
                  description: List of user objectIds that are exempted from the set subscription tenant policies for the user's tenant.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetTenantPolicyResponse"
  /subscriptions/{subscription_id}/providers/Microsoft.Subscription/changeTenantRequest?api-version=2025-11-01-preview:
    get:
      operationId: Subscriptions_list_target_directory
      description: The operation to list Initiator Subscription Changed Request.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. The value must be an UUID. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedTargetDirectoryResultFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Subscription/changeTenantRequest/default?api-version=2025-11-01-preview:
    get:
      operationId: Subscriptions_get_target_directory
      description: The operation to view Initiator Subscription Changed Request.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. The value must be an UUID. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TargetDirectoryResultFlattened"
    put:
      operationId: Subscriptions_put_target_directory
      description: The operation to initiate Subscription Changed Request.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. The value must be an UUID. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/TargetDirectoryRequestProperties"
                  description: Target Directory request properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TargetDirectoryResult"
    delete:
      operationId: Subscriptions_delete_target_directory
      description: The operation to delete Initiator Subscription Changed Request.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. The value must be an UUID. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Subscription/subscriptions/{subscription_id}/acceptChangeTenant?api-version=2025-11-01-preview:
    post:
      operationId: Subscriptions_accept_target_directory
      description: The operation to accept Subscription Changed Request.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: Subscription Id. Required.
      responses:
        "200":
          description: Success
  /providers/Microsoft.Subscription/subscriptions/{subscription_id}/changeTenantStatus?api-version=2025-11-01-preview:
    post:
      operationId: Subscriptions_target_directory_status
      description: The operation for Acceptor to view the accepted request.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: Subscription Id. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TargetDirectoryResultProperties"
