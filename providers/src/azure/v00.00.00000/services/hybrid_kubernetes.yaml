openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Arc-enabled Kubernetes
  description: Connected Kubernetes clusters registered with Azure Arc (Microsoft.Kubernetes).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2026-05-01-stackql-generated
  x-serviceAlias: hybrid_kubernetes
  x-sdkPackage: azure-mgmt-hybridkubernetes
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    ConnectedClusterProperties:
      type: object
      description: Properties of the connected cluster.
      properties:
        agentPublicKeyCertificate:
          type: string
          description: Base64 encoded public certificate used by the agent to do the initial handshake to the backend services in Azure. Required.
        kubernetesVersion:
          type: string
          description: The Kubernetes version of the connected cluster resource.
        totalNodeCount:
          type: integer
          description: Number of nodes present in the connected cluster resource.
        totalCoreCount:
          type: integer
          description: Number of CPU cores present in the connected cluster resource.
        agentVersion:
          type: string
          description: Version of the agent running on the connected cluster resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the connected cluster resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        distribution:
          type: string
          description: The Kubernetes distribution running on this connected cluster.
        distributionVersion:
          type: string
          description: The Kubernetes distribution version on this connected cluster.
        infrastructure:
          type: string
          description: The infrastructure on which the Kubernetes cluster represented by this connected cluster is running on.
        offering:
          type: string
          description: Connected cluster offering.
        managedIdentityCertificateExpirationTime:
          type: string
          format: date-time
          description: Expiration time of the managed identity certificate.
        lastConnectivityTime:
          type: string
          format: date-time
          description: Time representing the last instance when heart beat was received from the cluster.
        connectivityStatus:
          $ref: "#/components/schemas/ConnectivityStatus"
          description: 'Represents the connectivity status of the connected cluster. Known values are: "Connecting", "Connected", "Offline", "Expired", and "AgentNotInstalled".'
        privateLinkState:
          $ref: "#/components/schemas/PrivateLinkState"
          description: 'Property which describes the state of private link on a connected cluster resource. Known values are: "Enabled" and "Disabled".'
        privateLinkScopeResourceId:
          type: string
          description: This is populated only if privateLinkState is enabled. The resource id of the private link scope this connected cluster is assigned to, if any.
        azureHybridBenefit:
          $ref: "#/components/schemas/AzureHybridBenefit"
          description: 'Indicates whether Azure Hybrid Benefit is opted in. Known values are: "True", "False", and "NotApplicable".'
        aadProfile:
          $ref: "#/components/schemas/AadProfile"
          description: AAD profile for the connected cluster.
        arcAgentProfile:
          $ref: "#/components/schemas/ArcAgentProfile"
          description: Arc agentry configuration for the provisioned cluster.
        securityProfile:
          $ref: "#/components/schemas/SecurityProfile"
          description: Security profile for the connected cluster.
        oidcIssuerProfile:
          $ref: "#/components/schemas/OidcIssuerProfile"
          description: Open ID Connect (OIDC) Issuer Profile for the connected cluster.
        gateway:
          $ref: "#/components/schemas/Gateway"
          description: Details of the gateway used by the Arc router for connectivity.
        arcAgentryConfigurations:
          type: array
          items:
            $ref: "#/components/schemas/ArcAgentryConfigurations"
          description: Configuration settings for customizing the behavior of the connected cluster.
        miscellaneousProperties:
          type: object
          properties: {}
          description: More properties related to the Connected Cluster.
      required:
        - agentPublicKeyCertificate
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
        - Accepted
    ConnectivityStatus:
      type: string
      enum:
        - Connecting
        - Connected
        - Offline
        - Expired
        - AgentNotInstalled
    PrivateLinkState:
      type: string
      enum:
        - Enabled
        - Disabled
    AzureHybridBenefit:
      type: string
      enum:
        - "True"
        - "False"
        - NotApplicable
    AadProfile:
      type: object
      description: AAD Profile specifies attributes for Azure Active Directory integration.
      properties:
        enableAzureRBAC:
          type: boolean
          description: Whether to enable Azure RBAC for Kubernetes authorization.
        adminGroupObjectIDs:
          type: array
          items:
            type: string
          description: The list of AAD group object IDs that will have admin role of the cluster.
        tenantID:
          type: string
          description: The AAD tenant ID to use for authentication. If not specified, will use the tenant of the deployment subscription.
    ArcAgentProfile:
      type: object
      description: Defines the Arc Agent properties for the clusters.
      properties:
        desiredAgentVersion:
          type: string
          description: Version of the Arc agents to be installed on the cluster resource.
        agentAutoUpgrade:
          $ref: "#/components/schemas/AutoUpgradeOptions"
          description: 'Indicates whether the Arc agents on the be upgraded automatically to the latest version. Defaults to Enabled. Known values are: "Enabled" and "Disabled".'
        systemComponents:
          type: array
          items:
            $ref: "#/components/schemas/SystemComponent"
          description: List of system extensions that are installed on the cluster resource.
        agentErrors:
          type: array
          items:
            $ref: "#/components/schemas/AgentError"
          description: List of arc agentry and system components errors on the cluster resource.
        agentState:
          type: string
          description: Represents the current state of the Arc agentry and its dependent components.
    AutoUpgradeOptions:
      type: string
      enum:
        - Enabled
        - Disabled
    SystemComponent:
      type: object
      description: System extensions and its current versions installed on the cluster resource.
      properties:
        type:
          type: string
          description: Type of the system extension.
        userSpecifiedVersion:
          type: string
          description: Version of the system extension to be installed on the cluster resource.
        majorVersion:
          type: integer
          description: Major Version of the system extension that is currently installed on the cluster resource.
        currentVersion:
          type: string
          description: Version of the system extension that is currently installed on the cluster resource.
    AgentError:
      type: object
      description: Agent Errors if any during agent or system component upgrade.
      properties:
        message:
          type: string
          description: Agent error message.
        severity:
          type: string
          description: Severity of the error message.
        component:
          type: string
          description: Agent component where error message occured.
        time:
          type: string
          format: date-time
          description: The timestamp of error occured (UTC).
    SecurityProfile:
      type: object
      description: Security Profile specifies attributes for cluster security features.
      properties:
        workloadIdentity:
          $ref: "#/components/schemas/SecurityProfileWorkloadIdentity"
          description: The workload identity feature webhook.
    SecurityProfileWorkloadIdentity:
      type: object
      description: The workload identity feature webhook.
      properties:
        enabled:
          type: boolean
          description: Whether to enable or disable the workload identity Webhook.
    OidcIssuerProfile:
      type: object
      description: OIDC Issuer Profile specifies attributes for workload identity integration.
      properties:
        enabled:
          type: boolean
          description: Whether to enable oidc issuer for workload identity integration.
        issuerUrl:
          type: string
          description: The issuer url for hybrid clusters connected to Arc used for the workload identity feature.
        selfHostedIssuerUrl:
          type: string
          description: The issuer url for public cloud clusters - AKS, EKS, GKE - used for the workload identity feature.
    Gateway:
      type: object
      description: Gateway.
      properties:
        enabled:
          type: boolean
          description: Indicates whether the gateway for arc router connectivity is enabled.
    ArcAgentryConfigurations:
      type: object
      description: ArcAgentryConfigurations.
      properties:
        feature:
          type: string
          description: Specifies the name of the feature for the configuration setting.
        settings:
          type: object
          properties: {}
          description: The configuration settings for the feature that do not contain any sensitive or secret information.
        protectedSettings:
          type: object
          properties: {}
          description: The configuration settings for the feature that contain any sensitive or secret information.
    ConnectedClusterIdentity:
      type: object
      description: Identity for the connected cluster.
      properties:
        principalId:
          type: string
          description: The principal id of connected cluster identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant id associated with the connected cluster. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ResourceIdentityType"
          description: "The type of identity used for the connected cluster. The type 'SystemAssigned, includes a system created identity. The type 'None' means no identity is assigned to the connected cluster. Required. Known values are: \"None\" and \"SystemAssigned\"."
      required:
        - type
    ResourceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
    ConnectedClusterKind:
      type: string
      enum:
        - ProvisionedCluster
    ConnectedClusterFlattened:
      type: object
      description: Flattened view of ConnectedCluster ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ConnectedClusterIdentity"
          description: The identity of the connected cluster. Required.
        kind:
          $ref: "#/components/schemas/ConnectedClusterKind"
          description: The kind of connected cluster. "ProvisionedCluster"
        agentPublicKeyCertificate:
          type: string
          description: Base64 encoded public certificate used by the agent to do the initial handshake to the backend services in Azure. Required.
        kubernetesVersion:
          type: string
          description: The Kubernetes version of the connected cluster resource.
        totalNodeCount:
          type: integer
          description: Number of nodes present in the connected cluster resource.
        totalCoreCount:
          type: integer
          description: Number of CPU cores present in the connected cluster resource.
        agentVersion:
          type: string
          description: Version of the agent running on the connected cluster resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the connected cluster resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        distribution:
          type: string
          description: The Kubernetes distribution running on this connected cluster.
        distributionVersion:
          type: string
          description: The Kubernetes distribution version on this connected cluster.
        infrastructure:
          type: string
          description: The infrastructure on which the Kubernetes cluster represented by this connected cluster is running on.
        offering:
          type: string
          description: Connected cluster offering.
        managedIdentityCertificateExpirationTime:
          type: string
          format: date-time
          description: Expiration time of the managed identity certificate.
        lastConnectivityTime:
          type: string
          format: date-time
          description: Time representing the last instance when heart beat was received from the cluster.
        connectivityStatus:
          $ref: "#/components/schemas/ConnectivityStatus"
          description: 'Represents the connectivity status of the connected cluster. Known values are: "Connecting", "Connected", "Offline", "Expired", and "AgentNotInstalled".'
        privateLinkState:
          $ref: "#/components/schemas/PrivateLinkState"
          description: 'Property which describes the state of private link on a connected cluster resource. Known values are: "Enabled" and "Disabled".'
        privateLinkScopeResourceId:
          type: string
          description: This is populated only if privateLinkState is enabled. The resource id of the private link scope this connected cluster is assigned to, if any.
        azureHybridBenefit:
          $ref: "#/components/schemas/AzureHybridBenefit"
          description: 'Indicates whether Azure Hybrid Benefit is opted in. Known values are: "True", "False", and "NotApplicable".'
        aadProfile:
          $ref: "#/components/schemas/AadProfile"
          description: AAD profile for the connected cluster.
        arcAgentProfile:
          $ref: "#/components/schemas/ArcAgentProfile"
          description: Arc agentry configuration for the provisioned cluster.
        securityProfile:
          $ref: "#/components/schemas/SecurityProfile"
          description: Security profile for the connected cluster.
        oidcIssuerProfile:
          $ref: "#/components/schemas/OidcIssuerProfile"
          description: Open ID Connect (OIDC) Issuer Profile for the connected cluster.
        gateway:
          $ref: "#/components/schemas/Gateway"
          description: Details of the gateway used by the Arc router for connectivity.
        arcAgentryConfigurations:
          type: array
          items:
            $ref: "#/components/schemas/ArcAgentryConfigurations"
          description: Configuration settings for customizing the behavior of the connected cluster.
        miscellaneousProperties:
          type: object
          properties: {}
          description: More properties related to the Connected Cluster.
    PagedConnectedClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ConnectedClusterFlattened"
        nextLink:
          type: string
    AuthenticationMethod:
      type: string
      enum:
        - Token
        - AAD
    HybridConnectionConfig:
      type: object
      description: Contains the REP (rendezvous endpoint) and “Sender” access token.
      properties:
        expirationTime:
          type: integer
          description: Timestamp when this token will be expired.
        hybridConnectionName:
          type: string
          description: Name of the connection.
        relay:
          type: string
          description: Name of the relay.
        token:
          type: string
          description: Sender access token.
        relayTid:
          type: string
          description: TenantID of the relay.
        relayType:
          type: string
          description: Type of relay.
    CredentialResult:
      type: object
      description: The credential result response.
      properties:
        name:
          type: string
          description: The name of the credential.
        value:
          type: string
          format: byte
          description: Base64-encoded Kubernetes configuration file.
    CredentialResults:
      type: object
      description: The list of credential result response.
      properties:
        hybridConnectionConfig:
          $ref: "#/components/schemas/HybridConnectionConfig"
          description: Contains the REP (rendezvous endpoint) and “Sender” access token.
        kubeconfigs:
          type: array
          items:
            $ref: "#/components/schemas/CredentialResult"
          description: Base64-encoded Kubernetes configuration file.
    ConnectedCluster:
      type: object
      description: Represents a connected cluster.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/ConnectedClusterProperties"
          description: Describes the connected cluster resource properties. Required.
        identity:
          $ref: "#/components/schemas/ConnectedClusterIdentity"
          description: The identity of the connected cluster. Required.
        kind:
          $ref: "#/components/schemas/ConnectedClusterKind"
          description: The kind of connected cluster. "ProvisionedCluster"
      required:
        - location
        - properties
        - identity
    ConnectedClusterPatchProperties:
      type: object
      description: Properties which can be patched on the connected cluster resource.
      properties:
        distribution:
          type: string
          description: Represents the distribution of the connected cluster.
        distributionVersion:
          type: string
          description: Represents the Kubernetes distribution version on this connected cluster.
        azureHybridBenefit:
          $ref: "#/components/schemas/AzureHybridBenefit"
          description: 'Indicates whether Azure Hybrid Benefit is opted in. Known values are: "True", "False", and "NotApplicable".'
        gateway:
          $ref: "#/components/schemas/Gateway"
          description: Indicates whether Gateway is enabled for the connected cluster resource.
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    connected_cluster:
      id: azure.hybrid_kubernetes.connected_cluster
      name: connected_cluster
      title: connected_cluster
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Kubernetes~1connectedClusters~1{cluster_name}?api-version=2026-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_replace:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Kubernetes~1connectedClusters~1{cluster_name}?api-version=2026-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update_async:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Kubernetes~1connectedClusters~1{cluster_name}?api-version=2026-05-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Kubernetes~1connectedClusters~1{cluster_name}?api-version=2026-05-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Kubernetes~1connectedClusters?api-version=2026-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Kubernetes~1connectedClusters?api-version=2026-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_cluster_user_credential:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Kubernetes~1connectedClusters~1{cluster_name}~1listClusterUserCredential?api-version=2026-05-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/connected_cluster/methods/list_cluster_user_credential"
          - $ref: "#/components/x-stackQL-resources/connected_cluster/methods/get"
          - $ref: "#/components/x-stackQL-resources/connected_cluster/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/connected_cluster/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/connected_cluster/methods/create_or_replace"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/connected_cluster/methods/create_or_replace"
        delete:
          - $ref: "#/components/x-stackQL-resources/connected_cluster/methods/delete"
    operations:
      id: azure.hybrid_kubernetes.operations
      name: operations
      title: operations
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Kubernetes~1operations?api-version=2026-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/get"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Kubernetes/connectedClusters/{cluster_name}?api-version=2026-05-01:
    get:
      operationId: ConnectedCluster_get
      description: Get the properties of the specified connected cluster. Returns the properties of the specified connected cluster, including name, identity, properties, and additional cluster details.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Kubernetes cluster on which get is called. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectedClusterFlattened"
    put:
      operationId: ConnectedCluster_begin_create_or_replace
      description: Register a new Kubernetes cluster with Azure Resource Manager. API to register a new Kubernetes cluster and create or replace a connected cluster tracked resource in Azure Resource Manager (ARM).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Kubernetes cluster on which get is called. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/ConnectedClusterProperties"
                  description: Describes the connected cluster resource properties. Required.
                identity:
                  $ref: "#/components/schemas/ConnectedClusterIdentity"
                  description: The identity of the connected cluster. Required.
                kind:
                  $ref: "#/components/schemas/ConnectedClusterKind"
                  description: The kind of connected cluster. "ProvisionedCluster"
              required:
                - location
                - properties
                - identity
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectedCluster"
    patch:
      operationId: ConnectedCluster_begin_update_async
      description: Updates a connected cluster. API to update certain properties of the connected cluster resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Kubernetes cluster on which get is called. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/ConnectedClusterPatchProperties"
                  description: Describes the connected cluster resource properties that can be updated during PATCH operation.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectedCluster"
    delete:
      operationId: ConnectedCluster_begin_delete
      description: Delete a connected cluster. Delete a connected cluster, removing the tracked resource in Azure Resource Manager (ARM).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Kubernetes cluster on which get is called. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Kubernetes/connectedClusters?api-version=2026-05-01:
    get:
      operationId: ConnectedCluster_list_by_resource_group
      description: Lists all connected clusters in the given Resource Group. API to enumerate registered connected K8s clusters under a Resource Group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConnectedClusterFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Kubernetes/connectedClusters?api-version=2026-05-01:
    get:
      operationId: ConnectedCluster_list_by_subscription
      description: Lists all connected clusters in the given Subscription. API to enumerate registered connected K8s clusters under a Subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConnectedClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Kubernetes/connectedClusters/{cluster_name}/listClusterUserCredential?api-version=2026-05-01:
    post:
      operationId: ConnectedCluster_list_cluster_user_credential
      description: Gets cluster user credentials of a connected cluster. Gets cluster user credentials of the connected cluster with a specified resource group and name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Kubernetes cluster on which get is called. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                authenticationMethod:
                  $ref: "#/components/schemas/AuthenticationMethod"
                  description: 'The mode of client authentication. Required. Known values are: "Token" and "AAD".'
                clientProxy:
                  type: boolean
                  description: Boolean value to indicate whether the request is for client side proxy or not. Required.
              required:
                - authenticationMethod
                - clientProxy
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialResults"
  /providers/Microsoft.Kubernetes/operations?api-version=2026-05-01:
    get:
      operationId: hybridkubernetes_get
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
