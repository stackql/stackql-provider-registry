openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Standby Pool Mgmt Client
  description: StandbyPoolMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-10-01-stackql-generated
  x-serviceAlias: standbypool
  x-sdkPackage: azure-mgmt-standbypool
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    StandbyContainerGroupPoolRuntimeViewResourceProperties:
      type: object
      description: Contains information about a standby pool as last known by the StandbyPool resource provider.
      properties:
        instanceCountSummary:
          type: array
          items:
            $ref: "#/components/schemas/ContainerGroupInstanceCountSummary"
          description: A list containing the counts of container groups in each possible state, as known by the StandbyPool resource provider. Required.
        status:
          $ref: "#/components/schemas/PoolStatus"
          description: Display status of the standby pool.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Displays the provisioning state of the standby pool. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
        prediction:
          $ref: "#/components/schemas/StandbyContainerGroupPoolPrediction"
          description: Displays prediction information of the standby pool.
    ContainerGroupInstanceCountSummary:
      type: object
      description: Displays the counts of container groups in each state, as known by the StandbyPool resource provider.
      properties:
        zone:
          type: integer
          description: The zone that the provided counts are in. It will not have a value if zones are not enabled.
        instanceCountsByState:
          type: array
          items:
            $ref: "#/components/schemas/PoolContainerGroupStateCount"
          description: The count of pooled container groups in each state for the given zone. Required.
      required:
        - instanceCountsByState
    PoolContainerGroupStateCount:
      type: object
      description: Displays the counts of pooled container groups in each state, as known by the StandbyPool resource provider.
      properties:
        state:
          $ref: "#/components/schemas/PoolContainerGroupState"
          description: 'The state that the pooled container groups count is for. Required. Known values are: "Running", "Creating", and "Deleting".'
        count:
          type: integer
          description: The count of pooled container groups in the given state. Required.
      required:
        - state
        - count
    PoolContainerGroupState:
      type: string
      enum:
        - Running
        - Creating
        - Deleting
    PoolStatus:
      type: object
      description: Displays StandbyPool status.
      properties:
        code:
          $ref: "#/components/schemas/HealthStateCode"
          description: 'Displays the healthy state of the StandbyPool. Required. Known values are: "HealthState/healthy" and "HealthState/degraded".'
        message:
          type: string
          description: Displays the StandbyPool health state details.
    HealthStateCode:
      type: string
      enum:
        - HealthState/healthy
        - HealthState/degraded
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Deleting
    StandbyContainerGroupPoolPrediction:
      type: object
      description: Displays prediction information of the standby pool.
      properties:
        forecastValues:
          $ref: "#/components/schemas/StandbyContainerGroupPoolForecastValues"
          description: Displays the forecast information of the standby pool. Required.
        forecastStartTime:
          type: string
          format: date-time
          description: Displays the UTC timestamp of when the prediction was retrieved for the standby pool. Required.
        forecastInfo:
          type: string
          description: Displays additional information for the prediction of the standby pool. Required.
    StandbyContainerGroupPoolForecastValues:
      type: object
      description: Displays the forecast information of the standby pool.
      properties:
        instancesRequestedCount:
          type: array
          items:
            type: integer
          description: Displays the predicted count of instances to be requested from the standby pool. Required.
    StandbyContainerGroupPoolRuntimeViewResourceFlattened:
      type: object
      description: Flattened view of StandbyContainerGroupPoolRuntimeViewResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        instanceCountSummary:
          type: array
          items:
            $ref: "#/components/schemas/ContainerGroupInstanceCountSummary"
          description: A list containing the counts of container groups in each possible state, as known by the StandbyPool resource provider. Required.
        status:
          $ref: "#/components/schemas/PoolStatus"
          description: Display status of the standby pool.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Displays the provisioning state of the standby pool. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
        prediction:
          $ref: "#/components/schemas/StandbyContainerGroupPoolPrediction"
          description: Displays prediction information of the standby pool.
    PagedStandbyContainerGroupPoolRuntimeViewResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StandbyContainerGroupPoolRuntimeViewResourceFlattened"
        nextLink:
          type: string
    StandbyContainerGroupPoolResourceProperties:
      type: object
      description: Details of the StandbyContainerGroupPool.
      properties:
        elasticityProfile:
          $ref: "#/components/schemas/StandbyContainerGroupPoolElasticityProfile"
          description: Specifies elasticity profile of standby container group pools. Required.
        containerGroupProperties:
          $ref: "#/components/schemas/ContainerGroupProperties"
          description: Specifies container group properties of standby container group pools. Required.
        zones:
          type: array
          items:
            type: string
          description: Specifies zones of standby container group pools.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
      required:
        - elasticityProfile
        - containerGroupProperties
    StandbyContainerGroupPoolElasticityProfile:
      type: object
      description: Specifies the elasticity profile of the standby container group pools.
      properties:
        maxReadyCapacity:
          type: integer
          description: Specifies maximum number of standby container groups in the standby pool. Required.
        refillPolicy:
          $ref: "#/components/schemas/RefillPolicy"
          description: Specifies refill policy of the pool. "always"
        dynamicSizing:
          $ref: "#/components/schemas/DynamicSizing"
          description: Specifies the dynamic sizing configuration.
      required:
        - maxReadyCapacity
    RefillPolicy:
      type: string
      enum:
        - always
    DynamicSizing:
      type: object
      description: Specifies the dynamic sizing configuration.
      properties:
        enabled:
          type: boolean
          description: Indicates whether dynamic sizing is enabled for the standby pool.
    ContainerGroupProperties:
      type: object
      description: Details of the ContainerGroupProperties.
      properties:
        containerGroupProfile:
          $ref: "#/components/schemas/ContainerGroupProfile"
          description: Specifies container group profile of standby container groups. Required.
        subnetIds:
          type: array
          items:
            $ref: "#/components/schemas/Subnet"
          description: Specifies subnet Ids for container group.
      required:
        - containerGroupProfile
    ContainerGroupProfile:
      type: object
      description: Details of the ContainerGroupProfile.
      properties:
        id:
          type: string
          description: Specifies container group profile id of standby container groups. Required.
        revision:
          type: integer
          description: Specifies revision of container group profile.
      required:
        - id
    Subnet:
      type: object
      description: Subnet of container group.
      properties:
        id:
          type: string
          description: Specifies ARM resource id of the subnet. Required.
      required:
        - id
    StandbyContainerGroupPoolResourceFlattened:
      type: object
      description: Flattened view of StandbyContainerGroupPoolResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        elasticityProfile:
          $ref: "#/components/schemas/StandbyContainerGroupPoolElasticityProfile"
          description: Specifies elasticity profile of standby container group pools. Required.
        containerGroupProperties:
          $ref: "#/components/schemas/ContainerGroupProperties"
          description: Specifies container group properties of standby container group pools. Required.
        zones:
          type: array
          items:
            type: string
          description: Specifies zones of standby container group pools.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
    PagedStandbyContainerGroupPoolResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StandbyContainerGroupPoolResourceFlattened"
        nextLink:
          type: string
    StandbyContainerGroupPoolResource:
      type: object
      description: A StandbyContainerGroupPoolResource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/StandbyContainerGroupPoolResourceProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    StandbyContainerGroupPoolResourceUpdateProperties:
      type: object
      description: The updatable properties of the StandbyContainerGroupPoolResource.
      properties:
        elasticityProfile:
          $ref: "#/components/schemas/StandbyContainerGroupPoolElasticityProfile"
          description: Specifies elasticity profile of standby container group pools.
        containerGroupProperties:
          $ref: "#/components/schemas/ContainerGroupProperties"
          description: Specifies container group properties of standby container group pools.
        zones:
          type: array
          items:
            type: string
          description: Specifies zones of standby container group pools.
    StandbyVirtualMachinePoolRuntimeViewResourceProperties:
      type: object
      description: Contains information about a standby pool as last known by the StandbyPool resource provider.
      properties:
        instanceCountSummary:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineInstanceCountSummary"
          description: "A list containing the counts of virtual machines in each possible power state for each zone if enabled, as known by the StandbyPool resource provider. If zones are not enabled on the attached VMSS, the list will contain a single entry without zone values. Note: any resources in the Running state may still be installing extensions / not fully provisioned. Required."
        status:
          $ref: "#/components/schemas/PoolStatus"
          description: Display status of the standby pool.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Displays the provisioning state of the standby pool. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
        prediction:
          $ref: "#/components/schemas/StandbyVirtualMachinePoolPrediction"
          description: Displays prediction information of the standby pool.
    VirtualMachineInstanceCountSummary:
      type: object
      description: "Contains the counts of VMs in each power state in a given zone, fault domain, as known by the StandbyPool resource provider. Note: any resources in the Running state may still be installing extensions / not fully provisioned."
      properties:
        zone:
          type: integer
          description: The zone that the provided counts are in. It will not have a value if zones are not enabled on the attached VMSS.
        instanceCountsByState:
          type: array
          items:
            $ref: "#/components/schemas/PoolVirtualMachineStateCount"
          description: The count of pooled virtual machines in each state for the given zone. Required.
      required:
        - instanceCountsByState
    PoolVirtualMachineStateCount:
      type: object
      description: Displays the counts of pooled virtual machines in each state, as known by the StandbyPool resource provider.
      properties:
        state:
          $ref: "#/components/schemas/PoolVirtualMachineState"
          description: 'The state that the pooled virtual machines count is for. Required. Known values are: "Running", "Creating", "Starting", "Deleting", "Deallocated", "Deallocating", "Hibernated", and "Hibernating".'
        count:
          type: integer
          description: The count of pooled virtual machines in the given state. Required.
      required:
        - state
        - count
    PoolVirtualMachineState:
      type: string
      enum:
        - Running
        - Creating
        - Starting
        - Deleting
        - Deallocated
        - Deallocating
        - Hibernated
        - Hibernating
    StandbyVirtualMachinePoolPrediction:
      type: object
      description: Displays prediction information of the standby pool.
      properties:
        forecastValues:
          $ref: "#/components/schemas/StandbyVirtualMachinePoolForecastValues"
          description: Displays the forecast information of the standby pool. Required.
        forecastStartTime:
          type: string
          format: date-time
          description: Displays the UTC timestamp of when the prediction was retrieved for the standby pool. Required.
        forecastInfo:
          type: string
          description: Displays additional information for the prediction of the standby pool. Required.
    StandbyVirtualMachinePoolForecastValues:
      type: object
      description: Displays the forecast information of the standby pool.
      properties:
        instancesRequestedCount:
          type: array
          items:
            type: integer
          description: Displays the predicted count of instances to be requested from the standby pool. Required.
    StandbyVirtualMachinePoolRuntimeViewResourceFlattened:
      type: object
      description: Flattened view of StandbyVirtualMachinePoolRuntimeViewResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        instanceCountSummary:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineInstanceCountSummary"
          description: "A list containing the counts of virtual machines in each possible power state for each zone if enabled, as known by the StandbyPool resource provider. If zones are not enabled on the attached VMSS, the list will contain a single entry without zone values. Note: any resources in the Running state may still be installing extensions / not fully provisioned. Required."
        status:
          $ref: "#/components/schemas/PoolStatus"
          description: Display status of the standby pool.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Displays the provisioning state of the standby pool. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
        prediction:
          $ref: "#/components/schemas/StandbyVirtualMachinePoolPrediction"
          description: Displays prediction information of the standby pool.
    PagedStandbyVirtualMachinePoolRuntimeViewResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StandbyVirtualMachinePoolRuntimeViewResourceFlattened"
        nextLink:
          type: string
    StandbyVirtualMachinePoolResourceProperties:
      type: object
      description: Details of the StandbyVirtualMachinePool.
      properties:
        elasticityProfile:
          $ref: "#/components/schemas/StandbyVirtualMachinePoolElasticityProfile"
          description: Specifies the elasticity profile of the standby virtual machine pools.
        virtualMachineState:
          $ref: "#/components/schemas/VirtualMachineState"
          description: 'Specifies the desired state of virtual machines in the pool. Required. Known values are: "Running", "Deallocated", and "Hibernated".'
        attachedVirtualMachineScaleSetId:
          type: string
          description: Specifies the fully qualified resource ID of a virtual machine scale set the pool is attached to.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
      required:
        - virtualMachineState
    StandbyVirtualMachinePoolElasticityProfile:
      type: object
      description: Details of the elasticity profile.
      properties:
        maxReadyCapacity:
          type: integer
          description: Specifies the maximum number of virtual machines in the standby virtual machine pool. Required.
        minReadyCapacity:
          type: integer
          description: Specifies the desired minimum number of virtual machines in the standby virtual machine pool. MinReadyCapacity cannot exceed MaxReadyCapacity.
        postProvisioningDelay:
          type: string
          description: Specifies the duration to wait after virtual machine provisioning before the virtual machine becomes available for use. The duration should be specified in ISO 8601 format (e.g., PT2S for 2 seconds).
        dynamicSizing:
          $ref: "#/components/schemas/DynamicSizing"
          description: Specifies the dynamic sizing configuration.
      required:
        - maxReadyCapacity
    VirtualMachineState:
      type: string
      enum:
        - Running
        - Deallocated
        - Hibernated
    StandbyVirtualMachinePoolResourceFlattened:
      type: object
      description: Flattened view of StandbyVirtualMachinePoolResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        elasticityProfile:
          $ref: "#/components/schemas/StandbyVirtualMachinePoolElasticityProfile"
          description: Specifies the elasticity profile of the standby virtual machine pools.
        virtualMachineState:
          $ref: "#/components/schemas/VirtualMachineState"
          description: 'Specifies the desired state of virtual machines in the pool. Required. Known values are: "Running", "Deallocated", and "Hibernated".'
        attachedVirtualMachineScaleSetId:
          type: string
          description: Specifies the fully qualified resource ID of a virtual machine scale set the pool is attached to.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
    PagedStandbyVirtualMachinePoolResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StandbyVirtualMachinePoolResourceFlattened"
        nextLink:
          type: string
    StandbyVirtualMachinePoolResource:
      type: object
      description: A StandbyVirtualMachinePoolResource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/StandbyVirtualMachinePoolResourceProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    StandbyVirtualMachinePoolResourceUpdateProperties:
      type: object
      description: The updatable properties of the StandbyVirtualMachinePoolResource.
      properties:
        elasticityProfile:
          $ref: "#/components/schemas/StandbyVirtualMachinePoolElasticityProfile"
          description: Specifies the elasticity profile of the standby virtual machine pools.
        virtualMachineState:
          $ref: "#/components/schemas/VirtualMachineState"
          description: 'Specifies the desired state of virtual machines in the pool. Known values are: "Running", "Deallocated", and "Hibernated".'
        attachedVirtualMachineScaleSetId:
          type: string
          description: Specifies the fully qualified resource ID of a virtual machine scale set the pool is attached to.
    StandbyVirtualMachineResourceProperties:
      type: object
      description: Details of the StandbyVirtualMachine.
      properties:
        virtualMachineResourceId:
          type: string
          description: Resource id of the virtual machine. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
      required:
        - virtualMachineResourceId
    StandbyVirtualMachineResourceFlattened:
      type: object
      description: Flattened view of StandbyVirtualMachineResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        virtualMachineResourceId:
          type: string
          description: Resource id of the virtual machine. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", "Canceled", and "Deleting".'
    PagedStandbyVirtualMachineResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StandbyVirtualMachineResourceFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    operations:
      id: azure.standbypool.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.StandbyPool~1operations?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    standby_container_group_pool_runtime_views:
      id: azure.standbypool.standby_container_group_pool_runtime_views
      name: standby_container_group_pool_runtime_views
      title: standby_container_group_pool_runtime_views
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools~1{standby_container_group_pool_name}~1runtimeViews~1{runtime_view}?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_standby_pool:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools~1{standby_container_group_pool_name}~1runtimeViews?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pool_runtime_views/methods/get"
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pool_runtime_views/methods/list_by_standby_pool"
        insert: []
        update: []
        delete: []
    standby_container_group_pools:
      id: azure.standbypool.standby_container_group_pools
      name: standby_container_group_pools
      title: standby_container_group_pools
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools~1{standby_container_group_pool_name}?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools~1{standby_container_group_pool_name}?api-version=2025-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools~1{standby_container_group_pool_name}?api-version=2025-10-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools~1{standby_container_group_pool_name}?api-version=2025-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StandbyPool~1standbyContainerGroupPools?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pools/methods/get"
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pools/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pools/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pools/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pools/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pools/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/standby_container_group_pools/methods/delete"
    standby_virtual_machine_pool_runtime_views:
      id: azure.standbypool.standby_virtual_machine_pool_runtime_views
      name: standby_virtual_machine_pool_runtime_views
      title: standby_virtual_machine_pool_runtime_views
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}~1runtimeViews~1{runtime_view}?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_standby_pool:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}~1runtimeViews?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pool_runtime_views/methods/get"
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pool_runtime_views/methods/list_by_standby_pool"
        insert: []
        update: []
        delete: []
    standby_virtual_machine_pools:
      id: azure.standbypool.standby_virtual_machine_pools
      name: standby_virtual_machine_pools
      title: standby_virtual_machine_pools
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}?api-version=2025-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}?api-version=2025-10-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}?api-version=2025-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pools/methods/get"
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pools/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pools/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pools/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pools/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pools/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machine_pools/methods/delete"
    standby_virtual_machines:
      id: azure.standbypool.standby_virtual_machines
      name: standby_virtual_machines
      title: standby_virtual_machines
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}~1standbyVirtualMachines~1{standby_virtual_machine_name}?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_standby_virtual_machine_pool_resource:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StandbyPool~1standbyVirtualMachinePools~1{standby_virtual_machine_pool_name}~1standbyVirtualMachines?api-version=2025-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machines/methods/get"
          - $ref: "#/components/x-stackQL-resources/standby_virtual_machines/methods/list_by_standby_virtual_machine_pool_resource"
        insert: []
        update: []
        delete: []
paths:
  /providers/Microsoft.StandbyPool/operations?api-version=2025-10-01:
    get:
      operationId: standbypool_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyContainerGroupPools/{standby_container_group_pool_name}/runtimeViews/{runtime_view}?api-version=2025-10-01:
    get:
      operationId: StandbyContainerGroupPoolRuntimeViews_get
      description: Get a StandbyContainerGroupPoolRuntimeViewResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_container_group_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby container group pool. Required.
        - name: runtime_view
          in: path
          required: true
          schema:
            type: string
          description: The unique identifier for the runtime view. The input string should be the word 'latest', which will get the latest runtime view of the pool, otherwise the request will fail with NotFound exception. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyContainerGroupPoolRuntimeViewResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyContainerGroupPools/{standby_container_group_pool_name}/runtimeViews?api-version=2025-10-01:
    get:
      operationId: StandbyContainerGroupPoolRuntimeViews_list_by_standby_pool
      description: List StandbyContainerGroupPoolRuntimeViewResource resources by StandbyContainerGroupPoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_container_group_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby container group pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStandbyContainerGroupPoolRuntimeViewResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyContainerGroupPools/{standby_container_group_pool_name}?api-version=2025-10-01:
    get:
      operationId: StandbyContainerGroupPools_get
      description: Get a StandbyContainerGroupPoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_container_group_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby container group pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyContainerGroupPoolResourceFlattened"
    put:
      operationId: StandbyContainerGroupPools_begin_create_or_update
      description: Create a StandbyContainerGroupPoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_container_group_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby container group pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/StandbyContainerGroupPoolResourceProperties"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyContainerGroupPoolResource"
    delete:
      operationId: StandbyContainerGroupPools_begin_delete
      description: Delete a StandbyContainerGroupPoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_container_group_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby container group pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: StandbyContainerGroupPools_update
      description: Update a StandbyContainerGroupPoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_container_group_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby container group pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/StandbyContainerGroupPoolResourceUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyContainerGroupPoolResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyContainerGroupPools?api-version=2025-10-01:
    get:
      operationId: StandbyContainerGroupPools_list_by_resource_group
      description: List StandbyContainerGroupPoolResource resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStandbyContainerGroupPoolResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.StandbyPool/standbyContainerGroupPools?api-version=2025-10-01:
    get:
      operationId: StandbyContainerGroupPools_list_by_subscription
      description: List StandbyContainerGroupPoolResource resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStandbyContainerGroupPoolResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyVirtualMachinePools/{standby_virtual_machine_pool_name}/runtimeViews/{runtime_view}?api-version=2025-10-01:
    get:
      operationId: StandbyVirtualMachinePoolRuntimeViews_get
      description: Get a StandbyVirtualMachinePoolRuntimeViewResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: runtime_view
          in: path
          required: true
          schema:
            type: string
          description: The unique identifier for the runtime view. The input string should be the word 'latest', which will get the latest runtime view of the pool, otherwise the request will fail with NotFound exception. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyVirtualMachinePoolRuntimeViewResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyVirtualMachinePools/{standby_virtual_machine_pool_name}/runtimeViews?api-version=2025-10-01:
    get:
      operationId: StandbyVirtualMachinePoolRuntimeViews_list_by_standby_pool
      description: List StandbyVirtualMachinePoolRuntimeViewResource resources by StandbyVirtualMachinePoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStandbyVirtualMachinePoolRuntimeViewResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyVirtualMachinePools/{standby_virtual_machine_pool_name}?api-version=2025-10-01:
    get:
      operationId: StandbyVirtualMachinePools_get
      description: Get a StandbyVirtualMachinePoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyVirtualMachinePoolResourceFlattened"
    put:
      operationId: StandbyVirtualMachinePools_begin_create_or_update
      description: Create a StandbyVirtualMachinePoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/StandbyVirtualMachinePoolResourceProperties"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyVirtualMachinePoolResource"
    delete:
      operationId: StandbyVirtualMachinePools_begin_delete
      description: Delete a StandbyVirtualMachinePoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: StandbyVirtualMachinePools_update
      description: Update a StandbyVirtualMachinePoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/StandbyVirtualMachinePoolResourceUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyVirtualMachinePoolResource"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyVirtualMachinePools?api-version=2025-10-01:
    get:
      operationId: StandbyVirtualMachinePools_list_by_resource_group
      description: List StandbyVirtualMachinePoolResource resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStandbyVirtualMachinePoolResourceFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.StandbyPool/standbyVirtualMachinePools?api-version=2025-10-01:
    get:
      operationId: StandbyVirtualMachinePools_list_by_subscription
      description: List StandbyVirtualMachinePoolResource resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStandbyVirtualMachinePoolResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyVirtualMachinePools/{standby_virtual_machine_pool_name}/standbyVirtualMachines/{standby_virtual_machine_name}?api-version=2025-10-01:
    get:
      operationId: StandbyVirtualMachines_get
      description: Get a StandbyVirtualMachineResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: standby_virtual_machine_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandbyVirtualMachineResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StandbyPool/standbyVirtualMachinePools/{standby_virtual_machine_pool_name}/standbyVirtualMachines?api-version=2025-10-01:
    get:
      operationId: StandbyVirtualMachines_list_by_standby_virtual_machine_pool_resource
      description: List StandbyVirtualMachineResource resources by StandbyVirtualMachinePoolResource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: standby_virtual_machine_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the standby virtual machine pool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStandbyVirtualMachineResourceFlattened"
