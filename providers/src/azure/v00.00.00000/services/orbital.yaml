openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Orbital
  description: This is the Microsoft Azure Orbital Management Client Library.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2022-11-01-stackql-generated
  x-serviceAlias: orbital
  x-sdkPackage: azure-mgmt-orbital
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    AvailableGroundStationFlattened:
      type: object
      description: Flattened view of AvailableGroundStation ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: ID of groundStation.
        name:
          type: string
          description: Name of the ground station.
        location:
          type: string
          description: Azure region.
        type:
          type: string
          description: Resource type.
        city:
          type: string
          description: City of ground station.
        providerName:
          type: string
          description: Ground station provider name.
        longitudeDegrees:
          type: number
          description: Longitude of the ground station in decimal degrees.
        latitudeDegrees:
          type: number
          description: Latitude of the ground station in decimal degrees.
        altitudeMeters:
          type: number
          description: Altitude of the ground station.
        releaseMode:
          type: string
          description: 'Release Status of a ground station. Known values are: "Preview" and "GA".'
    PagedAvailableGroundStationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AvailableGroundStationFlattened"
        nextLink:
          type: string
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    ContactProfilesPropertiesNetworkConfiguration:
      type: object
      description: Network configuration of customer virtual network. All required parameters must be populated in order to send to Azure.
      properties:
        subnetId:
          type: string
          description: ARM resource identifier of the subnet delegated to the Microsoft.Orbital/orbitalGateways. Needs to be at least a class C subnet, and should not have any IP created in it. Required.
      required:
        - subnetId
    ContactProfileThirdPartyConfiguration:
      type: object
      description: Contact Profile third-party partner configuration. All required parameters must be populated in order to send to Azure.
      properties:
        providerName:
          type: string
          description: Name of the third-party provider. Required.
        missionConfiguration:
          type: string
          description: Name of string referencing the configuration describing contact set-up for a particular mission. Expected values are those which have been created in collaboration with the partner network. Required.
      required:
        - providerName
        - missionConfiguration
    ContactProfileLink:
      type: object
      description: Contact Profile Link. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: Link name. Required.
        polarization:
          type: string
          description: 'Polarization. e.g. (RHCP, LHCP). Required. Known values are: "RHCP", "LHCP", "linearVertical", and "linearHorizontal".'
        direction:
          type: string
          description: 'Direction (Uplink or Downlink). Required. Known values are: "Uplink" and "Downlink".'
        gainOverTemperature:
          type: number
          description: Gain to noise temperature in db/K. It is the required G/T by the customer. Not used yet.
        eirpdBW:
          type: number
          description: Effective Isotropic Radiated Power (EIRP) in dBW. It is the required EIRP by the customer. Not used yet.
        channels:
          type: array
          items:
            $ref: "#/components/schemas/ContactProfileLinkChannel"
          description: Contact Profile Link Channel. Required.
      required:
        - name
        - polarization
        - direction
        - channels
    ContactProfileLinkChannel:
      type: object
      description: Contact Profile Link Channel. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: Channel name. Required.
        centerFrequencyMHz:
          type: number
          description: Center Frequency in MHz. Required.
        bandwidthMHz:
          type: number
          description: Bandwidth in MHz. Required.
        endPoint:
          $ref: "#/components/schemas/EndPoint"
          description: Customer end point to store and retrieve data during a contact with the spacecraft. Required.
        modulationConfiguration:
          type: string
          description: Copy of the modem configuration file such as Kratos QRadio. Only valid for uplink directions. If provided, the modem connects to the customer endpoint and accepts commands from the customer instead of a VITA.49 stream.
        demodulationConfiguration:
          type: string
          description: Copy of the modem configuration file such as Kratos QRadio or Kratos QuantumRx. Only valid for downlink directions. If provided, the modem connects to the customer endpoint and sends demodulated data instead of a VITA.49 stream.
        encodingConfiguration:
          type: string
          description: Currently unused.
        decodingConfiguration:
          type: string
          description: Currently unused.
      required:
        - name
        - centerFrequencyMHz
        - bandwidthMHz
        - endPoint
    EndPoint:
      type: object
      description: Customer end point to store and retrieve data during a contact with the spacecraft. All required parameters must be populated in order to send to Azure.
      properties:
        ipAddress:
          type: string
          description: IP Address (IPv4). Required.
        endPointName:
          type: string
          description: Name of an end point. Required.
        port:
          type: string
          description: TCP port to listen on to receive data. Required.
        protocol:
          type: string
          description: 'Protocol either UDP or TCP. Required. Known values are: "TCP" and "UDP".'
      required:
        - ipAddress
        - endPointName
        - port
        - protocol
    ContactProfileFlattened:
      type: object
      description: Flattened view of ContactProfile ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        provisioningState:
          type: string
          description: "The current state of the resource's creation, deletion, or modification. Known values are: \"creating\", \"succeeded\", \"failed\", \"canceled\", \"updating\", and \"deleting\"."
        minimumViableContactDuration:
          type: string
          description: Minimum viable contact duration in ISO 8601 format. Used for listing the available contacts with a spacecraft at a given ground station.
        minimumElevationDegrees:
          type: number
          description: Minimum viable elevation for the contact in decimal degrees. Used for listing the available contacts with a spacecraft at a given ground station.
        autoTrackingConfiguration:
          type: string
          description: 'Auto-tracking configuration. Known values are: "disabled", "xBand", and "sBand".'
        eventHubUri:
          type: string
          description: ARM resource identifier of the Event Hub used for telemetry. Requires granting Orbital Resource Provider the rights to send telemetry into the hub.
        networkConfiguration:
          $ref: "#/components/schemas/ContactProfilesPropertiesNetworkConfiguration"
          description: Network configuration of customer virtual network. Required.
        thirdPartyConfigurations:
          type: array
          items:
            $ref: "#/components/schemas/ContactProfileThirdPartyConfiguration"
          description: Third-party mission configuration of the Contact Profile. Describes RF links, modem processing, and IP endpoints.
        links:
          type: array
          items:
            $ref: "#/components/schemas/ContactProfileLink"
          description: Links of the Contact Profile. Describes RF links, modem processing, and IP endpoints. Required.
    PagedContactProfileFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ContactProfileFlattened"
        nextLink:
          type: string
    ContactProfile:
      type: object
      description: Customer creates a Contact Profile Resource, which will contain all of the configurations required for scheduling a contact. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          type: object
          properties:
            provisioningState:
              type: string
              description: "The current state of the resource's creation, deletion, or modification. Known values are: \"creating\", \"succeeded\", \"failed\", \"canceled\", \"updating\", and \"deleting\"."
            minimumViableContactDuration:
              type: string
              description: Minimum viable contact duration in ISO 8601 format. Used for listing the available contacts with a spacecraft at a given ground station.
            minimumElevationDegrees:
              type: number
              description: Minimum viable elevation for the contact in decimal degrees. Used for listing the available contacts with a spacecraft at a given ground station.
            autoTrackingConfiguration:
              type: string
              description: 'Auto-tracking configuration. Known values are: "disabled", "xBand", and "sBand".'
            eventHubUri:
              type: string
              description: ARM resource identifier of the Event Hub used for telemetry. Requires granting Orbital Resource Provider the rights to send telemetry into the hub.
            networkConfiguration:
              $ref: "#/components/schemas/ContactProfilesPropertiesNetworkConfiguration"
              description: Network configuration of customer virtual network. Required.
            thirdPartyConfigurations:
              type: array
              items:
                $ref: "#/components/schemas/ContactProfileThirdPartyConfiguration"
              description: Third-party mission configuration of the Contact Profile. Describes RF links, modem processing, and IP endpoints.
            links:
              type: array
              items:
                $ref: "#/components/schemas/ContactProfileLink"
              description: Links of the Contact Profile. Describes RF links, modem processing, and IP endpoints. Required.
      required:
        - location
        - properties
    ContactsPropertiesAntennaConfiguration:
      type: object
      description: The configuration associated with the allocated antenna.
      properties:
        destinationIp:
          type: string
          description: The destination IP a packet can be sent to. This would for example be the TCP endpoint you would send data to.
        sourceIps:
          type: array
          items:
            type: string
          description: List of Source IP.
    ContactsPropertiesContactProfile:
      type: object
      description: The reference to the contact profile resource. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Resource ID. Required.
      required:
        - id
    ContactFlattened:
      type: object
      description: Flattened view of Contact ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        provisioningState:
          type: string
          description: "The current state of the resource's creation, deletion, or modification. Known values are: \"creating\", \"succeeded\", \"failed\", \"canceled\", \"updating\", and \"deleting\"."
        status:
          type: string
          description: 'Status of a contact. Known values are: "scheduled", "cancelled", "succeeded", "failed", and "providerCancelled".'
        reservationStartTime:
          type: string
          format: date-time
          description: Reservation start time of a contact (ISO 8601 UTC standard). Required.
        reservationEndTime:
          type: string
          format: date-time
          description: Reservation end time of a contact (ISO 8601 UTC standard). Required.
        rxStartTime:
          type: string
          format: date-time
          description: Receive start time of a contact (ISO 8601 UTC standard).
        rxEndTime:
          type: string
          format: date-time
          description: Receive end time of a contact (ISO 8601 UTC standard).
        txStartTime:
          type: string
          format: date-time
          description: Transmit start time of a contact (ISO 8601 UTC standard).
        txEndTime:
          type: string
          format: date-time
          description: Transmit end time of a contact (ISO 8601 UTC standard).
        errorMessage:
          type: string
          description: Any error message while scheduling a contact.
        maximumElevationDegrees:
          type: number
          description: Maximum elevation of the antenna during the contact in decimal degrees.
        startAzimuthDegrees:
          type: number
          description: Azimuth of the antenna at the start of the contact in decimal degrees.
        endAzimuthDegrees:
          type: number
          description: Azimuth of the antenna at the end of the contact in decimal degrees.
        groundStationName:
          type: string
          description: Azure Ground Station name. Required.
        startElevationDegrees:
          type: number
          description: Spacecraft elevation above the horizon at contact start.
        endElevationDegrees:
          type: number
          description: Spacecraft elevation above the horizon at contact end.
        antennaConfiguration:
          $ref: "#/components/schemas/ContactsPropertiesAntennaConfiguration"
          description: The configuration associated with the allocated antenna.
        contactProfile:
          $ref: "#/components/schemas/ContactsPropertiesContactProfile"
          description: The reference to the contact profile resource. Required.
    PagedContactFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ContactFlattened"
        nextLink:
          type: string
    Contact:
      type: object
      description: Customer creates a contact resource for a spacecraft resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            provisioningState:
              type: string
              description: "The current state of the resource's creation, deletion, or modification. Known values are: \"creating\", \"succeeded\", \"failed\", \"canceled\", \"updating\", and \"deleting\"."
            status:
              type: string
              description: 'Status of a contact. Known values are: "scheduled", "cancelled", "succeeded", "failed", and "providerCancelled".'
            reservationStartTime:
              type: string
              format: date-time
              description: Reservation start time of a contact (ISO 8601 UTC standard). Required.
            reservationEndTime:
              type: string
              format: date-time
              description: Reservation end time of a contact (ISO 8601 UTC standard). Required.
            rxStartTime:
              type: string
              format: date-time
              description: Receive start time of a contact (ISO 8601 UTC standard).
            rxEndTime:
              type: string
              format: date-time
              description: Receive end time of a contact (ISO 8601 UTC standard).
            txStartTime:
              type: string
              format: date-time
              description: Transmit start time of a contact (ISO 8601 UTC standard).
            txEndTime:
              type: string
              format: date-time
              description: Transmit end time of a contact (ISO 8601 UTC standard).
            errorMessage:
              type: string
              description: Any error message while scheduling a contact.
            maximumElevationDegrees:
              type: number
              description: Maximum elevation of the antenna during the contact in decimal degrees.
            startAzimuthDegrees:
              type: number
              description: Azimuth of the antenna at the start of the contact in decimal degrees.
            endAzimuthDegrees:
              type: number
              description: Azimuth of the antenna at the end of the contact in decimal degrees.
            groundStationName:
              type: string
              description: Azure Ground Station name. Required.
            startElevationDegrees:
              type: number
              description: Spacecraft elevation above the horizon at contact start.
            endElevationDegrees:
              type: number
              description: Spacecraft elevation above the horizon at contact end.
            antennaConfiguration:
              $ref: "#/components/schemas/ContactsPropertiesAntennaConfiguration"
              description: The configuration associated with the allocated antenna.
            contactProfile:
              $ref: "#/components/schemas/ContactsPropertiesContactProfile"
              description: The reference to the contact profile resource. Required.
      required:
        - properties
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    OperationResultErrorProperties:
      type: object
      description: Operation result error properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        code:
          type: string
          description: The code of the error.
        message:
          type: string
          description: The error message.
    OperationResult:
      type: object
      description: Operation Result Entity. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: ID of the resource.
        name:
          type: string
          description: Name of the resource.
        status:
          type: string
          description: 'The status of operation. Known values are: "Succeeded", "Canceled", "Failed", and "Running".'
        startTime:
          type: string
          format: date-time
          description: The operation start time (ISO 8601 UTC standard).
        endTime:
          type: string
          format: date-time
          description: The operation end time (ISO 8601 UTC standard).
        percentComplete:
          type: number
          description: Percentage completed.
        value:
          type: array
          items:
            type: object
            properties: {}
          description: A list of results when the operation returns multiple results.
        nextLink:
          type: string
          description: The URL to get the next set of results.
        properties:
          type: object
          properties: {}
          description: Operation result properties.
        error:
          $ref: "#/components/schemas/OperationResultErrorProperties"
          description: Operation result error properties.
    SpacecraftLink:
      type: object
      description: List of authorized spacecraft links per ground station and the expiration date of the authorization. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: Link name. Required.
        centerFrequencyMHz:
          type: number
          description: Center Frequency in MHz. Required.
        bandwidthMHz:
          type: number
          description: Bandwidth in MHz. Required.
        direction:
          type: string
          description: 'Direction (Uplink or Downlink). Required. Known values are: "Uplink" and "Downlink".'
        polarization:
          type: string
          description: 'Polarization. e.g. (RHCP, LHCP). Required. Known values are: "RHCP", "LHCP", "linearVertical", and "linearHorizontal".'
        authorizations:
          type: array
          items:
            $ref: "#/components/schemas/AuthorizedGroundstation"
          description: Authorized Ground Stations.
      required:
        - name
        - centerFrequencyMHz
        - bandwidthMHz
        - direction
        - polarization
    AuthorizedGroundstation:
      type: object
      description: Authorized groundstation. All required parameters must be populated in order to send to Azure.
      properties:
        groundStation:
          type: string
          description: Groundstation name. Required.
        expirationDate:
          type: string
          format: date
          description: Date of authorization expiration. Required.
      required:
        - groundStation
        - expirationDate
    SpacecraftFlattened:
      type: object
      description: Flattened view of Spacecraft ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        provisioningState:
          type: string
          description: "The current state of the resource's creation, deletion, or modification. Known values are: \"creating\", \"succeeded\", \"failed\", \"canceled\", \"updating\", and \"deleting\"."
        noradId:
          type: string
          description: NORAD ID of the spacecraft.
        titleLine:
          type: string
          description: Title line of the two-line element set (TLE). Required.
        tleLine1:
          type: string
          description: Line 1 of the two-line element set (TLE). Required.
        tleLine2:
          type: string
          description: Line 2 of the two-line element set (TLE). Required.
        links:
          type: array
          items:
            $ref: "#/components/schemas/SpacecraftLink"
          description: Immutable list of Spacecraft links. Required.
    PagedSpacecraftFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SpacecraftFlattened"
        nextLink:
          type: string
    Spacecraft:
      type: object
      description: Customer creates a spacecraft resource to schedule a contact. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          type: object
          properties:
            provisioningState:
              type: string
              description: "The current state of the resource's creation, deletion, or modification. Known values are: \"creating\", \"succeeded\", \"failed\", \"canceled\", \"updating\", and \"deleting\"."
            noradId:
              type: string
              description: NORAD ID of the spacecraft.
            titleLine:
              type: string
              description: Title line of the two-line element set (TLE). Required.
            tleLine1:
              type: string
              description: Line 1 of the two-line element set (TLE). Required.
            tleLine2:
              type: string
              description: Line 2 of the two-line element set (TLE). Required.
            links:
              type: array
              items:
                $ref: "#/components/schemas/SpacecraftLink"
              description: Immutable list of Spacecraft links. Required.
      required:
        - location
        - properties
    AvailableContacts:
      type: object
      description: Customer retrieves list of Available Contacts for a spacecraft resource. Later, one of the available contact can be selected to create a contact. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        spacecraft:
          $ref: "#/components/schemas/AvailableContactsSpacecraft"
          description: The reference to the spacecraft resource.
        groundStationName:
          type: string
          description: Name of Azure Ground Station.
        properties:
          type: object
          properties:
            maximumElevationDegrees:
              type: number
              description: Maximum elevation of the antenna during the contact in decimal degrees.
            txStartTime:
              type: string
              format: date-time
              description: Time at which antenna transmit will be enabled (ISO 8601 UTC standard).
            txEndTime:
              type: string
              format: date-time
              description: Time at which antenna transmit will be disabled (ISO 8601 UTC standard).
            rxStartTime:
              type: string
              format: date-time
              description: Earliest time to receive a signal (ISO 8601 UTC standard).
            rxEndTime:
              type: string
              format: date-time
              description: Time to lost receiving a signal (ISO 8601 UTC standard).
            startAzimuthDegrees:
              type: number
              description: Azimuth of the antenna at the start of the contact in decimal degrees.
            endAzimuthDegrees:
              type: number
              description: Azimuth of the antenna at the end of the contact in decimal degrees.
            startElevationDegrees:
              type: number
              description: Spacecraft elevation above the horizon at contact start.
            endElevationDegrees:
              type: number
              description: Spacecraft elevation above the horizon at contact end.
    AvailableContactsSpacecraft:
      type: object
      description: The reference to the spacecraft resource. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Resource ID. Required.
      required:
        - id
    PagedAvailableContacts:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AvailableContacts"
        nextLink:
          type: string
  x-stackQL-resources:
    available_ground_stations:
      id: azure.orbital.available_ground_stations
      name: available_ground_stations
      title: available_ground_stations
      methods:
        list_by_capability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Orbital~1availableGroundStations?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/available_ground_stations/methods/list_by_capability"
        insert: []
        update: []
        delete: []
    contact_profiles:
      id: azure.orbital.contact_profiles
      name: contact_profiles
      title: contact_profiles
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1contactProfiles~1{contact_profile_name}?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1contactProfiles~1{contact_profile_name}?api-version=2022-11-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1contactProfiles~1{contact_profile_name}?api-version=2022-11-01/delete"
          response:
            openAPIDocKey: "204"
        update_tags:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1contactProfiles~1{contact_profile_name}?api-version=2022-11-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1contactProfiles?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Orbital~1contactProfiles?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/contact_profiles/methods/get"
          - $ref: "#/components/x-stackQL-resources/contact_profiles/methods/list"
          - $ref: "#/components/x-stackQL-resources/contact_profiles/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/contact_profiles/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/contact_profiles/methods/update_tags"
        replace:
          - $ref: "#/components/x-stackQL-resources/contact_profiles/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/contact_profiles/methods/delete"
    contacts:
      id: azure.orbital.contacts
      name: contacts
      title: contacts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}~1contacts~1{contact_name}?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}~1contacts~1{contact_name}?api-version=2022-11-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}~1contacts~1{contact_name}?api-version=2022-11-01/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}~1contacts?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/contacts/methods/get"
          - $ref: "#/components/x-stackQL-resources/contacts/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/contacts/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/contacts/methods/delete"
    operations:
      id: azure.orbital.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Orbital~1operations?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    operations_results:
      id: azure.orbital.operations_results
      name: operations_results
      title: operations_results
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Orbital~1locations~1{location}~1operationResults~1{operation_id}?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations_results/methods/get"
        insert: []
        update: []
        delete: []
    spacecrafts:
      id: azure.orbital.spacecrafts
      name: spacecrafts
      title: spacecrafts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}?api-version=2022-11-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}?api-version=2022-11-01/delete"
          response:
            openAPIDocKey: "204"
        update_tags:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}?api-version=2022-11-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Orbital~1spacecrafts?api-version=2022-11-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_available_contacts:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Orbital~1spacecrafts~1{spacecraft_name}~1listAvailableContacts?api-version=2022-11-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/spacecrafts/methods/get"
          - $ref: "#/components/x-stackQL-resources/spacecrafts/methods/list"
          - $ref: "#/components/x-stackQL-resources/spacecrafts/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/spacecrafts/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/spacecrafts/methods/update_tags"
        replace:
          - $ref: "#/components/x-stackQL-resources/spacecrafts/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/spacecrafts/methods/delete"
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.Orbital/availableGroundStations?api-version=2022-11-01:
    get:
      operationId: AvailableGroundStations_list_by_capability
      description: Returns list of available ground stations.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: capability
          in: query
          required: true
          schema:
            type: string
          description: 'Ground Station Capability. Known values are: "EarthObservation" and "Communication". Required.'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAvailableGroundStationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/contactProfiles/{contact_profile_name}?api-version=2022-11-01:
    get:
      operationId: ContactProfiles_get
      description: Gets the specified contact Profile in a specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: contact_profile_name
          in: path
          required: true
          schema:
            type: string
          description: Contact Profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactProfileFlattened"
    put:
      operationId: ContactProfiles_begin_create_or_update
      description: Creates or updates a contact profile.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: contact_profile_name
          in: path
          required: true
          schema:
            type: string
          description: Contact Profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subnetId:
                  type: string
                  description: ARM resource identifier of the subnet delegated to the Microsoft.Orbital/orbitalGateways. Needs to be at least a class C subnet, and should not have any IP created in it. Required.
              required:
                - subnetId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactProfile"
    delete:
      operationId: ContactProfiles_begin_delete
      description: Deletes a specified contact profile resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: contact_profile_name
          in: path
          required: true
          schema:
            type: string
          description: Contact Profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: ContactProfiles_begin_update_tags
      description: Updates the specified contact profile tags.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: contact_profile_name
          in: path
          required: true
          schema:
            type: string
          description: Contact Profile name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactProfile"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/contactProfiles?api-version=2022-11-01:
    get:
      operationId: ContactProfiles_list
      description: Returns list of contact profiles by Resource Group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: An opaque string that the resource provider uses to skip over previously-returned results. This is used when a previous list operation call returned a partial result. If a previous response contains a nextLink element, the value of the nextLink element will include a skiptoken parameter that specifies a starting point to use for subsequent calls. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedContactProfileFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Orbital/contactProfiles?api-version=2022-11-01:
    get:
      operationId: ContactProfiles_list_by_subscription
      description: Returns list of contact profiles by Subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: An opaque string that the resource provider uses to skip over previously-returned results. This is used when a previous list operation call returned a partial result. If a previous response contains a nextLink element, the value of the nextLink element will include a skiptoken parameter that specifies a starting point to use for subsequent calls. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedContactProfileFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/spacecrafts/{spacecraft_name}/contacts/{contact_name}?api-version=2022-11-01:
    get:
      operationId: Contacts_get
      description: Gets the specified contact in a specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: contact_name
          in: path
          required: true
          schema:
            type: string
          description: Contact name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactFlattened"
    put:
      operationId: Contacts_begin_create
      description: Creates a contact.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: contact_name
          in: path
          required: true
          schema:
            type: string
          description: Contact name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    provisioningState:
                      type: string
                      description: "The current state of the resource's creation, deletion, or modification. Known values are: \"creating\", \"succeeded\", \"failed\", \"canceled\", \"updating\", and \"deleting\"."
                    reservationStartTime:
                      type: string
                      format: date-time
                      description: Reservation start time of a contact (ISO 8601 UTC standard). Required.
                    reservationEndTime:
                      type: string
                      format: date-time
                      description: Reservation end time of a contact (ISO 8601 UTC standard). Required.
                    groundStationName:
                      type: string
                      description: Azure Ground Station name. Required.
                    contactProfile:
                      $ref: "#/components/schemas/ContactsPropertiesContactProfile"
                      description: The reference to the contact profile resource. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contact"
    delete:
      operationId: Contacts_begin_delete
      description: Deletes a specified contact.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: contact_name
          in: path
          required: true
          schema:
            type: string
          description: Contact name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/spacecrafts/{spacecraft_name}/contacts?api-version=2022-11-01:
    get:
      operationId: Contacts_list
      description: Returns list of contacts by spacecraftName.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: An opaque string that the resource provider uses to skip over previously-returned results. This is used when a previous list operation call returned a partial result. If a previous response contains a nextLink element, the value of the nextLink element will include a skiptoken parameter that specifies a starting point to use for subsequent calls. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedContactFlattened"
  /providers/Microsoft.Orbital/operations?api-version=2022-11-01:
    get:
      operationId: orbital_list
      description: Lists all of the available Orbital Rest API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/providers/Microsoft.Orbital/locations/{location}/operationResults/{operation_id}?api-version=2022-11-01:
    get:
      operationId: OperationsResults_begin_get
      description: Returns operation results.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: operation_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of an ongoing async operation. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/spacecrafts/{spacecraft_name}?api-version=2022-11-01:
    get:
      operationId: Spacecrafts_get
      description: Gets the specified spacecraft in a specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpacecraftFlattened"
    put:
      operationId: Spacecrafts_begin_create_or_update
      description: Creates or updates a spacecraft resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Link name. Required.
                centerFrequencyMHz:
                  type: number
                  description: Center Frequency in MHz. Required.
                bandwidthMHz:
                  type: number
                  description: Bandwidth in MHz. Required.
                direction:
                  type: string
                  description: 'Direction (Uplink or Downlink). Required. Known values are: "Uplink" and "Downlink".'
                polarization:
                  type: string
                  description: 'Polarization. e.g. (RHCP, LHCP). Required. Known values are: "RHCP", "LHCP", "linearVertical", and "linearHorizontal".'
              required:
                - name
                - centerFrequencyMHz
                - bandwidthMHz
                - direction
                - polarization
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Spacecraft"
    delete:
      operationId: Spacecrafts_begin_delete
      description: Deletes a specified spacecraft resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Spacecrafts_begin_update_tags
      description: Updates the specified spacecraft tags.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Spacecraft"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/spacecrafts?api-version=2022-11-01:
    get:
      operationId: Spacecrafts_list
      description: Returns list of spacecrafts by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: An opaque string that the resource provider uses to skip over previously-returned results. This is used when a previous list operation call returned a partial result. If a previous response contains a nextLink element, the value of the nextLink element will include a skiptoken parameter that specifies a starting point to use for subsequent calls. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSpacecraftFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.Orbital/spacecrafts?api-version=2022-11-01:
    get:
      operationId: Spacecrafts_list_by_subscription
      description: Returns list of spacecrafts by subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: An opaque string that the resource provider uses to skip over previously-returned results. This is used when a previous list operation call returned a partial result. If a previous response contains a nextLink element, the value of the nextLink element will include a skiptoken parameter that specifies a starting point to use for subsequent calls. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSpacecraftFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Orbital/spacecrafts/{spacecraft_name}/listAvailableContacts?api-version=2022-11-01:
    post:
      operationId: Spacecrafts_begin_list_available_contacts
      description: Returns list of available contacts. A contact is available if the spacecraft is visible from the ground station for more than the minimum viable contact duration provided in the contact profile.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: spacecraft_name
          in: path
          required: true
          schema:
            type: string
          description: Spacecraft ID. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: Resource ID. Required.
              required:
                - id
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAvailableContacts"
