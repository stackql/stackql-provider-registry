openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Machine Learning Compute Management Client
  description: These APIs allow end users to operate on Azure Machine Learning Compute resources. They support the following operations:Create or update a clusterGet a clusterPatch a clusterDelete a clusterGet keys for a clusterCheck if updates are available for system services in a clusterUpdate system services in a clusterGet all clusters in a resource groupGet all clusters in a subscription.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2017-08-01-preview-stackql-generated
  x-serviceAlias: machinelearningcompute
  x-sdkPackage: azure-mgmt-machinelearningcompute
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    ResourceOperation:
      type: object
      description: Resource operation.
      properties:
        name:
          type: string
          description: Name of this operation.
        display:
          $ref: "#/components/schemas/ResourceOperationDisplay"
          description: Display of the operation.
        origin:
          type: string
          description: The operation origin.
    ResourceOperationDisplay:
      type: object
      description: Display of the operation.
      properties:
        provider:
          type: string
          description: The resource provider name.
        resource:
          type: string
          description: The resource name.
        operation:
          type: string
          description: The operation.
        description:
          type: string
          description: The description of the operation.
    AvailableOperations:
      type: object
      description: Available operation list.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourceOperation"
          description: An array of available operations.
    ErrorResponseWrapper:
      type: object
      description: Wrapper for error response to follow ARM guidelines.
      properties:
        error:
          $ref: "#/components/schemas/ErrorResponse"
          description: The error response.
    ErrorResponse:
      type: object
      description: Error response information. All required parameters must be populated in order to send to Azure.
      properties:
        code:
          type: string
          description: Error code. Required.
        message:
          type: string
          description: Error message. Required.
        details:
          type: array
          items:
            $ref: "#/components/schemas/ErrorDetail"
          description: An array of error detail objects.
      required:
        - code
        - message
    ErrorDetail:
      type: object
      description: Error detail information. All required parameters must be populated in order to send to Azure.
      properties:
        code:
          type: string
          description: Error code. Required.
        message:
          type: string
          description: Error message. Required.
      required:
        - code
        - message
    StorageAccountProperties:
      type: object
      description: Properties of Storage Account.
      properties:
        resourceId:
          type: string
          description: ARM resource ID of the Azure Storage Account to store CLI specific files. If not provided one will be created. This cannot be changed once the cluster is created.
    ContainerRegistryProperties:
      type: object
      description: Properties of Azure Container Registry.
      properties:
        resourceId:
          type: string
          description: ARM resource ID of the Azure Container Registry used to store Docker images for web services in the cluster. If not provided one will be created. This cannot be changed once the cluster is created.
    AcsClusterProperties:
      type: object
      description: Information about the container service backing the cluster. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        clusterFqdn:
          type: string
          description: The FQDN of the cluster.
        orchestratorType:
          type: string
          description: 'Type of orchestrator. It cannot be changed once the cluster is created. Required. Known values are: "Kubernetes" and "None".'
        orchestratorProperties:
          $ref: "#/components/schemas/KubernetesClusterProperties"
          description: Orchestrator specific properties.
        systemServices:
          type: array
          items:
            $ref: "#/components/schemas/SystemService"
          description: The system services deployed to the cluster.
        masterCount:
          type: integer
          description: The number of master nodes in the container service.
        agentCount:
          type: integer
          description: The number of agent nodes in the Container Service. This can be changed to scale the cluster.
        agentVmSize:
          type: string
          description: 'The Azure VM size of the agent VM nodes. This cannot be changed once the cluster is created. This list is non exhaustive; refer to https://docs.microsoft.com/en-us/azure/virtual-machines/windows/sizes for the possible VM sizes. Known values are: "Standard_A0", "Standard_A1", "Standard_A2", "Standard_A3", "Standard_A4", "Standard_A5", "Standard_A6", "Standard_A7", "Standard_A8", "Standard_A9", "Standard_A10", "Standard_A11", "Standard_D1", "Standard_D2", "Standard_D3", "Standard_D4", "Standard_D11", "Standard_D12", "Standard_D13", "Standard_D14", "Standard_D1_v2", "Standard_D2_v2", "Standard_D3_v2", "Standard_D4_v2", "Standard_D5_v2", "Standard_D11_v2", "Standard_D12_v2", "Standard_D13_v2", "Standard_D14_v2", "Standard_G1", "Standard_G2", "Standard_G3", "Standard_G4", "Standard_G5", "Standard_DS1", "Standard_DS2", "Standard_DS3", "Standard_DS4", "Standard_DS11", "Standard_DS12", "Standard_DS13", "Standard_DS14", "Standard_GS1", "Standard_GS2", "Standard_GS3", "Standard_GS4", and "Standard_GS5".'
      required:
        - orchestratorType
    KubernetesClusterProperties:
      type: object
      description: Kubernetes cluster specific properties.
      properties:
        servicePrincipal:
          $ref: "#/components/schemas/ServicePrincipalProperties"
          description: The Azure Service Principal used by Kubernetes.
    ServicePrincipalProperties:
      type: object
      description: The Azure service principal used by Kubernetes for configuring load balancers. All required parameters must be populated in order to send to Azure.
      properties:
        clientId:
          type: string
          description: The service principal client ID. Required.
        secret:
          type: string
          description: The service principal secret. This is not returned in response of GET/PUT on the resource. To see this please call listKeys. Required.
      required:
        - clientId
        - secret
    SystemService:
      type: object
      description: Information about a system service deployed in the cluster. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        systemServiceType:
          type: string
          description: 'The system service type. Required. Known values are: "None", "ScoringFrontEnd", and "BatchFrontEnd".'
        publicIpAddress:
          type: string
          description: The public IP address of the system service.
        version:
          type: string
          description: The state of the system service.
      required:
        - systemServiceType
    AppInsightsProperties:
      type: object
      description: Properties of App Insights.
      properties:
        resourceId:
          type: string
          description: ARM resource ID of the App Insights.
    GlobalServiceConfiguration:
      type: object
      description: Global configuration for services in the cluster.
      properties:
        "":
          type: object
          properties: {}
          description: Unmatched properties from the message are deserialized to this collection.
        etag:
          type: string
          description: The configuration ETag for updates.
        ssl:
          $ref: "#/components/schemas/SslConfiguration"
          description: The SSL configuration properties.
        serviceAuth:
          $ref: "#/components/schemas/ServiceAuthConfiguration"
          description: Optional global authorization keys for all user services deployed in cluster. These are used if the service does not have auth keys.
        autoScale:
          $ref: "#/components/schemas/AutoScaleConfiguration"
          description: The auto-scale configuration.
    SslConfiguration:
      type: object
      description: SSL configuration. If configured data-plane calls to user services will be exposed over SSL only.
      properties:
        status:
          type: string
          description: 'SSL status. Allowed values are Enabled and Disabled. Known values are: "Enabled" and "Disabled".'
        cert:
          type: string
          description: The SSL cert data in PEM format.
        key:
          type: string
          description: The SSL key data in PEM format. This is not returned in response of GET/PUT on the resource. To see this please call listKeys API.
        cname:
          type: string
          description: The CName of the certificate.
    ServiceAuthConfiguration:
      type: object
      description: Global service auth configuration properties. These are the data-plane authorization keys and are used if a service doesn't define it's own. All required parameters must be populated in order to send to Azure.
      properties:
        primaryAuthKeyHash:
          type: string
          description: The primary auth key hash. This is not returned in response of GET/PUT on the resource.. To see this please call listKeys API. Required.
        secondaryAuthKeyHash:
          type: string
          description: The secondary auth key hash. This is not returned in response of GET/PUT on the resource.. To see this please call listKeys API. Required.
      required:
        - primaryAuthKeyHash
        - secondaryAuthKeyHash
    AutoScaleConfiguration:
      type: object
      description: AutoScale configuration properties.
      properties:
        status:
          type: string
          description: 'If auto-scale is enabled for all services. Each service can turn it off individually. Known values are: "Enabled" and "Disabled".'
        minReplicas:
          type: integer
          description: The minimum number of replicas for each service.
        maxReplicas:
          type: integer
          description: The maximum number of replicas for each service.
        targetUtilization:
          type: number
          description: The target utilization.
        refreshPeriodInSeconds:
          type: integer
          description: Refresh period in seconds.
    OperationalizationClusterFlattened:
      type: object
      description: Flattened view of OperationalizationCluster ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Specifies the resource ID.
        name:
          type: string
          description: Specifies the name of the resource.
        location:
          type: string
          description: Specifies the location of the resource. Required.
        type:
          type: string
          description: Specifies the type of the resource.
        tags:
          type: object
          properties: {}
          description: Contains resource tags defined as key/value pairs.
        description:
          type: string
          description: The description of the cluster.
        createdOn:
          type: string
          format: date-time
          description: The date and time when the cluster was created.
        modifiedOn:
          type: string
          format: date-time
          description: The date and time when the cluster was last modified.
        provisioningState:
          type: string
          description: 'The provision state of the cluster. Valid values are Unknown, Updating, Provisioning, Succeeded, and Failed. Known values are: "Unknown", "Updating", "Creating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        provisioningErrors:
          type: array
          items:
            $ref: "#/components/schemas/ErrorResponseWrapper"
          description: List of provisioning errors reported by the resource provider.
        clusterType:
          type: string
          description: 'The cluster type. Known values are: "ACS" and "Local".'
        storageAccount:
          $ref: "#/components/schemas/StorageAccountProperties"
          description: Storage Account properties.
        containerRegistry:
          $ref: "#/components/schemas/ContainerRegistryProperties"
          description: Container Registry properties.
        containerService:
          $ref: "#/components/schemas/AcsClusterProperties"
          description: Parameters for the Azure Container Service cluster.
        appInsights:
          $ref: "#/components/schemas/AppInsightsProperties"
          description: AppInsights configuration.
        globalServiceConfiguration:
          $ref: "#/components/schemas/GlobalServiceConfiguration"
          description: Contains global configuration for the web services in the cluster.
    PagedOperationalizationClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OperationalizationClusterFlattened"
        nextLink:
          type: string
    OperationalizationCluster:
      type: object
      description: Instance of an Azure ML Operationalization Cluster resource. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Specifies the resource ID.
        name:
          type: string
          description: Specifies the name of the resource.
        location:
          type: string
          description: Specifies the location of the resource. Required.
        type:
          type: string
          description: Specifies the type of the resource.
        tags:
          type: object
          properties: {}
          description: Contains resource tags defined as key/value pairs.
        properties:
          type: object
          properties:
            description:
              type: string
              description: The description of the cluster.
            createdOn:
              type: string
              format: date-time
              description: The date and time when the cluster was created.
            modifiedOn:
              type: string
              format: date-time
              description: The date and time when the cluster was last modified.
            provisioningState:
              type: string
              description: 'The provision state of the cluster. Valid values are Unknown, Updating, Provisioning, Succeeded, and Failed. Known values are: "Unknown", "Updating", "Creating", "Deleting", "Succeeded", "Failed", and "Canceled".'
            provisioningErrors:
              type: array
              items:
                $ref: "#/components/schemas/ErrorResponseWrapper"
              description: List of provisioning errors reported by the resource provider.
            clusterType:
              type: string
              description: 'The cluster type. Known values are: "ACS" and "Local".'
            storageAccount:
              $ref: "#/components/schemas/StorageAccountProperties"
              description: Storage Account properties.
            containerRegistry:
              $ref: "#/components/schemas/ContainerRegistryProperties"
              description: Container Registry properties.
            containerService:
              $ref: "#/components/schemas/AcsClusterProperties"
              description: Parameters for the Azure Container Service cluster.
            appInsights:
              $ref: "#/components/schemas/AppInsightsProperties"
              description: AppInsights configuration.
            globalServiceConfiguration:
              $ref: "#/components/schemas/GlobalServiceConfiguration"
              description: Contains global configuration for the web services in the cluster.
      required:
        - location
    OperationalizationClusterCredentials:
      type: object
      description: Credentials to resources in the cluster.
      properties:
        storageAccount:
          $ref: "#/components/schemas/StorageAccountCredentials"
          description: Credentials for the Storage Account.
        containerRegistry:
          $ref: "#/components/schemas/ContainerRegistryCredentials"
          description: Credentials for Azure Container Registry.
        containerService:
          $ref: "#/components/schemas/ContainerServiceCredentials"
          description: Credentials for Azure Container Service.
        appInsights:
          $ref: "#/components/schemas/AppInsightsCredentials"
          description: Credentials for Azure AppInsights.
        serviceAuthConfiguration:
          $ref: "#/components/schemas/ServiceAuthConfiguration"
          description: Global authorization keys for all user services deployed in cluster. These are used if the service does not have auth keys.
        sslConfiguration:
          $ref: "#/components/schemas/SslConfiguration"
          description: The SSL configuration for the services.
    StorageAccountCredentials:
      type: object
      description: Access information for the storage account. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        resourceId:
          type: string
          description: The ARM resource ID of the storage account.
        primaryKey:
          type: string
          description: The primary key of the storage account.
        secondaryKey:
          type: string
          description: The secondary key of the storage account.
    ContainerRegistryCredentials:
      type: object
      description: Information about the Azure Container Registry which contains the images deployed to the cluster. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        loginServer:
          type: string
          description: The ACR login server name. User name is the first part of the FQDN.
        password:
          type: string
          description: The ACR primary password.
        password2:
          type: string
          description: The ACR secondary password.
        username:
          type: string
          description: The ACR login username.
    ContainerServiceCredentials:
      type: object
      description: Information about the Azure Container Registry which contains the images deployed to the cluster. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        acsKubeConfig:
          type: string
          description: The ACS kube config file.
        servicePrincipalConfiguration:
          $ref: "#/components/schemas/ServicePrincipalProperties"
          description: Service principal configuration used by Kubernetes.
        imagePullSecretName:
          type: string
          description: The ACR image pull secret name which was created in Kubernetes.
    AppInsightsCredentials:
      type: object
      description: AppInsights credentials.
      properties:
        appId:
          type: string
          description: The AppInsights application ID.
        instrumentationKey:
          type: string
          description: The AppInsights instrumentation key. This is not returned in response of GET/PUT on the resource. To see this please call listKeys API.
    CheckSystemServicesUpdatesAvailableResponse:
      type: object
      description: Information about updates available for system services in a cluster. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        updatesAvailable:
          type: string
          description: 'Yes if updates are available for the system services, No if not. Known values are: "Yes" and "No".'
    UpdateSystemServicesResponse:
      type: object
      description: Response of the update system services API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        updateStatus:
          type: string
          description: 'Update status. Known values are: "Unknown", "Updating", "Creating", "Deleting", "Succeeded", "Failed", and "Canceled".'
        updateStartedOn:
          type: string
          format: date-time
          description: The date and time when the last system services update was started.
        updateCompletedOn:
          type: string
          format: date-time
          description: The date and time when the last system services update completed.
  x-stackQL-resources:
    machine_learning_compute:
      id: azure.machinelearningcompute.machine_learning_compute
      name: machine_learning_compute
      title: machine_learning_compute
      methods:
        list_available_operations:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.MachineLearningCompute~1operations?api-version=2017-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/machine_learning_compute/methods/list_available_operations"
        insert: []
        update: []
        delete: []
    operationalization_clusters:
      id: azure.machinelearningcompute.operationalization_clusters
      name: operationalization_clusters
      title: operationalization_clusters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters~1{cluster_name}?api-version=2017-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters~1{cluster_name}?api-version=2017-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters~1{cluster_name}?api-version=2017-08-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters~1{cluster_name}?api-version=2017-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters?api-version=2017-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_subscription_id:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters?api-version=2017-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_keys:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters~1{cluster_name}~1listKeys?api-version=2017-08-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        check_system_services_updates_available:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters~1{cluster_name}~1checkSystemServicesUpdatesAvailable?api-version=2017-08-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        update_system_services:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.MachineLearningCompute~1operationalizationClusters~1{cluster_name}~1updateSystemServices?api-version=2017-08-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operationalization_clusters/methods/get"
          - $ref: "#/components/x-stackQL-resources/operationalization_clusters/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/operationalization_clusters/methods/list_by_subscription_id"
        insert:
          - $ref: "#/components/x-stackQL-resources/operationalization_clusters/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/operationalization_clusters/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/operationalization_clusters/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/operationalization_clusters/methods/delete"
paths:
  /providers/Microsoft.MachineLearningCompute/operations?api-version=2017-08-01-preview:
    get:
      operationId: MachineLearningCompute_list_available_operations
      description: Gets all available operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailableOperations"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.MachineLearningCompute/operationalizationClusters/{cluster_name}?api-version=2017-08-01-preview:
    get:
      operationId: OperationalizationClusters_get
      description: Gets the operationalization cluster resource view. Note that the credentials are not returned by this call. Call ListKeys to get them.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationalizationClusterFlattened"
    put:
      operationId: OperationalizationClusters_begin_create_or_update
      description: Create or update an operationalization cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Specifies the location of the resource. Required.
                tags:
                  type: object
                  properties: {}
                  description: Contains resource tags defined as key/value pairs.
                properties:
                  type: object
                  properties:
                    description:
                      type: string
                      description: The description of the cluster.
                    clusterType:
                      type: string
                      description: 'The cluster type. Known values are: "ACS" and "Local".'
                    storageAccount:
                      $ref: "#/components/schemas/StorageAccountProperties"
                      description: Storage Account properties.
                    containerRegistry:
                      $ref: "#/components/schemas/ContainerRegistryProperties"
                      description: Container Registry properties.
                    containerService:
                      $ref: "#/components/schemas/AcsClusterProperties"
                      description: Parameters for the Azure Container Service cluster.
                    appInsights:
                      $ref: "#/components/schemas/AppInsightsProperties"
                      description: AppInsights configuration.
                    globalServiceConfiguration:
                      $ref: "#/components/schemas/GlobalServiceConfiguration"
                      description: Contains global configuration for the web services in the cluster.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationalizationCluster"
    patch:
      operationId: OperationalizationClusters_update
      description: The PATCH operation can be used to update only the tags for a cluster. Use PUT operation to update other properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets a list of key value pairs that describe the resource. These tags can be used in viewing and grouping this resource (across resource groups). A maximum of 15 tags can be provided for a resource. Each tag must have a key no greater in length than 128 characters and a value no greater in length than 256 characters.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationalizationCluster"
    delete:
      operationId: OperationalizationClusters_begin_delete
      description: Deletes the specified cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: deleteAll
          in: query
          required: false
          schema:
            type: boolean
          description: If true, deletes all resources associated with this cluster. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.MachineLearningCompute/operationalizationClusters?api-version=2017-08-01-preview:
    get:
      operationId: OperationalizationClusters_list_by_resource_group
      description: Gets the clusters in the specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: Continuation token for pagination. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperationalizationClusterFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.MachineLearningCompute/operationalizationClusters?api-version=2017-08-01-preview:
    get:
      operationId: OperationalizationClusters_list_by_subscription_id
      description: Gets the operationalization clusters in the specified subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $skiptoken
          in: query
          required: false
          schema:
            type: string
          description: Continuation token for pagination. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperationalizationClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.MachineLearningCompute/operationalizationClusters/{cluster_name}/listKeys?api-version=2017-08-01-preview:
    post:
      operationId: OperationalizationClusters_list_keys
      description: Gets the credentials for the specified cluster such as Storage, ACR and ACS credentials. This is a long running operation because it fetches keys from dependencies.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationalizationClusterCredentials"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.MachineLearningCompute/operationalizationClusters/{cluster_name}/checkSystemServicesUpdatesAvailable?api-version=2017-08-01-preview:
    post:
      operationId: OperationalizationClusters_check_system_services_updates_available
      description: Checks if updates are available for system services in the cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckSystemServicesUpdatesAvailableResponse"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.MachineLearningCompute/operationalizationClusters/{cluster_name}/updateSystemServices?api-version=2017-08-01-preview:
    post:
      operationId: OperationalizationClusters_begin_update_system_services
      description: Updates system services in a cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resource group in which the cluster is located. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateSystemServicesResponse"
