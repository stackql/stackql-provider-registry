openapi: 3.0.0
servers:
  - url: https://{endpoint}/language
    variables:
      endpoint:
        default: ""
        description: The service endpoint host (no scheme), e.g. myaccount.table.cosmos.azure.com:443 - value of the client `Endpoint` parameter.
info:
  title: Conversation Analysis Client
  description: The language service conversations API is a suite of natural language processing (NLP) skills that can be used to analyze structured conversations (textual or spoken). The synchronous API in this suite accepts a request and mediates among multiple language projects, such as LUIS Generally Available, Question Answering, Conversational Language Understanding, and then calls the best candidate service to handle the request. At last, it returns a response with the candidate service's response as a payload.\n\n In some cases, this API needs to forward requests and responses between the caller and an upstream service. The asynchronous APIs in this suite enable tasks like Conversation Summarization and Conversational PII detection.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-11-15-preview-stackql-generated
  x-serviceAlias: ai_language_conversations
  x-sdkPackage: azure-ai-language-conversations
  x-plane: data
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    AnalyzeConversationActionResult:
      type: object
      description: "The base class of a conversation input task result. You probably want to use the sub-classes and not this class directly. Known sub-classes are: ConversationActionResult, ConversationalAITaskResult"
      properties:
        kind:
          type: string
          description: 'The base class of a conversation input task result. Required. Known values are: "ConversationResult" and "ConversationalAIResult".'
      required:
        - kind
  x-stackQL-resources:
    analyze_conversations:
      id: azure.ai_language_conversations.analyze_conversations
      name: analyze_conversations
      title: analyze_conversations
      methods:
        analyze_conversation:
          operation:
            $ref: "#/paths/~1:analyze-conversations?api-version=2025-11-15-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
    cancel_jobs:
      id: azure.ai_language_conversations.cancel_jobs
      name: cancel_jobs
      title: cancel_jobs
      methods:
        cancel_job:
          operation:
            $ref: "#/paths/~1analyze-conversations~1jobs~1{job_id}:cancel?api-version=2025-11-15-preview/post"
          response:
            openAPIDocKey: "204"
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
paths:
  /:analyze-conversations?api-version=2025-11-15-preview:
    post:
      operationId: _ConversationAnalysisClient_analyze_conversation
      description: Analyzes the input conversation utterance.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                kind:
                  type: string
                  description: 'The base class of a conversation input task. Required. Known values are: "Conversation" and "ConversationalAI".'
              required:
                - kind
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnalyzeConversationActionResult"
  /analyze-conversations/jobs/{job_id}:cancel?api-version=2025-11-15-preview:
    post:
      operationId: _ConversationAnalysisClient_begin_cancel_job
      description: Cancel a long-running Text Analysis conversations job. Cancel a long-running job for text analysis of conversations.
      parameters:
        - name: job_id
          in: path
          required: true
          schema:
            type: string
          description: The job ID to cancel. Required.
      responses:
        "204":
          description: Success (no content / accepted)
