openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Large Instance Mgmt Client
  description: The AzureLargeInstance Management client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-07-20-preview-stackql-generated
  x-serviceAlias: largeinstance
  x-sdkPackage: azure-mgmt-largeinstance
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    AzureLargeInstanceProperties:
      type: object
      description: Describes the properties of an Azure Large Instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        hardwareProfile:
          $ref: "#/components/schemas/HardwareProfile"
          description: Specifies the hardware settings for the Azure Large Instance.
        storageProfile:
          $ref: "#/components/schemas/StorageProfile"
          description: Specifies the storage settings for the Azure Large Instance disks.
        osProfile:
          $ref: "#/components/schemas/OsProfile"
          description: Specifies the operating system settings for the Azure Large Instance.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Specifies the network settings for the Azure Large Instance.
        azureLargeInstanceId:
          type: string
          description: Specifies the Azure Large Instance unique ID.
        powerState:
          type: string
          description: 'Resource power state. Known values are: "starting", "started", "stopping", "stopped", "restarting", and "unknown".'
        proximityPlacementGroup:
          type: string
          description: Resource proximity placement group.
        hwRevision:
          type: string
          description: Hardware revision of an Azure Large Instance.
        partnerNodeId:
          type: string
          description: ARM ID of another AzureLargeInstance that will share a network with this AzureLargeInstance.
        provisioningState:
          type: string
          description: 'State of provisioning of the AzureLargeInstance. Known values are: "Accepted", "Creating", "Updating", "Failed", "Succeeded", "Deleting", "Migrating", and "Canceled".'
    HardwareProfile:
      type: object
      description: Specifies the hardware settings for the Azure Large Instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        hardwareType:
          type: string
          description: 'Name of the hardware type (vendor and/or their product name). Known values are: "Cisco_UCS", "HPE", and "SDFLEX".'
        azureLargeInstanceSize:
          type: string
          description: 'Specifies the Azure Large Instance SKU. Known values are: "S72m", "S144m", "S72", "S144", "S192", "S192m", "S192xm", "S96", "S112", "S224", "S224m", "S224om", "S224oo", "S224oom", "S224ooo", "S224se", "S384", "S384m", "S384xm", "S384xxm", "S448", "S448m", "S448om", "S448oo", "S448oom", "S448ooo", "S448se", "S576m", "S576xm", "S672", "S672m", "S672om", "S672oo", "S672oom", "S672ooo", "S768", "S768m", "S768xm", "S896", "S896m", "S896om", "S896oo", "S896oom", "S896ooo", and "S960m".'
    StorageProfile:
      type: object
      description: Specifies the storage settings for the Azure Large Instance disks. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        nfsIpAddress:
          type: string
          description: IP Address to connect to storage.
        osDisks:
          type: array
          items:
            $ref: "#/components/schemas/Disk"
          description: Specifies information about the operating system disk used by Azure Large Instance.
    Disk:
      type: object
      description: Specifies the disk information fo the Azure Large Instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The disk name.
        diskSizeGB:
          type: integer
          description: Specifies the size of an empty data disk in gigabytes.
        lun:
          type: integer
          description: Specifies the logical unit number of the data disk. This value is used to identify data disks within the VM and therefore must be unique for each data disk attached to a VM.
    OsProfile:
      type: object
      description: Specifies the operating system settings for the Azure Large Instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        computerName:
          type: string
          description: Specifies the host OS name of the Azure Large Instance.
        osType:
          type: string
          description: This property allows you to specify the type of the OS.
        version:
          type: string
          description: Specifies version of operating system.
        sshPublicKey:
          type: string
          description: Specifies the SSH public key used to access the operating system.
    NetworkProfile:
      type: object
      description: Specifies the network settings for the Azure Large Instance disks. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/IpAddress"
          description: Specifies the network interfaces for the Azure Large Instance.
        circuitId:
          type: string
          description: Specifies the circuit id for connecting to express route.
    IpAddress:
      type: object
      description: Specifies the IP address of the network interface.
      properties:
        ipAddress:
          type: string
          description: Specifies the IP address of the network interface.
    AzureLargeInstanceFlattened:
      type: object
      description: Flattened view of AzureLargeInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        hardwareProfile:
          $ref: "#/components/schemas/HardwareProfile"
          description: Specifies the hardware settings for the Azure Large Instance.
        storageProfile:
          $ref: "#/components/schemas/StorageProfile"
          description: Specifies the storage settings for the Azure Large Instance disks.
        osProfile:
          $ref: "#/components/schemas/OsProfile"
          description: Specifies the operating system settings for the Azure Large Instance.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Specifies the network settings for the Azure Large Instance.
        azureLargeInstanceId:
          type: string
          description: Specifies the Azure Large Instance unique ID.
        powerState:
          type: string
          description: 'Resource power state. Known values are: "starting", "started", "stopping", "stopped", "restarting", and "unknown".'
        proximityPlacementGroup:
          type: string
          description: Resource proximity placement group.
        hwRevision:
          type: string
          description: Hardware revision of an Azure Large Instance.
        partnerNodeId:
          type: string
          description: ARM ID of another AzureLargeInstance that will share a network with this AzureLargeInstance.
        provisioningState:
          type: string
          description: 'State of provisioning of the AzureLargeInstance. Known values are: "Accepted", "Creating", "Updating", "Failed", "Succeeded", "Deleting", "Migrating", and "Canceled".'
    PagedAzureLargeInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AzureLargeInstanceFlattened"
        nextLink:
          type: string
    AzureLargeInstance:
      type: object
      description: Azure Large Instance info on Azure (ARM properties and AzureLargeInstance properties). Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AzureLargeInstanceProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    OperationStatusResult:
      type: object
      description: The current status of an async operation. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified ID for the async operation.
        resourceId:
          type: string
          description: Fully qualified ID of the resource against which the original async operation was started.
        name:
          type: string
          description: Name of the async operation.
        status:
          type: string
          description: Operation status. Required.
        percentComplete:
          type: number
          description: Percent of the operation that is complete.
        startTime:
          type: string
          format: date-time
          description: The start time of the operation.
        endTime:
          type: string
          format: date-time
          description: The end time of the operation.
        operations:
          type: array
          items:
            $ref: "#/components/schemas/OperationStatusResult"
          description: The operations list.
        error:
          $ref: "#/components/schemas/ErrorDetail"
          description: If present, details of the operation error.
      required:
        - status
    ErrorDetail:
      type: object
      description: The error detail. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        code:
          type: string
          description: The error code.
        message:
          type: string
          description: The error message.
        target:
          type: string
          description: The error target.
        details:
          type: array
          items:
            $ref: "#/components/schemas/ErrorDetail"
          description: The error details.
        additionalInfo:
          type: array
          items:
            $ref: "#/components/schemas/ErrorAdditionalInfo"
          description: The error additional info.
    ErrorAdditionalInfo:
      type: object
      description: The resource management error additional info. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        type:
          type: string
          description: The additional info type.
        info:
          type: object
          properties: {}
          description: The additional info.
    AzureLargeStorageInstanceProperties:
      type: object
      description: Describes the properties of an AzureLargeStorageInstance.
      properties:
        azureLargeStorageInstanceUniqueIdentifier:
          type: string
          description: Specifies the AzureLargeStorageInstance unique ID.
        storageProperties:
          $ref: "#/components/schemas/StorageProperties"
          description: Specifies the storage properties for the AzureLargeStorage instance.
    StorageProperties:
      type: object
      description: described the storage properties of the azure large storage instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provisioningState:
          type: string
          description: 'State of provisioning of the AzureLargeStorageInstance. Known values are: "Accepted", "Creating", "Updating", "Failed", "Succeeded", "Deleting", "Canceled", and "Migrating".'
        offeringType:
          type: string
          description: the offering type for which the resource is getting provisioned.
        storageType:
          type: string
          description: the storage protocol for which the resource is getting provisioned.
        generation:
          type: string
          description: the kind of storage instance.
        hardwareType:
          type: string
          description: 'the hardware type of the storage instance. Known values are: "Cisco_UCS", "HPE", and "SDFLEX".'
        workloadType:
          type: string
          description: the workload for which the resource is getting provisioned.
        storageBillingProperties:
          $ref: "#/components/schemas/StorageBillingProperties"
          description: the billing related information for the resource.
    StorageBillingProperties:
      type: object
      description: Describes the billing related details of the AzureLargeStorageInstance.
      properties:
        billingMode:
          type: string
          description: the billing mode for the storage instance.
        sku:
          type: string
          description: the SKU type that is provisioned.
    AzureLargeStorageInstanceFlattened:
      type: object
      description: Flattened view of AzureLargeStorageInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        azureLargeStorageInstanceUniqueIdentifier:
          type: string
          description: Specifies the AzureLargeStorageInstance unique ID.
        storageProperties:
          $ref: "#/components/schemas/StorageProperties"
          description: Specifies the storage properties for the AzureLargeStorage instance.
    PagedAzureLargeStorageInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AzureLargeStorageInstanceFlattened"
        nextLink:
          type: string
    AzureLargeStorageInstance:
      type: object
      description: AzureLargeStorageInstance info on Azure (ARM properties and AzureLargeStorageInstance properties). Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AzureLargeStorageInstanceProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    azure_large_instance:
      id: azure.largeinstance.azure_large_instance
      name: azure_large_instance
      title: azure_large_instance
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeInstances~1{azure_large_instance_name}?api-version=2023-07-20-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeInstances~1{azure_large_instance_name}?api-version=2023-07-20-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzureLargeInstance~1azureLargeInstances?api-version=2023-07-20-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeInstances?api-version=2023-07-20-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        restart:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeInstances~1{azure_large_instance_name}~1restart?api-version=2023-07-20-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        shutdown:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeInstances~1{azure_large_instance_name}~1shutdown?api-version=2023-07-20-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        start:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeInstances~1{azure_large_instance_name}~1start?api-version=2023-07-20-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/azure_large_instance/methods/get"
          - $ref: "#/components/x-stackQL-resources/azure_large_instance/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/azure_large_instance/methods/list_by_subscription"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/azure_large_instance/methods/update"
        delete: []
    azure_large_storage_instance:
      id: azure.largeinstance.azure_large_storage_instance
      name: azure_large_storage_instance
      title: azure_large_storage_instance
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeStorageInstances~1{azure_large_storage_instance_name}?api-version=2023-07-20-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeStorageInstances~1{azure_large_storage_instance_name}?api-version=2023-07-20-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AzureLargeInstance~1azureLargeStorageInstances?api-version=2023-07-20-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AzureLargeInstance~1azureLargeStorageInstances?api-version=2023-07-20-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/azure_large_storage_instance/methods/get"
          - $ref: "#/components/x-stackQL-resources/azure_large_storage_instance/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/azure_large_storage_instance/methods/list_by_subscription"
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/azure_large_storage_instance/methods/update"
        delete: []
    operations:
      id: azure.largeinstance.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AzureLargeInstance~1operations?api-version=2023-07-20-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureLargeInstance/azureLargeInstances/{azure_large_instance_name}?api-version=2023-07-20-preview:
    get:
      operationId: AzureLargeInstance_get
      description: Gets an Azure Large Instance for the specified subscription, resource group, and instance name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_large_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AzureLargeInstance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureLargeInstanceFlattened"
    patch:
      operationId: AzureLargeInstance_update
      description: Patches the Tags field of an Azure Large Instance for the specified subscription, resource group, and instance name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_large_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AzureLargeInstance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureLargeInstance"
  /subscriptions/{subscription_id}/providers/Microsoft.AzureLargeInstance/azureLargeInstances?api-version=2023-07-20-preview:
    get:
      operationId: AzureLargeInstance_list_by_subscription
      description: Gets a list of Azure Large Instances in the specified subscription. The operations returns various properties of each Azure Large Instance.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureLargeInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureLargeInstance/azureLargeInstances?api-version=2023-07-20-preview:
    get:
      operationId: AzureLargeInstance_list_by_resource_group
      description: Gets a list of Azure Large Instances in the specified subscription and resource group. The operations returns various properties of each Azure Large Instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureLargeInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureLargeInstance/azureLargeInstances/{azure_large_instance_name}/restart?api-version=2023-07-20-preview:
    post:
      operationId: AzureLargeInstance_begin_restart
      description: The operation to restart an Azure Large Instance (only for compute instances).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_large_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AzureLargeInstance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                forceState:
                  type: string
                  description: 'Whether to force restart by shutting all processes. Known values are: "active" and "inactive".'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatusResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureLargeInstance/azureLargeInstances/{azure_large_instance_name}/shutdown?api-version=2023-07-20-preview:
    post:
      operationId: AzureLargeInstance_begin_shutdown
      description: The operation to shutdown an Azure Large Instance (only for compute instances).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_large_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AzureLargeInstance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatusResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureLargeInstance/azureLargeInstances/{azure_large_instance_name}/start?api-version=2023-07-20-preview:
    post:
      operationId: AzureLargeInstance_begin_start
      description: The operation to start an Azure Large Instance (only for compute instances).
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_large_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AzureLargeInstance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatusResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureLargeInstance/azureLargeStorageInstances/{azure_large_storage_instance_name}?api-version=2023-07-20-preview:
    get:
      operationId: AzureLargeStorageInstance_get
      description: Gets an Azure Large Storage instance for the specified subscription, resource group, and instance name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_large_storage_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AzureLargeStorageInstance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureLargeStorageInstanceFlattened"
    patch:
      operationId: AzureLargeStorageInstance_update
      description: Patches the Tags field of a Azure Large Storage Instance for the specified subscription, resource group, and instance name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_large_storage_instance_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AzureLargeStorageInstance. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureLargeStorageInstance"
  /subscriptions/{subscription_id}/providers/Microsoft.AzureLargeInstance/azureLargeStorageInstances?api-version=2023-07-20-preview:
    get:
      operationId: AzureLargeStorageInstance_list_by_subscription
      description: Gets a list of AzureLargeStorageInstances in the specified subscription. The operations returns various properties of each Azure LargeStorage instance.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureLargeStorageInstanceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AzureLargeInstance/azureLargeStorageInstances?api-version=2023-07-20-preview:
    get:
      operationId: AzureLargeStorageInstance_list_by_resource_group
      description: Gets a list of AzureLargeStorageInstances in the specified subscription and resource group. The operations returns various properties of each Azure LargeStorage instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureLargeStorageInstanceFlattened"
  /providers/Microsoft.AzureLargeInstance/operations?api-version=2023-07-20-preview:
    get:
      operationId: largeinstance_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
