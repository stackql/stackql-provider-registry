openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Portal Dashboards
  description: Shared portal dashboards and tenant configurations (Microsoft.Portal).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2020-09-01-preview-stackql-generated
  x-serviceAlias: portal
  x-sdkPackage: azure-mgmt-portal
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    DashboardPropertiesWithProvisioningState:
      type: object
      description: Dashboard Properties with Provisioning state. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        lenses:
          type: array
          items:
            $ref: "#/components/schemas/DashboardLens"
          description: The dashboard lenses.
        metadata:
          type: object
          properties: {}
          description: The dashboard metadata.
        provisioningState:
          type: string
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", and "Canceled".'
    DashboardLens:
      type: object
      description: A dashboard lens. All required parameters must be populated in order to send to server.
      properties:
        order:
          type: integer
          description: The lens order. Required.
        parts:
          type: array
          items:
            $ref: "#/components/schemas/DashboardParts"
          description: The dashboard parts. Required.
        metadata:
          type: object
          properties: {}
          description: The dashboard len's metadata.
      required:
        - order
        - parts
    DashboardParts:
      type: object
      description: A dashboard part. All required parameters must be populated in order to send to server.
      properties:
        position:
          $ref: "#/components/schemas/DashboardPartsPosition"
          description: The dashboard's part position. Required.
        metadata:
          $ref: "#/components/schemas/DashboardPartMetadata"
          description: The dashboard part's metadata.
      required:
        - position
    DashboardPartsPosition:
      type: object
      description: The dashboard's part position. All required parameters must be populated in order to send to server.
      properties:
        x:
          type: integer
          description: The dashboard's part x coordinate. Required.
        "y":
          type: integer
          description: The dashboard's part y coordinate. Required.
        rowSpan:
          type: integer
          description: The dashboard's part row span. Required.
        colSpan:
          type: integer
          description: The dashboard's part column span. Required.
        metadata:
          type: object
          properties: {}
          description: The dashboard part's metadata.
      required:
        - x
        - "y"
        - rowSpan
        - colSpan
    DashboardPartMetadata:
      type: object
      description: "A dashboard part metadata. You probably want to use the sub-classes and not this class directly. Known sub-classes are: MarkdownPartMetadata All required parameters must be populated in order to send to server."
      properties:
        type:
          type: string
          description: The dashboard part metadata type. Required. "Extension/HubsExtension/PartType/MarkdownPart"
      required:
        - type
    DashboardFlattened:
      type: object
      description: Flattened view of Dashboard ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        lenses:
          type: array
          items:
            $ref: "#/components/schemas/DashboardLens"
          description: The dashboard lenses.
        metadata:
          type: object
          properties: {}
          description: The dashboard metadata.
        provisioningState:
          type: string
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedDashboardFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DashboardFlattened"
        nextLink:
          type: string
    Dashboard:
      type: object
      description: The shared dashboard resource definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/DashboardPropertiesWithProvisioningState"
          description: The resource-specific properties for this resource.
      required:
        - location
    DashboardProperties:
      type: object
      description: The shared dashboard properties.
      properties:
        lenses:
          type: array
          items:
            $ref: "#/components/schemas/DashboardLens"
          description: The dashboard lenses.
        metadata:
          type: object
          properties: {}
          description: The dashboard metadata.
    Violation:
      type: object
      description: Violation information. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Id of the item that violates tenant configuration.
        userId:
          type: string
          description: Id of the user who owns violated item.
        errorMessage:
          type: string
          description: Error message.
    PagedViolation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Violation"
        nextLink:
          type: string
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    ConfigurationProperties:
      type: object
      description: Tenant Configuration Properties with Provisioning state. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        enforcePrivateMarkdownStorage:
          type: boolean
          description: When flag is set to true Markdown tile will require external storage configuration (URI). The inline content configuration will be prohibited.
        provisioningState:
          type: string
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", and "Canceled".'
    ConfigurationFlattened:
      type: object
      description: Flattened view of Configuration ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        enforcePrivateMarkdownStorage:
          type: boolean
          description: When flag is set to true Markdown tile will require external storage configuration (URI). The inline content configuration will be prohibited.
        provisioningState:
          type: string
          description: 'The status of the last operation. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedConfigurationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ConfigurationFlattened"
        nextLink:
          type: string
    Configuration:
      type: object
      description: The tenant configuration resource definition. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/ConfigurationProperties"
          description: The resource-specific properties for this resource.
  x-stackQL-resources:
    dashboards:
      id: azure.portal.dashboards
      name: dashboards
      title: dashboards
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Portal~1dashboards~1{dashboard_name}?api-version=2020-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Portal~1dashboards~1{dashboard_name}?api-version=2020-09-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Portal~1dashboards~1{dashboard_name}?api-version=2020-09-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Portal~1dashboards~1{dashboard_name}?api-version=2020-09-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Portal~1dashboards?api-version=2020-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Portal~1dashboards?api-version=2020-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/dashboards/methods/get"
          - $ref: "#/components/x-stackQL-resources/dashboards/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/dashboards/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/dashboards/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/dashboards/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/dashboards/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/dashboards/methods/delete"
    list_tenant_configuration_violations:
      id: azure.portal.list_tenant_configuration_violations
      name: list_tenant_configuration_violations
      title: list_tenant_configuration_violations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Portal~1listTenantConfigurationViolations?api-version=2020-09-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/list_tenant_configuration_violations/methods/list"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.portal.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Portal~1operations?api-version=2020-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    tenant_configurations:
      id: azure.portal.tenant_configurations
      name: tenant_configurations
      title: tenant_configurations
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Portal~1tenantConfigurations~1{configuration_name}?api-version=2020-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Portal~1tenantConfigurations~1{configuration_name}?api-version=2020-09-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Portal~1tenantConfigurations~1{configuration_name}?api-version=2020-09-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Portal~1tenantConfigurations?api-version=2020-09-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/tenant_configurations/methods/get"
          - $ref: "#/components/x-stackQL-resources/tenant_configurations/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/tenant_configurations/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/tenant_configurations/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Portal/dashboards/{dashboard_name}?api-version=2020-09-01-preview:
    get:
      operationId: Dashboards_get
      description: Gets the Dashboard.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: dashboard_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the dashboard. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardFlattened"
    put:
      operationId: Dashboards_create_or_update
      description: Creates or updates a Dashboard.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: dashboard_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the dashboard. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/DashboardPropertiesWithProvisioningState"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Dashboard"
    patch:
      operationId: Dashboards_update
      description: Updates an existing Dashboard.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: dashboard_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the dashboard. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/DashboardProperties"
                  description: The shared dashboard properties.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Dashboard"
    delete:
      operationId: Dashboards_delete
      description: Deletes the Dashboard.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: dashboard_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the dashboard. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.Portal/dashboards?api-version=2020-09-01-preview:
    get:
      operationId: Dashboards_list_by_subscription
      description: Gets all the dashboards within a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDashboardFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Portal/dashboards?api-version=2020-09-01-preview:
    get:
      operationId: Dashboards_list_by_resource_group
      description: Gets all the Dashboards within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDashboardFlattened"
  /providers/Microsoft.Portal/listTenantConfigurationViolations?api-version=2020-09-01-preview:
    post:
      operationId: ListTenantConfigurationViolations_list
      description: Gets list of items that violate tenant's configuration.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedViolation"
  /providers/Microsoft.Portal/operations?api-version=2020-09-01-preview:
    get:
      operationId: portal_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /providers/Microsoft.Portal/tenantConfigurations/{configuration_name}?api-version=2020-09-01-preview:
    get:
      operationId: TenantConfigurations_get
      description: Gets the tenant configuration.
      parameters:
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Configuration. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigurationFlattened"
    put:
      operationId: TenantConfigurations_create
      description: Create the tenant configuration. If configuration already exists - update it. User has to be a Tenant Admin for this operation.
      parameters:
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Configuration. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ConfigurationProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Configuration"
    delete:
      operationId: TenantConfigurations_delete
      description: Delete the tenant configuration. User has to be a Tenant Admin for this operation.
      parameters:
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the Configuration. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /providers/Microsoft.Portal/tenantConfigurations?api-version=2020-09-01-preview:
    get:
      operationId: TenantConfigurations_list
      description: Gets list of the tenant configurations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConfigurationFlattened"
