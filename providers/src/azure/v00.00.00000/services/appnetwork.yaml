openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: App Network Mgmt Client
  description: AppNetworkMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-08-01-preview-stackql-generated
  x-serviceAlias: appnetwork
  x-sdkPackage: azure-mgmt-appnetwork
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    AppLinkMemberProperties:
      type: object
      description: AppLink Member properties.
      properties:
        clusterType:
          $ref: "#/components/schemas/ClusterType"
          description: Cluster type. "AKS"
        metadata:
          $ref: "#/components/schemas/Metadata"
          description: AppLink Member Metadata. Required.
        upgradeProfile:
          $ref: "#/components/schemas/UpgradeProfile"
          description: Upgrade profile.
        observabilityProfile:
          $ref: "#/components/schemas/ObservabilityProfile"
          description: Observability profile.
        connectivityProfile:
          $ref: "#/components/schemas/ConnectivityProfile"
          description: Connectivity profile.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - metadata
    ClusterType:
      type: string
      enum:
        - AKS
    Metadata:
      type: object
      description: AppLinkMember metadata.
      properties:
        resourceId:
          type: string
          description: Resource ID. Required.
      required:
        - resourceId
    UpgradeProfile:
      type: object
      description: AppLinkMember upgrade profile.
      properties:
        mode:
          $ref: "#/components/schemas/UpgradeMode"
          description: 'Upgrade mode. Required. Known values are: "FullyManaged" and "SelfManaged".'
        fullyManagedUpgradeProfile:
          $ref: "#/components/schemas/FullyManagedUpgradeProfile"
          description: Fully managed upgrade profile.
        selfManagedUpgradeProfile:
          $ref: "#/components/schemas/SelfManagedUpgradeProfile"
          description: Self managed upgrade profile.
      required:
        - mode
    UpgradeMode:
      type: string
      enum:
        - FullyManaged
        - SelfManaged
    FullyManagedUpgradeProfile:
      type: object
      description: AppLinkMember fully managed upgrade profile.
      properties:
        releaseChannel:
          $ref: "#/components/schemas/UpgradeReleaseChannel"
          description: 'Release channel. Required. Known values are: "Rapid" and "Stable".'
      required:
        - releaseChannel
    UpgradeReleaseChannel:
      type: string
      enum:
        - Rapid
        - Stable
    SelfManagedUpgradeProfile:
      type: object
      description: AppLinkMember self managed upgrade profile.
      properties:
        version:
          type: string
          description: Istio version. Required.
      required:
        - version
    ObservabilityProfile:
      type: object
      description: AppLinkMember observability profile.
      properties:
        metrics:
          $ref: "#/components/schemas/MetricsProfile"
          description: Metrics configuration.
    MetricsProfile:
      type: object
      description: AppLinkMember metrics profile.
      properties:
        metricsEndpoint:
          type: string
          description: Metrics endpoint URL.
    ConnectivityProfile:
      type: object
      description: AppLinkMember connectivity profile.
      properties:
        eastWestGateway:
          $ref: "#/components/schemas/EastWestGatewayProfile"
          description: East-West gateway profile.
        privateConnect:
          $ref: "#/components/schemas/PrivateConnectProfile"
          description: Private connect profile.
    EastWestGatewayProfile:
      type: object
      description: AppLinkMember east-west gateway profile.
      properties:
        visibility:
          $ref: "#/components/schemas/EastWestGatewayVisibility"
          description: 'East-West gateway visibility. Required. Known values are: "Internal" and "External".'
      required:
        - visibility
    EastWestGatewayVisibility:
      type: string
      enum:
        - Internal
        - External
    PrivateConnectProfile:
      type: object
      description: AppLinkMember private connect profile.
      properties:
        subnetResourceId:
          type: string
          description: Delegated Subnet to AppLink. Required.
      required:
        - subnetResourceId
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
        - Accepted
    AppLinkMemberFlattened:
      type: object
      description: Flattened view of AppLinkMember ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        clusterType:
          $ref: "#/components/schemas/ClusterType"
          description: Cluster type. "AKS"
        metadata:
          $ref: "#/components/schemas/Metadata"
          description: AppLink Member Metadata. Required.
        upgradeProfile:
          $ref: "#/components/schemas/UpgradeProfile"
          description: Upgrade profile.
        observabilityProfile:
          $ref: "#/components/schemas/ObservabilityProfile"
          description: Observability profile.
        connectivityProfile:
          $ref: "#/components/schemas/ConnectivityProfile"
          description: Connectivity profile.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedAppLinkMemberFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AppLinkMemberFlattened"
        nextLink:
          type: string
    AppLinkMember:
      type: object
      description: AppLink Member resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AppLinkMemberProperties"
          description: The resource-specific properties for this resource.
      required:
        - location
    AppLinkMemberUpdateProperties:
      type: object
      description: The updatable properties of the AppLinkMember.
      properties:
        upgradeProfile:
          $ref: "#/components/schemas/UpgradeProfile"
          description: Upgrade profile.
        observabilityProfile:
          $ref: "#/components/schemas/ObservabilityProfile"
          description: Observability profile.
        connectivityProfile:
          $ref: "#/components/schemas/ConnectivityProfile"
          description: Connectivity profile.
    AppLinkProperties:
      type: object
      description: AppLink properties.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    AppLinkFlattened:
      type: object
      description: Flattened view of AppLink ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedAppLinkFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AppLinkFlattened"
        nextLink:
          type: string
    AppLink:
      type: object
      description: AppLink resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AppLinkProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    AvailableVersionProperties:
      type: object
      description: AppLink available version properties.
      properties:
        kubernetesVersion:
          type: string
          description: Kubernetes version. Required.
        fullyManagedVersions:
          $ref: "#/components/schemas/FullyManagedVersions"
          description: Fully managed versions. Required.
        selfManagedVersions:
          $ref: "#/components/schemas/SelfManagedVersions"
          description: Self managed versions. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - kubernetesVersion
        - fullyManagedVersions
        - selfManagedVersions
    FullyManagedVersions:
      type: object
      description: Fully managed versions.
      properties:
        releaseChannels:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseChannelInfo"
          description: Release channels. Required.
      required:
        - releaseChannels
    ReleaseChannelInfo:
      type: object
      description: Release channel information.
      properties:
        releaseChannel:
          type: string
          description: Release channel. Required.
        version:
          type: string
          description: Istio version behind release channel. Required.
      required:
        - releaseChannel
        - version
    SelfManagedVersions:
      type: object
      description: Self managed versions.
      properties:
        versions:
          type: array
          items:
            $ref: "#/components/schemas/VersionInfo"
          description: Istio versions. Required.
      required:
        - versions
    VersionInfo:
      type: object
      description: Version information.
      properties:
        version:
          type: string
          description: Istio version. Required.
        upgrades:
          type: array
          items:
            type: string
          description: Available upgrades. Required.
      required:
        - version
        - upgrades
    AvailableVersionFlattened:
      type: object
      description: Flattened view of AvailableVersion ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        kubernetesVersion:
          type: string
          description: Kubernetes version. Required.
        fullyManagedVersions:
          $ref: "#/components/schemas/FullyManagedVersions"
          description: Fully managed versions. Required.
        selfManagedVersions:
          $ref: "#/components/schemas/SelfManagedVersions"
          description: Self managed versions. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedAvailableVersionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AvailableVersionFlattened"
        nextLink:
          type: string
    OperationDisplay:
      type: object
      description: Localized display information for an operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    UpgradeHistoryProperties:
      type: object
      description: AppLinkMember upgrade history properties.
      properties:
        startTimestamp:
          type: string
          format: date-time
          description: Start timestamp. Required.
        endTimestamp:
          type: string
          format: date-time
          description: End timestamp.
        initiatedBy:
          type: string
          description: Upgrade initiator. Required.
        fromVersion:
          type: string
          description: Version upgraded from. Required.
        toVersion:
          type: string
          description: Version upgraded to. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
      required:
        - startTimestamp
        - initiatedBy
        - fromVersion
        - toVersion
    UpgradeHistoryFlattened:
      type: object
      description: Flattened view of UpgradeHistory ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        startTimestamp:
          type: string
          format: date-time
          description: Start timestamp. Required.
        endTimestamp:
          type: string
          format: date-time
          description: End timestamp.
        initiatedBy:
          type: string
          description: Upgrade initiator. Required.
        fromVersion:
          type: string
          description: Version upgraded from. Required.
        toVersion:
          type: string
          description: Version upgraded to. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
    PagedUpgradeHistoryFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/UpgradeHistoryFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    app_link_members:
      id: azure.appnetwork.app_link_members
      name: app_link_members
      title: app_link_members
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}~1appLinkMembers~1{app_link_member_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}~1appLinkMembers~1{app_link_member_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}~1appLinkMembers~1{app_link_member_name}?api-version=2025-08-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}~1appLinkMembers~1{app_link_member_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_app_link:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}~1appLinkMembers?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/app_link_members/methods/get"
          - $ref: "#/components/x-stackQL-resources/app_link_members/methods/list_by_app_link"
        insert:
          - $ref: "#/components/x-stackQL-resources/app_link_members/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/app_link_members/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/app_link_members/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/app_link_members/methods/delete"
    app_links:
      id: azure.appnetwork.app_links
      name: app_links
      title: app_links
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}?api-version=2025-08-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}?api-version=2025-08-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}?api-version=2025-08-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AppLink~1appLinks?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/app_links/methods/get"
          - $ref: "#/components/x-stackQL-resources/app_links/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/app_links/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/app_links/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/app_links/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/app_links/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/app_links/methods/delete"
    available_versions:
      id: azure.appnetwork.available_versions
      name: available_versions
      title: available_versions
      methods:
        list_by_location:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.AppLink~1locations~1{location}~1availableVersions?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/available_versions/methods/list_by_location"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.appnetwork.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.AppLink~1operations?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    upgrade_histories:
      id: azure.appnetwork.upgrade_histories
      name: upgrade_histories
      title: upgrade_histories
      methods:
        list_by_app_link_member:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.AppLink~1appLinks~1{app_link_name}~1appLinkMembers~1{app_link_member_name}~1upgradeHistories?api-version=2025-08-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/upgrade_histories/methods/list_by_app_link_member"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AppLink/appLinks/{app_link_name}/appLinkMembers/{app_link_member_name}?api-version=2025-08-01-preview:
    get:
      operationId: AppLinkMembers_get
      description: Get an AppLinkMember.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: app_link_member_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLinkMember. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppLinkMemberFlattened"
    put:
      operationId: AppLinkMembers_begin_create_or_update
      description: Create an AppLinkMember.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: app_link_member_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLinkMember. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AppLinkMemberProperties"
                  description: The resource-specific properties for this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppLinkMember"
    patch:
      operationId: AppLinkMembers_begin_update
      description: Update an AppLinkMember.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: app_link_member_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLinkMember. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/AppLinkMemberUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppLinkMember"
    delete:
      operationId: AppLinkMembers_begin_delete
      description: Delete an AppLinkMember.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: app_link_member_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLinkMember. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AppLink/appLinks/{app_link_name}/appLinkMembers?api-version=2025-08-01-preview:
    get:
      operationId: AppLinkMembers_list_by_app_link
      description: List AppLinkMember resources by AppLink.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAppLinkMemberFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AppLink/appLinks/{app_link_name}?api-version=2025-08-01-preview:
    get:
      operationId: AppLinks_get
      description: Get an AppLink.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppLinkFlattened"
    put:
      operationId: AppLinks_begin_create_or_update
      description: Create an AppLink.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AppLinkProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppLink"
    patch:
      operationId: AppLinks_begin_update
      description: Update an AppLink.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppLink"
    delete:
      operationId: AppLinks_begin_delete
      description: Delete an AppLink.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AppLink/appLinks?api-version=2025-08-01-preview:
    get:
      operationId: AppLinks_list_by_resource_group
      description: List AppLink resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAppLinkFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.AppLink/appLinks?api-version=2025-08-01-preview:
    get:
      operationId: AppLinks_list_by_subscription
      description: List AppLink resources by subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAppLinkFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.AppLink/locations/{location}/availableVersions?api-version=2025-08-01-preview:
    get:
      operationId: AvailableVersions_list_by_location
      description: List AvailableVersion resources by location.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: kubernetesVersion
          in: query
          required: false
          schema:
            type: string
          description: Kubernetes version to filter profiles. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAvailableVersionFlattened"
  /providers/Microsoft.AppLink/operations?api-version=2025-08-01-preview:
    get:
      operationId: appnetwork_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.AppLink/appLinks/{app_link_name}/appLinkMembers/{app_link_member_name}/upgradeHistories?api-version=2025-08-01-preview:
    get:
      operationId: UpgradeHistories_list_by_app_link_member
      description: List UpgradeHistory resources by AppLinkMember.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: app_link_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLink. Required.
        - name: app_link_member_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AppLinkMember. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedUpgradeHistoryFlattened"
