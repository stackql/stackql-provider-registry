openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Resource Mover
  description: Move collections and move resources for cross-region migration.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-08-01-stackql-generated
  x-serviceAlias: resource_mover
  x-sdkPackage: azure-mgmt-resourcemover
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    Identity:
      type: object
      description: Defines the MSI properties of the Move Collection.
      properties:
        type:
          type: string
          description: 'The type of identity used for the resource mover service. Known values are: "None", "SystemAssigned", and "UserAssigned".'
        principalId:
          type: string
          description: Gets or sets the principal id.
        tenantId:
          type: string
          description: Gets or sets the tenant id.
    MoveCollectionProperties:
      type: object
      description: Defines the move collection properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        sourceRegion:
          type: string
          description: Gets or sets the source region.
        targetRegion:
          type: string
          description: Gets or sets the target region.
        moveRegion:
          type: string
          description: Gets or sets the move region which indicates the region where the VM Regional to Zonal move will be conducted.
        provisioningState:
          type: string
          description: 'Defines the provisioning states. Known values are: "Succeeded", "Updating", "Creating", and "Failed".'
        version:
          type: string
          description: Gets or sets the version of move collection.
        moveType:
          type: string
          description: 'Defines the MoveType. Known values are: "RegionToRegion" and "RegionToZone".'
        errors:
          $ref: "#/components/schemas/MoveCollectionPropertiesErrors"
          description: Defines the move collection errors.
    MoveCollectionPropertiesErrors:
      type: object
      description: Defines the move collection errors.
      properties:
        properties:
          $ref: "#/components/schemas/MoveResourceErrorBody"
          description: The move resource error body.
    MoveResourceErrorBody:
      type: object
      description: An error response from the Azure Migrate service. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        code:
          type: string
          description: An identifier for the error. Codes are invariant and are intended to be consumed programmatically.
        message:
          type: string
          description: A message describing the error, intended to be suitable for display in a user interface.
        target:
          type: string
          description: The target of the particular error. For example, the name of the property in error.
        details:
          type: array
          items:
            $ref: "#/components/schemas/MoveResourceErrorBody"
          description: A list of additional details about the error.
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    MoveCollectionFlattened:
      type: object
      description: Flattened view of MoveCollection ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource.
        etag:
          type: string
          description: The etag of the resource.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives.
        identity:
          $ref: "#/components/schemas/Identity"
          description: Defines the MSI properties of the Move Collection.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        sourceRegion:
          type: string
          description: Gets or sets the source region.
        targetRegion:
          type: string
          description: Gets or sets the target region.
        moveRegion:
          type: string
          description: Gets or sets the move region which indicates the region where the VM Regional to Zonal move will be conducted.
        provisioningState:
          type: string
          description: 'Defines the provisioning states. Known values are: "Succeeded", "Updating", "Creating", and "Failed".'
        version:
          type: string
          description: Gets or sets the version of move collection.
        moveType:
          type: string
          description: 'Defines the MoveType. Known values are: "RegionToRegion" and "RegionToZone".'
        errors:
          $ref: "#/components/schemas/MoveCollectionPropertiesErrors"
          description: Defines the move collection errors.
    PagedMoveCollectionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MoveCollectionFlattened"
        nextLink:
          type: string
    RequiredForResourcesCollection:
      type: object
      description: Required for resources collection.
      properties:
        sourceIds:
          type: array
          items:
            type: string
          description: Gets or sets the list of source Ids for which the input resource is required.
    MoveCollection:
      type: object
      description: Define the move collection. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource.
        etag:
          type: string
          description: The etag of the resource.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives.
        identity:
          $ref: "#/components/schemas/Identity"
          description: Defines the MSI properties of the Move Collection.
        properties:
          $ref: "#/components/schemas/MoveCollectionProperties"
          description: Defines the move collection properties.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
    OperationStatus:
      type: object
      description: Operation status REST resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Operation name.
        status:
          type: string
          description: Status of the operation. ARM expects the terminal status to be one of Succeeded/ Failed/ Canceled. All other values imply that the operation is still running.
        startTime:
          type: string
          description: Start time.
        endTime:
          type: string
          description: End time.
        error:
          $ref: "#/components/schemas/OperationStatusError"
          description: Error stating all error details for the operation.
        properties:
          type: object
          properties: {}
          description: Custom data.
    OperationStatusError:
      type: object
      description: Class for operation status errors. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        code:
          type: string
          description: The error code.
        message:
          type: string
          description: The error message.
        details:
          type: array
          items:
            $ref: "#/components/schemas/OperationStatusError"
          description: The error details.
        additionalInfo:
          type: array
          items:
            $ref: "#/components/schemas/OperationErrorAdditionalInfo"
          description: The additional info.
    OperationErrorAdditionalInfo:
      type: object
      description: The operation error info. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        type:
          type: string
          description: The error type.
        info:
          $ref: "#/components/schemas/MoveErrorInfo"
          description: The operation error info.
    MoveErrorInfo:
      type: object
      description: The move custom error info. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        moveResources:
          type: array
          items:
            $ref: "#/components/schemas/AffectedMoveResource"
          description: The affected move resources.
    AffectedMoveResource:
      type: object
      description: The RP custom operation error info. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: The affected move resource id.
        sourceId:
          type: string
          description: The affected move resource source id.
        moveResources:
          type: array
          items:
            $ref: "#/components/schemas/AffectedMoveResource"
          description: The affected move resources.
    MoveResourceProperties:
      type: object
      description: Defines the move resource properties. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        provisioningState:
          type: string
          description: 'Defines the provisioning states. Known values are: "Succeeded", "Updating", "Creating", and "Failed".'
        sourceId:
          type: string
          description: Gets or sets the Source ARM Id of the resource. Required.
        targetId:
          type: string
          description: Gets or sets the Target ARM Id of the resource.
        existingTargetId:
          type: string
          description: Gets or sets the existing target ARM Id of the resource.
        resourceSettings:
          $ref: "#/components/schemas/ResourceSettings"
          description: Gets or sets the resource settings.
        sourceResourceSettings:
          $ref: "#/components/schemas/ResourceSettings"
          description: Gets or sets the source resource settings.
        moveStatus:
          $ref: "#/components/schemas/MoveResourcePropertiesMoveStatus"
          description: Defines the move resource status.
        dependsOn:
          type: array
          items:
            $ref: "#/components/schemas/MoveResourceDependency"
          description: Gets or sets the move resource dependencies.
        dependsOnOverrides:
          type: array
          items:
            $ref: "#/components/schemas/MoveResourceDependencyOverride"
          description: Gets or sets the move resource dependencies overrides.
        isResolveRequired:
          type: boolean
          description: Gets a value indicating whether the resolve action is required over the move collection.
        errors:
          $ref: "#/components/schemas/MoveResourcePropertiesErrors"
          description: Defines the move resource errors.
      required:
        - sourceId
    ResourceSettings:
      type: object
      description: "Gets or sets the resource settings. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AvailabilitySetResourceSettings, DiskEncryptionSetResourceSettings, VirtualMachineResourceSettings, KeyVaultResourceSettings, LoadBalancerResourceSettings, NetworkInterfaceResourceSettings, NetworkSecurityGroupResourceSettings, PublicIPAddressResourceSettings, VirtualNetworkResourceSettings, SqlServerResourceSettings, SqlDatabaseResourceSettings, SqlElasticPoolResourceSettings, ResourceGroupResourceSettings All required parameters must be populated in order to send to Azure."
      properties:
        resourceType:
          type: string
          description: The resource type. For example, the value can be Microsoft.Compute/virtualMachines. Required.
        targetResourceName:
          type: string
          description: Gets or sets the target Resource name.
        targetResourceGroupName:
          type: string
          description: Gets or sets the target resource group name.
      required:
        - resourceType
    MoveResourcePropertiesMoveStatus:
      type: object
      description: Defines the move resource status. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        moveState:
          type: string
          description: 'Defines the MoveResource states. Known values are: "AssignmentPending", "PreparePending", "PrepareInProgress", "PrepareFailed", "MovePending", "MoveInProgress", "MoveFailed", "DiscardInProgress", "DiscardFailed", "CommitPending", "CommitInProgress", "CommitFailed", "Committed", "DeleteSourcePending", and "ResourceMoveCompleted".'
        jobStatus:
          $ref: "#/components/schemas/JobStatus"
          description: Defines the job status.
        errors:
          $ref: "#/components/schemas/MoveResourceError"
          description: An error response from the azure resource mover service.
    JobStatus:
      type: object
      description: Defines the job status. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        jobName:
          type: string
          description: Defines the job name. "InitialSync"
        jobProgress:
          type: string
          description: Gets or sets the monitoring job percentage.
    MoveResourceError:
      type: object
      description: An error response from the azure resource mover service.
      properties:
        properties:
          $ref: "#/components/schemas/MoveResourceErrorBody"
          description: The move resource error body.
    MoveResourceDependency:
      type: object
      description: Defines the dependency of the move resource.
      properties:
        id:
          type: string
          description: Gets the source ARM ID of the dependent resource.
        resolutionStatus:
          type: string
          description: Gets the dependency resolution status.
        resolutionType:
          type: string
          description: 'Defines the resolution type. Known values are: "Manual" and "Automatic".'
        dependencyType:
          type: string
          description: 'Defines the dependency type. Known values are: "RequiredForPrepare" and "RequiredForMove".'
        manualResolution:
          $ref: "#/components/schemas/ManualResolutionProperties"
          description: Defines the properties for manual resolution.
        automaticResolution:
          $ref: "#/components/schemas/AutomaticResolutionProperties"
          description: Defines the properties for automatic resolution.
        isOptional:
          type: string
          description: Gets or sets a value indicating whether the dependency is optional.
    ManualResolutionProperties:
      type: object
      description: Defines the properties for manual resolution.
      properties:
        targetId:
          type: string
          description: Gets or sets the target resource ARM ID of the dependent resource if the resource type is Manual.
    AutomaticResolutionProperties:
      type: object
      description: Defines the properties for automatic resolution.
      properties:
        moveResourceId:
          type: string
          description: Gets the MoveResource ARM ID of the dependent resource if the resolution type is Automatic.
    MoveResourceDependencyOverride:
      type: object
      description: Defines the dependency override of the move resource.
      properties:
        id:
          type: string
          description: Gets or sets the ARM ID of the dependent resource.
        targetId:
          type: string
          description: Gets or sets the resource ARM id of either the MoveResource or the resource ARM ID of the dependent resource.
    MoveResourcePropertiesErrors:
      type: object
      description: Defines the move resource errors.
      properties:
        properties:
          $ref: "#/components/schemas/MoveResourceErrorBody"
          description: The move resource error body.
    MoveResourceFlattened:
      type: object
      description: Flattened view of MoveResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
        provisioningState:
          type: string
          description: 'Defines the provisioning states. Known values are: "Succeeded", "Updating", "Creating", and "Failed".'
        sourceId:
          type: string
          description: Gets or sets the Source ARM Id of the resource. Required.
        targetId:
          type: string
          description: Gets or sets the Target ARM Id of the resource.
        existingTargetId:
          type: string
          description: Gets or sets the existing target ARM Id of the resource.
        resourceSettings:
          $ref: "#/components/schemas/ResourceSettings"
          description: Gets or sets the resource settings.
        sourceResourceSettings:
          $ref: "#/components/schemas/ResourceSettings"
          description: Gets or sets the source resource settings.
        moveStatus:
          $ref: "#/components/schemas/MoveResourcePropertiesMoveStatus"
          description: Defines the move resource status.
        dependsOn:
          type: array
          items:
            $ref: "#/components/schemas/MoveResourceDependency"
          description: Gets or sets the move resource dependencies.
        dependsOnOverrides:
          type: array
          items:
            $ref: "#/components/schemas/MoveResourceDependencyOverride"
          description: Gets or sets the move resource dependencies overrides.
        isResolveRequired:
          type: boolean
          description: Gets a value indicating whether the resolve action is required over the move collection.
        errors:
          $ref: "#/components/schemas/MoveResourcePropertiesErrors"
          description: Defines the move resource errors.
    PagedMoveResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/MoveResourceFlattened"
        nextLink:
          type: string
    MoveResource:
      type: object
      description: Defines the move resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource.
        properties:
          $ref: "#/components/schemas/MoveResourceProperties"
          description: Defines the move resource properties.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Metadata pertaining to creation and last modification of the resource.
    OperationsDiscovery:
      type: object
      description: Operations discovery class.
      properties:
        name:
          type: string
          description: 'Gets or sets Name of the API. The name of the operation being performed on this particular object. It should match the action name that appears in RBAC / the event service. Examples of operations include: * Microsoft.Compute/virtualMachine/capture/action * Microsoft.Compute/virtualMachine/restart/action * Microsoft.Compute/virtualMachine/write * Microsoft.Compute/virtualMachine/read * Microsoft.Compute/virtualMachine/delete Each action should include, in order: (1) Resource Provider Namespace (2) Type hierarchy for which the action applies (e.g. server/databases for a SQL Azure database) (3) Read, Write, Action or Delete indicating which type applies. If it is a PUT/PATCH on a collection or named value, Write should be used. If it is a GET, Read should be used. If it is a DELETE, Delete should be used. If it is a POST, Action should be used. As a note: all resource providers would need to include the "{Resource Provider Namespace}/register/action" operation in their response. This API is used to register for their service, and should include details about the operation (e.g. a localized name for the resource provider + any special considerations like PII release).'
        isDataAction:
          type: boolean
          description: Indicates whether the operation is a data action.
        display:
          $ref: "#/components/schemas/Display"
          description: Contains the localized display information for this particular operation / action. These value will be used by several clients for (1) custom role definitions for RBAC; (2) complex query filters for the event service; and (3) audit history / records for management operations.
        origin:
          type: string
          description: Gets or sets Origin. The intended executor of the operation; governs the display of the operation in the RBAC UX and the audit logs UX. Default value is "user,system".
        properties:
          type: object
          properties: {}
          description: ClientDiscovery properties.
    Display:
      type: object
      description: Contains the localized display information for this particular operation / action. These value will be used by several clients for (1) custom role definitions for RBAC; (2) complex query filters for the event service; and (3) audit history / records for management operations.
      properties:
        provider:
          type: string
          description: Gets or sets the provider. The localized friendly form of the resource provider name – it is expected to also include the publisher/company responsible. It should use Title Casing and begin with "Microsoft" for 1st party services. e.g. "Microsoft Monitoring Insights" or "Microsoft Compute.".
        resource:
          type: string
          description: Gets or sets the resource. The localized friendly form of the resource related to this action/operation – it should match the public documentation for the resource provider. It should use Title Casing. This value should be unique for a particular URL type (e.g. nested types should *not* reuse their parent’s display.resource field) e.g. "Virtual Machines" or "Scheduler Job Collections", or "Virtual Machine VM Sizes" or "Scheduler Jobs".
        operation:
          type: string
          description: "Gets or sets the operation. The localized friendly name for the operation, as it should be shown to the user. It should be concise (to fit in drop downs) but clear (i.e. self-documenting). It should use Title Casing. Prescriptive guidance: Read Create or Update Delete 'ActionName'."
        description:
          type: string
          description: "Gets or sets the description. The localized friendly description for the operation, as it should be shown to the user. It should be thorough, yet concise – it will be used in tool tips and detailed views. Prescriptive guidance for namespace: Read any 'display.provider' resource Create or Update any 'display.provider' resource Delete any 'display.provider' resource Perform any other action on any 'display.provider' resource Prescriptive guidance for namespace: Read any 'display.resource' Create or Update any 'display.resource' Delete any 'display.resource' 'ActionName' any 'display.resources'."
    OperationsDiscoveryCollection:
      type: object
      description: Collection of ClientDiscovery details.
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OperationsDiscovery"
          description: Gets or sets the ClientDiscovery details.
        nextLink:
          type: string
          description: Gets or sets the value of next link.
    UnresolvedDependency:
      type: object
      description: Unresolved dependency.
      properties:
        count:
          type: integer
          description: Gets or sets the count.
        id:
          type: string
          description: Gets or sets the arm id of the dependency.
    PagedUnresolvedDependency:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/UnresolvedDependency"
        nextLink:
          type: string
  x-stackQL-resources:
    move_collections:
      id: azure.resource_mover.move_collections
      name: move_collections
      title: move_collections
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}?api-version=2023-08-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}?api-version=2023-08-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}?api-version=2023-08-01/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list_move_collections_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Migrate~1moveCollections?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_move_collections_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_required_for:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1requiredFor?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
        prepare:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1prepare?api-version=2023-08-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        initiate_move:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1initiateMove?api-version=2023-08-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        commit:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1commit?api-version=2023-08-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        discard:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1discard?api-version=2023-08-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        resolve_dependencies:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1resolveDependencies?api-version=2023-08-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        bulk_remove:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1bulkRemove?api-version=2023-08-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/move_collections/methods/list_required_for"
          - $ref: "#/components/x-stackQL-resources/move_collections/methods/get"
          - $ref: "#/components/x-stackQL-resources/move_collections/methods/list_move_collections_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/move_collections/methods/list_move_collections_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/move_collections/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/move_collections/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/move_collections/methods/delete"
    move_resources:
      id: azure.resource_mover.move_resources
      name: move_resources
      title: move_resources
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1moveResources~1{move_resource_name}?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1moveResources~1{move_resource_name}?api-version=2023-08-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1moveResources~1{move_resource_name}?api-version=2023-08-01/delete"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1moveResources?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/move_resources/methods/get"
          - $ref: "#/components/x-stackQL-resources/move_resources/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/move_resources/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/move_resources/methods/delete"
    operations_discovery:
      id: azure.resource_mover.operations_discovery
      name: operations_discovery
      title: operations_discovery
      methods:
        get:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.Migrate~1operations?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations_discovery/methods/get"
        insert: []
        update: []
        delete: []
    unresolved_dependencies:
      id: azure.resource_mover.unresolved_dependencies
      name: unresolved_dependencies
      title: unresolved_dependencies
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Migrate~1moveCollections~1{move_collection_name}~1unresolvedDependencies?api-version=2023-08-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/unresolved_dependencies/methods/get"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}?api-version=2023-08-01:
    get:
      operationId: MoveCollections_get
      description: Gets the move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MoveCollectionFlattened"
    put:
      operationId: MoveCollections_create
      description: Creates or updates a move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: Defines the MSI properties of the Move Collection.
                properties:
                  $ref: "#/components/schemas/MoveCollectionProperties"
                  description: Defines the move collection properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MoveCollection"
    patch:
      operationId: MoveCollections_update
      description: Updates a move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: Defines the MSI properties of the Move Collection.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MoveCollection"
    delete:
      operationId: MoveCollections_begin_delete
      description: Deletes a move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/providers/Microsoft.Migrate/moveCollections?api-version=2023-08-01:
    get:
      operationId: MoveCollections_list_move_collections_by_subscription
      description: Get all Move Collections. Get all the Move Collections in the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMoveCollectionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections?api-version=2023-08-01:
    get:
      operationId: MoveCollections_list_move_collections_by_resource_group
      description: Get all Move Collections. Get all the Move Collections in the resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMoveCollectionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/requiredFor?api-version=2023-08-01:
    get:
      operationId: MoveCollections_list_required_for
      description: List of the move resources for which an arm resource is required for.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: sourceId
          in: query
          required: true
          schema:
            type: string
          description: The sourceId for which the api is invoked. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequiredForResourcesCollection"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/prepare?api-version=2023-08-01:
    post:
      operationId: MoveCollections_begin_prepare
      description: Initiates prepare for the set of resources included in the request body. The prepare operation is on the moveResources that are in the moveState 'PreparePending' or 'PrepareFailed', on a successful completion the moveResource moveState do a transition to MovePending. To aid the user to prerequisite the operation the client can call operation with validateOnly property set to true.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                validateOnly:
                  type: boolean
                  description: Gets or sets a value indicating whether the operation needs to only run pre-requisite.
                moveResources:
                  type: array
                  items:
                    type: string
                  description: Gets or sets the list of resource Id's, by default it accepts move resource id's unless the input type is switched via moveResourceInputType property. Required.
                moveResourceInputType:
                  type: string
                  description: 'Defines the move resource input type. Known values are: "MoveResourceId" and "MoveResourceSourceId".'
              required:
                - moveResources
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/initiateMove?api-version=2023-08-01:
    post:
      operationId: MoveCollections_begin_initiate_move
      description: Moves the set of resources included in the request body. The move operation is triggered after the moveResources are in the moveState 'MovePending' or 'MoveFailed', on a successful completion the moveResource moveState do a transition to CommitPending. To aid the user to prerequisite the operation the client can call operation with validateOnly property set to true.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                validateOnly:
                  type: boolean
                  description: Gets or sets a value indicating whether the operation needs to only run pre-requisite.
                moveResources:
                  type: array
                  items:
                    type: string
                  description: Gets or sets the list of resource Id's, by default it accepts move resource id's unless the input type is switched via moveResourceInputType property. Required.
                moveResourceInputType:
                  type: string
                  description: 'Defines the move resource input type. Known values are: "MoveResourceId" and "MoveResourceSourceId".'
              required:
                - moveResources
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/commit?api-version=2023-08-01:
    post:
      operationId: MoveCollections_begin_commit
      description: Commits the set of resources included in the request body. The commit operation is triggered on the moveResources in the moveState 'CommitPending' or 'CommitFailed', on a successful completion the moveResource moveState do a transition to Committed. To aid the user to prerequisite the operation the client can call operation with validateOnly property set to true.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                validateOnly:
                  type: boolean
                  description: Gets or sets a value indicating whether the operation needs to only run pre-requisite.
                moveResources:
                  type: array
                  items:
                    type: string
                  description: Gets or sets the list of resource Id's, by default it accepts move resource id's unless the input type is switched via moveResourceInputType property. Required.
                moveResourceInputType:
                  type: string
                  description: 'Defines the move resource input type. Known values are: "MoveResourceId" and "MoveResourceSourceId".'
              required:
                - moveResources
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/discard?api-version=2023-08-01:
    post:
      operationId: MoveCollections_begin_discard
      description: Discards the set of resources included in the request body. The discard operation is triggered on the moveResources in the moveState 'CommitPending' or 'DiscardFailed', on a successful completion the moveResource moveState do a transition to MovePending. To aid the user to prerequisite the operation the client can call operation with validateOnly property set to true.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                validateOnly:
                  type: boolean
                  description: Gets or sets a value indicating whether the operation needs to only run pre-requisite.
                moveResources:
                  type: array
                  items:
                    type: string
                  description: Gets or sets the list of resource Id's, by default it accepts move resource id's unless the input type is switched via moveResourceInputType property. Required.
                moveResourceInputType:
                  type: string
                  description: 'Defines the move resource input type. Known values are: "MoveResourceId" and "MoveResourceSourceId".'
              required:
                - moveResources
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/resolveDependencies?api-version=2023-08-01:
    post:
      operationId: MoveCollections_begin_resolve_dependencies
      description: Computes, resolves and validate the dependencies of the moveResources in the move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/bulkRemove?api-version=2023-08-01:
    post:
      operationId: MoveCollections_begin_bulk_remove
      description: Removes the set of move resources included in the request body from move collection. The orchestration is done by service. To aid the user to prerequisite the operation the client can call operation with validateOnly property set to true.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                validateOnly:
                  type: boolean
                  description: Gets or sets a value indicating whether the operation needs to only run pre-requisite.
                moveResources:
                  type: array
                  items:
                    type: string
                  description: Gets or sets the list of resource Id's, by default it accepts move resource id's unless the input type is switched via moveResourceInputType property.
                moveResourceInputType:
                  type: string
                  description: 'Defines the move resource input type. Known values are: "MoveResourceId" and "MoveResourceSourceId".'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/moveResources/{move_resource_name}?api-version=2023-08-01:
    get:
      operationId: MoveResources_get
      description: Gets the Move Resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: move_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Resource Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MoveResourceFlattened"
    put:
      operationId: MoveResources_begin_create
      description: Creates or updates a Move Resource in the move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: move_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Resource Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/MoveResourceProperties"
                  description: Defines the move resource properties.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MoveResource"
    delete:
      operationId: MoveResources_begin_delete
      description: Deletes a Move Resource from the move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: move_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Resource Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/moveResources?api-version=2023-08-01:
    get:
      operationId: MoveResources_list
      description: Lists the Move Resources in the move collection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. For example, you can use $filter=Properties/ProvisioningState eq 'Succeeded'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedMoveResourceFlattened"
  /providers/Microsoft.Migrate/operations?api-version=2023-08-01:
    get:
      operationId: OperationsDiscovery_get
      description: get.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationsDiscoveryCollection"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Migrate/moveCollections/{move_collection_name}/unresolvedDependencies?api-version=2023-08-01:
    get:
      operationId: UnresolvedDependencies_get
      description: Gets a list of unresolved dependencies.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: move_collection_name
          in: path
          required: true
          schema:
            type: string
          description: The Move Collection Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: dependencyLevel
          in: query
          required: false
          schema:
            type: string
          description: 'Defines the dependency level. Known values are: "Direct" and "Descendant". Default value is None.'
        - name: $orderby
          in: query
          required: false
          schema:
            type: string
          description: OData order by query option. For example, you can use $orderby=Count desc. Default value is None.
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: The filter to apply on the operation. For example, $apply=filter(count eq 2). Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedUnresolvedDependency"
