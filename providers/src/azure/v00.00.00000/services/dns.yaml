openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure DNS
  description: Public DNS zones and record sets.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2018-05-01-stackql-generated
  x-serviceAlias: dns
  x-sdkPackage: azure-mgmt-dns
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SubResource:
      type: object
      description: A reference to a another resource.
      properties:
        id:
          type: string
          description: Resource Id.
    DnsResourceReference:
      type: object
      description: Represents a single Azure resource and its referencing DNS records.
      properties:
        dnsResources:
          type: array
          items:
            $ref: "#/components/schemas/SubResource"
          description: A list of dns Records.
        targetResource:
          $ref: "#/components/schemas/SubResource"
          description: A reference to an azure resource from where the dns resource value is taken.
    DnsResourceReferenceResultFlattened:
      type: object
      description: Flattened view of DnsResourceReferenceResult ('properties' hoisted to the top level).
      properties:
        dnsResourceReferences:
          type: array
          items:
            $ref: "#/components/schemas/DnsResourceReference"
          description: The result of dns resource reference request. A list of dns resource references for each of the azure resource in the request.
    ARecord:
      type: object
      description: An A record.
      properties:
        ipv4Address:
          type: string
          description: The IPv4 address of this A record.
    AaaaRecord:
      type: object
      description: An AAAA record.
      properties:
        ipv6Address:
          type: string
          description: The IPv6 address of this AAAA record.
    MxRecord:
      type: object
      description: An MX record.
      properties:
        preference:
          type: integer
          description: The preference value for this MX record.
        exchange:
          type: string
          description: The domain name of the mail host for this MX record.
    NsRecord:
      type: object
      description: An NS record.
      properties:
        nsdname:
          type: string
          description: The name server name for this NS record.
    PtrRecord:
      type: object
      description: A PTR record.
      properties:
        ptrdname:
          type: string
          description: The PTR target domain name for this PTR record.
    SrvRecord:
      type: object
      description: An SRV record.
      properties:
        priority:
          type: integer
          description: The priority value for this SRV record.
        weight:
          type: integer
          description: The weight value for this SRV record.
        port:
          type: integer
          description: The port value for this SRV record.
        target:
          type: string
          description: The target domain name for this SRV record.
    TxtRecord:
      type: object
      description: A TXT record.
      properties:
        value:
          type: array
          items:
            type: string
          description: The text value of this TXT record.
    CnameRecord:
      type: object
      description: A CNAME record.
      properties:
        cname:
          type: string
          description: The canonical name for this CNAME record.
    SoaRecord:
      type: object
      description: An SOA record.
      properties:
        host:
          type: string
          description: The domain name of the authoritative name server for this SOA record.
        email:
          type: string
          description: The email contact for this SOA record.
        serialNumber:
          type: integer
          description: The serial number for this SOA record.
        refreshTime:
          type: integer
          description: The refresh value for this SOA record.
        retryTime:
          type: integer
          description: The retry time for this SOA record.
        expireTime:
          type: integer
          description: The expire time for this SOA record.
        minimumTTL:
          type: integer
          description: The minimum value for this SOA record. By convention this is used to determine the negative caching duration.
    CaaRecord:
      type: object
      description: A CAA record.
      properties:
        flags:
          type: integer
          description: The flags for this CAA record as an integer between 0 and 255.
        tag:
          type: string
          description: The tag for this CAA record.
        value:
          type: string
          description: The value for this CAA record.
    RecordSetFlattened:
      type: object
      description: Flattened view of RecordSet ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: The ID of the record set.
        name:
          type: string
          description: The name of the record set.
        type:
          type: string
          description: The type of the record set.
        etag:
          type: string
          description: The etag of the record set.
        metadata:
          type: object
          properties: {}
          description: The metadata attached to the record set.
        TTL:
          type: integer
          description: The TTL (time-to-live) of the records in the record set.
        fqdn:
          type: string
          description: Fully qualified domain name of the record set.
        provisioningState:
          type: string
          description: provisioning State of the record set.
        targetResource:
          $ref: "#/components/schemas/SubResource"
          description: A reference to an azure resource from where the dns resource value is taken.
        ARecords:
          type: array
          items:
            $ref: "#/components/schemas/ARecord"
          description: The list of A records in the record set.
        AAAARecords:
          type: array
          items:
            $ref: "#/components/schemas/AaaaRecord"
          description: The list of AAAA records in the record set.
        MXRecords:
          type: array
          items:
            $ref: "#/components/schemas/MxRecord"
          description: The list of MX records in the record set.
        NSRecords:
          type: array
          items:
            $ref: "#/components/schemas/NsRecord"
          description: The list of NS records in the record set.
        PTRRecords:
          type: array
          items:
            $ref: "#/components/schemas/PtrRecord"
          description: The list of PTR records in the record set.
        SRVRecords:
          type: array
          items:
            $ref: "#/components/schemas/SrvRecord"
          description: The list of SRV records in the record set.
        TXTRecords:
          type: array
          items:
            $ref: "#/components/schemas/TxtRecord"
          description: The list of TXT records in the record set.
        CNAMERecord:
          $ref: "#/components/schemas/CnameRecord"
          description: The CNAME record in the record set.
        SOARecord:
          $ref: "#/components/schemas/SoaRecord"
          description: The SOA record in the record set.
        caaRecords:
          type: array
          items:
            $ref: "#/components/schemas/CaaRecord"
          description: The list of CAA records in the record set.
    PagedRecordSetFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RecordSetFlattened"
        nextLink:
          type: string
    RecordSet:
      type: object
      description: Describes a DNS record set (a collection of DNS records with the same name and type). Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: The ID of the record set.
        name:
          type: string
          description: The name of the record set.
        type:
          type: string
          description: The type of the record set.
        etag:
          type: string
          description: The etag of the record set.
        properties:
          type: object
          properties:
            metadata:
              type: object
              properties: {}
              description: The metadata attached to the record set.
            TTL:
              type: integer
              description: The TTL (time-to-live) of the records in the record set.
            fqdn:
              type: string
              description: Fully qualified domain name of the record set.
            provisioningState:
              type: string
              description: provisioning State of the record set.
            targetResource:
              $ref: "#/components/schemas/SubResource"
              description: A reference to an azure resource from where the dns resource value is taken.
            ARecords:
              type: array
              items:
                $ref: "#/components/schemas/ARecord"
              description: The list of A records in the record set.
            AAAARecords:
              type: array
              items:
                $ref: "#/components/schemas/AaaaRecord"
              description: The list of AAAA records in the record set.
            MXRecords:
              type: array
              items:
                $ref: "#/components/schemas/MxRecord"
              description: The list of MX records in the record set.
            NSRecords:
              type: array
              items:
                $ref: "#/components/schemas/NsRecord"
              description: The list of NS records in the record set.
            PTRRecords:
              type: array
              items:
                $ref: "#/components/schemas/PtrRecord"
              description: The list of PTR records in the record set.
            SRVRecords:
              type: array
              items:
                $ref: "#/components/schemas/SrvRecord"
              description: The list of SRV records in the record set.
            TXTRecords:
              type: array
              items:
                $ref: "#/components/schemas/TxtRecord"
              description: The list of TXT records in the record set.
            CNAMERecord:
              $ref: "#/components/schemas/CnameRecord"
              description: The CNAME record in the record set.
            SOARecord:
              $ref: "#/components/schemas/SoaRecord"
              description: The SOA record in the record set.
            caaRecords:
              type: array
              items:
                $ref: "#/components/schemas/CaaRecord"
              description: The list of CAA records in the record set.
    ZoneFlattened:
      type: object
      description: Flattened view of Zone ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location. Required.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        etag:
          type: string
          description: The etag of the zone.
        maxNumberOfRecordSets:
          type: integer
          description: The maximum number of record sets that can be created in this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        maxNumberOfRecordsPerRecordSet:
          type: integer
          description: The maximum number of records per record set that can be created in this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        numberOfRecordSets:
          type: integer
          description: The current number of record sets in this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        nameServers:
          type: array
          items:
            type: string
          description: The name servers for this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
        zoneType:
          type: string
          description: 'The type of this DNS zone (Public or Private). Known values are: "Public" and "Private".'
        registrationVirtualNetworks:
          type: array
          items:
            $ref: "#/components/schemas/SubResource"
          description: A list of references to virtual networks that register hostnames in this DNS zone. This is a only when ZoneType is Private.
        resolutionVirtualNetworks:
          type: array
          items:
            $ref: "#/components/schemas/SubResource"
          description: A list of references to virtual networks that resolve records in this DNS zone. This is a only when ZoneType is Private.
    PagedZoneFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ZoneFlattened"
        nextLink:
          type: string
    Zone:
      type: object
      description: Describes a DNS zone. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location. Required.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        etag:
          type: string
          description: The etag of the zone.
        properties:
          type: object
          properties:
            maxNumberOfRecordSets:
              type: integer
              description: The maximum number of record sets that can be created in this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            maxNumberOfRecordsPerRecordSet:
              type: integer
              description: The maximum number of records per record set that can be created in this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            numberOfRecordSets:
              type: integer
              description: The current number of record sets in this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            nameServers:
              type: array
              items:
                type: string
              description: The name servers for this DNS zone. This is a read-only property and any attempt to set this value will be ignored.
            zoneType:
              type: string
              description: 'The type of this DNS zone (Public or Private). Known values are: "Public" and "Private".'
            registrationVirtualNetworks:
              type: array
              items:
                $ref: "#/components/schemas/SubResource"
              description: A list of references to virtual networks that register hostnames in this DNS zone. This is a only when ZoneType is Private.
            resolutionVirtualNetworks:
              type: array
              items:
                $ref: "#/components/schemas/SubResource"
              description: A list of references to virtual networks that resolve records in this DNS zone. This is a only when ZoneType is Private.
      required:
        - location
  x-stackQL-resources:
    dns_resource_reference:
      id: azure.dns.dns_resource_reference
      name: dns_resource_reference
      title: dns_resource_reference
      methods:
        get_by_target_resources:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Network~1getDnsResourceReference?api-version=2018-05-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/dns_resource_reference/methods/get_by_target_resources"
        insert: []
        update: []
        delete: []
    record_sets:
      id: azure.dns.record_sets
      name: record_sets
      title: record_sets
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2018-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2018-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2018-05-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}~1{record_type}~1{relative_record_set_name}?api-version=2018-05-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_type:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}~1{record_type}?api-version=2018-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_dns_zone:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}~1recordsets?api-version=2018-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_all_by_dns_zone:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}~1all?api-version=2018-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/get"
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/list_by_type"
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/list_by_dns_zone"
        insert:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/record_sets/methods/delete"
    zones:
      id: azure.dns.zones
      name: zones
      title: zones
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}?api-version=2018-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}?api-version=2018-05-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}?api-version=2018-05-01/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones~1{zone_name}?api-version=2018-05-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.Network~1dnszones?api-version=2018-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.Network~1dnsZones?api-version=2018-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/zones/methods/get"
          - $ref: "#/components/x-stackQL-resources/zones/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/zones/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/zones/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/zones/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/zones/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/zones/methods/delete"
paths:
  /subscriptions/{subscription_id}/providers/Microsoft.Network/getDnsResourceReference?api-version=2018-05-01:
    post:
      operationId: DnsResourceReference_get_by_target_resources
      description: Returns the DNS records specified by the referencing targetResourceIds.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    targetResources:
                      type: array
                      items:
                        $ref: "#/components/schemas/SubResource"
                      description: A list of references to azure resources for which referencing dns records need to be queried.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DnsResourceReferenceResultFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/dnsZones/{zone_name}/{record_type}/{relative_record_set_name}?api-version=2018-05-01:
    get:
      operationId: RecordSets_get
      description: Gets a record set.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Known values are: "A", "AAAA", "CAA", "CNAME", "MX", "NS", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordSetFlattened"
    put:
      operationId: RecordSets_create_or_update
      description: Creates or updates a record set within a DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Record sets of type SOA can be updated but not created (they are created when the DNS zone is created). Known values are: "A", "AAAA", "CAA", "CNAME", "MX", "NS", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The etag of the record set. Omit this value to always overwrite the current record set. Specify the last-seen etag value to prevent accidentally overwriting any concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new record set to be created, but to prevent updating an existing record set. Other values will be ignored. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                etag:
                  type: string
                  description: The etag of the record set.
                properties:
                  type: object
                  properties:
                    metadata:
                      type: object
                      properties: {}
                      description: The metadata attached to the record set.
                    TTL:
                      type: integer
                      description: The TTL (time-to-live) of the records in the record set.
                    targetResource:
                      $ref: "#/components/schemas/SubResource"
                      description: A reference to an azure resource from where the dns resource value is taken.
                    ARecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/ARecord"
                      description: The list of A records in the record set.
                    AAAARecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/AaaaRecord"
                      description: The list of AAAA records in the record set.
                    MXRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/MxRecord"
                      description: The list of MX records in the record set.
                    NSRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/NsRecord"
                      description: The list of NS records in the record set.
                    PTRRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/PtrRecord"
                      description: The list of PTR records in the record set.
                    SRVRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/SrvRecord"
                      description: The list of SRV records in the record set.
                    TXTRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/TxtRecord"
                      description: The list of TXT records in the record set.
                    CNAMERecord:
                      $ref: "#/components/schemas/CnameRecord"
                      description: The CNAME record in the record set.
                    SOARecord:
                      $ref: "#/components/schemas/SoaRecord"
                      description: The SOA record in the record set.
                    caaRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/CaaRecord"
                      description: The list of CAA records in the record set.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordSet"
    patch:
      operationId: RecordSets_update
      description: Updates a record set within a DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Known values are: "A", "AAAA", "CAA", "CNAME", "MX", "NS", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The etag of the record set. Omit this value to always overwrite the current record set. Specify the last-seen etag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                etag:
                  type: string
                  description: The etag of the record set.
                properties:
                  type: object
                  properties:
                    metadata:
                      type: object
                      properties: {}
                      description: The metadata attached to the record set.
                    TTL:
                      type: integer
                      description: The TTL (time-to-live) of the records in the record set.
                    targetResource:
                      $ref: "#/components/schemas/SubResource"
                      description: A reference to an azure resource from where the dns resource value is taken.
                    ARecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/ARecord"
                      description: The list of A records in the record set.
                    AAAARecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/AaaaRecord"
                      description: The list of AAAA records in the record set.
                    MXRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/MxRecord"
                      description: The list of MX records in the record set.
                    NSRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/NsRecord"
                      description: The list of NS records in the record set.
                    PTRRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/PtrRecord"
                      description: The list of PTR records in the record set.
                    SRVRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/SrvRecord"
                      description: The list of SRV records in the record set.
                    TXTRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/TxtRecord"
                      description: The list of TXT records in the record set.
                    CNAMERecord:
                      $ref: "#/components/schemas/CnameRecord"
                      description: The CNAME record in the record set.
                    SOARecord:
                      $ref: "#/components/schemas/SoaRecord"
                      description: The SOA record in the record set.
                    caaRecords:
                      type: array
                      items:
                        $ref: "#/components/schemas/CaaRecord"
                      description: The list of CAA records in the record set.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordSet"
    delete:
      operationId: RecordSets_delete
      description: Deletes a record set from a DNS zone. This operation cannot be undone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: relative_record_set_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the record set, relative to the name of the zone. Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of DNS record in this record set. Record sets of type SOA cannot be deleted (they are deleted when the DNS zone is deleted). Known values are: "A", "AAAA", "CAA", "CNAME", "MX", "NS", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The etag of the record set. Omit this value to always delete the current record set. Specify the last-seen etag value to prevent accidentally deleting any concurrent changes. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/dnsZones/{zone_name}/{record_type}?api-version=2018-05-01:
    get:
      operationId: RecordSets_list_by_type
      description: Lists the record sets of a specified type in a DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: record_type
          in: path
          required: true
          schema:
            type: string
          description: 'The type of record sets to enumerate. Known values are: "A", "AAAA", "CAA", "CNAME", "MX", "NS", "PTR", "SOA", "SRV", and "TXT". Required.'
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. If not specified, returns up to 100 record sets. Default value is None.
        - name: $recordsetnamesuffix
          in: query
          required: false
          schema:
            type: string
          description: The suffix label of the record set name that has to be used to filter the record set enumerations. If this parameter is specified, Enumeration will return only records that end with .\ . Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRecordSetFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/dnsZones/{zone_name}/recordsets?api-version=2018-05-01:
    get:
      operationId: RecordSets_list_by_dns_zone
      description: Lists all record sets in a DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. If not specified, returns up to 100 record sets. Default value is None.
        - name: $recordsetnamesuffix
          in: query
          required: false
          schema:
            type: string
          description: The suffix label of the record set name that has to be used to filter the record set enumerations. If this parameter is specified, Enumeration will return only records that end with .\ . Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRecordSetFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/dnsZones/{zone_name}/all?api-version=2018-05-01:
    get:
      operationId: RecordSets_list_all_by_dns_zone
      description: Lists all record sets in a DNS zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. If not specified, returns up to 100 record sets. Default value is None.
        - name: $recordsetnamesuffix
          in: query
          required: false
          schema:
            type: string
          description: The suffix label of the record set name that has to be used to filter the record set enumerations. If this parameter is specified, Enumeration will return only records that end with .\ . Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRecordSetFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/dnsZones/{zone_name}?api-version=2018-05-01:
    get:
      operationId: Zones_get
      description: Gets a DNS zone. Retrieves the zone properties, but not the record sets within the zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZoneFlattened"
    put:
      operationId: Zones_create_or_update
      description: Creates or updates a DNS zone. Does not modify DNS records within the zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The etag of the DNS zone. Omit this value to always overwrite the current zone. Specify the last-seen etag value to prevent accidentally overwriting any concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new DNS zone to be created, but to prevent updating an existing zone. Other values will be ignored. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location. Required.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                etag:
                  type: string
                  description: The etag of the zone.
                properties:
                  type: object
                  properties:
                    zoneType:
                      type: string
                      description: 'The type of this DNS zone (Public or Private). Known values are: "Public" and "Private".'
                    registrationVirtualNetworks:
                      type: array
                      items:
                        $ref: "#/components/schemas/SubResource"
                      description: A list of references to virtual networks that register hostnames in this DNS zone. This is a only when ZoneType is Private.
                    resolutionVirtualNetworks:
                      type: array
                      items:
                        $ref: "#/components/schemas/SubResource"
                      description: A list of references to virtual networks that resolve records in this DNS zone. This is a only when ZoneType is Private.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Zone"
    delete:
      operationId: Zones_begin_delete
      description: "Deletes a DNS zone. WARNING: All DNS records in the zone will also be deleted. This operation cannot be undone."
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The etag of the DNS zone. Omit this value to always delete the current zone. Specify the last-seen etag value to prevent accidentally deleting any concurrent changes. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Zones_update
      description: Updates a DNS zone. Does not modify DNS records within the zone.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: zone_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the DNS zone (without a terminating dot). Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The etag of the DNS zone. Omit this value to always overwrite the current zone. Specify the last-seen etag value to prevent accidentally overwriting any concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Zone"
  /subscriptions/{subscription_id}/providers/Microsoft.Network/dnszones?api-version=2018-05-01:
    get:
      operationId: Zones_list
      description: Lists the DNS zones in all resource groups in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of DNS zones to return. If not specified, returns up to 100 zones. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedZoneFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Network/dnsZones?api-version=2018-05-01:
    get:
      operationId: Zones_list_by_resource_group
      description: Lists the DNS zones within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: The maximum number of record sets to return. If not specified, returns up to 100 record sets. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedZoneFlattened"
