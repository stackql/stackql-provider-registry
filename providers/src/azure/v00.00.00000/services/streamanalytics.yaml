openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Stream Analytics Management Client
  description: Stream Analytics Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-10-01-preview-stackql-generated
  x-serviceAlias: streamanalytics
  x-sdkPackage: azure-mgmt-streamanalytics
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    ClusterSku:
      type: object
      description: The SKU of the cluster. This determines the size/capacity of the cluster. Required on PUT (CreateOrUpdate) requests.
      properties:
        name:
          type: string
          description: Specifies the SKU name of the cluster. Required on PUT (CreateOrUpdate) requests. "Default"
        capacity:
          type: integer
          description: Denotes the number of streaming units the cluster can support. Valid values for this property are multiples of 36 with a minimum value of 36 and maximum value of 216. Required on PUT (CreateOrUpdate) requests.
    ClusterProperties:
      type: object
      description: The properties associated with a Stream Analytics cluster. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        createdDate:
          type: string
          format: date-time
          description: The date this cluster was created.
        clusterId:
          type: string
          description: Unique identifier for the cluster.
        provisioningState:
          type: string
          description: 'The status of the cluster provisioning. The three terminal states are: Succeeded, Failed and Canceled. Known values are: "Succeeded", "Failed", "Canceled", and "InProgress".'
        capacityAllocated:
          type: integer
          description: Represents the number of streaming units currently being used on the cluster.
        capacityAssigned:
          type: integer
          description: Represents the sum of the SUs of all streaming jobs associated with the cluster. If all of the jobs were running, this would be the capacity allocated.
    ClusterFlattened:
      type: object
      description: Flattened view of Cluster ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives.
        sku:
          $ref: "#/components/schemas/ClusterSku"
          description: The SKU of the cluster. This determines the size/capacity of the cluster. Required on PUT (CreateOrUpdate) requests.
        etag:
          type: string
          description: The current entity tag for the cluster. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        createdDate:
          type: string
          format: date-time
          description: The date this cluster was created.
        clusterId:
          type: string
          description: Unique identifier for the cluster.
        provisioningState:
          type: string
          description: 'The status of the cluster provisioning. The three terminal states are: Succeeded, Failed and Canceled. Known values are: "Succeeded", "Failed", "Canceled", and "InProgress".'
        capacityAllocated:
          type: integer
          description: Represents the number of streaming units currently being used on the cluster.
        capacityAssigned:
          type: integer
          description: Represents the sum of the SUs of all streaming jobs associated with the cluster. If all of the jobs were running, this would be the capacity allocated.
    PagedClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ClusterFlattened"
        nextLink:
          type: string
    Cluster:
      type: object
      description: A Stream Analytics Cluster object. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives.
        sku:
          $ref: "#/components/schemas/ClusterSku"
          description: The SKU of the cluster. This determines the size/capacity of the cluster. Required on PUT (CreateOrUpdate) requests.
        etag:
          type: string
          description: The current entity tag for the cluster. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        properties:
          $ref: "#/components/schemas/ClusterProperties"
          description: The properties associated with a Stream Analytics cluster.
    ClusterJob:
      type: object
      description: A streaming job. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource ID of the streaming job.
        streamingUnits:
          type: integer
          description: The number of streaming units that are used by the streaming job.
        jobState:
          type: string
          description: 'The current execution state of the streaming job. Known values are: "Created", "Starting", "Running", "Stopping", "Stopped", "Deleting", "Failed", "Degraded", "Restarting", and "Scaling".'
    PagedClusterJob:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ClusterJob"
        nextLink:
          type: string
    FunctionProperties:
      type: object
      description: "The properties that are associated with a function. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AggregateFunctionProperties, ScalarFunctionProperties Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure."
      properties:
        type:
          type: string
          description: Indicates the type of function. Required.
        etag:
          type: string
          description: The current entity tag for the function. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        properties:
          type: object
          properties:
            inputs:
              type: array
              items:
                $ref: "#/components/schemas/FunctionInput"
              description: ":vartype inputs: list[~azure.mgmt.streamanalytics.models.FunctionInput]"
            output:
              $ref: "#/components/schemas/FunctionOutput"
              description: Describes the output of a function.
            binding:
              $ref: "#/components/schemas/FunctionBinding"
              description: The physical binding of the function. For example, in the Azure Machine Learning web service’s case, this describes the endpoint.
      required:
        - type
    FunctionInput:
      type: object
      description: Describes one input parameter of a function.
      properties:
        dataType:
          type: string
          description: The (Azure Stream Analytics supported) data type of the function input parameter. A list of valid Azure Stream Analytics data types are described at https://msdn.microsoft.com/en-us/library/azure/dn835065.aspx.
        isConfigurationParameter:
          type: boolean
          description: A flag indicating if the parameter is a configuration parameter. True if this input parameter is expected to be a constant. Default is false.
    FunctionOutput:
      type: object
      description: Describes the output of a function.
      properties:
        dataType:
          type: string
          description: The (Azure Stream Analytics supported) data type of the function output. A list of valid Azure Stream Analytics data types are described at https://msdn.microsoft.com/en-us/library/azure/dn835065.aspx.
    FunctionBinding:
      type: object
      description: "The physical binding of the function. For example, in the Azure Machine Learning web service’s case, this describes the endpoint. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AzureMachineLearningStudioFunctionBinding, AzureMachineLearningServiceFunctionBinding, CSharpFunctionBinding, JavaScriptFunctionBinding All required parameters must be populated in order to send to Azure."
      properties:
        type:
          type: string
          description: Indicates the function binding type. Required.
      required:
        - type
    FunctionFlattened:
      type: object
      description: Flattened view of Function ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        etag:
          type: string
          description: The current entity tag for the function. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        properties:
          type: object
          properties:
            inputs:
              type: array
              items:
                $ref: "#/components/schemas/FunctionInput"
              description: ":vartype inputs: list[~azure.mgmt.streamanalytics.models.FunctionInput]"
            output:
              $ref: "#/components/schemas/FunctionOutput"
              description: Describes the output of a function.
            binding:
              $ref: "#/components/schemas/FunctionBinding"
              description: The physical binding of the function. For example, in the Azure Machine Learning web service’s case, this describes the endpoint.
    PagedFunctionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FunctionFlattened"
        nextLink:
          type: string
    Function:
      type: object
      description: A function object, containing all information associated with the named function. All functions are contained under a streaming job. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        properties:
          $ref: "#/components/schemas/FunctionProperties"
          description: The properties that are associated with a function.
    ResourceTestStatus:
      type: object
      description: Describes the status of the test operation along with error information, if applicable. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        status:
          type: string
          description: The status of the test operation.
        error:
          $ref: "#/components/schemas/ErrorResponse"
          description: Describes the error that occurred.
    ErrorResponse:
      type: object
      description: Describes the error that occurred. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        code:
          type: string
          description: Error code associated with the error that occurred.
        message:
          type: string
          description: Describes the error in detail.
    InputProperties:
      type: object
      description: "The properties that are associated with an input. You probably want to use the sub-classes and not this class directly. Known sub-classes are: ReferenceInputProperties, StreamInputProperties Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure."
      properties:
        type:
          type: string
          description: Indicates whether the input is a source of reference data or stream data. Required on PUT (CreateOrReplace) requests. Required.
        serialization:
          $ref: "#/components/schemas/Serialization"
          description: Describes how data from an input is serialized or how data is serialized when written to an output. Required on PUT (CreateOrReplace) requests.
        diagnostics:
          $ref: "#/components/schemas/Diagnostics"
          description: Describes conditions applicable to the Input, Output, or the job overall, that warrant customer attention.
        etag:
          type: string
          description: The current entity tag for the input. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        compression:
          $ref: "#/components/schemas/Compression"
          description: Describes how input data is compressed.
        partitionKey:
          type: string
          description: partitionKey Describes a key in the input data which is used for partitioning the input data.
        watermarkSettings:
          $ref: "#/components/schemas/InputWatermarkProperties"
          description: Settings which determine whether to read watermark events.
      required:
        - type
    Serialization:
      type: object
      description: "Describes how data from an input is serialized or how data is serialized when written to an output. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AvroSerialization, CsvSerialization, CustomClrSerialization, DeltaSerialization, JsonSerialization, ParquetSerialization All required parameters must be populated in order to send to Azure."
      properties:
        type:
          type: string
          description: 'Indicates the type of serialization that the input or output uses. Required on PUT (CreateOrReplace) requests. Required. Known values are: "Csv", "Avro", "Json", "CustomClr", "Parquet", and "Delta".'
      required:
        - type
    Diagnostics:
      type: object
      description: Describes conditions applicable to the Input, Output, or the job overall, that warrant customer attention. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        conditions:
          type: array
          items:
            $ref: "#/components/schemas/DiagnosticCondition"
          description: A collection of zero or more conditions applicable to the resource, or to the job overall, that warrant customer attention.
    DiagnosticCondition:
      type: object
      description: Condition applicable to the resource, or to the job overall, that warrant customer attention. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        since:
          type: string
          description: The UTC timestamp of when the condition started. Customers should be able to find a corresponding event in the ops log around this time.
        code:
          type: string
          description: The opaque diagnostic code.
        message:
          type: string
          description: The human-readable message describing the condition in detail. Localized in the Accept-Language of the client request.
    Compression:
      type: object
      description: Describes how input data is compressed. All required parameters must be populated in order to send to Azure.
      properties:
        type:
          type: string
          description: 'Indicates the type of compression that the input uses. Required on PUT (CreateOrReplace) requests. Known values are: "None", "GZip", and "Deflate".'
      required:
        - type
    InputWatermarkProperties:
      type: object
      description: Settings which determine whether to read watermark events.
      properties:
        watermarkMode:
          type: string
          description: 'The input watermark mode. Known values are: "None" and "ReadWatermark".'
    InputFlattened:
      type: object
      description: Flattened view of Input ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        serialization:
          $ref: "#/components/schemas/Serialization"
          description: Describes how data from an input is serialized or how data is serialized when written to an output. Required on PUT (CreateOrReplace) requests.
        diagnostics:
          $ref: "#/components/schemas/Diagnostics"
          description: Describes conditions applicable to the Input, Output, or the job overall, that warrant customer attention.
        etag:
          type: string
          description: The current entity tag for the input. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        compression:
          $ref: "#/components/schemas/Compression"
          description: Describes how input data is compressed.
        partitionKey:
          type: string
          description: partitionKey Describes a key in the input data which is used for partitioning the input data.
        watermarkSettings:
          $ref: "#/components/schemas/InputWatermarkProperties"
          description: Settings which determine whether to read watermark events.
    PagedInputFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/InputFlattened"
        nextLink:
          type: string
    Input:
      type: object
      description: An input object, containing all information associated with the named input. All inputs are contained under a streaming job. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        properties:
          $ref: "#/components/schemas/InputProperties"
          description: The properties that are associated with an input. Required on PUT (CreateOrReplace) requests.
    OperationDisplay:
      type: object
      description: Contains the localized display information for this particular operation / action. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name.
        resource:
          type: string
          description: The localized friendly form of the resource type related to this action/operation.
        operation:
          type: string
          description: The localized friendly name for the operation.
        description:
          type: string
          description: The localized friendly description for the operation.
    Operation:
      type: object
      description: A Stream Analytics REST API operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The name of the operation being performed on this particular object.
        isDataAction:
          type: boolean
          description: Indicates whether the operation is a data action.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Contains the localized display information for this particular operation / action.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    OutputDataSource:
      type: object
      description: "Describes the data source that output will be written to. You probably want to use the sub-classes and not this class directly. Known sub-classes are: GatewayMessageBusOutputDataSource, AzureFunctionOutputDataSource, PostgreSQLOutputDataSource, AzureDataLakeStoreOutputDataSource, EventHubV2OutputDataSource, AzureDataExplorerOutputDataSource, EventHubOutputDataSource, ServiceBusQueueOutputDataSource, ServiceBusTopicOutputDataSource, AzureSynapseOutputDataSource, AzureSqlDatabaseOutputDataSource, BlobOutputDataSource, DocumentDbOutputDataSource, AzureTableOutputDataSource, PowerBIOutputDataSource, RawOutputDatasource All required parameters must be populated in order to send to Azure."
      properties:
        type:
          type: string
          description: Indicates the type of data source output will be written to. Required on PUT (CreateOrReplace) requests. Required.
      required:
        - type
    LastOutputEventTimestamp:
      type: object
      description: An output event timestamp.
      properties:
        lastOutputEventTime:
          type: string
          description: The last output event time.
        lastUpdateTime:
          type: string
          description: The time that the last update happened.
    OutputWatermarkProperties:
      type: object
      description: Settings which determine whether to send watermarks to downstream.
      properties:
        watermarkMode:
          type: string
          description: 'The output watermark mode. Known values are: "None", "SendCurrentPartitionWatermark", and "SendLowestWatermarkAcrossPartitions".'
        maxWatermarkDifferenceAcrossPartitions:
          type: string
          description: Describes the maximal delta between the fastest and slowest partitions, so the out of order window that catches all necessary events in downstream jobs is well defined.
    OutputFlattened:
      type: object
      description: Flattened view of Output ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        datasource:
          $ref: "#/components/schemas/OutputDataSource"
          description: Describes the data source that output will be written to. Required on PUT (CreateOrReplace) requests.
        timeWindow:
          type: string
          description: The time frame for filtering Stream Analytics job outputs.
        sizeWindow:
          type: integer
          description: The size window to constrain a Stream Analytics output to.
        serialization:
          $ref: "#/components/schemas/Serialization"
          description: Describes how data from an input is serialized or how data is serialized when written to an output. Required on PUT (CreateOrReplace) requests.
        diagnostics:
          $ref: "#/components/schemas/Diagnostics"
          description: Describes conditions applicable to the Input, Output, or the job overall, that warrant customer attention.
        etag:
          type: string
          description: The current entity tag for the output. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        lastOutputEventTimestamps:
          type: array
          items:
            $ref: "#/components/schemas/LastOutputEventTimestamp"
          description: A list of the last output event times for each output partition. The index of the array corresponds to the partition number.
        watermarkSettings:
          $ref: "#/components/schemas/OutputWatermarkProperties"
          description: Settings which determine whether to send watermarks to downstream.
    PagedOutputFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/OutputFlattened"
        nextLink:
          type: string
    Output:
      type: object
      description: An output object, containing all information associated with the named output. All outputs are contained under a streaming job. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        properties:
          type: object
          properties:
            datasource:
              $ref: "#/components/schemas/OutputDataSource"
              description: Describes the data source that output will be written to. Required on PUT (CreateOrReplace) requests.
            timeWindow:
              type: string
              description: The time frame for filtering Stream Analytics job outputs.
            sizeWindow:
              type: integer
              description: The size window to constrain a Stream Analytics output to.
            serialization:
              $ref: "#/components/schemas/Serialization"
              description: Describes how data from an input is serialized or how data is serialized when written to an output. Required on PUT (CreateOrReplace) requests.
            diagnostics:
              $ref: "#/components/schemas/Diagnostics"
              description: Describes conditions applicable to the Input, Output, or the job overall, that warrant customer attention.
            etag:
              type: string
              description: The current entity tag for the output. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
            lastOutputEventTimestamps:
              type: array
              items:
                $ref: "#/components/schemas/LastOutputEventTimestamp"
              description: A list of the last output event times for each output partition. The index of the array corresponds to the partition number.
            watermarkSettings:
              $ref: "#/components/schemas/OutputWatermarkProperties"
              description: Settings which determine whether to send watermarks to downstream.
    PrivateEndpointProperties:
      type: object
      description: The properties associated with a private endpoint. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        createdDate:
          type: string
          description: The date when this private endpoint was created.
        manualPrivateLinkServiceConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateLinkServiceConnection"
          description: A list of connections to the remote resource. Immutable after it is set.
    PrivateLinkServiceConnection:
      type: object
      description: A grouping of information about the connection to the remote resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        properties:
          type: object
          properties:
            privateLinkServiceId:
              type: string
              description: The resource id of the private link service. Required on PUT (CreateOrUpdate) requests.
            groupIds:
              type: array
              items:
                type: string
              description: The ID(s) of the group(s) obtained from the remote resource that this private endpoint should connect to. Required on PUT (CreateOrUpdate) requests.
            requestMessage:
              type: string
              description: A message passed to the owner of the remote resource with this connection request. Restricted to 140 chars.
            privateLinkServiceConnectionState:
              $ref: "#/components/schemas/PrivateLinkConnectionState"
              description: A collection of read-only information about the state of the connection to the private remote resource.
    PrivateLinkConnectionState:
      type: object
      description: A collection of read-only information about the state of the connection to the private remote resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        status:
          type: string
          description: Indicates whether the connection has been Approved/Rejected/Removed by the owner of the remote resource/service.
        description:
          type: string
          description: The reason for approval/rejection of the connection.
        actionsRequired:
          type: string
          description: A message indicating if changes on the service provider require any updates on the consumer.
    PrivateEndpointFlattened:
      type: object
      description: Flattened view of PrivateEndpoint ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        etag:
          type: string
          description: Unique opaque string (generally a GUID) that represents the metadata state of the resource (private endpoint) and changes whenever the resource is updated. Required on PUT (CreateOrUpdate) requests.
        createdDate:
          type: string
          description: The date when this private endpoint was created.
        manualPrivateLinkServiceConnections:
          type: array
          items:
            $ref: "#/components/schemas/PrivateLinkServiceConnection"
          description: A list of connections to the remote resource. Immutable after it is set.
    PagedPrivateEndpointFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointFlattened"
        nextLink:
          type: string
    PrivateEndpoint:
      type: object
      description: Complete information about the private endpoint. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        properties:
          $ref: "#/components/schemas/PrivateEndpointProperties"
          description: The properties associated with a private endpoint.
        etag:
          type: string
          description: Unique opaque string (generally a GUID) that represents the metadata state of the resource (private endpoint) and changes whenever the resource is updated. Required on PUT (CreateOrUpdate) requests.
    GetStreamingJobSkuResultSku:
      type: object
      description: The properties that are associated with a SKU.
      properties:
        name:
          type: string
          description: The name of the SKU. "Standard"
    SkuCapacity:
      type: object
      description: Describes scaling information of a SKU. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        minimum:
          type: integer
          description: Specifies the minimum streaming units that the streaming job can use.
        maximum:
          type: integer
          description: Specifies the maximum streaming units that the streaming job can use.
        default:
          type: integer
          description: Specifies the default streaming units that the streaming job can use.
        scaleType:
          type: string
          description: 'The scale type applicable to the SKU. Known values are: "automatic", "manual", and "none".'
        allowedValues:
          type: array
          items:
            type: integer
          description: Specifies the valid streaming units a streaming job can scale to.
    GetStreamingJobSkuResult:
      type: object
      description: Describes an available SKU information. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        resourceType:
          type: string
          description: The type of resource the SKU applies to. "Microsoft.StreamAnalytics/streamingjobs"
        sku:
          $ref: "#/components/schemas/GetStreamingJobSkuResultSku"
          description: The properties that are associated with a SKU.
        capacity:
          $ref: "#/components/schemas/SkuCapacity"
          description: Describes scaling information of a SKU.
    PagedGetStreamingJobSkuResult:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/GetStreamingJobSkuResult"
        nextLink:
          type: string
    Sku:
      type: object
      description: The properties that are associated with a SKU.
      properties:
        name:
          type: string
          description: The name of the SKU. Required on PUT (CreateOrReplace) requests. "Standard"
        capacity:
          type: integer
          description: The capacity of the SKU.
    Identity:
      type: object
      description: Describes how identity is verified. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        tenantId:
          type: string
          description: The tenantId of the identity.
        principalId:
          type: string
          description: The principalId of the identity.
        type:
          type: string
          description: The type of identity, can be SystemAssigned or UserAssigned.
        userAssignedIdentities:
          type: object
          properties: {}
          description: The user assigned identities associated with the streaming job resource.
    Transformation:
      type: object
      description: A transformation object, containing all information associated with the named transformation. All transformations are contained under a streaming job. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        properties:
          type: object
          properties:
            streamingUnits:
              type: integer
              description: Specifies the number of streaming units that the streaming job uses.
            validStreamingUnits:
              type: array
              items:
                type: integer
              description: Specifies the valid streaming units a streaming job can scale to.
            query:
              type: string
              description: "Specifies the query that will be run in the streaming job. You can learn more about the Stream Analytics Query Language (SAQL) here: https://msdn.microsoft.com/library/azure/dn834998 . Required on PUT (CreateOrReplace) requests."
            etag:
              type: string
              description: The current entity tag for the transformation. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
    JobStorageAccount:
      type: object
      description: The properties that are associated with an Azure Storage account with MSI.
      properties:
        accountName:
          type: string
          description: The name of the Azure Storage account. Required on PUT (CreateOrReplace) requests.
        accountKey:
          type: string
          description: The account key for the Azure Storage account. Required on PUT (CreateOrReplace) requests.
        authenticationMode:
          type: string
          description: 'Authentication Mode. Known values are: "Msi", "UserToken", and "ConnectionString".'
    External:
      type: object
      description: The storage account where the custom code artifacts are located.
      properties:
        storageAccount:
          $ref: "#/components/schemas/StorageAccount"
          description: The properties that are associated with an Azure Storage account.
        container:
          type: string
          description: The UserCustomCode container.
        path:
          type: string
          description: The UserCustomCode path.
        refreshConfiguration:
          $ref: "#/components/schemas/RefreshConfiguration"
          description: The refresh parameters for any/all updatable user defined functions present in the job config.
    StorageAccount:
      type: object
      description: The properties that are associated with an Azure Storage account.
      properties:
        accountName:
          type: string
          description: The name of the Azure Storage account. Required on PUT (CreateOrReplace) requests.
        accountKey:
          type: string
          description: The account key for the Azure Storage account. Required on PUT (CreateOrReplace) requests.
        authenticationMode:
          type: string
          description: 'Authentication Mode. Known values are: "Msi", "UserToken", and "ConnectionString".'
    RefreshConfiguration:
      type: object
      description: The refresh parameters for any/all updatable user defined functions present in the job config.
      properties:
        pathPattern:
          type: string
          description: The blob path pattern. Not a regular expression. It represents a pattern against which blob names will be matched to determine whether or not they should be included as input or output to the job. See https://docs.microsoft.com/en-us/rest/api/streamanalytics/stream-analytics-input or https://docs.microsoft.com/en-us/rest/api/streamanalytics/stream-analytics-output for a more detailed explanation and example.
        dateFormat:
          type: string
          description: The date format. Wherever {date} appears in pathPattern, the value of this property is used as the date format instead.
        timeFormat:
          type: string
          description: The time format. Wherever {time} appears in pathPattern, the value of this property is used as the time format instead.
        refreshInterval:
          type: string
          description: The refresh interval.
        refreshType:
          type: string
          description: 'This property indicates which data refresh option to use, Blocking or Nonblocking. Known values are: "Blocking" and "Nonblocking".'
    ClusterInfo:
      type: object
      description: The properties associated with a Stream Analytics cluster.
      properties:
        id:
          type: string
          description: The resource id of cluster.
    StreamingJobFlattened:
      type: object
      description: Flattened view of StreamingJob ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives.
        sku:
          $ref: "#/components/schemas/Sku"
          description: Describes the SKU of the streaming job. Required on PUT (CreateOrReplace) requests.
        identity:
          $ref: "#/components/schemas/Identity"
          description: Describes the managed identity assigned to this job that can be used to authenticate with inputs and outputs.
        jobId:
          type: string
          description: A GUID uniquely identifying the streaming job. This GUID is generated upon creation of the streaming job.
        provisioningState:
          type: string
          description: Describes the provisioning status of the streaming job.
        jobState:
          type: string
          description: Describes the state of the streaming job.
        jobType:
          type: string
          description: "Describes the type of the job. Valid modes are `Cloud` and 'Edge'. Known values are: \"Cloud\" and \"Edge\"."
        outputStartMode:
          type: string
          description: 'This property should only be utilized when it is desired that the job be started immediately upon creation. Value may be JobStartTime, CustomTime, or LastOutputEventTime to indicate whether the starting point of the output event stream should start whenever the job is started, start at a custom user time stamp specified via the outputStartTime property, or start from the last event output time. Known values are: "JobStartTime", "CustomTime", and "LastOutputEventTime".'
        outputStartTime:
          type: string
          format: date-time
          description: Value is either an ISO-8601 formatted time stamp that indicates the starting point of the output event stream, or null to indicate that the output event stream will start whenever the streaming job is started. This property must have a value if outputStartMode is set to CustomTime.
        lastOutputEventTime:
          type: string
          format: date-time
          description: Value is either an ISO-8601 formatted timestamp indicating the last output event time of the streaming job or null indicating that output has not yet been produced. In case of multiple outputs or multiple streams, this shows the latest value in that set.
        eventsOutOfOrderPolicy:
          type: string
          description: 'Indicates the policy to apply to events that arrive out of order in the input event stream. Known values are: "Adjust" and "Drop".'
        outputErrorPolicy:
          type: string
          description: 'Indicates the policy to apply to events that arrive at the output and cannot be written to the external storage due to being malformed (missing column values, column values of wrong type or size). Known values are: "Stop" and "Drop".'
        eventsOutOfOrderMaxDelayInSeconds:
          type: integer
          description: The maximum tolerable delay in seconds where out-of-order events can be adjusted to be back in order.
        eventsLateArrivalMaxDelayInSeconds:
          type: integer
          description: The maximum tolerable delay in seconds where events arriving late could be included. Supported range is -1 to 1814399 (20.23:59:59 days) and -1 is used to specify wait indefinitely. If the property is absent, it is interpreted to have a value of -1.
        dataLocale:
          type: string
          description: The data locale of the stream analytics job. Value should be the name of a supported .NET Culture from the set https://msdn.microsoft.com/en-us/library/system.globalization.culturetypes(v=vs.110).aspx. Defaults to 'en-US' if none specified.
        compatibilityLevel:
          type: string
          description: 'Controls certain runtime behaviors of the streaming job. Known values are: "1.0" and "1.2".'
        createdDate:
          type: string
          format: date-time
          description: Value is an ISO-8601 formatted UTC timestamp indicating when the streaming job was created.
        inputs:
          type: array
          items:
            $ref: "#/components/schemas/Input"
          description: A list of one or more inputs to the streaming job. The name property for each input is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual input.
        transformation:
          $ref: "#/components/schemas/Transformation"
          description: Indicates the query and the number of streaming units to use for the streaming job. The name property of the transformation is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
        outputs:
          type: array
          items:
            $ref: "#/components/schemas/Output"
          description: A list of one or more outputs for the streaming job. The name property for each output is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual output.
        functions:
          type: array
          items:
            $ref: "#/components/schemas/Function"
          description: A list of one or more functions for the streaming job. The name property for each function is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
        etag:
          type: string
          description: The current entity tag for the streaming job. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
        jobStorageAccount:
          $ref: "#/components/schemas/JobStorageAccount"
          description: The properties that are associated with an Azure Storage account with MSI.
        contentStoragePolicy:
          type: string
          description: 'Valid values are JobStorageAccount and SystemAccount. If set to JobStorageAccount, this requires the user to also specify jobStorageAccount property. . Known values are: "SystemAccount" and "JobStorageAccount".'
        externals:
          $ref: "#/components/schemas/External"
          description: The storage account where the custom code artifacts are located.
        cluster:
          $ref: "#/components/schemas/ClusterInfo"
          description: The cluster which streaming jobs will run on.
    PagedStreamingJobFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StreamingJobFlattened"
        nextLink:
          type: string
    StreamingJob:
      type: object
      description: A streaming job object, containing all information associated with the named streaming job. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource Id for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. Ex- Microsoft.Compute/virtualMachines or Microsoft.Storage/storageAccounts.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives.
        sku:
          $ref: "#/components/schemas/Sku"
          description: Describes the SKU of the streaming job. Required on PUT (CreateOrReplace) requests.
        identity:
          $ref: "#/components/schemas/Identity"
          description: Describes the managed identity assigned to this job that can be used to authenticate with inputs and outputs.
        properties:
          type: object
          properties:
            sku:
              $ref: "#/components/schemas/Sku"
              description: Describes the SKU of the streaming job. Required on PUT (CreateOrReplace) requests.
            jobId:
              type: string
              description: A GUID uniquely identifying the streaming job. This GUID is generated upon creation of the streaming job.
            provisioningState:
              type: string
              description: Describes the provisioning status of the streaming job.
            jobState:
              type: string
              description: Describes the state of the streaming job.
            jobType:
              type: string
              description: "Describes the type of the job. Valid modes are `Cloud` and 'Edge'. Known values are: \"Cloud\" and \"Edge\"."
            outputStartMode:
              type: string
              description: 'This property should only be utilized when it is desired that the job be started immediately upon creation. Value may be JobStartTime, CustomTime, or LastOutputEventTime to indicate whether the starting point of the output event stream should start whenever the job is started, start at a custom user time stamp specified via the outputStartTime property, or start from the last event output time. Known values are: "JobStartTime", "CustomTime", and "LastOutputEventTime".'
            outputStartTime:
              type: string
              format: date-time
              description: Value is either an ISO-8601 formatted time stamp that indicates the starting point of the output event stream, or null to indicate that the output event stream will start whenever the streaming job is started. This property must have a value if outputStartMode is set to CustomTime.
            lastOutputEventTime:
              type: string
              format: date-time
              description: Value is either an ISO-8601 formatted timestamp indicating the last output event time of the streaming job or null indicating that output has not yet been produced. In case of multiple outputs or multiple streams, this shows the latest value in that set.
            eventsOutOfOrderPolicy:
              type: string
              description: 'Indicates the policy to apply to events that arrive out of order in the input event stream. Known values are: "Adjust" and "Drop".'
            outputErrorPolicy:
              type: string
              description: 'Indicates the policy to apply to events that arrive at the output and cannot be written to the external storage due to being malformed (missing column values, column values of wrong type or size). Known values are: "Stop" and "Drop".'
            eventsOutOfOrderMaxDelayInSeconds:
              type: integer
              description: The maximum tolerable delay in seconds where out-of-order events can be adjusted to be back in order.
            eventsLateArrivalMaxDelayInSeconds:
              type: integer
              description: The maximum tolerable delay in seconds where events arriving late could be included. Supported range is -1 to 1814399 (20.23:59:59 days) and -1 is used to specify wait indefinitely. If the property is absent, it is interpreted to have a value of -1.
            dataLocale:
              type: string
              description: The data locale of the stream analytics job. Value should be the name of a supported .NET Culture from the set https://msdn.microsoft.com/en-us/library/system.globalization.culturetypes(v=vs.110).aspx. Defaults to 'en-US' if none specified.
            compatibilityLevel:
              type: string
              description: 'Controls certain runtime behaviors of the streaming job. Known values are: "1.0" and "1.2".'
            createdDate:
              type: string
              format: date-time
              description: Value is an ISO-8601 formatted UTC timestamp indicating when the streaming job was created.
            inputs:
              type: array
              items:
                $ref: "#/components/schemas/Input"
              description: A list of one or more inputs to the streaming job. The name property for each input is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual input.
            transformation:
              $ref: "#/components/schemas/Transformation"
              description: Indicates the query and the number of streaming units to use for the streaming job. The name property of the transformation is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
            outputs:
              type: array
              items:
                $ref: "#/components/schemas/Output"
              description: A list of one or more outputs for the streaming job. The name property for each output is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual output.
            functions:
              type: array
              items:
                $ref: "#/components/schemas/Function"
              description: A list of one or more functions for the streaming job. The name property for each function is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
            etag:
              type: string
              description: The current entity tag for the streaming job. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
            jobStorageAccount:
              $ref: "#/components/schemas/JobStorageAccount"
              description: The properties that are associated with an Azure Storage account with MSI.
            contentStoragePolicy:
              type: string
              description: 'Valid values are JobStorageAccount and SystemAccount. If set to JobStorageAccount, this requires the user to also specify jobStorageAccount property. . Known values are: "SystemAccount" and "JobStorageAccount".'
            externals:
              $ref: "#/components/schemas/External"
              description: The storage account where the custom code artifacts are located.
            cluster:
              $ref: "#/components/schemas/ClusterInfo"
              description: The cluster which streaming jobs will run on.
    SubscriptionQuota:
      type: object
      description: Describes the current quota for the subscription. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        properties:
          type: object
          properties:
            maxCount:
              type: integer
              description: The max permitted usage of this resource.
            currentCount:
              type: integer
              description: The current usage of this resource.
    SubscriptionQuotaFlattened:
      type: object
      description: Flattened view of SubscriptionQuota ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        maxCount:
          type: integer
          description: The max permitted usage of this resource.
        currentCount:
          type: integer
          description: The current usage of this resource.
    SubscriptionQuotasListResultFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SubscriptionQuotaFlattened"
        nextLink:
          type: string
    TestQueryDiagnostics:
      type: object
      description: Diagnostics information related to query testing. All required parameters must be populated in order to send to Azure.
      properties:
        writeUri:
          type: string
          description: The SAS URI to the container or directory. Required.
        path:
          type: string
          description: The path to the subdirectory.
      required:
        - writeUri
    QueryTestingResult:
      type: object
      description: The result of the query testing request. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        error:
          $ref: "#/components/schemas/ErrorError"
          description: Error definition properties.
        status:
          type: string
          description: 'The status of the query testing request. Known values are: "Started", "Success", "CompilerError", "RuntimeError", "Timeout", and "UnknownError".'
        outputUri:
          type: string
          description: The SAS URL to the outputs payload.
    ErrorError:
      type: object
      description: Error definition properties.
      properties:
        code:
          type: string
          description: Error code.
        message:
          type: string
          description: Error message.
        target:
          type: string
          description: Error target.
        details:
          type: array
          items:
            $ref: "#/components/schemas/ErrorDetails"
          description: Error details.
    ErrorDetails:
      type: object
      description: Common error details representation.
      properties:
        code:
          type: string
          description: Error code.
        target:
          type: string
          description: Error target.
        message:
          type: string
          description: Error message.
    QueryInput:
      type: object
      description: An input for the query compilation. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: The name of the input. Required.
        type:
          type: string
          description: The type of the input, can be Stream or Reference. Required.
      required:
        - name
        - type
    QueryFunction:
      type: object
      description: A function for the query compilation. All required parameters must be populated in order to send to Azure.
      properties:
        name:
          type: string
          description: The name of the function. Required.
        type:
          type: string
          description: The type of the function. Required.
        bindingType:
          type: string
          description: The type of the function binding. Required.
        inputs:
          type: array
          items:
            $ref: "#/components/schemas/FunctionInput"
          description: The inputs for the function. Required.
        output:
          $ref: "#/components/schemas/FunctionOutput"
          description: An output for the function. Required.
      required:
        - name
        - type
        - bindingType
        - inputs
        - output
    QueryCompilationResult:
      type: object
      description: The result of the query compilation request. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        errors:
          type: array
          items:
            $ref: "#/components/schemas/QueryCompilationError"
          description: Error messages produced by the compiler.
        warnings:
          type: array
          items:
            type: string
          description: Warning messages produced by the compiler.
        inputs:
          type: array
          items:
            type: string
          description: All input names used by the query.
        outputs:
          type: array
          items:
            type: string
          description: All output names used by the query.
        functions:
          type: array
          items:
            type: string
          description: All function names used by the query.
    QueryCompilationError:
      type: object
      description: An error produced by the compiler. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        message:
          type: string
          description: The content of the error message.
        startLine:
          type: integer
          description: Describes the error location in the original query. Not set if isGlobal is true.
        startColumn:
          type: integer
          description: Describes the error location in the original query. Not set if isGlobal is true.
        endLine:
          type: integer
          description: Describes the error location in the original query. Not set if isGlobal is true.
        endColumn:
          type: integer
          description: Describes the error location in the original query. Not set if isGlobal is true.
        isGlobal:
          type: boolean
          description: Whether the error is not for a specific part but for the entire query.
    SampleInputResult:
      type: object
      description: The result of the sample input request. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        error:
          $ref: "#/components/schemas/ErrorError"
          description: Error definition properties.
        status:
          type: string
          description: 'The status of the sample input request. Known values are: "ReadAllEventsInRange", "NoEventsFoundInRange", and "ErrorConnectingToInput".'
        diagnostics:
          type: array
          items:
            type: string
          description: Diagnostics messages. E.g. message indicating some partitions from the input have no data.
        eventsDownloadUrl:
          type: string
          description: A SAS URL to download the sampled input data.
        lastArrivalTime:
          type: string
          description: The timestamp for the last event in the data. It is in DateTime format.
    TestDatasourceResult:
      type: object
      description: The result of the test input or output request. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        error:
          $ref: "#/components/schemas/ErrorError"
          description: Error definition properties.
        status:
          type: string
          description: 'The status of the sample output request. Known values are: "TestSucceeded" and "TestFailed".'
    TransformationFlattened:
      type: object
      description: Flattened view of Transformation ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource Id.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        streamingUnits:
          type: integer
          description: Specifies the number of streaming units that the streaming job uses.
        validStreamingUnits:
          type: array
          items:
            type: integer
          description: Specifies the valid streaming units a streaming job can scale to.
        query:
          type: string
          description: "Specifies the query that will be run in the streaming job. You can learn more about the Stream Analytics Query Language (SAQL) here: https://msdn.microsoft.com/library/azure/dn834998 . Required on PUT (CreateOrReplace) requests."
        etag:
          type: string
          description: The current entity tag for the transformation. This is an opaque string. You can use it to detect whether the resource has changed between requests. You can also use it in the If-Match or If-None-Match headers for write operations for optimistic concurrency.
  x-stackQL-resources:
    clusters:
      id: azure.streamanalytics.clusters
      name: clusters
      title: clusters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}?api-version=2020-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}?api-version=2020-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}?api-version=2020-03-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}?api-version=2020-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1clusters?api-version=2020-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters?api-version=2020-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_streaming_jobs:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}~1listStreamingJobs?api-version=2020-03-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/get"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/delete"
    functions:
      id: azure.streamanalytics.functions
      name: functions
      title: functions
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1functions~1{function_name}?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_replace:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1functions~1{function_name}?api-version=2021-10-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1functions~1{function_name}?api-version=2021-10-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1functions~1{function_name}?api-version=2021-10-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_streaming_job:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1functions?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        test:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1functions~1{function_name}~1test?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        retrieve_default_definition:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1functions~1{function_name}~1retrieveDefaultDefinition?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/functions/methods/get"
          - $ref: "#/components/x-stackQL-resources/functions/methods/list_by_streaming_job"
        insert:
          - $ref: "#/components/x-stackQL-resources/functions/methods/create_or_replace"
        update:
          - $ref: "#/components/x-stackQL-resources/functions/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/functions/methods/create_or_replace"
        delete:
          - $ref: "#/components/x-stackQL-resources/functions/methods/delete"
    inputs:
      id: azure.streamanalytics.inputs
      name: inputs
      title: inputs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1inputs~1{input_name}?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_replace:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1inputs~1{input_name}?api-version=2021-10-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1inputs~1{input_name}?api-version=2021-10-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1inputs~1{input_name}?api-version=2021-10-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_streaming_job:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1inputs?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        test:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1inputs~1{input_name}~1test?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/inputs/methods/get"
          - $ref: "#/components/x-stackQL-resources/inputs/methods/list_by_streaming_job"
        insert:
          - $ref: "#/components/x-stackQL-resources/inputs/methods/create_or_replace"
        update:
          - $ref: "#/components/x-stackQL-resources/inputs/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/inputs/methods/create_or_replace"
        delete:
          - $ref: "#/components/x-stackQL-resources/inputs/methods/delete"
    operations:
      id: azure.streamanalytics.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.StreamAnalytics~1operations?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    outputs:
      id: azure.streamanalytics.outputs
      name: outputs
      title: outputs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1outputs~1{output_name}?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_replace:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1outputs~1{output_name}?api-version=2021-10-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1outputs~1{output_name}?api-version=2021-10-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1outputs~1{output_name}?api-version=2021-10-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_streaming_job:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1outputs?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        test:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1outputs~1{output_name}~1test?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/outputs/methods/get"
          - $ref: "#/components/x-stackQL-resources/outputs/methods/list_by_streaming_job"
        insert:
          - $ref: "#/components/x-stackQL-resources/outputs/methods/create_or_replace"
        update:
          - $ref: "#/components/x-stackQL-resources/outputs/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/outputs/methods/create_or_replace"
        delete:
          - $ref: "#/components/x-stackQL-resources/outputs/methods/delete"
    private_endpoints:
      id: azure.streamanalytics.private_endpoints
      name: private_endpoints
      title: private_endpoints
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}~1privateEndpoints~1{private_endpoint_name}?api-version=2020-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}~1privateEndpoints~1{private_endpoint_name}?api-version=2020-03-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}~1privateEndpoints~1{private_endpoint_name}?api-version=2020-03-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1clusters~1{cluster_name}~1privateEndpoints?api-version=2020-03-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_endpoints/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_endpoints/methods/list_by_cluster"
        insert:
          - $ref: "#/components/x-stackQL-resources/private_endpoints/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/private_endpoints/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/private_endpoints/methods/delete"
    sku:
      id: azure.streamanalytics.sku
      name: sku
      title: sku
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1skus?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/sku/methods/list"
        insert: []
        update: []
        delete: []
    streaming_jobs:
      id: azure.streamanalytics.streaming_jobs
      name: streaming_jobs
      title: streaming_jobs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
          request:
            nativeCasing: camel
        create_or_replace:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}?api-version=2021-10-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}?api-version=2021-10-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}?api-version=2021-10-01-preview/delete"
          response:
            openAPIDocKey: "204"
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1streamingjobs?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
        start:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1start?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        stop:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1stop?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "204"
        scale:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1scale?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/streaming_jobs/methods/get"
          - $ref: "#/components/x-stackQL-resources/streaming_jobs/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/streaming_jobs/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/streaming_jobs/methods/create_or_replace"
        update:
          - $ref: "#/components/x-stackQL-resources/streaming_jobs/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/streaming_jobs/methods/create_or_replace"
        delete:
          - $ref: "#/components/x-stackQL-resources/streaming_jobs/methods/delete"
    subscriptions:
      id: azure.streamanalytics.subscriptions
      name: subscriptions
      title: subscriptions
      methods:
        list_quotas:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1locations~1{location}~1quotas?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        test_query:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1locations~1{location}~1testQuery?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        compile_query:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1locations~1{location}~1compileQuery?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        sample_input:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1locations~1{location}~1sampleInput?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        test_input:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1locations~1{location}~1testInput?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        test_output:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StreamAnalytics~1locations~1{location}~1testOutput?api-version=2021-10-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/subscriptions/methods/list_quotas"
        insert: []
        update: []
        delete: []
    transformations:
      id: azure.streamanalytics.transformations
      name: transformations
      title: transformations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1transformations~1{transformation_name}?api-version=2021-10-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_replace:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1transformations~1{transformation_name}?api-version=2021-10-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.StreamAnalytics~1streamingjobs~1{job_name}~1transformations~1{transformation_name}?api-version=2021-10-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/transformations/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/transformations/methods/create_or_replace"
        update:
          - $ref: "#/components/x-stackQL-resources/transformations/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/transformations/methods/create_or_replace"
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/clusters/{cluster_name}?api-version=2020-03-01-preview:
    get:
      operationId: Clusters_get
      description: Gets information about the specified cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClusterFlattened"
    put:
      operationId: Clusters_begin_create_or_update
      description: Creates a Stream Analytics Cluster or replaces an already existing cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the resource. Omit this value to always overwrite the current record set. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new resource to be created, but to prevent updating an existing record set. Other values will result in a 412 Pre-condition Failed response. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives.
                sku:
                  $ref: "#/components/schemas/ClusterSku"
                  description: The SKU of the cluster. This determines the size/capacity of the cluster. Required on PUT (CreateOrUpdate) requests.
                properties:
                  $ref: "#/components/schemas/ClusterProperties"
                  description: The properties associated with a Stream Analytics cluster.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cluster"
    patch:
      operationId: Clusters_begin_update
      description: Updates an existing cluster. This can be used to partially update (ie. update one or two properties) a cluster without affecting the rest of the cluster definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the resource. Omit this value to always overwrite the current record set. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives.
                sku:
                  $ref: "#/components/schemas/ClusterSku"
                  description: The SKU of the cluster. This determines the size/capacity of the cluster. Required on PUT (CreateOrUpdate) requests.
                properties:
                  $ref: "#/components/schemas/ClusterProperties"
                  description: The properties associated with a Stream Analytics cluster.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cluster"
    delete:
      operationId: Clusters_begin_delete
      description: Deletes the specified cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/clusters?api-version=2020-03-01-preview:
    get:
      operationId: Clusters_list_by_subscription
      description: Lists all of the clusters in the given subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/clusters?api-version=2020-03-01-preview:
    get:
      operationId: Clusters_list_by_resource_group
      description: Lists all of the clusters in the given resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/clusters/{cluster_name}/listStreamingJobs?api-version=2020-03-01-preview:
    post:
      operationId: Clusters_list_streaming_jobs
      description: Lists all of the streaming jobs in the given cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterJob"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/functions/{function_name}?api-version=2021-10-01-preview:
    get:
      operationId: Functions_get
      description: Gets details about the specified function.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: function_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the function. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FunctionFlattened"
    put:
      operationId: Functions_create_or_replace
      description: Creates a function or replaces an already existing function under an existing streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: function_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the function. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the function. Omit this value to always overwrite the current function. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new function to be created, but to prevent updating an existing function. Other values will result in a 412 Pre-condition Failed response. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  $ref: "#/components/schemas/FunctionProperties"
                  description: The properties that are associated with a function.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Function"
    patch:
      operationId: Functions_update
      description: Updates an existing function under an existing streaming job. This can be used to partially update (ie. update one or two properties) a function without affecting the rest the job or function definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: function_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the function. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the function. Omit this value to always overwrite the current function. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  $ref: "#/components/schemas/FunctionProperties"
                  description: The properties that are associated with a function.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Function"
    delete:
      operationId: Functions_delete
      description: Deletes a function from the streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: function_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the function. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/functions?api-version=2021-10-01-preview:
    get:
      operationId: Functions_list_by_streaming_job
      description: Lists all of the functions under the specified streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: The $select OData query parameter. This is a comma-separated list of structural properties to include in the response, or "\ *" to include all properties. By default, all properties are returned except diagnostics. Currently only accepts '*\ ' as a valid value. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFunctionFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/functions/{function_name}/test?api-version=2021-10-01-preview:
    post:
      operationId: Functions_begin_test
      description: Tests if the information provided for a function is valid. This can range from testing the connection to the underlying web service behind the function or making sure the function code provided is syntactically correct.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: function_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the function. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  $ref: "#/components/schemas/FunctionProperties"
                  description: The properties that are associated with a function.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceTestStatus"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/functions/{function_name}/retrieveDefaultDefinition?api-version=2021-10-01-preview:
    post:
      operationId: Functions_retrieve_default_definition
      description: Retrieves the default definition of a function based on the parameters specified.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: function_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the function. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                bindingType:
                  type: string
                  description: Indicates the function binding type. Required.
              required:
                - bindingType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Function"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/inputs/{input_name}?api-version=2021-10-01-preview:
    get:
      operationId: Inputs_get
      description: Gets details about the specified input.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: input_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the input. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InputFlattened"
    put:
      operationId: Inputs_create_or_replace
      description: Creates an input or replaces an already existing input under an existing streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: input_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the input. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the input. Omit this value to always overwrite the current input. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new input to be created, but to prevent updating an existing input. Other values will result in a 412 Pre-condition Failed response. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  $ref: "#/components/schemas/InputProperties"
                  description: The properties that are associated with an input. Required on PUT (CreateOrReplace) requests.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Input"
    patch:
      operationId: Inputs_update
      description: Updates an existing input under an existing streaming job. This can be used to partially update (ie. update one or two properties) an input without affecting the rest the job or input definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: input_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the input. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the input. Omit this value to always overwrite the current input. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  $ref: "#/components/schemas/InputProperties"
                  description: The properties that are associated with an input. Required on PUT (CreateOrReplace) requests.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Input"
    delete:
      operationId: Inputs_delete
      description: Deletes an input from the streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: input_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the input. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/inputs?api-version=2021-10-01-preview:
    get:
      operationId: Inputs_list_by_streaming_job
      description: Lists all of the inputs under the specified streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: The $select OData query parameter. This is a comma-separated list of structural properties to include in the response, or "\ *" to include all properties. By default, all properties are returned except diagnostics. Currently only accepts '*\ ' as a valid value. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedInputFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/inputs/{input_name}/test?api-version=2021-10-01-preview:
    post:
      operationId: Inputs_begin_test
      description: Tests whether an input’s datasource is reachable and usable by the Azure Stream Analytics service.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: input_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the input. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  $ref: "#/components/schemas/InputProperties"
                  description: The properties that are associated with an input. Required on PUT (CreateOrReplace) requests.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceTestStatus"
  /providers/Microsoft.StreamAnalytics/operations?api-version=2021-10-01-preview:
    get:
      operationId: streamanalytics_list
      description: Lists all of the available Stream Analytics related operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/outputs/{output_name}?api-version=2021-10-01-preview:
    get:
      operationId: Outputs_get
      description: Gets details about the specified output.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: output_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the output. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutputFlattened"
    put:
      operationId: Outputs_create_or_replace
      description: Creates an output or replaces an already existing output under an existing streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: output_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the output. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the output. Omit this value to always overwrite the current output. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new output to be created, but to prevent updating an existing output. Other values will result in a 412 Pre-condition Failed response. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  type: object
                  properties:
                    datasource:
                      $ref: "#/components/schemas/OutputDataSource"
                      description: Describes the data source that output will be written to. Required on PUT (CreateOrReplace) requests.
                    timeWindow:
                      type: string
                      description: The time frame for filtering Stream Analytics job outputs.
                    sizeWindow:
                      type: integer
                      description: The size window to constrain a Stream Analytics output to.
                    serialization:
                      $ref: "#/components/schemas/Serialization"
                      description: Describes how data from an input is serialized or how data is serialized when written to an output. Required on PUT (CreateOrReplace) requests.
                    watermarkSettings:
                      $ref: "#/components/schemas/OutputWatermarkProperties"
                      description: Settings which determine whether to send watermarks to downstream.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Output"
    patch:
      operationId: Outputs_update
      description: Updates an existing output under an existing streaming job. This can be used to partially update (ie. update one or two properties) an output without affecting the rest the job or output definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: output_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the output. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the output. Omit this value to always overwrite the current output. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  type: object
                  properties:
                    datasource:
                      $ref: "#/components/schemas/OutputDataSource"
                      description: Describes the data source that output will be written to. Required on PUT (CreateOrReplace) requests.
                    timeWindow:
                      type: string
                      description: The time frame for filtering Stream Analytics job outputs.
                    sizeWindow:
                      type: integer
                      description: The size window to constrain a Stream Analytics output to.
                    serialization:
                      $ref: "#/components/schemas/Serialization"
                      description: Describes how data from an input is serialized or how data is serialized when written to an output. Required on PUT (CreateOrReplace) requests.
                    watermarkSettings:
                      $ref: "#/components/schemas/OutputWatermarkProperties"
                      description: Settings which determine whether to send watermarks to downstream.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Output"
    delete:
      operationId: Outputs_delete
      description: Deletes an output from the streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: output_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the output. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/outputs?api-version=2021-10-01-preview:
    get:
      operationId: Outputs_list_by_streaming_job
      description: Lists all of the outputs under the specified streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $select
          in: query
          required: false
          schema:
            type: string
          description: The $select OData query parameter. This is a comma-separated list of structural properties to include in the response, or "\ *" to include all properties. By default, all properties are returned except diagnostics. Currently only accepts '*\ ' as a valid value. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOutputFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/outputs/{output_name}/test?api-version=2021-10-01-preview:
    post:
      operationId: Outputs_begin_test
      description: Tests whether an output’s datasource is reachable and usable by the Azure Stream Analytics service.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: output_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the output. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  type: object
                  properties:
                    datasource:
                      $ref: "#/components/schemas/OutputDataSource"
                      description: Describes the data source that output will be written to. Required on PUT (CreateOrReplace) requests.
                    timeWindow:
                      type: string
                      description: The time frame for filtering Stream Analytics job outputs.
                    sizeWindow:
                      type: integer
                      description: The size window to constrain a Stream Analytics output to.
                    serialization:
                      $ref: "#/components/schemas/Serialization"
                      description: Describes how data from an input is serialized or how data is serialized when written to an output. Required on PUT (CreateOrReplace) requests.
                    watermarkSettings:
                      $ref: "#/components/schemas/OutputWatermarkProperties"
                      description: Settings which determine whether to send watermarks to downstream.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceTestStatus"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/clusters/{cluster_name}/privateEndpoints/{private_endpoint_name}?api-version=2020-03-01-preview:
    get:
      operationId: PrivateEndpoints_get
      description: Gets information about the specified Private Endpoint.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: private_endpoint_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointFlattened"
    put:
      operationId: PrivateEndpoints_create_or_update
      description: Creates a Stream Analytics Private Endpoint or replaces an already existing Private Endpoint.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: private_endpoint_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the resource. Omit this value to always overwrite the current record set. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new resource to be created, but to prevent updating an existing record set. Other values will result in a 412 Pre-condition Failed response. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/PrivateEndpointProperties"
                  description: The properties associated with a private endpoint.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpoint"
    delete:
      operationId: PrivateEndpoints_begin_delete
      description: Delete the specified private endpoint.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: private_endpoint_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/clusters/{cluster_name}/privateEndpoints?api-version=2020-03-01-preview:
    get:
      operationId: PrivateEndpoints_list_by_cluster
      description: Lists the private endpoints in the cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateEndpointFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/skus?api-version=2021-10-01-preview:
    get:
      operationId: Sku_list
      description: Gets a list of available SKUs about the specified streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGetStreamingJobSkuResult"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}?api-version=2021-10-01-preview:
    get:
      operationId: StreamingJobs_get
      description: Gets details about the specified streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $expand
          in: query
          required: false
          schema:
            type: string
          description: The $expand OData query parameter. This is a comma-separated list of additional streaming job properties to include in the response, beyond the default set returned when this parameter is absent. The default set is all streaming job properties other than 'inputs', 'transformation', 'outputs', and 'functions'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamingJobFlattened"
    put:
      operationId: StreamingJobs_begin_create_or_replace
      description: Creates a streaming job or replaces an already existing streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the streaming job. Omit this value to always overwrite the current record set. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new streaming job to be created, but to prevent updating an existing record set. Other values will result in a 412 Pre-condition Failed response. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: Describes the SKU of the streaming job. Required on PUT (CreateOrReplace) requests.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: Describes the managed identity assigned to this job that can be used to authenticate with inputs and outputs.
                properties:
                  type: object
                  properties:
                    sku:
                      $ref: "#/components/schemas/Sku"
                      description: Describes the SKU of the streaming job. Required on PUT (CreateOrReplace) requests.
                    jobType:
                      type: string
                      description: "Describes the type of the job. Valid modes are `Cloud` and 'Edge'. Known values are: \"Cloud\" and \"Edge\"."
                    outputStartMode:
                      type: string
                      description: 'This property should only be utilized when it is desired that the job be started immediately upon creation. Value may be JobStartTime, CustomTime, or LastOutputEventTime to indicate whether the starting point of the output event stream should start whenever the job is started, start at a custom user time stamp specified via the outputStartTime property, or start from the last event output time. Known values are: "JobStartTime", "CustomTime", and "LastOutputEventTime".'
                    outputStartTime:
                      type: string
                      format: date-time
                      description: Value is either an ISO-8601 formatted time stamp that indicates the starting point of the output event stream, or null to indicate that the output event stream will start whenever the streaming job is started. This property must have a value if outputStartMode is set to CustomTime.
                    eventsOutOfOrderPolicy:
                      type: string
                      description: 'Indicates the policy to apply to events that arrive out of order in the input event stream. Known values are: "Adjust" and "Drop".'
                    outputErrorPolicy:
                      type: string
                      description: 'Indicates the policy to apply to events that arrive at the output and cannot be written to the external storage due to being malformed (missing column values, column values of wrong type or size). Known values are: "Stop" and "Drop".'
                    eventsOutOfOrderMaxDelayInSeconds:
                      type: integer
                      description: The maximum tolerable delay in seconds where out-of-order events can be adjusted to be back in order.
                    eventsLateArrivalMaxDelayInSeconds:
                      type: integer
                      description: The maximum tolerable delay in seconds where events arriving late could be included. Supported range is -1 to 1814399 (20.23:59:59 days) and -1 is used to specify wait indefinitely. If the property is absent, it is interpreted to have a value of -1.
                    dataLocale:
                      type: string
                      description: The data locale of the stream analytics job. Value should be the name of a supported .NET Culture from the set https://msdn.microsoft.com/en-us/library/system.globalization.culturetypes(v=vs.110).aspx. Defaults to 'en-US' if none specified.
                    compatibilityLevel:
                      type: string
                      description: 'Controls certain runtime behaviors of the streaming job. Known values are: "1.0" and "1.2".'
                    inputs:
                      type: array
                      items:
                        $ref: "#/components/schemas/Input"
                      description: A list of one or more inputs to the streaming job. The name property for each input is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual input.
                    transformation:
                      $ref: "#/components/schemas/Transformation"
                      description: Indicates the query and the number of streaming units to use for the streaming job. The name property of the transformation is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
                    outputs:
                      type: array
                      items:
                        $ref: "#/components/schemas/Output"
                      description: A list of one or more outputs for the streaming job. The name property for each output is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual output.
                    functions:
                      type: array
                      items:
                        $ref: "#/components/schemas/Function"
                      description: A list of one or more functions for the streaming job. The name property for each function is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
                    jobStorageAccount:
                      $ref: "#/components/schemas/JobStorageAccount"
                      description: The properties that are associated with an Azure Storage account with MSI.
                    contentStoragePolicy:
                      type: string
                      description: 'Valid values are JobStorageAccount and SystemAccount. If set to JobStorageAccount, this requires the user to also specify jobStorageAccount property. . Known values are: "SystemAccount" and "JobStorageAccount".'
                    externals:
                      $ref: "#/components/schemas/External"
                      description: The storage account where the custom code artifacts are located.
                    cluster:
                      $ref: "#/components/schemas/ClusterInfo"
                      description: The cluster which streaming jobs will run on.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamingJob"
    patch:
      operationId: StreamingJobs_update
      description: Updates an existing streaming job. This can be used to partially update (ie. update one or two properties) a streaming job without affecting the rest the job definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the streaming job. Omit this value to always overwrite the current record set. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives.
                sku:
                  $ref: "#/components/schemas/Sku"
                  description: Describes the SKU of the streaming job. Required on PUT (CreateOrReplace) requests.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: Describes the managed identity assigned to this job that can be used to authenticate with inputs and outputs.
                properties:
                  type: object
                  properties:
                    sku:
                      $ref: "#/components/schemas/Sku"
                      description: Describes the SKU of the streaming job. Required on PUT (CreateOrReplace) requests.
                    jobType:
                      type: string
                      description: "Describes the type of the job. Valid modes are `Cloud` and 'Edge'. Known values are: \"Cloud\" and \"Edge\"."
                    outputStartMode:
                      type: string
                      description: 'This property should only be utilized when it is desired that the job be started immediately upon creation. Value may be JobStartTime, CustomTime, or LastOutputEventTime to indicate whether the starting point of the output event stream should start whenever the job is started, start at a custom user time stamp specified via the outputStartTime property, or start from the last event output time. Known values are: "JobStartTime", "CustomTime", and "LastOutputEventTime".'
                    outputStartTime:
                      type: string
                      format: date-time
                      description: Value is either an ISO-8601 formatted time stamp that indicates the starting point of the output event stream, or null to indicate that the output event stream will start whenever the streaming job is started. This property must have a value if outputStartMode is set to CustomTime.
                    eventsOutOfOrderPolicy:
                      type: string
                      description: 'Indicates the policy to apply to events that arrive out of order in the input event stream. Known values are: "Adjust" and "Drop".'
                    outputErrorPolicy:
                      type: string
                      description: 'Indicates the policy to apply to events that arrive at the output and cannot be written to the external storage due to being malformed (missing column values, column values of wrong type or size). Known values are: "Stop" and "Drop".'
                    eventsOutOfOrderMaxDelayInSeconds:
                      type: integer
                      description: The maximum tolerable delay in seconds where out-of-order events can be adjusted to be back in order.
                    eventsLateArrivalMaxDelayInSeconds:
                      type: integer
                      description: The maximum tolerable delay in seconds where events arriving late could be included. Supported range is -1 to 1814399 (20.23:59:59 days) and -1 is used to specify wait indefinitely. If the property is absent, it is interpreted to have a value of -1.
                    dataLocale:
                      type: string
                      description: The data locale of the stream analytics job. Value should be the name of a supported .NET Culture from the set https://msdn.microsoft.com/en-us/library/system.globalization.culturetypes(v=vs.110).aspx. Defaults to 'en-US' if none specified.
                    compatibilityLevel:
                      type: string
                      description: 'Controls certain runtime behaviors of the streaming job. Known values are: "1.0" and "1.2".'
                    inputs:
                      type: array
                      items:
                        $ref: "#/components/schemas/Input"
                      description: A list of one or more inputs to the streaming job. The name property for each input is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual input.
                    transformation:
                      $ref: "#/components/schemas/Transformation"
                      description: Indicates the query and the number of streaming units to use for the streaming job. The name property of the transformation is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
                    outputs:
                      type: array
                      items:
                        $ref: "#/components/schemas/Output"
                      description: A list of one or more outputs for the streaming job. The name property for each output is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual output.
                    functions:
                      type: array
                      items:
                        $ref: "#/components/schemas/Function"
                      description: A list of one or more functions for the streaming job. The name property for each function is required when specifying this property in a PUT request. This property cannot be modify via a PATCH operation. You must use the PATCH API available for the individual transformation.
                    jobStorageAccount:
                      $ref: "#/components/schemas/JobStorageAccount"
                      description: The properties that are associated with an Azure Storage account with MSI.
                    contentStoragePolicy:
                      type: string
                      description: 'Valid values are JobStorageAccount and SystemAccount. If set to JobStorageAccount, this requires the user to also specify jobStorageAccount property. . Known values are: "SystemAccount" and "JobStorageAccount".'
                    externals:
                      $ref: "#/components/schemas/External"
                      description: The storage account where the custom code artifacts are located.
                    cluster:
                      $ref: "#/components/schemas/ClusterInfo"
                      description: The cluster which streaming jobs will run on.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamingJob"
    delete:
      operationId: StreamingJobs_begin_delete
      description: Deletes a streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/streamingjobs?api-version=2021-10-01-preview:
    get:
      operationId: StreamingJobs_list
      description: Lists all of the streaming jobs in the given subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $expand
          in: query
          required: false
          schema:
            type: string
          description: The $expand OData query parameter. This is a comma-separated list of additional streaming job properties to include in the response, beyond the default set returned when this parameter is absent. The default set is all streaming job properties other than 'inputs', 'transformation', 'outputs', and 'functions'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStreamingJobFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs?api-version=2021-10-01-preview:
    get:
      operationId: StreamingJobs_list_by_resource_group
      description: Lists all of the streaming jobs in the specified resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $expand
          in: query
          required: false
          schema:
            type: string
          description: The $expand OData query parameter. This is a comma-separated list of additional streaming job properties to include in the response, beyond the default set returned when this parameter is absent. The default set is all streaming job properties other than 'inputs', 'transformation', 'outputs', and 'functions'. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStreamingJobFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/start?api-version=2021-10-01-preview:
    post:
      operationId: StreamingJobs_begin_start
      description: Starts a streaming job. Once a job is started it will start processing input events and produce output.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                outputStartMode:
                  type: string
                  description: 'Value may be JobStartTime, CustomTime, or LastOutputEventTime to indicate whether the starting point of the output event stream should start whenever the job is started, start at a custom user time stamp specified via the outputStartTime property, or start from the last event output time. Known values are: "JobStartTime", "CustomTime", and "LastOutputEventTime".'
                outputStartTime:
                  type: string
                  format: date-time
                  description: Value is either an ISO-8601 formatted time stamp that indicates the starting point of the output event stream, or null to indicate that the output event stream will start whenever the streaming job is started. This property must have a value if outputStartMode is set to CustomTime.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/stop?api-version=2021-10-01-preview:
    post:
      operationId: StreamingJobs_begin_stop
      description: Stops a running streaming job. This will cause a running streaming job to stop processing input events and producing output.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/scale?api-version=2021-10-01-preview:
    post:
      operationId: StreamingJobs_begin_scale
      description: Scales a streaming job when the job is running.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                streamingUnits:
                  type: integer
                  description: Specifies the number of streaming units that the streaming job will scale to.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/locations/{location}/quotas?api-version=2021-10-01-preview:
    get:
      operationId: Subscriptions_list_quotas
      description: Retrieves the subscription's current quota information in a particular region.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: "The region to which the request is sent. You can find out which regions Azure Stream Analytics is supported in here: https://azure.microsoft.com/en-us/regions/. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionQuotasListResultFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/locations/{location}/testQuery?api-version=2021-10-01-preview:
    post:
      operationId: Subscriptions_begin_test_query
      description: Test the Stream Analytics query on a sample input.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: "The region to which the request is sent. You can find out which regions Azure Stream Analytics is supported in here: https://azure.microsoft.com/en-us/regions/. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                diagnostics:
                  $ref: "#/components/schemas/TestQueryDiagnostics"
                  description: Diagnostics information related to query testing.
                streamingJob:
                  $ref: "#/components/schemas/StreamingJob"
                  description: Stream analytics job object which defines the input, output, and transformation for the query testing. Required.
              required:
                - streamingJob
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryTestingResult"
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/locations/{location}/compileQuery?api-version=2021-10-01-preview:
    post:
      operationId: Subscriptions_compile_query
      description: Compile the Stream Analytics query.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: "The region to which the request is sent. You can find out which regions Azure Stream Analytics is supported in here: https://azure.microsoft.com/en-us/regions/. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                query:
                  type: string
                  description: The query to compile. Required.
                inputs:
                  type: array
                  items:
                    $ref: "#/components/schemas/QueryInput"
                  description: The inputs for the query compilation.
                functions:
                  type: array
                  items:
                    $ref: "#/components/schemas/QueryFunction"
                  description: The functions for the query compilation.
                jobType:
                  type: string
                  description: "Describes the type of the job. Valid values are `Cloud` and 'Edge'. Required. Known values are: \"Cloud\" and \"Edge\"."
                compatibilityLevel:
                  type: string
                  description: 'The query to compile. Known values are: "1.0" and "1.2".'
              required:
                - query
                - jobType
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryCompilationResult"
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/locations/{location}/sampleInput?api-version=2021-10-01-preview:
    post:
      operationId: Subscriptions_begin_sample_input
      description: Sample the Stream Analytics input data.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: "The region to which the request is sent. You can find out which regions Azure Stream Analytics is supported in here: https://azure.microsoft.com/en-us/regions/. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                input:
                  $ref: "#/components/schemas/Input"
                  description: The stream analytics input to sample.
                compatibilityLevel:
                  type: string
                  description: Defaults to the default ASA job compatibility level. Today it is 1.2.
                eventsUri:
                  type: string
                  description: The SAS URI of the storage blob for service to write the sampled events to. If this parameter is not provided, service will write events to he system account and share a temporary SAS URI to it.
                dataLocale:
                  type: string
                  description: Defaults to en-US.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SampleInputResult"
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/locations/{location}/testInput?api-version=2021-10-01-preview:
    post:
      operationId: Subscriptions_begin_test_input
      description: Test the Stream Analytics input.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: "The region to which the request is sent. You can find out which regions Azure Stream Analytics is supported in here: https://azure.microsoft.com/en-us/regions/. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                input:
                  $ref: "#/components/schemas/Input"
                  description: The stream analytics input to test. Required.
              required:
                - input
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestDatasourceResult"
  /subscriptions/{subscription_id}/providers/Microsoft.StreamAnalytics/locations/{location}/testOutput?api-version=2021-10-01-preview:
    post:
      operationId: Subscriptions_begin_test_output
      description: Test the Stream Analytics output.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: "The region to which the request is sent. You can find out which regions Azure Stream Analytics is supported in here: https://azure.microsoft.com/en-us/regions/. Required."
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                output:
                  $ref: "#/components/schemas/Output"
                  description: The stream analytics output to test. Required.
              required:
                - output
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestDatasourceResult"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.StreamAnalytics/streamingjobs/{job_name}/transformations/{transformation_name}?api-version=2021-10-01-preview:
    get:
      operationId: Transformations_get
      description: Gets details about the specified transformation.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: transformation_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the transformation. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TransformationFlattened"
    put:
      operationId: Transformations_create_or_replace
      description: Creates a transformation or replaces an already existing transformation under an existing streaming job.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: transformation_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the transformation. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the transformation. Omit this value to always overwrite the current transformation. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
          description: Set to '*' to allow a new transformation to be created, but to prevent updating an existing transformation. Other values will result in a 412 Pre-condition Failed response. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  type: object
                  properties:
                    streamingUnits:
                      type: integer
                      description: Specifies the number of streaming units that the streaming job uses.
                    validStreamingUnits:
                      type: array
                      items:
                        type: integer
                      description: Specifies the valid streaming units a streaming job can scale to.
                    query:
                      type: string
                      description: "Specifies the query that will be run in the streaming job. You can learn more about the Stream Analytics Query Language (SAQL) here: https://msdn.microsoft.com/library/azure/dn834998 . Required on PUT (CreateOrReplace) requests."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Transformation"
    patch:
      operationId: Transformations_update
      description: Updates an existing transformation under an existing streaming job. This can be used to partially update (ie. update one or two properties) a transformation without affecting the rest the job or transformation definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: job_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the streaming job. Required.
        - name: transformation_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the transformation. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: If-Match
          in: header
          required: false
          schema:
            type: string
          description: The ETag of the transformation. Omit this value to always overwrite the current transformation. Specify the last-seen ETag value to prevent accidentally overwriting concurrent changes. Default value is None.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Resource name.
                properties:
                  type: object
                  properties:
                    streamingUnits:
                      type: integer
                      description: Specifies the number of streaming units that the streaming job uses.
                    validStreamingUnits:
                      type: array
                      items:
                        type: integer
                      description: Specifies the valid streaming units a streaming job can scale to.
                    query:
                      type: string
                      description: "Specifies the query that will be run in the streaming job. You can learn more about the Stream Analytics Query Language (SAQL) here: https://msdn.microsoft.com/library/azure/dn834998 . Required on PUT (CreateOrReplace) requests."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Transformation"
