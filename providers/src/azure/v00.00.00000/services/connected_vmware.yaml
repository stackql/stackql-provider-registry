openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Arc-enabled VMware vSphere
  description: Arc-manage vSphere VMs, templates, networks and inventory through Azure (Microsoft.ConnectedVMwarevSphere).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-10-01-stackql-generated
  x-serviceAlias: connected_vmware
  x-sdkPackage: azure-mgmt-connectedvmware
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    ExtendedLocation:
      type: object
      description: The extended location.
      properties:
        type:
          type: string
          description: The extended location type.
        name:
          type: string
          description: The extended location name.
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    ResourceStatus:
      type: object
      description: The resource status information. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        type:
          type: string
          description: The type of the condition.
        status:
          type: string
          description: Status of the condition.
        reason:
          type: string
          description: The reason for the condition's status.
        message:
          type: string
          description: A human readable message indicating details about the status.
        severity:
          type: string
          description: Severity with which to treat failures of this type of condition.
        lastUpdatedAt:
          type: string
          format: date-time
          description: The last update time for this condition.
    ClusterFlattened:
      type: object
      description: Flattened view of Cluster ('properties' hoisted to the top level).
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        vCenterId:
          type: string
          description: Gets or sets the ARM Id of the vCenter resource in which this cluster resides.
        moRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the cluster.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the cluster.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the cluster.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        usedMemoryGB:
          type: integer
          description: Gets the used physical memory on the cluster in GB.
        totalMemoryGB:
          type: integer
          description: Gets the total amount of physical memory on the cluster in GB.
        usedCpuMHz:
          type: integer
          description: Gets the used CPU usage across all cores on the cluster in MHz.
        totalCpuMHz:
          type: integer
          description: Gets the max CPU usage across all cores on the cluster in MHz.
        datastoreIds:
          type: array
          items:
            type: string
          description: Gets the datastore ARM ids.
        networkIds:
          type: array
          items:
            type: string
          description: Gets the network ARM ids.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ClusterFlattened"
        nextLink:
          type: string
    Cluster:
      type: object
      description: Define the cluster. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            vCenterId:
              type: string
              description: Gets or sets the ARM Id of the vCenter resource in which this cluster resides.
            moRefId:
              type: string
              description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the cluster.
            inventoryItemId:
              type: string
              description: Gets or sets the inventory Item ID for the cluster.
            moName:
              type: string
              description: Gets or sets the vCenter Managed Object name for the cluster.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            usedMemoryGB:
              type: integer
              description: Gets the used physical memory on the cluster in GB.
            totalMemoryGB:
              type: integer
              description: Gets the total amount of physical memory on the cluster in GB.
            usedCpuMHz:
              type: integer
              description: Gets the used CPU usage across all cores on the cluster in MHz.
            totalCpuMHz:
              type: integer
              description: Gets the max CPU usage across all cores on the cluster in MHz.
            datastoreIds:
              type: array
              items:
                type: string
              description: Gets the datastore ARM ids.
            networkIds:
              type: array
              items:
                type: string
              description: Gets the network ARM ids.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - location
    DatastoreFlattened:
      type: object
      description: Flattened view of Datastore ('properties' hoisted to the top level).
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        vCenterId:
          type: string
          description: Gets or sets the ARM Id of the vCenter resource in which this datastore resides.
        moRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the datastore.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the datastore.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the datastore.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        capacityGB:
          type: integer
          description: Gets or sets Maximum capacity of this datastore in GBs.
        freeSpaceGB:
          type: integer
          description: Gets or sets Available space of this datastore in GBs.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedDatastoreFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DatastoreFlattened"
        nextLink:
          type: string
    Datastore:
      type: object
      description: Define the datastore. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            vCenterId:
              type: string
              description: Gets or sets the ARM Id of the vCenter resource in which this datastore resides.
            moRefId:
              type: string
              description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the datastore.
            inventoryItemId:
              type: string
              description: Gets or sets the inventory Item ID for the datastore.
            moName:
              type: string
              description: Gets or sets the vCenter Managed Object name for the datastore.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            capacityGB:
              type: integer
              description: Gets or sets Maximum capacity of this datastore in GBs.
            freeSpaceGB:
              type: integer
              description: Gets or sets Available space of this datastore in GBs.
            provisioningState:
              type: string
              description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - location
    HostFlattened:
      type: object
      description: Flattened view of Host ('properties' hoisted to the top level).
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        vCenterId:
          type: string
          description: Gets or sets the ARM Id of the vCenter resource in which this host resides.
        moRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the host.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the host.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the host.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        overallMemoryUsageGB:
          type: integer
          description: Gets the used physical memory on the host in GB.
        memorySizeGB:
          type: integer
          description: Gets the total amount of physical memory on the host in GB.
        overallCpuUsageMHz:
          type: integer
          description: Gets the used CPU usage across all cores in MHz.
        cpuMhz:
          type: integer
          description: Gets the max CPU usage across all cores in MHz.
        datastoreIds:
          type: array
          items:
            type: string
          description: Gets the datastore ARM ids.
        networkIds:
          type: array
          items:
            type: string
          description: Gets the network ARM ids.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedHostFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/HostFlattened"
        nextLink:
          type: string
    Host:
      type: object
      description: Define the host. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            vCenterId:
              type: string
              description: Gets or sets the ARM Id of the vCenter resource in which this host resides.
            moRefId:
              type: string
              description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the host.
            inventoryItemId:
              type: string
              description: Gets or sets the inventory Item ID for the host.
            moName:
              type: string
              description: Gets or sets the vCenter Managed Object name for the host.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            overallMemoryUsageGB:
              type: integer
              description: Gets the used physical memory on the host in GB.
            memorySizeGB:
              type: integer
              description: Gets the total amount of physical memory on the host in GB.
            overallCpuUsageMHz:
              type: integer
              description: Gets the used CPU usage across all cores in MHz.
            cpuMhz:
              type: integer
              description: Gets the max CPU usage across all cores in MHz.
            datastoreIds:
              type: array
              items:
                type: string
              description: Gets the datastore ARM ids.
            networkIds:
              type: array
              items:
                type: string
              description: Gets the network ARM ids.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - location
    InventoryItemFlattened:
      type: object
      description: Flattened view of InventoryItem ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        inventoryType:
          type: string
          description: 'They inventory type. Required. Known values are: "ResourcePool", "VirtualMachine", "VirtualMachineTemplate", "VirtualNetwork", "Cluster", "Datastore", and "Host".'
        managedResourceId:
          type: string
          description: Gets or sets the tracked resource id corresponding to the inventory resource.
        moRefId:
          type: string
          description: Gets or sets the MoRef (Managed Object Reference) ID for the inventory item.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the inventory item.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedInventoryItemFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/InventoryItemFlattened"
        nextLink:
          type: string
    InventoryItem:
      type: object
      description: Defines the inventory item. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            inventoryType:
              type: string
              description: 'They inventory type. Required. Known values are: "ResourcePool", "VirtualMachine", "VirtualMachineTemplate", "VirtualNetwork", "Cluster", "Datastore", and "Host".'
            managedResourceId:
              type: string
              description: Gets or sets the tracked resource id corresponding to the inventory resource.
            moRefId:
              type: string
              description: Gets or sets the MoRef (Managed Object Reference) ID for the inventory item.
            moName:
              type: string
              description: Gets or sets the vCenter Managed Object name for the inventory item.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - properties
    OperationDisplay:
      type: object
      description: Properties of the operation.
      properties:
        provider:
          type: string
          description: Provider name.
        resource:
          type: string
          description: Resource name.
        operation:
          type: string
          description: Operation name.
        description:
          type: string
          description: Description of the operation.
    Operation:
      type: object
      description: Operation provided by provider.
      properties:
        name:
          type: string
          description: Name of the operation.
        isDataAction:
          type: boolean
          description: Indicates whether the operation is data action or not.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Properties of the operation.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    ResourcePoolFlattened:
      type: object
      description: Flattened view of ResourcePool ('properties' hoisted to the top level).
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        vCenterId:
          type: string
          description: Gets or sets the ARM Id of the vCenter resource in which this resource pool resides.
        moRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the resource pool.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource pool.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the resource pool.
        cpuSharesLevel:
          type: string
          description: Gets or sets CPUSharesLevel which specifies the CPU allocation level for this pool. This property is used in relative allocation between resource consumers.
        cpuReservationMHz:
          type: integer
          description: Gets or sets CPUReservationMHz which specifies the CPU size in MHz that is guaranteed to be available.
        cpuLimitMHz:
          type: integer
          description: Gets or sets CPULimitMHz which specifies a CPU usage limit in MHz. Utilization will not exceed this limit even if there are available resources.
        memSharesLevel:
          type: string
          description: Gets or sets CPUSharesLevel which specifies the memory allocation level for this pool. This property is used in relative allocation between resource consumers.
        memReservationMB:
          type: integer
          description: Gets or sets MemReservationMB which specifies the guaranteed available memory in megabytes.
        memLimitMB:
          type: integer
          description: Gets or sets MemLimitMB specifies a memory usage limit in megabytes. Utilization will not exceed the specified limit even if there are available resources.
        memOverallUsageGB:
          type: integer
          description: Gets the used physical memory on the pool in GB.
        memCapacityGB:
          type: integer
          description: Gets the total amount of physical memory on the pool in GB.
        cpuOverallUsageMHz:
          type: integer
          description: Gets the used CPU usage across all cores on the pool in MHz.
        cpuCapacityMHz:
          type: integer
          description: Gets the max CPU usage across all cores on the pool in MHz.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        datastoreIds:
          type: array
          items:
            type: string
          description: Gets the datastore ARM ids.
        networkIds:
          type: array
          items:
            type: string
          description: Gets the network ARM ids.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedResourcePoolFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourcePoolFlattened"
        nextLink:
          type: string
    ResourcePool:
      type: object
      description: Define the resourcePool. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            vCenterId:
              type: string
              description: Gets or sets the ARM Id of the vCenter resource in which this resource pool resides.
            moRefId:
              type: string
              description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the resource pool.
            inventoryItemId:
              type: string
              description: Gets or sets the inventory Item ID for the resource pool.
            moName:
              type: string
              description: Gets or sets the vCenter Managed Object name for the resource pool.
            cpuSharesLevel:
              type: string
              description: Gets or sets CPUSharesLevel which specifies the CPU allocation level for this pool. This property is used in relative allocation between resource consumers.
            cpuReservationMHz:
              type: integer
              description: Gets or sets CPUReservationMHz which specifies the CPU size in MHz that is guaranteed to be available.
            cpuLimitMHz:
              type: integer
              description: Gets or sets CPULimitMHz which specifies a CPU usage limit in MHz. Utilization will not exceed this limit even if there are available resources.
            memSharesLevel:
              type: string
              description: Gets or sets CPUSharesLevel which specifies the memory allocation level for this pool. This property is used in relative allocation between resource consumers.
            memReservationMB:
              type: integer
              description: Gets or sets MemReservationMB which specifies the guaranteed available memory in megabytes.
            memLimitMB:
              type: integer
              description: Gets or sets MemLimitMB specifies a memory usage limit in megabytes. Utilization will not exceed the specified limit even if there are available resources.
            memOverallUsageGB:
              type: integer
              description: Gets the used physical memory on the pool in GB.
            memCapacityGB:
              type: integer
              description: Gets the total amount of physical memory on the pool in GB.
            cpuOverallUsageMHz:
              type: integer
              description: Gets the used CPU usage across all cores on the pool in MHz.
            cpuCapacityMHz:
              type: integer
              description: Gets the max CPU usage across all cores on the pool in MHz.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            datastoreIds:
              type: array
              items:
                type: string
              description: Gets the datastore ARM ids.
            networkIds:
              type: array
              items:
                type: string
              description: Gets the network ARM ids.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - location
    VICredential:
      type: object
      description: Username / Password Credentials to connect to vcenter.
      properties:
        username:
          type: string
          description: Gets or sets username to connect with the vCenter.
        password:
          type: string
          description: Gets or sets the password to connect with the vCenter.
    VCenterFlattened:
      type: object
      description: Flattened view of VCenter ('properties' hoisted to the top level).
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        fqdn:
          type: string
          description: Gets or sets the FQDN/IPAddress of the vCenter. Required.
        port:
          type: integer
          description: Gets or sets the port of the vCenter.
        version:
          type: string
          description: Gets or sets the version of the vCenter.
        instanceUuid:
          type: string
          description: Gets or sets the instance UUID of the vCenter.
        connectionStatus:
          type: string
          description: Gets or sets the connection status to the vCenter.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        credentials:
          $ref: "#/components/schemas/VICredential"
          description: Username / Password Credentials to connect to vcenter.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVCenterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VCenterFlattened"
        nextLink:
          type: string
    VCenter:
      type: object
      description: Defines the vCenter. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            fqdn:
              type: string
              description: Gets or sets the FQDN/IPAddress of the vCenter. Required.
            port:
              type: integer
              description: Gets or sets the port of the vCenter.
            version:
              type: string
              description: Gets or sets the version of the vCenter.
            instanceUuid:
              type: string
              description: Gets or sets the instance UUID of the vCenter.
            connectionStatus:
              type: string
              description: Gets or sets the connection status to the vCenter.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            credentials:
              $ref: "#/components/schemas/VICredential"
              description: Username / Password Credentials to connect to vcenter.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - location
        - properties
    GuestCredential:
      type: object
      description: Username / Password Credentials to connect to guest.
      properties:
        username:
          type: string
          description: Gets or sets username to connect with the guest.
        password:
          type: string
          description: Gets or sets the password to connect with the guest.
    HttpProxyConfiguration:
      type: object
      description: HTTP Proxy configuration for the VM.
      properties:
        httpsProxy:
          type: string
          description: Gets or sets httpsProxy url.
    GuestAgentFlattened:
      type: object
      description: Flattened view of GuestAgent ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        credentials:
          $ref: "#/components/schemas/GuestCredential"
          description: Username / Password Credentials to provision guest agent.
        privateLinkScopeResourceId:
          type: string
          description: The resource id of the private link scope this machine is assigned to, if any.
        httpProxyConfig:
          $ref: "#/components/schemas/HttpProxyConfiguration"
          description: HTTP Proxy configuration for the VM.
        provisioningAction:
          type: string
          description: 'Gets or sets the guest agent provisioning action. Known values are: "install", "uninstall", and "repair".'
        status:
          type: string
          description: Gets or sets the guest agent status.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedGuestAgentFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/GuestAgentFlattened"
        nextLink:
          type: string
    GuestAgent:
      type: object
      description: Defines the GuestAgent. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            credentials:
              $ref: "#/components/schemas/GuestCredential"
              description: Username / Password Credentials to provision guest agent.
            privateLinkScopeResourceId:
              type: string
              description: The resource id of the private link scope this machine is assigned to, if any.
            httpProxyConfig:
              $ref: "#/components/schemas/HttpProxyConfiguration"
              description: HTTP Proxy configuration for the VM.
            provisioningAction:
              type: string
              description: 'Gets or sets the guest agent provisioning action. Known values are: "install", "uninstall", and "repair".'
            status:
              type: string
              description: Gets or sets the guest agent status.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PlacementProfile:
      type: object
      description: Specifies the compute and storage placement settings for the virtual machine.
      properties:
        resourcePoolId:
          type: string
          description: Gets or sets the ARM Id of the resourcePool resource on which this virtual machine will deploy.
        clusterId:
          type: string
          description: Gets or sets the ARM Id of the cluster resource on which this virtual machine will deploy.
        hostId:
          type: string
          description: Gets or sets the ARM Id of the host resource on which this virtual machine will deploy.
        datastoreId:
          type: string
          description: Gets or sets the ARM Id of the datastore resource on which the data for the virtual machine will be kept.
    OsProfileForVMInstance:
      type: object
      description: Specifies the operating system settings for the virtual machine. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        computerName:
          type: string
          description: Gets or sets computer name.
        adminUsername:
          type: string
          description: Gets or sets administrator username.
        adminPassword:
          type: string
          description: Sets administrator password.
        guestId:
          type: string
          description: Gets or sets the guestId.
        osType:
          type: string
          description: 'Gets or sets the type of the os. Known values are: "Windows", "Linux", and "Other".'
        osSku:
          type: string
          description: Gets or sets os sku.
        toolsRunningStatus:
          type: string
          description: Gets or sets the current running status of VMware Tools running in the guest operating system.
        toolsVersionStatus:
          type: string
          description: Gets or sets the current version status of VMware Tools installed in the guest operating system.
        toolsVersion:
          type: string
          description: Gets or sets the current version of VMware Tools.
    HardwareProfile:
      type: object
      description: Specifies the hardware settings for the virtual machine. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        memorySizeMB:
          type: integer
          description: Gets or sets memory size in MBs for the vm.
        numCPUs:
          type: integer
          description: Gets or sets the number of vCPUs for the vm.
        numCoresPerSocket:
          type: integer
          description: Gets or sets the number of cores per socket for the vm. Defaults to 1 if unspecified.
        cpuHotAddEnabled:
          type: boolean
          description: Gets or sets a value indicating whether virtual processors can be added while this virtual machine is running.
        cpuHotRemoveEnabled:
          type: boolean
          description: Gets or sets a value indicating whether virtual processors can be removed while this virtual machine is running.
        memoryHotAddEnabled:
          type: boolean
          description: Gets or sets a value indicating whether memory can be added while this virtual machine is running.
    NetworkProfile:
      type: object
      description: Specifies the network interfaces of the virtual machine.
      properties:
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/NetworkInterface"
          description: Gets or sets the list of network interfaces associated with the virtual machine.
    NetworkInterface:
      type: object
      description: Network Interface model. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Gets or sets the name of the network interface.
        label:
          type: string
          description: Gets or sets the label of the virtual network in vCenter that the nic is connected to.
        ipAddresses:
          type: array
          items:
            type: string
          description: Gets or sets the nic ip addresses.
        macAddress:
          type: string
          description: Gets or sets the NIC MAC address.
        networkId:
          type: string
          description: Gets or sets the ARM Id of the network resource to connect the virtual machine.
        nicType:
          type: string
          description: 'NIC type. Known values are: "vmxnet3", "vmxnet2", "vmxnet", "e1000", "e1000e", and "pcnet32".'
        powerOnBoot:
          type: string
          description: 'Gets or sets the power on boot. Known values are: "enabled" and "disabled".'
        networkMoRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID of the virtual network that the nic is connected to.
        networkMoName:
          type: string
          description: Gets or sets the name of the virtual network in vCenter that the nic is connected to.
        deviceKey:
          type: integer
          description: Gets or sets the device key value.
        ipSettings:
          $ref: "#/components/schemas/NicIPSettings"
          description: Gets or sets the ipsettings.
    NicIPSettings:
      type: object
      description: Defines the network interface ip settings. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        allocationMethod:
          type: string
          description: 'Gets or sets the nic allocation method. Known values are: "unset", "dynamic", "static", "linklayer", "random", and "other".'
        dnsServers:
          type: array
          items:
            type: string
          description: Gets or sets the dns servers.
        gateway:
          type: array
          items:
            type: string
          description: Gets or sets the gateway.
        ipAddress:
          type: string
          description: Gets or sets the ip address for the nic.
        subnetMask:
          type: string
          description: Gets or sets the mask.
        primaryWinsServer:
          type: string
          description: Gets or sets the primary server.
        secondaryWinsServer:
          type: string
          description: Gets or sets the secondary server.
        ipAddressInfo:
          type: array
          items:
            $ref: "#/components/schemas/NicIPAddressSettings"
          description: Gets or sets the IP address information being reported for this NIC. This contains the same IPv4 information above plus IPV6 information.
    NicIPAddressSettings:
      type: object
      description: IP address information for a virtual network adapter reported by the fabric. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        allocationMethod:
          type: string
          description: Gets the ip address allocation method.
        ipAddress:
          type: string
          description: Gets the ip address for the nic.
        subnetMask:
          type: string
          description: Gets the mask.
    StorageProfile:
      type: object
      description: Specifies the storage settings for the virtual machine disks. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDisk"
          description: Gets or sets the list of virtual disks associated with the virtual machine.
        scsiControllers:
          type: array
          items:
            $ref: "#/components/schemas/VirtualSCSIController"
          description: Gets or sets the list of virtual SCSI controllers associated with the virtual machine.
    VirtualDisk:
      type: object
      description: Virtual disk model. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Gets or sets the name of the virtual disk.
        label:
          type: string
          description: Gets or sets the label of the virtual disk in vCenter.
        diskObjectId:
          type: string
          description: Gets or sets the disk object id.
        diskSizeGB:
          type: integer
          description: Gets or sets the disk total size.
        deviceKey:
          type: integer
          description: Gets or sets the device key value.
        diskMode:
          type: string
          description: 'Gets or sets the disk mode. Known values are: "persistent", "independent_persistent", and "independent_nonpersistent".'
        controllerKey:
          type: integer
          description: Gets or sets the controller id.
        unitNumber:
          type: integer
          description: Gets or sets the unit number of the disk on the controller.
        deviceName:
          type: string
          description: Gets or sets the device name.
        diskType:
          type: string
          description: 'Gets or sets the disk backing type. Known values are: "flat", "pmem", "rawphysical", "rawvirtual", "sparse", "sesparse", and "unknown".'
    VirtualSCSIController:
      type: object
      description: This data object type contains the properties of a SCSI controller device attached to a virtual machine that is reported by the controller.
      properties:
        type:
          type: string
          description: 'Gets or sets the controller type. Known values are: "lsilogic", "buslogic", "pvscsi", and "lsilogicsas".'
        controllerKey:
          type: integer
          description: Gets or sets the key of the controller.
        busNumber:
          type: integer
          description: Gets or sets the bus number of the controller.
        scsiCtlrUnitNumber:
          type: integer
          description: Gets or sets the SCSI controller unit number.
        sharing:
          type: string
          description: 'Gets or sets the sharing mode. Known values are: "noSharing", "physicalSharing", and "virtualSharing".'
    SecurityProfile:
      type: object
      description: Specifies the Security profile settings for the virtual machine.
      properties:
        uefiSettings:
          $ref: "#/components/schemas/UefiSettings"
          description: Specifies the security settings like secure boot used while creating the virtual machine.
    UefiSettings:
      type: object
      description: Specifies the security settings like secure boot used while creating the virtual machine.
      properties:
        secureBootEnabled:
          type: boolean
          description: Specifies whether secure boot should be enabled on the virtual machine.
    InfrastructureProfile:
      type: object
      description: Specifies the vCenter infrastructure specific settings for the virtual machine. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        templateId:
          type: string
          description: Gets or sets the ARM Id of the template resource to deploy the virtual machine.
        vCenterId:
          type: string
          description: Gets or sets the ARM Id of the vCenter resource in which this resource pool resides.
        moRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the virtual machine.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the virtual machine.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the virtual machine.
        folderPath:
          type: string
          description: Gets or sets the folder path of the vm.
        instanceUuid:
          type: string
          description: Gets or sets the instance uuid of the vm.
        smbiosUuid:
          type: string
          description: Gets or sets the SMBIOS UUID of the vm.
        firmwareType:
          type: string
          description: 'Firmware type. Known values are: "bios" and "efi".'
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
    VirtualMachineInstanceFlattened:
      type: object
      description: Flattened view of VirtualMachineInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        placementProfile:
          $ref: "#/components/schemas/PlacementProfile"
          description: Placement properties.
        osProfile:
          $ref: "#/components/schemas/OsProfileForVMInstance"
          description: OS properties.
        hardwareProfile:
          $ref: "#/components/schemas/HardwareProfile"
          description: Hardware properties.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Network properties.
        storageProfile:
          $ref: "#/components/schemas/StorageProfile"
          description: Storage properties.
        securityProfile:
          $ref: "#/components/schemas/SecurityProfile"
          description: Gets the security profile.
        infrastructureProfile:
          $ref: "#/components/schemas/InfrastructureProfile"
          description: Gets the infrastructure profile.
        powerState:
          type: string
          description: Gets the power state of the virtual machine.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
        resourceUid:
          type: string
          description: Gets or sets a unique identifier for the vm resource.
    PagedVirtualMachineInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineInstanceFlattened"
        nextLink:
          type: string
    VirtualMachineInstance:
      type: object
      description: Define the virtualMachineInstance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        properties:
          type: object
          properties:
            placementProfile:
              $ref: "#/components/schemas/PlacementProfile"
              description: Placement properties.
            osProfile:
              $ref: "#/components/schemas/OsProfileForVMInstance"
              description: OS properties.
            hardwareProfile:
              $ref: "#/components/schemas/HardwareProfile"
              description: Hardware properties.
            networkProfile:
              $ref: "#/components/schemas/NetworkProfile"
              description: Network properties.
            storageProfile:
              $ref: "#/components/schemas/StorageProfile"
              description: Storage properties.
            securityProfile:
              $ref: "#/components/schemas/SecurityProfile"
              description: Gets the security profile.
            infrastructureProfile:
              $ref: "#/components/schemas/InfrastructureProfile"
              description: Gets the infrastructure profile.
            powerState:
              type: string
              description: Gets the power state of the virtual machine.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
            resourceUid:
              type: string
              description: Gets or sets a unique identifier for the vm resource.
    StorageProfileUpdate:
      type: object
      description: Specifies the storage settings for the virtual machine disks.
      properties:
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDiskUpdate"
          description: Gets or sets the list of virtual disks associated with the virtual machine.
    VirtualDiskUpdate:
      type: object
      description: Defines the virtual disk update.
      properties:
        name:
          type: string
          description: Gets or sets the name of the virtual disk.
        diskSizeGB:
          type: integer
          description: Gets or sets the disk total size.
        deviceKey:
          type: integer
          description: Gets or sets the device key value.
        diskMode:
          type: string
          description: 'Gets or sets the disk mode. Known values are: "persistent", "independent_persistent", and "independent_nonpersistent".'
        controllerKey:
          type: integer
          description: Gets or sets the controller id.
        unitNumber:
          type: integer
          description: Gets or sets the unit number of the disk on the controller.
        deviceName:
          type: string
          description: Gets or sets the device name.
        diskType:
          type: string
          description: 'Gets or sets the disk backing type. Known values are: "flat", "pmem", "rawphysical", "rawvirtual", "sparse", "sesparse", and "unknown".'
    NetworkProfileUpdate:
      type: object
      description: Specifies the network interfaces of the virtual machine.
      properties:
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/NetworkInterfaceUpdate"
          description: Gets or sets the list of network interfaces associated with the virtual machine.
    NetworkInterfaceUpdate:
      type: object
      description: Defines the network interface update.
      properties:
        name:
          type: string
          description: Gets or sets the name of the network interface.
        networkId:
          type: string
          description: Gets or sets the ARM Id of the network resource to connect the virtual machine.
        nicType:
          type: string
          description: 'NIC type. Known values are: "vmxnet3", "vmxnet2", "vmxnet", "e1000", "e1000e", and "pcnet32".'
        powerOnBoot:
          type: string
          description: 'Gets or sets the power on boot. Known values are: "enabled" and "disabled".'
        deviceKey:
          type: integer
          description: Gets or sets the device key value.
    VirtualMachineTemplateFlattened:
      type: object
      description: Flattened view of VirtualMachineTemplate ('properties' hoisted to the top level).
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        vCenterId:
          type: string
          description: Gets or sets the ARM Id of the vCenter resource in which this template resides.
        moRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the virtual machine template.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the virtual machine template.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the virtual machine template.
        memorySizeMB:
          type: integer
          description: Gets or sets memory size in MBs for the template.
        numCPUs:
          type: integer
          description: Gets or sets the number of vCPUs for the template.
        numCoresPerSocket:
          type: integer
          description: Gets or sets the number of cores per socket for the template. Defaults to 1 if unspecified.
        osType:
          type: string
          description: 'Gets or sets the type of the os. Known values are: "Windows", "Linux", and "Other".'
        osName:
          type: string
          description: Gets or sets os name.
        folderPath:
          type: string
          description: Gets or sets the folder path of the template.
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/NetworkInterface"
          description: Gets or sets the network interfaces of the template.
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDisk"
          description: Gets or sets the disks the template.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        toolsVersionStatus:
          type: string
          description: Gets or sets the current version status of VMware Tools installed in the guest operating system.
        toolsVersion:
          type: string
          description: Gets or sets the current version of VMware Tools.
        firmwareType:
          type: string
          description: 'Firmware type. Known values are: "bios" and "efi".'
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVirtualMachineTemplateFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineTemplateFlattened"
        nextLink:
          type: string
    VirtualMachineTemplate:
      type: object
      description: Define the virtualMachineTemplate. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            vCenterId:
              type: string
              description: Gets or sets the ARM Id of the vCenter resource in which this template resides.
            moRefId:
              type: string
              description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the virtual machine template.
            inventoryItemId:
              type: string
              description: Gets or sets the inventory Item ID for the virtual machine template.
            moName:
              type: string
              description: Gets or sets the vCenter Managed Object name for the virtual machine template.
            memorySizeMB:
              type: integer
              description: Gets or sets memory size in MBs for the template.
            numCPUs:
              type: integer
              description: Gets or sets the number of vCPUs for the template.
            numCoresPerSocket:
              type: integer
              description: Gets or sets the number of cores per socket for the template. Defaults to 1 if unspecified.
            osType:
              type: string
              description: 'Gets or sets the type of the os. Known values are: "Windows", "Linux", and "Other".'
            osName:
              type: string
              description: Gets or sets os name.
            folderPath:
              type: string
              description: Gets or sets the folder path of the template.
            networkInterfaces:
              type: array
              items:
                $ref: "#/components/schemas/NetworkInterface"
              description: Gets or sets the network interfaces of the template.
            disks:
              type: array
              items:
                $ref: "#/components/schemas/VirtualDisk"
              description: Gets or sets the disks the template.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            toolsVersionStatus:
              type: string
              description: Gets or sets the current version status of VMware Tools installed in the guest operating system.
            toolsVersion:
              type: string
              description: Gets or sets the current version of VMware Tools.
            firmwareType:
              type: string
              description: 'Firmware type. Known values are: "bios" and "efi".'
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - location
    VirtualNetworkFlattened:
      type: object
      description: Flattened view of VirtualNetwork ('properties' hoisted to the top level).
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        uuid:
          type: string
          description: Gets or sets a unique identifier for this resource.
        vCenterId:
          type: string
          description: Gets or sets the ARM Id of the vCenter resource in which this template resides.
        moRefId:
          type: string
          description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the virtual network.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the virtual network.
        moName:
          type: string
          description: Gets or sets the vCenter Managed Object name for the virtual network.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/ResourceStatus"
          description: The resource status information.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVirtualNetworkFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNetworkFlattened"
        nextLink:
          type: string
    VirtualNetwork:
      type: object
      description: Define the virtualNetwork. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        location:
          type: string
          description: Gets or sets the location. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location.
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: The system data.
        tags:
          type: object
          properties: {}
          description: Gets or sets the Resource tags.
        name:
          type: string
          description: Gets or sets the name.
        id:
          type: string
          description: Gets or sets the Id.
        type:
          type: string
          description: Gets or sets the type of the resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        properties:
          type: object
          properties:
            uuid:
              type: string
              description: Gets or sets a unique identifier for this resource.
            vCenterId:
              type: string
              description: Gets or sets the ARM Id of the vCenter resource in which this template resides.
            moRefId:
              type: string
              description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the virtual network.
            inventoryItemId:
              type: string
              description: Gets or sets the inventory Item ID for the virtual network.
            moName:
              type: string
              description: Gets or sets the vCenter Managed Object name for the virtual network.
            customResourceName:
              type: string
              description: Gets the name of the corresponding resource in Kubernetes.
            statuses:
              type: array
              items:
                $ref: "#/components/schemas/ResourceStatus"
              description: The resource status information.
            provisioningState:
              type: string
              description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - location
    VmInstanceHybridIdentityMetadataFlattened:
      type: object
      description: Flattened view of VmInstanceHybridIdentityMetadata ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        resourceUid:
          type: string
          description: The unique identifier for the resource.
        publicKey:
          type: string
          description: Gets or sets the Public Key.
        provisioningState:
          type: string
          description: 'Gets the provisioning state. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVmInstanceHybridIdentityMetadataFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VmInstanceHybridIdentityMetadataFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    clusters:
      id: azure.connected_vmware.clusters
      name: clusters
      title: clusters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1clusters~1{cluster_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1clusters~1{cluster_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1clusters~1{cluster_name}?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1clusters~1{cluster_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConnectedVMwarevSphere~1clusters?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1clusters?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/get"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/delete"
    datastores:
      id: azure.connected_vmware.datastores
      name: datastores
      title: datastores
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1datastores~1{datastore_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1datastores~1{datastore_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1datastores~1{datastore_name}?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1datastores~1{datastore_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConnectedVMwarevSphere~1datastores?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1datastores?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/datastores/methods/get"
          - $ref: "#/components/x-stackQL-resources/datastores/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/datastores/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/datastores/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/datastores/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/datastores/methods/delete"
    hosts:
      id: azure.connected_vmware.hosts
      name: hosts
      title: hosts
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1hosts~1{host_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1hosts~1{host_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1hosts~1{host_name}?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1hosts~1{host_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConnectedVMwarevSphere~1hosts?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1hosts?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/hosts/methods/get"
          - $ref: "#/components/x-stackQL-resources/hosts/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/hosts/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/hosts/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/hosts/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/hosts/methods/delete"
    inventory_items:
      id: azure.connected_vmware.inventory_items
      name: inventory_items
      title: inventory_items
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}~1inventoryItems~1{inventory_item_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}~1inventoryItems~1{inventory_item_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}~1inventoryItems~1{inventory_item_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_vcenter:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}~1inventoryItems?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/get"
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/list_by_vcenter"
        insert:
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/delete"
    operations:
      id: azure.connected_vmware.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ConnectedVMwarevSphere~1operations?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    resource_pools:
      id: azure.connected_vmware.resource_pools
      name: resource_pools
      title: resource_pools
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1resourcePools~1{resource_pool_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1resourcePools~1{resource_pool_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1resourcePools~1{resource_pool_name}?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1resourcePools~1{resource_pool_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConnectedVMwarevSphere~1resourcePools?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1resourcePools?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/get"
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/resource_pools/methods/delete"
    vcenters:
      id: azure.connected_vmware.vcenters
      name: vcenters
      title: vcenters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters~1{vcenter_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1vcenters?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/vcenters/methods/get"
          - $ref: "#/components/x-stackQL-resources/vcenters/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/vcenters/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/vcenters/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/vcenters/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/vcenters/methods/delete"
    vm_instance_guest_agents:
      id: azure.connected_vmware.vm_instance_guest_agents
      name: vm_instance_guest_agents
      title: vm_instance_guest_agents
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1guestAgents~1default?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1guestAgents~1default?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1guestAgents~1default?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
        list_raw:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1guestAgents?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/vm_instance_guest_agents/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/vm_instance_guest_agents/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/vm_instance_guest_agents/methods/delete"
    virtual_machine_instances:
      id: azure.connected_vmware.virtual_machine_instances
      name: virtual_machine_instances
      title: virtual_machine_instances
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        stop:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1stop?api-version=2023-10-01/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        start:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1start?api-version=2023-10-01/post"
          response:
            openAPIDocKey: "204"
        restart:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1restart?api-version=2023-10-01/post"
          response:
            openAPIDocKey: "204"
        list_raw:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/delete"
    virtual_machine_templates:
      id: azure.connected_vmware.virtual_machine_templates
      name: virtual_machine_templates
      title: virtual_machine_templates
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineTemplates?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineTemplates?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/delete"
    virtual_networks:
      id: azure.connected_vmware.virtual_networks
      name: virtual_networks
      title: virtual_networks
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-01/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualNetworks?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualNetworks?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/delete"
    vm_instance_hybrid_identity_metadata:
      id: azure.connected_vmware.vm_instance_hybrid_identity_metadata
      name: vm_instance_hybrid_identity_metadata
      title: vm_instance_hybrid_identity_metadata
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1hybridIdentityMetadata~1default?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_raw:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ConnectedVMwarevSphere~1virtualMachineInstances~1default~1hybridIdentityMetadata?api-version=2023-10-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/vm_instance_hybrid_identity_metadata/methods/get"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/clusters/{cluster_name}?api-version=2023-10-01:
    get:
      operationId: Clusters_get
      description: Gets a cluster. Implements cluster GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClusterFlattened"
    put:
      operationId: Clusters_begin_create
      description: Implements cluster PUT method. Create Or Update cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Gets or sets the location. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    vCenterId:
                      type: string
                      description: Gets or sets the ARM Id of the vCenter resource in which this cluster resides.
                    moRefId:
                      type: string
                      description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the cluster.
                    inventoryItemId:
                      type: string
                      description: Gets or sets the inventory Item ID for the cluster.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cluster"
    patch:
      operationId: Clusters_update
      description: Updates a cluster. API to update certain properties of the cluster resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cluster"
    delete:
      operationId: Clusters_begin_delete
      description: Deletes an cluster. Implements cluster DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ConnectedVMwarevSphere/clusters?api-version=2023-10-01:
    get:
      operationId: Clusters_list
      description: Implements GET clusters in a subscription. List of clusters in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/clusters?api-version=2023-10-01:
    get:
      operationId: Clusters_list_by_resource_group
      description: Implements GET clusters in a resource group. List of clusters in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/datastores/{datastore_name}?api-version=2023-10-01:
    get:
      operationId: Datastores_get
      description: Gets a datastore. Implements datastore GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: datastore_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the datastore. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatastoreFlattened"
    put:
      operationId: Datastores_begin_create
      description: Implements datastore PUT method. Create Or Update datastore.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: datastore_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the datastore. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Gets or sets the location. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    vCenterId:
                      type: string
                      description: Gets or sets the ARM Id of the vCenter resource in which this datastore resides.
                    moRefId:
                      type: string
                      description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the datastore.
                    inventoryItemId:
                      type: string
                      description: Gets or sets the inventory Item ID for the datastore.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Datastore"
    patch:
      operationId: Datastores_update
      description: Updates a datastore. API to update certain properties of the datastore resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: datastore_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the datastore. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Datastore"
    delete:
      operationId: Datastores_begin_delete
      description: Deletes an datastore. Implements datastore DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: datastore_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the datastore. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ConnectedVMwarevSphere/datastores?api-version=2023-10-01:
    get:
      operationId: Datastores_list
      description: Implements GET datastores in a subscription. List of datastores in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatastoreFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/datastores?api-version=2023-10-01:
    get:
      operationId: Datastores_list_by_resource_group
      description: Implements GET datastores in a resource group. List of datastores in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDatastoreFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/hosts/{host_name}?api-version=2023-10-01:
    get:
      operationId: Hosts_get
      description: Gets a host. Implements host GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: host_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the host. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HostFlattened"
    put:
      operationId: Hosts_begin_create
      description: Implements host PUT method. Create Or Update host.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: host_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the host. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Gets or sets the location. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    vCenterId:
                      type: string
                      description: Gets or sets the ARM Id of the vCenter resource in which this host resides.
                    moRefId:
                      type: string
                      description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the host.
                    inventoryItemId:
                      type: string
                      description: Gets or sets the inventory Item ID for the host.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Host"
    patch:
      operationId: Hosts_update
      description: Updates a host. API to update certain properties of the host resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: host_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the host. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Host"
    delete:
      operationId: Hosts_begin_delete
      description: Deletes an host. Implements host DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: host_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the host. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ConnectedVMwarevSphere/hosts?api-version=2023-10-01:
    get:
      operationId: Hosts_list
      description: Implements GET hosts in a subscription. List of hosts in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedHostFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/hosts?api-version=2023-10-01:
    get:
      operationId: Hosts_list_by_resource_group
      description: Implements GET hosts in a resource group. List of hosts in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedHostFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/vcenters/{vcenter_name}/inventoryItems/{inventory_item_name}?api-version=2023-10-01:
    get:
      operationId: InventoryItems_get
      description: Gets InventoryItem. Implements InventoryItem GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: inventory_item_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the inventoryItem. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryItemFlattened"
    put:
      operationId: InventoryItems_create
      description: Implements InventoryItem PUT method. Create Or Update InventoryItem.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: inventory_item_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the inventoryItem. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    inventoryType:
                      type: string
                      description: 'They inventory type. Required. Known values are: "ResourcePool", "VirtualMachine", "VirtualMachineTemplate", "VirtualNetwork", "Cluster", "Datastore", and "Host".'
                    managedResourceId:
                      type: string
                      description: Gets or sets the tracked resource id corresponding to the inventory resource.
                    moRefId:
                      type: string
                      description: Gets or sets the MoRef (Managed Object Reference) ID for the inventory item.
                    moName:
                      type: string
                      description: Gets or sets the vCenter Managed Object name for the inventory item.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryItem"
    delete:
      operationId: InventoryItems_delete
      description: Deletes an inventoryItem. Implements inventoryItem DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: inventory_item_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the inventoryItem. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/vcenters/{vcenter_name}/inventoryItems?api-version=2023-10-01:
    get:
      operationId: InventoryItems_list_by_vcenter
      description: Implements GET inventoryItems in a vCenter. Returns the list of inventoryItems of the given vCenter.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedInventoryItemFlattened"
  /providers/Microsoft.ConnectedVMwarevSphere/operations?api-version=2023-10-01:
    get:
      operationId: connectedvmware_list
      description: Returns list of all operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/resourcePools/{resource_pool_name}?api-version=2023-10-01:
    get:
      operationId: ResourcePools_get
      description: Gets a resourcePool. Implements resourcePool GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: resource_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resourcePool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourcePoolFlattened"
    put:
      operationId: ResourcePools_begin_create
      description: Implements resourcePool PUT method. Create Or Update resourcePool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: resource_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resourcePool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Gets or sets the location. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    vCenterId:
                      type: string
                      description: Gets or sets the ARM Id of the vCenter resource in which this resource pool resides.
                    moRefId:
                      type: string
                      description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the resource pool.
                    inventoryItemId:
                      type: string
                      description: Gets or sets the inventory Item ID for the resource pool.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourcePool"
    patch:
      operationId: ResourcePools_update
      description: Updates a resourcePool. API to update certain properties of the resourcePool resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: resource_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resourcePool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourcePool"
    delete:
      operationId: ResourcePools_begin_delete
      description: Deletes an resourcePool. Implements resourcePool DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: resource_pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the resourcePool. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ConnectedVMwarevSphere/resourcePools?api-version=2023-10-01:
    get:
      operationId: ResourcePools_list
      description: Implements GET resourcePools in a subscription. List of resourcePools in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourcePoolFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/resourcePools?api-version=2023-10-01:
    get:
      operationId: ResourcePools_list_by_resource_group
      description: Implements GET resourcePools in a resource group. List of resourcePools in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourcePoolFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/vcenters/{vcenter_name}?api-version=2023-10-01:
    get:
      operationId: VCenters_get
      description: Gets a vCenter. Implements vCenter GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VCenterFlattened"
    put:
      operationId: VCenters_begin_create
      description: Implements vCenter PUT method. Create Or Update vCenter.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Gets or sets the location. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    fqdn:
                      type: string
                      description: Gets or sets the FQDN/IPAddress of the vCenter. Required.
                    port:
                      type: integer
                      description: Gets or sets the port of the vCenter.
                    credentials:
                      $ref: "#/components/schemas/VICredential"
                      description: Username / Password Credentials to connect to vcenter.
              required:
                - location
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VCenter"
    patch:
      operationId: VCenters_update
      description: Updates a vCenter. API to update certain properties of the vCenter resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VCenter"
    delete:
      operationId: VCenters_begin_delete
      description: Deletes an vCenter. Implements vCenter DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: vcenter_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the vCenter. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ConnectedVMwarevSphere/vcenters?api-version=2023-10-01:
    get:
      operationId: VCenters_list
      description: Implements GET vCenters in a subscription. List of vCenters in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVCenterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/vcenters?api-version=2023-10-01:
    get:
      operationId: VCenters_list_by_resource_group
      description: Implements GET vCenters in a resource group. List of vCenters in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVCenterFlattened"
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/guestAgents/default?api-version=2023-10-01:
    get:
      operationId: VMInstanceGuestAgents_get
      description: Gets GuestAgent. Implements GuestAgent GET method.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GuestAgentFlattened"
    put:
      operationId: VMInstanceGuestAgents_begin_create
      description: Implements GuestAgent PUT method. Create Or Update GuestAgent.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    credentials:
                      $ref: "#/components/schemas/GuestCredential"
                      description: Username / Password Credentials to provision guest agent.
                    privateLinkScopeResourceId:
                      type: string
                      description: The resource id of the private link scope this machine is assigned to, if any.
                    httpProxyConfig:
                      $ref: "#/components/schemas/HttpProxyConfiguration"
                      description: HTTP Proxy configuration for the VM.
                    provisioningAction:
                      type: string
                      description: 'Gets or sets the guest agent provisioning action. Known values are: "install", "uninstall", and "repair".'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GuestAgent"
    delete:
      operationId: VMInstanceGuestAgents_begin_delete
      description: Deletes an GuestAgent. Implements GuestAgent DELETE method.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/guestAgents?api-version=2023-10-01:
    get:
      operationId: VMInstanceGuestAgents_list
      description: Implements GET GuestAgent in a vm. Returns the list of GuestAgent of the given vm.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGuestAgentFlattened"
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default?api-version=2023-10-01:
    get:
      operationId: VirtualMachineInstances_get
      description: Gets a virtual machine. Retrieves information about a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineInstanceFlattened"
    put:
      operationId: VirtualMachineInstances_begin_create_or_update
      description: Implements virtual machine PUT method. The operation to create or update a virtual machine instance. Please note some properties can be set only during virtual machine instance creation.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                properties:
                  type: object
                  properties:
                    placementProfile:
                      $ref: "#/components/schemas/PlacementProfile"
                      description: Placement properties.
                    osProfile:
                      $ref: "#/components/schemas/OsProfileForVMInstance"
                      description: OS properties.
                    hardwareProfile:
                      $ref: "#/components/schemas/HardwareProfile"
                      description: Hardware properties.
                    networkProfile:
                      $ref: "#/components/schemas/NetworkProfile"
                      description: Network properties.
                    storageProfile:
                      $ref: "#/components/schemas/StorageProfile"
                      description: Storage properties.
                    securityProfile:
                      $ref: "#/components/schemas/SecurityProfile"
                      description: Gets the security profile.
                    infrastructureProfile:
                      $ref: "#/components/schemas/InfrastructureProfile"
                      description: Gets the infrastructure profile.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineInstance"
    patch:
      operationId: VirtualMachineInstances_begin_update
      description: Updates a virtual machine. The operation to update a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    hardwareProfile:
                      $ref: "#/components/schemas/HardwareProfile"
                      description: Specifies the hardware settings for the virtual machine.
                    storageProfile:
                      $ref: "#/components/schemas/StorageProfileUpdate"
                      description: Specifies the storage settings for the virtual machine disks.
                    networkProfile:
                      $ref: "#/components/schemas/NetworkProfileUpdate"
                      description: Specifies the network interfaces of the virtual machine.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineInstance"
    delete:
      operationId: VirtualMachineInstances_begin_delete
      description: Deletes an virtual machine. The operation to delete a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
        - name: deleteFromHost
          in: query
          required: false
          schema:
            type: boolean
          description: Whether to delete the VM from the vCenter. Default value is None.
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances?api-version=2023-10-01:
    get:
      operationId: VirtualMachineInstances_list
      description: Implements List virtual machine instances. Lists all of the virtual machine instances within the specified parent resource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineInstanceFlattened"
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/stop?api-version=2023-10-01:
    post:
      operationId: VirtualMachineInstances_begin_stop
      description: Implements the operation to stop a virtual machine. The operation to power off (stop) a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                skipShutdown:
                  type: boolean
                  description: Gets or sets a value indicating whether to request non-graceful VM shutdown. True value for this flag indicates non-graceful shutdown whereas false indicates otherwise. Defaults to false.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/start?api-version=2023-10-01:
    post:
      operationId: VirtualMachineInstances_begin_start
      description: Implements the operation to start a virtual machine. The operation to start a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/restart?api-version=2023-10-01:
    post:
      operationId: VirtualMachineInstances_begin_restart
      description: Implements the operation to restart a virtual machine. The operation to restart a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineTemplates/{virtual_machine_template_name}?api-version=2023-10-01:
    get:
      operationId: VirtualMachineTemplates_get
      description: Gets a virtual machine template. Implements virtual machine template GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual machine template resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineTemplateFlattened"
    put:
      operationId: VirtualMachineTemplates_begin_create
      description: Implements virtual machine template PUT method. Create Or Update virtual machine template.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual machine template resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Gets or sets the location. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    vCenterId:
                      type: string
                      description: Gets or sets the ARM Id of the vCenter resource in which this template resides.
                    moRefId:
                      type: string
                      description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the virtual machine template.
                    inventoryItemId:
                      type: string
                      description: Gets or sets the inventory Item ID for the virtual machine template.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineTemplate"
    patch:
      operationId: VirtualMachineTemplates_update
      description: Updates a virtual machine template. API to update certain properties of the virtual machine template resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual machine template resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineTemplate"
    delete:
      operationId: VirtualMachineTemplates_begin_delete
      description: Deletes an virtual machine template. Implements virtual machine template DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual machine template resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineTemplates?api-version=2023-10-01:
    get:
      operationId: VirtualMachineTemplates_list
      description: Implements GET virtualMachineTemplates in a subscription. List of virtualMachineTemplates in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineTemplateFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineTemplates?api-version=2023-10-01:
    get:
      operationId: VirtualMachineTemplates_list_by_resource_group
      description: Implements GET virtualMachineTemplates in a resource group. List of virtualMachineTemplates in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineTemplateFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/virtualNetworks/{virtual_network_name}?api-version=2023-10-01:
    get:
      operationId: VirtualNetworks_get
      description: Gets a virtual network. Implements virtual network GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual network resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetworkFlattened"
    put:
      operationId: VirtualNetworks_begin_create
      description: Implements virtual network PUT method. Create Or Update virtual network.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual network resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Gets or sets the location. Required.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets the Resource tags.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
                properties:
                  type: object
                  properties:
                    vCenterId:
                      type: string
                      description: Gets or sets the ARM Id of the vCenter resource in which this template resides.
                    moRefId:
                      type: string
                      description: Gets or sets the vCenter MoRef (Managed Object Reference) ID for the virtual network.
                    inventoryItemId:
                      type: string
                      description: Gets or sets the inventory Item ID for the virtual network.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetwork"
    patch:
      operationId: VirtualNetworks_update
      description: Updates a virtual network. API to update certain properties of the virtual network resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual network resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetwork"
    delete:
      operationId: VirtualNetworks_begin_delete
      description: Deletes an virtual network. Implements virtual network DELETE method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the virtual network resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Whether force delete was specified. Default value is None.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ConnectedVMwarevSphere/virtualNetworks?api-version=2023-10-01:
    get:
      operationId: VirtualNetworks_list
      description: Implements GET virtualNetworks in a subscription. List of virtualNetworks in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualNetworkFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ConnectedVMwarevSphere/virtualNetworks?api-version=2023-10-01:
    get:
      operationId: VirtualNetworks_list_by_resource_group
      description: Implements GET virtualNetworks in a resource group. List of virtualNetworks in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The Resource Group Name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualNetworkFlattened"
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/hybridIdentityMetadata/default?api-version=2023-10-01:
    get:
      operationId: VmInstanceHybridIdentityMetadata_get
      description: Gets HybridIdentityMetadata. Implements HybridIdentityMetadata GET method.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VmInstanceHybridIdentityMetadataFlattened"
  /{resource_uri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/hybridIdentityMetadata?api-version=2023-10-01:
    get:
      operationId: VmInstanceHybridIdentityMetadata_list
      description: Implements GET HybridIdentityMetadata in a vm. Returns the list of HybridIdentityMetadata of the given vm.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the Hybrid Compute machine resource to be extended. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVmInstanceHybridIdentityMetadataFlattened"
