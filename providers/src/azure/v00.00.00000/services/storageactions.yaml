openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Storage Actions Mgmt Client
  description: The Azure Storage Actions Management API.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-01-01-stackql-generated
  x-serviceAlias: storageactions
  x-sdkPackage: azure-mgmt-storageactions
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    OperationDisplay:
      type: object
      description: Localized display information for and operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: REST API Operation.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    StorageTaskAssignment:
      type: object
      description: Storage Task Assignment associated with this Storage Task.
      properties:
        id:
          type: string
          description: Resource ID of the Storage Task Assignment.
    PagedStorageTaskAssignment:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskAssignment"
        nextLink:
          type: string
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities).
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    StorageTaskProperties:
      type: object
      description: Properties of the storage task.
      properties:
        taskVersion:
          type: integer
          description: Storage task version.
        enabled:
          type: boolean
          description: Storage Task is enabled when set to true and disabled when set to false. Required.
        description:
          type: string
          description: Text that describes the purpose of the storage task. Required.
        action:
          $ref: "#/components/schemas/StorageTaskAction"
          description: The storage task action that is executed. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Represents the provisioning state of the storage task. Known values are: "ValidateSubscriptionQuotaBegin", "ValidateSubscriptionQuotaEnd", "Accepted", "Creating", "Succeeded", "Deleting", "Canceled", and "Failed".'
        creationTimeInUtc:
          type: string
          format: date-time
          description: The creation date and time of the storage task in UTC.
      required:
        - enabled
        - description
        - action
    StorageTaskAction:
      type: object
      description: The storage task action represents conditional statements and operations to be performed on target objects.
      properties:
        if:
          $ref: "#/components/schemas/IfCondition"
          description: The if block of storage task operation. Required.
        else:
          $ref: "#/components/schemas/ElseCondition"
          description: The else block of storage task operation.
      required:
        - if
    IfCondition:
      type: object
      description: The if block of storage task operation.
      properties:
        condition:
          type: string
          description: Condition predicate to evaluate each object. See `https://aka.ms/storagetaskconditions `_ for valid properties and operators. Required.
        operations:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskOperation"
          description: List of operations to execute when the condition predicate satisfies. Required.
      required:
        - condition
        - operations
    StorageTaskOperation:
      type: object
      description: Represents an operation to be performed on the object.
      properties:
        name:
          $ref: "#/components/schemas/StorageTaskOperationName"
          description: 'The operation to be performed on the object. Required. Known values are: "SetBlobTier", "SetBlobTags", "SetBlobImmutabilityPolicy", "SetBlobLegalHold", "SetBlobExpiry", "DeleteBlob", and "UndeleteBlob".'
        parameters:
          type: object
          properties: {}
          description: Key-value parameters for the operation.
        onSuccess:
          $ref: "#/components/schemas/OnSuccess"
          description: Action to be taken when the operation is successful for a object. "continue"
        onFailure:
          $ref: "#/components/schemas/OnFailure"
          description: Action to be taken when the operation fails for a object. "break"
      required:
        - name
    StorageTaskOperationName:
      type: string
      enum:
        - SetBlobTier
        - SetBlobTags
        - SetBlobImmutabilityPolicy
        - SetBlobLegalHold
        - SetBlobExpiry
        - DeleteBlob
        - UndeleteBlob
    OnSuccess:
      type: string
      enum:
        - continue
    OnFailure:
      type: string
      enum:
        - break
    ElseCondition:
      type: object
      description: The else block of storage task operation.
      properties:
        operations:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskOperation"
          description: List of operations to execute in the else block. Required.
      required:
        - operations
    ProvisioningState:
      type: string
      enum:
        - ValidateSubscriptionQuotaBegin
        - ValidateSubscriptionQuotaEnd
        - Accepted
        - Creating
        - Succeeded
        - Deleting
        - Canceled
        - Failed
    StorageTaskFlattened:
      type: object
      description: Flattened view of StorageTask ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identity of the resource. Required.
        taskVersion:
          type: integer
          description: Storage task version.
        enabled:
          type: boolean
          description: Storage Task is enabled when set to true and disabled when set to false. Required.
        description:
          type: string
          description: Text that describes the purpose of the storage task. Required.
        action:
          $ref: "#/components/schemas/StorageTaskAction"
          description: The storage task action that is executed. Required.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Represents the provisioning state of the storage task. Known values are: "ValidateSubscriptionQuotaBegin", "ValidateSubscriptionQuotaEnd", "Accepted", "Creating", "Succeeded", "Deleting", "Canceled", and "Failed".'
        creationTimeInUtc:
          type: string
          format: date-time
          description: The creation date and time of the storage task in UTC.
    PagedStorageTaskFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskFlattened"
        nextLink:
          type: string
    StorageTask:
      type: object
      description: Represents Storage Task.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identity of the resource. Required.
        properties:
          $ref: "#/components/schemas/StorageTaskProperties"
          description: Properties of the storage task. Required.
      required:
        - location
        - identity
        - properties
    StorageTaskUpdateProperties:
      type: object
      description: Properties of the storage task.
      properties:
        taskVersion:
          type: integer
          description: Storage task version.
        enabled:
          type: boolean
          description: Storage Task is enabled when set to true and disabled when set to false.
        description:
          type: string
          description: Text that describes the purpose of the storage task.
        action:
          $ref: "#/components/schemas/StorageTaskAction"
          description: The storage task action that is executed.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Represents the provisioning state of the storage task. Known values are: "ValidateSubscriptionQuotaBegin", "ValidateSubscriptionQuotaEnd", "Accepted", "Creating", "Succeeded", "Deleting", "Canceled", and "Failed".'
        creationTimeInUtc:
          type: string
          format: date-time
          description: The creation date and time of the storage task in UTC.
    StorageTaskPreviewActionProperties:
      type: object
      description: Storage task preview action properties.
      properties:
        container:
          $ref: "#/components/schemas/StorageTaskPreviewContainerProperties"
          description: Properties of a sample container to test for a match with the preview action. Required.
        blobs:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskPreviewBlobProperties"
          description: Properties of some sample blobs in the container to test for matches with the preview action. Required.
        action:
          $ref: "#/components/schemas/StorageTaskPreviewActionCondition"
          description: Preview action to test. Required.
      required:
        - container
        - blobs
        - action
    StorageTaskPreviewContainerProperties:
      type: object
      description: Storage task preview container properties.
      properties:
        name:
          type: string
          description: Name of test container.
        metadata:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskPreviewKeyValueProperties"
          description: metadata key value pairs to be tested for a match against the provided condition.
    StorageTaskPreviewKeyValueProperties:
      type: object
      description: Storage task preview object key value pair properties.
      properties:
        key:
          type: string
          description: Represents the key property of the pair.
        value:
          type: string
          description: Represents the value property of the pair.
    StorageTaskPreviewBlobProperties:
      type: object
      description: Storage task preview container properties.
      properties:
        name:
          type: string
          description: Name of test blob.
        properties:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskPreviewKeyValueProperties"
          description: properties key value pairs to be tested for a match against the provided condition.
        metadata:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskPreviewKeyValueProperties"
          description: metadata key value pairs to be tested for a match against the provided condition.
        tags:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskPreviewKeyValueProperties"
          description: tags key value pairs to be tested for a match against the provided condition.
        matchedBlock:
          $ref: "#/components/schemas/MatchedBlockName"
          description: 'Represents the condition block name that matched blob properties. Known values are: "If", "Else", and "None".'
    MatchedBlockName:
      type: string
      enum:
        - If
        - Else
        - None
    StorageTaskPreviewActionCondition:
      type: object
      description: Represents the storage task conditions to be tested for a match with container and blob properties.
      properties:
        if:
          $ref: "#/components/schemas/StorageTaskPreviewActionIfCondition"
          description: The condition to be tested for a match with container and blob properties. Required.
        elseBlockExists:
          type: boolean
          description: Specify whether the else block is present in the condition. Required.
      required:
        - if
        - elseBlockExists
    StorageTaskPreviewActionIfCondition:
      type: object
      description: Represents storage task preview action condition.
      properties:
        condition:
          type: string
          description: Storage task condition to bes tested for a match.
    StorageTaskPreviewAction:
      type: object
      description: Storage Task Preview Action.
      properties:
        properties:
          $ref: "#/components/schemas/StorageTaskPreviewActionProperties"
          description: Properties of the storage task preview. Required.
      required:
        - properties
    StorageTaskReportProperties:
      type: object
      description: Storage task execution report for a run instance.
      properties:
        taskAssignmentId:
          type: string
          description: Resource ID of the Storage Task Assignment associated with this reported run.
        storageAccountId:
          type: string
          description: Resource ID of the Storage Account where this reported run executed.
        startTime:
          type: string
          description: Start time of the run instance. Filter options such as startTime gt '2023-06-26T20:51:24.4494016Z' and other comparison operators can be used as described for DateTime properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        finishTime:
          type: string
          description: End time of the run instance. Filter options such as startTime gt '2023-06-26T20:51:24.4494016Z' and other comparison operators can be used as described for DateTime properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectsTargetedCount:
          type: string
          description: Total number of objects that meet the condition as defined in the storage task assignment execution context. Filter options such as objectsTargetedCount gt 50 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectsOperatedOnCount:
          type: string
          description: Total number of objects that meet the storage tasks condition and were operated upon. Filter options such as objectsOperatedOnCount ge 100 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectFailedCount:
          type: string
          description: Total number of objects where task operation failed when was attempted. Filter options such as objectFailedCount eq 0 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectsSucceededCount:
          type: string
          description: Total number of objects where task operation succeeded when was attempted.Filter options such as objectsSucceededCount gt 150 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        runStatusError:
          type: string
          description: Well known Azure Storage error code that represents the error encountered during execution of the run instance.
        runStatusEnum:
          $ref: "#/components/schemas/RunStatusEnum"
          description: 'Represents the status of the execution. Known values are: "InProgress" and "Finished".'
        summaryReportPath:
          type: string
          description: Full path to the verbose report stored in the reporting container as specified in the assignment execution context for the storage account.
        taskId:
          type: string
          description: Resource ID of the Storage Task applied during this run.
        taskVersion:
          type: string
          description: Storage Task Version.
        runResult:
          $ref: "#/components/schemas/RunResult"
          description: 'Represents the overall result of the execution for the run instance. Known values are: "Succeeded" and "Failed".'
    RunStatusEnum:
      type: string
      enum:
        - InProgress
        - Finished
    RunResult:
      type: string
      enum:
        - Succeeded
        - Failed
    StorageTaskReportInstanceFlattened:
      type: object
      description: Flattened view of StorageTaskReportInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        taskAssignmentId:
          type: string
          description: Resource ID of the Storage Task Assignment associated with this reported run.
        storageAccountId:
          type: string
          description: Resource ID of the Storage Account where this reported run executed.
        startTime:
          type: string
          description: Start time of the run instance. Filter options such as startTime gt '2023-06-26T20:51:24.4494016Z' and other comparison operators can be used as described for DateTime properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        finishTime:
          type: string
          description: End time of the run instance. Filter options such as startTime gt '2023-06-26T20:51:24.4494016Z' and other comparison operators can be used as described for DateTime properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectsTargetedCount:
          type: string
          description: Total number of objects that meet the condition as defined in the storage task assignment execution context. Filter options such as objectsTargetedCount gt 50 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectsOperatedOnCount:
          type: string
          description: Total number of objects that meet the storage tasks condition and were operated upon. Filter options such as objectsOperatedOnCount ge 100 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectFailedCount:
          type: string
          description: Total number of objects where task operation failed when was attempted. Filter options such as objectFailedCount eq 0 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        objectsSucceededCount:
          type: string
          description: Total number of objects where task operation succeeded when was attempted.Filter options such as objectsSucceededCount gt 150 and other comparison operators can be used as described for Numerical properties in `https://learn.microsoft.com/en-us/rest/api/storageservices/querying-tables-and-entities#supported-comparison-operators `_.
        runStatusError:
          type: string
          description: Well known Azure Storage error code that represents the error encountered during execution of the run instance.
        runStatusEnum:
          $ref: "#/components/schemas/RunStatusEnum"
          description: 'Represents the status of the execution. Known values are: "InProgress" and "Finished".'
        summaryReportPath:
          type: string
          description: Full path to the verbose report stored in the reporting container as specified in the assignment execution context for the storage account.
        taskId:
          type: string
          description: Resource ID of the Storage Task applied during this run.
        taskVersion:
          type: string
          description: Storage Task Version.
        runResult:
          $ref: "#/components/schemas/RunResult"
          description: 'Represents the overall result of the execution for the run instance. Known values are: "Succeeded" and "Failed".'
    PagedStorageTaskReportInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/StorageTaskReportInstanceFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    operations:
      id: azure.storageactions.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.StorageActions~1operations?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    storage_task_assignment:
      id: azure.storageactions.storage_task_assignment
      name: storage_task_assignment
      title: storage_task_assignment
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageActions~1storageTasks~1{storage_task_name}~1storageTaskAssignments?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/storage_task_assignment/methods/list"
        insert: []
        update: []
        delete: []
    storage_tasks:
      id: azure.storageactions.storage_tasks
      name: storage_tasks
      title: storage_tasks
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageActions~1storageTasks~1{storage_task_name}?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageActions~1storageTasks~1{storage_task_name}?api-version=2023-01-01/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageActions~1storageTasks~1{storage_task_name}?api-version=2023-01-01/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageActions~1storageTasks~1{storage_task_name}?api-version=2023-01-01/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageActions~1storageTasks?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StorageActions~1storageTasks?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        preview_actions:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.StorageActions~1locations~1{location}~1previewActions?api-version=2023-01-01/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/storage_tasks/methods/get"
          - $ref: "#/components/x-stackQL-resources/storage_tasks/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/storage_tasks/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/storage_tasks/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/storage_tasks/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/storage_tasks/methods/delete"
    storage_tasks_report:
      id: azure.storageactions.storage_tasks_report
      name: storage_tasks_report
      title: storage_tasks_report
      methods:
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.StorageActions~1storageTasks~1{storage_task_name}~1reports?api-version=2023-01-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/storage_tasks_report/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /providers/Microsoft.StorageActions/operations?api-version=2023-01-01:
    get:
      operationId: storageactions_list
      description: Lists all of the available Storage Actions Rest API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StorageActions/storageTasks/{storage_task_name}/storageTaskAssignments?api-version=2023-01-01:
    get:
      operationId: StorageTaskAssignment_list
      description: Lists Resource IDs of the Storage Task Assignments associated with this Storage Task.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the storage task within the specified resource group. Storage task names must be between 3 and 18 characters in length and use numbers and lower-case letters only. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $maxpagesize
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStorageTaskAssignment"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StorageActions/storageTasks/{storage_task_name}?api-version=2023-01-01:
    get:
      operationId: StorageTasks_get
      description: Get the storage task properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the storage task within the specified resource group. Storage task names must be between 3 and 18 characters in length and use numbers and lower-case letters only. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageTaskFlattened"
    put:
      operationId: StorageTasks_begin_create
      description: Asynchronously creates a new storage task resource with the specified parameters. If a storage task is already created and a subsequent create request is issued with different properties, the storage task properties will be updated. If a storage task is already created and a subsequent create or update request is issued with the exact same set of properties, the request will succeed.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the storage task within the specified resource group. Storage task names must be between 3 and 18 characters in length and use numbers and lower-case letters only. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identity of the resource. Required.
                properties:
                  $ref: "#/components/schemas/StorageTaskProperties"
                  description: Properties of the storage task. Required.
              required:
                - location
                - identity
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageTask"
    patch:
      operationId: StorageTasks_begin_update
      description: Update storage task properties.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the storage task within the specified resource group. Storage task names must be between 3 and 18 characters in length and use numbers and lower-case letters only. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The identity of the resource.
                tags:
                  type: object
                  properties: {}
                  description: Gets or sets a list of key value pairs that describe the resource. These tags can be used in viewing and grouping this resource (across resource groups). A maximum of 15 tags can be provided for a resource. Each tag must have a key no greater in length than 128 characters and a value no greater in length than 256 characters.
                properties:
                  $ref: "#/components/schemas/StorageTaskUpdateProperties"
                  description: Properties of the storage task.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageTask"
    delete:
      operationId: StorageTasks_begin_delete
      description: Delete the storage task resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the storage task within the specified resource group. Storage task names must be between 3 and 18 characters in length and use numbers and lower-case letters only. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StorageActions/storageTasks?api-version=2023-01-01:
    get:
      operationId: StorageTasks_list_by_resource_group
      description: Lists all the storage tasks available under the given resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStorageTaskFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.StorageActions/storageTasks?api-version=2023-01-01:
    get:
      operationId: StorageTasks_list_by_subscription
      description: Lists all the storage tasks available under the subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStorageTaskFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.StorageActions/locations/{location}/previewActions?api-version=2023-01-01:
    post:
      operationId: StorageTasks_preview_actions
      description: Runs the input conditions against input object metadata properties and designates matched objects in response.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/StorageTaskPreviewActionProperties"
                  description: Properties of the storage task preview. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageTaskPreviewAction"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.StorageActions/storageTasks/{storage_task_name}/reports?api-version=2023-01-01:
    get:
      operationId: StorageTasksReport_list
      description: Fetch the storage tasks run report summary for each assignment.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: storage_task_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the storage task within the specified resource group. Storage task names must be between 3 and 18 characters in length and use numbers and lower-case letters only. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $maxpagesize
          in: query
          required: false
          schema:
            type: integer
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: Optional. When specified, it can be used to query using reporting properties. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedStorageTaskReportInstanceFlattened"
