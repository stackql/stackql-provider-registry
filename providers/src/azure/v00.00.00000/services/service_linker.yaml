openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Service Connector
  description: Link compute services to backing services with connection configuration (Microsoft.ServiceLinker).
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-07-01-preview-stackql-generated
  x-serviceAlias: service_linker
  x-sdkPackage: azure-mgmt-servicelinker
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    DaprProperties:
      type: object
      description: Indicates some additional properties for dapr client type. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        version:
          type: string
          description: The dapr component version.
        componentType:
          type: string
          description: The dapr component type.
        secretStoreComponent:
          type: string
          description: The name of a secret store dapr to retrieve secret.
        metadata:
          type: array
          items:
            $ref: "#/components/schemas/DaprMetadata"
          description: Additional dapr metadata.
        scopes:
          type: array
          items:
            type: string
          description: The dapr component scopes.
        runtimeVersion:
          type: string
          description: The runtime version supported by the properties.
        bindingComponentDirection:
          type: string
          description: 'The direction supported by the dapr binding component. Known values are: "input" and "output".'
    DaprMetadata:
      type: object
      description: The dapr component metadata.
      properties:
        name:
          type: string
          description: Metadata property name.
        value:
          type: string
          description: Metadata property value.
        secretRef:
          type: string
          description: The secret name where dapr could get value.
        description:
          type: string
          description: The description of the metadata, returned from configuration api.
        required:
          type: string
          description: 'The value indicating whether the metadata is required or not. Known values are: "true" and "false".'
    ConfigurationName:
      type: object
      description: The configuration names.
      properties:
        value:
          type: string
          description: ":vartype value: str"
        description:
          type: string
          description: Description for the configuration name.
        required:
          type: boolean
          description: Represent the configuration is required or not.
    ConfigurationNameItemFlattened:
      type: object
      description: Flattened view of ConfigurationNameItem ('properties' hoisted to the top level).
      properties:
        targetService:
          type: string
          description: The target service provider name and resource name.
        clientType:
          type: string
          description: 'The client type for configuration names. Known values are: "none", "dotnet", "java", "python", "go", "php", "ruby", "django", "nodejs", "springBoot", "kafka-springBoot", "jms-springBoot", and "dapr".'
        authType:
          type: string
          description: 'The auth type. Known values are: "systemAssignedIdentity", "userAssignedIdentity", "servicePrincipalSecret", "servicePrincipalCertificate", "secret", "accessKey", "userAccount", and "easyAuthMicrosoftEntraID".'
        secretType:
          type: string
          description: 'Indicates where the secrets in configuration from. Used when secrets are from Keyvault. Known values are: "rawValue" and "keyVaultSecret".'
        daprProperties:
          $ref: "#/components/schemas/DaprProperties"
          description: "Deprecated, please use #/definitions/DaprConfigurationList instead."
        names:
          type: array
          items:
            $ref: "#/components/schemas/ConfigurationName"
          description: The configuration names to be set in compute service environment.
    PagedConfigurationNameItemFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ConfigurationNameItemFlattened"
        nextLink:
          type: string
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    TargetServiceBase:
      type: object
      description: "The target service properties. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AzureResource, ConfluentBootstrapServer, ConfluentSchemaRegistry, FabricPlatform, SelfHostedServer All required parameters must be populated in order to send to server."
      properties:
        type:
          type: string
          description: 'The target service type. Required. Known values are: "AzureResource", "ConfluentBootstrapServer", "ConfluentSchemaRegistry", "SelfHostedServer", and "FabricPlatform".'
      required:
        - type
    AuthInfoBase:
      type: object
      description: "The authentication info. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AccessKeyInfoBase, EasyAuthMicrosoftEntraIDAuthInfo, SecretAuthInfo, ServicePrincipalCertificateAuthInfo, ServicePrincipalSecretAuthInfo, SystemAssignedIdentityAuthInfo, UserAccountAuthInfo, UserAssignedIdentityAuthInfo All required parameters must be populated in order to send to server."
      properties:
        authType:
          type: string
          description: 'The authentication type. Required. Known values are: "systemAssignedIdentity", "userAssignedIdentity", "servicePrincipalSecret", "servicePrincipalCertificate", "secret", "accessKey", "userAccount", and "easyAuthMicrosoftEntraID".'
        authMode:
          type: string
          description: 'Optional. Indicates how to configure authentication. If optInAllAuth, service linker configures authentication such as enabling identity on source resource and granting RBAC roles. If optOutAllAuth, opt out authentication setup. Default is optInAllAuth. Known values are: "optInAllAuth" and "optOutAllAuth".'
      required:
        - authType
    VNetSolution:
      type: object
      description: The VNet solution for linker.
      properties:
        type:
          type: string
          description: 'Type of VNet solution. Known values are: "serviceEndpoint" and "privateLink".'
        deleteOrUpdateBehavior:
          type: string
          description: 'Indicates whether to clean up previous operation when Linker is updating or deleting. Known values are: "Default" and "ForcedCleanup".'
    SecretStore:
      type: object
      description: An option to store secret value in secure place.
      properties:
        keyVaultId:
          type: string
          description: The key vault id to store secret.
        keyVaultSecretName:
          type: string
          description: The key vault secret name to store secret, only valid when storing one secret.
    PublicNetworkSolution:
      type: object
      description: Indicates public network solution, include firewall rules.
      properties:
        deleteOrUpdateBehavior:
          type: string
          description: 'Indicates whether to clean up previous operation(such as firewall rules) when Linker is updating or deleting. Known values are: "Default" and "ForcedCleanup".'
        action:
          type: string
          description: 'Optional. Indicates public network solution. If enable, enable public network access of target service with best try. Default is enable. If optOut, opt out public network access configuration. Known values are: "Internal", "enable", and "optOut".'
        firewallRules:
          $ref: "#/components/schemas/FirewallRules"
          description: Describe firewall rules of target service to make sure source application could connect to the target.
    FirewallRules:
      type: object
      description: Target service's firewall rules. to allow connections from source service.
      properties:
        ipRanges:
          type: array
          items:
            type: string
          description: This value specifies the set of IP addresses or IP address ranges in CIDR form to be included as the allowed list of client IPs for a given database account.
        azureServices:
          type: string
          description: 'Allow Azure services to access the target service if true. Known values are: "true" and "false".'
        callerClientIP:
          type: string
          description: 'Allow caller client IP to access the target service if true. the property is used when connecting local application to target service. Known values are: "true" and "false".'
    ConfigurationInfo:
      type: object
      description: The configuration information, used to generate configurations or save to applications.
      properties:
        deleteOrUpdateBehavior:
          type: string
          description: 'Indicates whether to clean up previous operation when Linker is updating or deleting. Known values are: "Default" and "ForcedCleanup".'
        action:
          type: string
          description: 'Optional, indicate whether to apply configurations on source application. If enable, generate configurations and applied to the source application. Default is enable. If optOut, no configuration change will be made on source. Known values are: "Internal", "enable", and "optOut".'
        customizedKeys:
          type: object
          properties: {}
          description: Optional. A dictionary of default key name and customized key name mapping. If not specified, default key name will be used for generate configurations.
        daprProperties:
          $ref: "#/components/schemas/DaprProperties"
          description: Indicates some additional properties for dapr client type.
        additionalConfigurations:
          type: object
          properties: {}
          description: A dictionary of additional configurations to be added. Service will auto generate a set of basic configurations and this property is to full fill more customized configurations.
        additionalConnectionStringProperties:
          type: object
          properties: {}
          description: A dictionary of additional properties to be added in the end of connection string.
        configurationStore:
          $ref: "#/components/schemas/ConfigurationStore"
          description: An option to store configuration into different place.
    ConfigurationStore:
      type: object
      description: An option to store configuration into different place.
      properties:
        appConfigurationId:
          type: string
          description: The app configuration id to store configuration.
    LinkerResourceFlattened:
      type: object
      description: Flattened view of LinkerResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        targetService:
          $ref: "#/components/schemas/TargetServiceBase"
          description: The target service properties.
        authInfo:
          $ref: "#/components/schemas/AuthInfoBase"
          description: The authentication type.
        clientType:
          type: string
          description: 'The application client type. Known values are: "none", "dotnet", "java", "python", "go", "php", "ruby", "django", "nodejs", "springBoot", "kafka-springBoot", "jms-springBoot", and "dapr".'
        provisioningState:
          type: string
          description: The provisioning state.
        vNetSolution:
          $ref: "#/components/schemas/VNetSolution"
          description: The VNet solution.
        secretStore:
          $ref: "#/components/schemas/SecretStore"
          description: An option to store secret value in secure place.
        scope:
          type: string
          description: connection scope in source service.
        publicNetworkSolution:
          $ref: "#/components/schemas/PublicNetworkSolution"
          description: The network solution.
        configurationInfo:
          $ref: "#/components/schemas/ConfigurationInfo"
          description: The connection information consumed by applications, including secrets, connection strings.
    PagedLinkerResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/LinkerResourceFlattened"
        nextLink:
          type: string
    LinkerResource:
      type: object
      description: Linker of source and target resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            targetService:
              $ref: "#/components/schemas/TargetServiceBase"
              description: The target service properties.
            authInfo:
              $ref: "#/components/schemas/AuthInfoBase"
              description: The authentication type.
            clientType:
              type: string
              description: 'The application client type. Known values are: "none", "dotnet", "java", "python", "go", "php", "ruby", "django", "nodejs", "springBoot", "kafka-springBoot", "jms-springBoot", and "dapr".'
            provisioningState:
              type: string
              description: The provisioning state.
            vNetSolution:
              $ref: "#/components/schemas/VNetSolution"
              description: The VNet solution.
            secretStore:
              $ref: "#/components/schemas/SecretStore"
              description: An option to store secret value in secure place.
            scope:
              type: string
              description: connection scope in source service.
            publicNetworkSolution:
              $ref: "#/components/schemas/PublicNetworkSolution"
              description: The network solution.
            configurationInfo:
              $ref: "#/components/schemas/ConfigurationInfo"
              description: The connection information consumed by applications, including secrets, connection strings.
    DryrunParameters:
      type: object
      description: "The parameters of the dryrun. You probably want to use the sub-classes and not this class directly. Known sub-classes are: CreateOrUpdateDryrunParameters All required parameters must be populated in order to send to server."
      properties:
        actionName:
          type: string
          description: The name of action for you dryrun job. Required. "createOrUpdate"
      required:
        - actionName
    DryrunPrerequisiteResult:
      type: object
      description: "A result of dryrun. You probably want to use the sub-classes and not this class directly. Known sub-classes are: BasicErrorDryrunPrerequisiteResult, PermissionsMissingDryrunPrerequisiteResult All required parameters must be populated in order to send to server."
      properties:
        type:
          type: string
          description: 'The type of dryrun result. Required. Known values are: "basicError" and "permissionsMissing".'
      required:
        - type
    DryrunOperationPreview:
      type: object
      description: The preview of the operations for creation.
      properties:
        name:
          type: string
          description: The operation name.
        operationType:
          type: string
          description: 'The operation type. Known values are: "configConnection", "configNetwork", and "configAuth".'
        description:
          type: string
          description: The description of the operation.
        action:
          type: string
          description: The action defined by RBAC, refer https://docs.microsoft.com/azure/role-based-access-control/role-definitions#actions-format.
        scope:
          type: string
          description: The scope of the operation, refer https://docs.microsoft.com/azure/role-based-access-control/scope-overview.
    DryrunResourceFlattened:
      type: object
      description: Flattened view of DryrunResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        parameters:
          $ref: "#/components/schemas/DryrunParameters"
          description: The parameters of the dryrun.
        prerequisiteResults:
          type: array
          items:
            $ref: "#/components/schemas/DryrunPrerequisiteResult"
          description: the result of the dryrun.
        operationPreviews:
          type: array
          items:
            $ref: "#/components/schemas/DryrunOperationPreview"
          description: the preview of the operations for creation.
        provisioningState:
          type: string
          description: The provisioning state.
    PagedDryrunResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DryrunResourceFlattened"
        nextLink:
          type: string
    DryrunResource:
      type: object
      description: a dryrun job resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            parameters:
              $ref: "#/components/schemas/DryrunParameters"
              description: The parameters of the dryrun.
            prerequisiteResults:
              type: array
              items:
                $ref: "#/components/schemas/DryrunPrerequisiteResult"
              description: the result of the dryrun.
            operationPreviews:
              type: array
              items:
                $ref: "#/components/schemas/DryrunOperationPreview"
              description: the preview of the operations for creation.
            provisioningState:
              type: string
              description: The provisioning state.
    ValidateOperationResult:
      type: object
      description: The validation operation result for a Linker.
      properties:
        resourceId:
          type: string
          description: Validated Linker id.
        status:
          type: string
          description: Validation operation status.
        properties:
          type: object
          properties:
            linkerName:
              type: string
              description: The linker name.
            isConnectionAvailable:
              type: boolean
              description: A boolean value indicating whether the connection is available or not.
            reportStartTimeUtc:
              type: string
              format: date-time
              description: The start time of the validation report.
            reportEndTimeUtc:
              type: string
              format: date-time
              description: The end time of the validation report.
            sourceId:
              type: string
              description: The resource id of the Linker source application.
            targetId:
              type: string
              description: The resource Id of target service.
            authType:
              type: string
              description: 'The authentication type. Known values are: "systemAssignedIdentity", "userAssignedIdentity", "servicePrincipalSecret", "servicePrincipalCertificate", "secret", "accessKey", "userAccount", and "easyAuthMicrosoftEntraID".'
            validationDetail:
              type: array
              items:
                $ref: "#/components/schemas/ValidationResultItem"
              description: The detail of validation result.
    ValidationResultItem:
      type: object
      description: The validation item for a Linker.
      properties:
        name:
          type: string
          description: The validation item name.
        description:
          type: string
          description: The display name of validation item.
        result:
          type: string
          description: 'The result of validation. Known values are: "success", "failure", and "warning".'
        errorMessage:
          type: string
          description: The error message of validation result.
        errorCode:
          type: string
          description: The error code of validation result.
    ConfigurationResult:
      type: object
      description: Configurations for source resource, include appSettings, connectionString and serviceBindings.
      properties:
        configurations:
          type: array
          items:
            $ref: "#/components/schemas/SourceConfiguration"
          description: The configuration properties for source resource.
    SourceConfiguration:
      type: object
      description: A configuration item for source resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The name of setting.
        value:
          type: string
          description: The value of setting.
        configType:
          type: string
          description: 'The type of setting. Known values are: "Default" and "KeyVaultSecret".'
        keyVaultReferenceIdentity:
          type: string
          description: The identity for key vault reference, system or user-assigned managed identity ID.
        description:
          type: string
          description: Descriptive information for the configuration.
    DaprConfigurationResourceFlattened:
      type: object
      description: Flattened view of DaprConfigurationResource ('properties' hoisted to the top level).
      properties:
        targetType:
          type: string
          description: Supported target resource type, extract from resource id, uppercase.
        authType:
          type: string
          description: 'The authentication type. Known values are: "systemAssignedIdentity", "userAssignedIdentity", "servicePrincipalSecret", "servicePrincipalCertificate", "secret", "accessKey", "userAccount", and "easyAuthMicrosoftEntraID".'
        daprProperties:
          $ref: "#/components/schemas/DaprProperties"
          description: Indicates some additional properties for dapr client type.
    PagedDaprConfigurationResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/DaprConfigurationResourceFlattened"
        nextLink:
          type: string
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: 'Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. Known values are: "Internal", "enable", and "optOut".'
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
  x-stackQL-resources:
    configuration_names:
      id: azure.service_linker.configuration_names
      name: configuration_names
      title: configuration_names
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ServiceLinker~1configurationNames?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/configuration_names/methods/list"
        insert: []
        update: []
        delete: []
    connector:
      id: azure.service_linker.connector
      name: connector
      title: connector
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1connectors~1{connector_name}?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1connectors~1{connector_name}?api-version=2024-07-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1connectors~1{connector_name}?api-version=2024-07-01-preview/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1connectors~1{connector_name}?api-version=2024-07-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1connectors?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_dryrun:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1dryruns?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_dryrun:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_dryrun:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update_dryrun:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_dryrun:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/delete"
          response:
            openAPIDocKey: "200"
        validate:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1connectors~1{connector_name}~1validate?api-version=2024-07-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        generate_configurations:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourcegroups~1{resource_group_name}~1providers~1Microsoft.ServiceLinker~1locations~1{location}~1connectors~1{connector_name}~1generateConfigurations?api-version=2024-07-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/connector/methods/get"
          - $ref: "#/components/x-stackQL-resources/connector/methods/get_dryrun"
          - $ref: "#/components/x-stackQL-resources/connector/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/connector/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/connector/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/connector/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/connector/methods/delete"
    linker:
      id: azure.service_linker.linker
      name: linker
      title: linker
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers~1{linker_name}?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers~1{linker_name}?api-version=2024-07-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers~1{linker_name}?api-version=2024-07-01-preview/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers~1{linker_name}?api-version=2024-07-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_configurations:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers~1{linker_name}~1listConfigurations?api-version=2024-07-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
        validate:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers~1{linker_name}~1validateLinker?api-version=2024-07-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/linker/methods/get"
          - $ref: "#/components/x-stackQL-resources/linker/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/linker/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/linker/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/linker/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/linker/methods/delete"
    linkers:
      id: azure.service_linker.linkers
      name: linkers
      title: linkers
      methods:
        list_dryrun:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1dryruns?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_dapr_configurations:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1daprConfigurations?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_dryrun:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_dryrun:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update_dryrun:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_dryrun:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1dryruns~1{dryrun_name}?api-version=2024-07-01-preview/delete"
          response:
            openAPIDocKey: "200"
        generate_configurations:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ServiceLinker~1linkers~1{linker_name}~1generateConfigurations?api-version=2024-07-01-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/linkers/methods/get_dryrun"
          - $ref: "#/components/x-stackQL-resources/linkers/methods/list_dryrun"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.service_linker.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ServiceLinker~1operations?api-version=2024-07-01-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /providers/Microsoft.ServiceLinker/configurationNames?api-version=2024-07-01-preview:
    get:
      operationId: ConfigurationNames_list
      description: Lists the configuration names generated by Service Connector for all target, client types, auth types.
      parameters:
        - name: $filter
          in: query
          required: false
          schema:
            type: string
          description: OData filter options. Default value is None.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: OData skipToken option for pagination. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConfigurationNameItemFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.ServiceLinker/locations/{location}/connectors/{connector_name}?api-version=2024-07-01-preview:
    get:
      operationId: Connector_get
      description: Returns Connector resource for a given name.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkerResourceFlattened"
    put:
      operationId: Connector_begin_create_or_update
      description: Create or update Connector resource.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    targetService:
                      $ref: "#/components/schemas/TargetServiceBase"
                      description: The target service properties.
                    authInfo:
                      $ref: "#/components/schemas/AuthInfoBase"
                      description: The authentication type.
                    clientType:
                      type: string
                      description: 'The application client type. Known values are: "none", "dotnet", "java", "python", "go", "php", "ruby", "django", "nodejs", "springBoot", "kafka-springBoot", "jms-springBoot", and "dapr".'
                    vNetSolution:
                      $ref: "#/components/schemas/VNetSolution"
                      description: The VNet solution.
                    secretStore:
                      $ref: "#/components/schemas/SecretStore"
                      description: An option to store secret value in secure place.
                    scope:
                      type: string
                      description: connection scope in source service.
                    publicNetworkSolution:
                      $ref: "#/components/schemas/PublicNetworkSolution"
                      description: The network solution.
                    configurationInfo:
                      $ref: "#/components/schemas/ConfigurationInfo"
                      description: The connection information consumed by applications, including secrets, connection strings.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkerResource"
    delete:
      operationId: Connector_begin_delete
      description: Delete a Connector.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of resource. Required.
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Connector_begin_update
      description: Operation to update an existing Connector.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    targetService:
                      $ref: "#/components/schemas/TargetServiceBase"
                      description: The target service properties.
                    authInfo:
                      $ref: "#/components/schemas/AuthInfoBase"
                      description: The authentication type.
                    clientType:
                      type: string
                      description: 'The application client type. Known values are: "none", "dotnet", "java", "python", "go", "php", "ruby", "django", "nodejs", "springBoot", "kafka-springBoot", "jms-springBoot", and "dapr".'
                    vNetSolution:
                      $ref: "#/components/schemas/VNetSolution"
                      description: The VNet solution.
                    secretStore:
                      $ref: "#/components/schemas/SecretStore"
                      description: An option to store secret value in secure place.
                    scope:
                      type: string
                      description: connection scope in source service.
                    publicNetworkSolution:
                      $ref: "#/components/schemas/PublicNetworkSolution"
                      description: The network solution.
                    configurationInfo:
                      $ref: "#/components/schemas/ConfigurationInfo"
                      description: The connection information consumed by applications, including secrets, connection strings.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkerResource"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.ServiceLinker/locations/{location}/connectors?api-version=2024-07-01-preview:
    get:
      operationId: Connector_list
      description: Returns list of connector which connects to the resource, which supports to config the target service during the resource provision.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLinkerResourceFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.ServiceLinker/locations/{location}/dryruns?api-version=2024-07-01-preview:
    get:
      operationId: Connector_list_dryrun
      description: list dryrun jobs.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDryrunResourceFlattened"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.ServiceLinker/locations/{location}/dryruns/{dryrun_name}?api-version=2024-07-01-preview:
    get:
      operationId: Connector_get_dryrun
      description: get a dryrun job.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DryrunResourceFlattened"
    put:
      operationId: Connector_begin_create_dryrun
      description: create a dryrun job to do necessary check before actual creation.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    parameters:
                      $ref: "#/components/schemas/DryrunParameters"
                      description: The parameters of the dryrun.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DryrunResource"
    patch:
      operationId: Connector_begin_update_dryrun
      description: update a dryrun job to do necessary check before actual creation.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    parameters:
                      $ref: "#/components/schemas/DryrunParameters"
                      description: The parameters of the dryrun.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DryrunResource"
    delete:
      operationId: Connector_delete_dryrun
      description: delete a dryrun job.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      responses:
        "200":
          description: Success
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.ServiceLinker/locations/{location}/connectors/{connector_name}/validate?api-version=2024-07-01-preview:
    post:
      operationId: Connector_begin_validate
      description: Validate a Connector.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateOperationResult"
  /subscriptions/{subscription_id}/resourcegroups/{resource_group_name}/providers/Microsoft.ServiceLinker/locations/{location}/connectors/{connector_name}/generateConfigurations?api-version=2024-07-01-preview:
    post:
      operationId: Connector_generate_configurations
      description: Generate configurations for a Connector.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the target subscription. Required.
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of Azure region. Required.
        - name: connector_name
          in: path
          required: true
          schema:
            type: string
          description: The name of resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                deleteOrUpdateBehavior:
                  type: string
                  description: 'Indicates whether to clean up previous operation when Linker is updating or deleting. Known values are: "Default" and "ForcedCleanup".'
                action:
                  type: string
                  description: 'Optional, indicate whether to apply configurations on source application. If enable, generate configurations and applied to the source application. Default is enable. If optOut, no configuration change will be made on source. Known values are: "Internal", "enable", and "optOut".'
                customizedKeys:
                  type: object
                  properties: {}
                  description: Optional. A dictionary of default key name and customized key name mapping. If not specified, default key name will be used for generate configurations.
                daprProperties:
                  $ref: "#/components/schemas/DaprProperties"
                  description: Indicates some additional properties for dapr client type.
                additionalConfigurations:
                  type: object
                  properties: {}
                  description: A dictionary of additional configurations to be added. Service will auto generate a set of basic configurations and this property is to full fill more customized configurations.
                additionalConnectionStringProperties:
                  type: object
                  properties: {}
                  description: A dictionary of additional properties to be added in the end of connection string.
                configurationStore:
                  $ref: "#/components/schemas/ConfigurationStore"
                  description: An option to store configuration into different place.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigurationResult"
  /{resource_uri}/providers/Microsoft.ServiceLinker/linkers/{linker_name}?api-version=2024-07-01-preview:
    get:
      operationId: Linker_get
      description: Returns Linker resource for a given name.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: linker_name
          in: path
          required: true
          schema:
            type: string
          description: The name Linker resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkerResourceFlattened"
    put:
      operationId: Linker_begin_create_or_update
      description: Create or update Linker resource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: linker_name
          in: path
          required: true
          schema:
            type: string
          description: The name Linker resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    targetService:
                      $ref: "#/components/schemas/TargetServiceBase"
                      description: The target service properties.
                    authInfo:
                      $ref: "#/components/schemas/AuthInfoBase"
                      description: The authentication type.
                    clientType:
                      type: string
                      description: 'The application client type. Known values are: "none", "dotnet", "java", "python", "go", "php", "ruby", "django", "nodejs", "springBoot", "kafka-springBoot", "jms-springBoot", and "dapr".'
                    vNetSolution:
                      $ref: "#/components/schemas/VNetSolution"
                      description: The VNet solution.
                    secretStore:
                      $ref: "#/components/schemas/SecretStore"
                      description: An option to store secret value in secure place.
                    scope:
                      type: string
                      description: connection scope in source service.
                    publicNetworkSolution:
                      $ref: "#/components/schemas/PublicNetworkSolution"
                      description: The network solution.
                    configurationInfo:
                      $ref: "#/components/schemas/ConfigurationInfo"
                      description: The connection information consumed by applications, including secrets, connection strings.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkerResource"
    delete:
      operationId: Linker_begin_delete
      description: Delete a Linker.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: linker_name
          in: path
          required: true
          schema:
            type: string
          description: The name Linker resource. Required.
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Linker_begin_update
      description: Operation to update an existing Linker.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: linker_name
          in: path
          required: true
          schema:
            type: string
          description: The name Linker resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    targetService:
                      $ref: "#/components/schemas/TargetServiceBase"
                      description: The target service properties.
                    authInfo:
                      $ref: "#/components/schemas/AuthInfoBase"
                      description: The authentication type.
                    clientType:
                      type: string
                      description: 'The application client type. Known values are: "none", "dotnet", "java", "python", "go", "php", "ruby", "django", "nodejs", "springBoot", "kafka-springBoot", "jms-springBoot", and "dapr".'
                    vNetSolution:
                      $ref: "#/components/schemas/VNetSolution"
                      description: The VNet solution.
                    secretStore:
                      $ref: "#/components/schemas/SecretStore"
                      description: An option to store secret value in secure place.
                    scope:
                      type: string
                      description: connection scope in source service.
                    publicNetworkSolution:
                      $ref: "#/components/schemas/PublicNetworkSolution"
                      description: The network solution.
                    configurationInfo:
                      $ref: "#/components/schemas/ConfigurationInfo"
                      description: The connection information consumed by applications, including secrets, connection strings.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkerResource"
  /{resource_uri}/providers/Microsoft.ServiceLinker/linkers?api-version=2024-07-01-preview:
    get:
      operationId: Linker_list
      description: Returns list of Linkers which connects to the resource. which supports to config both application and target service during the resource provision.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedLinkerResourceFlattened"
  /{resource_uri}/providers/Microsoft.ServiceLinker/linkers/{linker_name}/listConfigurations?api-version=2024-07-01-preview:
    post:
      operationId: Linker_list_configurations
      description: list source configurations for a Linker.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: linker_name
          in: path
          required: true
          schema:
            type: string
          description: The name Linker resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigurationResult"
  /{resource_uri}/providers/Microsoft.ServiceLinker/linkers/{linker_name}/validateLinker?api-version=2024-07-01-preview:
    post:
      operationId: Linker_begin_validate
      description: Validate a Linker.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: linker_name
          in: path
          required: true
          schema:
            type: string
          description: The name Linker resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateOperationResult"
  /{resource_uri}/providers/Microsoft.ServiceLinker/dryruns?api-version=2024-07-01-preview:
    get:
      operationId: Linkers_list_dryrun
      description: list dryrun jobs.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDryrunResourceFlattened"
  /{resource_uri}/providers/Microsoft.ServiceLinker/daprConfigurations?api-version=2024-07-01-preview:
    get:
      operationId: Linkers_list_dapr_configurations
      description: List the dapr configuration supported by Service Connector.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedDaprConfigurationResourceFlattened"
  /{resource_uri}/providers/Microsoft.ServiceLinker/dryruns/{dryrun_name}?api-version=2024-07-01-preview:
    get:
      operationId: Linkers_get_dryrun
      description: get a dryrun job.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DryrunResourceFlattened"
    put:
      operationId: Linkers_begin_create_dryrun
      description: create a dryrun job to do necessary check before actual creation.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    parameters:
                      $ref: "#/components/schemas/DryrunParameters"
                      description: The parameters of the dryrun.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DryrunResource"
    patch:
      operationId: Linkers_begin_update_dryrun
      description: add a dryrun job to do necessary check before actual creation.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    parameters:
                      $ref: "#/components/schemas/DryrunParameters"
                      description: The parameters of the dryrun.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DryrunResource"
    delete:
      operationId: Linkers_delete_dryrun
      description: delete a dryrun job.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: dryrun_name
          in: path
          required: true
          schema:
            type: string
          description: The name of dryrun. Required.
      responses:
        "200":
          description: Success
  /{resource_uri}/providers/Microsoft.ServiceLinker/linkers/{linker_name}/generateConfigurations?api-version=2024-07-01-preview:
    post:
      operationId: Linkers_generate_configurations
      description: Generate configurations for a Linker.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource to be connected. Required.
        - name: linker_name
          in: path
          required: true
          schema:
            type: string
          description: The name Linker resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                deleteOrUpdateBehavior:
                  type: string
                  description: 'Indicates whether to clean up previous operation when Linker is updating or deleting. Known values are: "Default" and "ForcedCleanup".'
                action:
                  type: string
                  description: 'Optional, indicate whether to apply configurations on source application. If enable, generate configurations and applied to the source application. Default is enable. If optOut, no configuration change will be made on source. Known values are: "Internal", "enable", and "optOut".'
                customizedKeys:
                  type: object
                  properties: {}
                  description: Optional. A dictionary of default key name and customized key name mapping. If not specified, default key name will be used for generate configurations.
                daprProperties:
                  $ref: "#/components/schemas/DaprProperties"
                  description: Indicates some additional properties for dapr client type.
                additionalConfigurations:
                  type: object
                  properties: {}
                  description: A dictionary of additional configurations to be added. Service will auto generate a set of basic configurations and this property is to full fill more customized configurations.
                additionalConnectionStringProperties:
                  type: object
                  properties: {}
                  description: A dictionary of additional properties to be added in the end of connection string.
                configurationStore:
                  $ref: "#/components/schemas/ConfigurationStore"
                  description: An option to store configuration into different place.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigurationResult"
  /providers/Microsoft.ServiceLinker/operations?api-version=2024-07-01-preview:
    get:
      operationId: servicelinker_list
      description: Lists the available ServiceLinker REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
