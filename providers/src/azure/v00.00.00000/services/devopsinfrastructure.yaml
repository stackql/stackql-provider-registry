openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Dev Ops Infrastructure Mgmt Client
  description: DevOpsInfrastructureMgmtClient.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-10-19-stackql-generated
  x-serviceAlias: devopsinfrastructure
  x-sdkPackage: azure-mgmt-devopsinfrastructure
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    ImageVersionProperties:
      type: object
      description: Details of the ImageVersionProperties.
      properties:
        version:
          type: string
          description: Version of the image. Required.
      required:
        - version
    ImageVersionFlattened:
      type: object
      description: Flattened view of ImageVersion ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        version:
          type: string
          description: Version of the image. Required.
    PagedImageVersionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ImageVersionFlattened"
        nextLink:
          type: string
    Paged_models:
      type: object
      properties:
        value:
          type: array
          items:
            type: object
            properties: {}
        nextLink:
          type: string
    PoolProperties:
      type: object
      description: Pool properties.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the current operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        maximumConcurrency:
          type: integer
          description: Defines how many resources can there be created at any given time. Required.
        organizationProfile:
          $ref: "#/components/schemas/OrganizationProfile"
          description: Defines the organization in which the pool will be used. Required.
        agentProfile:
          $ref: "#/components/schemas/AgentProfile"
          description: Defines how the machine will be handled once it executed a job. Required.
        fabricProfile:
          $ref: "#/components/schemas/FabricProfile"
          description: Defines the type of fabric the agent will run on. Required.
        devCenterProjectResourceId:
          type: string
          description: The resource id of the DevCenter Project the pool belongs to. Required.
      required:
        - maximumConcurrency
        - organizationProfile
        - agentProfile
        - fabricProfile
        - devCenterProjectResourceId
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
        - Provisioning
        - Updating
        - Deleting
        - Accepted
    OrganizationProfile:
      type: object
      description: "Defines the organization in which the pool will be used. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AzureDevOpsOrganizationProfile, GitHubOrganizationProfile"
      properties:
        kind:
          type: string
          description: Discriminator property for OrganizationProfile. Required. Default value is None.
      required:
        - kind
    AgentProfile:
      type: object
      description: "The agent profile of the machines in the pool. You probably want to use the sub-classes and not this class directly. Known sub-classes are: Stateful, StatelessAgentProfile"
      properties:
        kind:
          type: string
          description: Discriminator property for AgentProfile. Required. Default value is None.
        resourcePredictions:
          $ref: "#/components/schemas/ResourcePredictions"
          description: Defines pool buffer/stand-by agents.
        resourcePredictionsProfile:
          $ref: "#/components/schemas/ResourcePredictionsProfile"
          description: Defines how the pool buffer/stand-by agents is provided.
      required:
        - kind
    ResourcePredictions:
      type: object
      description: Defines pool buffer.
      properties: {}
    ResourcePredictionsProfile:
      type: object
      description: "Determines how the stand-by scheme should be provided. You probably want to use the sub-classes and not this class directly. Known sub-classes are: AutomaticResourcePredictionsProfile, ManualResourcePredictionsProfile"
      properties:
        kind:
          type: string
          description: 'Determines how the stand-by scheme should be provided. Required. Known values are: "Manual" and "Automatic".'
      required:
        - kind
    FabricProfile:
      type: object
      description: "Defines the type of fabric the agent will run on. You probably want to use the sub-classes and not this class directly. Known sub-classes are: VmssFabricProfile"
      properties:
        kind:
          type: string
          description: Discriminator property for FabricProfile. Required. Default value is None.
      required:
        - kind
    ManagedServiceIdentity:
      type: object
      description: Managed service identity (system assigned and/or user assigned identities). Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        principalId:
          type: string
          description: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
        tenantId:
          type: string
          description: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
        type:
          $ref: "#/components/schemas/ManagedServiceIdentityType"
          description: 'The type of managed identity assigned to this resource. Required. Known values are: "None", "SystemAssigned", "UserAssigned", and "SystemAssigned,UserAssigned".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: The identities assigned to this resource by the user.
      required:
        - type
    ManagedServiceIdentityType:
      type: string
      enum:
        - None
        - SystemAssigned
        - UserAssigned
        - SystemAssigned,UserAssigned
    PoolFlattened:
      type: object
      description: Flattened view of Pool ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the current operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        maximumConcurrency:
          type: integer
          description: Defines how many resources can there be created at any given time. Required.
        organizationProfile:
          $ref: "#/components/schemas/OrganizationProfile"
          description: Defines the organization in which the pool will be used. Required.
        agentProfile:
          $ref: "#/components/schemas/AgentProfile"
          description: Defines how the machine will be handled once it executed a job. Required.
        fabricProfile:
          $ref: "#/components/schemas/FabricProfile"
          description: Defines the type of fabric the agent will run on. Required.
        devCenterProjectResourceId:
          type: string
          description: The resource id of the DevCenter Project the pool belongs to. Required.
    PagedPoolFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PoolFlattened"
        nextLink:
          type: string
    Pool:
      type: object
      description: Concrete tracked resource types can be created by aliasing this type using a specific property type. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/PoolProperties"
          description: The resource-specific properties for this resource.
        identity:
          $ref: "#/components/schemas/ManagedServiceIdentity"
          description: The managed service identities assigned to this resource.
      required:
        - location
    PoolUpdateProperties:
      type: object
      description: The updatable properties of the Pool.
      properties:
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'The status of the current operation. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", and "Accepted".'
        maximumConcurrency:
          type: integer
          description: Defines how many resources can there be created at any given time.
        organizationProfile:
          $ref: "#/components/schemas/OrganizationProfile"
          description: Defines the organization in which the pool will be used.
        agentProfile:
          $ref: "#/components/schemas/AgentProfile"
          description: Defines how the machine will be handled once it executed a job.
        fabricProfile:
          $ref: "#/components/schemas/FabricProfile"
          description: Defines the type of fabric the agent will run on.
        devCenterProjectResourceId:
          type: string
          description: The resource id of the DevCenter Project the pool belongs to.
    ResourceDetailsObjectProperties:
      type: object
      description: Details of the ResourceDetailsObject.
      properties:
        status:
          $ref: "#/components/schemas/ResourceStatus"
          description: 'The status of the resource. Required. Known values are: "Ready", "NotReady", "Allocated", "PendingReturn", "Returned", "Leased", "Provisioning", "Updating", "Starting", "PendingReimage", and "Reimaging".'
        image:
          type: string
          description: The image name of the resource. Required.
        imageVersion:
          type: string
          description: The version of the image running on the resource. Required.
      required:
        - status
        - image
        - imageVersion
    ResourceStatus:
      type: string
      enum:
        - Ready
        - NotReady
        - Allocated
        - PendingReturn
        - Returned
        - Leased
        - Provisioning
        - Updating
        - Starting
        - PendingReimage
        - Reimaging
    ResourceDetailsObjectFlattened:
      type: object
      description: Flattened view of ResourceDetailsObject ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        status:
          $ref: "#/components/schemas/ResourceStatus"
          description: 'The status of the resource. Required. Known values are: "Ready", "NotReady", "Allocated", "PendingReturn", "Returned", "Leased", "Provisioning", "Updating", "Starting", "PendingReimage", and "Reimaging".'
        image:
          type: string
          description: The image name of the resource. Required.
        imageVersion:
          type: string
          description: The version of the image running on the resource. Required.
    PagedResourceDetailsObjectFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourceDetailsObjectFlattened"
        nextLink:
          type: string
    ResourceSkuProperties:
      type: object
      description: Properties of a ResourceSku.
      properties:
        resourceType:
          type: string
          description: The type of resource the SKU applies to. Required.
        tier:
          type: string
          description: The tier of virtual machines in a scale set. Required.
        size:
          type: string
          description: The size of the SKU. Required.
        family:
          type: string
          description: The family of the SKU. Required.
        locations:
          type: array
          items:
            type: string
          description: The set of locations that the SKU is available. Required.
        locationInfo:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuLocationInfo"
          description: A list of locations and availability zones in those locations where the SKU is available. Required.
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuCapabilities"
          description: Name value pairs to describe the capability. Required.
        restrictions:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuRestrictions"
          description: The restrictions of the SKU. Required.
      required:
        - resourceType
        - tier
        - size
        - family
        - locations
        - locationInfo
        - capabilities
        - restrictions
    ResourceSkuLocationInfo:
      type: object
      description: Describes an available Compute SKU Location Information.
      properties:
        location:
          type: string
          description: Location of the SKU. Required.
        zones:
          type: array
          items:
            type: string
          description: List of availability zones where the SKU is supported. Required.
        zoneDetails:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuZoneDetails"
          description: Gets details of capabilities available to a SKU in specific zones. Required.
      required:
        - location
        - zones
        - zoneDetails
    ResourceSkuZoneDetails:
      type: object
      description: Describes The zonal capabilities of a SKU.
      properties:
        name:
          type: array
          items:
            type: string
          description: Gets the set of zones that the SKU is available in with the specified capabilities. Required.
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuCapabilities"
          description: A list of capabilities that are available for the SKU in the specified list of zones. Required.
      required:
        - name
        - capabilities
    ResourceSkuCapabilities:
      type: object
      description: Describes The SKU capabilities object.
      properties:
        name:
          type: string
          description: The name of the SKU capability. Required.
        value:
          type: string
          description: The value of the SKU capability. Required.
      required:
        - name
        - value
    ResourceSkuRestrictions:
      type: object
      description: The restrictions of the SKU.
      properties:
        type:
          $ref: "#/components/schemas/ResourceSkuRestrictionsType"
          description: 'the type of restrictions. Known values are: "Location" and "Zone".'
        values:
          type: array
          items:
            type: string
          description: The value of restrictions. If the restriction type is set to location. This would be different locations where the SKU is restricted. Required.
        restrictionInfo:
          $ref: "#/components/schemas/ResourceSkuRestrictionInfo"
          description: The information about the restriction where the SKU cannot be used. Required.
        reasonCode:
          $ref: "#/components/schemas/ResourceSkuRestrictionsReasonCode"
          description: 'the reason for restriction. Known values are: "QuotaId" and "NotAvailableForSubscription".'
      required:
        - values
        - restrictionInfo
    ResourceSkuRestrictionsType:
      type: string
      enum:
        - Location
        - Zone
    ResourceSkuRestrictionInfo:
      type: object
      description: Describes an available Compute SKU Restriction Information.
      properties:
        locations:
          type: array
          items:
            type: string
          description: Locations where the SKU is restricted.
        zones:
          type: array
          items:
            type: string
          description: List of availability zones where the SKU is restricted.
    ResourceSkuRestrictionsReasonCode:
      type: string
      enum:
        - QuotaId
        - NotAvailableForSubscription
    ResourceSkuFlattened:
      type: object
      description: Flattened view of ResourceSku ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: "Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. # pylint: disable=line-too-long"
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        resourceType:
          type: string
          description: The type of resource the SKU applies to. Required.
        tier:
          type: string
          description: The tier of virtual machines in a scale set. Required.
        size:
          type: string
          description: The size of the SKU. Required.
        family:
          type: string
          description: The family of the SKU. Required.
        locations:
          type: array
          items:
            type: string
          description: The set of locations that the SKU is available. Required.
        locationInfo:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuLocationInfo"
          description: A list of locations and availability zones in those locations where the SKU is available. Required.
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuCapabilities"
          description: Name value pairs to describe the capability. Required.
        restrictions:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuRestrictions"
          description: The restrictions of the SKU. Required.
    PagedResourceSkuFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSkuFlattened"
        nextLink:
          type: string
    Quota:
      type: object
      description: Describes Resource Quota. Readonly variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          $ref: "#/components/schemas/QuotaName"
          description: The name of the quota.
        id:
          type: string
          description: Fully qualified ARM resource id. Required.
        unit:
          type: string
          description: The unit of usage measurement. Required.
        currentValue:
          type: integer
          description: The current usage of the resource. Required.
        limit:
          type: integer
          description: The maximum permitted usage of the resource. Required.
      required:
        - id
        - unit
        - currentValue
        - limit
    QuotaName:
      type: object
      description: The Quota Names.
      properties:
        value:
          type: string
          description: The name of the resource.
        localizedValue:
          type: string
          description: The localized name of the resource.
    PagedQuota:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Quota"
        nextLink:
          type: string
  x-stackQL-resources:
    image_versions:
      id: azure.devopsinfrastructure.image_versions
      name: image_versions
      title: image_versions
      methods:
        list_by_image:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DevOpsInfrastructure~1images~1{image_name}~1versions?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/image_versions/methods/list_by_image"
        insert: []
        update: []
        delete: []
    operations:
      id: azure.devopsinfrastructure.operations
      name: operations
      title: operations
      methods:
        list_raw:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.DevOpsInfrastructure~1operations?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
    pools:
      id: azure.devopsinfrastructure.pools
      name: pools
      title: pools
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DevOpsInfrastructure~1pools~1{pool_name}?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DevOpsInfrastructure~1pools~1{pool_name}?api-version=2024-10-19/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DevOpsInfrastructure~1pools~1{pool_name}?api-version=2024-10-19/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DevOpsInfrastructure~1pools~1{pool_name}?api-version=2024-10-19/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DevOpsInfrastructure~1pools?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DevOpsInfrastructure~1pools?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/pools/methods/get"
          - $ref: "#/components/x-stackQL-resources/pools/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/pools/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/pools/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/pools/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/pools/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/pools/methods/delete"
    resource_details:
      id: azure.devopsinfrastructure.resource_details
      name: resource_details
      title: resource_details
      methods:
        list_by_pool:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DevOpsInfrastructure~1pools~1{pool_name}~1resources?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_details/methods/list_by_pool"
        insert: []
        update: []
        delete: []
    sku:
      id: azure.devopsinfrastructure.sku
      name: sku
      title: sku
      methods:
        list_by_location:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DevOpsInfrastructure~1locations~1{location_name}~1skus?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/sku/methods/list_by_location"
        insert: []
        update: []
        delete: []
    subscription_usages:
      id: azure.devopsinfrastructure.subscription_usages
      name: subscription_usages
      title: subscription_usages
      methods:
        usages:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DevOpsInfrastructure~1locations~1{location}~1usages?api-version=2024-10-19/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DevOpsInfrastructure/images/{image_name}/versions?api-version=2024-10-19:
    get:
      operationId: ImageVersions_list_by_image
      description: List ImageVersion resources by Image.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: image_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the image. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedImageVersionFlattened"
  /providers/Microsoft.DevOpsInfrastructure/operations?api-version=2024-10-19:
    get:
      operationId: devopsinfrastructure_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Paged_models"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DevOpsInfrastructure/pools/{pool_name}?api-version=2024-10-19:
    get:
      operationId: Pools_get
      description: Get a Pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the pool. It needs to be globally unique. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PoolFlattened"
    put:
      operationId: Pools_begin_create_or_update
      description: Create a Pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the pool. It needs to be globally unique. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/PoolProperties"
                  description: The resource-specific properties for this resource.
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pool"
    patch:
      operationId: Pools_begin_update
      description: Update a Pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the pool. It needs to be globally unique. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/ManagedServiceIdentity"
                  description: The managed service identities assigned to this resource.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/PoolUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pool"
    delete:
      operationId: Pools_begin_delete
      description: Delete a Pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the pool. It needs to be globally unique. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DevOpsInfrastructure/pools?api-version=2024-10-19:
    get:
      operationId: Pools_list_by_resource_group
      description: List Pool resources by resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPoolFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.DevOpsInfrastructure/pools?api-version=2024-10-19:
    get:
      operationId: Pools_list_by_subscription
      description: List Pool resources by subscription ID.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPoolFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DevOpsInfrastructure/pools/{pool_name}/resources?api-version=2024-10-19:
    get:
      operationId: ResourceDetails_list_by_pool
      description: List ResourceDetailsObject resources by Pool.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: pool_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the pool. It needs to be globally unique. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourceDetailsObjectFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.DevOpsInfrastructure/locations/{location_name}/skus?api-version=2024-10-19:
    get:
      operationId: Sku_list_by_location
      description: List ResourceSku resources by subscription ID.
      parameters:
        - name: location_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the location. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourceSkuFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.DevOpsInfrastructure/locations/{location}/usages?api-version=2024-10-19:
    get:
      operationId: SubscriptionUsages_usages
      description: List Quota resources by subscription ID.
      parameters:
        - name: location
          in: path
          required: true
          schema:
            type: string
          description: The name of the Azure region. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedQuota"
