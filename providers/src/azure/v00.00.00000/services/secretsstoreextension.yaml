openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Secrets Store Extension Mgmt Client
  description: Microsoft.SecretSyncController resource provider.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2024-08-21-preview-stackql-generated
  x-serviceAlias: secretsstoreextension
  x-sdkPackage: azure-mgmt-secretsstoreextension
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    AzureKeyVaultSecretProviderClassProperties:
      type: object
      description: The properties of the AzureKeyVaultSecretProviderClass.
      properties:
        keyvaultName:
          type: string
          description: The name of the Azure Key Vault to sync secrets from. Required.
        clientId:
          type: string
          description: The user assigned managed identity client ID that should be used to access the Azure Key Vault. Required.
        tenantId:
          type: string
          description: The Azure Active Directory tenant ID that should be used for authenticating requests to the Azure Key Vault. Required.
        objects:
          type: string
          description: Objects defines the desired state of synced K8s secret objects.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the AzureKeyVaultSecretProviderClass instance. Known values are: "Succeeded", "Failed", and "Canceled".'
      required:
        - keyvaultName
        - clientId
        - tenantId
    ProvisioningState:
      type: string
      enum:
        - Succeeded
        - Failed
        - Canceled
    ExtendedLocation:
      type: object
      description: The complex type of the extended location.
      properties:
        name:
          type: string
          description: The name of the extended location. Required.
        type:
          $ref: "#/components/schemas/ExtendedLocationType"
          description: 'The type of the extended location. Required. Known values are: "EdgeZone" and "CustomLocation".'
      required:
        - name
        - type
    ExtendedLocationType:
      type: string
      enum:
        - EdgeZone
        - CustomLocation
    AzureKeyVaultSecretProviderClassFlattened:
      type: object
      description: Flattened view of AzureKeyVaultSecretProviderClass ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: ":vartype extended_location: ~azure.mgmt.secretsstoreextension.models.ExtendedLocation"
        keyvaultName:
          type: string
          description: The name of the Azure Key Vault to sync secrets from. Required.
        clientId:
          type: string
          description: The user assigned managed identity client ID that should be used to access the Azure Key Vault. Required.
        tenantId:
          type: string
          description: The Azure Active Directory tenant ID that should be used for authenticating requests to the Azure Key Vault. Required.
        objects:
          type: string
          description: Objects defines the desired state of synced K8s secret objects.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the AzureKeyVaultSecretProviderClass instance. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedAzureKeyVaultSecretProviderClassFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AzureKeyVaultSecretProviderClassFlattened"
        nextLink:
          type: string
    AzureKeyVaultSecretProviderClass:
      type: object
      description: The AzureKeyVaultSecretProviderClass resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AzureKeyVaultSecretProviderClassProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: ":vartype extended_location: ~azure.mgmt.secretsstoreextension.models.ExtendedLocation"
      required:
        - location
    AzureKeyVaultSecretProviderClassUpdateProperties:
      type: object
      description: The updatable properties of the AzureKeyVaultSecretProviderClass.
      properties:
        keyvaultName:
          type: string
          description: The name of the Azure Key Vault to sync secrets from.
        clientId:
          type: string
          description: The user assigned managed identity client ID that should be used to access the Azure Key Vault.
        tenantId:
          type: string
          description: The Azure Active Directory tenant ID that should be used for authenticating requests to the Azure Key Vault.
        objects:
          type: string
          description: Objects defines the desired state of synced K8s secret objects.
    OperationDisplay:
      type: object
      description: Localized display information for and operation.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Origin:
      type: string
      enum:
        - user
        - system
        - user,system
    ActionType:
      type: string
      enum:
        - Internal
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure Resource Manager/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          $ref: "#/components/schemas/Origin"
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          $ref: "#/components/schemas/ActionType"
          description: Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    SecretSyncProperties:
      type: object
      description: The properties of the SecretSync instance.
      properties:
        secretProviderClassName:
          type: string
          description: SecretProviderClassName specifies the name of the SecretProviderClass resource, which contains the information needed to access the cloud provider secret store. Required.
        serviceAccountName:
          type: string
          description: ServiceAccountName specifies the name of the service account used to access the cloud provider secret store. The audience field in the service account token must be passed as parameter in the controller configuration. The audience is used when requesting a token from the API server for the service account; the supported audiences are defined by each provider. Required.
        kubernetesSecretType:
          $ref: "#/components/schemas/KubernetesSecretType"
          description: 'Type specifies the type of the Kubernetes secret object, e.g. "Opaque" or"kubernetes.io/tls". The controller must have permission to create secrets of the specified type. Required. Known values are: "Opaque" and "kubernetes.io/tls".'
        forceSynchronization:
          type: string
          description: ForceSynchronization can be used to force the secret synchronization. The secret synchronization is triggered by changing the value in this field. This field is not used to resolve synchronization conflicts.
        objectSecretMapping:
          type: array
          items:
            $ref: "#/components/schemas/KubernetesSecretObjectMapping"
          description: An array of SecretObjectData that maps secret data from the external secret provider to the Kubernetes secret. Each entry specifies the source secret in the external provider and the corresponding key in the Kubernetes secret. Required.
        status:
          $ref: "#/components/schemas/SecretSyncStatus"
          description: SecretSyncStatus defines the observed state of the secret synchronization process.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the SecretSync instance. Known values are: "Succeeded", "Failed", and "Canceled".'
      required:
        - secretProviderClassName
        - serviceAccountName
        - kubernetesSecretType
        - objectSecretMapping
    KubernetesSecretType:
      type: string
      enum:
        - Opaque
        - kubernetes.io/tls
    KubernetesSecretObjectMapping:
      type: object
      description: Properties defining the mapping between a cloud secret store object and a Kubernetes Secret.
      properties:
        sourcePath:
          type: string
          description: SourcePath is the identifier for the secret data as defined by the external secret provider. This is the key or path to the secret in the provider's system, which gets mounted to a specific path in the pod. The value should match the name of the secret as specified in the SecretProviderClass's objects array. Required.
        targetKey:
          type: string
          description: TargetKey is the key in the Kubernetes secret's data field where the secret value will be stored. This key is used to reference the secret data within Kubernetes, and it should be unique within the secret. Required.
      required:
        - sourcePath
        - targetKey
    SecretSyncStatus:
      type: object
      description: SecretSyncStatus defines the observed state of the secret synchronization process.
      properties:
        lastSuccessfulSyncTime:
          type: string
          format: date-time
          description: LastSuccessfulSyncTime represents the last time the secret was retrieved from the Provider and updated.
        conditions:
          type: array
          items:
            $ref: "#/components/schemas/SecretSyncCondition"
          description: Conditions represent the status of the secret create and update processes. The status can be True, False, or Unknown with various reasons and messages explaining the state. Examples of reasons include CreateSucceeded, ProviderError, InvalidClusterSecretLabelError, InvalidClusterSecretAnnotationError, UnknownError, ValidatingAdmissionPolicyCheckFailed, UserInputValidationFailed, ControllerSpcError, ControllerInternalError, NoValueChange, and ValueChangeOrForceUpdateDetected.
    SecretSyncCondition:
      type: object
      description: A condition represents the status of the secret create and update processes.
      properties:
        lastTransitionTime:
          type: string
          format: date-time
          description: LastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
        message:
          type: string
          description: Message is a human readable message indicating details about the transition. This may be an empty string. Required.
        observedGeneration:
          type: integer
          description: ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
        reason:
          type: string
          description: Reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty. Required.
        status:
          $ref: "#/components/schemas/StatusConditionType"
          description: 'Status of the condition, one of True, False, Unknown. Required. Known values are: "True", "False", and "Unknown".'
        type:
          type: string
          description: Type of condition in CamelCase or in foo.example.com/CamelCase. Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to de-conflict is important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt). Required.
    StatusConditionType:
      type: string
      enum:
        - "True"
        - "False"
        - Unknown
    SecretSyncFlattened:
      type: object
      description: Flattened view of SecretSync ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: ":vartype extended_location: ~azure.mgmt.secretsstoreextension.models.ExtendedLocation"
        secretProviderClassName:
          type: string
          description: SecretProviderClassName specifies the name of the SecretProviderClass resource, which contains the information needed to access the cloud provider secret store. Required.
        serviceAccountName:
          type: string
          description: ServiceAccountName specifies the name of the service account used to access the cloud provider secret store. The audience field in the service account token must be passed as parameter in the controller configuration. The audience is used when requesting a token from the API server for the service account; the supported audiences are defined by each provider. Required.
        kubernetesSecretType:
          $ref: "#/components/schemas/KubernetesSecretType"
          description: 'Type specifies the type of the Kubernetes secret object, e.g. "Opaque" or"kubernetes.io/tls". The controller must have permission to create secrets of the specified type. Required. Known values are: "Opaque" and "kubernetes.io/tls".'
        forceSynchronization:
          type: string
          description: ForceSynchronization can be used to force the secret synchronization. The secret synchronization is triggered by changing the value in this field. This field is not used to resolve synchronization conflicts.
        objectSecretMapping:
          type: array
          items:
            $ref: "#/components/schemas/KubernetesSecretObjectMapping"
          description: An array of SecretObjectData that maps secret data from the external secret provider to the Kubernetes secret. Each entry specifies the source secret in the external provider and the corresponding key in the Kubernetes secret. Required.
        status:
          $ref: "#/components/schemas/SecretSyncStatus"
          description: SecretSyncStatus defines the observed state of the secret synchronization process.
        provisioningState:
          $ref: "#/components/schemas/ProvisioningState"
          description: 'Provisioning state of the SecretSync instance. Known values are: "Succeeded", "Failed", and "Canceled".'
    PagedSecretSyncFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/SecretSyncFlattened"
        nextLink:
          type: string
    SecretSync:
      type: object
      description: The SecretSync resource.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/SecretSyncProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: ":vartype extended_location: ~azure.mgmt.secretsstoreextension.models.ExtendedLocation"
      required:
        - location
    SecretSyncUpdateProperties:
      type: object
      description: The updatable properties of the SecretSync.
      properties:
        secretProviderClassName:
          type: string
          description: SecretProviderClassName specifies the name of the SecretProviderClass resource, which contains the information needed to access the cloud provider secret store.
        serviceAccountName:
          type: string
          description: ServiceAccountName specifies the name of the service account used to access the cloud provider secret store. The audience field in the service account token must be passed as parameter in the controller configuration. The audience is used when requesting a token from the API server for the service account; the supported audiences are defined by each provider.
        kubernetesSecretType:
          $ref: "#/components/schemas/KubernetesSecretType"
          description: 'Type specifies the type of the Kubernetes secret object, e.g. "Opaque" or"kubernetes.io/tls". The controller must have permission to create secrets of the specified type. Known values are: "Opaque" and "kubernetes.io/tls".'
        forceSynchronization:
          type: string
          description: ForceSynchronization can be used to force the secret synchronization. The secret synchronization is triggered by changing the value in this field. This field is not used to resolve synchronization conflicts.
        objectSecretMapping:
          type: array
          items:
            $ref: "#/components/schemas/KubernetesSecretObjectMapping"
          description: An array of SecretObjectData that maps secret data from the external secret provider to the Kubernetes secret. Each entry specifies the source secret in the external provider and the corresponding key in the Kubernetes secret.
  x-stackQL-resources:
    azure_key_vault_secret_provider_classes:
      id: azure.secretsstoreextension.azure_key_vault_secret_provider_classes
      name: azure_key_vault_secret_provider_classes
      title: azure_key_vault_secret_provider_classes
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1azureKeyVaultSecretProviderClasses~1{azure_key_vault_secret_provider_class_name}?api-version=2024-08-21-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1azureKeyVaultSecretProviderClasses~1{azure_key_vault_secret_provider_class_name}?api-version=2024-08-21-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1azureKeyVaultSecretProviderClasses~1{azure_key_vault_secret_provider_class_name}?api-version=2024-08-21-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1azureKeyVaultSecretProviderClasses~1{azure_key_vault_secret_provider_class_name}?api-version=2024-08-21-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1azureKeyVaultSecretProviderClasses?api-version=2024-08-21-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.SecretSyncController~1azureKeyVaultSecretProviderClasses?api-version=2024-08-21-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/azure_key_vault_secret_provider_classes/methods/get"
          - $ref: "#/components/x-stackQL-resources/azure_key_vault_secret_provider_classes/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/azure_key_vault_secret_provider_classes/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/azure_key_vault_secret_provider_classes/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/azure_key_vault_secret_provider_classes/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/azure_key_vault_secret_provider_classes/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/azure_key_vault_secret_provider_classes/methods/delete"
    operations:
      id: azure.secretsstoreextension.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.SecretSyncController~1operations?api-version=2024-08-21-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    secret_syncs:
      id: azure.secretsstoreextension.secret_syncs
      name: secret_syncs
      title: secret_syncs
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1secretSyncs~1{secret_sync_name}?api-version=2024-08-21-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1secretSyncs~1{secret_sync_name}?api-version=2024-08-21-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1secretSyncs~1{secret_sync_name}?api-version=2024-08-21-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1secretSyncs~1{secret_sync_name}?api-version=2024-08-21-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.SecretSyncController~1secretSyncs?api-version=2024-08-21-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.SecretSyncController~1secretSyncs?api-version=2024-08-21-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/secret_syncs/methods/get"
          - $ref: "#/components/x-stackQL-resources/secret_syncs/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/secret_syncs/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/secret_syncs/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/secret_syncs/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/secret_syncs/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/secret_syncs/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.SecretSyncController/azureKeyVaultSecretProviderClasses/{azure_key_vault_secret_provider_class_name}?api-version=2024-08-21-preview:
    get:
      operationId: AzureKeyVaultSecretProviderClasses_get
      description: Gets the properties of an AzureKeyVaultSecretProviderClass instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_key_vault_secret_provider_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AzureKeyVaultSecretProviderClass. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureKeyVaultSecretProviderClassFlattened"
    put:
      operationId: AzureKeyVaultSecretProviderClasses_begin_create_or_update
      description: Creates, or updates, an AzureKeyVaultSecretProviderClass instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_key_vault_secret_provider_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AzureKeyVaultSecretProviderClass. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AzureKeyVaultSecretProviderClassProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: ":vartype extended_location: ~azure.mgmt.secretsstoreextension.models.ExtendedLocation"
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureKeyVaultSecretProviderClass"
    patch:
      operationId: AzureKeyVaultSecretProviderClasses_begin_update
      description: Updates an AzureKeyVaultSecretProviderClass instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_key_vault_secret_provider_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AzureKeyVaultSecretProviderClass. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/AzureKeyVaultSecretProviderClassUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AzureKeyVaultSecretProviderClass"
    delete:
      operationId: AzureKeyVaultSecretProviderClasses_begin_delete
      description: Deletes an AzureKeyVaultSecretProviderClass instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: azure_key_vault_secret_provider_class_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the AzureKeyVaultSecretProviderClass. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.SecretSyncController/azureKeyVaultSecretProviderClasses?api-version=2024-08-21-preview:
    get:
      operationId: AzureKeyVaultSecretProviderClasses_list_by_resource_group
      description: Lists the AzureKeyVaultSecretProviderClass instances within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureKeyVaultSecretProviderClassFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.SecretSyncController/azureKeyVaultSecretProviderClasses?api-version=2024-08-21-preview:
    get:
      operationId: AzureKeyVaultSecretProviderClasses_list_by_subscription
      description: Lists the AzureKeyVaultSecretProviderClass instances within an Azure subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAzureKeyVaultSecretProviderClassFlattened"
  /providers/Microsoft.SecretSyncController/operations?api-version=2024-08-21-preview:
    get:
      operationId: secretsstoreextension_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.SecretSyncController/secretSyncs/{secret_sync_name}?api-version=2024-08-21-preview:
    get:
      operationId: SecretSyncs_get
      description: Gets the properties of a SecretSync instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: secret_sync_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the SecretSync. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretSyncFlattened"
    put:
      operationId: SecretSyncs_begin_create_or_update
      description: Creates new or updates a SecretSync instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: secret_sync_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the SecretSync. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/SecretSyncProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: ":vartype extended_location: ~azure.mgmt.secretsstoreextension.models.ExtendedLocation"
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretSync"
    patch:
      operationId: SecretSyncs_begin_update
      description: Updates a SecretSync instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: secret_sync_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the SecretSync. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  $ref: "#/components/schemas/SecretSyncUpdateProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretSync"
    delete:
      operationId: SecretSyncs_begin_delete
      description: Deletes a SecretSync instance.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: secret_sync_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the SecretSync. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.SecretSyncController/secretSyncs?api-version=2024-08-21-preview:
    get:
      operationId: SecretSyncs_list_by_resource_group
      description: Lists the SecretSync instances within a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSecretSyncFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.SecretSyncController/secretSyncs?api-version=2024-08-21-preview:
    get:
      operationId: SecretSyncs_list_by_subscription
      description: Lists the SecretSync instances within an Azure subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedSecretSyncFlattened"
