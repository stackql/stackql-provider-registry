openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Azure Cosmos DB for PostgreSQL
  description: Distributed PostgreSQL clusters powered by Citus.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-03-02-preview-stackql-generated
  x-serviceAlias: cosmosdb_for_postgresql
  x-sdkPackage: azure-mgmt-cosmosdbforpostgresql
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    MaintenanceWindow:
      type: object
      description: Schedule settings for regular cluster updates.
      properties:
        customWindow:
          type: string
          description: Indicates whether custom maintenance window is enabled or not.
        startHour:
          type: integer
          description: Start hour within preferred day of the week for maintenance window.
        startMinute:
          type: integer
          description: Start minute within the start hour for maintenance window.
        dayOfWeek:
          type: integer
          description: Preferred day of the week for maintenance window.
    ServerNameItem:
      type: object
      description: The name object for a server. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The name of a server.
        fullyQualifiedDomainName:
          type: string
          description: The fully qualified domain name of a server.
    SimplePrivateEndpointConnection:
      type: object
      description: A private endpoint connection. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            privateEndpoint:
              $ref: "#/components/schemas/PrivateEndpointProperty"
              description: Private endpoint which the connection belongs to.
            groupIds:
              type: array
              items:
                type: string
              description: Group ids of the private endpoint connection.
            privateLinkServiceConnectionState:
              $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
              description: A collection of information about the state of the connection between service consumer and provider.
    PrivateEndpointProperty:
      type: object
      description: Property to represent resource id of the private endpoint.
      properties:
        id:
          type: string
          description: Resource id of the private endpoint.
    PrivateLinkServiceConnectionState:
      type: object
      description: A collection of information about the state of the connection between service consumer and provider.
      properties:
        status:
          type: string
          description: 'Indicates whether the connection has been Approved/Rejected/Removed by the owner of the service. Known values are: "Pending", "Approved", and "Rejected".'
        description:
          type: string
          description: The reason for approval/rejection of the connection.
        actionsRequired:
          type: string
          description: A message indicating if changes on the service provider require any updates on the consumer.
    AuthConfig:
      type: object
      description: Authentication configuration of a cluster.
      properties:
        activeDirectoryAuth:
          type: string
          description: 'Known values are: "enabled" and "disabled".'
        passwordAuth:
          type: string
          description: 'Known values are: "enabled" and "disabled".'
    ClusterFlattened:
      type: object
      description: Flattened view of Cluster ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        administratorLogin:
          type: string
          description: The administrator's login name of the servers in the cluster.
        administratorLoginPassword:
          type: string
          description: The password of the administrator login. Required for creation.
        provisioningState:
          type: string
          description: Provisioning state of the cluster.
        state:
          type: string
          description: A state of a cluster/server that is visible to user.
        postgresqlVersion:
          type: string
          description: The major PostgreSQL version on all cluster servers.
        citusVersion:
          type: string
          description: The Citus extension version on all cluster servers.
        maintenanceWindow:
          $ref: "#/components/schemas/MaintenanceWindow"
          description: Maintenance window of a cluster.
        preferredPrimaryZone:
          type: string
          description: Preferred primary availability zone (AZ) for all cluster servers.
        enableShardsOnCoordinator:
          type: boolean
          description: If distributed tables are placed on coordinator or not. Should be set to 'true' on single node clusters. Requires shard rebalancing after value is changed.
        enableHa:
          type: boolean
          description: If high availability (HA) is enabled or not for the cluster.
        coordinatorServerEdition:
          type: string
          description: "The edition of a coordinator server (default: GeneralPurpose). Required for creation."
        coordinatorStorageQuotaInMb:
          type: integer
          description: The storage of a server in MB. Required for creation. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information.
        coordinatorVCores:
          type: integer
          description: "The vCores count of a server (max: 96). Required for creation. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information."
        coordinatorEnablePublicIpAccess:
          type: boolean
          description: If public access is enabled on coordinator.
        nodeServerEdition:
          type: string
          description: "The edition of a node server (default: MemoryOptimized)."
        nodeCount:
          type: integer
          description: Worker node count of the cluster. When node count is 0, it represents a single node configuration with the ability to create distributed tables on that node. 2 or more worker nodes represent multi-node configuration. Node count value cannot be 1. Required for creation.
        nodeStorageQuotaInMb:
          type: integer
          description: The storage in MB on each worker node. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information.
        nodeVCores:
          type: integer
          description: "The compute in vCores on each worker node (max: 104). See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information."
        nodeEnablePublicIpAccess:
          type: boolean
          description: If public access is enabled on worker nodes.
        serverNames:
          type: array
          items:
            $ref: "#/components/schemas/ServerNameItem"
          description: The list of server names in the cluster.
        sourceResourceId:
          type: string
          description: The resource id of source cluster for read replica clusters.
        sourceLocation:
          type: string
          description: The Azure region of source cluster for read replica clusters.
        pointInTimeUTC:
          type: string
          format: date-time
          description: Date and time in UTC (ISO8601 format) for cluster restore.
        readReplicas:
          type: array
          items:
            type: string
          description: The array of read replica clusters.
        earliestRestoreTime:
          type: string
          format: date-time
          description: The earliest restore point time (ISO8601 format) for the cluster.
        privateEndpointConnections:
          type: array
          items:
            $ref: "#/components/schemas/SimplePrivateEndpointConnection"
          description: The private endpoint connections for a cluster.
        databaseName:
          type: string
          description: The database name of the cluster. Only one database per cluster is supported.
        enableGeoBackup:
          type: boolean
          description: If cluster backup is stored in another Azure region in addition to the copy of the backup stored in the cluster's region. Enabled only at the time of cluster creation.
        authConfig:
          $ref: "#/components/schemas/AuthConfig"
          description: Authentication configuration of a cluster.
    PagedClusterFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ClusterFlattened"
        nextLink:
          type: string
    Cluster:
      type: object
      description: Represents a cluster. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          type: object
          properties:
            administratorLogin:
              type: string
              description: The administrator's login name of the servers in the cluster.
            administratorLoginPassword:
              type: string
              description: The password of the administrator login. Required for creation.
            provisioningState:
              type: string
              description: Provisioning state of the cluster.
            state:
              type: string
              description: A state of a cluster/server that is visible to user.
            postgresqlVersion:
              type: string
              description: The major PostgreSQL version on all cluster servers.
            citusVersion:
              type: string
              description: The Citus extension version on all cluster servers.
            maintenanceWindow:
              $ref: "#/components/schemas/MaintenanceWindow"
              description: Maintenance window of a cluster.
            preferredPrimaryZone:
              type: string
              description: Preferred primary availability zone (AZ) for all cluster servers.
            enableShardsOnCoordinator:
              type: boolean
              description: If distributed tables are placed on coordinator or not. Should be set to 'true' on single node clusters. Requires shard rebalancing after value is changed.
            enableHa:
              type: boolean
              description: If high availability (HA) is enabled or not for the cluster.
            coordinatorServerEdition:
              type: string
              description: "The edition of a coordinator server (default: GeneralPurpose). Required for creation."
            coordinatorStorageQuotaInMb:
              type: integer
              description: The storage of a server in MB. Required for creation. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information.
            coordinatorVCores:
              type: integer
              description: "The vCores count of a server (max: 96). Required for creation. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information."
            coordinatorEnablePublicIpAccess:
              type: boolean
              description: If public access is enabled on coordinator.
            nodeServerEdition:
              type: string
              description: "The edition of a node server (default: MemoryOptimized)."
            nodeCount:
              type: integer
              description: Worker node count of the cluster. When node count is 0, it represents a single node configuration with the ability to create distributed tables on that node. 2 or more worker nodes represent multi-node configuration. Node count value cannot be 1. Required for creation.
            nodeStorageQuotaInMb:
              type: integer
              description: The storage in MB on each worker node. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information.
            nodeVCores:
              type: integer
              description: "The compute in vCores on each worker node (max: 104). See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information."
            nodeEnablePublicIpAccess:
              type: boolean
              description: If public access is enabled on worker nodes.
            serverNames:
              type: array
              items:
                $ref: "#/components/schemas/ServerNameItem"
              description: The list of server names in the cluster.
            sourceResourceId:
              type: string
              description: The resource id of source cluster for read replica clusters.
            sourceLocation:
              type: string
              description: The Azure region of source cluster for read replica clusters.
            pointInTimeUTC:
              type: string
              format: date-time
              description: Date and time in UTC (ISO8601 format) for cluster restore.
            readReplicas:
              type: array
              items:
                type: string
              description: The array of read replica clusters.
            earliestRestoreTime:
              type: string
              format: date-time
              description: The earliest restore point time (ISO8601 format) for the cluster.
            privateEndpointConnections:
              type: array
              items:
                $ref: "#/components/schemas/SimplePrivateEndpointConnection"
              description: The private endpoint connections for a cluster.
            databaseName:
              type: string
              description: The database name of the cluster. Only one database per cluster is supported.
            enableGeoBackup:
              type: boolean
              description: If cluster backup is stored in another Azure region in addition to the copy of the backup stored in the cluster's region. Enabled only at the time of cluster creation.
            authConfig:
              $ref: "#/components/schemas/AuthConfig"
              description: Authentication configuration of a cluster.
      required:
        - location
    NameAvailability:
      type: object
      description: Represents cluster name availability.
      properties:
        message:
          type: string
          description: Error message.
        nameAvailable:
          type: boolean
          description: Indicates whether the cluster name is available.
        name:
          type: string
          description: Name of the cluster.
        type:
          type: string
          description: Type of the cluster.
    ServerRoleGroupConfiguration:
      type: object
      description: Represents server role group configuration value. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        role:
          type: string
          description: 'The role of servers in the server role group. Required. Known values are: "Coordinator" and "Worker".'
        value:
          type: string
          description: Value of the configuration. Required.
        defaultValue:
          type: string
          description: Default value of the configuration.
        source:
          type: string
          description: Source of the configuration.
      required:
        - role
        - value
    ConfigurationFlattened:
      type: object
      description: Flattened view of Configuration ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        description:
          type: string
          description: Description of the configuration.
        dataType:
          type: string
          description: 'Data type of the configuration. Known values are: "Boolean", "Numeric", "Integer", and "Enumeration".'
        allowedValues:
          type: string
          description: Allowed values of the configuration.
        requiresRestart:
          type: boolean
          description: If configuration change requires restart.
        serverRoleGroupConfigurations:
          type: array
          items:
            $ref: "#/components/schemas/ServerRoleGroupConfiguration"
          description: The list of server role group configuration values.
        provisioningState:
          type: string
          description: 'Provisioning state of the configuration. Known values are: "Succeeded", "Canceled", "InProgress", and "Failed".'
    ServerConfigurationFlattened:
      type: object
      description: Flattened view of ServerConfiguration ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        value:
          type: string
          description: Value of the configuration.
        source:
          type: string
          description: Source of the configuration.
        description:
          type: string
          description: Description of the configuration.
        defaultValue:
          type: string
          description: Default value of the configuration.
        dataType:
          type: string
          description: 'Data type of the configuration. Known values are: "Boolean", "Numeric", "Integer", and "Enumeration".'
        allowedValues:
          type: string
          description: Allowed values of the configuration.
        requiresRestart:
          type: boolean
          description: If configuration change requires restart.
        provisioningState:
          type: string
          description: 'Provisioning state of the configuration. Known values are: "Succeeded", "Canceled", "InProgress", and "Failed".'
    PagedServerConfigurationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ServerConfigurationFlattened"
        nextLink:
          type: string
    PagedConfigurationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ConfigurationFlattened"
        nextLink:
          type: string
    ServerConfiguration:
      type: object
      description: Represents a configuration. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            value:
              type: string
              description: Value of the configuration.
            source:
              type: string
              description: Source of the configuration.
            description:
              type: string
              description: Description of the configuration.
            defaultValue:
              type: string
              description: Default value of the configuration.
            dataType:
              type: string
              description: 'Data type of the configuration. Known values are: "Boolean", "Numeric", "Integer", and "Enumeration".'
            allowedValues:
              type: string
              description: Allowed values of the configuration.
            requiresRestart:
              type: boolean
              description: If configuration change requires restart.
            provisioningState:
              type: string
              description: 'Provisioning state of the configuration. Known values are: "Succeeded", "Canceled", "InProgress", and "Failed".'
    FirewallRuleFlattened:
      type: object
      description: Flattened view of FirewallRule ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        startIpAddress:
          type: string
          description: The start IP address of the cluster firewall rule. Must be IPv4 format. Required.
        endIpAddress:
          type: string
          description: The end IP address of the cluster firewall rule. Must be IPv4 format. Required.
        provisioningState:
          type: string
          description: 'Provisioning state of the firewall rule. Known values are: "Succeeded", "Canceled", "InProgress", and "Failed".'
    PagedFirewallRuleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/FirewallRuleFlattened"
        nextLink:
          type: string
    FirewallRule:
      type: object
      description: Represents a cluster firewall rule. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to Azure.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            startIpAddress:
              type: string
              description: The start IP address of the cluster firewall rule. Must be IPv4 format. Required.
            endIpAddress:
              type: string
              description: The end IP address of the cluster firewall rule. Must be IPv4 format. Required.
            provisioningState:
              type: string
              description: 'Provisioning state of the firewall rule. Known values are: "Succeeded", "Canceled", "InProgress", and "Failed".'
      required:
        - properties
    OperationDisplay:
      type: object
      description: Display metadata associated with the operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: Operation resource provider name.
        resource:
          type: string
          description: Resource on which the operation is performed.
        operation:
          type: string
          description: Localized friendly name for the operation.
        description:
          type: string
          description: Operation description.
    Operation:
      type: object
      description: REST API operation definition. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: The name of the operation being performed on this particular object.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: The localized display information for this particular operation or action.
        isDataAction:
          type: boolean
          description: Indicates whether the operation is a data action.
        origin:
          type: string
          description: 'The intended executor of the operation. Known values are: "NotSpecified", "user", and "system".'
        properties:
          type: object
          properties: {}
          description: Additional descriptions for the operation.
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    PrivateEndpoint:
      type: object
      description: The private endpoint resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: The ARM identifier for private endpoint.
    PrivateEndpointConnectionFlattened:
      type: object
      description: Flattened view of PrivateEndpointConnection ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        groupIds:
          type: array
          items:
            type: string
          description: The group ids for the private endpoint resource.
        privateEndpoint:
          $ref: "#/components/schemas/PrivateEndpoint"
          description: The private endpoint resource.
        privateLinkServiceConnectionState:
          $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
          description: A collection of information about the state of the connection between service consumer and provider.
        provisioningState:
          type: string
          description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
    PagedPrivateEndpointConnectionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
        nextLink:
          type: string
    PrivateEndpointConnection:
      type: object
      description: The private endpoint connection resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            groupIds:
              type: array
              items:
                type: string
              description: The group ids for the private endpoint resource.
            privateEndpoint:
              $ref: "#/components/schemas/PrivateEndpoint"
              description: The private endpoint resource.
            privateLinkServiceConnectionState:
              $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
              description: A collection of information about the state of the connection between service consumer and provider.
            provisioningState:
              type: string
              description: 'The provisioning state of the private endpoint connection resource. Known values are: "Succeeded", "Creating", "Deleting", and "Failed".'
    PrivateLinkResourceFlattened:
      type: object
      description: Flattened view of PrivateLinkResource ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        groupId:
          type: string
          description: The private link resource group id.
        requiredMembers:
          type: array
          items:
            type: string
          description: The private link resource required member names.
        requiredZoneNames:
          type: array
          items:
            type: string
          description: The private link resource private link DNS zone name.
    PagedPrivateLinkResourceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/PrivateLinkResourceFlattened"
        nextLink:
          type: string
    RoleFlattened:
      type: object
      description: Flattened view of Role ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        roleType:
          type: string
          description: 'Known values are: "user" and "admin".'
        password:
          type: string
          description: The password of the cluster role. If an identity is used, password will not be required.
        provisioningState:
          type: string
          description: 'Provisioning state of the role. Known values are: "Succeeded", "Canceled", "InProgress", and "Failed".'
        externalIdentity:
          type: object
          properties:
            objectId:
              type: string
              description: ":vartype object_id: str"
            principalType:
              type: string
              description: 'Known values are: "user", "servicePrincipal", and "group".'
            tenantId:
              type: string
              description: ":vartype tenant_id: str"
    PagedRoleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/RoleFlattened"
        nextLink:
          type: string
    Role:
      type: object
      description: Represents a cluster role. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          type: object
          properties:
            roleType:
              type: string
              description: 'Known values are: "user" and "admin".'
            password:
              type: string
              description: The password of the cluster role. If an identity is used, password will not be required.
            provisioningState:
              type: string
              description: 'Provisioning state of the role. Known values are: "Succeeded", "Canceled", "InProgress", and "Failed".'
            externalIdentity:
              type: object
              properties:
                objectId:
                  type: string
                  description: ":vartype object_id: str"
                principalType:
                  type: string
                  description: 'Known values are: "user", "servicePrincipal", and "group".'
                tenantId:
                  type: string
                  description: ":vartype tenant_id: str"
    ClusterServerFlattened:
      type: object
      description: Flattened view of ClusterServer ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}".
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        serverEdition:
          type: string
          description: The edition of a server.
        storageQuotaInMb:
          type: integer
          description: The storage of a server in MB.
        vCores:
          type: integer
          description: The vCores count of a server.
        enableHa:
          type: boolean
          description: If high availability (HA) is enabled or not for the server.
        enablePublicIpAccess:
          type: boolean
          description: If public access is enabled on server.
        isReadOnly:
          type: boolean
          description: If server database is set to read-only by system maintenance depending on high disk space usage.
        administratorLogin:
          type: string
          description: The administrator's login name of the servers in the cluster.
        fullyQualifiedDomainName:
          type: string
          description: The fully qualified domain name of a server.
        role:
          type: string
          description: 'The role of server in the cluster. Known values are: "Coordinator" and "Worker".'
        state:
          type: string
          description: A state of a cluster/server that is visible to user.
        haState:
          type: string
          description: A state of HA feature for the cluster.
        availabilityZone:
          type: string
          description: Availability Zone information of the server.
        postgresqlVersion:
          type: string
          description: The major PostgreSQL version of server.
        citusVersion:
          type: string
          description: The Citus extension version of server.
    PagedClusterServerFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ClusterServerFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    clusters:
      id: azure.cosmosdb_for_postgresql.clusters
      name: clusters
      title: clusters
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}?api-version=2023-03-02-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}?api-version=2023-03-02-preview/delete"
          response:
            openAPIDocKey: "204"
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}?api-version=2023-03-02-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        restart:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1restart?api-version=2023-03-02-preview/post"
          response:
            openAPIDocKey: "204"
        start:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1start?api-version=2023-03-02-preview/post"
          response:
            openAPIDocKey: "204"
        stop:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1stop?api-version=2023-03-02-preview/post"
          response:
            openAPIDocKey: "204"
        promote_read_replica:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1promote?api-version=2023-03-02-preview/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        check_name_availability:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.DBforPostgreSQL~1checkNameAvailability?api-version=2023-03-02-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/get"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/check_name_availability"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/clusters/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/clusters/methods/delete"
    configurations:
      id: azure.cosmosdb_for_postgresql.configurations
      name: configurations
      title: configurations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1configurations~1{configuration_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_server:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1servers~1{server_name}~1configurations?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1configurations?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        get_coordinator:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1coordinatorConfigurations~1{configuration_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update_on_coordinator:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1coordinatorConfigurations~1{configuration_name}?api-version=2023-03-02-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        get_node:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1nodeConfigurations~1{configuration_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        update_on_node:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1nodeConfigurations~1{configuration_name}?api-version=2023-03-02-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/configurations/methods/get"
          - $ref: "#/components/x-stackQL-resources/configurations/methods/list_by_server"
          - $ref: "#/components/x-stackQL-resources/configurations/methods/list_by_cluster"
        insert: []
        update: []
        delete: []
    firewall_rules:
      id: azure.cosmosdb_for_postgresql.firewall_rules
      name: firewall_rules
      title: firewall_rules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1firewallRules~1{firewall_rule_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1firewallRules~1{firewall_rule_name}?api-version=2023-03-02-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1firewallRules~1{firewall_rule_name}?api-version=2023-03-02-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1firewallRules?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/get"
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/list_by_cluster"
        insert:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/firewall_rules/methods/delete"
    operations:
      id: azure.cosmosdb_for_postgresql.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.DBforPostgreSQL~1operations?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    private_endpoint_connections:
      id: azure.cosmosdb_for_postgresql.private_endpoint_connections
      name: private_endpoint_connections
      title: private_endpoint_connections
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2023-03-02-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1privateEndpointConnections~1{private_endpoint_connection_name}?api-version=2023-03-02-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1privateEndpointConnections?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/list_by_cluster"
        insert:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/create_or_update"
        update: []
        replace:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/private_endpoint_connections/methods/delete"
    private_link_resources:
      id: azure.cosmosdb_for_postgresql.private_link_resources
      name: private_link_resources
      title: private_link_resources
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1privateLinkResources~1{private_link_resource_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1privateLinkResources?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/private_link_resources/methods/get"
          - $ref: "#/components/x-stackQL-resources/private_link_resources/methods/list_by_cluster"
        insert: []
        update: []
        delete: []
    roles:
      id: azure.cosmosdb_for_postgresql.roles
      name: roles
      title: roles
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1roles~1{role_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1roles~1{role_name}?api-version=2023-03-02-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1roles~1{role_name}?api-version=2023-03-02-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1roles?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/roles/methods/get"
          - $ref: "#/components/x-stackQL-resources/roles/methods/list_by_cluster"
        insert:
          - $ref: "#/components/x-stackQL-resources/roles/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/roles/methods/delete"
    servers:
      id: azure.cosmosdb_for_postgresql.servers
      name: servers
      title: servers
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1servers~1{server_name}?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_cluster:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.DBforPostgreSQL~1serverGroupsv2~1{cluster_name}~1servers?api-version=2023-03-02-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/servers/methods/get"
          - $ref: "#/components/x-stackQL-resources/servers/methods/list_by_cluster"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}?api-version=2023-03-02-preview:
    get:
      operationId: Clusters_get
      description: Gets information about a cluster such as compute and storage configuration and cluster lifecycle metadata such as cluster creation date and time.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClusterFlattened"
    put:
      operationId: Clusters_begin_create
      description: Creates a new cluster with servers.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  type: object
                  properties:
                    administratorLoginPassword:
                      type: string
                      description: The password of the administrator login. Required for creation.
                    postgresqlVersion:
                      type: string
                      description: The major PostgreSQL version on all cluster servers.
                    citusVersion:
                      type: string
                      description: The Citus extension version on all cluster servers.
                    maintenanceWindow:
                      $ref: "#/components/schemas/MaintenanceWindow"
                      description: Maintenance window of a cluster.
                    preferredPrimaryZone:
                      type: string
                      description: Preferred primary availability zone (AZ) for all cluster servers.
                    enableShardsOnCoordinator:
                      type: boolean
                      description: If distributed tables are placed on coordinator or not. Should be set to 'true' on single node clusters. Requires shard rebalancing after value is changed.
                    enableHa:
                      type: boolean
                      description: If high availability (HA) is enabled or not for the cluster.
                    coordinatorServerEdition:
                      type: string
                      description: "The edition of a coordinator server (default: GeneralPurpose). Required for creation."
                    coordinatorStorageQuotaInMb:
                      type: integer
                      description: The storage of a server in MB. Required for creation. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information.
                    coordinatorVCores:
                      type: integer
                      description: "The vCores count of a server (max: 96). Required for creation. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information."
                    coordinatorEnablePublicIpAccess:
                      type: boolean
                      description: If public access is enabled on coordinator.
                    nodeServerEdition:
                      type: string
                      description: "The edition of a node server (default: MemoryOptimized)."
                    nodeCount:
                      type: integer
                      description: Worker node count of the cluster. When node count is 0, it represents a single node configuration with the ability to create distributed tables on that node. 2 or more worker nodes represent multi-node configuration. Node count value cannot be 1. Required for creation.
                    nodeStorageQuotaInMb:
                      type: integer
                      description: The storage in MB on each worker node. See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information.
                    nodeVCores:
                      type: integer
                      description: "The compute in vCores on each worker node (max: 104). See https://learn.microsoft.com/azure/cosmos-db/postgresql/resources-compute for more information."
                    nodeEnablePublicIpAccess:
                      type: boolean
                      description: If public access is enabled on worker nodes.
                    sourceResourceId:
                      type: string
                      description: The resource id of source cluster for read replica clusters.
                    sourceLocation:
                      type: string
                      description: The Azure region of source cluster for read replica clusters.
                    pointInTimeUTC:
                      type: string
                      format: date-time
                      description: Date and time in UTC (ISO8601 format) for cluster restore.
                    databaseName:
                      type: string
                      description: The database name of the cluster. Only one database per cluster is supported.
                    enableGeoBackup:
                      type: boolean
                      description: If cluster backup is stored in another Azure region in addition to the copy of the backup stored in the cluster's region. Enabled only at the time of cluster creation.
                    authConfig:
                      $ref: "#/components/schemas/AuthConfig"
                      description: Authentication configuration of a cluster.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cluster"
    delete:
      operationId: Clusters_begin_delete
      description: Deletes a cluster together with servers in it.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
    patch:
      operationId: Clusters_begin_update
      description: Updates an existing cluster. The request body can contain one or several properties from the cluster definition.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Application-specific metadata in the form of key-value pairs.
                properties:
                  type: object
                  properties:
                    administratorLoginPassword:
                      type: string
                      description: The password of the administrator login. Each cluster is created with pre-defined administrative role called ‘citus’.
                    postgresqlVersion:
                      type: string
                      description: The major PostgreSQL version on all cluster servers.
                    citusVersion:
                      type: string
                      description: The Citus extension version on all cluster servers.
                    enableShardsOnCoordinator:
                      type: boolean
                      description: If distributed tables are placed on coordinator or not. Should be set to 'true' on single node clusters. Requires shard rebalancing after value is changed.
                    enableHa:
                      type: boolean
                      description: If high availability (HA) is enabled or not for the cluster.
                    preferredPrimaryZone:
                      type: string
                      description: Preferred primary availability zone (AZ) for all cluster servers.
                    coordinatorServerEdition:
                      type: string
                      description: "The edition of the coordinator (default: GeneralPurpose)."
                    coordinatorStorageQuotaInMb:
                      type: integer
                      description: The storage of the coordinator in MB.
                    coordinatorVCores:
                      type: integer
                      description: "The vCores count of the coordinator (max: 96)."
                    coordinatorEnablePublicIpAccess:
                      type: boolean
                      description: If public access is enabled on coordinator.
                    nodeServerEdition:
                      type: string
                      description: "The edition of a node (default: MemoryOptimized)."
                    nodeCount:
                      type: integer
                      description: Worker node count of the cluster. When node count is 0, it represents a single node configuration with the ability to create distributed tables on that node. 2 or more worker nodes represent multi-node configuration. Node count value cannot be 1.
                    nodeStorageQuotaInMb:
                      type: integer
                      description: The storage in MB on each worker node.
                    nodeVCores:
                      type: integer
                      description: "The compute in vCores on each worker node (max: 104)."
                    maintenanceWindow:
                      $ref: "#/components/schemas/MaintenanceWindow"
                      description: Maintenance window of a cluster.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cluster"
  /subscriptions/{subscription_id}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2?api-version=2023-03-02-preview:
    get:
      operationId: Clusters_list
      description: Lists all clusters in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2?api-version=2023-03-02-preview:
    get:
      operationId: Clusters_list_by_resource_group
      description: Lists all clusters in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/restart?api-version=2023-03-02-preview:
    post:
      operationId: Clusters_begin_restart
      description: Restarts all nodes in the cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/start?api-version=2023-03-02-preview:
    post:
      operationId: Clusters_begin_start
      description: Starts stopped compute on all cluster nodes.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/stop?api-version=2023-03-02-preview:
    post:
      operationId: Clusters_begin_stop
      description: Stops compute on all cluster nodes.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/promote?api-version=2023-03-02-preview:
    post:
      operationId: Clusters_begin_promote_read_replica
      description: Promotes read replica cluster to an independent read-write cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enableGeoBackup:
                  type: boolean
                  description: Cluster name to verify.
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.DBforPostgreSQL/checkNameAvailability?api-version=2023-03-02-preview:
    post:
      operationId: Clusters_check_name_availability
      description: Checks availability of a cluster name. Cluster names should be globally unique; at least 3 characters and at most 40 characters long; they must only contain lowercase letters, numbers, and hyphens; and must not start or end with a hyphen.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Cluster name to verify. Required.
                type:
                  type: string
                  description: Resource type used for verification. Required. Default value is "Microsoft.DBforPostgreSQL/serverGroupsv2".
              required:
                - name
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NameAvailability"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/configurations/{configuration_name}?api-version=2023-03-02-preview:
    get:
      operationId: Configurations_get
      description: Gets information of a configuration for coordinator and nodes.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigurationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/servers/{server_name}/configurations?api-version=2023-03-02-preview:
    get:
      operationId: Configurations_list_by_server
      description: List all the configurations of a server in cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: server_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the server. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedServerConfigurationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/configurations?api-version=2023-03-02-preview:
    get:
      operationId: Configurations_list_by_cluster
      description: List all the configurations of a cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedConfigurationFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/coordinatorConfigurations/{configuration_name}?api-version=2023-03-02-preview:
    get:
      operationId: Configurations_get_coordinator
      description: Gets information of a configuration for coordinator.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerConfigurationFlattened"
    put:
      operationId: Configurations_begin_update_on_coordinator
      description: Updates configuration of coordinator in a cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    value:
                      type: string
                      description: Value of the configuration.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerConfiguration"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/nodeConfigurations/{configuration_name}?api-version=2023-03-02-preview:
    get:
      operationId: Configurations_get_node
      description: Gets information of a configuration for worker nodes.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerConfigurationFlattened"
    put:
      operationId: Configurations_begin_update_on_node
      description: Updates configuration of worker nodes in a cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: configuration_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster configuration. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    value:
                      type: string
                      description: Value of the configuration.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerConfiguration"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/firewallRules/{firewall_rule_name}?api-version=2023-03-02-preview:
    get:
      operationId: FirewallRules_get
      description: Gets information about a cluster firewall rule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: firewall_rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FirewallRuleFlattened"
    put:
      operationId: FirewallRules_begin_create_or_update
      description: Creates a new cluster firewall rule or updates an existing cluster firewall rule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: firewall_rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    startIpAddress:
                      type: string
                      description: The start IP address of the cluster firewall rule. Must be IPv4 format. Required.
                    endIpAddress:
                      type: string
                      description: The end IP address of the cluster firewall rule. Must be IPv4 format. Required.
              required:
                - properties
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FirewallRule"
    delete:
      operationId: FirewallRules_begin_delete
      description: Deletes a cluster firewall rule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: firewall_rule_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster firewall rule. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/firewallRules?api-version=2023-03-02-preview:
    get:
      operationId: FirewallRules_list_by_cluster
      description: Lists all the firewall rules on cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedFirewallRuleFlattened"
  /providers/Microsoft.DBforPostgreSQL/operations?api-version=2023-03-02-preview:
    get:
      operationId: cosmosdbforpostgresql_list
      description: Lists all of the available REST API operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/privateEndpointConnections/{private_endpoint_connection_name}?api-version=2023-03-02-preview:
    get:
      operationId: PrivateEndpointConnections_get
      description: Gets private endpoint connection.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnectionFlattened"
    put:
      operationId: PrivateEndpointConnections_begin_create_or_update
      description: Approves or Rejects a private endpoint connection with a given name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    privateEndpoint:
                      $ref: "#/components/schemas/PrivateEndpoint"
                      description: The private endpoint resource.
                    privateLinkServiceConnectionState:
                      $ref: "#/components/schemas/PrivateLinkServiceConnectionState"
                      description: A collection of information about the state of the connection between service consumer and provider.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateEndpointConnection"
    delete:
      operationId: PrivateEndpointConnections_begin_delete
      description: Deletes a private endpoint connection with a given name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: private_endpoint_connection_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private endpoint connection associated with the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/privateEndpointConnections?api-version=2023-03-02-preview:
    get:
      operationId: PrivateEndpointConnections_list_by_cluster
      description: Gets list of private endpoint connections on a cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateEndpointConnectionFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/privateLinkResources/{private_link_resource_name}?api-version=2023-03-02-preview:
    get:
      operationId: PrivateLinkResources_get
      description: Gets a private link resource for cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: private_link_resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the private link resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivateLinkResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/privateLinkResources?api-version=2023-03-02-preview:
    get:
      operationId: PrivateLinkResources_list_by_cluster
      description: Gets the private link resources for cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedPrivateLinkResourceFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/roles/{role_name}?api-version=2023-03-02-preview:
    get:
      operationId: Roles_get
      description: Gets information about a cluster role.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: role_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster role. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleFlattened"
    put:
      operationId: Roles_begin_create
      description: Creates a new role or updates an existing role.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: role_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster role. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    roleType:
                      type: string
                      description: 'Known values are: "user" and "admin".'
                    password:
                      type: string
                      description: The password of the cluster role. If an identity is used, password will not be required.
                    externalIdentity:
                      type: object
                      properties:
                        objectId:
                          type: string
                          description: ":vartype object_id: str"
                        principalType:
                          type: string
                          description: 'Known values are: "user", "servicePrincipal", and "group".'
                        tenantId:
                          type: string
                          description: ":vartype tenant_id: str"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
    delete:
      operationId: Roles_begin_delete
      description: Deletes a cluster role.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: role_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster role. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/roles?api-version=2023-03-02-preview:
    get:
      operationId: Roles_list_by_cluster
      description: List all the roles in a given cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedRoleFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/servers/{server_name}?api-version=2023-03-02-preview:
    get:
      operationId: Servers_get
      description: Gets information about a server in cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: server_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the server. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClusterServerFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.DBforPostgreSQL/serverGroupsv2/{cluster_name}/servers?api-version=2023-03-02-preview:
    get:
      operationId: Servers_list_by_cluster
      description: Lists servers of a cluster.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cluster_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the cluster. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedClusterServerFlattened"
