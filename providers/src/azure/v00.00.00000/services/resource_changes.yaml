openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Changes Client
  description: Changes Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2022-05-01-stackql-generated
  x-serviceAlias: resource_changes
  x-sdkPackage: azure-mgmt-resource-changes
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    ChangeProperties:
      type: object
      description: The properties of a change. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        targetResourceId:
          type: string
          description: The fully qualified ID of the target resource that was changed.
        targetResourceType:
          type: string
          description: The namespace and type of the resource.
        changeType:
          type: string
          description: 'The type of change that was captured in the resource. Known values are: "Update", "Delete", and "Create".'
        changeAttributes:
          $ref: "#/components/schemas/ChangeAttributes"
          description: Details about the change resource.
        changes:
          type: object
          properties: {}
          description: A dictionary with changed property name as a key and the change details as the value.
    ChangeAttributes:
      type: object
      description: Details about the change resource. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        correlationId:
          type: string
          description: The ARM correlation ID of the change resource.
        timestamp:
          type: string
          description: The time the change(s) on the target resource ocurred.
        changesCount:
          type: integer
          description: The number of changes this resource captures.
        previousResourceSnapshotId:
          type: string
          description: The GUID of the previous snapshot.
        newResourceSnapshotId:
          type: string
          description: The GUID of the new snapshot.
    ChangeResourceResultFlattened:
      type: object
      description: Flattened view of ChangeResourceResult ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        targetResourceId:
          type: string
          description: The fully qualified ID of the target resource that was changed.
        targetResourceType:
          type: string
          description: The namespace and type of the resource.
        changeType:
          type: string
          description: 'The type of change that was captured in the resource. Known values are: "Update", "Delete", and "Create".'
        changeAttributes:
          $ref: "#/components/schemas/ChangeAttributes"
          description: Details about the change resource.
        changes:
          type: object
          properties: {}
          description: A dictionary with changed property name as a key and the change details as the value.
    PagedChangeResourceResultFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ChangeResourceResultFlattened"
        nextLink:
          type: string
  x-stackQL-resources:
    changes:
      id: azure.resource_changes.changes
      name: changes
      title: changes
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{resource_provider_namespace}~1{resource_type}~1{resource_name}~1providers~1Microsoft.Resources~1changes~1{change_resource_id}?api-version=2022-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1{resource_provider_namespace}~1{resource_type}~1{resource_name}~1providers~1Microsoft.Resources~1changes?api-version=2022-05-01/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/changes/methods/get"
          - $ref: "#/components/x-stackQL-resources/changes/methods/list"
        insert: []
        update: []
        delete: []
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{resource_provider_namespace}/{resource_type}/{resource_name}/providers/Microsoft.Resources/changes/{change_resource_id}?api-version=2022-05-01:
    get:
      operationId: Changes_get
      description: Obtains the specified change resource for the target resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: resource_provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource provider namespace. Required.
        - name: resource_type
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource type. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource. Required.
        - name: change_resource_id
          in: path
          required: true
          schema:
            type: string
          description: The ID of the change resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeResourceResultFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/{resource_provider_namespace}/{resource_type}/{resource_name}/providers/Microsoft.Resources/changes?api-version=2022-05-01:
    get:
      operationId: Changes_list
      description: Obtains a list of change resources from the past 14 days for the target resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. Required.
        - name: resource_provider_namespace
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource provider namespace. Required.
        - name: resource_type
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource type. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: $top
          in: query
          required: false
          schema:
            type: integer
          description: (Optional) Set the maximum number of results per response. Default value is 100.
        - name: $skipToken
          in: query
          required: false
          schema:
            type: string
          description: (Optional) The page-continuation token. Default value is None.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedChangeResourceResultFlattened"
