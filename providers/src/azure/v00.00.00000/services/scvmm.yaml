openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Sc Vmm Mgmt Client
  description: The Microsoft.ScVmm Rest API spec.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2023-10-07-stackql-generated
  x-serviceAlias: scvmm
  x-sdkPackage: azure-mgmt-scvmm
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          type: string
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          type: string
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    AvailabilitySetProperties:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        availabilitySetName:
          type: string
          description: Name of the availability set.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    ExtendedLocation:
      type: object
      description: The extended location.
      properties:
        type:
          type: string
          description: The extended location type.
        name:
          type: string
          description: The extended location name.
    AvailabilitySetFlattened:
      type: object
      description: Flattened view of AvailabilitySet ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
        availabilitySetName:
          type: string
          description: Name of the availability set.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedAvailabilitySetFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/AvailabilitySetFlattened"
        nextLink:
          type: string
    AvailabilitySet:
      type: object
      description: The AvailabilitySets resource definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/AvailabilitySetProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
      required:
        - location
        - extendedLocation
    CloudProperties:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource.
        uuid:
          type: string
          description: Unique ID of the cloud.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        cloudName:
          type: string
          description: Name of the cloud in VmmServer.
        cloudCapacity:
          $ref: "#/components/schemas/CloudCapacity"
          description: Capacity of the cloud.
        storageQoSPolicies:
          type: array
          items:
            $ref: "#/components/schemas/StorageQosPolicy"
          description: List of QoS policies available for the cloud.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    CloudCapacity:
      type: object
      description: Cloud Capacity model. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        cpuCount:
          type: integer
          description: CPUCount specifies the maximum number of CPUs that can be allocated in the cloud.
        memoryMB:
          type: integer
          description: MemoryMB specifies a memory usage limit in megabytes.
        vmCount:
          type: integer
          description: VMCount gives the max number of VMs that can be deployed in the cloud.
    StorageQosPolicy:
      type: object
      description: The StorageQoSPolicy definition.
      properties:
        name:
          type: string
          description: The name of the policy.
        id:
          type: string
          description: The ID of the QoS policy.
        iopsMaximum:
          type: integer
          description: The maximum IO operations per second.
        iopsMinimum:
          type: integer
          description: The minimum IO operations per second.
        bandwidthLimit:
          type: integer
          description: The Bandwidth Limit for internet traffic.
        policyId:
          type: string
          description: The underlying policy.
    CloudFlattened:
      type: object
      description: Flattened view of Cloud ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource.
        uuid:
          type: string
          description: Unique ID of the cloud.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        cloudName:
          type: string
          description: Name of the cloud in VmmServer.
        cloudCapacity:
          $ref: "#/components/schemas/CloudCapacity"
          description: Capacity of the cloud.
        storageQoSPolicies:
          type: array
          items:
            $ref: "#/components/schemas/StorageQosPolicy"
          description: List of QoS policies available for the cloud.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedCloudFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CloudFlattened"
        nextLink:
          type: string
    Cloud:
      type: object
      description: The Clouds resource definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/CloudProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
      required:
        - location
        - extendedLocation
    GuestAgentProperties:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        uuid:
          type: string
          description: Gets a unique identifier for this resource.
        credentials:
          $ref: "#/components/schemas/GuestCredential"
          description: Username / Password Credentials to provision guest agent.
        httpProxyConfig:
          $ref: "#/components/schemas/HttpProxyConfiguration"
          description: HTTP Proxy configuration for the VM.
        provisioningAction:
          type: string
          description: 'Gets or sets the guest agent provisioning action. Known values are: "install", "uninstall", and "repair".'
        status:
          type: string
          description: Gets the guest agent status.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    GuestCredential:
      type: object
      description: Username / Password Credentials to connect to guest. All required parameters must be populated in order to send to server.
      properties:
        username:
          type: string
          description: Gets or sets username to connect with the guest. Required.
        password:
          type: string
          description: Gets or sets the password to connect with the guest. Required.
      required:
        - username
        - password
    HttpProxyConfiguration:
      type: object
      description: HTTP Proxy configuration for the VM.
      properties:
        httpsProxy:
          type: string
          description: Gets or sets httpsProxy url.
    GuestAgentFlattened:
      type: object
      description: Flattened view of GuestAgent ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        uuid:
          type: string
          description: Gets a unique identifier for this resource.
        credentials:
          $ref: "#/components/schemas/GuestCredential"
          description: Username / Password Credentials to provision guest agent.
        httpProxyConfig:
          $ref: "#/components/schemas/HttpProxyConfiguration"
          description: HTTP Proxy configuration for the VM.
        provisioningAction:
          type: string
          description: 'Gets or sets the guest agent provisioning action. Known values are: "install", "uninstall", and "repair".'
        status:
          type: string
          description: Gets the guest agent status.
        customResourceName:
          type: string
          description: Gets the name of the corresponding resource in Kubernetes.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedGuestAgentFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/GuestAgentFlattened"
        nextLink:
          type: string
    GuestAgent:
      type: object
      description: Defines the GuestAgent. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/GuestAgentProperties"
          description: The resource-specific properties for this resource.
    InventoryItemProperties:
      type: object
      description: "Defines the resource properties. You probably want to use the sub-classes and not this class directly. Known sub-classes are: CloudInventoryItem, VirtualMachineInventoryItem, VirtualMachineTemplateInventoryItem, VirtualNetworkInventoryItem Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server."
      properties:
        inventoryType:
          type: string
          description: 'They inventory type. Required. Known values are: "Cloud", "VirtualNetwork", "VirtualMachine", and "VirtualMachineTemplate".'
        managedResourceId:
          type: string
          description: Gets the tracked resource id corresponding to the inventory resource.
        uuid:
          type: string
          description: Gets the UUID (which is assigned by Vmm) for the inventory item.
        inventoryItemName:
          type: string
          description: Gets the Managed Object name in Vmm for the inventory item.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - inventoryType
    InventoryItemFlattened:
      type: object
      description: Flattened view of InventoryItem ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
        inventoryType:
          type: string
          description: 'They inventory type. Required. Known values are: "Cloud", "VirtualNetwork", "VirtualMachine", and "VirtualMachineTemplate".'
        managedResourceId:
          type: string
          description: Gets the tracked resource id corresponding to the inventory resource.
        uuid:
          type: string
          description: Gets the UUID (which is assigned by Vmm) for the inventory item.
        inventoryItemName:
          type: string
          description: Gets the Managed Object name in Vmm for the inventory item.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedInventoryItemFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/InventoryItemFlattened"
        nextLink:
          type: string
    InventoryItem:
      type: object
      description: Defines the inventory item. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/InventoryItemProperties"
          description: The resource-specific properties for this resource.
        kind:
          type: string
          description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
        display:
          $ref: "#/components/schemas/OperationDisplay"
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
        nextLink:
          type: string
    VirtualMachineInstanceProperties:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        availabilitySets:
          type: array
          items:
            $ref: "#/components/schemas/AvailabilitySetListItem"
          description: Availability Sets in vm.
        osProfile:
          $ref: "#/components/schemas/OsProfileForVmInstance"
          description: OS properties.
        hardwareProfile:
          $ref: "#/components/schemas/HardwareProfile"
          description: Hardware properties.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Network properties.
        storageProfile:
          $ref: "#/components/schemas/StorageProfile"
          description: Storage properties.
        infrastructureProfile:
          $ref: "#/components/schemas/InfrastructureProfile"
          description: Gets the infrastructure profile.
        powerState:
          type: string
          description: Gets the power state of the virtual machine.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    AvailabilitySetListItem:
      type: object
      description: Availability Set model.
      properties:
        id:
          type: string
          description: Gets the ARM Id of the microsoft.scvmm/availabilitySets resource.
        name:
          type: string
          description: Gets or sets the name of the availability set.
    OsProfileForVmInstance:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        adminPassword:
          type: string
          description: Admin password of the virtual machine.
        computerName:
          type: string
          description: Gets or sets computer name.
        osType:
          type: string
          description: 'Gets the type of the os. Known values are: "Windows", "Linux", and "Other".'
        osSku:
          type: string
          description: Gets os sku.
        osVersion:
          type: string
          description: Gets os version.
    HardwareProfile:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        memoryMB:
          type: integer
          description: MemoryMB is the size of a virtual machine's memory, in MB.
        cpuCount:
          type: integer
          description: Gets or sets the number of vCPUs for the vm.
        limitCpuForMigration:
          type: string
          description: 'Gets or sets a value indicating whether to enable processor compatibility mode for live migration of VMs. Known values are: "true" and "false".'
        dynamicMemoryEnabled:
          type: string
          description: 'Gets or sets a value indicating whether to enable dynamic memory or not. Known values are: "true" and "false".'
        dynamicMemoryMaxMB:
          type: integer
          description: Gets or sets the max dynamic memory for the vm.
        dynamicMemoryMinMB:
          type: integer
          description: Gets or sets the min dynamic memory for the vm.
        isHighlyAvailable:
          type: string
          description: 'Gets highly available property. Known values are: "true" and "false".'
    NetworkProfile:
      type: object
      description: Defines the resource properties.
      properties:
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/NetworkInterface"
          description: Gets or sets the list of network interfaces associated with the virtual machine.
    NetworkInterface:
      type: object
      description: Network Interface model. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Gets or sets the name of the network interface.
        displayName:
          type: string
          description: Gets the display name of the network interface as shown in the vmmServer. This is the fallback label for a NIC when the name is not set.
        ipv4Addresses:
          type: array
          items:
            type: string
          description: Gets the nic ipv4 addresses.
        ipv6Addresses:
          type: array
          items:
            type: string
          description: Gets the nic ipv6 addresses.
        macAddress:
          type: string
          description: Gets or sets the nic MAC address.
        virtualNetworkId:
          type: string
          description: Gets or sets the ARM Id of the Microsoft.ScVmm/virtualNetwork resource to connect the nic.
        networkName:
          type: string
          description: Gets the name of the virtual network in vmmServer that the nic is connected to.
        ipv4AddressType:
          type: string
          description: 'Gets or sets the ipv4 address type. Known values are: "Dynamic" and "Static".'
        ipv6AddressType:
          type: string
          description: 'Gets or sets the ipv6 address type. Known values are: "Dynamic" and "Static".'
        macAddressType:
          type: string
          description: 'Gets or sets the mac address type. Known values are: "Dynamic" and "Static".'
        nicId:
          type: string
          description: Gets or sets the nic id.
    StorageProfile:
      type: object
      description: Defines the resource properties.
      properties:
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDisk"
          description: Gets or sets the list of virtual disks associated with the virtual machine.
    VirtualDisk:
      type: object
      description: Virtual disk model. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: Gets or sets the name of the disk.
        displayName:
          type: string
          description: Gets the display name of the virtual disk as shown in the vmmServer. This is the fallback label for a disk when the name is not set.
        diskId:
          type: string
          description: Gets or sets the disk id.
        diskSizeGB:
          type: integer
          description: Gets or sets the disk total size.
        maxDiskSizeGB:
          type: integer
          description: Gets the max disk size.
        bus:
          type: integer
          description: Gets or sets the disk bus.
        lun:
          type: integer
          description: Gets or sets the disk lun.
        busType:
          type: string
          description: Gets or sets the disk bus type.
        vhdType:
          type: string
          description: Gets or sets the disk vhd type.
        volumeType:
          type: string
          description: Gets the disk volume type.
        vhdFormatType:
          type: string
          description: Gets the disk vhd format type.
        templateDiskId:
          type: string
          description: Gets or sets the disk id in the template.
        storageQoSPolicy:
          $ref: "#/components/schemas/StorageQosPolicyDetails"
          description: The QoS policy for the disk.
        createDiffDisk:
          type: string
          description: 'Gets or sets a value indicating diff disk. Known values are: "true" and "false".'
    StorageQosPolicyDetails:
      type: object
      description: The StorageQoSPolicyDetails definition.
      properties:
        name:
          type: string
          description: The name of the policy.
        id:
          type: string
          description: The ID of the QoS policy.
    InfrastructureProfile:
      type: object
      description: Specifies the vmmServer infrastructure specific settings for the virtual machine instance. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        cloudId:
          type: string
          description: ARM Id of the cloud resource to use for deploying the vm.
        templateId:
          type: string
          description: ARM Id of the template resource to use for deploying the vm.
        vmName:
          type: string
          description: VMName is the name of VM on the SCVmm server.
        uuid:
          type: string
          description: Unique ID of the virtual machine.
        lastRestoredVMCheckpoint:
          $ref: "#/components/schemas/Checkpoint"
          description: Last restored checkpoint in the vm.
        checkpoints:
          type: array
          items:
            $ref: "#/components/schemas/Checkpoint"
          description: Checkpoints in the vm.
        checkpointType:
          type: string
          description: Type of checkpoint supported for the vm.
        generation:
          type: integer
          description: Gets or sets the generation for the vm.
        biosGuid:
          type: string
          description: Gets or sets the bios guid for the vm.
    Checkpoint:
      type: object
      description: Defines the resource properties.
      properties:
        parentCheckpointID:
          type: string
          description: Gets ID of parent of the checkpoint.
        checkpointID:
          type: string
          description: Gets ID of the checkpoint.
        name:
          type: string
          description: Gets name of the checkpoint.
        description:
          type: string
          description: Gets description of the checkpoint.
    VirtualMachineInstanceFlattened:
      type: object
      description: Flattened view of VirtualMachineInstance ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location. Required.
        availabilitySets:
          type: array
          items:
            $ref: "#/components/schemas/AvailabilitySetListItem"
          description: Availability Sets in vm.
        osProfile:
          $ref: "#/components/schemas/OsProfileForVmInstance"
          description: OS properties.
        hardwareProfile:
          $ref: "#/components/schemas/HardwareProfile"
          description: Hardware properties.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfile"
          description: Network properties.
        storageProfile:
          $ref: "#/components/schemas/StorageProfile"
          description: Storage properties.
        infrastructureProfile:
          $ref: "#/components/schemas/InfrastructureProfile"
          description: Gets the infrastructure profile.
        powerState:
          type: string
          description: Gets the power state of the virtual machine.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVirtualMachineInstanceFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineInstanceFlattened"
        nextLink:
          type: string
    VirtualMachineInstance:
      type: object
      description: Define the virtualMachineInstance. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        properties:
          $ref: "#/components/schemas/VirtualMachineInstanceProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: Gets or sets the extended location. Required.
      required:
        - extendedLocation
    VirtualMachineInstanceUpdateProperties:
      type: object
      description: Virtual Machine Instance Properties Update model.
      properties:
        availabilitySets:
          type: array
          items:
            $ref: "#/components/schemas/AvailabilitySetListItem"
          description: Availability Sets in vm.
        hardwareProfile:
          $ref: "#/components/schemas/HardwareProfileUpdate"
          description: Hardware properties.
        networkProfile:
          $ref: "#/components/schemas/NetworkProfileUpdate"
          description: Network properties.
        storageProfile:
          $ref: "#/components/schemas/StorageProfileUpdate"
          description: Storage properties.
        infrastructureProfile:
          $ref: "#/components/schemas/InfrastructureProfileUpdate"
          description: Gets the infrastructure profile.
    HardwareProfileUpdate:
      type: object
      description: Defines the resource update properties.
      properties:
        memoryMB:
          type: integer
          description: MemoryMB is the size of a virtual machine's memory, in MB.
        cpuCount:
          type: integer
          description: Gets or sets the number of vCPUs for the vm.
        limitCpuForMigration:
          type: string
          description: 'Gets or sets a value indicating whether to enable processor compatibility mode for live migration of VMs. Known values are: "true" and "false".'
        dynamicMemoryEnabled:
          type: string
          description: 'Gets or sets a value indicating whether to enable dynamic memory or not. Known values are: "true" and "false".'
        dynamicMemoryMaxMB:
          type: integer
          description: Gets or sets the max dynamic memory for the vm.
        dynamicMemoryMinMB:
          type: integer
          description: Gets or sets the min dynamic memory for the vm.
    NetworkProfileUpdate:
      type: object
      description: Defines the resource update properties.
      properties:
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/NetworkInterfaceUpdate"
          description: Gets or sets the list of network interfaces associated with the virtual machine.
    NetworkInterfaceUpdate:
      type: object
      description: Network Interface Update model.
      properties:
        name:
          type: string
          description: Gets or sets the name of the network interface.
        macAddress:
          type: string
          description: Gets or sets the nic MAC address.
        virtualNetworkId:
          type: string
          description: Gets or sets the ARM Id of the Microsoft.ScVmm/virtualNetwork resource to connect the nic.
        ipv4AddressType:
          type: string
          description: 'Gets or sets the ipv4 address type. Known values are: "Dynamic" and "Static".'
        ipv6AddressType:
          type: string
          description: 'Gets or sets the ipv6 address type. Known values are: "Dynamic" and "Static".'
        macAddressType:
          type: string
          description: 'Gets or sets the mac address type. Known values are: "Dynamic" and "Static".'
        nicId:
          type: string
          description: Gets or sets the nic id.
    StorageProfileUpdate:
      type: object
      description: Defines the resource update properties.
      properties:
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDiskUpdate"
          description: Gets or sets the list of virtual disks associated with the virtual machine.
    VirtualDiskUpdate:
      type: object
      description: Virtual Disk Update model.
      properties:
        name:
          type: string
          description: Gets or sets the name of the disk.
        diskId:
          type: string
          description: Gets or sets the disk id.
        diskSizeGB:
          type: integer
          description: Gets or sets the disk total size.
        bus:
          type: integer
          description: Gets or sets the disk bus.
        lun:
          type: integer
          description: Gets or sets the disk lun.
        busType:
          type: string
          description: Gets or sets the disk bus type.
        vhdType:
          type: string
          description: Gets or sets the disk vhd type.
        storageQoSPolicy:
          $ref: "#/components/schemas/StorageQosPolicyDetails"
          description: The QoS policy for the disk.
    InfrastructureProfileUpdate:
      type: object
      description: Specifies the vmmServer infrastructure specific update settings for the virtual machine instance.
      properties:
        checkpointType:
          type: string
          description: Type of checkpoint supported for the vm.
    VirtualMachineTemplateProperties:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource.
        uuid:
          type: string
          description: Unique ID of the virtual machine template.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        osType:
          type: string
          description: 'Gets the type of the os. Known values are: "Windows", "Linux", and "Other".'
        osName:
          type: string
          description: Gets os name.
        computerName:
          type: string
          description: Gets computer name.
        memoryMB:
          type: integer
          description: MemoryMB is the desired size of a virtual machine's memory, in MB.
        cpuCount:
          type: integer
          description: Gets the desired number of vCPUs for the vm.
        limitCpuForMigration:
          type: string
          description: 'Gets a value indicating whether to enable processor compatibility mode for live migration of VMs. Known values are: "true" and "false".'
        dynamicMemoryEnabled:
          type: string
          description: 'Gets a value indicating whether to enable dynamic memory or not. Known values are: "true" and "false".'
        isCustomizable:
          type: string
          description: 'Gets a value indicating whether the vm template is customizable or not. Known values are: "true" and "false".'
        dynamicMemoryMaxMB:
          type: integer
          description: Gets the max dynamic memory for the vm.
        dynamicMemoryMinMB:
          type: integer
          description: Gets the min dynamic memory for the vm.
        isHighlyAvailable:
          type: string
          description: 'Gets highly available property. Known values are: "true" and "false".'
        generation:
          type: integer
          description: Gets the generation for the vm.
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/NetworkInterface"
          description: Gets the network interfaces of the template.
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDisk"
          description: Gets the disks of the template.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    VirtualMachineTemplateFlattened:
      type: object
      description: Flattened view of VirtualMachineTemplate ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource.
        uuid:
          type: string
          description: Unique ID of the virtual machine template.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        osType:
          type: string
          description: 'Gets the type of the os. Known values are: "Windows", "Linux", and "Other".'
        osName:
          type: string
          description: Gets os name.
        computerName:
          type: string
          description: Gets computer name.
        memoryMB:
          type: integer
          description: MemoryMB is the desired size of a virtual machine's memory, in MB.
        cpuCount:
          type: integer
          description: Gets the desired number of vCPUs for the vm.
        limitCpuForMigration:
          type: string
          description: 'Gets a value indicating whether to enable processor compatibility mode for live migration of VMs. Known values are: "true" and "false".'
        dynamicMemoryEnabled:
          type: string
          description: 'Gets a value indicating whether to enable dynamic memory or not. Known values are: "true" and "false".'
        isCustomizable:
          type: string
          description: 'Gets a value indicating whether the vm template is customizable or not. Known values are: "true" and "false".'
        dynamicMemoryMaxMB:
          type: integer
          description: Gets the max dynamic memory for the vm.
        dynamicMemoryMinMB:
          type: integer
          description: Gets the min dynamic memory for the vm.
        isHighlyAvailable:
          type: string
          description: 'Gets highly available property. Known values are: "true" and "false".'
        generation:
          type: integer
          description: Gets the generation for the vm.
        networkInterfaces:
          type: array
          items:
            $ref: "#/components/schemas/NetworkInterface"
          description: Gets the network interfaces of the template.
        disks:
          type: array
          items:
            $ref: "#/components/schemas/VirtualDisk"
          description: Gets the disks of the template.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVirtualMachineTemplateFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualMachineTemplateFlattened"
        nextLink:
          type: string
    VirtualMachineTemplate:
      type: object
      description: The VirtualMachineTemplates resource definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/VirtualMachineTemplateProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
      required:
        - location
        - extendedLocation
    VirtualNetworkProperties:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource.
        uuid:
          type: string
          description: Unique ID of the virtual network.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        networkName:
          type: string
          description: Name of the virtual network in vmmServer.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    VirtualNetworkFlattened:
      type: object
      description: Flattened view of VirtualNetwork ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
        inventoryItemId:
          type: string
          description: Gets or sets the inventory Item ID for the resource.
        uuid:
          type: string
          description: Unique ID of the virtual network.
        vmmServerId:
          type: string
          description: ARM Id of the vmmServer resource in which this resource resides.
        networkName:
          type: string
          description: Name of the virtual network in vmmServer.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVirtualNetworkFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VirtualNetworkFlattened"
        nextLink:
          type: string
    VirtualNetwork:
      type: object
      description: The VirtualNetworks resource definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/VirtualNetworkProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
      required:
        - location
        - extendedLocation
    VmInstanceHybridIdentityMetadataProperties:
      type: object
      description: Describes the properties of Hybrid Identity Metadata for a Virtual Machine. Variables are only populated by the server, and will be ignored when sending a request.
      properties:
        resourceUid:
          type: string
          description: The unique identifier for the resource.
        publicKey:
          type: string
          description: Gets or sets the Public Key.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    VmInstanceHybridIdentityMetadataFlattened:
      type: object
      description: Flattened view of VmInstanceHybridIdentityMetadata ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        resourceUid:
          type: string
          description: The unique identifier for the resource.
        publicKey:
          type: string
          description: Gets or sets the Public Key.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVmInstanceHybridIdentityMetadataFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VmInstanceHybridIdentityMetadataFlattened"
        nextLink:
          type: string
    VmmServerProperties:
      type: object
      description: Defines the resource properties. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        credentials:
          $ref: "#/components/schemas/VmmCredential"
          description: Credentials to connect to VmmServer.
        fqdn:
          type: string
          description: Fqdn is the hostname/ip of the vmmServer. Required.
        port:
          type: integer
          description: Port is the port on which the vmmServer is listening.
        connectionStatus:
          type: string
          description: Gets the connection status to the vmmServer.
        errorMessage:
          type: string
          description: Gets any error message if connection to vmmServer is having any issue.
        uuid:
          type: string
          description: Unique ID of vmmServer.
        version:
          type: string
          description: Version is the version of the vmmSever.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
      required:
        - fqdn
    VmmCredential:
      type: object
      description: Credentials to connect to VmmServer.
      properties:
        username:
          type: string
          description: Username to use to connect to VmmServer.
        password:
          type: string
          description: Password to use to connect to VmmServer.
    VmmServerFlattened:
      type: object
      description: Flattened view of VmmServer ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
        credentials:
          $ref: "#/components/schemas/VmmCredential"
          description: Credentials to connect to VmmServer.
        fqdn:
          type: string
          description: Fqdn is the hostname/ip of the vmmServer. Required.
        port:
          type: integer
          description: Port is the port on which the vmmServer is listening.
        connectionStatus:
          type: string
          description: Gets the connection status to the vmmServer.
        errorMessage:
          type: string
          description: Gets any error message if connection to vmmServer is having any issue.
        uuid:
          type: string
          description: Unique ID of vmmServer.
        version:
          type: string
          description: Version is the version of the vmmSever.
        provisioningState:
          type: string
          description: 'Provisioning state of the resource. Known values are: "Succeeded", "Failed", "Canceled", "Provisioning", "Updating", "Deleting", "Accepted", and "Created".'
    PagedVmmServerFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/VmmServerFlattened"
        nextLink:
          type: string
    VmmServer:
      type: object
      description: The VmmServers resource definition. Variables are only populated by the server, and will be ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: 'Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}". # pylint: disable=line-too-long'
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/VmmServerProperties"
          description: The resource-specific properties for this resource.
        extendedLocation:
          $ref: "#/components/schemas/ExtendedLocation"
          description: The extended location. Required.
      required:
        - location
        - extendedLocation
  x-stackQL-resources:
    availability_sets:
      id: azure.scvmm.availability_sets
      name: availability_sets
      title: availability_sets
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1availabilitySets~1{availability_set_resource_name}?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1availabilitySets~1{availability_set_resource_name}?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1availabilitySets~1{availability_set_resource_name}?api-version=2023-10-07/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1availabilitySets~1{availability_set_resource_name}?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ScVmm~1availabilitySets?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1availabilitySets?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/availability_sets/methods/get"
          - $ref: "#/components/x-stackQL-resources/availability_sets/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/availability_sets/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/availability_sets/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/availability_sets/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/availability_sets/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/availability_sets/methods/delete"
    clouds:
      id: azure.scvmm.clouds
      name: clouds
      title: clouds
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1clouds~1{cloud_resource_name}?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1clouds~1{cloud_resource_name}?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1clouds~1{cloud_resource_name}?api-version=2023-10-07/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1clouds~1{cloud_resource_name}?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ScVmm~1clouds?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1clouds?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/clouds/methods/get"
          - $ref: "#/components/x-stackQL-resources/clouds/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/clouds/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/clouds/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/clouds/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/clouds/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/clouds/methods/delete"
    guest_agents:
      id: azure.scvmm.guest_agents
      name: guest_agents
      title: guest_agents
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1guestAgents~1default?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1guestAgents~1default?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1guestAgents~1default?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
        list_by_virtual_machine_instance:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1guestAgents?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/guest_agents/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/guest_agents/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/guest_agents/methods/delete"
    inventory_items:
      id: azure.scvmm.inventory_items
      name: inventory_items
      title: inventory_items
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}~1inventoryItems~1{inventory_item_resource_name}?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}~1inventoryItems~1{inventory_item_resource_name}?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}~1inventoryItems~1{inventory_item_resource_name}?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
        list_by_vmm_server:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}~1inventoryItems?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/get"
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/list_by_vmm_server"
        insert:
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/inventory_items/methods/delete"
    operations:
      id: azure.scvmm.operations
      name: operations
      title: operations
      methods:
        list:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ScVmm~1operations?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/list"
        insert: []
        update: []
        delete: []
    virtual_machine_instances:
      id: azure.scvmm.virtual_machine_instances
      name: virtual_machine_instances
      title: virtual_machine_instances
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default?api-version=2023-10-07/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        create_checkpoint:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1createCheckpoint?api-version=2023-10-07/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete_checkpoint:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1deleteCheckpoint?api-version=2023-10-07/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        restart:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1restart?api-version=2023-10-07/post"
          response:
            openAPIDocKey: "204"
        restore_checkpoint:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1restoreCheckpoint?api-version=2023-10-07/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        start:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1start?api-version=2023-10-07/post"
          response:
            openAPIDocKey: "204"
        stop:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1stop?api-version=2023-10-07/post"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        list_raw:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/get"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/create_or_update"
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/create_checkpoint"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_instances/methods/delete"
    virtual_machine_templates:
      id: azure.scvmm.virtual_machine_templates
      name: virtual_machine_templates
      title: virtual_machine_templates
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-07/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualMachineTemplates~1{virtual_machine_template_name}?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ScVmm~1virtualMachineTemplates?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualMachineTemplates?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_machine_templates/methods/delete"
    virtual_networks:
      id: azure.scvmm.virtual_networks
      name: virtual_networks
      title: virtual_networks
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-07/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualNetworks~1{virtual_network_name}?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ScVmm~1virtualNetworks?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1virtualNetworks?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/get"
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/virtual_networks/methods/delete"
    vm_instance_hybrid_identity_metadatas:
      id: azure.scvmm.vm_instance_hybrid_identity_metadatas
      name: vm_instance_hybrid_identity_metadatas
      title: vm_instance_hybrid_identity_metadatas
      methods:
        get:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1hybridIdentityMetadata~1default?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        list_by_virtual_machine_instance:
          operation:
            $ref: "#/paths/~1{resource_uri}~1providers~1Microsoft.ScVmm~1virtualMachineInstances~1default~1hybridIdentityMetadata?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/vm_instance_hybrid_identity_metadatas/methods/get"
        insert: []
        update: []
        delete: []
    vmm_servers:
      id: azure.scvmm.vmm_servers
      name: vmm_servers
      title: vmm_servers
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}?api-version=2023-10-07/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}?api-version=2023-10-07/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers~1{vmm_server_name}?api-version=2023-10-07/delete"
          response:
            openAPIDocKey: "204"
          request:
            nativeCasing: camel
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ScVmm~1vmmServers?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ScVmm~1vmmServers?api-version=2023-10-07/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/vmm_servers/methods/get"
          - $ref: "#/components/x-stackQL-resources/vmm_servers/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/vmm_servers/methods/list_by_subscription"
        insert:
          - $ref: "#/components/x-stackQL-resources/vmm_servers/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/vmm_servers/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/vmm_servers/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/vmm_servers/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/availabilitySets/{availability_set_resource_name}?api-version=2023-10-07:
    get:
      operationId: AvailabilitySets_get
      description: Gets an AvailabilitySet. Implements AvailabilitySet GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: availability_set_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AvailabilitySet. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailabilitySetFlattened"
    put:
      operationId: AvailabilitySets_begin_create_or_update
      description: Implements AvailabilitySets PUT method. Onboards the ScVmm availability set as an Azure resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: availability_set_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AvailabilitySet. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/AvailabilitySetProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extended location. Required.
              required:
                - location
                - extendedLocation
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailabilitySet"
    patch:
      operationId: AvailabilitySets_begin_update
      description: Implements the AvailabilitySets PATCH method. Updates the AvailabilitySets resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: availability_set_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AvailabilitySet. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailabilitySet"
    delete:
      operationId: AvailabilitySets_begin_delete
      description: Implements AvailabilitySet DELETE method. Deregisters the ScVmm availability set from Azure.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: availability_set_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the AvailabilitySet. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: string
          description: 'Forces the resource to be deleted. Known values are: "true" and "false". Default value is None.'
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ScVmm/availabilitySets?api-version=2023-10-07:
    get:
      operationId: AvailabilitySets_list_by_subscription
      description: Implements GET AvailabilitySets in a subscription. List of AvailabilitySets in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAvailabilitySetFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/availabilitySets?api-version=2023-10-07:
    get:
      operationId: AvailabilitySets_list_by_resource_group
      description: Implements GET AvailabilitySets in a resource group. List of AvailabilitySets in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedAvailabilitySetFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/clouds/{cloud_resource_name}?api-version=2023-10-07:
    get:
      operationId: Clouds_get
      description: Gets a Cloud. Implements Cloud GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cloud_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Cloud. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CloudFlattened"
    put:
      operationId: Clouds_begin_create_or_update
      description: Implements Clouds PUT method. Onboards the ScVmm fabric cloud as an Azure cloud resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cloud_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Cloud. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/CloudProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extended location. Required.
              required:
                - location
                - extendedLocation
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cloud"
    patch:
      operationId: Clouds_begin_update
      description: Implements the Clouds PATCH method. Updates the Clouds resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cloud_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Cloud. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cloud"
    delete:
      operationId: Clouds_begin_delete
      description: Implements Cloud resource DELETE method. Deregisters the ScVmm fabric cloud from Azure.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: cloud_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the Cloud. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: string
          description: 'Forces the resource to be deleted. Known values are: "true" and "false". Default value is None.'
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ScVmm/clouds?api-version=2023-10-07:
    get:
      operationId: Clouds_list_by_subscription
      description: Implements GET Clouds in a subscription. List of Clouds in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCloudFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/clouds?api-version=2023-10-07:
    get:
      operationId: Clouds_list_by_resource_group
      description: Implements GET Clouds in a resource group. List of Clouds in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCloudFlattened"
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/guestAgents/default?api-version=2023-10-07:
    get:
      operationId: GuestAgents_get
      description: Gets GuestAgent. Implements GuestAgent GET method.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GuestAgentFlattened"
    put:
      operationId: GuestAgents_begin_create
      description: Implements GuestAgent PUT method. Create Or Update GuestAgent.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/GuestAgentProperties"
                  description: The resource-specific properties for this resource.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GuestAgent"
    delete:
      operationId: GuestAgents_delete
      description: Deletes a GuestAgent resource. Implements GuestAgent DELETE method.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/guestAgents?api-version=2023-10-07:
    get:
      operationId: GuestAgents_list_by_virtual_machine_instance
      description: Implements GET GuestAgent in a vm. Returns the list of GuestAgent of the given vm.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedGuestAgentFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/vmmServers/{vmm_server_name}/inventoryItems/{inventory_item_resource_name}?api-version=2023-10-07:
    get:
      operationId: InventoryItems_get
      description: Implements GET InventoryItem method. Shows an inventory item.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: inventory_item_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the inventoryItem. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryItemFlattened"
    put:
      operationId: InventoryItems_create
      description: Implements InventoryItem PUT method. Create Or Update InventoryItem.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: inventory_item_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the inventoryItem. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/InventoryItemProperties"
                  description: The resource-specific properties for this resource.
                kind:
                  type: string
                  description: Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type; e.g. ApiApps are a kind of Microsoft.Web/sites type. If supported, the resource provider must validate and persist this value.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryItem"
    delete:
      operationId: InventoryItems_delete
      description: Implements inventoryItem DELETE method. Deletes an inventoryItem.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: inventory_item_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the inventoryItem. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/vmmServers/{vmm_server_name}/inventoryItems?api-version=2023-10-07:
    get:
      operationId: InventoryItems_list_by_vmm_server
      description: Implements GET for the list of Inventory Items in the VMMServer. Returns the list of inventoryItems in the given VmmServer.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedInventoryItemFlattened"
  /providers/Microsoft.ScVmm/operations?api-version=2023-10-07:
    get:
      operationId: scvmm_list
      description: List the operations for the provider.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedOperation"
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default?api-version=2023-10-07:
    get:
      operationId: VirtualMachineInstances_get
      description: Gets a virtual machine. Retrieves information about a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineInstanceFlattened"
    put:
      operationId: VirtualMachineInstances_begin_create_or_update
      description: Implements virtual machine PUT method. The operation to create or update a virtual machine instance. Please note some properties can be set only during virtual machine instance creation.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/VirtualMachineInstanceProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: Gets or sets the extended location. Required.
              required:
                - extendedLocation
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineInstance"
    patch:
      operationId: VirtualMachineInstances_begin_update
      description: Updates a virtual machine. The operation to update a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/VirtualMachineInstanceUpdateProperties"
                  description: The update properties of the VirtualMachineInstance.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineInstance"
    delete:
      operationId: VirtualMachineInstances_begin_delete
      description: Deletes an virtual machine. The operation to delete a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
        - name: force
          in: query
          required: false
          schema:
            type: string
          description: 'Forces the resource to be deleted. Known values are: "true" and "false". Default value is None.'
        - name: deleteFromHost
          in: query
          required: false
          schema:
            type: string
          description: 'Whether to disable the VM from azure and also delete it from Vmm. Known values are: "true" and "false". Default value is None.'
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances?api-version=2023-10-07:
    get:
      operationId: VirtualMachineInstances_list
      description: Implements List virtual machine instances. Lists all of the virtual machine instances within the specified parent resource.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineInstanceFlattened"
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/createCheckpoint?api-version=2023-10-07:
    post:
      operationId: VirtualMachineInstances_begin_create_checkpoint
      description: Implements the operation to creates a checkpoint in a virtual machine instance. Creates a checkpoint in virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of the checkpoint.
                description:
                  type: string
                  description: Description of the checkpoint.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/deleteCheckpoint?api-version=2023-10-07:
    post:
      operationId: VirtualMachineInstances_begin_delete_checkpoint
      description: Implements the operation to delete a checkpoint in a virtual machine instance. Deletes a checkpoint in virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: ID of the checkpoint to be deleted.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/restart?api-version=2023-10-07:
    post:
      operationId: VirtualMachineInstances_begin_restart
      description: Implements the operation to restart a virtual machine. The operation to restart a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/restoreCheckpoint?api-version=2023-10-07:
    post:
      operationId: VirtualMachineInstances_begin_restore_checkpoint
      description: Implements the operation to restores to a checkpoint in a virtual machine instance. Restores to a checkpoint in virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: ID of the checkpoint to be restored to.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/start?api-version=2023-10-07:
    post:
      operationId: VirtualMachineInstances_begin_start
      description: Implements the operation to start a virtual machine. The operation to start a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/stop?api-version=2023-10-07:
    post:
      operationId: VirtualMachineInstances_begin_stop
      description: Implements the operation to stop a virtual machine. The operation to power off (stop) a virtual machine instance.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                skipShutdown:
                  type: string
                  description: 'Gets or sets a value indicating whether to request non-graceful VM shutdown. True value for this flag indicates non-graceful shutdown whereas false indicates otherwise. Defaults to false. Known values are: "true" and "false".'
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/virtualMachineTemplates/{virtual_machine_template_name}?api-version=2023-10-07:
    get:
      operationId: VirtualMachineTemplates_get
      description: Gets a VirtualMachineTemplate. Implements VirtualMachineTemplate GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualMachineTemplate. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineTemplateFlattened"
    put:
      operationId: VirtualMachineTemplates_begin_create_or_update
      description: Implements VirtualMachineTemplates PUT method. Onboards the ScVmm VM Template as an Azure VM Template resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualMachineTemplate. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/VirtualMachineTemplateProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extended location. Required.
              required:
                - location
                - extendedLocation
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineTemplate"
    patch:
      operationId: VirtualMachineTemplates_begin_update
      description: Implements the VirtualMachineTemplate PATCH method. Updates the VirtualMachineTemplate resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualMachineTemplate. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualMachineTemplate"
    delete:
      operationId: VirtualMachineTemplates_begin_delete
      description: Implements VirtualMachineTemplate DELETE method. Deregisters the ScVmm VM Template from Azure.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_machine_template_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualMachineTemplate. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: string
          description: 'Forces the resource to be deleted. Known values are: "true" and "false". Default value is None.'
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ScVmm/virtualMachineTemplates?api-version=2023-10-07:
    get:
      operationId: VirtualMachineTemplates_list_by_subscription
      description: Implements GET VirtualMachineTemplates in a subscription. List of VirtualMachineTemplates in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineTemplateFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/virtualMachineTemplates?api-version=2023-10-07:
    get:
      operationId: VirtualMachineTemplates_list_by_resource_group
      description: Implements GET VirtualMachineTemplates in a resource group. List of VirtualMachineTemplates in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualMachineTemplateFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/virtualNetworks/{virtual_network_name}?api-version=2023-10-07:
    get:
      operationId: VirtualNetworks_get
      description: Gets a VirtualNetwork. Implements VirtualNetwork GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualNetwork. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetworkFlattened"
    put:
      operationId: VirtualNetworks_begin_create_or_update
      description: Implements VirtualNetworks PUT method. Onboards the ScVmm virtual network as an Azure virtual network resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualNetwork. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/VirtualNetworkProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extended location. Required.
              required:
                - location
                - extendedLocation
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetwork"
    patch:
      operationId: VirtualNetworks_begin_update
      description: Implements the VirtualNetworks PATCH method. Updates the VirtualNetworks resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualNetwork. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VirtualNetwork"
    delete:
      operationId: VirtualNetworks_begin_delete
      description: Implements VirtualNetwork DELETE method. Deregisters the ScVmm virtual network from Azure.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: virtual_network_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VirtualNetwork. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: string
          description: 'Forces the resource to be deleted. Known values are: "true" and "false". Default value is None.'
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ScVmm/virtualNetworks?api-version=2023-10-07:
    get:
      operationId: VirtualNetworks_list_by_subscription
      description: Implements GET VirtualNetworks in a subscription. List of VirtualNetworks in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualNetworkFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/virtualNetworks?api-version=2023-10-07:
    get:
      operationId: VirtualNetworks_list_by_resource_group
      description: Implements GET VirtualNetworks in a resource group. List of VirtualNetworks in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVirtualNetworkFlattened"
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/hybridIdentityMetadata/default?api-version=2023-10-07:
    get:
      operationId: VmInstanceHybridIdentityMetadatas_get
      description: Gets HybridIdentityMetadata. Implements HybridIdentityMetadata GET method.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VmInstanceHybridIdentityMetadataFlattened"
  /{resource_uri}/providers/Microsoft.ScVmm/virtualMachineInstances/default/hybridIdentityMetadata?api-version=2023-10-07:
    get:
      operationId: VmInstanceHybridIdentityMetadatas_list_by_virtual_machine_instance
      description: Implements GET HybridIdentityMetadata in a vm. Returns the list of HybridIdentityMetadata of the given VM.
      parameters:
        - name: resource_uri
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified Azure Resource manager identifier of the resource. Required.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVmInstanceHybridIdentityMetadataFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/vmmServers/{vmm_server_name}?api-version=2023-10-07:
    get:
      operationId: VmmServers_get
      description: Gets a VMMServer. Implements VmmServer GET method.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VmmServerFlattened"
    put:
      operationId: VmmServers_begin_create_or_update
      description: Implements VmmServers PUT method. Onboards the SCVmm fabric as an Azure VmmServer resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/VmmServerProperties"
                  description: The resource-specific properties for this resource.
                extendedLocation:
                  $ref: "#/components/schemas/ExtendedLocation"
                  description: The extended location. Required.
              required:
                - location
                - extendedLocation
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VmmServer"
    patch:
      operationId: VmmServers_begin_update
      description: Implements VmmServers PATCH method. Updates the VmmServers resource.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VmmServer"
    delete:
      operationId: VmmServers_begin_delete
      description: Implements VmmServers DELETE method. Removes the SCVmm fabric from Azure.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: vmm_server_name
          in: path
          required: true
          schema:
            type: string
          description: Name of the VmmServer. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          required: false
          schema:
            type: string
          description: 'Forces the resource to be deleted. Known values are: "true" and "false". Default value is None.'
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/providers/Microsoft.ScVmm/vmmServers?api-version=2023-10-07:
    get:
      operationId: VmmServers_list_by_subscription
      description: Implements GET VmmServers in a subscription. List of VmmServers in a subscription.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVmmServerFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ScVmm/vmmServers?api-version=2023-10-07:
    get:
      operationId: VmmServers_list_by_resource_group
      description: Implements GET VmmServers in a resource group. List of VmmServers in a resource group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedVmmServerFlattened"
