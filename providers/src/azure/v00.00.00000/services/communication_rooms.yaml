openapi: 3.0.0
servers:
  - url: https://{endpoint}
    variables:
      endpoint:
        default: ""
        description: The service endpoint host (no scheme), e.g. myaccount.table.cosmos.azure.com:443 - value of the client `endpoint` parameter.
info:
  title: Rooms Client
  description: A client to interact with the AzureCommunicationService Rooms gateway. This client provides operations to manage rooms.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2025-03-13-stackql-generated
  x-serviceAlias: communication_rooms
  x-sdkPackage: azure-communication-rooms
  x-plane: data
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas: {}
  x-stackQL-resources:
    participants:
      id: azure.communication_rooms.participants
      name: participants
      title: participants
      methods:
        list_raw:
          operation:
            $ref: "#/paths/~1rooms~1{room_id}~1participants?api-version=2025-03-13/get"
          response:
            openAPIDocKey: "200"
        update:
          operation:
            $ref: "#/paths/~1rooms~1{room_id}~1participants?api-version=2025-03-13/patch"
          response:
            openAPIDocKey: "200"
      sqlVerbs:
        select: []
        insert: []
        update:
          - $ref: "#/components/x-stackQL-resources/participants/methods/update"
        delete: []
    rooms:
      id: azure.communication_rooms.rooms
      name: rooms
      title: rooms
      methods:
        get_raw:
          operation:
            $ref: "#/paths/~1rooms~1{room_id}?api-version=2025-03-13/get"
          response:
            openAPIDocKey: "200"
        update:
          operation:
            $ref: "#/paths/~1rooms~1{room_id}?api-version=2025-03-13/patch"
          response:
            openAPIDocKey: "200"
        delete:
          operation:
            $ref: "#/paths/~1rooms~1{room_id}?api-version=2025-03-13/delete"
          response:
            openAPIDocKey: "204"
        list_raw:
          operation:
            $ref: "#/paths/~1rooms?api-version=2025-03-13/get"
          response:
            openAPIDocKey: "200"
        create:
          operation:
            $ref: "#/paths/~1rooms?api-version=2025-03-13/post"
          response:
            openAPIDocKey: "200"
      sqlVerbs:
        select: []
        insert:
          - $ref: "#/components/x-stackQL-resources/rooms/methods/create"
        update:
          - $ref: "#/components/x-stackQL-resources/rooms/methods/update"
        delete:
          - $ref: "#/components/x-stackQL-resources/rooms/methods/delete"
paths:
  /rooms/{room_id}/participants?api-version=2025-03-13:
    get:
      operationId: Participants_list
      description: Get participants in a room. Get participants in a room.
      parameters:
        - name: room_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the room to get participants from. Required.
      responses:
        "200":
          description: Success
    patch:
      operationId: Participants_update
      description: Update participants in a room. Update participants in a room.
      parameters:
        - name: room_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the room to update the participants in. Required.
      responses:
        "200":
          description: Success
  /rooms/{room_id}?api-version=2025-03-13:
    get:
      operationId: Rooms_get
      description: Retrieves an existing room by id. Retrieves an existing room by id.
      parameters:
        - name: room_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the room requested. Required.
      responses:
        "200":
          description: Success
    patch:
      operationId: Rooms_update
      description: Update a room with given changes. Update a room with given changes.
      parameters:
        - name: room_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the room requested. Required.
      responses:
        "200":
          description: Success
    delete:
      operationId: Rooms_delete
      description: Delete a room. Delete a room.
      parameters:
        - name: room_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the room to be deleted. Required.
      responses:
        "204":
          description: Success (no content / accepted)
  /rooms?api-version=2025-03-13:
    get:
      operationId: Rooms_list
      description: Retrieves all created rooms. Retrieves all created rooms.
      parameters: []
      responses:
        "200":
          description: Success
    post:
      operationId: Rooms_create
      description: Creates a new room. Creates a new room.
      parameters: []
      responses:
        "200":
          description: Success
