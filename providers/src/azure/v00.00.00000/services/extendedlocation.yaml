openapi: 3.0.0
servers:
  - url: https://management.azure.com/
info:
  title: Custom Locations Mgmt Client
  description: The customLocations Rest API spec.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2021-08-31-preview-stackql-generated
  x-serviceAlias: extendedlocation
  x-sdkPackage: azure-mgmt-extendedlocation
  x-plane: mgmt
security:
  - azure_auth:
      - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    SystemData:
      type: object
      description: Metadata pertaining to creation and last modification of the resource.
      properties:
        createdBy:
          type: string
          description: The identity that created the resource.
        createdByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that created the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        createdAt:
          type: string
          format: date-time
          description: The timestamp of resource creation (UTC).
        lastModifiedBy:
          type: string
          description: The identity that last modified the resource.
        lastModifiedByType:
          $ref: "#/components/schemas/CreatedByType"
          description: 'The type of identity that last modified the resource. Known values are: "User", "Application", "ManagedIdentity", and "Key".'
        lastModifiedAt:
          type: string
          format: date-time
          description: The timestamp of resource last modification (UTC).
    CreatedByType:
      type: string
      enum:
        - User
        - Application
        - ManagedIdentity
        - Key
    CustomLocationProperties:
      type: object
      description: Properties for a custom location.
      properties:
        authentication:
          $ref: "#/components/schemas/CustomLocationPropertiesAuthentication"
          description: This is optional input that contains the authentication that should be used to generate the namespace.
        clusterExtensionIds:
          type: array
          items:
            type: string
          description: Contains the reference to the add-on that contains charts to deploy CRDs and operators.
        displayName:
          type: string
          description: Display name for the Custom Locations location.
        hostResourceId:
          type: string
          description: Connected Cluster or AKS Cluster. The Custom Locations RP will perform a checkAccess API for listAdminCredentials permissions.
        hostType:
          $ref: "#/components/schemas/HostType"
          description: Type of host the Custom Locations is referencing (Kubernetes, etc...). "Kubernetes"
        namespace:
          type: string
          description: Kubernetes namespace that will be created on the specified cluster.
        provisioningState:
          type: string
          description: Provisioning State for the Custom Location.
    CustomLocationPropertiesAuthentication:
      type: object
      description: This is optional input that contains the authentication that should be used to generate the namespace.
      properties:
        type:
          type: string
          description: The type of the Custom Locations authentication.
        value:
          type: string
          description: The kubeconfig value.
    HostType:
      type: string
      enum:
        - Kubernetes
    Identity:
      type: object
      description: Identity for the resource.
      properties:
        principalId:
          type: string
          description: The principal ID of resource identity.
        tenantId:
          type: string
          description: The tenant ID of resource.
        type:
          $ref: "#/components/schemas/ResourceIdentityType"
          description: 'The identity type. Known values are: "SystemAssigned" and "None".'
    ResourceIdentityType:
      type: string
      enum:
        - SystemAssigned
        - None
    CustomLocationFlattened:
      type: object
      description: Flattened view of CustomLocation ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        identity:
          $ref: "#/components/schemas/Identity"
          description: Identity for the resource.
        authentication:
          $ref: "#/components/schemas/CustomLocationPropertiesAuthentication"
          description: This is optional input that contains the authentication that should be used to generate the namespace.
        clusterExtensionIds:
          type: array
          items:
            type: string
          description: Contains the reference to the add-on that contains charts to deploy CRDs and operators.
        displayName:
          type: string
          description: Display name for the Custom Locations location.
        hostResourceId:
          type: string
          description: Connected Cluster or AKS Cluster. The Custom Locations RP will perform a checkAccess API for listAdminCredentials permissions.
        hostType:
          $ref: "#/components/schemas/HostType"
          description: Type of host the Custom Locations is referencing (Kubernetes, etc...). "Kubernetes"
        namespace:
          type: string
          description: Kubernetes namespace that will be created on the specified cluster.
        provisioningState:
          type: string
          description: Provisioning State for the Custom Location.
    PagedCustomLocationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CustomLocationFlattened"
        nextLink:
          type: string
    CustomLocation:
      type: object
      description: Custom Locations definition.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/CustomLocationProperties"
          description: The set of properties specific to a Custom Location.
        identity:
          $ref: "#/components/schemas/Identity"
          description: Identity for the resource.
      required:
        - location
    CustomLocationOperationValueDisplay:
      type: object
      description: Describes the properties of a Custom Locations Operation Value Display.
      properties:
        description:
          type: string
          description: The description of the operation.
        operation:
          type: string
          description: The display name of the compute operation.
        provider:
          type: string
          description: The resource provider for the operation.
        resource:
          type: string
          description: The display name of the resource the operation applies to.
    CustomLocationOperation:
      type: object
      description: Custom Locations operation.
      properties:
        display:
          $ref: "#/components/schemas/CustomLocationOperationValueDisplay"
          description: Describes the properties of a Custom Locations Operation Value Display.
        isDataAction:
          type: boolean
          description: Is this Operation a data plane operation.
        name:
          type: string
          description: The name of the compute operation.
        origin:
          type: string
          description: The origin of the compute operation.
    PagedCustomLocationOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/CustomLocationOperation"
        nextLink:
          type: string
    EnabledResourceTypeProperties:
      type: object
      description: Properties for EnabledResourceType of a custom location.
      properties:
        clusterExtensionId:
          type: string
          description: Cluster Extension ID.
        extensionType:
          type: string
          description: Cluster Extension Type.
        typesMetadata:
          type: array
          items:
            $ref: "#/components/schemas/EnabledResourceTypePropertiesTypesMetadataItem"
          description: Metadata of the Resource Type.
    EnabledResourceTypePropertiesTypesMetadataItem:
      type: object
      description: Metadata of the Resource Type.
      properties:
        apiVersion:
          type: string
          description: Api Version of Resource Type.
        resourceProviderNamespace:
          type: string
          description: Resource Provider Namespace of Resource Type.
        resourceType:
          type: string
          description: Resource Type.
    EnabledResourceTypeFlattened:
      type: object
      description: Flattened view of EnabledResourceType ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        clusterExtensionId:
          type: string
          description: Cluster Extension ID.
        extensionType:
          type: string
          description: Cluster Extension Type.
        typesMetadata:
          type: array
          items:
            $ref: "#/components/schemas/EnabledResourceTypePropertiesTypesMetadataItem"
          description: Metadata of the Resource Type.
    PagedEnabledResourceTypeFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/EnabledResourceTypeFlattened"
        nextLink:
          type: string
    CustomLocationFindTargetResourceGroupResult:
      type: object
      description: The Find Target Resource Group operation response.
      properties:
        matchedResourceSyncRule:
          type: string
          description: The matching resource sync rule is the particular resource sync rule that matched the match expressions and labels and had lowest priority. This is the rule responsible for mapping the target resource to the target resource group.
        targetResourceGroup:
          type: string
          description: The target resource group of matching resource sync rule. The labels from the request will be used to find out matching resource sync rule against the selector property of the resource sync rule. The one with highest priority will be returned if there are multiple matching rules.
    ResourceSyncRuleProperties:
      type: object
      description: Properties for a resource sync rule. For an unmapped custom resource, its labels will be used to find out matching resource sync rules using the selector property of the resource sync rule. If this resource sync rule has highest priority among all matching rules, then the unmapped custom resource will be projected to the target resource group associated with this resource sync rule.
      properties:
        priority:
          type: integer
          description: Priority represents a priority of the Resource Sync Rule.
        provisioningState:
          type: string
          description: Provisioning State for the Resource Sync Rule.
        selector:
          $ref: "#/components/schemas/ResourceSyncRulePropertiesSelector"
          description: A label selector is composed of two parts, matchLabels and matchExpressions. The first part, matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is 'key', the operator is 'In', and the values array contains only 'value'. The second part, matchExpressions is a list of resource selector requirements. Valid operators include In, NotIn, Exists, and DoesNotExist. The values set must be non-empty in the case of In and NotIn. The values set must be empty in the case of Exists and DoesNotExist. All of the requirements, from both matchLabels and matchExpressions must all be satisfied in order to match.
        targetResourceGroup:
          type: string
          description: For an unmapped custom resource, its labels will be used to find matching resource sync rules. If this resource sync rule is one of the matching rules with highest priority, then the unmapped custom resource will be projected to the target resource group associated with this resource sync rule. The user creating this resource sync rule should have write permissions on the target resource group and this write permission will be validated when creating the resource sync rule.
    ResourceSyncRulePropertiesSelector:
      type: object
      description: A label selector is composed of two parts, matchLabels and matchExpressions. The first part, matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is 'key', the operator is 'In', and the values array contains only 'value'. The second part, matchExpressions is a list of resource selector requirements. Valid operators include In, NotIn, Exists, and DoesNotExist. The values set must be non-empty in the case of In and NotIn. The values set must be empty in the case of Exists and DoesNotExist. All of the requirements, from both matchLabels and matchExpressions must all be satisfied in order to match.
      properties:
        matchExpressions:
          type: array
          items:
            $ref: "#/components/schemas/MatchExpressionsProperties"
          description: MatchExpressions is a list of resource selector requirements. Valid operators include In, NotIn, Exists, and DoesNotExist. The values set must be non-empty in the case of In and NotIn. The values set must be empty in the case of Exists and DoesNotExist.
        matchLabels:
          type: object
          properties: {}
          description: MatchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is 'key', the operator is 'In', and the values array contains only 'value'.
    MatchExpressionsProperties:
      type: object
      description: Resource Sync Rules matchExpression property definition.
      properties:
        key:
          type: string
          description: Key is the label key that the selector applies to.
        operator:
          type: string
          description: The Operator field represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
        values:
          type: array
          items:
            type: string
          description: The label value.
    ResourceSyncRuleFlattened:
      type: object
      description: Flattened view of ResourceSyncRule ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        priority:
          type: integer
          description: Priority represents a priority of the Resource Sync Rule.
        provisioningState:
          type: string
          description: Provisioning State for the Resource Sync Rule.
        selector:
          $ref: "#/components/schemas/ResourceSyncRulePropertiesSelector"
          description: A label selector is composed of two parts, matchLabels and matchExpressions. The first part, matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is 'key', the operator is 'In', and the values array contains only 'value'. The second part, matchExpressions is a list of resource selector requirements. Valid operators include In, NotIn, Exists, and DoesNotExist. The values set must be non-empty in the case of In and NotIn. The values set must be empty in the case of Exists and DoesNotExist. All of the requirements, from both matchLabels and matchExpressions must all be satisfied in order to match.
        targetResourceGroup:
          type: string
          description: For an unmapped custom resource, its labels will be used to find matching resource sync rules. If this resource sync rule is one of the matching rules with highest priority, then the unmapped custom resource will be projected to the target resource group associated with this resource sync rule. The user creating this resource sync rule should have write permissions on the target resource group and this write permission will be validated when creating the resource sync rule.
    PagedResourceSyncRuleFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSyncRuleFlattened"
        nextLink:
          type: string
    ResourceSyncRule:
      type: object
      description: Resource Sync Rules definition.
      properties:
        id:
          type: string
          description: Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
        name:
          type: string
          description: The name of the resource.
        type:
          type: string
          description: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts".
        systemData:
          $ref: "#/components/schemas/SystemData"
          description: Azure Resource Manager metadata containing createdBy and modifiedBy information.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        location:
          type: string
          description: The geo-location where the resource lives. Required.
        properties:
          $ref: "#/components/schemas/ResourceSyncRuleProperties"
          description: The set of properties specific to a Resource Sync Rule.
      required:
        - location
  x-stackQL-resources:
    custom_locations:
      id: azure.extendedlocation.custom_locations
      name: custom_locations
      title: custom_locations
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}?api-version=2021-08-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}?api-version=2021-08-31-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}?api-version=2021-08-31-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}?api-version=2021-08-31-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations?api-version=2021-08-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_by_subscription:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1providers~1Microsoft.ExtendedLocation~1customLocations?api-version=2021-08-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        list_operations:
          operation:
            $ref: "#/paths/~1providers~1Microsoft.ExtendedLocation~1operations?api-version=2021-08-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
        list_enabled_resource_types:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}~1enabledResourceTypes?api-version=2021-08-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
        find_target_resource_group:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}~1findTargetResourceGroup?api-version=2021-08-31-preview/post"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/get"
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/list_by_resource_group"
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/list_by_subscription"
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/list_operations"
        insert:
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/custom_locations/methods/delete"
    resource_sync_rules:
      id: azure.extendedlocation.resource_sync_rules
      name: resource_sync_rules
      title: resource_sync_rules
      methods:
        get:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}~1resourceSyncRules~1{child_resource_name}?api-version=2021-08-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: '{{ $row := . }}{ {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }'
        create_or_update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}~1resourceSyncRules~1{child_resource_name}?api-version=2021-08-31-preview/put"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        update:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}~1resourceSyncRules~1{child_resource_name}?api-version=2021-08-31-preview/patch"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
          request:
            nativeCasing: camel
          config:
            requestBodyTranslate:
              algorithm: naive
        delete:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}~1resourceSyncRules~1{child_resource_name}?api-version=2021-08-31-preview/delete"
          response:
            openAPIDocKey: "204"
        list_by_custom_location_id:
          operation:
            $ref: "#/paths/~1subscriptions~1{subscription_id}~1resourceGroups~1{resource_group_name}~1providers~1Microsoft.ExtendedLocation~1customLocations~1{resource_name}~1resourceSyncRules?api-version=2021-08-31-preview/get"
          response:
            openAPIDocKey: "200"
            mediaType: application/json
            objectKey: $.value
            overrideMediaType: application/json
            transform:
              type: golang_template_json_v0.3.0
              body: |-
                {"value": [
                {{- range $i, $row := index . "value" }}{{ if $i }},{{ end }}
                  { {{ $p := index $row "properties" }}{{ if eq (kindOf $p) "map" }}{{ range $k, $v := $p }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}{{ end }}{{ range $k, $v := $row }}{{ printf "%q" $k }}: {{ toJson $v }}, {{ end }}"_flattened": true }
                {{- end }}
                ], "nextLink": {{ toJson (index . "nextLink") }}}
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/resource_sync_rules/methods/get"
          - $ref: "#/components/x-stackQL-resources/resource_sync_rules/methods/list_by_custom_location_id"
        insert:
          - $ref: "#/components/x-stackQL-resources/resource_sync_rules/methods/create_or_update"
        update:
          - $ref: "#/components/x-stackQL-resources/resource_sync_rules/methods/update"
        replace:
          - $ref: "#/components/x-stackQL-resources/resource_sync_rules/methods/create_or_update"
        delete:
          - $ref: "#/components/x-stackQL-resources/resource_sync_rules/methods/delete"
paths:
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ExtendedLocation/customLocations/{resource_name}?api-version=2021-08-31-preview:
    get:
      operationId: CustomLocations_get
      description: Gets a Custom Location. Gets the details of the customLocation with a specified resource group and name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomLocationFlattened"
    put:
      operationId: CustomLocations_begin_create_or_update
      description: Creates or updates a Custom Location. Creates or updates a Custom Location in the specified Subscription and Resource Group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/CustomLocationProperties"
                  description: The set of properties specific to a Custom Location.
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: Identity for the resource.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomLocation"
    patch:
      operationId: CustomLocations_update
      description: Updates a Custom Location. Updates a Custom Location with the specified Resource Name in the specified Resource Group and Subscription.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identity:
                  $ref: "#/components/schemas/Identity"
                  description: Identity for the resource.
                properties:
                  $ref: "#/components/schemas/CustomLocationProperties"
                  description: The Custom Locations patchable properties.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomLocation"
    delete:
      operationId: CustomLocations_begin_delete
      description: Deletes a Custom Location. Deletes the Custom Location with the specified Resource Name, Resource Group, and Subscription Id.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ExtendedLocation/customLocations?api-version=2021-08-31-preview:
    get:
      operationId: CustomLocations_list_by_resource_group
      description: Gets a list of Custom Locations in the specified subscription and resource group. Gets a list of Custom Locations in the specified subscription and resource group. The operation returns properties of each Custom Location.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCustomLocationFlattened"
  /subscriptions/{subscription_id}/providers/Microsoft.ExtendedLocation/customLocations?api-version=2021-08-31-preview:
    get:
      operationId: CustomLocations_list_by_subscription
      description: Gets a list of Custom Locations in a subscription. Gets a list of Custom Locations in the specified subscription. The operation returns properties of each Custom Location.
      parameters:
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCustomLocationFlattened"
  /providers/Microsoft.ExtendedLocation/operations?api-version=2021-08-31-preview:
    get:
      operationId: CustomLocations_list_operations
      description: Lists all available Custom Locations operations.
      parameters: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedCustomLocationOperation"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ExtendedLocation/customLocations/{resource_name}/enabledResourceTypes?api-version=2021-08-31-preview:
    get:
      operationId: CustomLocations_list_enabled_resource_types
      description: Gets the list of Enabled Resource Types. Gets the list of the Enabled Resource Types.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedEnabledResourceTypeFlattened"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ExtendedLocation/customLocations/{resource_name}/findTargetResourceGroup?api-version=2021-08-31-preview:
    post:
      operationId: CustomLocations_find_target_resource_group
      description: Gets matching target resource group for resource sync. Returns the target resource group associated with the resource sync rules of the Custom Location that match the rules passed in with the Find Target Resource Group Request.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                labels:
                  type: object
                  properties: {}
                  description: Labels of the custom resource, this is a map of {key,value} pairs.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomLocationFindTargetResourceGroupResult"
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ExtendedLocation/customLocations/{resource_name}/resourceSyncRules/{child_resource_name}?api-version=2021-08-31-preview:
    get:
      operationId: ResourceSyncRules_get
      description: Gets a Resource Sync Rule. Gets the details of the resourceSyncRule with a specified resource group, subscription id Custom Location resource name and Resource Sync Rule name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: child_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Resource Sync Rule name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceSyncRuleFlattened"
    put:
      operationId: ResourceSyncRules_begin_create_or_update
      description: Creates or updates a Resource Sync Rule. Creates or updates a Resource Sync Rule in the parent Custom Location, Subscription Id and Resource Group.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: child_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Resource Sync Rule name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                location:
                  type: string
                  description: The geo-location where the resource lives. Required.
                properties:
                  $ref: "#/components/schemas/ResourceSyncRuleProperties"
                  description: The set of properties specific to a Resource Sync Rule.
              required:
                - location
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceSyncRule"
    patch:
      operationId: ResourceSyncRules_begin_update
      description: Updates a Resource Sync Rule. Updates a Resource Sync Rule with the specified Resource Sync Rule name in the specified Resource Group, Subscription and Custom Location name.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: child_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Resource Sync Rule name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  $ref: "#/components/schemas/ResourceSyncRuleProperties"
                  description: The Resource Sync Rules patchable properties.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceSyncRule"
    delete:
      operationId: ResourceSyncRules_delete
      description: Deletes a Resource Sync Rule. Deletes the Resource Sync Rule with the specified Resource Sync Rule Name, Custom Location Resource Name, Resource Group, and Subscription Id.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: child_resource_name
          in: path
          required: true
          schema:
            type: string
          description: Resource Sync Rule name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Success (no content / accepted)
  /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.ExtendedLocation/customLocations/{resource_name}/resourceSyncRules?api-version=2021-08-31-preview:
    get:
      operationId: ResourceSyncRules_list_by_custom_location_id
      description: Lists all Resource Sync Rules in a Custom Location. Gets a list of Resource Sync Rules in the specified subscription. The operation returns properties of each Resource Sync Rule.
      parameters:
        - name: resource_group_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the resource group. The name is case insensitive. Required.
        - name: resource_name
          in: path
          required: true
          schema:
            type: string
          description: Custom Locations name. Required.
        - name: subscription_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResourceSyncRuleFlattened"
