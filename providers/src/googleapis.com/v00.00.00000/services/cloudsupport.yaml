components:
  parameters:
    _.xgafv:
      description: V1 error format.
      in: query
      name: $.xgafv
      schema:
        enum:
          - '1'
          - '2'
        type: string
    access_token:
      description: OAuth access token.
      in: query
      name: access_token
      schema:
        type: string
    alt:
      description: Data format for response.
      in: query
      name: alt
      schema:
        enum:
          - json
          - media
          - proto
        type: string
    callback:
      description: JSONP
      in: query
      name: callback
      schema:
        type: string
    fields:
      description: Selector specifying which fields to include in a partial response.
      in: query
      name: fields
      schema:
        type: string
    key:
      description: >-
        API key. Your API key identifies your project and provides you with API
        access, quota, and reports. Required unless you provide an OAuth 2.0
        token.
      in: query
      name: key
      schema:
        type: string
    oauth_token:
      description: OAuth 2.0 token for the current user.
      in: query
      name: oauth_token
      schema:
        type: string
    prettyPrint:
      description: Returns response with indentations and line breaks.
      in: query
      name: prettyPrint
      schema:
        type: boolean
    quotaUser:
      description: >-
        Available to use for quota purposes for server-side applications. Can be
        any arbitrary string assigned to a user, but should not exceed 40
        characters.
      in: query
      name: quotaUser
      schema:
        type: string
    uploadType:
      description: Legacy upload protocol for media (e.g. "media", "multipart").
      in: query
      name: uploadType
      schema:
        type: string
    upload_protocol:
      description: Upload protocol for media (e.g. "raw", "multipart").
      in: query
      name: upload_protocol
      schema:
        type: string
  schemas:
    Actor:
      description: >-
        An Actor represents an entity that performed an action. For example, an
        actor could be a user who posted a comment on a support case, a user who
        uploaded an attachment, or a service account that created a support
        case.
      id: Actor
      properties:
        displayName:
          description: >-
            The name to display for the actor. If not provided, it is inferred
            from credentials supplied during case creation. When an email is
            provided, a display name must also be provided. This will be
            obfuscated if the user is a Google Support agent.
          type: string
        email:
          deprecated: true
          description: >-
            The email address of the actor. If not provided, it is inferred from
            the credentials supplied during case creation. When a name is
            provided, an email must also be provided. If the user is a Google
            Support agent, this is obfuscated. This field is deprecated. Use
            `username` instead.
          type: string
        googleSupport:
          description: Output only. Whether the actor is a Google support actor.
          readOnly: true
          type: boolean
        username:
          description: >-
            Output only. The username of the actor. It may look like an email or
            other format provided by the identity provider. If not provided, it
            is inferred from the credentials supplied. When a name is provided,
            a username must also be provided. If the user is a Google Support
            agent, this will not be set.
          readOnly: true
          type: string
      type: object
    Attachment:
      description: >-
        An Attachment contains metadata about a file that was uploaded to a case
        - it is NOT a file itself. That being said, the name of an Attachment
        object can be used to download its accompanying file through the
        `media.download` endpoint. While attachments can be uploaded in the
        console at the same time as a comment, they're associated on a "case"
        level, not a "comment" level.
      id: Attachment
      properties:
        createTime:
          description: Output only. The time at which the attachment was created.
          format: google-datetime
          readOnly: true
          type: string
        creator:
          $ref: '#/components/schemas/Actor'
          description: >-
            Output only. The user who uploaded the attachment. Note, the name
            and email will be obfuscated if the attachment was uploaded by
            Google support.
          readOnly: true
        filename:
          description: The filename of the attachment (e.g. `"graph.jpg"`).
          type: string
        mimeType:
          description: Output only. The MIME type of the attachment (e.g. text/plain).
          readOnly: true
          type: string
        name:
          description: Output only. Identifier. The resource name of the attachment.
          readOnly: true
          type: string
        sizeBytes:
          description: Output only. The size of the attachment in bytes.
          format: int64
          readOnly: true
          type: string
      type: object
    Blobstore2Info:
      description: '# gdata.* are outside protos with mising documentation'
      id: Blobstore2Info
      properties:
        blobGeneration:
          description: '# gdata.* are outside protos with mising documentation'
          format: int64
          type: string
        blobId:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        downloadExternalReadToken:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        downloadReadHandle:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        readToken:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        uploadFragmentListCreationInfo:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        uploadMetadataContainer:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
      type: object
    Case:
      description: >-
        A Case is an object that contains the details of a support case. It
        contains fields for the time it was created, its priority, its
        classification, and more. Cases can also have comments and attachments
        that get added over time. A case is parented by a Google Cloud
        organization or project. Organizations are identified by a number, so
        the name of a case parented by an organization would look like this: ```
        organizations/123/cases/456 ``` Projects have two unique identifiers, an
        ID and a number, and they look like this: ``` projects/abc/cases/456 ```
        ``` projects/123/cases/456 ``` You can use either of them when calling
        the API. To learn more about project identifiers, see
        [AIP-2510](https://google.aip.dev/cloud/2510).
      id: Case
      properties:
        classification:
          $ref: '#/components/schemas/CaseClassification'
          description: The issue classification applicable to this case.
        contactEmail:
          description: >-
            A user-supplied email address to send case update notifications for.
            This should only be used in BYOID flows, where we cannot infer the
            user's email address directly from their EUCs.
          type: string
        createTime:
          description: Output only. The time this case was created.
          format: google-datetime
          readOnly: true
          type: string
        creator:
          $ref: '#/components/schemas/Actor'
          description: >-
            The user who created the case. Note: The name and email will be
            obfuscated if the case was created by Google Support.
        description:
          description: A broad description of the issue.
          type: string
        displayName:
          description: The short summary of the issue reported in this case.
          type: string
        escalated:
          description: Whether the case is currently escalated.
          type: boolean
        languageCode:
          description: >-
            The language the user has requested to receive support in. This
            should be a BCP 47 language code (e.g., `"en"`, `"zh-CN"`,
            `"zh-TW"`, `"ja"`, `"ko"`). If no language or an unsupported
            language is specified, this field defaults to English (en). Language
            selection during case creation may affect your available support
            options. For a list of supported languages and their support working
            hours, see:
            https://cloud.google.com/support/docs/language-working-hours
          type: string
        name:
          description: Identifier. The resource name for the case.
          type: string
        priority:
          description: The priority of this case.
          enum:
            - PRIORITY_UNSPECIFIED
            - P0
            - P1
            - P2
            - P3
            - P4
          enumDescriptions:
            - Priority is undefined or has not been set yet.
            - Extreme impact on a production service. Service is hard down.
            - >-
              Critical impact on a production service. Service is currently
              unusable.
            - >-
              Severe impact on a production service. Service is usable but
              greatly impaired.
            - >-
              Medium impact on a production service. Service is available, but
              moderately impaired.
            - >-
              General questions or minor issues. Production service is fully
              available.
          type: string
        state:
          description: Output only. The current status of the support case.
          enum:
            - STATE_UNSPECIFIED
            - NEW
            - IN_PROGRESS_GOOGLE_SUPPORT
            - ACTION_REQUIRED
            - SOLUTION_PROVIDED
            - CLOSED
          enumDescriptions:
            - Case is in an unknown state.
            - >-
              The case has been created but no one is assigned to work on it
              yet.
            - The case is currently being handled by Google support.
            - Google is waiting for a response.
            - A solution has been offered for the case, but it isn't yet closed.
            - The case has been resolved.
          readOnly: true
          type: string
        subscriberEmailAddresses:
          description: The email addresses to receive updates on this case.
          items:
            type: string
          type: array
        testCase:
          description: >-
            Whether this case was created for internal API testing and should
            not be acted on by the support team.
          type: boolean
        timeZone:
          description: >-
            The timezone of the user who created the support case. It should be
            in a format IANA recognizes: https://www.iana.org/time-zones. There
            is no additional validation done by the API.
          type: string
        updateTime:
          description: Output only. The time this case was last updated.
          format: google-datetime
          readOnly: true
          type: string
      type: object
    CaseClassification:
      description: >-
        A Case Classification represents the topic that a case is about. It's
        very important to use accurate classifications, because they're used to
        route your cases to specialists who can help you. A classification
        always has an ID that is its unique identifier. A valid ID is required
        when creating a case.
      id: CaseClassification
      properties:
        displayName:
          description: >-
            A display name for the classification. The display name is not
            static and can change. To uniquely and consistently identify
            classifications, use the `CaseClassification.id` field.
          type: string
        id:
          description: >-
            The unique ID for a classification. Must be specified for case
            creation. To retrieve valid classification IDs for case creation,
            use `caseClassifications.search`. Classification IDs returned by
            `caseClassifications.search` are guaranteed to be valid for at least
            6 months. If a given classification is deactiveated, it will
            immediately stop being returned. After 6 months, `case.create`
            requests using the classification ID will fail.
          type: string
      type: object
    CloseCaseRequest:
      description: The request message for the CloseCase endpoint.
      id: CloseCaseRequest
      properties: {}
      type: object
    Comment:
      description: >-
        A comment associated with a support case. Case comments are the primary
        way for Google Support to communicate with a user who has opened a case.
        When a user responds to Google Support, the user's responses also appear
        as comments.
      id: Comment
      properties:
        body:
          description: The full comment body. Maximum of 12800 characters.
          type: string
        createTime:
          description: Output only. The time when the comment was created.
          format: google-datetime
          readOnly: true
          type: string
        creator:
          $ref: '#/components/schemas/Actor'
          description: >-
            Output only. The user or Google Support agent who created the
            comment.
          readOnly: true
        name:
          description: Output only. Identifier. The resource name of the comment.
          readOnly: true
          type: string
        plainTextBody:
          deprecated: true
          description: >-
            Output only. DEPRECATED. DO NOT USE. A duplicate of the `body`
            field. This field is only present for legacy reasons.
          readOnly: true
          type: string
      type: object
    CompositeMedia:
      description: '# gdata.* are outside protos with mising documentation'
      id: CompositeMedia
      properties:
        blobRef:
          deprecated: true
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        blobstore2Info:
          $ref: '#/components/schemas/Blobstore2Info'
          description: '# gdata.* are outside protos with mising documentation'
        cosmoBinaryReference:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        crc32cHash:
          description: '# gdata.* are outside protos with mising documentation'
          format: uint32
          type: integer
        inline:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        length:
          description: '# gdata.* are outside protos with mising documentation'
          format: int64
          type: string
        md5Hash:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        objectId:
          $ref: '#/components/schemas/ObjectId'
          description: '# gdata.* are outside protos with mising documentation'
        path:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        referenceType:
          description: '# gdata.* are outside protos with mising documentation'
          enum:
            - PATH
            - BLOB_REF
            - INLINE
            - BIGSTORE_REF
            - COSMO_BINARY_REFERENCE
          enumDescriptions:
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
          type: string
        sha1Hash:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
      type: object
    ContentTypeInfo:
      description: '# gdata.* are outside protos with mising documentation'
      id: ContentTypeInfo
      properties:
        bestGuess:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        fromBytes:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        fromFileName:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        fromFusionId:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        fromHeader:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        fromUrlPath:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        fusionIdDetectionMetadata:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
      type: object
    CreateAttachmentRequest:
      description: The request message for the CreateAttachment endpoint.
      id: CreateAttachmentRequest
      properties:
        attachment:
          $ref: '#/components/schemas/Attachment'
          description: Required. The attachment to be created.
      type: object
    DiffChecksumsResponse:
      description: '# gdata.* are outside protos with mising documentation'
      id: DiffChecksumsResponse
      properties:
        checksumsLocation:
          $ref: '#/components/schemas/CompositeMedia'
          description: '# gdata.* are outside protos with mising documentation'
        chunkSizeBytes:
          description: '# gdata.* are outside protos with mising documentation'
          format: int64
          type: string
        objectLocation:
          $ref: '#/components/schemas/CompositeMedia'
          description: '# gdata.* are outside protos with mising documentation'
        objectSizeBytes:
          description: '# gdata.* are outside protos with mising documentation'
          format: int64
          type: string
        objectVersion:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
      type: object
    DiffDownloadResponse:
      description: '# gdata.* are outside protos with mising documentation'
      id: DiffDownloadResponse
      properties:
        objectLocation:
          $ref: '#/components/schemas/CompositeMedia'
          description: '# gdata.* are outside protos with mising documentation'
      type: object
    DiffUploadRequest:
      description: '# gdata.* are outside protos with mising documentation'
      id: DiffUploadRequest
      properties:
        checksumsInfo:
          $ref: '#/components/schemas/CompositeMedia'
          description: '# gdata.* are outside protos with mising documentation'
        objectInfo:
          $ref: '#/components/schemas/CompositeMedia'
          description: '# gdata.* are outside protos with mising documentation'
        objectVersion:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
      type: object
    DiffUploadResponse:
      description: '# gdata.* are outside protos with mising documentation'
      id: DiffUploadResponse
      properties:
        objectVersion:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        originalObject:
          $ref: '#/components/schemas/CompositeMedia'
          description: '# gdata.* are outside protos with mising documentation'
      type: object
    DiffVersionResponse:
      description: '# gdata.* are outside protos with mising documentation'
      id: DiffVersionResponse
      properties:
        objectSizeBytes:
          description: '# gdata.* are outside protos with mising documentation'
          format: int64
          type: string
        objectVersion:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
      type: object
    DownloadParameters:
      description: '# gdata.* are outside protos with mising documentation'
      id: DownloadParameters
      properties:
        allowGzipCompression:
          description: '# gdata.* are outside protos with mising documentation'
          type: boolean
        ignoreRange:
          description: '# gdata.* are outside protos with mising documentation'
          type: boolean
      type: object
    Empty:
      description: >-
        A generic empty message that you can re-use to avoid defining duplicated
        empty messages in your APIs. A typical example is to use it as the
        request or the response type of an API method. For instance: service Foo
        { rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); }
      id: Empty
      properties: {}
      type: object
    EscalateCaseRequest:
      description: The request message for the EscalateCase endpoint.
      id: EscalateCaseRequest
      properties:
        escalation:
          $ref: '#/components/schemas/Escalation'
          description: The escalation information to be sent with the escalation request.
      type: object
    Escalation:
      description: An escalation of a support case.
      id: Escalation
      properties:
        justification:
          description: >-
            Required. A free text description to accompany the `reason` field
            above. Provides additional context on why the case is being
            escalated.
          type: string
        reason:
          description: Required. The reason why the Case is being escalated.
          enum:
            - REASON_UNSPECIFIED
            - RESOLUTION_TIME
            - TECHNICAL_EXPERTISE
            - BUSINESS_IMPACT
          enumDescriptions:
            - >-
              The escalation reason is in an unknown state or has not been
              specified.
            - The case is taking too long to resolve.
            - >-
              The support agent does not have the expertise required to
              successfully resolve the issue.
            - The issue is having a significant business impact.
          type: string
      type: object
    ExpungeSupportEventSubscriptionRequest:
      description: Request message for ExpungeSupportEventSubscription.
      id: ExpungeSupportEventSubscriptionRequest
      properties: {}
      type: object
    ListAttachmentsResponse:
      description: The response message for the ListAttachments endpoint.
      id: ListAttachmentsResponse
      properties:
        attachments:
          description: The list of attachments associated with a case.
          items:
            $ref: '#/components/schemas/Attachment'
          type: array
        nextPageToken:
          description: >-
            A token to retrieve the next page of results. Set this in the
            `page_token` field of subsequent `cases.attachments.list` requests.
            If unspecified, there are no more results to retrieve.
          type: string
      type: object
    ListCasesResponse:
      description: The response message for the ListCases endpoint.
      id: ListCasesResponse
      properties:
        cases:
          description: >-
            The list of cases associated with the parent after any filters have
            been applied.
          items:
            $ref: '#/components/schemas/Case'
          type: array
        nextPageToken:
          description: >-
            A token to retrieve the next page of results. Set this in the
            `page_token` field of subsequent `cases.list` requests. If
            unspecified, there are no more results to retrieve.
          type: string
      type: object
    ListCommentsResponse:
      description: The response message for the ListComments endpoint.
      id: ListCommentsResponse
      properties:
        comments:
          description: List of the comments associated with the case.
          items:
            $ref: '#/components/schemas/Comment'
          type: array
        nextPageToken:
          description: >-
            A token to retrieve the next page of results. Set this in the
            `page_token` field of subsequent `cases.comments.list` requests. If
            unspecified, there are no more results to retrieve.
          type: string
      type: object
    ListSupportEventSubscriptionsResponse:
      description: Response message for ListSupportEventSubscriptions.
      id: ListSupportEventSubscriptionsResponse
      properties:
        nextPageToken:
          description: >-
            A token, which can be sent as `page_token` to retrieve the next
            page. If this field is omitted, there are no subsequent pages.
          type: string
        supportEventSubscriptions:
          description: The support event subscriptions.
          items:
            $ref: '#/components/schemas/SupportEventSubscription'
          type: array
      type: object
    Media:
      description: '# gdata.* are outside protos with mising documentation'
      id: Media
      properties:
        algorithm:
          deprecated: true
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        bigstoreObjectRef:
          deprecated: true
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        blobRef:
          deprecated: true
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        blobstore2Info:
          $ref: '#/components/schemas/Blobstore2Info'
          description: '# gdata.* are outside protos with mising documentation'
        compositeMedia:
          description: '# gdata.* are outside protos with mising documentation'
          items:
            $ref: '#/components/schemas/CompositeMedia'
          type: array
        contentType:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        contentTypeInfo:
          $ref: '#/components/schemas/ContentTypeInfo'
          description: '# gdata.* are outside protos with mising documentation'
        cosmoBinaryReference:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        crc32cHash:
          description: '# gdata.* are outside protos with mising documentation'
          format: uint32
          type: integer
        diffChecksumsResponse:
          $ref: '#/components/schemas/DiffChecksumsResponse'
          description: '# gdata.* are outside protos with mising documentation'
        diffDownloadResponse:
          $ref: '#/components/schemas/DiffDownloadResponse'
          description: '# gdata.* are outside protos with mising documentation'
        diffUploadRequest:
          $ref: '#/components/schemas/DiffUploadRequest'
          description: '# gdata.* are outside protos with mising documentation'
        diffUploadResponse:
          $ref: '#/components/schemas/DiffUploadResponse'
          description: '# gdata.* are outside protos with mising documentation'
        diffVersionResponse:
          $ref: '#/components/schemas/DiffVersionResponse'
          description: '# gdata.* are outside protos with mising documentation'
        downloadParameters:
          $ref: '#/components/schemas/DownloadParameters'
          description: '# gdata.* are outside protos with mising documentation'
        filename:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        hash:
          deprecated: true
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        hashVerified:
          description: '# gdata.* are outside protos with mising documentation'
          type: boolean
        inline:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        isPotentialRetry:
          description: '# gdata.* are outside protos with mising documentation'
          type: boolean
        length:
          description: '# gdata.* are outside protos with mising documentation'
          format: int64
          type: string
        md5Hash:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        mediaId:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        objectId:
          $ref: '#/components/schemas/ObjectId'
          description: '# gdata.* are outside protos with mising documentation'
        path:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        referenceType:
          description: '# gdata.* are outside protos with mising documentation'
          enum:
            - PATH
            - BLOB_REF
            - INLINE
            - GET_MEDIA
            - COMPOSITE_MEDIA
            - BIGSTORE_REF
            - DIFF_VERSION_RESPONSE
            - DIFF_CHECKSUMS_RESPONSE
            - DIFF_DOWNLOAD_RESPONSE
            - DIFF_UPLOAD_REQUEST
            - DIFF_UPLOAD_RESPONSE
            - COSMO_BINARY_REFERENCE
            - ARBITRARY_BYTES
          enumDescriptions:
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
            - '# gdata.* are outside protos with mising documentation'
          type: string
        sha1Hash:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        sha256Hash:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        sha512Hash:
          description: '# gdata.* are outside protos with mising documentation'
          format: byte
          type: string
        timestamp:
          description: '# gdata.* are outside protos with mising documentation'
          format: uint64
          type: string
        token:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
      type: object
    ObjectId:
      description: '# gdata.* are outside protos with mising documentation'
      id: ObjectId
      properties:
        bucketName:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
        generation:
          description: '# gdata.* are outside protos with mising documentation'
          format: int64
          type: string
        objectName:
          description: '# gdata.* are outside protos with mising documentation'
          type: string
      type: object
    SearchCaseClassificationsResponse:
      description: The response message for SearchCaseClassifications endpoint.
      id: SearchCaseClassificationsResponse
      properties:
        caseClassifications:
          description: The classifications retrieved.
          items:
            $ref: '#/components/schemas/CaseClassification'
          type: array
        nextPageToken:
          description: >-
            A token to retrieve the next page of results. Set this in the
            `page_token` field of subsequent `caseClassifications.list`
            requests. If unspecified, there are no more results to retrieve.
          type: string
      type: object
    SearchCasesResponse:
      description: The response message for the SearchCases endpoint.
      id: SearchCasesResponse
      properties:
        cases:
          description: >-
            The list of cases associated with the parent after any filters have
            been applied.
          items:
            $ref: '#/components/schemas/Case'
          type: array
        nextPageToken:
          description: >-
            A token to retrieve the next page of results. Set this in the
            `page_token` field of subsequent `cases.search` requests. If
            unspecified, there are no more results to retrieve.
          type: string
      type: object
    SupportEventSubscription:
      description: A support event subscription.
      id: SupportEventSubscription
      properties:
        createTime:
          description: Output only. The time at which the subscription was created.
          format: google-datetime
          readOnly: true
          type: string
        deleteTime:
          description: Output only. The time at which the subscription was deleted.
          format: google-datetime
          readOnly: true
          type: string
        failureReason:
          description: >-
            Output only. Reason why subscription is failing. State of
            subscription must be FAILING in order for this to have a value.
          enum:
            - FAILURE_REASON_UNSPECIFIED
            - PERMISSION_DENIED
            - TOPIC_NOT_FOUND
            - OTHER
          enumDescriptions:
            - Unspecified failure reason.
            - >-
              The service account (i.e.
              cloud-support-apievents@system.gserviceaccount.com) lacks the
              permission to publish to the customer's Pub/Sub topic.
            - The specified Pub/Sub topic does not exist.
            - Message failed to publish due to a system-side error.
          readOnly: true
          type: string
        name:
          description: Identifier. The resource name of the support event subscription.
          type: string
        pubSubTopic:
          description: >-
            Required. The name of the Pub/Sub topic to publish notifications to.
            Format: projects/{project}/topics/{topic}
          type: string
        purgeTime:
          description: Output only. The time at which the subscription will be purged.
          format: google-datetime
          readOnly: true
          type: string
        state:
          description: Output only. The state of the subscription.
          enum:
            - STATE_UNSPECIFIED
            - WORKING
            - FAILING
            - DELETED
          enumDescriptions:
            - Unspecified state.
            - Subscription is active and working.
            - >-
              Subscription is failing. Notifications cannot be published for
              some reason.
            - >-
              Subscription has been deleted and is pending purge. Notifications
              are not sent for deleted subscriptions. Deleted subscriptions are
              purged after their `purge_time` has passed.
          readOnly: true
          type: string
        updateTime:
          description: Output only. The time at which the subscription was last updated.
          format: google-datetime
          readOnly: true
          type: string
      type: object
    UndeleteSupportEventSubscriptionRequest:
      description: Request message for UndeleteSupportEventSubscription.
      id: UndeleteSupportEventSubscriptionRequest
      properties: {}
      type: object
  securitySchemes:
    Oauth2:
      description: Oauth 2.0 implicit authentication
      flows:
        implicit:
          authorizationUrl: https://accounts.google.com/o/oauth2/auth
          scopes: &ref_0
            https://www.googleapis.com/auth/cloud-platform: >-
              See, edit, configure, and delete your Google Cloud data and see
              the email address for your Google Account.
      type: oauth2
    Oauth2c:
      description: Oauth 2.0 authorization code authentication
      flows:
        authorizationCode:
          authorizationUrl: https://accounts.google.com/o/oauth2/auth
          scopes: *ref_0
          tokenUrl: https://accounts.google.com/o/oauth2/token
      type: oauth2
  x-stackQL-resources:
    attachments:
      id: google.cloudsupport.attachments
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1v2~1{parentType}~1{parent}~1attachments/get'
          response:
            mediaType: application/json
            objectKey: $.attachments
            openAPIDocKey: '200'
      name: attachments
      sqlVerbs:
        delete: []
        insert: []
        replace: []
        select:
          - $ref: '#/components/x-stackQL-resources/attachments/methods/list'
          - $ref: '#/components/x-stackQL-resources/attachments/methods/get'
        update: []
      title: Attachments
    cases:
      id: google.cloudsupport.cases
      methods:
        close:
          operation:
            $ref: '#/paths/~1v2~1{name}:close/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        create:
          operation:
            $ref: '#/paths/~1v2~1{parentType}~1{parent}~1cases/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        escalate:
          operation:
            $ref: '#/paths/~1v2~1{name}:escalate/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1v2~1{parentType}~1{parent}~1cases/get'
          response:
            mediaType: application/json
            objectKey: $.cases
            openAPIDocKey: '200'
        patch:
          operation:
            $ref: '#/paths/~1v2~1{name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        search:
          operation:
            $ref: '#/paths/~1v2~1caseClassifications:search/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      name: cases
      sqlVerbs:
        delete: []
        insert:
          - $ref: '#/components/x-stackQL-resources/cases/methods/create'
        replace: []
        select:
          - $ref: '#/components/x-stackQL-resources/cases/methods/list'
        update:
          - $ref: '#/components/x-stackQL-resources/cases/methods/patch'
      title: Cases
    comments:
      id: google.cloudsupport.comments
      methods:
        create:
          operation:
            $ref: '#/paths/~1v2~1{parentType}~1{parent}~1comments/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1v2~1{parentType}~1{parent}~1comments/get'
          response:
            mediaType: application/json
            objectKey: $.comments
            openAPIDocKey: '200'
      name: comments
      sqlVerbs:
        delete: []
        insert:
          - $ref: '#/components/x-stackQL-resources/comments/methods/create'
        replace: []
        select:
          - $ref: '#/components/x-stackQL-resources/comments/methods/list'
        update: []
      title: Comments
    media:
      id: google.cloudsupport.media
      methods:
        download:
          operation:
            $ref: '#/paths/~1v2~1{name}:download/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        upload:
          operation:
            $ref: '#/paths/~1v2~1{parentType}~1{parent}~1attachments/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      name: media
      sqlVerbs:
        delete: []
        insert: []
        replace: []
        select: []
        update: []
      title: Media
    support_event_subscriptions:
      id: google.cloudsupport.support_event_subscriptions
      methods:
        create:
          operation:
            $ref: >-
              #/paths/~1v2~1organizations~1{organizationsId}~1supportEventSubscriptions/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: >-
              #/paths/~1v2~1organizations~1{organizationsId}~1supportEventSubscriptions~1{supportEventSubscriptionsId}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        expunge:
          operation:
            $ref: >-
              #/paths/~1v2~1organizations~1{organizationsId}~1supportEventSubscriptions~1{supportEventSubscriptionsId}:expunge/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: >-
              #/paths/~1v2~1organizations~1{organizationsId}~1supportEventSubscriptions~1{supportEventSubscriptionsId}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: >-
              #/paths/~1v2~1organizations~1{organizationsId}~1supportEventSubscriptions/get
          response:
            mediaType: application/json
            objectKey: $.supportEventSubscriptions
            openAPIDocKey: '200'
        patch:
          operation:
            $ref: >-
              #/paths/~1v2~1organizations~1{organizationsId}~1supportEventSubscriptions~1{supportEventSubscriptionsId}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        undelete:
          operation:
            $ref: >-
              #/paths/~1v2~1organizations~1{organizationsId}~1supportEventSubscriptions~1{supportEventSubscriptionsId}:undelete/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      name: support_event_subscriptions
      sqlVerbs:
        delete:
          - $ref: >-
              #/components/x-stackQL-resources/support_event_subscriptions/methods/delete
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/support_event_subscriptions/methods/create
        replace: []
        select:
          - $ref: >-
              #/components/x-stackQL-resources/support_event_subscriptions/methods/get
          - $ref: >-
              #/components/x-stackQL-resources/support_event_subscriptions/methods/list
        update:
          - $ref: >-
              #/components/x-stackQL-resources/support_event_subscriptions/methods/patch
      title: Support_event_subscriptions
externalDocs:
  url: https://cloud.google.com/support/docs/apis
info:
  contact:
    email: info@stackql.io
    name: StackQL Studios
    url: https://github.com/stackql/google-discovery-to-openapi
  description: >-
    Manages Google Cloud technical support cases for Customer Care support
    offerings. 
  title: Google Cloud Support API
  version: v2
  x-discovery-doc-revision: '20260809'
  x-generated-date: '2026-08-20'
openapi: 3.1.0
paths:
  /v2/caseClassifications:search:
    get:
      description: >-
        Retrieve valid classifications to use when creating a support case.
        Classifications are hierarchical. Each classification is a string
        containing all levels of the hierarchy separated by `" > "`. For
        example, `"Technical Issue > Compute > Compute Engine"`. Classification
        IDs returned by this endpoint are valid for at least six months. When a
        classification is deactivated, this endpoint immediately stops returning
        it. After six months, `case.create` requests using the classification
        will fail. EXAMPLES: cURL: ```shell curl \ --header "Authorization:
        Bearer $(gcloud auth print-access-token)" \
        'https://cloudsupport.googleapis.com/v2/caseClassifications:search?query=display_name:"*Compute%20Engine*"'
        ``` Python: ```python import googleapiclient.discovery supportApiService
        = googleapiclient.discovery.build( serviceName="cloudsupport",
        version="v2",
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version=v2",
        ) request = supportApiService.caseClassifications().search(
        query='display_name:"*Compute Engine*"' ) print(request.execute()) ```
      operationId: cloudsupport.caseClassifications.search
      parameters:
        - in: query
          name: pageSize
          schema:
            format: int32
            type: integer
        - in: query
          name: pageToken
          schema:
            type: string
        - in: query
          name: query
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchCaseClassificationsResponse'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: &ref_1
      - $ref: '#/components/parameters/_.xgafv'
      - $ref: '#/components/parameters/access_token'
      - $ref: '#/components/parameters/alt'
      - $ref: '#/components/parameters/callback'
      - $ref: '#/components/parameters/fields'
      - $ref: '#/components/parameters/key'
      - $ref: '#/components/parameters/oauth_token'
      - $ref: '#/components/parameters/prettyPrint'
      - $ref: '#/components/parameters/quotaUser'
      - $ref: '#/components/parameters/uploadType'
      - $ref: '#/components/parameters/upload_protocol'
  /v2/organizations/{organizationsId}/supportEventSubscriptions:
    get:
      description: >-
        Lists support event subscriptions. EXAMPLES: cURL: ```shell
        parent="organizations/123456789" curl \ --header "Authorization: Bearer
        $(gcloud auth print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$parent/supportEventSubscriptions"
        ``` Python: ```python import googleapiclient.discovery api_version =
        "v2" supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.supportEventSubscriptions().list(
        parent="organizations/123456789" ) print(request.execute()) ```
      operationId: cloudsupport.organizations.supportEventSubscriptions.list
      parameters:
        - in: path
          name: organizationsId
          required: true
          schema:
            type: string
        - in: query
          name: filter
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            format: int32
            type: integer
        - in: query
          name: pageToken
          schema:
            type: string
        - in: query
          name: showDeleted
          schema:
            type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSupportEventSubscriptionsResponse'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
    post:
      description: >-
        Creates a support event subscription for an organization. EXAMPLES:
        cURL: ```shell parent="organizations/123456789" curl \ --request POST \
        --header "Authorization: Bearer $(gcloud auth print-access-token)" \
        --header 'Content-Type: application/json' \ --data '{ "pub_sub_topic":
        "projects/my-project/topics/my-topic" }' \
        "https://cloudsupport.googleapis.com/v2/$parent/supportEventSubscriptions"
        ``` Python: ```python import googleapiclient.discovery api_version =
        "v2" supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.supportEventSubscriptions().create(
        parent="organizations/123456789", body={ "pub_sub_topic":
        "projects/my-project/topics/my-topic" }, ) print(request.execute()) ```
      operationId: cloudsupport.organizations.supportEventSubscriptions.create
      parameters:
        - in: path
          name: organizationsId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SupportEventSubscription'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportEventSubscription'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/organizations/{organizationsId}/supportEventSubscriptions/{supportEventSubscriptionsId}:
    delete:
      description: >-
        Soft deletes a support event subscription. EXAMPLES: cURL: ```shell
        support_event_subscription="organizations/123456789/supportEventSubscriptions/abcdef123456"
        curl \ --request DELETE \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$support_event_subscription" ```
        Python: ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService).supportEventSubscriptions().delete(
        name="organizations/123456789/supportEventSubscriptions/abcdef123456" )
        print(request.execute()) ```
      operationId: cloudsupport.organizations.supportEventSubscriptions.delete
      parameters:
        - in: path
          name: organizationsId
          required: true
          schema:
            type: string
        - in: path
          name: supportEventSubscriptionsId
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportEventSubscription'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    get:
      description: >-
        Gets a support event subscription. EXAMPLES: cURL: ```shell
        support_event_subscription="organizations/123456789/supportEventSubscriptions/abcdef123456"
        curl \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$support_event_subscription" ```
        Python: ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.supportEventSubscriptions().get(
        name="organizations/123456789/supportEventSubscriptions/abcdef123456" )
        print(request.execute()) ```
      operationId: cloudsupport.organizations.supportEventSubscriptions.get
      parameters:
        - in: path
          name: organizationsId
          required: true
          schema:
            type: string
        - in: path
          name: supportEventSubscriptionsId
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportEventSubscription'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
    patch:
      description: >-
        Updates a support event subscription. EXAMPLES: cURL: ```shell
        support_event_subscription="organizations/123456789/supportEventSubscriptions/abcdef123456"
        curl \ --request PATCH \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \ --header "Content-Type: application/json" \
        --data '{ "pub_sub_topic": "projects/my-project/topics/new-topic" }' \
        "https://cloudsupport.googleapis.com/v2/$support_event_subscription?updateMask=pub_sub_topic"
        ``` Python: ```python import googleapiclient.discovery api_version =
        "v2" supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.supportEventSubscriptions().patch(
        name="organizations/123456789/supportEventSubscriptions/abcdef123456",
        body={ "pub_sub_topic": "projects/my-project/topics/new-topic" }, )
        print(request.execute()) ```
      operationId: cloudsupport.organizations.supportEventSubscriptions.patch
      parameters:
        - in: path
          name: organizationsId
          required: true
          schema:
            type: string
        - in: path
          name: supportEventSubscriptionsId
          required: true
          schema:
            type: string
        - in: query
          name: updateMask
          schema:
            format: google-fieldmask
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SupportEventSubscription'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportEventSubscription'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/organizations/{organizationsId}/supportEventSubscriptions/{supportEventSubscriptionsId}:expunge:
    parameters: *ref_1
    post:
      description: >-
        Expunges a support event subscription. EXAMPLES: cURL: ```shell
        support_event_subscription="organizations/123456789/supportEventSubscriptions/abcdef123456"
        curl \ --request POST \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$support_event_subscription:expunge"
        ``` Python: ```python import googleapiclient.discovery api_version =
        "v2" supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.supportEventSubscriptions().expunge(
        name="organizations/123456789/supportEventSubscriptions/abcdef123456" )
        print(request.execute()) ```
      operationId: cloudsupport.organizations.supportEventSubscriptions.expunge
      parameters:
        - in: path
          name: organizationsId
          required: true
          schema:
            type: string
        - in: path
          name: supportEventSubscriptionsId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExpungeSupportEventSubscriptionRequest'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Empty'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/organizations/{organizationsId}/supportEventSubscriptions/{supportEventSubscriptionsId}:undelete:
    parameters: *ref_1
    post:
      description: >-
        Undeletes a support event subscription. EXAMPLES: cURL: ```shell
        support_event_subscription="organizations/123456789/supportEventSubscriptions/abcdef123456"
        curl \ --request POST \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$support_event_subscription:undelete"
        ``` Python: ```python import googleapiclient.discovery api_version =
        "v2" supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.supportEventSubscriptions().undelete(
        name="organizations/123456789/supportEventSubscriptions/abcdef123456" )
        print(request.execute()) ```
      operationId: cloudsupport.organizations.supportEventSubscriptions.undelete
      parameters:
        - in: path
          name: organizationsId
          required: true
          schema:
            type: string
        - in: path
          name: supportEventSubscriptionsId
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UndeleteSupportEventSubscriptionRequest'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportEventSubscription'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/{name}:
    get:
      description: >-
        Retrieve an attachment associated with a support case. EXAMPLES: cURL:
        ```shell
        attachment="projects/some-project/cases/23598314/attachments/0684M00000P3h1fQAB"
        curl \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$attachment" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = ( supportApiService.cases() .attachments()
        .get(name="projects/some-project/cases/43595344/attachments/0684M00000P3h1fQAB")
        ) print(request.execute()) ```
      operationId: cloudsupport.cases.attachments.get
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
    patch:
      description: >-
        Update a case. Only some fields can be updated. EXAMPLES: cURL: ```shell
        case="projects/some-project/cases/43595344" curl \ --request PATCH \
        --header "Authorization: Bearer $(gcloud auth print-access-token)" \
        --header "Content-Type: application/json" \ --data '{ "priority": "P1"
        }' \ "https://cloudsupport.googleapis.com/v2/$case?updateMask=priority"
        ``` Python: ```python import googleapiclient.discovery api_version =
        "v2" supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.cases().patch(
        name="projects/some-project/cases/43112854", body={ "displayName": "This
        is Now a New Title", "priority": "P2", }, ) print(request.execute()) ```
      operationId: cloudsupport.cases.patch
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
        - in: query
          name: updateMask
          schema:
            format: google-fieldmask
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Case'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Case'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/{name}:close:
    parameters: *ref_1
    post:
      description: >-
        Close a case. EXAMPLES: cURL: ```shell
        case="projects/some-project/cases/43595344" curl \ --request POST \
        --header "Authorization: Bearer $(gcloud auth print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$case:close" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.cases().close(
        name="projects/some-project/cases/43595344" ) print(request.execute())
        ```
      operationId: cloudsupport.cases.close
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloseCaseRequest'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Case'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/{name}:download:
    get:
      description: >-
        Download a file attached to a case. When this endpoint is called, no
        "response body" will be returned. Instead, the attachment's blob will be
        returned. Note: HTTP requests must append "?alt=media" to the URL.
        EXAMPLES: cURL: ```shell
        name="projects/some-project/cases/43594844/attachments/0674M00000WijAnZAJ"
        curl \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$name:download?alt=media" ```
        Python: ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.media().download(
        name="projects/some-project/cases/43595344/attachments/0684M00000Pw6pHQAR"
        ) request.uri = request.uri.split("?")[0] + "?alt=media"
        print(request.execute()) ```
      operationId: cloudsupport.media.download
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Media'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
  /v2/{name}:escalate:
    parameters: *ref_1
    post:
      description: >-
        Escalate a case, starting the Google Cloud Support escalation management
        process. This operation is only available for some support services. Go
        to https://cloud.google.com/support and look for 'Technical support
        escalations' in the feature list to find out which ones let you do that.
        EXAMPLES: cURL: ```shell case="projects/some-project/cases/43595344"
        curl \ --request POST \ --header "Authorization: Bearer $(gcloud auth
        print-access-token)" \ --header "Content-Type: application/json" \
        --data '{ "escalation": { "reason": "BUSINESS_IMPACT", "justification":
        "This is a test escalation." } }' \
        "https://cloudsupport.googleapis.com/v2/$case:escalate" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.cases().escalate(
        name="projects/some-project/cases/43595344", body={ "escalation": {
        "reason": "BUSINESS_IMPACT", "justification": "This is a test
        escalation.", }, }, ) print(request.execute()) ```
      operationId: cloudsupport.cases.escalate
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EscalateCaseRequest'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Case'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/{parentType}/{parent}/attachments:
    get:
      description: >-
        List all the attachments associated with a support case. EXAMPLES: cURL:
        ```shell case="projects/some-project/cases/23598314" curl \ --header
        "Authorization: Bearer $(gcloud auth print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$case/attachments" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = ( supportApiService.cases() .attachments()
        .list(parent="projects/some-project/cases/43595344") )
        print(request.execute()) ```
      operationId: cloudsupport.cases.attachments.list
      parameters:
        - in: path
          name: parentType
          required: true
          schema:
            type: string
        - in: path
          name: parent
          required: true
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            format: int32
            type: integer
        - in: query
          name: pageToken
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAttachmentsResponse'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
    post:
      description: >-
        Create a file attachment on a case or Cloud resource. The attachment
        must have the following fields set: `filename`. EXAMPLES: cURL: ```shell
        echo "This text is in a file I'm uploading using CSAPI." \ >
        "./example_file.txt" case="projects/some-project/cases/43594844" curl \
        --header "Authorization: Bearer $(gcloud auth print-access-token)" \
        --data-binary @"./example_file.txt" \
        "https://cloudsupport.googleapis.com/upload/v2/$case/attachments?attachment.filename=uploaded_via_curl.txt"
        ``` Python: ```python import googleapiclient.discovery api_version =
        "v2" supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) file_path = "./example_file.txt" with open(file_path, "w") as file:
        file.write( "This text is inside a file I'm going to upload using the
        Cloud Support API.", ) request = supportApiService.media().upload(
        parent="projects/some-project/cases/43595344", media_body=file_path )
        request.uri = request.uri.split("?")[0] +
        "?attachment.filename=uploaded_via_python.txt" print(request.execute())
        ```
      operationId: cloudsupport.media.upload
      parameters:
        - in: path
          name: parentType
          required: true
          schema:
            type: string
        - in: path
          name: parent
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAttachmentRequest'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/{parentType}/{parent}/cases:
    get:
      description: >-
        Retrieve all cases under a parent, but not its children. For example,
        listing cases under an organization only returns the cases that are
        directly parented by that organization. To retrieve cases under an
        organization and its projects, use `cases.search`. EXAMPLES: cURL:
        ```shell parent="projects/some-project" curl \ --header "Authorization:
        Bearer $(gcloud auth print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$parent/cases" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request =
        supportApiService.cases().list(parent="projects/some-project")
        print(request.execute()) ```
      operationId: cloudsupport.cases.list
      parameters:
        - in: path
          name: parentType
          required: true
          schema:
            type: string
        - in: path
          name: parent
          required: true
          schema:
            type: string
        - in: query
          name: filter
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            format: int32
            type: integer
        - in: query
          name: pageToken
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCasesResponse'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
    post:
      description: >-
        Create a new case and associate it with a parent. It must have the
        following fields set: `display_name`, `description`, `classification`,
        and `priority`. If you're just testing the API and don't want to route
        your case to an agent, set `testCase=true`. EXAMPLES: cURL: ```shell
        parent="projects/some-project" curl \ --request POST \ --header
        "Authorization: Bearer $(gcloud auth print-access-token)" \ --header
        'Content-Type: application/json' \ --data '{ "display_name": "Test case
        created by me.", "description": "a random test case, feel free to
        close", "classification": { "id":
        "100IK2AKCLHMGRJ9CDGMOCGP8DM6UTB4BT262T31BT1M2T31DHNMENPO6KS36CPJ786L2TBFEHGN6NPI64R3CDHN8880G08I1H3MURR7DHII0GRCDTQM8"
        }, "time_zone": "-07:00", "subscriber_email_addresses": [
        "foo@domain.com", "bar@domain.com" ], "testCase": true, "priority": "P3"
        }' \ "https://cloudsupport.googleapis.com/v2/$parent/cases" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.cases().create(
        parent="projects/some-project", body={ "displayName": "A Test Case",
        "description": "This is a test case.", "testCase": True, "priority":
        "P2", "classification": { "id":
        "100IK2AKCLHMGRJ9CDGMOCGP8DM6UTB4BT262T31BT1M2T31DHNMENPO6KS36CPJ786L2TBFEHGN6NPI64R3CDHN8880G08I1H3MURR7DHII0GRCDTQM8"
        }, }, ) print(request.execute()) ```
      operationId: cloudsupport.cases.create
      parameters:
        - in: path
          name: parentType
          required: true
          schema:
            type: string
        - in: path
          name: parent
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Case'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Case'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
  /v2/{parentType}/{parent}/cases:search:
    get:
      description: >-
        Search for cases using a query. EXAMPLES: cURL: ```shell
        parent="projects/some-project" curl \ --header "Authorization: Bearer
        $(gcloud auth print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$parent/cases:search" ```
        Python: ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = supportApiService.cases().search(
        parent="projects/some-project", query="state=OPEN" )
        print(request.execute()) ```
      operationId: cloudsupport.cases.search
      parameters:
        - in: path
          name: parentType
          required: true
          schema:
            type: string
        - in: path
          name: parent
          required: true
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            format: int32
            type: integer
        - in: query
          name: pageToken
          schema:
            type: string
        - in: query
          name: query
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchCasesResponse'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
  /v2/{parentType}/{parent}/comments:
    get:
      description: >-
        List all the comments associated with a case. EXAMPLES: cURL: ```shell
        case="projects/some-project/cases/43595344" curl \ --header
        "Authorization: Bearer $(gcloud auth print-access-token)" \
        "https://cloudsupport.googleapis.com/v2/$case/comments" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = ( supportApiService.cases() .comments()
        .list(parent="projects/some-project/cases/43595344") )
        print(request.execute()) ```
      operationId: cloudsupport.cases.comments.list
      parameters:
        - in: path
          name: parentType
          required: true
          schema:
            type: string
        - in: path
          name: parent
          required: true
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            format: int32
            type: integer
        - in: query
          name: pageToken
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCommentsResponse'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
    parameters: *ref_1
    post:
      description: >-
        Add a new comment to a case. The comment must have the following fields
        set: `body`. EXAMPLES: cURL: ```shell
        case="projects/some-project/cases/43591344" curl \ --request POST \
        --header "Authorization: Bearer $(gcloud auth print-access-token)" \
        --header 'Content-Type: application/json' \ --data '{ "body": "This is a
        test comment." }' \
        "https://cloudsupport.googleapis.com/v2/$case/comments" ``` Python:
        ```python import googleapiclient.discovery api_version = "v2"
        supportApiService = googleapiclient.discovery.build(
        serviceName="cloudsupport", version=api_version,
        discoveryServiceUrl=f"https://cloudsupport.googleapis.com/$discovery/rest?version={api_version}",
        ) request = ( supportApiService.cases() .comments() .create(
        parent="projects/some-project/cases/43595344", body={"body": "This is a
        test comment."}, ) ) print(request.execute()) ```
      operationId: cloudsupport.cases.comments.create
      parameters:
        - in: path
          name: parentType
          required: true
          schema:
            type: string
        - in: path
          name: parent
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Comment'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comment'
          description: Successful response
      security:
        - Oauth2:
            - https://www.googleapis.com/auth/cloud-platform
          Oauth2c:
            - https://www.googleapis.com/auth/cloud-platform
servers:
  - url: https://cloudsupport.googleapis.com
