openapi: 3.1.0
info:
  contact:
    name: StackQL Studios
    url: 'https://github.com/stackql/google-discovery-to-openapi'
    email: info@stackql.io
  title: Cloud Identity-Aware Proxy API
  description: Controls access to cloud applications running on Google Cloud Platform.
  version: v1
  x-discovery-doc-revision: '20230727'
  x-generated-date: '2023-08-23'
externalDocs:
  url: 'https://cloud.google.com/iap'
servers:
  - url: 'https://iap.googleapis.com'
components:
  securitySchemes:
    Oauth2:
      type: oauth2
      description: Oauth 2.0 implicit authentication
      flows:
        implicit:
          authorizationUrl: 'https://accounts.google.com/o/oauth2/auth'
          scopes: &ref_0
            'https://www.googleapis.com/auth/cloud-platform': 'See, edit, configure, and delete your Google Cloud data and see the email address for your Google Account.'
    Oauth2c:
      type: oauth2
      description: Oauth 2.0 authorization code authentication
      flows:
        authorizationCode:
          authorizationUrl: 'https://accounts.google.com/o/oauth2/auth'
          tokenUrl: 'https://accounts.google.com/o/oauth2/token'
          scopes: *ref_0
  schemas:
    Policy:
      properties:
        etag:
          format: byte
          type: string
          description: '`etag` is used for optimistic concurrency control as a way to help prevent simultaneous updates of a policy from overwriting each other. It is strongly suggested that systems make use of the `etag` in the read-modify-write cycle to perform policy updates in order to avoid race conditions: An `etag` is returned in the response to `getIamPolicy`, and systems are expected to put that etag in the request to `setIamPolicy` to ensure that their change will be applied to the same version of the policy. **Important:** If you use IAM Conditions, you must include the `etag` field whenever you call `setIamPolicy`. If you omit this field, then IAM allows you to overwrite a version `3` policy with a version `1` policy, and all of the conditions in the version `3` policy are lost.'
        version:
          format: int32
          type: integer
          description: 'Specifies the format of the policy. Valid values are `0`, `1`, and `3`. Requests that specify an invalid value are rejected. Any operation that affects conditional role bindings must specify version `3`. This requirement applies to the following operations: * Getting a policy that includes a conditional role binding * Adding a conditional role binding to a policy * Changing a conditional role binding in a policy * Removing any role binding, with or without a condition, from a policy that includes conditions **Important:** If you use IAM Conditions, you must include the `etag` field whenever you call `setIamPolicy`. If you omit this field, then IAM allows you to overwrite a version `3` policy with a version `1` policy, and all of the conditions in the version `3` policy are lost. If a policy does not include any conditions, operations on that policy may specify any valid version or leave the field unset. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).'
        bindings:
          description: 'Associates a list of `members`, or principals, with a `role`. Optionally, may specify a `condition` that determines how and when the `bindings` are applied. Each of the `bindings` must contain at least one principal. The `bindings` in a `Policy` can refer to up to 1,500 principals; up to 250 of these principals can be Google groups. Each occurrence of a principal counts towards these limits. For example, if the `bindings` grant 50 different roles to `user:alice@example.com`, and not to any other principal, then you can add another 1,450 principals to the `bindings` in the `Policy`.'
          type: array
          items:
            $ref: '#/components/schemas/Binding'
      description: 'An Identity and Access Management (IAM) policy, which specifies access controls for Google Cloud resources. A `Policy` is a collection of `bindings`. A `binding` binds one or more `members`, or principals, to a single `role`. Principals can be user accounts, service accounts, Google groups, and domains (such as G Suite). A `role` is a named list of permissions; each `role` can be an IAM predefined role or a user-created custom role. For some types of Google Cloud resources, a `binding` can also specify a `condition`, which is a logical expression that allows access to a resource only if the expression evaluates to `true`. A condition can add constraints based on attributes of the request, the resource, or both. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies). **JSON example:** { "bindings": [ { "role": "roles/resourcemanager.organizationAdmin", "members": [ "user:mike@example.com", "group:admins@example.com", "domain:google.com", "serviceAccount:my-project-id@appspot.gserviceaccount.com" ] }, { "role": "roles/resourcemanager.organizationViewer", "members": [ "user:eve@example.com" ], "condition": { "title": "expirable access", "description": "Does not grant access after Sep 2020", "expression": "request.time < timestamp(''2020-10-01T00:00:00.000Z'')", } } ], "etag": "BwWWja0YfJA=", "version": 3 } **YAML example:** bindings: - members: - user:mike@example.com - group:admins@example.com - domain:google.com - serviceAccount:my-project-id@appspot.gserviceaccount.com role: roles/resourcemanager.organizationAdmin - members: - user:eve@example.com role: roles/resourcemanager.organizationViewer condition: title: expirable access description: Does not grant access after Sep 2020 expression: request.time < timestamp(''2020-10-01T00:00:00.000Z'') etag: BwWWja0YfJA= version: 3 For a description of IAM and its features, see the [IAM documentation](https://cloud.google.com/iam/docs/).'
      id: Policy
      type: object
    CorsSettings:
      description: Allows customers to configure HTTP request paths that'll allow HTTP OPTIONS call to bypass authentication and authorization.
      id: CorsSettings
      properties:
        allowHttpOptions:
          description: 'Configuration to allow HTTP OPTIONS calls to skip authorization. If undefined, IAP will not apply any special logic to OPTIONS requests.'
          type: boolean
      type: object
    AllowedDomainsSettings:
      id: AllowedDomainsSettings
      properties:
        domains:
          items:
            type: string
          description: List of trusted domains.
          type: array
        enable:
          type: boolean
          description: Configuration for customers to opt in for the feature.
      description: Configuration for IAP allowed domains. Lets you to restrict access to an app and allow access to only the domains that you list.
      type: object
    ResetIdentityAwareProxyClientSecretRequest:
      type: object
      id: ResetIdentityAwareProxyClientSecretRequest
      description: The request sent to ResetIdentityAwareProxyClientSecret.
      properties: {}
    ListIdentityAwareProxyClientsResponse:
      description: Response message for ListIdentityAwareProxyClients.
      type: object
      id: ListIdentityAwareProxyClientsResponse
      properties:
        identityAwareProxyClients:
          description: Clients existing in the brand.
          items:
            $ref: '#/components/schemas/IdentityAwareProxyClient'
          type: array
        nextPageToken:
          type: string
          description: 'A token, which can be send as `page_token` to retrieve the next page. If this field is omitted, there are no subsequent pages.'
    OAuthSettings:
      description: Configuration for OAuth login&consent flow behavior as well as for OAuth Credentials.
      type: object
      properties:
        loginHint:
          type: string
          description: 'Domain hint to send as hd=? parameter in OAuth request flow. Enables redirect to primary IDP by skipping Google''s login screen. https://developers.google.com/identity/protocols/OpenIDConnect#hd-param Note: IAP does not verify that the id token''s hd claim matches this value since access behavior is managed by IAM policies.'
      id: OAuthSettings
    GetPolicyOptions:
      properties:
        requestedPolicyVersion:
          format: int32
          type: integer
          description: 'Optional. The maximum policy version that will be used to format the policy. Valid values are 0, 1, and 3. Requests specifying an invalid value will be rejected. Requests for policies with any conditional role bindings must specify version 3. Policies with no conditional role bindings may specify any valid value or leave the field unset. The policy in the response might use the policy version that you specified, or it might use a lower policy version. For example, if you specify version 3, but the policy has no conditional role bindings, the response uses version 1. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).'
      type: object
      id: GetPolicyOptions
      description: Encapsulates settings provided to GetIamPolicy.
    Brand:
      id: Brand
      properties:
        orgInternalOnly:
          description: Output only. Whether the brand is only intended for usage inside the G Suite organization only.
          type: boolean
          readOnly: true
        supportEmail:
          type: string
          description: Support email displayed on the OAuth consent screen.
        name:
          description: 'Output only. Identifier of the brand. NOTE: GCP project number achieves the same brand identification purpose as only one brand per project can be created.'
          readOnly: true
          type: string
        applicationTitle:
          type: string
          description: Application name displayed on OAuth consent screen.
      type: object
      description: 'OAuth brand data. NOTE: Only contains a portion of the data that describes a brand.'
    PolicyDelegationSettings:
      type: object
      id: PolicyDelegationSettings
      properties:
        iamServiceName:
          type: string
          description: 'The DNS name of the service (e.g. "resourcemanager.googleapis.com"). This should be the domain name part of the full resource names (see https://aip.dev/122#full-resource-names), which is usually the same as IamServiceSpec.service of the service where the resource type is defined.'
        iamPermission:
          type: string
          description: Permission to check in IAM.
        resource:
          $ref: '#/components/schemas/Resource'
          description: IAM resource to check permission on
        policyName:
          description: Policy name to be checked
          $ref: '#/components/schemas/PolicyName'
      description: 'PolicyDelegationConfig allows google-internal teams to use IAP for apps hosted in a tenant project. Using these settings, the app can delegate permission check to happen against the linked customer project. This is only ever supposed to be used by google internal teams, hence the restriction on the proto.'
    Expr:
      type: object
      id: Expr
      properties:
        expression:
          description: Textual representation of an expression in Common Expression Language syntax.
          type: string
        description:
          type: string
          description: 'Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.'
        title:
          description: 'Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.'
          type: string
        location:
          type: string
          description: 'Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.'
      description: 'Represents a textual expression in the Common Expression Language (CEL) syntax. CEL is a C-like expression language. The syntax and semantics of CEL are documented at https://github.com/google/cel-spec. Example (Comparison): title: "Summary size limit" description: "Determines if a summary is less than 100 chars" expression: "document.summary.size() < 100" Example (Equality): title: "Requestor is owner" description: "Determines if requestor is the document owner" expression: "document.owner == request.auth.claims.email" Example (Logic): title: "Public documents" description: "Determine whether the document should be publicly visible" expression: "document.type != ''private'' && document.type != ''internal''" Example (Data Manipulation): title: "Notification string" description: "Create a notification string with a timestamp." expression: "''New message received at '' + string(document.create_time)" The exact variables and functions that may be referenced within an expression are determined by the service that evaluates it. See the service documentation for additional information.'
    SetIamPolicyRequest:
      properties:
        policy:
          description: 'REQUIRED: The complete policy to be applied to the `resource`. The size of the policy is limited to a few 10s of KB. An empty policy is a valid policy but certain Google Cloud services (such as Projects) might reject them.'
          $ref: '#/components/schemas/Policy'
      type: object
      id: SetIamPolicyRequest
      description: Request message for `SetIamPolicy` method.
    AccessSettings:
      properties:
        oauthSettings:
          $ref: '#/components/schemas/OAuthSettings'
          description: Settings to configure IAP's OAuth behavior.
        corsSettings:
          $ref: '#/components/schemas/CorsSettings'
          description: Configuration to allow cross-origin requests via IAP.
        policyDelegationSettings:
          $ref: '#/components/schemas/PolicyDelegationSettings'
          description: Settings to configure Policy delegation for apps hosted in tenant projects. INTERNAL_ONLY.
        reauthSettings:
          description: Settings to configure reauthentication policies in IAP.
          $ref: '#/components/schemas/ReauthSettings'
        allowedDomainsSettings:
          description: Settings to configure and enable allowed domains.
          $ref: '#/components/schemas/AllowedDomainsSettings'
        gcipSettings:
          description: GCIP claims and endpoint configurations for 3p identity providers.
          $ref: '#/components/schemas/GcipSettings'
      description: Access related settings for IAP protected apps.
      id: AccessSettings
      type: object
    GcipSettings:
      type: object
      description: Allows customers to configure tenant_id for GCIP instance per-app.
      id: GcipSettings
      properties:
        loginPageUri:
          description: 'Login page URI associated with the GCIP tenants. Typically, all resources within the same project share the same login page, though it could be overridden at the sub resource level.'
          type: string
        tenantIds:
          items:
            type: string
          description: 'GCIP tenant ids that are linked to the IAP resource. tenant_ids could be a string beginning with a number character to indicate authenticating with GCIP tenant flow, or in the format of _ to indicate authenticating with GCIP agent flow. If agent flow is used, tenant_ids should only contain one single element, while for tenant flow, tenant_ids can contain multiple elements.'
          type: array
    AccessDeniedPageSettings:
      id: AccessDeniedPageSettings
      properties:
        accessDeniedPageUri:
          type: string
          description: The URI to be redirected to when access is denied.
        remediationTokenGenerationEnabled:
          description: Whether to generate remediation token on access denied events to this application.
          type: boolean
        generateTroubleshootingUri:
          description: Whether to generate a troubleshooting URL on access denied events to this application.
          type: boolean
      type: object
      description: 'Custom content configuration for access denied page. IAP allows customers to define a custom URI to use as the error page when access is denied to users. If IAP prevents access to this page, the default IAP error page will be displayed instead.'
    Empty:
      type: object
      properties: {}
      id: Empty
      description: 'A generic empty message that you can re-use to avoid defining duplicated empty messages in your APIs. A typical example is to use it as the request or the response type of an API method. For instance: service Foo { rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); }'
    IdentityAwareProxyClient:
      description: Contains the data that describes an Identity Aware Proxy owned client.
      id: IdentityAwareProxyClient
      type: object
      properties:
        secret:
          description: Output only. Client secret of the OAuth client.
          type: string
          readOnly: true
        name:
          type: string
          readOnly: true
          description: Output only. Unique identifier of the OAuth client.
        displayName:
          type: string
          description: Human-friendly name given to the OAuth client.
    ValidateIapAttributeExpressionResponse:
      id: ValidateIapAttributeExpressionResponse
      description: 'API requires a return message, but currently all response strings will fit in the status and public message. In the future, this response can hold AST validation info.'
      properties: {}
      type: object
    CsmSettings:
      id: CsmSettings
      type: object
      description: 'Configuration for RCToken generated for service mesh workloads protected by IAP. RCToken are IAP generated JWTs that can be verified at the application. The RCToken is primarily used for service mesh deployments, and can be scoped to a single mesh by configuring the audience field accordingly.'
      properties:
        rctokenAud:
          description: Audience claim set in the generated RCToken. This value is not validated by IAP.
          type: string
    AttributePropagationSettings:
      properties:
        expression:
          description: 'Raw string CEL expression. Must return a list of attributes. A maximum of 45 attributes can be selected. Expressions can select different attribute types from `attributes`: `attributes.saml_attributes`, `attributes.iap_attributes`. The following functions are supported: - filter `.filter(, )`: Returns a subset of `` where `` is true for every item. - in ` in `: Returns true if `` contains ``. - selectByName `.selectByName()`: Returns the attribute in `` with the given `` name, otherwise returns empty. - emitAs `.emitAs()`: Sets the `` name field to the given `` for propagation in selected output credentials. - strict `.strict()`: Ignores the `x-goog-iap-attr-` prefix for the provided `` when propagating with the `HEADER` output credential, such as request headers. - append `.append()` OR `.append()`: Appends the provided `` or `` to the end of ``. Example expression: `attributes.saml_attributes.filter(x, x.name in [''test'']).append(attributes.iap_attributes.selectByName(''exact'').emitAs(''custom'').strict())`'
          type: string
        enable:
          description: 'Whether the provided attribute propagation settings should be evaluated on user requests. If set to true, attributes returned from the expression will be propagated in the set output credentials.'
          type: boolean
        outputCredentials:
          items:
            enumDescriptions:
              - An output credential is required.
              - Propagate attributes in the headers with "x-goog-iap-attr-" prefix.
              - 'Propagate attributes in the JWT of the form: `"additional_claims": { "my_attribute": ["value1", "value2"] }`'
              - 'Propagate attributes in the RCToken of the form: `"additional_claims": { "my_attribute": ["value1", "value2"] }`'
            type: string
            enum:
              - OUTPUT_CREDENTIALS_UNSPECIFIED
              - HEADER
              - JWT
              - RCTOKEN
          type: array
          description: Which output credentials attributes selected by the CEL expression should be propagated in. All attributes will be fully duplicated in each selected output credential.
      description: Configuration for propagating attributes to applications protected by IAP.
      type: object
      id: AttributePropagationSettings
    PolicyName:
      description: 'An internal name for an IAM policy, based on the resource to which the policy applies. Not to be confused with a resource''s external full resource name. For more information on this distinction, see go/iam-full-resource-names.'
      id: PolicyName
      type: object
      properties:
        id:
          type: string
          description: 'Identifies an instance of the type. ID format varies by type. The ID format is defined in the IAM .service file that defines the type, either in path_mapping or in a comment.'
        region:
          description: 'For Cloud IAM: The location of the Policy. Must be empty or "global" for Policies owned by global IAM. Must name a region from prodspec/cloud-iam-cloudspec for Regional IAM Policies, see go/iam-faq#where-is-iam-currently-deployed. For Local IAM: This field should be set to "local".'
          type: string
        type:
          type: string
          description: 'Resource type. Types are defined in IAM''s .service files. Valid values for type might be ''gce'', ''gcs'', ''project'', ''account'' etc.'
    TestIamPermissionsResponse:
      id: TestIamPermissionsResponse
      properties:
        permissions:
          items:
            type: string
          type: array
          description: A subset of `TestPermissionsRequest.permissions` that the caller is allowed.
      type: object
      description: Response message for `TestIamPermissions` method.
    TestIamPermissionsRequest:
      type: object
      description: Request message for `TestIamPermissions` method.
      properties:
        permissions:
          description: 'The set of permissions to check for the `resource`. Permissions with wildcards (such as `*` or `storage.*`) are not allowed. For more information see [IAM Overview](https://cloud.google.com/iam/docs/overview#permissions).'
          items:
            type: string
          type: array
      id: TestIamPermissionsRequest
    IapSettings:
      description: The IAP configurable settings.
      id: IapSettings
      type: object
      properties:
        applicationSettings:
          $ref: '#/components/schemas/ApplicationSettings'
          description: Top level wrapper for all application related settings in IAP
        accessSettings:
          $ref: '#/components/schemas/AccessSettings'
          description: Top level wrapper for all access related setting in IAP
        name:
          type: string
          description: Required. The resource name of the IAP protected resource.
    ApplicationSettings:
      properties:
        csmSettings:
          description: Settings to configure IAP's behavior for a service mesh.
          $ref: '#/components/schemas/CsmSettings'
        attributePropagationSettings:
          $ref: '#/components/schemas/AttributePropagationSettings'
          description: Settings to configure attribute propagation.
        accessDeniedPageSettings:
          $ref: '#/components/schemas/AccessDeniedPageSettings'
          description: Customization for Access Denied page.
        cookieDomain:
          type: string
          description: 'The Domain value to set for cookies generated by IAP. This value is not validated by the API, but will be ignored at runtime if invalid.'
      type: object
      description: Wrapper over application specific settings for IAP.
      id: ApplicationSettings
    GetIamPolicyRequest:
      properties:
        options:
          $ref: '#/components/schemas/GetPolicyOptions'
          description: 'OPTIONAL: A `GetPolicyOptions` object for specifying options to `GetIamPolicy`.'
      description: Request message for `GetIamPolicy` method.
      id: GetIamPolicyRequest
      type: object
    Resource:
      id: Resource
      type: object
      properties:
        labels:
          description: 'The service defined labels of the resource on which the conditions will be evaluated. The semantics - including the key names - are vague to IAM. If the effective condition has a reference to a `resource.labels[foo]` construct, IAM consults with this map to retrieve the values associated with `foo` key for Conditions evaluation. If the provided key is not found in the labels map, the condition would evaluate to false. This field is in limited use. If your intended use case is not expected to express resource.labels attribute in IAM Conditions, leave this field empty. Before planning on using this attribute please: * Read go/iam-conditions-labels-comm and ensure your service can meet the data availability and management requirements. * Talk to iam-conditions-eng@ about your use case.'
          type: object
          additionalProperties:
            type: string
        service:
          type: string
          description: 'The name of the service this resource belongs to. It is configured using the official_service_name of the Service as defined in service configurations under //configs/cloud/resourcetypes. For example, the official_service_name of cloud resource manager service is set as ''cloudresourcemanager.googleapis.com'' according to //configs/cloud/resourcetypes/google/cloud/resourcemanager/prod.yaml'
        name:
          type: string
          description: 'Name of the resource on which conditions will be evaluated. Must use the Relative Resource Name of the resource, which is the URI path of the resource without the leading "/". Examples are "projects/_/buckets/[BUCKET-ID]" for storage buckets or "projects/[PROJECT-ID]/global/firewalls/[FIREWALL-ID]" for a firewall. This field is required for evaluating conditions with rules on resource names. For a `list` permission check, the resource.name value must be set to the parent resource. If the parent resource is a project, this field should be left unset.'
        type:
          description: 'The public resource type name of the resource on which conditions will be evaluated. It is configured using the official_name of the ResourceType as defined in service configurations under //configs/cloud/resourcetypes. For example, the official_name for GCP projects is set as ''cloudresourcemanager.googleapis.com/Project'' according to //configs/cloud/resourcetypes/google/cloud/resourcemanager/prod.yaml For details see go/iam-conditions-integration-guide.'
          type: string
    TunnelDestGroup:
      id: TunnelDestGroup
      properties:
        cidrs:
          description: Unordered list. List of CIDRs that this group applies to.
          type: array
          items:
            type: string
        fqdns:
          description: Unordered list. List of FQDNs that this group applies to.
          items:
            type: string
          type: array
        name:
          type: string
          description: Required. Immutable. Identifier for the TunnelDestGroup. Must be unique within the project and contain only lower case letters (a-z) and dashes (-).
      description: A TunnelDestGroup.
      type: object
    ListTunnelDestGroupsResponse:
      description: The response from ListTunnelDestGroups.
      type: object
      id: ListTunnelDestGroupsResponse
      properties:
        nextPageToken:
          type: string
          description: 'A token that you can send as `page_token` to retrieve the next page. If this field is omitted, there are no subsequent pages.'
        tunnelDestGroups:
          items:
            $ref: '#/components/schemas/TunnelDestGroup'
          type: array
          description: TunnelDestGroup existing in the project.
    ReauthSettings:
      properties:
        maxAge:
          type: string
          description: 'Reauth session lifetime, how long before a user has to reauthenticate again.'
          format: google-duration
        policyType:
          type: string
          description: How IAP determines the effective policy in cases of hierarchial policies. Policies are merged from higher in the hierarchy to lower in the hierarchy.
          enumDescriptions:
            - Default value. This value is unused.
            - 'This policy acts as a minimum to other policies, lower in the hierarchy. Effective policy may only be the same or stricter.'
            - This policy acts as a default if no other reauth policy is set.
          enum:
            - POLICY_TYPE_UNSPECIFIED
            - MINIMUM
            - DEFAULT
        method:
          type: string
          enum:
            - METHOD_UNSPECIFIED
            - LOGIN
            - PASSWORD
            - SECURE_KEY
            - ENROLLED_SECOND_FACTORS
          description: Reauth method requested.
          enumDeprecated:
            - false
            - false
            - true
            - false
            - false
          enumDescriptions:
            - Reauthentication disabled.
            - Prompts the user to log in again.
            - ''
            - User must use their secure key 2nd factor device.
            - User can use any enabled 2nd factor.
      type: object
      id: ReauthSettings
      description: Configuration for IAP reauthentication policies.
    Binding:
      type: object
      description: 'Associates `members`, or principals, with a `role`.'
      properties:
        members:
          description: 'Specifies the principals requesting access for a Google Cloud resource. `members` can have the following values: * `allUsers`: A special identifier that represents anyone who is on the internet; with or without a Google account. * `allAuthenticatedUsers`: A special identifier that represents anyone who is authenticated with a Google account or a service account. Does not include identities that come from external identity providers (IdPs) through identity federation. * `user:{emailid}`: An email address that represents a specific Google account. For example, `alice@example.com` . * `serviceAccount:{emailid}`: An email address that represents a Google service account. For example, `my-other-app@appspot.gserviceaccount.com`. * `serviceAccount:{projectid}.svc.id.goog[{namespace}/{kubernetes-sa}]`: An identifier for a [Kubernetes service account](https://cloud.google.com/kubernetes-engine/docs/how-to/kubernetes-service-accounts). For example, `my-project.svc.id.goog[my-namespace/my-kubernetes-sa]`. * `group:{emailid}`: An email address that represents a Google group. For example, `admins@example.com`. * `domain:{domain}`: The G Suite domain (primary) that represents all the users of that domain. For example, `google.com` or `example.com`. * `deleted:user:{emailid}?uid={uniqueid}`: An email address (plus unique identifier) representing a user that has been recently deleted. For example, `alice@example.com?uid=123456789012345678901`. If the user is recovered, this value reverts to `user:{emailid}` and the recovered user retains the role in the binding. * `deleted:serviceAccount:{emailid}?uid={uniqueid}`: An email address (plus unique identifier) representing a service account that has been recently deleted. For example, `my-other-app@appspot.gserviceaccount.com?uid=123456789012345678901`. If the service account is undeleted, this value reverts to `serviceAccount:{emailid}` and the undeleted service account retains the role in the binding. * `deleted:group:{emailid}?uid={uniqueid}`: An email address (plus unique identifier) representing a Google group that has been recently deleted. For example, `admins@example.com?uid=123456789012345678901`. If the group is recovered, this value reverts to `group:{emailid}` and the recovered group retains the role in the binding.'
          items:
            type: string
          type: array
        condition:
          description: 'The condition that is associated with this binding. If the condition evaluates to `true`, then this binding applies to the current request. If the condition evaluates to `false`, then this binding does not apply to the current request. However, a different role binding might grant the same role to one or more of the principals in this binding. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).'
          $ref: '#/components/schemas/Expr'
        role:
          type: string
          description: 'Role that is assigned to the list of `members`, or principals. For example, `roles/viewer`, `roles/editor`, or `roles/owner`.'
      id: Binding
    ListBrandsResponse:
      type: object
      description: Response message for ListBrands.
      properties:
        brands:
          type: array
          items:
            $ref: '#/components/schemas/Brand'
          description: Brands existing in the project.
      id: ListBrandsResponse
  parameters:
    uploadType:
      description: 'Legacy upload protocol for media (e.g. "media", "multipart").'
      in: query
      name: uploadType
      schema:
        type: string
    oauth_token:
      description: OAuth 2.0 token for the current user.
      in: query
      name: oauth_token
      schema:
        type: string
    callback:
      description: JSONP
      in: query
      name: callback
      schema:
        type: string
    key:
      description: 'API key. Your API key identifies your project and provides you with API access, quota, and reports. Required unless you provide an OAuth 2.0 token.'
      in: query
      name: key
      schema:
        type: string
    prettyPrint:
      description: Returns response with indentations and line breaks.
      in: query
      name: prettyPrint
      schema:
        type: boolean
    upload_protocol:
      description: 'Upload protocol for media (e.g. "raw", "multipart").'
      in: query
      name: upload_protocol
      schema:
        type: string
    quotaUser:
      description: 'Available to use for quota purposes for server-side applications. Can be any arbitrary string assigned to a user, but should not exceed 40 characters.'
      in: query
      name: quotaUser
      schema:
        type: string
    access_token:
      description: OAuth access token.
      in: query
      name: access_token
      schema:
        type: string
    fields:
      description: Selector specifying which fields to include in a partial response.
      in: query
      name: fields
      schema:
        type: string
    _.xgafv:
      description: V1 error format.
      in: query
      name: _.xgafv
      schema:
        type: string
        enum:
          - '1'
          - '2'
    alt:
      description: Data format for response.
      in: query
      name: alt
      schema:
        type: string
        enum:
          - json
          - media
          - proto
  x-stackQL-resources:
    iap_iam_policies:
      id: google.iap.iap_iam_policies
      name: iap_iam_policies
      title: Iap Iam Policies
      methods:
        set_iam_policy:
          operation:
            $ref: '#/paths/~1v1~1{v1Id}:setIamPolicy/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        test_iam_permissions:
          operation:
            $ref: '#/paths/~1v1~1{v1Id}:testIamPermissions/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get_iam_policy:
          operation:
            $ref: '#/paths/~1v1~1{v1Id}:getIamPolicy/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.bindings
        _get_iam_policy:
          operation:
            $ref: '#/paths/~1v1~1{v1Id}:getIamPolicy/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/iap_iam_policies/methods/get_iam_policy'
        insert: []
        update: []
        delete: []
    iap:
      id: google.iap.iap
      name: iap
      title: Iap
      methods:
        validate_attribute_expression:
          operation:
            $ref: '#/paths/~1v1~1{v1Id}:validateAttributeExpression/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
    iap_iap_settings:
      id: google.iap.iap_iap_settings
      name: iap_iap_settings
      title: Iap Iap Settings
      methods:
        update_iap_settings:
          operation:
            $ref: '#/paths/~1v1~1{v1Id}:iapSettings/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get_iap_settings:
          operation:
            $ref: '#/paths/~1v1~1{v1Id}:iapSettings/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/iap_iap_settings/methods/get_iap_settings'
        insert: []
        update: []
        delete: []
    brands:
      id: google.iap.brands
      name: brands
      title: Brands
      methods:
        get:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands~1{brandsId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        create:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/brands/methods/get'
          - $ref: '#/components/x-stackQL-resources/brands/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/brands/methods/create'
        update: []
        delete: []
    identity_aware_proxy_clients:
      id: google.iap.identity_aware_proxy_clients
      name: identity_aware_proxy_clients
      title: Identity Aware Proxy Clients
      methods:
        delete:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands~1{brandsId}~1identityAwareProxyClients~1{identityAwareProxyClientsId}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands~1{brandsId}~1identityAwareProxyClients~1{identityAwareProxyClientsId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        create:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands~1{brandsId}~1identityAwareProxyClients/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands~1{brandsId}~1identityAwareProxyClients/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.identityAwareProxyClients
        _list:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands~1{brandsId}~1identityAwareProxyClients/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        reset_secret:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1brands~1{brandsId}~1identityAwareProxyClients~1{identityAwareProxyClientsId}:resetSecret/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/identity_aware_proxy_clients/methods/get'
          - $ref: '#/components/x-stackQL-resources/identity_aware_proxy_clients/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/identity_aware_proxy_clients/methods/create'
        update: []
        delete:
          - $ref: '#/components/x-stackQL-resources/identity_aware_proxy_clients/methods/delete'
    dest_groups:
      id: google.iap.dest_groups
      name: dest_groups
      title: Dest Groups
      methods:
        patch:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1iap_tunnel~1locations~1{locationsId}~1destGroups~1{destGroupsId}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1iap_tunnel~1locations~1{locationsId}~1destGroups~1{destGroupsId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1iap_tunnel~1locations~1{locationsId}~1destGroups~1{destGroupsId}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        create:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1iap_tunnel~1locations~1{locationsId}~1destGroups/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1iap_tunnel~1locations~1{locationsId}~1destGroups/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.tunnelDestGroups
        _list:
          operation:
            $ref: '#/paths/~1v1~1projects~1{projectsId}~1iap_tunnel~1locations~1{locationsId}~1destGroups/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/dest_groups/methods/get'
          - $ref: '#/components/x-stackQL-resources/dest_groups/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/dest_groups/methods/create'
        update: []
        delete:
          - $ref: '#/components/x-stackQL-resources/dest_groups/methods/delete'
paths:
  '/v1/{v1Id}:setIamPolicy':
    parameters: &ref_1
      - $ref: '#/components/parameters/uploadType'
      - $ref: '#/components/parameters/oauth_token'
      - $ref: '#/components/parameters/callback'
      - $ref: '#/components/parameters/key'
      - $ref: '#/components/parameters/prettyPrint'
      - $ref: '#/components/parameters/upload_protocol'
      - $ref: '#/components/parameters/quotaUser'
      - $ref: '#/components/parameters/access_token'
      - $ref: '#/components/parameters/fields'
      - $ref: '#/components/parameters/_.xgafv'
      - $ref: '#/components/parameters/alt'
    post:
      description: 'Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: https://cloud.google.com/iap/docs/managing-access#managing_access_via_the_api'
      operationId: iap.setIamPolicy
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetIamPolicyRequest'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Policy'
      parameters:
        - in: path
          name: v1Id
          required: true
          schema:
            type: string
      x-stackQL-resource: iap_iam_policies
      x-stackQL-method: set_iam_policy
      x-stackQL-verb: exec
  '/v1/{v1Id}:validateAttributeExpression':
    parameters: *ref_1
    post:
      description: Validates a given CEL expression conforms to IAP restrictions.
      operationId: iap.validateAttributeExpression
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidateIapAttributeExpressionResponse'
      parameters:
        - in: path
          name: v1Id
          required: true
          schema:
            type: string
        - in: query
          name: expression
          schema:
            type: string
      x-stackQL-resource: iap
      x-stackQL-method: validate_attribute_expression
      x-stackQL-verb: exec
  '/v1/{v1Id}:testIamPermissions':
    parameters: *ref_1
    post:
      description: 'Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: https://cloud.google.com/iap/docs/managing-access#managing_access_via_the_api'
      operationId: iap.testIamPermissions
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestIamPermissionsRequest'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestIamPermissionsResponse'
      parameters:
        - in: path
          name: v1Id
          required: true
          schema:
            type: string
      x-stackQL-resource: iap_iam_policies
      x-stackQL-method: test_iam_permissions
      x-stackQL-verb: exec
  '/v1/{v1Id}:iapSettings':
    parameters: *ref_1
    patch:
      description: Updates the IAP settings on a particular IAP protected resource. It replaces all fields unless the `update_mask` is set.
      operationId: iap.updateIapSettings
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IapSettings'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IapSettings'
      parameters:
        - in: path
          name: v1Id
          required: true
          schema:
            type: string
        - in: query
          name: updateMask
          schema:
            type: string
      x-stackQL-resource: iap_iap_settings
      x-stackQL-method: update_iap_settings
      x-stackQL-verb: exec
    get:
      description: Gets the IAP settings on a particular IAP protected resource.
      operationId: iap.getIapSettings
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IapSettings'
      parameters:
        - in: path
          name: v1Id
          required: true
          schema:
            type: string
      x-stackQL-resource: iap_iap_settings
      x-stackQL-method: get_iap_settings
      x-stackQL-verb: select
  '/v1/{v1Id}:getIamPolicy':
    parameters: *ref_1
    post:
      description: 'Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: https://cloud.google.com/iap/docs/managing-access#managing_access_via_the_api'
      operationId: iap.getIamPolicy
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetIamPolicyRequest'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Policy'
      parameters:
        - in: path
          name: v1Id
          required: true
          schema:
            type: string
      x-stackQL-resource: iap_iam_policies
      x-stackQL-method: get_iam_policy
      x-stackQL-verb: select
      x-stackQL-objectKey: $.bindings
  '/v1/projects/{projectsId}/brands/{brandsId}':
    parameters: *ref_1
    get:
      description: Retrieves the OAuth brand of the project.
      operationId: iap.projects.brands.get
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Brand'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: brandsId
          required: true
          schema:
            type: string
      x-stackQL-resource: brands
      x-stackQL-method: get
      x-stackQL-verb: select
  '/v1/projects/{projectsId}/brands':
    parameters: *ref_1
    post:
      description: 'Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.'
      operationId: iap.projects.brands.create
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Brand'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Brand'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
      x-stackQL-resource: brands
      x-stackQL-method: create
      x-stackQL-verb: insert
    get:
      description: Lists the existing brands for the project.
      operationId: iap.projects.brands.list
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListBrandsResponse'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
      x-stackQL-resource: brands
      x-stackQL-method: list
      x-stackQL-verb: select
  '/v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}':
    parameters: *ref_1
    delete:
      description: 'Deletes an Identity Aware Proxy (IAP) OAuth client. Useful for removing obsolete clients, managing the number of clients in a given project, and cleaning up after tests. Requires that the client is owned by IAP.'
      operationId: iap.projects.brands.identityAwareProxyClients.delete
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Empty'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: brandsId
          required: true
          schema:
            type: string
        - in: path
          name: identityAwareProxyClientsId
          required: true
          schema:
            type: string
      x-stackQL-resource: identity_aware_proxy_clients
      x-stackQL-method: delete
      x-stackQL-verb: delete
    get:
      description: Retrieves an Identity Aware Proxy (IAP) OAuth client. Requires that the client is owned by IAP.
      operationId: iap.projects.brands.identityAwareProxyClients.get
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityAwareProxyClient'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: brandsId
          required: true
          schema:
            type: string
        - in: path
          name: identityAwareProxyClientsId
          required: true
          schema:
            type: string
      x-stackQL-resource: identity_aware_proxy_clients
      x-stackQL-method: get
      x-stackQL-verb: select
  '/v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients':
    parameters: *ref_1
    post:
      description: Creates an Identity Aware Proxy (IAP) OAuth client. The client is owned by IAP. Requires that the brand for the project exists and that it is set for internal-only use.
      operationId: iap.projects.brands.identityAwareProxyClients.create
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IdentityAwareProxyClient'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityAwareProxyClient'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: brandsId
          required: true
          schema:
            type: string
      x-stackQL-resource: identity_aware_proxy_clients
      x-stackQL-method: create
      x-stackQL-verb: insert
    get:
      description: Lists the existing clients for the brand.
      operationId: iap.projects.brands.identityAwareProxyClients.list
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListIdentityAwareProxyClientsResponse'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: brandsId
          required: true
          schema:
            type: string
        - in: query
          name: pageToken
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            type: integer
      x-stackQL-resource: identity_aware_proxy_clients
      x-stackQL-method: list
      x-stackQL-verb: select
      x-stackQL-objectKey: $.identityAwareProxyClients
  '/v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}:resetSecret':
    parameters: *ref_1
    post:
      description: Resets an Identity Aware Proxy (IAP) OAuth client secret. Useful if the secret was compromised. Requires that the client is owned by IAP.
      operationId: iap.projects.brands.identityAwareProxyClients.resetSecret
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetIdentityAwareProxyClientSecretRequest'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityAwareProxyClient'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: brandsId
          required: true
          schema:
            type: string
        - in: path
          name: identityAwareProxyClientsId
          required: true
          schema:
            type: string
      x-stackQL-resource: identity_aware_proxy_clients
      x-stackQL-method: reset_secret
      x-stackQL-verb: exec
  '/v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}':
    parameters: *ref_1
    patch:
      description: Updates a TunnelDestGroup.
      operationId: iap.projects.iap_tunnel.locations.destGroups.patch
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TunnelDestGroup'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TunnelDestGroup'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: locationsId
          required: true
          schema:
            type: string
        - in: path
          name: destGroupsId
          required: true
          schema:
            type: string
        - in: query
          name: updateMask
          schema:
            type: string
      x-stackQL-resource: dest_groups
      x-stackQL-method: patch
      x-stackQL-verb: exec
    get:
      description: Retrieves an existing TunnelDestGroup.
      operationId: iap.projects.iap_tunnel.locations.destGroups.get
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TunnelDestGroup'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: locationsId
          required: true
          schema:
            type: string
        - in: path
          name: destGroupsId
          required: true
          schema:
            type: string
      x-stackQL-resource: dest_groups
      x-stackQL-method: get
      x-stackQL-verb: select
    delete:
      description: Deletes a TunnelDestGroup.
      operationId: iap.projects.iap_tunnel.locations.destGroups.delete
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Empty'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: locationsId
          required: true
          schema:
            type: string
        - in: path
          name: destGroupsId
          required: true
          schema:
            type: string
      x-stackQL-resource: dest_groups
      x-stackQL-method: delete
      x-stackQL-verb: delete
  '/v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups':
    parameters: *ref_1
    post:
      description: Creates a new TunnelDestGroup.
      operationId: iap.projects.iap_tunnel.locations.destGroups.create
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TunnelDestGroup'
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TunnelDestGroup'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: locationsId
          required: true
          schema:
            type: string
        - in: query
          name: tunnelDestGroupId
          schema:
            type: string
      x-stackQL-resource: dest_groups
      x-stackQL-method: create
      x-stackQL-verb: insert
    get:
      description: 'Lists the existing TunnelDestGroups. To group across all locations, use a `-` as the location ID. For example: `/v1/projects/123/iap_tunnel/locations/-/destGroups`'
      operationId: iap.projects.iap_tunnel.locations.destGroups.list
      security:
        - Oauth2:
            - 'https://www.googleapis.com/auth/cloud-platform'
          Oauth2c:
            - 'https://www.googleapis.com/auth/cloud-platform'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListTunnelDestGroupsResponse'
      parameters:
        - in: path
          name: projectsId
          required: true
          schema:
            type: string
        - in: path
          name: locationsId
          required: true
          schema:
            type: string
        - in: query
          name: pageToken
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            type: integer
      x-stackQL-resource: dest_groups
      x-stackQL-method: list
      x-stackQL-verb: select
      x-stackQL-objectKey: $.tunnelDestGroups
