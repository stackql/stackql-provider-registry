openapi: 3.0.3
info:
  title: ai_gateway API
  description: vercel ai_gateway API
  version: 0.0.1
paths:
  /v1/ai-gateway/virtual-model-configs:
    post:
      description: Create a virtual model config (VMC)
      operationId: createAiGatewayVirtualModelConfig
      security:
        - bearerToken: []
      summary: Create virtual model config
      tags:
        - api-ai-gateway
      responses:
        '201':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayVirtualModelConfig'
        '400':
          description: ''
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '409':
          description: ''
        '410':
          description: ''
        '429':
          description: ''
        '500':
          description: ''
      parameters:
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    get:
      description: Get a virtual model config
      operationId: getAiGatewayVirtualModelConfig
      security:
        - bearerToken: []
      summary: Get virtual model config
      tags:
        - api-ai-gateway
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                properties:
                  ownerId:
                    type: string
                    description: Team (owner) that owns this VMC.
                  virtualModelSlug:
                    type: string
                    description: Client-facing alias used as the model slug in Gateway calls.
                  displayName:
                    type: string
                    description: Human-readable name for UI.
                  description:
                    type: string
                    description: Optional description for UI.
                  deleted:
                    type: boolean
                    enum:
                      - false
                      - true
                    description: Whether this VMC is soft-deleted.
                  status:
                    type: string
                    description: 'UI lifecycle status: draft, active, or archived.'
                  visibility:
                    type: string
                    description: 'Visibility in listings: public, internal, or stealth.'
                  updatedBy:
                    type: string
                    description: User id that last updated this VMC.
                  kind:
                    type: string
                    description: 'VMC kind: alias, relay, or router.'
                  baseUrl:
                    type: string
                    description: 'For kind=relay: URL the gateway forwards requests to as a transparent proxy.'
                  instanceId:
                    type: string
                    description: The concrete model-provider instance this VMC resolves to.
                  providerOrder:
                    items:
                      type: string
                    type: array
                    description: Ordered list of providers to try as fallbacks on failure.
                  providerOnly:
                    items:
                      type: string
                    type: array
                    description: Restrict routing to only these providers.
                  providerOptions:
                    additionalProperties:
                      $ref: '#/components/schemas/AiGatewayProviderOptionBag'
                    type: object
                    description: Arbitrary per-provider AI SDK options, keyed by gateway provider slug.
                  inferenceRegion:
                    properties:
                      providers:
                        additionalProperties:
                          nullable: true
                          properties:
                            scope:
                              type: string
                              enum:
                                - global
                                - specific
                                - zone
                              description: 'Pin scope: `specific` (one provider region), `zone` (geo zone), or `global`.'
                            geoRegion:
                              type: string
                              description: Geo zone (e.g. "us", "eu").
                            providerRegion:
                              type: string
                              description: Provider-specific region identifier.
                          type: object
                          description: Per-provider region overrides keyed by provider slug.
                        type: object
                        description: Per-provider region overrides keyed by provider slug.
                      scope:
                        type: string
                        enum:
                          - global
                          - specific
                          - zone
                        description: 'Pin scope: `specific` (one provider region), `zone` (geo zone), or `global`.'
                      geoRegion:
                        type: string
                        description: Geo zone (e.g. "us", "eu").
                      providerRegion:
                        type: string
                        description: Provider-specific region identifier.
                    type: object
                    description: Region pinned on the VMC for system-credential routing (alias/router only).
                  modelSlug:
                    type: string
                    description: Canonical model slug this VMC maps to (e.g. "creator/model"). Not used by kind=router.
                  models:
                    items:
                      type: string
                    type: array
                    description: 'For kind=router: ordered candidates, model slugs or router references. Otherwise: fallback models.'
                  selector:
                    type: string
                    enum:
                      - cost
                      - priority
                      - tps
                      - ttft
                    description: 'For kind=router: how to order candidates.'
                  requires:
                    items:
                      type: string
                    type: array
                    description: 'For kind=router: capability tags a candidate must have.'
                  byokCredentialIds:
                    items:
                      type: string
                    type: array
                    description: BYOK credential IDs allowed for this VMC.
                  observabilityTags:
                    items:
                      type: string
                    type: array
                    description: Observability tags attached to requests through this VMC.
                  sort:
                    type: string
                    enum:
                      - cost
                      - latency
                      - price
                      - throughput
                      - tps
                      - ttft
                    description: Rank eligible providers by an attribute.
                  has:
                    items:
                      type: string
                      enum:
                        - implicit-caching
                        - vision
                      description: Limit providers to those with these features.
                    type: array
                    description: Limit providers to those with these features.
                  caching:
                    type: string
                    enum:
                      - auto
                    description: Use caching if available.
                  serviceTier:
                    type: string
                    enum:
                      - fast
                      - flex
                      - priority
                    description: Service tier for providers that support it.
                  providerTimeouts:
                    properties:
                      byok:
                        additionalProperties:
                          type: number
                        type: object
                    type: object
                    description: Per-request provider timeouts in ms, keyed by provider slug for BYOK credentials.
                  zeroDataRetention:
                    type: boolean
                    enum:
                      - false
                      - true
                    description: Only use providers with zero data retention.
                  hipaaCompliant:
                    type: boolean
                    enum:
                      - false
                      - true
                    description: Only use HIPAA-compliant providers.
                  disallowPromptTraining:
                    type: boolean
                    enum:
                      - false
                      - true
                    description: Only use providers that will not train on your prompts.
                  speed:
                    type: string
                    enum:
                      - fast
                    description: Only use fastest providers with short timeouts.
                  allowFallbackFromFast:
                    type: boolean
                    enum:
                      - false
                      - true
                    description: Allow fallback from fast to standard providers on failure.
                  createdAt:
                    type: number
                    description: Creation timestamp (epoch ms).
                  updatedAt:
                    type: number
                    description: Last update timestamp (epoch ms).
                  virtualModelConfigs:
                    items:
                      $ref: '#/components/schemas/AiGatewayVirtualModelConfig'
                    type: array
                    description: The page of VMCs.
                  cursor:
                    nullable: true
                    type: string
                    description: Cursor for the next page, or null when no more pages remain.
                required:
                  - createdAt
                  - deleted
                  - kind
                  - ownerId
                  - status
                  - updatedAt
                  - virtualModelSlug
                  - cursor
                  - virtualModelConfigs
                type: object
                description: Public response shape for virtual model configs. Used so OpenAPI generation can avoid ElectroDB's recursive EntityItem types.
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: ownerId
          in: query
          schema:
            type: string
        - name: virtualModelSlug
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
        - name: cursor
          in: query
          schema:
            type: string
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    patch:
      description: Update a virtual model config
      operationId: updateAiGatewayVirtualModelConfig
      security:
        - bearerToken: []
      summary: Update virtual model config
      tags:
        - api-ai-gateway
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayVirtualModelConfig'
        '400':
          description: ''
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    delete:
      description: Delete a virtual model config (soft delete)
      operationId: deleteAiGatewayVirtualModelConfig
      security:
        - bearerToken: []
      summary: Delete virtual model config
      tags:
        - api-ai-gateway
      responses:
        '204':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: ownerId
          in: query
          required: false
          schema:
            type: string
        - name: virtualModelSlug
          in: query
          required: true
          schema:
            type: string
        - name: updatedBy
          in: query
          required: false
          schema:
            type: string
        - name: actingIp
          in: query
          required: false
          schema:
            type: string
        - name: actingUserAgent
          in: query
          required: false
          schema:
            type: string
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
  /v1/ai-gateway/virtual-model-configs/list:
    get:
      description: List virtual model configs. With `ownerId`, returns all of that team's VMCs. Without it, pages through VMCs across all teams (newest-first, `limit`/`cursor`).
      operationId: listAiGatewayVirtualModelConfigs
      security:
        - bearerToken: []
      summary: List virtual model configs
      tags:
        - api-ai-gateway
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayVirtualModelConfigList'
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: ownerId
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
        - name: cursor
          in: query
          schema:
            type: string
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
  /v1/ai-gateway/virtual-model-configs/{vmc_slug}:
    get:
      description: Get a virtual model config by path slug
      operationId: getAiGatewayVirtualModelConfigBySlug
      security:
        - bearerToken: []
      summary: Get virtual model config
      tags:
        - api-ai-gateway
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayVirtualModelConfig'
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: ownerId
          in: query
          required: false
          schema:
            type: string
        - name: vmc_slug
          in: path
          required: true
          schema:
            type: string
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    patch:
      description: Update a virtual model config by path slug
      operationId: updateAiGatewayVirtualModelConfigBySlug
      security:
        - bearerToken: []
      summary: Update virtual model config
      tags:
        - api-ai-gateway
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayVirtualModelConfig'
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: vmc_slug
          in: path
          required: true
          schema:
            type: string
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    delete:
      description: Delete a virtual model config by path slug (soft delete)
      operationId: deleteAiGatewayVirtualModelConfigBySlug
      security:
        - bearerToken: []
      summary: Delete virtual model config
      tags:
        - api-ai-gateway
      responses:
        '204':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: ownerId
          in: query
          required: false
          schema:
            type: string
        - name: vmc_slug
          in: path
          required: true
          schema:
            type: string
        - name: updatedBy
          in: query
          required: false
          schema:
            type: string
        - name: actingIp
          in: query
          required: false
          schema:
            type: string
        - name: actingUserAgent
          in: query
          required: false
          schema:
            type: string
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
  /v1/ai-gateway/rules:
    post:
      description: Create a routing rule
      operationId: createAiGatewayRule
      security:
        - bearerToken: []
      summary: Create rule
      tags:
        - ai-gateway
      responses:
        '201':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayRule'
        '400':
          description: ''
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '409':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    get:
      description: List the authenticated team's routing rules
      operationId: listAiGatewayRules
      security:
        - bearerToken: []
      summary: List rules
      tags:
        - ai-gateway
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayRuleList'
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: includeDisabled
          in: query
          schema:
            type: string
            enum:
              - 'true'
              - 'false'
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    patch:
      description: Update a routing rule (enabled, action, or description)
      operationId: updateAiGatewayRule
      security:
        - bearerToken: []
      summary: Update rule
      tags:
        - ai-gateway
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiGatewayRule'
        '400':
          description: ''
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
    delete:
      description: Delete a routing rule (soft delete)
      operationId: deleteAiGatewayRule
      security:
        - bearerToken: []
      summary: Delete rule
      tags:
        - ai-gateway
      responses:
        '204':
          description: ''
          content:
            application/json:
              schema:
                nullable: true
        '400':
          description: One of the provided values in the request query is invalid.
        '401':
          description: The request is not authorized.
        '403':
          description: You do not have permission to access this resource.
        '404':
          description: ''
        '410':
          description: ''
        '500':
          description: ''
      parameters:
        - name: ruleId
          in: query
          required: true
          schema:
            type: string
        - description: The Team identifier to perform the request on behalf of.
          in: query
          name: teamId
          schema:
            type: string
            example: team_1a2b3c4d5e6f7g8h9i0j1k2l
        - description: The Team slug to perform the request on behalf of.
          in: query
          name: slug
          schema:
            type: string
            example: my-team-url-slug
components:
  schemas:
    AiGatewayVirtualModelConfig:
      properties:
        ownerId:
          type: string
          description: Team (owner) that owns this VMC.
        virtualModelSlug:
          type: string
          description: Client-facing alias used as the model slug in Gateway calls.
        displayName:
          type: string
          description: Human-readable name for UI.
        description:
          type: string
          description: Optional description for UI.
        deleted:
          type: boolean
          enum:
            - false
            - true
          description: Whether this VMC is soft-deleted.
        status:
          type: string
          description: 'UI lifecycle status: draft, active, or archived.'
        visibility:
          type: string
          description: 'Visibility in listings: public, internal, or stealth.'
        updatedBy:
          type: string
          description: User id that last updated this VMC.
        kind:
          type: string
          description: 'VMC kind: alias, relay, or router.'
        baseUrl:
          type: string
          description: 'For kind=relay: URL the gateway forwards requests to as a transparent proxy.'
        instanceId:
          type: string
          description: The concrete model-provider instance this VMC resolves to.
        providerOrder:
          items:
            type: string
          type: array
          description: Ordered list of providers to try as fallbacks on failure.
        providerOnly:
          items:
            type: string
          type: array
          description: Restrict routing to only these providers.
        providerOptions:
          additionalProperties:
            $ref: '#/components/schemas/AiGatewayProviderOptionBag'
          type: object
          description: Arbitrary per-provider AI SDK options, keyed by gateway provider slug.
        inferenceRegion:
          properties:
            providers:
              additionalProperties:
                nullable: true
                properties:
                  scope:
                    type: string
                    enum:
                      - global
                      - specific
                      - zone
                    description: 'Pin scope: `specific` (one provider region), `zone` (geo zone), or `global`.'
                  geoRegion:
                    type: string
                    description: Geo zone (e.g. "us", "eu").
                  providerRegion:
                    type: string
                    description: Provider-specific region identifier.
                type: object
                description: Per-provider region overrides keyed by provider slug.
              type: object
              description: Per-provider region overrides keyed by provider slug.
            scope:
              type: string
              enum:
                - global
                - specific
                - zone
              description: 'Pin scope: `specific` (one provider region), `zone` (geo zone), or `global`.'
            geoRegion:
              type: string
              description: Geo zone (e.g. "us", "eu").
            providerRegion:
              type: string
              description: Provider-specific region identifier.
          type: object
          description: Region pinned on the VMC for system-credential routing (alias/router only).
        modelSlug:
          type: string
          description: Canonical model slug this VMC maps to (e.g. "creator/model"). Not used by kind=router.
        models:
          items:
            type: string
          type: array
          description: 'For kind=router: ordered candidates, model slugs or router references. Otherwise: fallback models.'
        selector:
          type: string
          enum:
            - cost
            - priority
            - tps
            - ttft
          description: 'For kind=router: how to order candidates.'
        requires:
          items:
            type: string
          type: array
          description: 'For kind=router: capability tags a candidate must have.'
        byokCredentialIds:
          items:
            type: string
          type: array
          description: BYOK credential IDs allowed for this VMC.
        observabilityTags:
          items:
            type: string
          type: array
          description: Observability tags attached to requests through this VMC.
        sort:
          type: string
          enum:
            - cost
            - latency
            - price
            - throughput
            - tps
            - ttft
          description: Rank eligible providers by an attribute.
        has:
          items:
            type: string
            enum:
              - implicit-caching
              - vision
            description: Limit providers to those with these features.
          type: array
          description: Limit providers to those with these features.
        caching:
          type: string
          enum:
            - auto
          description: Use caching if available.
        serviceTier:
          type: string
          enum:
            - fast
            - flex
            - priority
          description: Service tier for providers that support it.
        providerTimeouts:
          properties:
            byok:
              additionalProperties:
                type: number
              type: object
          type: object
          description: Per-request provider timeouts in ms, keyed by provider slug for BYOK credentials.
        zeroDataRetention:
          type: boolean
          enum:
            - false
            - true
          description: Only use providers with zero data retention.
        hipaaCompliant:
          type: boolean
          enum:
            - false
            - true
          description: Only use HIPAA-compliant providers.
        disallowPromptTraining:
          type: boolean
          enum:
            - false
            - true
          description: Only use providers that will not train on your prompts.
        speed:
          type: string
          enum:
            - fast
          description: Only use fastest providers with short timeouts.
        allowFallbackFromFast:
          type: boolean
          enum:
            - false
            - true
          description: Allow fallback from fast to standard providers on failure.
        createdAt:
          type: number
          description: Creation timestamp (epoch ms).
        updatedAt:
          type: number
          description: Last update timestamp (epoch ms).
      required:
        - createdAt
        - deleted
        - kind
        - ownerId
        - status
        - updatedAt
        - virtualModelSlug
      type: object
      description: Public response shape for virtual model configs. Used so OpenAPI generation can avoid ElectroDB's recursive EntityItem types.
    AiGatewayVirtualModelConfigList:
      properties:
        virtualModelConfigs:
          items:
            $ref: '#/components/schemas/AiGatewayVirtualModelConfig'
          type: array
          description: The page of VMCs.
        cursor:
          nullable: true
          type: string
          description: Cursor for the next page, or null when no more pages remain.
      required:
        - cursor
        - virtualModelConfigs
      type: object
    AiGatewayRule:
      properties:
        ownerId:
          type: string
        ruleId:
          type: string
        type:
          type: string
          enum:
            - deny
            - rewrite
        match:
          properties:
            model:
              type: string
          type: object
        action:
          properties:
            rewriteModel:
              type: string
            reason:
              type: string
          type: object
        enabled:
          type: boolean
          enum:
            - false
            - true
        deleted:
          type: boolean
          enum:
            - false
            - true
        description:
          type: string
        createdBy:
          type: string
        updatedBy:
          type: string
        createdAt:
          type: number
        updatedAt:
          type: number
      required:
        - createdAt
        - enabled
        - ownerId
        - ruleId
        - type
        - updatedAt
      type: object
      description: Public response shape for AI Gateway routing rules. Used so OpenAPI generation can avoid ElectroDB's recursive EntityItem types.
    AiGatewayRuleList:
      properties:
        rules:
          items:
            $ref: '#/components/schemas/AiGatewayRule'
          type: array
      required:
        - rules
      type: object
    AiGatewayProviderOptionBag:
      additionalProperties: true
      type: object
      description: Arbitrary per-provider AI SDK options, keyed by gateway provider slug.
  x-stackQL-resources:
    virtual_model_configs:
      id: vercel.ai_gateway.virtual_model_configs
      name: virtual_model_configs
      title: Virtual Model Configs
      methods:
        create:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs/post'
          response:
            mediaType: application/json
            openAPIDocKey: '201'
          request:
            nativeCasing: camel
        get_by_query:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        delete:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '204'
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs~1list/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.virtualModelConfigs
          request:
            nativeCasing: camel
          config:
            pagination:
              requestToken:
                key: cursor
                location: query
              responseToken:
                key: $.cursor
                location: body
            queryParamPushdown:
              top:
                paramName: limit
                maxValue: 100
        get_by_slug:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs~1{vmc_slug}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update_by_slug:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs~1{vmc_slug}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        delete_by_slug:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1virtual-model-configs~1{vmc_slug}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '204'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/virtual_model_configs/methods/get_by_slug'
          - $ref: '#/components/x-stackQL-resources/virtual_model_configs/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/virtual_model_configs/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/virtual_model_configs/methods/update_by_slug'
          - $ref: '#/components/x-stackQL-resources/virtual_model_configs/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/virtual_model_configs/methods/delete_by_slug'
          - $ref: '#/components/x-stackQL-resources/virtual_model_configs/methods/delete'
        replace: []
    rules:
      id: vercel.ai_gateway.rules
      name: rules
      title: Rules
      methods:
        create:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1rules/post'
          response:
            mediaType: application/json
            openAPIDocKey: '201'
          request:
            nativeCasing: camel
        list:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1rules/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.rules
          request:
            nativeCasing: camel
        update:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1rules/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        delete:
          operation:
            $ref: '#/paths/~1v1~1ai-gateway~1rules/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '204'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/rules/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/rules/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/rules/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/rules/methods/delete'
        replace: []
servers:
  - url: https://api.vercel.com
