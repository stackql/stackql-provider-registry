openapi: 3.0.0
info:
  title: Sumo Logic Content API
  description: The content library - folders (personal, global, admin recommended, installed apps), content permissions, paths, and the asynchronous export, import, copy, move and delete jobs.
  version: 1.0.0
paths:
  /v2/content/folders:
    post:
      tags:
        - folderManagement
      summary: Create a new folder.
      description: Creates a new folder under the given parent folder. Set the header parameter `isAdminMode` to `"true"` to create a folder inside "Admin Recommended" folder.
      operationId: createFolder
      parameters:
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      requestBody:
        description: Information about the new folder.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FolderDefinition'
        required: true
      responses:
        '200':
          description: The folder has been created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/{id}:
    get:
      tags:
        - folderManagement
      summary: Get a folder.
      description: Get a folder with the given identifier. Set the header parameter `isAdminMode` to `"true"` if fetching a folder inside "Admin Recommended" folder.
      operationId: getFolder
      parameters:
        - name: id
          in: path
          description: Identifier of the folder to fetch.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Folder that was requested.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags:
        - folderManagement
      summary: Update a folder.
      description: Update an existing folder with the given identifier. Set the header parameter `isAdminMode` to `"true"` if updating a folder inside "Admin Recommended" folder.
      operationId: updateFolder
      parameters:
        - name: id
          in: path
          description: Identifier of the folder to update.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      requestBody:
        description: Information to update about the folder.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateFolderRequest'
        required: true
      responses:
        '200':
          description: The folder was successfully updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/personal:
    get:
      tags:
        - folderManagement
      summary: Get personal folder.
      description: Get the personal folder of the current user.
      operationId: getPersonalFolder
      responses:
        '200':
          description: |
            The personal folder of the current user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/global:
    get:
      tags:
        - folderManagement
      summary: Schedule Global View job
      description: |-
        Schedule an asynchronous job to get Global View. Global View contains all top-level content items that a user has permissions to view in the organization. User can traverse the top-level folders using GetFolder API to get rest of the content items. Make sure you set `isAdminMode` header parameter to `true` when traversing top-level items.

        _Global View is not a real folder, therefore there is no folder identifier associated with it_.

        _You get back a identifier of asynchronous job in response to this endpoint. See Asynchronous-Request section for more details on how to work with asynchronous request._
      operationId: getGlobalFolderAsync
      parameters:
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: An asynchronous job to get a list of all content items been scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeginAsyncJobResponse'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/global/{jobId}/status:
    get:
      tags:
        - folderManagement
      summary: Get Global View job status
      description: Get the status of an asynchronous Global View job for the given job identifier. If job succeeds, use Global View Result endpoint to fetch all content items that you have permissions to view.
      operationId: getGlobalFolderAsyncStatus
      parameters:
        - name: jobId
          in: path
          description: The identifier of the asynchronous Global View job.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Asynchronous Global View job status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobStatus'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/global/{jobId}/result:
    get:
      tags:
        - folderManagement
      summary: Get Global View job result
      description: Get result of a Global View job for the given job identifier. The result will be a list of all content items that a user has permissions to view in the organization.
      operationId: getGlobalFolderAsyncResult
      parameters:
        - name: jobId
          in: path
          description: The identifier of the asynchronous Global View job.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of all content items with view permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentList'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/adminRecommended:
    get:
      tags:
        - folderManagement
      summary: Schedule Admin Recommended folder job
      description: |-
        Schedule an asynchronous job to get the top-level Admin Recommended content items. You can read more about Admin Recommended folder [here](https://help.sumologic.com/Manage/Content_Sharing/Admin_Mode#move-important-content-to-admin-recommended).

        _You get back a identifier of asynchronous job in response to this endpoint. See Asynchronous-Request section for more details on how to work with asynchronous request._
      operationId: getAdminRecommendedFolderAsync
      parameters:
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: An asynchronous job to get the Admin Recommended folder has been scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeginAsyncJobResponse'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/adminRecommended/{jobId}/status:
    get:
      tags:
        - folderManagement
      summary: Get Admin Recommended folder job status
      description: Get the status of an asynchronous Admin Recommended folder job for the given job identifier. If job succeeds, use Admin Recommended Job Result endpoint to fetch top-level content items in Admin Recommended folder.
      operationId: getAdminRecommendedFolderAsyncStatus
      parameters:
        - name: jobId
          in: path
          description: The identifier of the asynchronous Admin Recommended folder job.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Asynchronous Admin Recommended folder job status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobStatus'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/adminRecommended/{jobId}/result:
    get:
      tags:
        - folderManagement
      summary: Get Admin Recommended folder job result
      description: Get result of an Admin Recommended job for the given job identifier. The result will be "Admin Recommended" folder with a list of top-level Admin Recommended content items in `children` field.
      operationId: getAdminRecommendedFolderAsyncResult
      parameters:
        - name: jobId
          in: path
          description: The identifier of the asynchronous Admin Recommended folder job.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Admin Recommended folder.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/installedApps:
    get:
      tags:
        - folderManagement
      summary: Schedule Installed Apps folder job
      description: |-
        Schedule an asynchronous job to get the top-level Installed Apps content items.

        _You get back a identifier of asynchronous job in response to this endpoint. See Asynchronous-Request section for more details on how to work with asynchronous request._
      operationId: getInstalledAppsFolderAsync
      parameters:
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: An asynchronous job to get the Installed Apps folder has been scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeginAsyncJobResponse'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/installedApps/{jobId}/status:
    get:
      tags:
        - folderManagement
      summary: Get Installed Apps folder job status
      description: Get the status of an asynchronous Installed Apps folder job for the given job identifier. If job succeeds, use Installed Apps Job Result endpoint to fetch top-level content items in Installed Apps folder.
      operationId: getInstalledAppsFolderAsyncStatus
      parameters:
        - name: jobId
          in: path
          description: The identifier of the asynchronous Installed Apps folder job.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Asynchronous Installed Apps folder job status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobStatus'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/installedApps/{jobId}/result:
    get:
      tags:
        - folderManagement
      summary: Get Installed Apps folder job result
      description: Get result of an Installed Apps job for the given job identifier. The result will be "Installed Apps" folder with a list of top-level Installed Apps content items in `children` field.
      operationId: getInstalledAppsFolderAsyncResult
      parameters:
        - name: jobId
          in: path
          description: The identifier of the asynchronous Installed Apps folder job.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Installed Apps folder.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Folder'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/permissions:
    get:
      tags:
        - contentPermissions
      summary: Get permissions of a content item
      description: Returns content permissions of a content item with the given identifier.
      operationId: getContentPermissions
      parameters:
        - name: id
          in: path
          description: The identifier of the content item.
          required: true
          schema:
            type: string
        - name: explicitOnly
          in: query
          description: 'There are two permission types: explicit and implicit. Permissions specifically assigned to the content item are explicit. Permissions derived from a parent content item, like a folder are implicit. To return only explicit permissions set this to true.'
          schema:
            type: boolean
            default: false
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: A list of permissions for the requested content item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentPermissionResult'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/permissions/add:
    put:
      tags:
        - contentPermissions
      summary: Add permissions to a content item.
      description: Add permissions to a content item with the given identifier.
      operationId: addContentPermissions
      parameters:
        - name: id
          in: path
          description: The identifier of the content item.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      requestBody:
        description: New permissions to add to the content item with the given identifier.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContentPermissionUpdateRequest'
        required: true
      responses:
        '200':
          description: Updated permission object for the requested content item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentPermissionResult'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/permissions/remove:
    put:
      tags:
        - contentPermissions
      summary: Remove permissions from a content item.
      description: Remove permissions from a content item with the given identifier.
      operationId: removeContentPermissions
      parameters:
        - name: id
          in: path
          description: The identifier of the content item.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      requestBody:
        description: Permissions to remove from a content item with the given identifier.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContentPermissionUpdateRequest'
        required: true
      responses:
        '200':
          description: Updated permissions for the requested content item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentPermissionResult'
        default:
          description: Operation failed with an error. Check that your request is valid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/path:
    get:
      tags:
        - contentManagement
      summary: Get content item by path.
      description: |-
        Get a content item corresponding to the given path.

        _Path is specified in the required query parameter `path`. The path should be URL encoded._ For example, to get "Acme Corp" folder of a user "user@sumo.com" you can use the following curl command:
          ```bash
          curl https://api.sumologic.com/api/v2/content/path?path=/Library/Users/user%40sumo.com/Acme%20Corp
          ```


        The absolute path to a content item should be specified to get the item. The content library has "Library" folder at the root level. For items in "Personal" folder, the base path is "/Library/Users/user@sumo.com" where "user@sumo.com" is the email address of the user. For example if a user with email address `wile@acme.com` has `Rockets` folder inside Personal folder, the path of Rockets folder will be `/Library/Users/wile@acme.com/Rockets`.

        For items in "Admin Recommended" folder, the base path is "/Library/Admin Recommended". For example, given a folder `Acme` in Admin Recommended folder, the path will be `/Library/Admin Recommended/Acme`.
      operationId: getItemByPath
      parameters:
        - name: path
          in: query
          description: Path of the content item to retrieve.
          required: true
          schema:
            type: string
          example: /Library/Users/user@sumo.com/SampleFolder
      responses:
        '200':
          description: Content item corresponding to the given path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Content'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{contentId}/path:
    get:
      tags:
        - contentManagement
      summary: Get path of an item.
      description: |
        Get full path of a content item with the given identifier.
      operationId: getPathById
      parameters:
        - name: contentId
          in: path
          description: Identifier of the content item to get the path.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Full path of the content item.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentPath'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/export:
    post:
      tags:
        - contentManagement
      summary: Start a content export job.
      description: |-
        Schedule an _asynchronous_ export of content with the given identifier. You will get back an asynchronous job identifier on success. Use the getAsyncExportStatus endpoint and the job identifier you got back in the response to track the status of an asynchronous export job.
        If the content item is a folder, everything under the folder is exported recursively. Keep in mind when exporting large folders that there is a limit of 1000 content objects that can be exported at once. If you want to import more than 1000 content objects, then be sure to split the import into batches of 1000 objects or less.
        The results from the export are compatible with the Library import feature in the Sumo Logic user interface as well as the API content import job.
      operationId: beginAsyncExport
      parameters:
        - name: id
          in: path
          description: The identifier of the content item to export. Identifiers from the Library in the Sumo user interface are provided in decimal format which is incompatible with this API. The identifier needs to be in hexadecimal format.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Export job has been scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeginAsyncJobResponse'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{contentId}/export/{jobId}/status:
    get:
      tags:
        - contentManagement
      summary: Content export job status.
      description: Get the status of an asynchronous content export request for the given job identifier. On success, use the getExportResult endpoint to get the result of the export job.
      operationId: getAsyncExportStatus
      parameters:
        - name: contentId
          in: path
          description: The identifier of the exported content item.
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          description: The identifier of the asynchronous export job.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The status of the export job.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobStatus'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{contentId}/export/{jobId}/result:
    get:
      tags:
        - contentManagement
      summary: Content export job result.
      description: Get results from content export job for the given job identifier. The results from this export are incompatible with the Library import feature in the Sumo user interface.
      operationId: getAsyncExportResult
      parameters:
        - name: contentId
          in: path
          description: The identifier of the exported content item.
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          description: The identifier of the asynchronous job.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The result of export job.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentSyncDefinition'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/{folderId}/import:
    post:
      tags:
        - contentManagement
      summary: Start a content import job.
      description: Schedule an asynchronous import of content inside an existing folder with the given identifier. Import requests can be used to create or update content within a folder. Content items need to have a unique name within their folder. If there is already a content item with the same name in the folder, you can set the `overwrite` parameter to `true` to overwrite existing content items. By default, the `overwrite` parameter is set to `false`, where the import will fail if a content item with the same name already exist. Keep in mind when importing large folders that there is a limit of 1000 content objects that can be imported at once. If you want to import more than 1000 content objects, then be sure to split the import into batches of 1000 objects or less.
      operationId: beginAsyncImport
      parameters:
        - name: folderId
          in: path
          description: The identifier of the folder to import into. Identifiers from the Library in the Sumo user interface are provided in decimal format which is incompatible with this API. The identifier needs to be in hexadecimal format.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
        - name: overwrite
          in: query
          description: Set this to "true" to overwrite a content item if the name already exists.
          required: false
          schema:
            type: boolean
            default: false
      requestBody:
        description: The content to import.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContentSyncDefinition'
        required: true
      responses:
        '200':
          description: Import job has been scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeginAsyncJobResponse'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/{folderId}/import/{jobId}/status:
    get:
      tags:
        - contentManagement
      summary: Content import job status.
      description: Get the status of a content import job for the given job identifier.
      operationId: getAsyncImportStatus
      parameters:
        - name: folderId
          in: path
          description: The identifier of the folder to import into.
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          description: The identifier of the import request.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The status of the import job.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobStatus'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/folders/{folderId}/import/{jobId}/result:
    get:
      tags:
        - contentManagement
      summary: Content import job result.
      description: Get the complete summary of content import job for the given job identifier.
      operationId: getAsyncImportResult
      parameters:
        - name: folderId
          in: path
          description: The identifier of the folder to import into.
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          description: The identifier of the import request.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The result of the import job.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportResult'
        default:
          description: The operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/delete:
    delete:
      tags:
        - contentManagement
      summary: Start a content deletion job.
      description: Start an asynchronous content deletion job with the given identifier.
      operationId: beginAsyncDelete
      parameters:
        - name: id
          in: path
          description: Identifier of the content to delete. Identifiers from the Library in the Sumo user interface are provided in decimal format which is incompatible with this API. The identifier needs to be in hexadecimal format.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Deletion job has been scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeginAsyncJobResponse'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/delete/{jobId}/status:
    get:
      tags:
        - contentManagement
      summary: Content deletion job status.
      description: Get the status of an asynchronous content deletion job request for the given job identifier.
      operationId: getAsyncDeleteStatus
      parameters:
        - name: id
          in: path
          description: Identifier of the content to delete.
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          description: The identifier of the asynchronous job.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The status of the content deletion job.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobStatus'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/copy:
    post:
      tags:
        - contentManagement
      summary: Start a content copy job.
      description: Start an asynchronous content copy job with the given identifier to the destination folder. If the content item is a folder, everything under the folder is copied recursively.
      operationId: beginAsyncCopy
      parameters:
        - name: id
          in: path
          description: The identifier of the content item to copy. Identifiers from the Library in the Sumo user interface are provided in decimal format which is incompatible with this API. The identifier needs to be in hexadecimal format.
          required: true
          schema:
            type: string
        - name: destinationFolder
          in: query
          description: The identifier of the destination folder.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Content copy job has been scheduled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeginAsyncJobResponse'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/copy/{jobId}/status:
    get:
      tags:
        - contentManagement
      summary: Content copy job status.
      description: |
        Get the status of the copy request with the given job identifier. On success, field `statusMessage` will contain identifier of the newly copied content in format: `id: {hexIdentifier}`.
      operationId: asyncCopyStatus
      parameters:
        - name: id
          in: path
          description: The identifier of the content which was copied.
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          description: The identifier of the asynchronous copy request job.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: The status of the content copy job.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobStatus'
              example:
                status: Success
                statusMessage: 'id: 0000000000000197'
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v2/content/{id}/move:
    post:
      tags:
        - contentManagement
      summary: Move an item.
      description: |
        Moves an item from its current location to another folder.
      operationId: moveItem
      parameters:
        - name: destinationFolderId
          in: query
          description: Identifier of the destination folder.
          required: true
          schema:
            type: string
        - name: id
          in: path
          description: Identifier of the item the user wants to move.
          required: true
          schema:
            type: string
        - name: isAdminMode
          in: header
          description: Set this to "true" if you want to perform the request as a Content Administrator.
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Content was moved successfully.
        default:
          description: Operation failed with an error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    FolderDefinition:
      required:
        - name
        - parentId
      type: object
      properties:
        name:
          maxLength: 128
          minLength: 1
          type: string
          description: The name of the folder.
          example: SampleFolder
        description:
          maxLength: 255
          minLength: 0
          type: string
          description: The description of the folder.
          example: This is a sample folder.
        parentId:
          type: string
          description: The identifier of the parent folder.
    Folder:
      type: object
      required:
        - createdAt
        - createdBy
        - modifiedAt
        - modifiedBy
        - id
        - itemType
        - name
        - parentId
        - permissions
      properties:
        createdAt:
          type: string
          description: Creation timestamp in UTC in [RFC3339](https://tools.ietf.org/html/rfc3339) format.
          format: date-time
          example: '2018-10-16T09:10:00.000Z'
        createdBy:
          type: string
          description: Identifier of the user who created the resource.
          example: 0000000006743FDD
        modifiedAt:
          type: string
          description: Last modification timestamp in UTC.
          format: date-time
          example: '2018-10-16T09:10:00.000Z'
        modifiedBy:
          type: string
          description: Identifier of the user who last modified the resource.
          example: 0000000006743FE8
        id:
          type: string
          description: Identifier of the content item.
          example: 000000000C1C17C6
        name:
          type: string
          description: The name of the content item.
          example: Personal
        itemType:
          type: string
          description: |-
            Type of the content item. Supported values are:
              1. Folder
              2. Search
              3. Report (for old dashboards)
              4. Dashboard (for new dashboards)
              5. Lookups
          example: Folder
        parentId:
          type: string
          description: Identifier of the parent content item.
          example: 0000000001C41EF2
        permissions:
          type: array
          description: List of permissions the user has on the content item.
          example:
            - View
            - GrantView
            - Edit
          items:
            type: string
        description:
          type: string
          description: Description of the content item.
          example: Personal folder for John Doe
        isScheduled:
          type: boolean
          description: Indicates whether the content item refers to scheduled search. This field is only relevant to `Search` content type.
          example: false
          default: false
        children:
          type: array
          description: A list of the content items.
          items:
            $ref: '#/components/schemas/Content'
    ErrorResponse:
      required:
        - errors
        - id
      type: object
      properties:
        id:
          type: string
          description: An identifier for the error; this is unique to the specific API request.
          example: IUUQI-DGH5I-TJ045
        errors:
          type: array
          description: A list of one or more causes of the error.
          example:
            - code: auth:password_too_short
              message: Your password was too short.
            - code: auth:password_character_classes
              message: Your password did not contain any non-alphanumeric characters
          items:
            $ref: '#/components/schemas/ErrorDescription'
    UpdateFolderRequest:
      required:
        - name
      type: object
      properties:
        name:
          maxLength: 128
          minLength: 1
          type: string
          description: The name of the folder.
          example: SampleFolder
        description:
          maxLength: 255
          minLength: 0
          type: string
          description: The description of the folder.
          example: This is a sample folder.
    BeginAsyncJobResponse:
      required:
        - id
      type: object
      properties:
        id:
          type: string
          description: Identifier to get the status of an asynchronous job.
          example: C03E086C137F38B4
    AsyncJobStatus:
      required:
        - status
      type: object
      properties:
        status:
          type: string
          description: Whether or not the request is in progress (`InProgress`), has completed successfully (`Success`), or has completed with an error (`Failed`).
        statusMessage:
          type: string
          description: Additional status message generated if the status is not `Failed`.
        error:
          $ref: '#/components/schemas/ErrorDescription'
      example:
        status: Success
        statusMessage: ''
    ContentList:
      required:
        - data
      type: object
      properties:
        data:
          type: array
          description: A list of the content items.
          items:
            $ref: '#/components/schemas/Content'
    ContentPermissionResult:
      required:
        - explicitPermissions
      type: object
      properties:
        explicitPermissions:
          type: array
          description: Explicitly assigned content permissions.
          items:
            $ref: '#/components/schemas/ContentPermissionAssignment'
        implicitPermissions:
          type: array
          description: Implicitly inherited content permissions.
          items:
            $ref: '#/components/schemas/ContentPermissionAssignment'
    ContentPermissionUpdateRequest:
      required:
        - contentPermissionAssignments
        - notificationMessage
        - notifyRecipients
      type: object
      properties:
        contentPermissionAssignments:
          type: array
          description: Content permissions to be updated.
          items:
            $ref: '#/components/schemas/ContentPermissionAssignment'
        notifyRecipients:
          type: boolean
          description: Set this to "true" to notify the users who had a permission update.
        notificationMessage:
          type: string
          description: The notification message sent to the users who had a permission update.
    Content:
      type: object
      required:
        - createdAt
        - createdBy
        - modifiedAt
        - modifiedBy
        - id
        - itemType
        - name
        - parentId
        - permissions
      properties:
        createdAt:
          type: string
          description: Creation timestamp in UTC in [RFC3339](https://tools.ietf.org/html/rfc3339) format.
          format: date-time
          example: '2018-10-16T09:10:00.000Z'
        createdBy:
          type: string
          description: Identifier of the user who created the resource.
          example: 0000000006743FDD
        modifiedAt:
          type: string
          description: Last modification timestamp in UTC.
          format: date-time
          example: '2018-10-16T09:10:00.000Z'
        modifiedBy:
          type: string
          description: Identifier of the user who last modified the resource.
          example: 0000000006743FE8
        id:
          type: string
          description: Identifier of the content item.
          example: 000000000C1C17C6
        name:
          type: string
          description: The name of the content item.
          example: Personal
        itemType:
          type: string
          description: |-
            Type of the content item. Supported values are:
              1. Folder
              2. Search
              3. Report (for old dashboards)
              4. Dashboard (for new dashboards)
              5. Lookups
          example: Folder
        parentId:
          type: string
          description: Identifier of the parent content item.
          example: 0000000001C41EF2
        permissions:
          type: array
          description: List of permissions the user has on the content item.
          example:
            - View
            - GrantView
            - Edit
          items:
            type: string
        description:
          type: string
          description: Description of the content item.
          example: Personal folder for John Doe
        isScheduled:
          type: boolean
          description: Indicates whether the content item refers to scheduled search. This field is only relevant to `Search` content type.
          example: false
          default: false
    ContentPath:
      required:
        - path
      type: object
      properties:
        path:
          type: string
          description: Path of the content item.
          example: /Library/Users/user@test.com/SampleFolder
        pathItems:
          type: array
          description: The items in the path of the content.
          items:
            $ref: '#/components/schemas/PathSegment'
    ContentSyncDefinition:
      required:
        - name
        - type
      type: object
      properties:
        type:
          type: string
          description: |-
            The content item type.
            **Note:**
             - `MewboardSyncDefinition` _is depreciated, and will soon be removed. Please use_ `DashboardV2SyncDefinition`
               _instead_.
             - Dashboard links are not supported for dashboards.
        name:
          type: string
          description: The name of the item.
      discriminator:
        propertyName: type
    ImportResult:
      type: object
      properties:
        status:
          type: string
          description: Whether or not the request is in progress (`InProgress`), has completed successfully (`Success`), or has completed with an error (`Failed`).
        summary:
          type: object
          properties:
            totalItems:
              type: integer
              description: Total content items attempted in the import.
              example: 15
            successCount:
              type: integer
              description: Number of content items successfully imported.
              example: 12
            failureCount:
              type: integer
              description: Number of content items that failed to import.
              example: 3
          description: Summary about the import job indicating total, success and failure count.
        failures:
          type: array
          description: Detailed listing of failed import items.
          items:
            $ref: '#/components/schemas/ImportErrorResultItem'
    ErrorDescription:
      required:
        - code
        - message
      type: object
      properties:
        code:
          type: string
          description: An error code describing the type of error.
          example: auth:password_too_short
        message:
          type: string
          description: A short English-language description of the error.
          example: Your password was too short.
        detail:
          type: string
          description: An optional fuller English-language description of the error.
          example: Your password was 5 characters long, the minimum length is 12 characters. See http://example.com/password for more information.
        meta:
          type: string
          description: An optional list of metadata about the error. (opaque JSON object)
          example:
            minLength: 12
            actualLength: 5
    ContentPermissionAssignment:
      required:
        - contentId
        - permissionName
        - sourceId
        - sourceType
      type: object
      properties:
        permissionName:
          pattern: ^(View|GrantView|Edit|GrantEdit|Manage|GrantManage)$
          type: string
          description: 'Content permission name. Valid values are: `View`, `GrantView`, `Edit`, `GrantEdit`, `Manage`, and `GrantManage`.'
          x-pattern-message: 'must be one of the following: `View`, `GrantView`, `Edit`, `GrantEdit`, `Manage`, `GrantManage`'
        sourceType:
          pattern: ^(user|role|org)$
          type: string
          description: 'Type of source for the permission. Valid values are: `user`, `role`, and `org`.'
          example: role
          x-pattern-message: 'must be one of the following: `user`, `role`, `org`'
        sourceId:
          type: string
          description: An identifier that belongs to the source type chosen above. For e.g. if the sourceType is set to "user", sourceId should be identifier of a user (same goes for `role` and `org` sourceType)
        contentId:
          type: string
          description: Unique identifier for the content item.
    MetadataModel:
      required:
        - createdAt
        - createdBy
        - modifiedAt
        - modifiedBy
      type: object
      properties:
        createdAt:
          type: string
          description: Creation timestamp in UTC in [RFC3339](https://tools.ietf.org/html/rfc3339) format.
          format: date-time
          example: '2018-10-16T09:10:00.000Z'
        createdBy:
          type: string
          description: Identifier of the user who created the resource.
          example: 0000000006743FDD
        modifiedAt:
          type: string
          description: Last modification timestamp in UTC.
          format: date-time
          example: '2018-10-16T09:10:00.000Z'
        modifiedBy:
          type: string
          description: Identifier of the user who last modified the resource.
          example: 0000000006743FE8
    PathSegment:
      required:
        - id
        - name
      type: object
      properties:
        id:
          type: string
          description: The identifier of the path segment.
          example: 0000000013D98A2A
        name:
          type: string
          description: The name of the path segment.
          example: Test Folder
        description:
          type: string
          description: An optional description of the path segment.
          example: This is a test folder
      description: A segment of a path.
    ImportErrorResultItem:
      type: object
      properties:
        path:
          type: string
          description: Full folder path to the failed item.
          example: /Marketing/Website Analytics/Daily Traffic Report
        type:
          type: string
          description: The type of the content item (e.g., Folder, Search, Dashboard).
          example: Dashboard
        error:
          type: string
          description: Reason why the item failed to import.
          example: Invalid JSON format in widget configuration.
  x-stackQL-resources:
    folders:
      id: sumologic.content.folders
      name: folders
      title: Folders
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1v2~1content~1folders/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            mediaType: application/json
            nativeCasing: camel
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1{id}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1{id}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            mediaType: application/json
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/folders/methods/get'
        insert:
          - $ref: '#/components/x-stackQL-resources/folders/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/folders/methods/update'
        delete: []
        replace: []
    personal_folder:
      id: sumologic.content.personal_folder
      name: personal_folder
      title: Personal Folder
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1personal/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/personal_folder/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    global_folder_jobs:
      id: sumologic.content.global_folder_jobs
      name: global_folder_jobs
      title: Global Folder Jobs
      methods:
        start:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1global/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1global~1{jobId}~1status/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/global_folder_jobs/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    global_folder_results:
      id: sumologic.content.global_folder_results
      name: global_folder_results
      title: Global Folder Results
      methods:
        list:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1global~1{jobId}~1result/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.data
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/global_folder_results/methods/list'
        insert: []
        update: []
        delete: []
        replace: []
    admin_recommended_folder_jobs:
      id: sumologic.content.admin_recommended_folder_jobs
      name: admin_recommended_folder_jobs
      title: Admin Recommended Folder Jobs
      methods:
        start:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1adminRecommended/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1adminRecommended~1{jobId}~1status/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/admin_recommended_folder_jobs/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    admin_recommended_folder_results:
      id: sumologic.content.admin_recommended_folder_results
      name: admin_recommended_folder_results
      title: Admin Recommended Folder Results
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1adminRecommended~1{jobId}~1result/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/admin_recommended_folder_results/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    installed_apps_folder_jobs:
      id: sumologic.content.installed_apps_folder_jobs
      name: installed_apps_folder_jobs
      title: Installed Apps Folder Jobs
      methods:
        start:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1installedApps/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1installedApps~1{jobId}~1status/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/installed_apps_folder_jobs/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    installed_apps_folder_results:
      id: sumologic.content.installed_apps_folder_results
      name: installed_apps_folder_results
      title: Installed Apps Folder Results
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1installedApps~1{jobId}~1result/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/installed_apps_folder_results/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    permissions:
      id: sumologic.content.permissions
      name: permissions
      title: Permissions
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1permissions/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        add:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1permissions~1add/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            mediaType: application/json
            nativeCasing: camel
        remove:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1permissions~1remove/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            mediaType: application/json
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/permissions/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    items:
      id: sumologic.content.items
      name: items
      title: Items
      methods:
        get_by_path:
          operation:
            $ref: '#/paths/~1v2~1content~1path/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        move:
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1move/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/items/methods/get_by_path'
        insert: []
        update: []
        delete: []
        replace: []
    paths:
      id: sumologic.content.paths
      name: paths
      title: Paths
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1{contentId}~1path/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/paths/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    export_jobs:
      id: sumologic.content.export_jobs
      name: export_jobs
      title: Export Jobs
      methods:
        start:
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1export/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1{contentId}~1export~1{jobId}~1status/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/export_jobs/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    export_results:
      id: sumologic.content.export_results
      name: export_results
      title: Export Results
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1{contentId}~1export~1{jobId}~1result/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/export_results/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    import_jobs:
      id: sumologic.content.import_jobs
      name: import_jobs
      title: Import Jobs
      methods:
        start:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1{folderId}~1import/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            mediaType: application/json
            nativeCasing: camel
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1{folderId}~1import~1{jobId}~1status/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/import_jobs/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    import_results:
      id: sumologic.content.import_results
      name: import_results
      title: Import Results
      methods:
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1folders~1{folderId}~1import~1{jobId}~1result/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/import_results/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    delete_jobs:
      id: sumologic.content.delete_jobs
      name: delete_jobs
      title: Delete Jobs
      methods:
        start:
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1delete/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1delete~1{jobId}~1status/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/delete_jobs/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    copy_jobs:
      id: sumologic.content.copy_jobs
      name: copy_jobs
      title: Copy Jobs
      methods:
        start:
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1copy/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1v2~1content~1{id}~1copy~1{jobId}~1status/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/copy_jobs/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
servers:
  - url: https://api.{region}.sumologic.com/api
    description: Sumo Logic deployment API endpoint
    variables:
      region:
        description: Sumo Logic deployment (au, ca, ch, de, eu, fed, in, jp, kr, us1, us2). Resolved from the SUMOLOGIC_ENVIRONMENT environment variable when it is set (x-stackQL-envVar, the same variable the Terraform provider reads); otherwise defaults to us2. A WHERE region = '...' value always takes precedence.
        enum:
          - au
          - ca
          - ch
          - de
          - eu
          - fed
          - in
          - jp
          - kr
          - us1
          - us2
        default: us2
        x-stackQL-envVar: SUMOLOGIC_ENVIRONMENT
x-stackQL-config:
  pagination:
    requestToken:
      key: token
      location: query
    responseToken:
      key: next
      location: body
