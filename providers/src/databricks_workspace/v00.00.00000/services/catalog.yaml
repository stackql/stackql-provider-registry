openapi: 3.0.0
info:
  title: Databricks Catalog API (workspace)
  description: >-
    OpenAPI specification for the Databricks catalog service (workspace-level
    APIs), generated from the Databricks Python SDK.
  version: 0.1.0
  x-stackql-sdk-version: 0.123.0
  x-stackql-date-generated: '2026-07-31'
  x-stackql-sdk-namespace: databricks.sdk.service.catalog
servers:
  - url: https://{deployment_name}.cloud.databricks.com
    variables:
      deployment_name:
        description: The Databricks Workspace Deployment Name
        default: dbc-abcd0123-a1bc
paths:
  /api/2.1/unity-catalog/agent-services:
    post:
      operationId: ai_gateway_create_agent_service
      summary: >-
        Creates an agent service in a Unity Catalog schema. An agent service is
        a governed securable that
      tags:
        - catalog
        - ai_gateway
      description: >-
        Creates an agent service in a Unity Catalog schema. An agent service is
        a governed securable that

        registers an AI agent and exposes it for discovery, access control, and
        auditing. The caller supplies

        the leaf name in ``agent_service_id`` and the agent service type, which
        is immutable after creation.


        You must be the owner of the parent schema or have the
        ``CREATE_SERVICE`` and ``USE_SCHEMA``

        privileges on the parent schema and ``USE_CATALOG`` on the parent
        catalog.


        :param agent_service: :class:`AgentService`
          The agent service to create. The server populates ``name`` from ``parent`` + ``agent_service_id``;
          clients should leave it unset.
        :param parent: str
          Resource name of the parent schema. Format: ``schemas/{catalog}.{schema}``. Each ``{...}`` component
          is capped at 255 characters individually.
        :param agent_service_id: str
          Leaf identifier for the agent service (the unqualified name within the parent schema, e.g.
          "support_agent").

        :returns: :class:`AgentService`
      parameters:
        - name: parent
          in: query
          required: true
          schema:
            type: string
          description: >-
            Resource name of the parent schema. Format:
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
        - name: agent_service_id
          in: query
          required: true
          schema:
            type: string
          description: >-
            Leaf identifier for the agent service (the unqualified name within
            the parent schema, e.g. "support_agent").
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                agent_service:
                  type: object
                  properties:
                    agent_service_type:
                      type: string
                      x-enum:
                        - AGENT_SERVICE_TYPE_EXTERNAL
                      description: >-
                        Type of agent service. Required on create. Immutable
                        after creation. For future internal / genie / app kinds,
                        determines how the backing agent surface is provisioned.
                    browse_only:
                      type: boolean
                      description: >-
                        Whether the caller sees only metadata available through
                        the BROWSE privilege.
                    comment:
                      type: string
                      description: User-provided description.
                    config:
                      type: object
                      properties:
                        base_path:
                          type: string
                          description: >-
                            Vendor base path appended to the connection url at
                            invocation time. Bedrock example:
                            ``/agents/AGENT123/agentAliases/ALIAS1/sessions``.
                            Empty when the agent platform does not require a
                            per-agent suffix.
                        inference_table:
                          type: object
                          properties:
                            parent:
                              type: string
                              description: >-
                                Parent UC schema where the inference table is
                                created. Format: ``schemas/{catalog}.{schema}``.
                                Set at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                            disabled:
                              type: boolean
                              description: >-
                                Indicates whether payload logging is disabled
                                (opt-out). Unset means that payload logging is
                                active (the on-by-default state coincides with
                                the proto zero-value, so the server never fills
                                this field for a client that leaves it unset).
                                Set ``disabled = true`` to pause runtime logging
                                while keeping the sub-message attached
                                (preserving ``parent`` and ``table_name_prefix``
                                for a later flip back to active). ``parent``
                                remains required either way.
                            is_deleted:
                              type: boolean
                              description: >-
                                True when the bound inference TABLE has been
                                deleted but the parent service still references
                                it. The dangling reference is surfaced (not
                                silently dropped) so callers can see the broken
                                dependency. AI Gateway payload logging fails
                                closed in this state.
                            table:
                              type: string
                              description: >-
                                Resolved UC table for payload logs. Format:
                                ``tables/{catalog}.{schema}.{table}``.
                            table_name_prefix:
                              type: string
                              description: >-
                                Prefix for the inference-table's UC-registered
                                name. The actual leaf name UC stores is
                                ``<table_name_prefix>_payload``; the
                                ``_payload`` suffix is appended automatically.
                                To find the actual UC table after Create, read
                                the ``table`` field on the response. Defaults to
                                ``<model_service_name>_payload`` when unset. Set
                                at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                          required:
                            - parent
                          description: >-
                            Inference table config for invocation payload
                            logging. Reuses the shared InferenceTableConfig
                            (same as ModelServiceConfig.inference_table).
                        model_service:
                          type: string
                          description: >-
                            Resource name of the model service backing this
                            agent. Empty when the agent platform manages its own
                            model selection (e.g. Bedrock Agents). Format:
                            ``model-services/{catalog}.{schema}.{model_service}``.
                        rate_limits:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_KEY_REQUEST_TAG
                                  - RATE_LIMIT_KEY_SERVICE
                                  - RATE_LIMIT_KEY_SERVICE_PRINCIPAL
                                  - RATE_LIMIT_KEY_USER
                                  - RATE_LIMIT_KEY_USER_DEFAULT
                                  - RATE_LIMIT_KEY_USER_GROUP
                                description: >-
                                  Scope key. Determines whether ``principal`` is
                                  required.
                              renewal_period:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_RENEWAL_PERIOD_HOUR
                                  - RATE_LIMIT_RENEWAL_PERIOD_MINUTE
                                description: Renewal period.
                              principal:
                                type: string
                                description: >-
                                  Principal this limit applies to: user email,
                                  group name, or service principal application
                                  ID. Required unless ``key`` is
                                  ``RATE_LIMIT_KEY_SERVICE``,
                                  ``RATE_LIMIT_KEY_USER_DEFAULT``, or
                                  ``RATE_LIMIT_KEY_REQUEST_TAG`` (which must not
                                  set a principal).
                              request_tag_key:
                                type: string
                                description: >-
                                  Request tag key this limit applies to.
                                  Required when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``, forbidden
                                  otherwise.
                              request_tag_value:
                                type: string
                                description: >-
                                  Request tag value this limit applies to. Only
                                  valid when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``. Leave unset to
                                  apply the limit to every value of
                                  ``request_tag_key`` (an any-value default); a
                                  set value is a specific override for that
                                  value.
                              requests:
                                type: integer
                                description: >-
                                  Max requests allowed within a renewal period.
                                  Leave unset for no request limit.
                              tokens:
                                type: integer
                                description: >-
                                  Max tokens allowed within a renewal period.
                                  Leave unset for no token limit.
                            required:
                              - key
                              - renewal_period
                            description: >-
                              A rate limit applied to service requests. Leave
                              ``requests`` or ``tokens`` unset to impose no
                                  limit on that dimension; set a value to cap that dimension within the renewal period.
                          description: >-
                            Per-principal rate limits applied to invocations
                            routed through this agent service. Repeated to
                            support per-USER / USER_GROUP / SERVICE_PRINCIPAL /
                            SERVICE / USER_DEFAULT scopes simultaneously,
                            mirroring McpServiceConfig.rate_limits. Empty when
                            no rate limit is configured.
                        source_connection:
                          type: object
                          properties:
                            name:
                              type: string
                            is_deleted:
                              type: boolean
                          required:
                            - name
                          description: External agent referenced via a UC Connection.
                        system_prompt:
                          type: string
                          description: System prompt prepended to invocations. Max 8 KB.
                      description: >-
                        Operational configuration: connection, base_path,
                        system_prompt, model_service, optional inference table,
                        optional rate limits. Required on CreateAgentService; on
                        UpdateAgentService it is required only when ``config``
                        (or a ``config.*`` subpath) appears in ``update_mask``.
                    create_time:
                      type: string
                      format: date-time
                      description: When the agent service was created.
                    created_by:
                      type: string
                      description: Creator identity.
                    effective_owner:
                      type: string
                      description: >-
                        The resolved owner of the agent service. Falls back to
                        the caller's identity when ``owner`` is not explicitly
                        set on creation.
                    etag:
                      type: string
                      description: >-
                        Optimistic concurrency control token. Server-generated
                        from the entity's state and returned on every read. To
                        use it as an if-match precondition on a mutation, echo
                        the last-read value back via the dedicated ``etag``
                        field on the Update / Delete request; the server rejects
                        the mutation if the stored etag differs.
                    metastore_id:
                      type: string
                      description: Metastore hosting the agent service.
                    name:
                      type: string
                      description: >-
                        Resource name of the agent service. Format:
                        ``agent-services/{catalog}.{schema}.{agent_service}``.
                        Each ``{...}`` component is capped at 255 characters
                        individually. Server-derived on Create from ``parent`` +
                        ``agent_service_id``; required and immutable on
                        Update/Get/Delete.
                    owner:
                      type: string
                      description: >-
                        The owner of the agent service. Write-only; read owner
                        via effective_owner.
                    update_time:
                      type: string
                      format: date-time
                      description: When the agent service was last modified.
                    updated_by:
                      type: string
                      description: Identity of the last updater.
                  description: >-
                    The agent service to create. The server populates ``name``
                    from ``parent`` + ``agent_service_id``; clients should leave
                    it unset.
              required:
                - agent_service
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentService'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
    get:
      operationId: ai_gateway_list_agent_services
      summary: >-
        Lists the agent services in a Unity Catalog schema. Provide ``parent``
        as
      tags:
        - catalog
        - ai_gateway
      description: >-
        Lists the agent services in a Unity Catalog schema. Provide ``parent``
        as

        ``schemas/{catalog}.{schema}``. Results are paginated; pass the returned
        ``next_page_token`` to fetch

        subsequent pages.


        Requires ``USE_CATALOG`` on the parent catalog and ``USE_SCHEMA`` on the
        parent schema. Only agent

        services the caller can access (as owner or through ``EXECUTE``,
        ``READ_METADATA``, or ``MANAGE``) are

        returned.


        :param include_browse: bool (optional)
          Whether to include agent services for which the principal can only access selective metadata.
        :param page_size: int (optional)
          Maximum number of agent services to return. Defaults to 100 when unset or 0; the maximum is 1000.
          Use ``next_page_token`` to retrieve additional pages.
        :param page_token: str (optional)
          Opaque pagination token from a previous request.
        :param parent: str (optional)
          Resource name of the parent schema to list within, as ``schemas/{catalog}.{schema}``. Each ``{...}``
          component is capped at 255 characters individually.

        :returns: Iterator over :class:`AgentService`
      parameters:
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include agent services for which the principal can only
            access selective metadata.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of agent services to return. Defaults to 100 when
            unset or 0; the maximum is 1000. Use ``next_page_token`` to retrieve
            additional pages.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token from a previous request.
        - name: parent
          in: query
          required: false
          schema:
            type: string
          description: >-
            Resource name of the parent schema to list within, as
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAgentServicesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
  /api/2.1/unity-catalog/mcp-services:
    post:
      operationId: ai_gateway_create_mcp_service
      summary: >-
        Creates an MCP service in a Unity Catalog schema. An MCP (Model Context
        Protocol) service is a
      tags:
        - catalog
        - ai_gateway
      description: >-
        Creates an MCP service in a Unity Catalog schema. An MCP (Model Context
        Protocol) service is a

        governed securable that registers an MCP server and exposes its tools
        for discovery, access control,

        and invocation. The caller supplies the leaf name in ``mcp_service_id``.


        You must be the owner of the parent schema or have the
        ``CREATE_SERVICE`` and ``USE_SCHEMA``

        privileges on the parent schema and ``USE_CATALOG`` on the parent
        catalog. You also need

        ``USE_CONNECTION`` on the connection the MCP service references.


        :param mcp_service: :class:`McpService`
          The MCP service to create. The server populates ``name`` from ``parent`` + ``mcp_service_id``;
          clients should leave it unset.
        :param parent: str
          Resource name of the parent schema. Format: ``schemas/{catalog}.{schema}``. Each ``{...}`` component
          is capped at 255 characters individually.
        :param mcp_service_id: str
          Leaf identifier for the MCP service (the unqualified name within the parent schema, e.g.
          "my_mcp_service").

        :returns: :class:`McpService`
      parameters:
        - name: parent
          in: query
          required: true
          schema:
            type: string
          description: >-
            Resource name of the parent schema. Format:
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
        - name: mcp_service_id
          in: query
          required: true
          schema:
            type: string
          description: >-
            Leaf identifier for the MCP service (the unqualified name within the
            parent schema, e.g. "my_mcp_service").
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                mcp_service:
                  type: object
                  properties:
                    browse_only:
                      type: boolean
                      description: >-
                        Whether the caller sees only metadata available through
                        the BROWSE privilege.
                    comment:
                      type: string
                      description: User-provided description.
                    config:
                      type: object
                      properties:
                        include_tool_selectors:
                          type: array
                          items:
                            type: string
                          description: >-
                            Glob or exact-match patterns selecting which tools
                            from the MCP server to expose. Prefix match for
                            patterns with ``*``, exact match otherwise. An empty
                            list means all tools are included. Per-element max
                            256 chars.
                        rate_limits:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_KEY_REQUEST_TAG
                                  - RATE_LIMIT_KEY_SERVICE
                                  - RATE_LIMIT_KEY_SERVICE_PRINCIPAL
                                  - RATE_LIMIT_KEY_USER
                                  - RATE_LIMIT_KEY_USER_DEFAULT
                                  - RATE_LIMIT_KEY_USER_GROUP
                                description: >-
                                  Scope key. Determines whether ``principal`` is
                                  required.
                              renewal_period:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_RENEWAL_PERIOD_HOUR
                                  - RATE_LIMIT_RENEWAL_PERIOD_MINUTE
                                description: Renewal period.
                              principal:
                                type: string
                                description: >-
                                  Principal this limit applies to: user email,
                                  group name, or service principal application
                                  ID. Required unless ``key`` is
                                  ``RATE_LIMIT_KEY_SERVICE``,
                                  ``RATE_LIMIT_KEY_USER_DEFAULT``, or
                                  ``RATE_LIMIT_KEY_REQUEST_TAG`` (which must not
                                  set a principal).
                              request_tag_key:
                                type: string
                                description: >-
                                  Request tag key this limit applies to.
                                  Required when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``, forbidden
                                  otherwise.
                              request_tag_value:
                                type: string
                                description: >-
                                  Request tag value this limit applies to. Only
                                  valid when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``. Leave unset to
                                  apply the limit to every value of
                                  ``request_tag_key`` (an any-value default); a
                                  set value is a specific override for that
                                  value.
                              requests:
                                type: integer
                                description: >-
                                  Max requests allowed within a renewal period.
                                  Leave unset for no request limit.
                              tokens:
                                type: integer
                                description: >-
                                  Max tokens allowed within a renewal period.
                                  Leave unset for no token limit.
                            required:
                              - key
                              - renewal_period
                            description: >-
                              A rate limit applied to service requests. Leave
                              ``requests`` or ``tokens`` unset to impose no
                                  limit on that dimension; set a value to cap that dimension within the renewal period.
                          description: >-
                            Per-principal rate limits applied to tool
                            invocations routed through this MCP service.
                            Repeated to support per-USER / USER_GROUP /
                            SERVICE_PRINCIPAL / SERVICE / USER_DEFAULT scopes
                            simultaneously, mirroring the
                            ``ModelServiceConfig.rate_limits`` shape. Empty when
                            no rate limit is configured.
                        source_connection:
                          type: object
                          properties:
                            name:
                              type: string
                            is_deleted:
                              type: boolean
                          required:
                            - name
                          description: UC Connection referencing the MCP server.
                      description: >-
                        Operational configuration: connection, tool selectors,
                        rate limit. Required on CreateMcpService; on
                        UpdateMcpService it is required only when ``config`` (or
                        a ``config.*`` subpath) appears in ``update_mask``.
                    create_time:
                      type: string
                      format: date-time
                      description: When the MCP service was created.
                    created_by:
                      type: string
                      description: Creator identity.
                    effective_owner:
                      type: string
                      description: >-
                        The resolved owner of the MCP service. Falls back to the
                        caller's identity when ``owner`` is not explicitly set
                        on creation.
                    etag:
                      type: string
                      description: >-
                        Optimistic concurrency control token. Server-generated
                        from the entity's state and returned on every read. To
                        use it as an if-match precondition on a mutation, echo
                        the last-read value back via the dedicated ``etag``
                        field on the Update / Delete request; the server rejects
                        the mutation if the stored etag differs.
                    metastore_id:
                      type: string
                      description: Metastore hosting the MCP service.
                    name:
                      type: string
                      description: >-
                        Resource name of the MCP service. Format:
                        ``mcp-services/{catalog}.{schema}.{mcp_service}``. Each
                        ``{...}`` component is capped at 255 characters
                        individually. Server-derived on Create from ``parent`` +
                        ``mcp_service_id``; required and immutable on
                        Update/Get/Delete.
                    owner:
                      type: string
                      description: >-
                        The owner of the MCP service. Write-only; read owner via
                        effective_owner.
                    update_time:
                      type: string
                      format: date-time
                      description: When the MCP service was last modified.
                    updated_by:
                      type: string
                      description: Identity of the last updater.
                  description: >-
                    The MCP service to create. The server populates ``name``
                    from ``parent`` + ``mcp_service_id``; clients should leave
                    it unset.
              required:
                - mcp_service
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpService'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
    get:
      operationId: ai_gateway_list_mcp_services
      summary: Lists the MCP services in a Unity Catalog schema. Provide ``parent`` as
      tags:
        - catalog
        - ai_gateway
      description: >-
        Lists the MCP services in a Unity Catalog schema. Provide ``parent`` as

        ``schemas/{catalog}.{schema}``. Results are paginated; pass the returned
        ``next_page_token`` to fetch

        subsequent pages.


        Requires ``USE_CATALOG`` on the parent catalog and ``USE_SCHEMA`` on the
        parent schema. Only MCP

        services the caller can access (as owner or through ``EXECUTE``,
        ``READ_METADATA``, or ``MANAGE``) are

        returned.


        :param include_browse: bool (optional)
          Whether to include MCP services for which the principal can only access selective metadata.
        :param page_size: int (optional)
          Maximum number of MCP services to return. Defaults to 100 when unset or 0; the maximum is 1000. Use
          ``next_page_token`` to retrieve additional pages.
        :param page_token: str (optional)
          Opaque pagination token from a previous request.
        :param parent: str (optional)
          Resource name of the parent schema to list within, as ``schemas/{catalog}.{schema}``. Each ``{...}``
          component is capped at 255 characters individually.
        :param view: :class:`ListMcpServicesRequestView` (optional)
          View selector controlling which fields are populated per row.

        :returns: Iterator over :class:`McpService`
      parameters:
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include MCP services for which the principal can only
            access selective metadata.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of MCP services to return. Defaults to 100 when unset
            or 0; the maximum is 1000. Use ``next_page_token`` to retrieve
            additional pages.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token from a previous request.
        - name: parent
          in: query
          required: false
          schema:
            type: string
          description: >-
            Resource name of the parent schema to list within, as
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
        - name: view
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/ListMcpServicesRequestView'
          description: View selector controlling which fields are populated per row.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMcpServicesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
  /api/2.1/unity-catalog/model-provider-services:
    post:
      operationId: ai_gateway_create_model_provider_service
      summary: >-
        Creates a model provider service in a Unity Catalog schema. A model
        provider service is a governed
      tags:
        - catalog
        - ai_gateway
      description: >-
        Creates a model provider service in a Unity Catalog schema. A model
        provider service is a governed

        connection to an external model provider (for example OpenAI, Azure
        OpenAI, or Amazon Bedrock) that

        model services reference to invoke that provider. The caller supplies
        the leaf name in

        ``model_provider_service_id``.


        You must be the owner of the parent schema or have the
        ``CREATE_SERVICE`` and ``USE_SCHEMA``

        privileges on the parent schema and ``USE_CATALOG`` on the parent
        catalog.


        :param model_provider_service: :class:`ModelProviderService`
          The model provider service to create. The server populates ``name`` from ``parent`` +
          ``model_provider_service_id``; clients should leave it unset.
        :param parent: str
          Resource name of the parent schema. Format: ``schemas/{catalog}.{schema}``. Each ``{...}`` component
          is capped at 255 characters individually.
        :param model_provider_service_id: str
          Leaf identifier for the provider service (the unqualified name within the parent schema, e.g.
          "openai_prod").

        :returns: :class:`ModelProviderService`
      parameters:
        - name: parent
          in: query
          required: true
          schema:
            type: string
          description: >-
            Resource name of the parent schema. Format:
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
        - name: model_provider_service_id
          in: query
          required: true
          schema:
            type: string
          description: >-
            Leaf identifier for the provider service (the unqualified name
            within the parent schema, e.g. "openai_prod").
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                model_provider_service:
                  type: object
                  properties:
                    browse_only:
                      type: boolean
                      description: >-
                        Whether the caller sees only metadata available through
                        the BROWSE privilege.
                    comment:
                      type: string
                      description: User-provided description.
                    config:
                      type: object
                      properties:
                        allow_all_targets:
                          type: boolean
                          description: >-
                            When true, accepts any model exposed by the upstream
                            provider; ``targets`` is not required and does not
                            restrict routability. When false, only models listed
                            in ``targets`` are routable.
                        amazon_bedrock:
                          type: object
                          properties:
                            direct:
                              type: object
                              properties:
                                aws_access_key:
                                  type: object
                                  properties:
                                    access_key_id:
                                      type: string
                                      description: >-
                                        AWS access key ID. Required on Create
                                        when using access-key auth. Treated as
                                        username-equivalent (not a secret
                                        value): round-trips on reads and is
                                        scrubbed from audit logs.
                                    secret_access_key:
                                      type: object
                                      properties:
                                        plaintext:
                                          type: string
                                          description: >-
                                            Inline plaintext credential. INPUT_ONLY:
                                            the value never round-trips on reads.
                                            Get and List responses omit
                                            ``plaintext``; the field's presence in
                                            the read shape only indicates that a
                                            secret is configured.
                                      description: >-
                                        AWS secret access key paired with
                                        ``access_key_id``. Required on Create
                                        when using access-key auth. Supplied as
                                        inline plaintext via
                                        ``ProviderSecret.plaintext``.
                                  description: >-
                                    AWS access-key-pair auth. Mutually exclusive
                                    with ``service_credential``. Supersedes the
                                    flat ``aws_access_key_id`` /
                                    ``aws_secret_access_key`` fields.
                                aws_access_key_id:
                                  type: string
                                  description: >-
                                    Deprecated flat AWS access key ID.
                                    Superseded by
                                    ``aws_access_key.access_key_id``. Kept for
                                    one migration cycle; the handler mirrors it
                                    to/from ``aws_access_key``. Treated as
                                    username-equivalent (not a secret value):
                                    round-trips on reads and is scrubbed from
                                    audit logs.
                                aws_secret_access_key:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Deprecated flat AWS secret access key.
                                    Superseded by
                                    ``aws_access_key.secret_access_key``. Kept
                                    for one migration cycle; the handler mirrors
                                    it to/from ``aws_access_key``. Supplied as
                                    inline plaintext via
                                    ``ProviderSecret.plaintext``.
                                region:
                                  type: string
                                  description: >-
                                    AWS region where the Bedrock endpoint is
                                    hosted (e.g., ``us-east-1``). Required on
                                    Create.
                                service_credential:
                                  type: object
                                  properties:
                                    name:
                                      type: string
                                      description: >-
                                        Resource name of the bound UC service
                                        credential, in the AIP-122 form
                                        ``credentials/{name}`` (a
                                        metastore-level single-part credential
                                        name). On create the caller supplies the
                                        name here. On read it reflects the
                                        credential's current name at read time.
                                  required:
                                    - name
                                  description: >-
                                    Reference to a UC service credential
                                    authorizing Bedrock requests. On Create the
                                    caller supplies ``service_credential.name``
                                    in the AIP-122 resource-name form
                                    ``credentials/{name}``. Required on Create
                                    when using UC-service-credential auth;
                                    mutually exclusive with ``aws_access_key``.
                                    The credential is referenced by name; its
                                    value is not carried here. On read the
                                    resolved ``id`` and ``is_deleted`` are also
                                    populated. Only supported on AWS-hosted
                                    workspaces; Create requests from other
                                    clouds are rejected with
                                    INVALID_PARAMETER_VALUE.
                              description: |-
                                Direct form of Amazon Bedrock provider config.

                                    Authentication is one of two mutually exclusive modes, exactly one of which must be supplied on
                                    Create:

                                    - Access keys: set ``aws_access_key``, leave ``service_credential`` unset.
                                    - UC service credential: set ``service_credential.name`` to the AIP-122 resource-name form
                                      ``credentials/{name}``, leave ``aws_access_key`` unset. The credential value lives in UC and
                                      is referenced by name, not held on this message. Setting more than one mode is rejected.
                          description: Amazon Bedrock provider configuration.
                        anthropic:
                          type: object
                          properties:
                            direct:
                              type: object
                              properties:
                                api_key:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Anthropic API key. Required on Create. Sent
                                    as the ``x-api-key`` header on outbound
                                    requests. Supplied as inline plaintext via
                                    ``ProviderSecret.plaintext``.
                              description: >-
                                Direct (inline-credentials) form: caller
                                supplies the API key in the request body.
                                Required on Create unless ``relayed`` is set.
                            relayed:
                              type: object
                              properties:
                                plan_type:
                                  type: string
                                  x-enum:
                                    - ANTHROPIC_RELAYED_PLAN_TYPE_MAX
                                    - >-
                                      ANTHROPIC_RELAYED_PLAN_TYPE_TEAM_ENTERPRISE
                                  description: >-
                                    Which Anthropic subscription tier the
                                    relayed token belongs to. Optional; when
                                    unset the MPS gets the full governance
                                    surface (see TEAM_ENTERPRISE). Immutable
                                    after Create, so the tier cannot be flipped
                                    in place.
                              description: >-
                                Relayed (credential-less) form: no Anthropic
                                credential is stored. Each inference request
                                instead carries the caller's own OAuth token,
                                which the platform forwards to Anthropic on
                                outbound requests. Mutually exclusive with
                                ``direct``; no ``api_key`` is required or
                                persisted.
                          description: >-
                            Anthropic provider configuration. Exactly one of
                            ``direct`` or ``relayed`` must be set on
                                Create; the two are mutually exclusive.
                        azure_openai:
                          type: object
                          properties:
                            direct:
                              type: object
                              properties:
                                api_key:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Azure OpenAI API key. Mutually exclusive
                                    with the Entra and service-credential modes.
                                    Supplied as inline plaintext via
                                    ``ProviderSecret.plaintext``.
                                base_url:
                                  type: string
                                  description: >-
                                    Full Azure OpenAI endpoint base URL, e.g.
                                    ``https://myresource.openai.azure.com``.
                                    Required on Create.
                                client_id:
                                  type: string
                                  description: >-
                                    Deprecated flat Entra client ID. Superseded
                                    by ``entra_service_principal.client_id``.
                                    Kept for one migration cycle; the handler
                                    mirrors it to/from
                                    ``entra_service_principal``.
                                client_secret:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Deprecated flat Entra client secret.
                                    Superseded by
                                    ``entra_service_principal.client_secret``.
                                    Kept for one migration cycle; the handler
                                    mirrors it to/from
                                    ``entra_service_principal``. Supplied as
                                    inline plaintext via
                                    ``ProviderSecret.plaintext``.
                                entra_service_principal:
                                  type: object
                                  properties:
                                    client_id:
                                      type: string
                                      description: >-
                                        Entra ID client (application) ID.
                                        Required on Create.
                                    client_secret:
                                      type: object
                                      properties:
                                        plaintext:
                                          type: string
                                          description: >-
                                            Inline plaintext credential. INPUT_ONLY:
                                            the value never round-trips on reads.
                                            Get and List responses omit
                                            ``plaintext``; the field's presence in
                                            the read shape only indicates that a
                                            secret is configured.
                                      description: >-
                                        Entra ID client secret. Supplied as
                                        inline plaintext via
                                        ``ProviderSecret.plaintext``.
                                    tenant_id:
                                      type: string
                                      description: >-
                                        Entra ID (Azure AD) tenant ID. Required
                                        on Create.
                                  description: >-
                                    Entra ID (service principal) auth. Mutually
                                    exclusive with ``api_key`` and
                                    ``service_credential``. Supersedes the flat
                                    ``tenant_id`` / ``client_id`` /
                                    ``client_secret`` fields.
                                service_credential:
                                  type: object
                                  properties:
                                    name:
                                      type: string
                                      description: >-
                                        Resource name of the bound UC service
                                        credential, in the AIP-122 form
                                        ``credentials/{name}`` (a
                                        metastore-level single-part credential
                                        name). On create the caller supplies the
                                        name here. On read it reflects the
                                        credential's current name at read time.
                                  required:
                                    - name
                                  description: >-
                                    Reference to a UC service credential
                                    authorizing Azure OpenAI requests. On Create
                                    the caller supplies
                                    ``service_credential.name`` in the AIP-122
                                    resource-name form ``credentials/{name}``.
                                    Required on Create when using
                                    UC-service-credential auth; mutually
                                    exclusive with ``api_key`` and
                                    ``entra_service_principal``. The credential
                                    is referenced by name; its value is not
                                    carried here. On read the resolved ``id``
                                    and ``is_deleted`` are also populated. Only
                                    supported on Azure-hosted workspaces; Create
                                    requests from other clouds are rejected with
                                    INVALID_PARAMETER_VALUE.
                                tenant_id:
                                  type: string
                                  description: >-
                                    Deprecated flat Entra tenant ID. Superseded
                                    by ``entra_service_principal.tenant_id``.
                                    Kept for one migration cycle; the handler
                                    mirrors it to/from
                                    ``entra_service_principal``.
                              description: >-
                                Direct form of Azure OpenAI provider config.
                                Exactly one of three mutually-exclusive auth
                                modes
                                    must be supplied on Create:

                                    - API key: set ``api_key``, leave ``entra_service_principal`` and ``service_credential`` unset.
                                    - Entra ID (service principal): set ``entra_service_principal``, leave ``api_key`` and
                                      ``service_credential`` unset.
                                    - UC service credential: set ``service_credential.name`` to the AIP-122 resource-name form
                                      ``credentials/{name}``, leave ``api_key`` and ``entra_service_principal`` unset. The
                                      credential value lives in UC and is referenced by name, not held on this message. Only
                                      supported on Azure-hosted workspaces. Setting more than one mode is rejected.
                          description: Azure OpenAI provider configuration.
                        custom:
                          type: object
                          properties:
                            direct:
                              type: object
                              properties:
                                api_key:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Bearer token forwarded as the
                                    ``Authorization: Bearer ...`` header on
                                    outbound requests. Supplied as inline
                                    plaintext via ``ProviderSecret.plaintext``.
                                    Set this for bearer-token auth.
                                base_url:
                                  type: string
                                  description: >-
                                    Endpoint URL of the OpenAI-compatible
                                    service (e.g.,
                                    ``https://api.example.com/v1``). Required on
                                    Create.
                              description: |-
                                Direct form of custom provider config.

                                    Authentication is one of two mutually exclusive modes, exactly one of which must be supplied on
                                    Create:

                                    - Bearer: set ``api_key``, leave ``header_auth`` unset. The secret is forwarded as
                                      ``Authorization: Bearer <secret>``.
                                    - Header: set ``header_auth``, leave ``api_key`` unset. The secret is forwarded as
                                      ``<api_key_name>: <api_key_value>``. Setting both modes or neither mode is rejected.
                          description: >-
                            Custom provider configuration: arbitrary HTTP
                            endpoint with bearer-token auth.
                        forward_headers:
                          type: boolean
                          description: >-
                            Whether to forward incoming request headers to the
                            upstream provider. Applies to managed (multi-model)
                            requests as well as passthrough requests served by
                            this provider service. Governance-level decision by
                            the provider service owner; not selectable per
                            inference call.
                        forward_query_parameters:
                          type: boolean
                          description: >-
                            Whether to forward incoming request query parameters
                            to the upstream provider. Same trust-boundary
                            semantics as ``forward_headers``.
                        forward_unmanaged_paths:
                          type: boolean
                          description: >-
                            Whether to forward request paths that fall outside
                            this service's managed API set to the upstream
                            provider as opaque passthrough. When true, requests
                            addressed to subpaths not recognized by the managed
                            API surface are proxied to the upstream provider
                            over the same provider connection. When false, only
                            managed-API paths are served. Governance-level
                            decision by the provider service owner; expanding
                            this expands the trust boundary that the
                            ModelProviderService exposes.
                        gemini_enterprise:
                          type: object
                          properties:
                            direct:
                              type: object
                              properties:
                                api_key:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Google Gemini Enterprise API key. Required
                                    on Create. Supplied as inline plaintext via
                                    ``ProviderSecret.plaintext``.
                                project_id:
                                  type: string
                                  description: >-
                                    GCP project ID hosting the Gemini Enterprise
                                    endpoint. Required on Create.
                                region:
                                  type: string
                                  description: >-
                                    GCP region of the Gemini Enterprise endpoint
                                    (e.g., ``us-central1``). Required on Create.
                              description: >-
                                Direct form of Gemini Enterprise provider
                                config.
                          description: Gemini Enterprise provider configuration.
                        inference_table:
                          type: object
                          properties:
                            parent:
                              type: string
                              description: >-
                                Parent UC schema where the inference table is
                                created. Format: ``schemas/{catalog}.{schema}``.
                                Set at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                            disabled:
                              type: boolean
                              description: >-
                                Indicates whether payload logging is disabled
                                (opt-out). Unset means that payload logging is
                                active (the on-by-default state coincides with
                                the proto zero-value, so the server never fills
                                this field for a client that leaves it unset).
                                Set ``disabled = true`` to pause runtime logging
                                while keeping the sub-message attached
                                (preserving ``parent`` and ``table_name_prefix``
                                for a later flip back to active). ``parent``
                                remains required either way.
                            is_deleted:
                              type: boolean
                              description: >-
                                True when the bound inference TABLE has been
                                deleted but the parent service still references
                                it. The dangling reference is surfaced (not
                                silently dropped) so callers can see the broken
                                dependency. AI Gateway payload logging fails
                                closed in this state.
                            table:
                              type: string
                              description: >-
                                Resolved UC table for payload logs. Format:
                                ``tables/{catalog}.{schema}.{table}``.
                            table_name_prefix:
                              type: string
                              description: >-
                                Prefix for the inference-table's UC-registered
                                name. The actual leaf name UC stores is
                                ``<table_name_prefix>_payload``; the
                                ``_payload`` suffix is appended automatically.
                                To find the actual UC table after Create, read
                                the ``table`` field on the response. Defaults to
                                ``<model_service_name>_payload`` when unset. Set
                                at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                          required:
                            - parent
                          description: >-
                            Inference table configuration for payload logging
                            when this provider service is invoked directly. When
                            it is invoked through a model service, the model
                            service's own inference table captures the
                            invocation instead. Mirrors
                            ``ModelServiceConfig.inference_table`` /
                            ``AgentServiceConfig.inference_table``.
                        microsoft_foundry:
                          type: object
                          properties:
                            direct:
                              type: object
                              properties:
                                api_key:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Microsoft AI Foundry API key. Mutually
                                    exclusive with the Entra and
                                    service-credential modes. Supplied as inline
                                    plaintext via ``ProviderSecret.plaintext``.
                                base_url:
                                  type: string
                                  description: >-
                                    Microsoft AI Foundry endpoint URL. Required
                                    on Create.
                                client_id:
                                  type: string
                                  description: >-
                                    Deprecated flat Entra client ID. Superseded
                                    by ``entra_service_principal.client_id``.
                                    Kept for one migration cycle; the handler
                                    mirrors it to/from
                                    ``entra_service_principal``.
                                client_secret:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    Deprecated flat Entra client secret.
                                    Superseded by
                                    ``entra_service_principal.client_secret``.
                                    Kept for one migration cycle; the handler
                                    mirrors it to/from
                                    ``entra_service_principal``. Supplied as
                                    inline plaintext via
                                    ``ProviderSecret.plaintext``.
                                entra_service_principal:
                                  type: object
                                  properties:
                                    client_id:
                                      type: string
                                      description: >-
                                        Entra ID client (application) ID.
                                        Required on Create.
                                    client_secret:
                                      type: object
                                      properties:
                                        plaintext:
                                          type: string
                                          description: >-
                                            Inline plaintext credential. INPUT_ONLY:
                                            the value never round-trips on reads.
                                            Get and List responses omit
                                            ``plaintext``; the field's presence in
                                            the read shape only indicates that a
                                            secret is configured.
                                      description: >-
                                        Entra ID client secret. Supplied as
                                        inline plaintext via
                                        ``ProviderSecret.plaintext``.
                                    tenant_id:
                                      type: string
                                      description: >-
                                        Entra ID (Azure AD) tenant ID. Required
                                        on Create.
                                  description: >-
                                    Entra ID (service principal) auth. Mutually
                                    exclusive with ``api_key`` and
                                    ``service_credential``. Supersedes the flat
                                    ``tenant_id`` / ``client_id`` /
                                    ``client_secret`` fields.
                                service_credential:
                                  type: object
                                  properties:
                                    name:
                                      type: string
                                      description: >-
                                        Resource name of the bound UC service
                                        credential, in the AIP-122 form
                                        ``credentials/{name}`` (a
                                        metastore-level single-part credential
                                        name). On create the caller supplies the
                                        name here. On read it reflects the
                                        credential's current name at read time.
                                  required:
                                    - name
                                  description: >-
                                    Reference to a UC service credential
                                    authorizing Microsoft Foundry requests. On
                                    Create the caller supplies
                                    ``service_credential.name`` in the AIP-122
                                    resource-name form ``credentials/{name}``.
                                    Required on Create when using
                                    UC-service-credential auth; mutually
                                    exclusive with ``api_key`` and
                                    ``entra_service_principal``. The credential
                                    is referenced by name; its value is not
                                    carried here. On read the resolved ``id``
                                    and ``is_deleted`` are also populated. Only
                                    supported on Azure-hosted workspaces; Create
                                    requests from other clouds are rejected with
                                    INVALID_PARAMETER_VALUE.
                                tenant_id:
                                  type: string
                                  description: >-
                                    Deprecated flat Entra tenant ID. Superseded
                                    by ``entra_service_principal.tenant_id``.
                                    Kept for one migration cycle; the handler
                                    mirrors it to/from
                                    ``entra_service_principal``.
                              description: >-
                                Direct form of Microsoft Foundry provider
                                config.

                                    Authentication is one of three mutually exclusive modes, exactly one of which must be supplied
                                    on Create:

                                    - API key: set ``api_key``, leave ``entra_service_principal`` and ``service_credential`` unset.
                                    - Entra ID (service principal): set ``entra_service_principal``, leave ``api_key`` and
                                      ``service_credential`` unset. AI Gateway exchanges these for an Entra bearer token on outbound
                                      requests via the OAuth2 client-credentials grant.
                                    - UC service credential: set ``service_credential.name`` to the AIP-122 resource-name form
                                      ``credentials/{name}``, leave ``api_key`` and ``entra_service_principal`` unset. The
                                      credential value lives in UC and is referenced by name, not held on this message. Only
                                      supported on Azure-hosted workspaces. Setting more than one mode is rejected.
                          description: Microsoft Foundry provider configuration.
                        openai:
                          type: object
                          properties:
                            direct:
                              type: object
                              properties:
                                api_key:
                                  type: object
                                  properties:
                                    plaintext:
                                      type: string
                                      description: >-
                                        Inline plaintext credential. INPUT_ONLY:
                                        the value never round-trips on reads.
                                        Get and List responses omit
                                        ``plaintext``; the field's presence in
                                        the read shape only indicates that a
                                        secret is configured.
                                  description: >-
                                    OpenAI API key. Required on Create. Supplied
                                    as inline plaintext via
                                    ``ProviderSecret.plaintext``.
                                base_url:
                                  type: string
                                  description: >-
                                    Optional custom base URL. Defaults to
                                    ``https://api.openai.com/v1``. Use for
                                    OpenAI-API-compatible third-party endpoints
                                    or in-network proxies.
                                organization:
                                  type: string
                                  description: >-
                                    Optional OpenAI organization ID. When set,
                                    the platform forwards it as the
                                    ``OpenAI-Organization`` header.
                              description: >-
                                Direct (inline-credentials) form of the OpenAI
                                provider config.
                          description: OpenAI provider configuration.
                        provider_type:
                          type: string
                          x-enum:
                            - EXTERNAL_MODEL_PROVIDER_TYPE_AMAZON_BEDROCK
                            - EXTERNAL_MODEL_PROVIDER_TYPE_ANTHROPIC
                            - EXTERNAL_MODEL_PROVIDER_TYPE_AZURE_OPENAI
                            - EXTERNAL_MODEL_PROVIDER_TYPE_CUSTOM
                            - EXTERNAL_MODEL_PROVIDER_TYPE_GEMINI_ENTERPRISE
                            - EXTERNAL_MODEL_PROVIDER_TYPE_MICROSOFT_FOUNDRY
                            - EXTERNAL_MODEL_PROVIDER_TYPE_OPENAI
                          description: >-
                            Provider type discriminator. Required at create
                            time; immutable after. Determines which variant of
                            the ``provider`` oneof must be set. May not be
                            changed via Update; attempts to include
                            ``config.provider_type`` in
                            ``UpdateModelProviderServiceRequest.update_mask``
                            are rejected. Required on CreateModelProviderService
                            and immutable thereafter.
                        rate_limits:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_KEY_REQUEST_TAG
                                  - RATE_LIMIT_KEY_SERVICE
                                  - RATE_LIMIT_KEY_SERVICE_PRINCIPAL
                                  - RATE_LIMIT_KEY_USER
                                  - RATE_LIMIT_KEY_USER_DEFAULT
                                  - RATE_LIMIT_KEY_USER_GROUP
                                description: >-
                                  Scope key. Determines whether ``principal`` is
                                  required.
                              renewal_period:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_RENEWAL_PERIOD_HOUR
                                  - RATE_LIMIT_RENEWAL_PERIOD_MINUTE
                                description: Renewal period.
                              principal:
                                type: string
                                description: >-
                                  Principal this limit applies to: user email,
                                  group name, or service principal application
                                  ID. Required unless ``key`` is
                                  ``RATE_LIMIT_KEY_SERVICE``,
                                  ``RATE_LIMIT_KEY_USER_DEFAULT``, or
                                  ``RATE_LIMIT_KEY_REQUEST_TAG`` (which must not
                                  set a principal).
                              request_tag_key:
                                type: string
                                description: >-
                                  Request tag key this limit applies to.
                                  Required when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``, forbidden
                                  otherwise.
                              request_tag_value:
                                type: string
                                description: >-
                                  Request tag value this limit applies to. Only
                                  valid when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``. Leave unset to
                                  apply the limit to every value of
                                  ``request_tag_key`` (an any-value default); a
                                  set value is a specific override for that
                                  value.
                              requests:
                                type: integer
                                description: >-
                                  Max requests allowed within a renewal period.
                                  Leave unset for no request limit.
                              tokens:
                                type: integer
                                description: >-
                                  Max tokens allowed within a renewal period.
                                  Leave unset for no token limit.
                            required:
                              - key
                              - renewal_period
                            description: >-
                              A rate limit applied to service requests. Leave
                              ``requests`` or ``tokens`` unset to impose no
                                  limit on that dimension; set a value to cap that dimension within the renewal period.
                          description: >-
                            Rate limits applied when this provider service is
                            invoked directly. When it is invoked through a model
                            service, the model service's own ``rate_limits``
                            apply instead. Mirrors
                            ``ModelServiceConfig.rate_limits`` /
                            ``McpServiceConfig.rate_limits``.
                        targets:
                          type: array
                          items:
                            type: object
                            properties:
                              model:
                                type: string
                                description: >-
                                  Provider-side model identifier (e.g. "gpt-5",
                                  "claude-opus-4-7"). This is a string on the
                                  LLM provider's side, not a UC entity. The UC
                                  governance hook for external destinations is
                                  the ModelProviderService referenced by
                                  ``ExternalModelConfig.model_provider_service``,
                                  not the model itself.
                              native_api_types:
                                type: array
                                items:
                                  type: string
                                description: >-
                                  Provider-native API types the model supports
                                  (e.g. "openai/v1/chat/completions"). Used by
                                  the platform for request/response translation
                                  from the unified API type. At most 64 entries
                                  of at most 256 characters each; the list is
                                  persisted into the destination binding's
                                  bounded storage envelope.
                            required:
                              - model
                            description: >-
                              Model target configuration for an external model
                              destination.
                          description: >-
                            Routing targets this provider service exposes
                            (provider-side model identifier + unified API types
                            per entry). Required (>=1) when ``allow_all_targets
                            = false``; optional and additive when
                            ``allow_all_targets = true``. References from
                            ``ExternalModelConfig.target`` must match an entry
                            here unless ``allow_all_targets = true``.
                      description: >-
                        Behavioral configuration: provider connection, model
                        catalog, and passthrough policy. See
                        ``ModelProviderServiceConfig`` for the per-field
                        contract. Required on CreateModelProviderService; on
                        Update it is required only when ``config`` (or a
                        ``config.*`` subpath) appears in ``update_mask``.
                    create_time:
                      type: string
                      format: date-time
                      description: When the provider service was created.
                    created_by:
                      type: string
                      description: Creator identity.
                    effective_owner:
                      type: string
                      description: >-
                        The resolved owner of the model provider service. Falls
                        back to the caller's identity when ``owner`` is not
                        explicitly set on creation.
                    etag:
                      type: string
                      description: >-
                        Optimistic concurrency control token. Server-generated
                        from the entity's state and returned on every read. To
                        use it as an if-match precondition on a mutation, echo
                        the last-read value back via the dedicated ``etag``
                        field on the Update / Delete request; the server rejects
                        the mutation if the stored etag differs.
                    metastore_id:
                      type: string
                      description: Metastore hosting the provider service.
                    name:
                      type: string
                      description: >-
                        Resource name of the provider service. Format:
                        ``model-provider-services/{catalog}.{schema}.{model_provider_service}``.
                        Each ``{...}`` component is capped at 255 characters
                        individually. Server-derived on Create from ``parent`` +
                        ``model_provider_service_id``; required and immutable on
                        Update/Get/Delete.
                    owner:
                      type: string
                      description: >-
                        The owner of the model provider service. Write-only;
                        read owner via effective_owner.
                    update_time:
                      type: string
                      format: date-time
                      description: When the provider service was last modified.
                    updated_by:
                      type: string
                      description: Identity of the last updater.
                  description: >-
                    The model provider service to create. The server populates
                    ``name`` from ``parent`` + ``model_provider_service_id``;
                    clients should leave it unset.
              required:
                - model_provider_service
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelProviderService'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
    get:
      operationId: ai_gateway_list_model_provider_services
      summary: >-
        Lists the model provider services in a Unity Catalog schema. Provide
        ``parent`` as
      tags:
        - catalog
        - ai_gateway
      description: >-
        Lists the model provider services in a Unity Catalog schema. Provide
        ``parent`` as

        ``schemas/{catalog}.{schema}``. Results are paginated; pass the returned
        ``next_page_token`` to fetch

        subsequent pages.


        Requires ``USE_CATALOG`` on the parent catalog and ``USE_SCHEMA`` on the
        parent schema. Only model

        provider services the caller can access (as owner or through
        ``EXECUTE``, ``READ_METADATA``, or

        ``MANAGE``) are returned.


        :param include_browse: bool (optional)
          Whether to include provider services for which the principal can only access selective metadata.
        :param page_size: int (optional)
          Maximum number of provider services to return. Defaults to 100 when unset or 0; the maximum is 1000.
          Use ``next_page_token`` to retrieve additional pages.
        :param page_token: str (optional)
          Opaque pagination token from a previous request.
        :param parent: str (optional)
          Resource name of the parent schema to list within, as ``schemas/{catalog}.{schema}``. Each ``{...}``
          component is capped at 255 characters individually.
        :param view: :class:`ListModelProviderServicesRequestView` (optional)
          View selector controlling which fields are populated per row.

        :returns: Iterator over :class:`ModelProviderService`
      parameters:
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include provider services for which the principal can
            only access selective metadata.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of provider services to return. Defaults to 100 when
            unset or 0; the maximum is 1000. Use ``next_page_token`` to retrieve
            additional pages.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token from a previous request.
        - name: parent
          in: query
          required: false
          schema:
            type: string
          description: >-
            Resource name of the parent schema to list within, as
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
        - name: view
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/ListModelProviderServicesRequestView'
          description: View selector controlling which fields are populated per row.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListModelProviderServicesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
  /api/2.1/unity-catalog/model-services:
    post:
      operationId: ai_gateway_create_model_service
      summary: >-
        Creates a model service in a Unity Catalog schema. A model service is a
        governed AI Gateway endpoint
      tags:
        - catalog
        - ai_gateway
      description: >-
        Creates a model service in a Unity Catalog schema. A model service is a
        governed AI Gateway endpoint

        that routes inference requests to one or more model destinations. The
        caller supplies the leaf name in

        ``model_service_id``.


        You must be the owner of the parent schema or have the
        ``CREATE_SERVICE`` and ``USE_SCHEMA``

        privileges on the parent schema and ``USE_CATALOG`` on the parent
        catalog.


        :param model_service: :class:`ModelService`
          The model service to create. The server populates ``name`` from ``parent`` + ``model_service_id``;
          clients should leave it unset.
        :param parent: str
          Resource name of the parent schema. Format: ``schemas/{catalog}.{schema}``. Each ``{...}`` component
          is capped at 255 characters individually.
        :param model_service_id: str
          Leaf identifier for the model service (the unqualified name within the parent schema, e.g.
          "my_model_service").

        :returns: :class:`ModelService`
      parameters:
        - name: parent
          in: query
          required: true
          schema:
            type: string
          description: >-
            Resource name of the parent schema. Format:
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
        - name: model_service_id
          in: query
          required: true
          schema:
            type: string
          description: >-
            Leaf identifier for the model service (the unqualified name within
            the parent schema, e.g. "my_model_service").
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                model_service:
                  type: object
                  properties:
                    browse_only:
                      type: boolean
                      description: >-
                        Whether the caller sees only metadata available through
                        the BROWSE privilege.
                    comment:
                      type: string
                      description: User-provided description.
                    config:
                      type: object
                      properties:
                        inference_table:
                          type: object
                          properties:
                            parent:
                              type: string
                              description: >-
                                Parent UC schema where the inference table is
                                created. Format: ``schemas/{catalog}.{schema}``.
                                Set at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                            disabled:
                              type: boolean
                              description: >-
                                Indicates whether payload logging is disabled
                                (opt-out). Unset means that payload logging is
                                active (the on-by-default state coincides with
                                the proto zero-value, so the server never fills
                                this field for a client that leaves it unset).
                                Set ``disabled = true`` to pause runtime logging
                                while keeping the sub-message attached
                                (preserving ``parent`` and ``table_name_prefix``
                                for a later flip back to active). ``parent``
                                remains required either way.
                            is_deleted:
                              type: boolean
                              description: >-
                                True when the bound inference TABLE has been
                                deleted but the parent service still references
                                it. The dangling reference is surfaced (not
                                silently dropped) so callers can see the broken
                                dependency. AI Gateway payload logging fails
                                closed in this state.
                            table:
                              type: string
                              description: >-
                                Resolved UC table for payload logs. Format:
                                ``tables/{catalog}.{schema}.{table}``.
                            table_name_prefix:
                              type: string
                              description: >-
                                Prefix for the inference-table's UC-registered
                                name. The actual leaf name UC stores is
                                ``<table_name_prefix>_payload``; the
                                ``_payload`` suffix is appended automatically.
                                To find the actual UC table after Create, read
                                the ``table`` field on the response. Defaults to
                                ``<model_service_name>_payload`` when unset. Set
                                at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                          required:
                            - parent
                          description: Inference table config for payload logging.
                        rate_limits:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_KEY_REQUEST_TAG
                                  - RATE_LIMIT_KEY_SERVICE
                                  - RATE_LIMIT_KEY_SERVICE_PRINCIPAL
                                  - RATE_LIMIT_KEY_USER
                                  - RATE_LIMIT_KEY_USER_DEFAULT
                                  - RATE_LIMIT_KEY_USER_GROUP
                                description: >-
                                  Scope key. Determines whether ``principal`` is
                                  required.
                              renewal_period:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_RENEWAL_PERIOD_HOUR
                                  - RATE_LIMIT_RENEWAL_PERIOD_MINUTE
                                description: Renewal period.
                              principal:
                                type: string
                                description: >-
                                  Principal this limit applies to: user email,
                                  group name, or service principal application
                                  ID. Required unless ``key`` is
                                  ``RATE_LIMIT_KEY_SERVICE``,
                                  ``RATE_LIMIT_KEY_USER_DEFAULT``, or
                                  ``RATE_LIMIT_KEY_REQUEST_TAG`` (which must not
                                  set a principal).
                              request_tag_key:
                                type: string
                                description: >-
                                  Request tag key this limit applies to.
                                  Required when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``, forbidden
                                  otherwise.
                              request_tag_value:
                                type: string
                                description: >-
                                  Request tag value this limit applies to. Only
                                  valid when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``. Leave unset to
                                  apply the limit to every value of
                                  ``request_tag_key`` (an any-value default); a
                                  set value is a specific override for that
                                  value.
                              requests:
                                type: integer
                                description: >-
                                  Max requests allowed within a renewal period.
                                  Leave unset for no request limit.
                              tokens:
                                type: integer
                                description: >-
                                  Max tokens allowed within a renewal period.
                                  Leave unset for no token limit.
                            required:
                              - key
                              - renewal_period
                            description: >-
                              A rate limit applied to service requests. Leave
                              ``requests`` or ``tokens`` unset to impose no
                                  limit on that dimension; set a value to cap that dimension within the renewal period.
                          description: >-
                            Rate limits applied to requests routed through this
                            model service.
                        routing:
                          type: object
                          properties:
                            destinations:
                              type: array
                              items:
                                type: object
                                properties:
                                  name:
                                    type: string
                                    description: >-
                                      User-facing label for this destination,
                                      used in routing references.
                                  destination_type:
                                    type: string
                                    x-enum:
                                      - >-
                                        DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL
                                      - >-
                                        DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL
                                      - >-
                                        DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                    description: >-
                                      Backing-model category. Determines which
                                      oneof variant is populated.
                                  external_model_config:
                                    type: object
                                    properties:
                                      model_provider_service:
                                        type: string
                                        description: >-
                                          Resource name of the governed
                                          ModelProviderService that owns provider
                                          auth and provider-specific
                                          configuration. The referenced
                                          ModelProviderService also carries the
                                          provider type, so this message does not
                                          surface it directly. Format:
                                          ``model-provider-services/{catalog}.{schema}.{model_provider_service}``.
                                          Each ``{...}`` component is capped at
                                          255 characters individually.
                                      target:
                                        type: object
                                        properties:
                                          model:
                                            type: string
                                            description: >-
                                              Provider-side model identifier (e.g.
                                              "gpt-5", "claude-opus-4-7"). This is a
                                              string on the LLM provider's side, not a
                                              UC entity. The UC governance hook for
                                              external destinations is the
                                              ModelProviderService referenced by
                                              ``ExternalModelConfig.model_provider_service``,
                                              not the model itself.
                                          native_api_types:
                                            type: array
                                            items:
                                              type: string
                                            description: >-
                                              Provider-native API types the model
                                              supports (e.g.
                                              "openai/v1/chat/completions"). Used by
                                              the platform for request/response
                                              translation from the unified API type.
                                              At most 64 entries of at most 256
                                              characters each; the list is persisted
                                              into the destination binding's bounded
                                              storage envelope.
                                        required:
                                          - model
                                        description: >-
                                          Routing target for the destination: the
                                          provider-side model selected from the
                                          referenced ModelProviderService's
                                          ``targets`` catalog, plus the unified
                                          API types the platform should translate
                                          to/from at request time.
                                    required:
                                      - model_provider_service
                                      - target
                                    description: >-
                                      Configuration for an
                                      external-foundation-model destination.
                                      Provider auth and provider-specific
                                          cloud configuration are owned by a separate, governed ModelProviderService entity referenced via
                                          ``model_provider_service``; the platform resolves the provider at invocation time.
                                  is_deleted:
                                    type: boolean
                                    description: >-
                                      True when the destination's backing UC
                                      entity (MODEL for foundation-model
                                      destinations, MODEL_PROVIDER_SERVICE for
                                      external destinations) has been deleted
                                      but the destination row still references
                                      it. The dangling destination is surfaced
                                      (not silently dropped) so callers can see
                                      the broken routing. Inference traffic
                                      through this destination fails closed
                                      (BAD_REQUEST / FAILED_PRECONDITION).
                                  pay_per_token_config:
                                    type: object
                                    properties:
                                      model:
                                        type: string
                                        description: >-
                                          Resource name of the UC model. Format:
                                          ``models/{catalog}.{schema}.{model}``.
                                    required:
                                      - model
                                    description: >-
                                      Configuration for a pay-per-token
                                      foundation-model destination. Identifies
                                      the foundation model
                                          by its UC resource name; the platform resolves it to a Model Serving endpoint at request time.
                                  provisioned_throughput_config:
                                    type: object
                                    properties:
                                      model_serving_endpoint:
                                        type: string
                                        description: >-
                                          Name of the backing Model Serving
                                          endpoint serving the provisioned-
                                          throughput foundation model, as the
                                          AIP-122 typed resource name
                                          ``serving-endpoints/{name}``. The same
                                          UC model can be served on multiple Model
                                          Serving endpoints (different throughput
                                          / region / config); the caller picks
                                          which one this destination routes to.
                                          The endpoint must exist at create time.
                                      model:
                                        type: string
                                        description: >-
                                          UC model FQN of the model served by the
                                          backing endpoint (e.g.,
                                          ``system.ai.databricks-claude-opus-4-6``).
                                          Resolved from Model Serving at
                                          Create/Update time.
                                    required:
                                      - model_serving_endpoint
                                    description: >-
                                      Configuration for a provisioned-throughput
                                      foundation-model destination. References a
                                          pre-existing Model Serving endpoint that serves the model; sizing (provisioned throughput, burst
                                          scaling, model version) is owned by the Model Serving endpoint itself, not by this message.
                                  traffic_percentage:
                                    type: integer
                                    description: >-
                                      Share of traffic sent to this destination,
                                      0-100. Optional on fallback destinations;
                                      see FallbackConfig.
                                required:
                                  - name
                                  - destination_type
                                description: >-
                                  A destination the model service can route
                                  traffic to. Exactly one of the per-type
                                  configs inside
                                      ``type_config`` must be set, and it must match ``destination_type``.
                              description: >-
                                Primary routing destinations. At most 10 are
                                allowed. At least one is required on
                                CreateModelService; on UpdateModelService it is
                                required only when ``config.routing`` (or a
                                ``config.routing.*`` subpath) appears in
                                ``update_mask``.
                            fallback:
                              type: object
                              properties:
                                destinations:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      name:
                                        type: string
                                        description: >-
                                          User-facing label for this destination,
                                          used in routing references.
                                      destination_type:
                                        type: string
                                        x-enum:
                                          - >-
                                            DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL
                                          - >-
                                            DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL
                                          - >-
                                            DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                        description: >-
                                          Backing-model category. Determines which
                                          oneof variant is populated.
                                      external_model_config:
                                        type: object
                                        properties:
                                          model_provider_service:
                                            type: string
                                            description: >-
                                              Resource name of the governed
                                              ModelProviderService that owns provider
                                              auth and provider-specific
                                              configuration. The referenced
                                              ModelProviderService also carries the
                                              provider type, so this message does not
                                              surface it directly. Format:
                                              ``model-provider-services/{catalog}.{schema}.{model_provider_service}``.
                                              Each ``{...}`` component is capped at
                                              255 characters individually.
                                          target:
                                            type: object
                                            properties:
                                              model:
                                                type: string
                                                description: >-
                                                  Provider-side model identifier (e.g.
                                                  "gpt-5", "claude-opus-4-7"). This is a
                                                  string on the LLM provider's side, not a
                                                  UC entity. The UC governance hook for
                                                  external destinations is the
                                                  ModelProviderService referenced by
                                                  ``ExternalModelConfig.model_provider_service``,
                                                  not the model itself.
                                              native_api_types:
                                                type: array
                                                items:
                                                  type: string
                                                description: >-
                                                  Provider-native API types the model
                                                  supports (e.g.
                                                  "openai/v1/chat/completions"). Used by
                                                  the platform for request/response
                                                  translation from the unified API type.
                                                  At most 64 entries of at most 256
                                                  characters each; the list is persisted
                                                  into the destination binding's bounded
                                                  storage envelope.
                                            required:
                                              - model
                                            description: >-
                                              Routing target for the destination: the
                                              provider-side model selected from the
                                              referenced ModelProviderService's
                                              ``targets`` catalog, plus the unified
                                              API types the platform should translate
                                              to/from at request time.
                                        required:
                                          - model_provider_service
                                          - target
                                        description: >-
                                          Configuration for an
                                          external-foundation-model destination.
                                          Provider auth and provider-specific
                                              cloud configuration are owned by a separate, governed ModelProviderService entity referenced via
                                              ``model_provider_service``; the platform resolves the provider at invocation time.
                                      is_deleted:
                                        type: boolean
                                        description: >-
                                          True when the destination's backing UC
                                          entity (MODEL for foundation-model
                                          destinations, MODEL_PROVIDER_SERVICE for
                                          external destinations) has been deleted
                                          but the destination row still references
                                          it. The dangling destination is surfaced
                                          (not silently dropped) so callers can
                                          see the broken routing. Inference
                                          traffic through this destination fails
                                          closed (BAD_REQUEST /
                                          FAILED_PRECONDITION).
                                      pay_per_token_config:
                                        type: object
                                        properties:
                                          model:
                                            type: string
                                            description: >-
                                              Resource name of the UC model. Format:
                                              ``models/{catalog}.{schema}.{model}``.
                                        required:
                                          - model
                                        description: >-
                                          Configuration for a pay-per-token
                                          foundation-model destination. Identifies
                                          the foundation model
                                              by its UC resource name; the platform resolves it to a Model Serving endpoint at request time.
                                      provisioned_throughput_config:
                                        type: object
                                        properties:
                                          model_serving_endpoint:
                                            type: string
                                            description: >-
                                              Name of the backing Model Serving
                                              endpoint serving the provisioned-
                                              throughput foundation model, as the
                                              AIP-122 typed resource name
                                              ``serving-endpoints/{name}``. The same
                                              UC model can be served on multiple Model
                                              Serving endpoints (different throughput
                                              / region / config); the caller picks
                                              which one this destination routes to.
                                              The endpoint must exist at create time.
                                          model:
                                            type: string
                                            description: >-
                                              UC model FQN of the model served by the
                                              backing endpoint (e.g.,
                                              ``system.ai.databricks-claude-opus-4-6``).
                                              Resolved from Model Serving at
                                              Create/Update time.
                                        required:
                                          - model_serving_endpoint
                                        description: >-
                                          Configuration for a
                                          provisioned-throughput foundation-model
                                          destination. References a
                                              pre-existing Model Serving endpoint that serves the model; sizing (provisioned throughput, burst
                                              scaling, model version) is owned by the Model Serving endpoint itself, not by this message.
                                      traffic_percentage:
                                        type: integer
                                        description: >-
                                          Share of traffic sent to this
                                          destination, 0-100. Optional on fallback
                                          destinations; see FallbackConfig.
                                    required:
                                      - name
                                      - destination_type
                                    description: >-
                                      A destination the model service can route
                                      traffic to. Exactly one of the per-type
                                      configs inside
                                          ``type_config`` must be set, and it must match ``destination_type``.
                                  description: >-
                                    Ordered list of fallback destinations.
                                    Traversal is in list order; the attempt
                                    count is the length of the list. At most 5
                                    are allowed.
                              description: >-
                                Fallback routing config, applied after primary
                                destinations fail.
                            first_token_timeout:
                              type: string
                              description: >-
                                Timeout for the first token of a streaming
                                response. If a destination does not return its
                                first token within this duration, AI Gateway
                                aborts the attempt and fails over to the next
                                destination. Applies to streaming requests only.
                                Leave unset for no first-token timeout.
                            traffic_splitting:
                              type: object
                              properties: {}
                              description: >-
                                Marker message selecting request-based traffic
                                splitting. Traffic is distributed according to
                                each destination's traffic_percentage value; no
                                configuration lives on this message itself.
                          description: >-
                            Routing configuration: destinations, routing
                            strategy, and fallback.
                      description: >-
                        Operational configuration: destinations, routing, rate
                        limits, inference table. Required on CreateModelService;
                        on UpdateModelService it is required only when
                        ``config`` (or a ``config.*`` subpath) appears in
                        ``update_mask``.
                    create_time:
                      type: string
                      format: date-time
                      description: When the model service was created.
                    created_by:
                      type: string
                      description: Creator identity.
                    effective_owner:
                      type: string
                      description: >-
                        The resolved owner of the ModelService. Falls back to
                        the caller's identity when ``owner`` is not explicitly
                        set on creation.
                    etag:
                      type: string
                      description: >-
                        Optimistic concurrency control token. Server-generated
                        from the entity's state and returned on every read. To
                        use it as an if-match precondition on a mutation, echo
                        the last-read value back via the dedicated ``etag``
                        field on the Update / Delete request; the server rejects
                        the mutation if the stored etag differs.
                    metastore_id:
                      type: string
                      description: Metastore hosting the model service.
                    name:
                      type: string
                      description: >-
                        Resource name of the model service. Format:
                        ``model-services/{catalog}.{schema}.{model_service}``.
                        Each ``{...}`` component is capped at 255 characters
                        individually. Server-derived on Create from ``parent`` +
                        ``model_service_id``; required and immutable on
                        Update/Get/Delete.
                    owner:
                      type: string
                      description: >-
                        The owner of the model service. Write-only; read owner
                        via effective_owner.
                    supported_api_types:
                      type: array
                      items:
                        type: string
                      description: >-
                        Unified API types this endpoint supports (e.g. "chat",
                        "embeddings", "completions"). Derived from the
                        destinations' backing models / providers at read time.
                    update_time:
                      type: string
                      format: date-time
                      description: When the model service was last modified.
                    updated_by:
                      type: string
                      description: Identity of the last updater.
                  description: >-
                    The model service to create. The server populates ``name``
                    from ``parent`` + ``model_service_id``; clients should leave
                    it unset.
              required:
                - model_service
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelService'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
    get:
      operationId: ai_gateway_list_model_services
      summary: >-
        Lists the model services in a Unity Catalog schema. Provide ``parent``
        as
      tags:
        - catalog
        - ai_gateway
      description: >-
        Lists the model services in a Unity Catalog schema. Provide ``parent``
        as

        ``schemas/{catalog}.{schema}``. Results are paginated; pass the returned
        ``next_page_token`` to fetch

        subsequent pages.


        Requires ``USE_CATALOG`` on the parent catalog and ``USE_SCHEMA`` on the
        parent schema. Only model

        services the caller can access (as owner or through ``EXECUTE``,
        ``READ_METADATA``, or ``MANAGE``) are

        returned.


        :param include_browse: bool (optional)
          Whether to include model services for which the principal can only access selective metadata.
        :param page_size: int (optional)
          Maximum number of model services to return. Defaults to 100 when unset or 0; the maximum is 1000.
          Use ``next_page_token`` to retrieve additional pages.
        :param page_token: str (optional)
          Opaque pagination token from a previous request.
        :param parent: str (optional)
          Resource name of the parent schema to list within, as ``schemas/{catalog}.{schema}``. Each ``{...}``
          component is capped at 255 characters individually.
        :param view: :class:`ListModelServicesRequestView` (optional)
          View selector controlling which fields are populated per row.

        :returns: Iterator over :class:`ModelService`
      parameters:
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include model services for which the principal can only
            access selective metadata.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of model services to return. Defaults to 100 when
            unset or 0; the maximum is 1000. Use ``next_page_token`` to retrieve
            additional pages.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token from a previous request.
        - name: parent
          in: query
          required: false
          schema:
            type: string
          description: >-
            Resource name of the parent schema to list within, as
            ``schemas/{catalog}.{schema}``. Each ``{...}`` component is capped
            at 255 characters individually.
        - name: view
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/ListModelServicesRequestView'
          description: View selector controlling which fields are populated per row.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListModelServicesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
  /api/2.1/unity-catalog/{name}:
    delete:
      operationId: ai_gateway_delete_model_service
      summary: >-
        Deletes the model service identified by its resource name. Optionally
        supply an ``etag`` to make the
      tags:
        - catalog
        - ai_gateway
      description: >-
        Deletes the model service identified by its resource name. Optionally
        supply an ``etag`` to make the

        delete conditional on the model service not having changed since it was
        read.


        You must be the owner of the model service or have ``MANAGE`` on it,
        plus ``USE_CATALOG`` on the

        parent catalog and ``USE_SCHEMA`` on the parent schema.


        :param name: str
          Resource name of the model service. Format: ``model-services/{catalog}.{schema}.{model_service}``.
          Each ``{...}`` component is capped at 255 characters individually.
        :param etag: str (optional)
          If-match precondition: when set, the delete proceeds only if the current server-side etag matches.
          Empty means unconditional delete.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: >-
            Resource name of the model service. Format:
            ``model-services/{catalog}.{schema}.{model_service}``. Each
            ``{...}`` component is capped at 255 characters individually.
        - name: etag
          in: query
          required: false
          schema:
            type: string
          description: >-
            If-match precondition: when set, the delete proceeds only if the
            current server-side etag matches. Empty means unconditional delete.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
    get:
      operationId: ai_gateway_get_model_service
      summary: Returns the model service identified by its resource name.
      tags:
        - catalog
        - ai_gateway
      description: >-
        Returns the model service identified by its resource name.


        You must be the owner of the model service or have ``EXECUTE``,
        ``READ_METADATA``, or ``MANAGE`` on

        it, plus ``USE_CATALOG`` on the parent catalog and ``USE_SCHEMA`` on the
        parent schema.


        :param name: str
          Resource name of the model service. Format: ``model-services/{catalog}.{schema}.{model_service}``.
          Each ``{...}`` component is capped at 255 characters individually.
        :param include_browse: bool (optional)
          Whether to include model services for which the principal can only access selective metadata.

        :returns: :class:`ModelService`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: >-
            Resource name of the model service. Format:
            ``model-services/{catalog}.{schema}.{model_service}``. Each
            ``{...}`` component is capped at 255 characters individually.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include model services for which the principal can only
            access selective metadata.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelService'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
    patch:
      operationId: ai_gateway_update_model_service
      summary: >-
        Updates a model service. Only the fields named in ``update_mask`` are
        changed; the resource name is
      tags:
        - catalog
        - ai_gateway
      description: >-
        Updates a model service. Only the fields named in ``update_mask`` are
        changed; the resource name is

        immutable. Optionally supply an ``etag`` to make the update conditional
        on the model service not

        having changed since it was read.


        You must be the owner of the model service or have ``MANAGE`` on it,
        plus ``USE_CATALOG`` on the

        parent catalog and ``USE_SCHEMA`` on the parent schema.


        :param name: str
          Resource name of the model service. Format: ``model-services/{catalog}.{schema}.{model_service}``.
          Each ``{...}`` component is capped at 255 characters individually. Server-derived on Create from
          ``parent`` + ``model_service_id``; required and immutable on Update/Get/Delete.
        :param model_service: :class:`ModelService`
          The model service with the updated field values. ``name`` identifies the resource
          (``model-services/{catalog}.{schema}.{model_service}``); only fields listed in ``update_mask`` are
          applied.
        :param update_mask: FieldMask
          The list of fields to update. The framework validates each path against the ``model_service`` field
          above. Wildcard paths (``paths: ["*"]``) are not supported; list each field path explicitly.
        :param etag: str (optional)
          If-match precondition: when set, the update proceeds only if the current server-side etag matches.
          Empty means an unconditional update.

        :returns: :class:`ModelService`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: >-
            Resource name of the model service. Format:
            ``model-services/{catalog}.{schema}.{model_service}``. Each
            ``{...}`` component is capped at 255 characters individually.
            Server-derived on Create from ``parent`` + ``model_service_id``;
            required and immutable on Update/Get/Delete.
        - name: update_mask
          in: query
          required: true
          schema:
            type: object
          description: >-
            The list of fields to update. The framework validates each path
            against the ``model_service`` field above. Wildcard paths (``paths:
            ["*"]``) are not supported; list each field path explicitly.
        - name: etag
          in: query
          required: false
          schema:
            type: string
          description: >-
            If-match precondition: when set, the update proceeds only if the
            current server-side etag matches. Empty means an unconditional
            update.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                model_service:
                  type: object
                  properties:
                    browse_only:
                      type: boolean
                      description: >-
                        Whether the caller sees only metadata available through
                        the BROWSE privilege.
                    comment:
                      type: string
                      description: User-provided description.
                    config:
                      type: object
                      properties:
                        inference_table:
                          type: object
                          properties:
                            parent:
                              type: string
                              description: >-
                                Parent UC schema where the inference table is
                                created. Format: ``schemas/{catalog}.{schema}``.
                                Set at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                            disabled:
                              type: boolean
                              description: >-
                                Indicates whether payload logging is disabled
                                (opt-out). Unset means that payload logging is
                                active (the on-by-default state coincides with
                                the proto zero-value, so the server never fills
                                this field for a client that leaves it unset).
                                Set ``disabled = true`` to pause runtime logging
                                while keeping the sub-message attached
                                (preserving ``parent`` and ``table_name_prefix``
                                for a later flip back to active). ``parent``
                                remains required either way.
                            is_deleted:
                              type: boolean
                              description: >-
                                True when the bound inference TABLE has been
                                deleted but the parent service still references
                                it. The dangling reference is surfaced (not
                                silently dropped) so callers can see the broken
                                dependency. AI Gateway payload logging fails
                                closed in this state.
                            table:
                              type: string
                              description: >-
                                Resolved UC table for payload logs. Format:
                                ``tables/{catalog}.{schema}.{table}``.
                            table_name_prefix:
                              type: string
                              description: >-
                                Prefix for the inference-table's UC-registered
                                name. The actual leaf name UC stores is
                                ``<table_name_prefix>_payload``; the
                                ``_payload`` suffix is appended automatically.
                                To find the actual UC table after Create, read
                                the ``table`` field on the response. Defaults to
                                ``<model_service_name>_payload`` when unset. Set
                                at create time and immutable thereafter;
                                changing it on an existing service is rejected.
                          required:
                            - parent
                          description: Inference table config for payload logging.
                        rate_limits:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_KEY_REQUEST_TAG
                                  - RATE_LIMIT_KEY_SERVICE
                                  - RATE_LIMIT_KEY_SERVICE_PRINCIPAL
                                  - RATE_LIMIT_KEY_USER
                                  - RATE_LIMIT_KEY_USER_DEFAULT
                                  - RATE_LIMIT_KEY_USER_GROUP
                                description: >-
                                  Scope key. Determines whether ``principal`` is
                                  required.
                              renewal_period:
                                type: string
                                x-enum:
                                  - RATE_LIMIT_RENEWAL_PERIOD_HOUR
                                  - RATE_LIMIT_RENEWAL_PERIOD_MINUTE
                                description: Renewal period.
                              principal:
                                type: string
                                description: >-
                                  Principal this limit applies to: user email,
                                  group name, or service principal application
                                  ID. Required unless ``key`` is
                                  ``RATE_LIMIT_KEY_SERVICE``,
                                  ``RATE_LIMIT_KEY_USER_DEFAULT``, or
                                  ``RATE_LIMIT_KEY_REQUEST_TAG`` (which must not
                                  set a principal).
                              request_tag_key:
                                type: string
                                description: >-
                                  Request tag key this limit applies to.
                                  Required when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``, forbidden
                                  otherwise.
                              request_tag_value:
                                type: string
                                description: >-
                                  Request tag value this limit applies to. Only
                                  valid when ``key`` is
                                  ``RATE_LIMIT_KEY_REQUEST_TAG``. Leave unset to
                                  apply the limit to every value of
                                  ``request_tag_key`` (an any-value default); a
                                  set value is a specific override for that
                                  value.
                              requests:
                                type: integer
                                description: >-
                                  Max requests allowed within a renewal period.
                                  Leave unset for no request limit.
                              tokens:
                                type: integer
                                description: >-
                                  Max tokens allowed within a renewal period.
                                  Leave unset for no token limit.
                            required:
                              - key
                              - renewal_period
                            description: >-
                              A rate limit applied to service requests. Leave
                              ``requests`` or ``tokens`` unset to impose no
                                  limit on that dimension; set a value to cap that dimension within the renewal period.
                          description: >-
                            Rate limits applied to requests routed through this
                            model service.
                        routing:
                          type: object
                          properties:
                            destinations:
                              type: array
                              items:
                                type: object
                                properties:
                                  name:
                                    type: string
                                    description: >-
                                      User-facing label for this destination,
                                      used in routing references.
                                  destination_type:
                                    type: string
                                    x-enum:
                                      - >-
                                        DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL
                                      - >-
                                        DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL
                                      - >-
                                        DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                    description: >-
                                      Backing-model category. Determines which
                                      oneof variant is populated.
                                  external_model_config:
                                    type: object
                                    properties:
                                      model_provider_service:
                                        type: string
                                        description: >-
                                          Resource name of the governed
                                          ModelProviderService that owns provider
                                          auth and provider-specific
                                          configuration. The referenced
                                          ModelProviderService also carries the
                                          provider type, so this message does not
                                          surface it directly. Format:
                                          ``model-provider-services/{catalog}.{schema}.{model_provider_service}``.
                                          Each ``{...}`` component is capped at
                                          255 characters individually.
                                      target:
                                        type: object
                                        properties:
                                          model:
                                            type: string
                                            description: >-
                                              Provider-side model identifier (e.g.
                                              "gpt-5", "claude-opus-4-7"). This is a
                                              string on the LLM provider's side, not a
                                              UC entity. The UC governance hook for
                                              external destinations is the
                                              ModelProviderService referenced by
                                              ``ExternalModelConfig.model_provider_service``,
                                              not the model itself.
                                          native_api_types:
                                            type: array
                                            items:
                                              type: string
                                            description: >-
                                              Provider-native API types the model
                                              supports (e.g.
                                              "openai/v1/chat/completions"). Used by
                                              the platform for request/response
                                              translation from the unified API type.
                                              At most 64 entries of at most 256
                                              characters each; the list is persisted
                                              into the destination binding's bounded
                                              storage envelope.
                                        required:
                                          - model
                                        description: >-
                                          Routing target for the destination: the
                                          provider-side model selected from the
                                          referenced ModelProviderService's
                                          ``targets`` catalog, plus the unified
                                          API types the platform should translate
                                          to/from at request time.
                                    required:
                                      - model_provider_service
                                      - target
                                    description: >-
                                      Configuration for an
                                      external-foundation-model destination.
                                      Provider auth and provider-specific
                                          cloud configuration are owned by a separate, governed ModelProviderService entity referenced via
                                          ``model_provider_service``; the platform resolves the provider at invocation time.
                                  is_deleted:
                                    type: boolean
                                    description: >-
                                      True when the destination's backing UC
                                      entity (MODEL for foundation-model
                                      destinations, MODEL_PROVIDER_SERVICE for
                                      external destinations) has been deleted
                                      but the destination row still references
                                      it. The dangling destination is surfaced
                                      (not silently dropped) so callers can see
                                      the broken routing. Inference traffic
                                      through this destination fails closed
                                      (BAD_REQUEST / FAILED_PRECONDITION).
                                  pay_per_token_config:
                                    type: object
                                    properties:
                                      model:
                                        type: string
                                        description: >-
                                          Resource name of the UC model. Format:
                                          ``models/{catalog}.{schema}.{model}``.
                                    required:
                                      - model
                                    description: >-
                                      Configuration for a pay-per-token
                                      foundation-model destination. Identifies
                                      the foundation model
                                          by its UC resource name; the platform resolves it to a Model Serving endpoint at request time.
                                  provisioned_throughput_config:
                                    type: object
                                    properties:
                                      model_serving_endpoint:
                                        type: string
                                        description: >-
                                          Name of the backing Model Serving
                                          endpoint serving the provisioned-
                                          throughput foundation model, as the
                                          AIP-122 typed resource name
                                          ``serving-endpoints/{name}``. The same
                                          UC model can be served on multiple Model
                                          Serving endpoints (different throughput
                                          / region / config); the caller picks
                                          which one this destination routes to.
                                          The endpoint must exist at create time.
                                      model:
                                        type: string
                                        description: >-
                                          UC model FQN of the model served by the
                                          backing endpoint (e.g.,
                                          ``system.ai.databricks-claude-opus-4-6``).
                                          Resolved from Model Serving at
                                          Create/Update time.
                                    required:
                                      - model_serving_endpoint
                                    description: >-
                                      Configuration for a provisioned-throughput
                                      foundation-model destination. References a
                                          pre-existing Model Serving endpoint that serves the model; sizing (provisioned throughput, burst
                                          scaling, model version) is owned by the Model Serving endpoint itself, not by this message.
                                  traffic_percentage:
                                    type: integer
                                    description: >-
                                      Share of traffic sent to this destination,
                                      0-100. Optional on fallback destinations;
                                      see FallbackConfig.
                                required:
                                  - name
                                  - destination_type
                                description: >-
                                  A destination the model service can route
                                  traffic to. Exactly one of the per-type
                                  configs inside
                                      ``type_config`` must be set, and it must match ``destination_type``.
                              description: >-
                                Primary routing destinations. At most 10 are
                                allowed. At least one is required on
                                CreateModelService; on UpdateModelService it is
                                required only when ``config.routing`` (or a
                                ``config.routing.*`` subpath) appears in
                                ``update_mask``.
                            fallback:
                              type: object
                              properties:
                                destinations:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      name:
                                        type: string
                                        description: >-
                                          User-facing label for this destination,
                                          used in routing references.
                                      destination_type:
                                        type: string
                                        x-enum:
                                          - >-
                                            DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL
                                          - >-
                                            DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL
                                          - >-
                                            DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                        description: >-
                                          Backing-model category. Determines which
                                          oneof variant is populated.
                                      external_model_config:
                                        type: object
                                        properties:
                                          model_provider_service:
                                            type: string
                                            description: >-
                                              Resource name of the governed
                                              ModelProviderService that owns provider
                                              auth and provider-specific
                                              configuration. The referenced
                                              ModelProviderService also carries the
                                              provider type, so this message does not
                                              surface it directly. Format:
                                              ``model-provider-services/{catalog}.{schema}.{model_provider_service}``.
                                              Each ``{...}`` component is capped at
                                              255 characters individually.
                                          target:
                                            type: object
                                            properties:
                                              model:
                                                type: string
                                                description: >-
                                                  Provider-side model identifier (e.g.
                                                  "gpt-5", "claude-opus-4-7"). This is a
                                                  string on the LLM provider's side, not a
                                                  UC entity. The UC governance hook for
                                                  external destinations is the
                                                  ModelProviderService referenced by
                                                  ``ExternalModelConfig.model_provider_service``,
                                                  not the model itself.
                                              native_api_types:
                                                type: array
                                                items:
                                                  type: string
                                                description: >-
                                                  Provider-native API types the model
                                                  supports (e.g.
                                                  "openai/v1/chat/completions"). Used by
                                                  the platform for request/response
                                                  translation from the unified API type.
                                                  At most 64 entries of at most 256
                                                  characters each; the list is persisted
                                                  into the destination binding's bounded
                                                  storage envelope.
                                            required:
                                              - model
                                            description: >-
                                              Routing target for the destination: the
                                              provider-side model selected from the
                                              referenced ModelProviderService's
                                              ``targets`` catalog, plus the unified
                                              API types the platform should translate
                                              to/from at request time.
                                        required:
                                          - model_provider_service
                                          - target
                                        description: >-
                                          Configuration for an
                                          external-foundation-model destination.
                                          Provider auth and provider-specific
                                              cloud configuration are owned by a separate, governed ModelProviderService entity referenced via
                                              ``model_provider_service``; the platform resolves the provider at invocation time.
                                      is_deleted:
                                        type: boolean
                                        description: >-
                                          True when the destination's backing UC
                                          entity (MODEL for foundation-model
                                          destinations, MODEL_PROVIDER_SERVICE for
                                          external destinations) has been deleted
                                          but the destination row still references
                                          it. The dangling destination is surfaced
                                          (not silently dropped) so callers can
                                          see the broken routing. Inference
                                          traffic through this destination fails
                                          closed (BAD_REQUEST /
                                          FAILED_PRECONDITION).
                                      pay_per_token_config:
                                        type: object
                                        properties:
                                          model:
                                            type: string
                                            description: >-
                                              Resource name of the UC model. Format:
                                              ``models/{catalog}.{schema}.{model}``.
                                        required:
                                          - model
                                        description: >-
                                          Configuration for a pay-per-token
                                          foundation-model destination. Identifies
                                          the foundation model
                                              by its UC resource name; the platform resolves it to a Model Serving endpoint at request time.
                                      provisioned_throughput_config:
                                        type: object
                                        properties:
                                          model_serving_endpoint:
                                            type: string
                                            description: >-
                                              Name of the backing Model Serving
                                              endpoint serving the provisioned-
                                              throughput foundation model, as the
                                              AIP-122 typed resource name
                                              ``serving-endpoints/{name}``. The same
                                              UC model can be served on multiple Model
                                              Serving endpoints (different throughput
                                              / region / config); the caller picks
                                              which one this destination routes to.
                                              The endpoint must exist at create time.
                                          model:
                                            type: string
                                            description: >-
                                              UC model FQN of the model served by the
                                              backing endpoint (e.g.,
                                              ``system.ai.databricks-claude-opus-4-6``).
                                              Resolved from Model Serving at
                                              Create/Update time.
                                        required:
                                          - model_serving_endpoint
                                        description: >-
                                          Configuration for a
                                          provisioned-throughput foundation-model
                                          destination. References a
                                              pre-existing Model Serving endpoint that serves the model; sizing (provisioned throughput, burst
                                              scaling, model version) is owned by the Model Serving endpoint itself, not by this message.
                                      traffic_percentage:
                                        type: integer
                                        description: >-
                                          Share of traffic sent to this
                                          destination, 0-100. Optional on fallback
                                          destinations; see FallbackConfig.
                                    required:
                                      - name
                                      - destination_type
                                    description: >-
                                      A destination the model service can route
                                      traffic to. Exactly one of the per-type
                                      configs inside
                                          ``type_config`` must be set, and it must match ``destination_type``.
                                  description: >-
                                    Ordered list of fallback destinations.
                                    Traversal is in list order; the attempt
                                    count is the length of the list. At most 5
                                    are allowed.
                              description: >-
                                Fallback routing config, applied after primary
                                destinations fail.
                            first_token_timeout:
                              type: string
                              description: >-
                                Timeout for the first token of a streaming
                                response. If a destination does not return its
                                first token within this duration, AI Gateway
                                aborts the attempt and fails over to the next
                                destination. Applies to streaming requests only.
                                Leave unset for no first-token timeout.
                            traffic_splitting:
                              type: object
                              properties: {}
                              description: >-
                                Marker message selecting request-based traffic
                                splitting. Traffic is distributed according to
                                each destination's traffic_percentage value; no
                                configuration lives on this message itself.
                          description: >-
                            Routing configuration: destinations, routing
                            strategy, and fallback.
                      description: >-
                        Operational configuration: destinations, routing, rate
                        limits, inference table. Required on CreateModelService;
                        on UpdateModelService it is required only when
                        ``config`` (or a ``config.*`` subpath) appears in
                        ``update_mask``.
                    create_time:
                      type: string
                      format: date-time
                      description: When the model service was created.
                    created_by:
                      type: string
                      description: Creator identity.
                    effective_owner:
                      type: string
                      description: >-
                        The resolved owner of the ModelService. Falls back to
                        the caller's identity when ``owner`` is not explicitly
                        set on creation.
                    etag:
                      type: string
                      description: >-
                        Optimistic concurrency control token. Server-generated
                        from the entity's state and returned on every read. To
                        use it as an if-match precondition on a mutation, echo
                        the last-read value back via the dedicated ``etag``
                        field on the Update / Delete request; the server rejects
                        the mutation if the stored etag differs.
                    metastore_id:
                      type: string
                      description: Metastore hosting the model service.
                    name:
                      type: string
                      description: >-
                        Resource name of the model service. Format:
                        ``model-services/{catalog}.{schema}.{model_service}``.
                        Each ``{...}`` component is capped at 255 characters
                        individually. Server-derived on Create from ``parent`` +
                        ``model_service_id``; required and immutable on
                        Update/Get/Delete.
                    owner:
                      type: string
                      description: >-
                        The owner of the model service. Write-only; read owner
                        via effective_owner.
                    supported_api_types:
                      type: array
                      items:
                        type: string
                      description: >-
                        Unified API types this endpoint supports (e.g. "chat",
                        "embeddings", "completions"). Derived from the
                        destinations' backing models / providers at read time.
                    update_time:
                      type: string
                      format: date-time
                      description: When the model service was last modified.
                    updated_by:
                      type: string
                      description: Identity of the last updater.
                  description: >-
                    The model service with the updated field values. ``name``
                    identifies the resource
                    (``model-services/{catalog}.{schema}.{model_service}``);
                    only fields listed in ``update_mask`` are applied.
              required:
                - model_service
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelService'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AiGatewayAPI
  /api/2.1/unity-catalog/artifact-allowlists/{artifact_type.value}:
    get:
      operationId: artifact_allowlists_get
      summary: >-
        Get the artifact allowlist of a certain artifact type. The caller must
        be a metastore admin or have
      tags:
        - catalog
        - artifact_allowlists
      description: >-
        Get the artifact allowlist of a certain artifact type. The caller must
        be a metastore admin or have

        the **MANAGE ALLOWLIST** privilege on the metastore.


        :param artifact_type: :class:`ArtifactType`
          The artifact type of the allowlist.

        :returns: :class:`ArtifactAllowlistInfo`
      parameters:
        - name: artifact_type.value
          in: path
          required: true
          schema:
            type: string
        - name: artifact_type
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/ArtifactType'
          description: The artifact type of the allowlist.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactAllowlistInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ArtifactAllowlistsAPI
    put:
      operationId: artifact_allowlists_update
      summary: >-
        Set the artifact allowlist of a certain artifact type. The whole
        artifact allowlist is replaced with
      tags:
        - catalog
        - artifact_allowlists
      description: >-
        Set the artifact allowlist of a certain artifact type. The whole
        artifact allowlist is replaced with

        the new allowlist. The caller must be a metastore admin or have the
        **MANAGE ALLOWLIST** privilege on

        the metastore.


        :param artifact_type: :class:`ArtifactType`
          The artifact type of the allowlist.
        :param artifact_matchers: List[:class:`ArtifactMatcher`]
          A list of allowed artifact match patterns.
        :param created_at: int (optional)
          Time at which this artifact allowlist was set, in epoch milliseconds.
        :param created_by: str (optional)
          Username of the user who set the artifact allowlist.
        :param metastore_id: str (optional)
          Unique identifier of parent metastore.

        :returns: :class:`ArtifactAllowlistInfo`
      parameters:
        - name: artifact_type.value
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                artifact_type:
                  type: string
                  x-enum:
                    - INIT_SCRIPT
                    - LIBRARY_JAR
                    - LIBRARY_MAVEN
                  description: The artifact type of the allowlist.
                artifact_matchers:
                  type: array
                  items:
                    type: object
                    properties:
                      artifact:
                        type: string
                      match_type:
                        type: string
                        x-enum:
                          - PREFIX_MATCH
                        description: The pattern matching type of the artifact
                    required:
                      - artifact
                      - match_type
                  description: A list of allowed artifact match patterns.
                created_at:
                  type: integer
                  description: >-
                    Time at which this artifact allowlist was set, in epoch
                    milliseconds.
                created_by:
                  type: string
                  description: Username of the user who set the artifact allowlist.
                metastore_id:
                  type: string
                  description: Unique identifier of parent metastore.
              required:
                - artifact_type
                - artifact_matchers
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactAllowlistInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ArtifactAllowlistsAPI
  /api/2.1/unity-catalog/catalogs:
    post:
      operationId: catalogs_create
      summary: >-
        Creates a new catalog instance in the parent metastore if the caller is
        a metastore admin or has the
      tags:
        - catalog
        - catalogs
      description: >-
        Creates a new catalog instance in the parent metastore if the caller is
        a metastore admin or has the

        **CREATE_CATALOG** privilege.


        :param name: str
          Name of catalog.
        :param catalog_federation_config: :class:`CatalogFederationConfig`
        (optional)
          Federation-only configuration, present only for FOREIGN catalogs.
        :param comment: str (optional)
          User-provided free-form text description.
        :param connection_name: str (optional)
          The name of the connection to an external data source.
        :param conversion_info: :class:`ConversionInfo` (optional)
          Status of conversion of FOREIGN catalog to UC Native catalog.
        :param custom_max_retention_hours: int (optional)
          Custom maximum retention period in hours for the catalog
        :param dr_replication_info: :class:`DrReplicationInfo` (optional)
          Disaster Recovery replication state snapshot.
        :param managed_encryption_settings: :class:`EncryptionSettings`
        (optional)
          Control CMK encryption for managed catalog data
        :param options: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.
        :param properties: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.
        :param provider_name: str (optional)
          The name of delta sharing provider.

          A Delta Sharing catalog is a catalog that is based on a Delta share on a remote sharing server.
        :param share_name: str (optional)
          The name of the share under the share provider.
        :param storage_root: str (optional)
          Storage root URL for managed tables within catalog.

        :returns: :class:`CatalogInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of catalog.
                catalog_federation_config:
                  type: object
                  properties:
                    schema_filter_type:
                      type: string
                      x-enum:
                        - SCHEMA_FILTER_TYPE_EXCLUDE
                        - SCHEMA_FILTER_TYPE_INCLUDE
                        - SCHEMA_FILTER_TYPE_INCLUDE_ALL
                      description: >-
                        Controls how the catalog's schema filter items are
                        treated.
                  description: >-
                    Federation-only configuration, present only for FOREIGN
                    catalogs.
                comment:
                  type: string
                  description: User-provided free-form text description.
                connection_name:
                  type: string
                  description: The name of the connection to an external data source.
                conversion_info:
                  type: object
                  properties:
                    state:
                      type: string
                      x-enum:
                        - COMPLETED
                        - IN_PROGRESS
                      description: The conversion state of the resource.
                  description: >-
                    Status of conversion of FOREIGN catalog to UC Native
                    catalog.
                custom_max_retention_hours:
                  type: integer
                  description: Custom maximum retention period in hours for the catalog
                dr_replication_info:
                  type: object
                  properties:
                    last_failover_time_ms:
                      type: integer
                      description: >-
                        Wall-clock epoch milliseconds when this catalog was last
                        promoted to primary via failover or failback. Set by DR
                        Manager. Used by Predictive Optimization to suppress
                        operations until sufficient workload history
                        accumulates.
                    replicated_entities:
                      type: string
                      description: >-
                        See
                        https://docs.google.com/document/d/1X0A_3hMhzuS2V1E3zB0x5wxPsFx70bVYK5rHep2AjW8.
                    status:
                      type: string
                      x-enum:
                        - DR_REPLICATION_STATUS_PRIMARY
                        - DR_REPLICATION_STATUS_SECONDARY
                      description: >-
                        Create a collection of name/value pairs.


                        Example enumeration:


                        >>> class Color(Enum):

                        ...     RED = 1

                        ...     BLUE = 2

                        ...     GREEN = 3


                        Access them by:


                        - attribute access:

                          >>> Color.RED
                          <Color.RED: 1>

                        - value lookup:

                          >>> Color(1)
                          <Color.RED: 1>

                        - name lookup:

                          >>> Color['RED']
                          <Color.RED: 1>

                        Enumerations can be iterated over, and know how many
                        members they have:


                        >>> len(Color)

                        3


                        >>> list(Color)

                        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                        Methods can be added to enumerations, and members can
                        have their own

                        attributes -- see the documentation for details.
                  description: Disaster Recovery replication state snapshot.
                managed_encryption_settings:
                  type: object
                  properties:
                    azure_encryption_settings:
                      type: object
                      properties:
                        azure_tenant_id:
                          type: string
                        azure_cmk_access_connector_id:
                          type: string
                        azure_cmk_managed_identity_id:
                          type: string
                      required:
                        - azure_tenant_id
                      description: >-
                        optional Azure settings - only required if an Azure CMK
                        is used.
                    azure_key_vault_key_id:
                      type: string
                      description: the AKV URL in Azure, null otherwise.
                    customer_managed_key_id:
                      type: string
                      description: the CMK uuid in AWS and GCP, null otherwise.
                  description: Control CMK encryption for managed catalog data
                options:
                  type: object
                  description: A map of key-value properties attached to the securable.
                properties:
                  type: object
                  description: A map of key-value properties attached to the securable.
                provider_name:
                  type: string
                  description: >-
                    The name of delta sharing provider. A Delta Sharing catalog
                    is a catalog that is based on a Delta share on a remote
                    sharing server.
                share_name:
                  type: string
                  description: The name of the share under the share provider.
                storage_root:
                  type: string
                  description: Storage root URL for managed tables within catalog.
              required:
                - name
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: CatalogsAPI
    get:
      operationId: catalogs_list
      summary: >-
        Gets an array of catalogs in the metastore. If the caller is the
        metastore admin, all catalogs will be
      tags:
        - catalog
        - catalogs
      description: >-
        Gets an array of catalogs in the metastore. If the caller is the
        metastore admin, all catalogs will be

        retrieved. Otherwise, only catalogs owned by the caller (or for which
        the caller has the

        **USE_CATALOG** privilege) will be retrieved. There is no guarantee of a
        specific ordering of the

        elements in the array.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param include_browse: bool (optional)
          Whether to include catalogs in the response for which the principal can only access selective
          metadata for
        :param include_unbound: bool (optional)
          Whether to include catalogs not bound to the workspace. Effective only if the user has permission to
          update the catalog–workspace binding.
        :param max_results: int (optional)
          Maximum number of catalogs to return.

          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to a value less than 0, an invalid parameter error is returned;
          - If not set, all valid catalogs are returned (not recommended).
          - Note: The number of returned catalogs might be less than the specified max_results size, even
            zero. The only definitive indication that no further catalogs can be fetched is when the
            next_page_token is unset from the response.
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`CatalogInfo`
      parameters:
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include catalogs in the response for which the principal
            can only access selective metadata for
        - name: include_unbound
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include catalogs not bound to the workspace. Effective
            only if the user has permission to update the catalog–workspace
            binding.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of catalogs to return. - when set to 0, the page
            length is set to a server configured value (recommended); - when set
            to a value greater than 0, the page length is the minimum of this
            value and a server configured value; - when set to a value less than
            0, an invalid parameter error is returned; - If not set, all valid
            catalogs are returned (not recommended). - Note: The number of
            returned catalogs might be less than the specified max_results size,
            even zero. The only definitive indication that no further catalogs
            can be fetched is when the next_page_token is unset from the
            response.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCatalogsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: CatalogsAPI
  /api/2.1/unity-catalog/catalogs/{name}:
    delete:
      operationId: catalogs_delete
      summary: >-
        Deletes the catalog that matches the supplied name. The caller must be a
        metastore admin or the owner
      tags:
        - catalog
        - catalogs
      description: >-
        Deletes the catalog that matches the supplied name. The caller must be a
        metastore admin or the owner

        of the catalog.


        :param name: str
          The name of the catalog.
        :param force: bool (optional)
          Force deletion even if the catalog is not empty.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog.
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Force deletion even if the catalog is not empty.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: CatalogsAPI
    get:
      operationId: catalogs_get
      summary: >-
        Gets the specified catalog in a metastore. The caller must be a
        metastore admin, the owner of the
      tags:
        - catalog
        - catalogs
      description: >-
        Gets the specified catalog in a metastore. The caller must be a
        metastore admin, the owner of the

        catalog, or a user that has the **USE_CATALOG** privilege set for their
        account.


        :param name: str
          The name of the catalog.
        :param include_browse: bool (optional)
          Whether to include catalogs in the response for which the principal can only access selective
          metadata for

        :returns: :class:`CatalogInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include catalogs in the response for which the principal
            can only access selective metadata for
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: CatalogsAPI
    patch:
      operationId: catalogs_update
      summary: >-
        Updates the catalog that matches the supplied name. The caller must be
        either the owner of the
      tags:
        - catalog
        - catalogs
      description: >-
        Updates the catalog that matches the supplied name. The caller must be
        either the owner of the

        catalog, or a metastore admin (when changing the owner field of the
        catalog).


        :param name: str
          The name of the catalog.
        :param catalog_federation_config: :class:`CatalogFederationConfig`
        (optional)
          Federation-only configuration, present only for FOREIGN catalogs.
        :param comment: str (optional)
          User-provided free-form text description.
        :param conversion_info: :class:`ConversionInfo` (optional)
          Status of conversion of FOREIGN catalog to UC Native catalog.
        :param custom_max_retention_hours: int (optional)
          Custom maximum retention period in hours for the catalog
        :param dr_replication_info: :class:`DrReplicationInfo` (optional)
          Disaster Recovery replication state snapshot.
        :param enable_predictive_optimization:
        :class:`EnablePredictiveOptimization` (optional)
          Whether predictive optimization should be enabled for this object and objects under it.
        :param isolation_mode: :class:`CatalogIsolationMode` (optional)
          Whether the current securable is accessible from all workspaces or a specific set of workspaces.
        :param managed_encryption_settings: :class:`EncryptionSettings`
        (optional)
          Control CMK encryption for managed catalog data
        :param new_name: str (optional)
          New name for the catalog.
        :param options: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.
        :param owner: str (optional)
          Username of current owner of catalog.
        :param properties: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.

        :returns: :class:`CatalogInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                catalog_federation_config:
                  type: object
                  properties:
                    schema_filter_type:
                      type: string
                      x-enum:
                        - SCHEMA_FILTER_TYPE_EXCLUDE
                        - SCHEMA_FILTER_TYPE_INCLUDE
                        - SCHEMA_FILTER_TYPE_INCLUDE_ALL
                      description: >-
                        Controls how the catalog's schema filter items are
                        treated.
                  description: >-
                    Federation-only configuration, present only for FOREIGN
                    catalogs.
                comment:
                  type: string
                  description: User-provided free-form text description.
                conversion_info:
                  type: object
                  properties:
                    state:
                      type: string
                      x-enum:
                        - COMPLETED
                        - IN_PROGRESS
                      description: The conversion state of the resource.
                  description: >-
                    Status of conversion of FOREIGN catalog to UC Native
                    catalog.
                custom_max_retention_hours:
                  type: integer
                  description: Custom maximum retention period in hours for the catalog
                dr_replication_info:
                  type: object
                  properties:
                    last_failover_time_ms:
                      type: integer
                      description: >-
                        Wall-clock epoch milliseconds when this catalog was last
                        promoted to primary via failover or failback. Set by DR
                        Manager. Used by Predictive Optimization to suppress
                        operations until sufficient workload history
                        accumulates.
                    replicated_entities:
                      type: string
                      description: >-
                        See
                        https://docs.google.com/document/d/1X0A_3hMhzuS2V1E3zB0x5wxPsFx70bVYK5rHep2AjW8.
                    status:
                      type: string
                      x-enum:
                        - DR_REPLICATION_STATUS_PRIMARY
                        - DR_REPLICATION_STATUS_SECONDARY
                      description: >-
                        Create a collection of name/value pairs.


                        Example enumeration:


                        >>> class Color(Enum):

                        ...     RED = 1

                        ...     BLUE = 2

                        ...     GREEN = 3


                        Access them by:


                        - attribute access:

                          >>> Color.RED
                          <Color.RED: 1>

                        - value lookup:

                          >>> Color(1)
                          <Color.RED: 1>

                        - name lookup:

                          >>> Color['RED']
                          <Color.RED: 1>

                        Enumerations can be iterated over, and know how many
                        members they have:


                        >>> len(Color)

                        3


                        >>> list(Color)

                        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                        Methods can be added to enumerations, and members can
                        have their own

                        attributes -- see the documentation for details.
                  description: Disaster Recovery replication state snapshot.
                enable_predictive_optimization:
                  type: string
                  x-enum:
                    - DISABLE
                    - ENABLE
                    - INHERIT
                  description: >-
                    Whether predictive optimization should be enabled for this
                    object and objects under it.
                isolation_mode:
                  type: string
                  x-enum:
                    - ISOLATED
                    - OPEN
                    - OPEN_IN_ACCOUNT
                  description: >-
                    Whether the current securable is accessible from all
                    workspaces or a specific set of workspaces.
                managed_encryption_settings:
                  type: object
                  properties:
                    azure_encryption_settings:
                      type: object
                      properties:
                        azure_tenant_id:
                          type: string
                        azure_cmk_access_connector_id:
                          type: string
                        azure_cmk_managed_identity_id:
                          type: string
                      required:
                        - azure_tenant_id
                      description: >-
                        optional Azure settings - only required if an Azure CMK
                        is used.
                    azure_key_vault_key_id:
                      type: string
                      description: the AKV URL in Azure, null otherwise.
                    customer_managed_key_id:
                      type: string
                      description: the CMK uuid in AWS and GCP, null otherwise.
                  description: Control CMK encryption for managed catalog data
                new_name:
                  type: string
                  description: New name for the catalog.
                options:
                  type: object
                  description: A map of key-value properties attached to the securable.
                owner:
                  type: string
                  description: Username of current owner of catalog.
                properties:
                  type: object
                  description: A map of key-value properties attached to the securable.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: CatalogsAPI
  /api/2.1/unity-catalog/connections:
    post:
      operationId: connections_create
      summary: Creates a new connection
      tags:
        - catalog
        - connections
      description: >-
        Creates a new connection


        Creates a new connection to an external data source. It allows users to
        specify connection details and

        configurations for interaction with the external server.


        :param name: str
          Name of the connection.
        :param connection_type: :class:`ConnectionType`
          The type of connection.
        :param options: Dict[str,str]
          A map of key-value properties attached to the securable.
        :param comment: str (optional)
          User-provided free-form text description.
        :param environment_settings: :class:`EnvironmentSettings` (optional)
          [Create,Update:OPT] Connection environment settings as EnvironmentSettings object.
        :param parent: str (optional)
          Parent schema for schema-level connections, in format "schemas/{catalog}.{schema}". Absent for
          metastore-level (L1) connections.
        :param properties: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.
        :param read_only: bool (optional)
          If the connection is read only.
        :param secrets: Dict[str,str] (optional)
          A map of option names to UC Secret references. Keys are connection option names (same as in
          OptionsKVPairs) and values are UC Secret fully qualified names.

        :returns: :class:`ConnectionInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of the connection.
                connection_type:
                  type: string
                  x-enum:
                    - ADOBE_COMMERCE
                    - ADP_WORKFORCE_NOW
                    - AHA
                    - AIRTABLE
                    - AMPLITUDE
                    - APPFIGURES
                    - APPLE_APP_STORE
                    - APPLE_SEARCH_ADS
                    - ATLASSIAN_ORGANIZATION
                    - AWIN
                    - AZURE_MONITOR_LOGS
                    - BIGLAKE
                    - BIGQUERY
                    - CERIDIAN_DAYFORCE
                    - CONFLUENCE
                    - DATABRICKS
                    - DELIGHTED
                    - DYNAMICS365
                    - EPIC_CLARITY
                    - FRESHSERVICE
                    - FRONT
                    - GA4_RAW_DATA
                    - GENESYS
                    - GITHUB
                    - GITLAB
                    - GLUE
                    - GMAIL
                    - GONG
                    - GOOGLE_ANALYTICS
                    - GOOGLE_CALENDAR
                    - GOOGLE_CLOUD_LAKEHOUSE
                    - GOOGLE_WORKSPACE
                    - GURU
                    - HIBOB
                    - HIVE_METASTORE
                    - HTTP
                    - HUBSPOT
                    - ICEBERG_REST
                    - IRONCLAD
                    - JDBC
                    - KINESIS
                    - LINEAR
                    - MARKETO
                    - META_MARKETING
                    - MICROSOFT_ENTRA_ID
                    - MONDAY_COM
                    - MYSQL
                    - NETSKOPE_LOGS
                    - NOTION
                    - ORACLE
                    - ORACLE_ELOQUA
                    - ORACLE_FUSION_CLOUD
                    - OUTLOOK
                    - PAGERDUTY
                    - PALANTIR
                    - PARTNERSTACK
                    - PENDO
                    - POSTGRESQL
                    - POWER_BI
                    - PUBSUB
                    - QUICKBOOKS
                    - REDSHIFT
                    - SALESFORCE
                    - SALESFORCE_DATA_CLOUD
                    - SALESLOFT
                    - SAP_SUCCESSFACTORS
                    - SAS
                    - SENDGRID
                    - SERVICENOW
                    - SHOPIFY
                    - SLACK_ACCESS_AND_INTEGRATION_LOGS
                    - SMARTSHEET
                    - SNAPCHAT_ADS
                    - SNOWFLAKE
                    - SPLUNK
                    - SQLDW
                    - SQLSERVER
                    - SQUARE
                    - TERADATA
                    - UNKNOWN_CONNECTION_TYPE
                    - VERKADA
                    - WORKDAY_RAAS
                    - YOUTUBE_ANALYTICS
                    - ZENDESK
                    - ZIP
                    - ZOHO_BOOKS
                    - ZOOM
                    - ZOOM_LOGS
                  description: The type of connection.
                options:
                  type: object
                  description: A map of key-value properties attached to the securable.
                comment:
                  type: string
                  description: User-provided free-form text description.
                environment_settings:
                  type: object
                  properties:
                    environment_version:
                      type: string
                    java_dependencies:
                      type: array
                      items:
                        type: string
                  description: >-
                    [Create,Update:OPT] Connection environment settings as
                    EnvironmentSettings object.
                parent:
                  type: string
                  description: >-
                    Parent schema for schema-level connections, in format
                    "schemas/{catalog}.{schema}". Absent for metastore-level
                    (L1) connections.
                properties:
                  type: object
                  description: A map of key-value properties attached to the securable.
                read_only:
                  type: boolean
                  description: If the connection is read only.
                secrets:
                  type: object
                  description: >-
                    A map of option names to UC Secret references. Keys are
                    connection option names (same as in OptionsKVPairs) and
                    values are UC Secret fully qualified names.
              required:
                - name
                - connection_type
                - options
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ConnectionsAPI
    get:
      operationId: connections_list
      summary: List all connections.
      tags:
        - catalog
        - connections
      description: >-
        List all connections.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param max_results: int (optional)
          Maximum number of connections to return.

          - If not set, all connections are returned (not recommended).
          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.
        :param parent: str (optional)
          Optional. Parent schema filter for listing schema-level connections, in format
          "schemas/{catalog}.{schema}".

        :returns: Iterator over :class:`ConnectionInfo`
      parameters:
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of connections to return. - If not set, all
            connections are returned (not recommended). - when set to a value
            greater than 0, the page length is the minimum of this value and a
            server configured value; - when set to 0, the page length is set to
            a server configured value (recommended); - when set to a value less
            than 0, an invalid parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
        - name: parent
          in: query
          required: false
          schema:
            type: string
          description: >-
            Optional. Parent schema filter for listing schema-level connections,
            in format "schemas/{catalog}.{schema}".
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListConnectionsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ConnectionsAPI
  /api/2.1/unity-catalog/connections/{name}:
    delete:
      operationId: connections_delete
      summary: Deletes the connection that matches the supplied name.
      tags:
        - catalog
        - connections
      description: |-
        Deletes the connection that matches the supplied name.

        :param name: str
          The name of the connection to be deleted.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the connection to be deleted.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ConnectionsAPI
    get:
      operationId: connections_get
      summary: Gets a connection from it's name.
      tags:
        - catalog
        - connections
      description: |-
        Gets a connection from it's name.

        :param name: str
          Name of the connection.

        :returns: :class:`ConnectionInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the connection.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ConnectionsAPI
    patch:
      operationId: connections_update
      summary: Updates the connection that matches the supplied name.
      tags:
        - catalog
        - connections
      description: |-
        Updates the connection that matches the supplied name.

        :param name: str
          Name of the connection.
        :param options: Dict[str,str]
          A map of key-value properties attached to the securable.
        :param environment_settings: :class:`EnvironmentSettings` (optional)
          [Create,Update:OPT] Connection environment settings as EnvironmentSettings object.
        :param new_name: str (optional)
          New name for the connection.
        :param owner: str (optional)
          Username of current owner of the connection.
        :param secrets: Dict[str,str] (optional)
          A map of option names to UC Secret references. Keys are connection option names (same as in
          OptionsKVPairs) and values are UC Secret fully qualified names.

        :returns: :class:`ConnectionInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the connection.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                options:
                  type: object
                  description: A map of key-value properties attached to the securable.
                environment_settings:
                  type: object
                  properties:
                    environment_version:
                      type: string
                    java_dependencies:
                      type: array
                      items:
                        type: string
                  description: >-
                    [Create,Update:OPT] Connection environment settings as
                    EnvironmentSettings object.
                new_name:
                  type: string
                  description: New name for the connection.
                owner:
                  type: string
                  description: Username of current owner of the connection.
                secrets:
                  type: object
                  description: >-
                    A map of option names to UC Secret references. Keys are
                    connection option names (same as in OptionsKVPairs) and
                    values are UC Secret fully qualified names.
              required:
                - options
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ConnectionsAPI
  /api/2.1/unity-catalog/entity-tag-assignments:
    post:
      operationId: entity_tag_assignments_create
      summary: Creates a tag assignment for an Unity Catalog entity.
      tags:
        - catalog
        - entity_tag_assignments
      description: >-
        Creates a tag assignment for an Unity Catalog entity.


        To add tags to Unity Catalog entities, you must own the entity or have
        the following privileges:


        - **APPLY TAG** on the entity

        - **USE SCHEMA** on the entity's parent schema

        - **USE CATALOG** on the entity's parent catalog


        To add a governed tag to Unity Catalog entities, you must also have the
        **ASSIGN** or **MANAGE**

        permission on the tag policy. See `Manage tag policy permissions

        <https://docs.databricks.com/aws/en/admin/tag-policies/manage-permissions>`__.


        :param tag_assignment: :class:`EntityTagAssignment`


        :returns: :class:`EntityTagAssignment`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tag_assignment:
                  type: object
                  properties:
                    entity_name:
                      type: string
                      description: >-
                        The fully qualified name of the entity to which the tag
                        is assigned
                    tag_key:
                      type: string
                      description: The key of the tag
                    entity_type:
                      type: string
                      description: The type of the entity to which the tag is assigned.
                    inherited:
                      type: boolean
                      description: Boolean which indicates whether this tag is inherited.
                    source_type:
                      type: string
                      x-enum:
                        - TAG_ASSIGNMENT_SOURCE_TYPE_SYSTEM_DATA_CLASSIFICATION
                      description: >-
                        The source type of the tag assignment, e.g.,
                        user-assigned or system-assigned
                    tag_value:
                      type: string
                      description: The value of the tag
                    update_time:
                      type: string
                      format: date-time
                      description: The timestamp when the tag assignment was last updated
                    updated_by:
                      type: string
                      description: The user or principal who updated the tag assignment
                  required:
                    - entity_name
                    - tag_key
                    - entity_type
                  description: Represents a tag assignment to an entity
              required:
                - tag_assignment
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EntityTagAssignment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: EntityTagAssignmentsAPI
  /api/2.1/unity-catalog/entity-tag-assignments/{entity_type}/{entity_name}/tags/{tag_key}:
    delete:
      operationId: entity_tag_assignments_delete
      summary: Deletes a tag assignment for an Unity Catalog entity by its key.
      tags:
        - catalog
        - entity_tag_assignments
      description: >-
        Deletes a tag assignment for an Unity Catalog entity by its key.


        To delete tags from Unity Catalog entities, you must own the entity or
        have the following privileges:


        - **APPLY TAG** on the entity

        - **USE_SCHEMA** on the entity's parent schema

        - **USE_CATALOG** on the entity's parent catalog


        To delete a governed tag from Unity Catalog entities, you must also have
        the **ASSIGN** or **MANAGE**

        permission on the tag policy. See `Manage tag policy permissions

        <https://docs.databricks.com/aws/en/admin/tag-policies/manage-permissions>`__.


        :param entity_type: str
          The type of the entity to which the tag is assigned.
        :param entity_name: str
          The fully qualified name of the entity to which the tag is assigned
        :param tag_key: str
          Required. The key of the tag to delete
      parameters:
        - name: entity_type
          in: path
          required: true
          schema:
            type: string
          description: The type of the entity to which the tag is assigned.
        - name: entity_name
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified name of the entity to which the tag is assigned
        - name: tag_key
          in: path
          required: true
          schema:
            type: string
          description: Required. The key of the tag to delete
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: EntityTagAssignmentsAPI
    get:
      operationId: entity_tag_assignments_get
      summary: Gets a tag assignment for an Unity Catalog entity by tag key.
      tags:
        - catalog
        - entity_tag_assignments
      description: |-
        Gets a tag assignment for an Unity Catalog entity by tag key.

        :param entity_type: str
          The type of the entity to which the tag is assigned.
        :param entity_name: str
          The fully qualified name of the entity to which the tag is assigned
        :param tag_key: str
          Required. The key of the tag
        :param include_inherited: bool (optional)
          Boolean which indicates whether this tag is inherited.

        :returns: :class:`EntityTagAssignment`
      parameters:
        - name: entity_type
          in: path
          required: true
          schema:
            type: string
          description: The type of the entity to which the tag is assigned.
        - name: entity_name
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified name of the entity to which the tag is assigned
        - name: tag_key
          in: path
          required: true
          schema:
            type: string
          description: Required. The key of the tag
        - name: include_inherited
          in: query
          required: false
          schema:
            type: boolean
          description: Boolean which indicates whether this tag is inherited.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EntityTagAssignment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: EntityTagAssignmentsAPI
    patch:
      operationId: entity_tag_assignments_update
      summary: Updates an existing tag assignment for an Unity Catalog entity.
      tags:
        - catalog
        - entity_tag_assignments
      description: >-
        Updates an existing tag assignment for an Unity Catalog entity.


        To update tags to Unity Catalog entities, you must own the entity or
        have the following privileges:


        - **APPLY TAG** on the entity

        - **USE SCHEMA** on the entity's parent schema

        - **USE CATALOG** on the entity's parent catalog


        To update a governed tag to Unity Catalog entities, you must also have
        the **ASSIGN** or **MANAGE**

        permission on the tag policy. See `Manage tag policy permissions

        <https://docs.databricks.com/aws/en/admin/tag-policies/manage-permissions>`__.


        :param entity_type: str
          The type of the entity to which the tag is assigned.
        :param entity_name: str
          The fully qualified name of the entity to which the tag is assigned
        :param tag_key: str
          The key of the tag
        :param tag_assignment: :class:`EntityTagAssignment`

        :param update_mask: str
          The field mask must be a single string, with multiple fields separated by commas (no spaces). The
          field path is relative to the resource object, using a dot (``.``) to navigate sub-fields (e.g.,
          ``author.given_name``). Specification of elements in sequence or map fields is not allowed, as only
          the entire collection field can be specified. Field names must exactly match the resource field
          names.

          A field mask of ``*`` indicates full replacement. It’s recommended to always explicitly list the
          fields being updated and avoid using ``*`` wildcards, as it can lead to unintended results if the
          API changes in the future.

        :returns: :class:`EntityTagAssignment`
      parameters:
        - name: entity_type
          in: path
          required: true
          schema:
            type: string
          description: The type of the entity to which the tag is assigned.
        - name: entity_name
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified name of the entity to which the tag is assigned
        - name: tag_key
          in: path
          required: true
          schema:
            type: string
          description: The key of the tag
        - name: update_mask
          in: query
          required: true
          schema:
            type: string
          description: >-
            The field mask must be a single string, with multiple fields
            separated by commas (no spaces). The field path is relative to the
            resource object, using a dot (``.``) to navigate sub-fields (e.g.,
            ``author.given_name``). Specification of elements in sequence or map
            fields is not allowed, as only the entire collection field can be
            specified. Field names must exactly match the resource field names.
            A field mask of ``*`` indicates full replacement. It’s recommended
            to always explicitly list the fields being updated and avoid using
            ``*`` wildcards, as it can lead to unintended results if the API
            changes in the future.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tag_assignment:
                  type: object
                  properties:
                    entity_name:
                      type: string
                      description: >-
                        The fully qualified name of the entity to which the tag
                        is assigned
                    tag_key:
                      type: string
                      description: The key of the tag
                    entity_type:
                      type: string
                      description: The type of the entity to which the tag is assigned.
                    inherited:
                      type: boolean
                      description: Boolean which indicates whether this tag is inherited.
                    source_type:
                      type: string
                      x-enum:
                        - TAG_ASSIGNMENT_SOURCE_TYPE_SYSTEM_DATA_CLASSIFICATION
                      description: >-
                        The source type of the tag assignment, e.g.,
                        user-assigned or system-assigned
                    tag_value:
                      type: string
                      description: The value of the tag
                    update_time:
                      type: string
                      format: date-time
                      description: The timestamp when the tag assignment was last updated
                    updated_by:
                      type: string
                      description: The user or principal who updated the tag assignment
                  required:
                    - entity_name
                    - tag_key
                    - entity_type
                  description: Represents a tag assignment to an entity
              required:
                - tag_assignment
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EntityTagAssignment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: EntityTagAssignmentsAPI
  /api/2.1/unity-catalog/entity-tag-assignments/{entity_type}/{entity_name}/tags:
    get:
      operationId: entity_tag_assignments_list
      summary: List tag assignments for an Unity Catalog entity
      tags:
        - catalog
        - entity_tag_assignments
      description: >-
        List tag assignments for an Unity Catalog entity


        PAGINATION BEHAVIOR: The API is by default paginated, a page may contain
        zero results while still

        providing a next_page_token. Clients must continue reading pages until
        next_page_token is absent,

        which is the only indication that the end of results has been reached.


        :param entity_type: str
          The type of the entity to which the tag is assigned.
        :param entity_name: str
          The fully qualified name of the entity to which the tag is assigned
        :param include_inherited: bool (optional)
          Boolean which indicates whether this tag is inherited.
        :param max_results: int (optional)
          Optional. Maximum number of tag assignments to return in a single page
        :param page_token: str (optional)
          Optional. Pagination token to retrieve the next page of results

        :returns: Iterator over :class:`EntityTagAssignment`
      parameters:
        - name: entity_type
          in: path
          required: true
          schema:
            type: string
          description: The type of the entity to which the tag is assigned.
        - name: entity_name
          in: path
          required: true
          schema:
            type: string
          description: The fully qualified name of the entity to which the tag is assigned
        - name: include_inherited
          in: query
          required: false
          schema:
            type: boolean
          description: Boolean which indicates whether this tag is inherited.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Optional. Maximum number of tag assignments to return in a single
            page
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Optional. Pagination token to retrieve the next page of results
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEntityTagAssignmentsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: EntityTagAssignmentsAPI
  /api/2.0/lineage-tracking/external-lineage:
    post:
      operationId: external_lineage_create_external_lineage_relationship
      summary: >-
        Creates an external lineage relationship between a Databricks or
        external metadata object and another
      tags:
        - catalog
        - external_lineage
      description: >-
        Creates an external lineage relationship between a Databricks or
        external metadata object and another

        external metadata object.


        :param external_lineage_relationship:
        :class:`CreateRequestExternalLineage`


        :returns: :class:`ExternalLineageRelationship`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                external_lineage_relationship:
                  type: object
                  properties:
                    source:
                      type: object
                      properties:
                        external_metadata:
                          type: object
                          properties:
                            name:
                              type: string
                        model_version:
                          type: object
                          properties:
                            name:
                              type: string
                            version:
                              type: string
                        path:
                          type: object
                          properties:
                            url:
                              type: string
                        table:
                          type: object
                          properties:
                            name:
                              type: string
                        volume:
                          type: object
                          properties:
                            name:
                              type: string
                    target:
                      type: object
                      properties:
                        external_metadata:
                          type: object
                          properties:
                            name:
                              type: string
                        model_version:
                          type: object
                          properties:
                            name:
                              type: string
                            version:
                              type: string
                        path:
                          type: object
                          properties:
                            url:
                              type: string
                        table:
                          type: object
                          properties:
                            name:
                              type: string
                        volume:
                          type: object
                          properties:
                            name:
                              type: string
                      description: Target object of the external lineage relationship.
                    columns:
                      type: array
                      items:
                        type: object
                        properties:
                          source:
                            type: string
                          target:
                            type: string
                      description: >-
                        List of column relationships between source and target
                        objects.
                    id:
                      type: string
                      description: Unique identifier of the external lineage relationship.
                    properties:
                      type: object
                      description: >-
                        Key-value properties associated with the external
                        lineage relationship.
                  required:
                    - source
                    - target
              required:
                - external_lineage_relationship
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalLineageRelationship'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLineageAPI
    delete:
      operationId: external_lineage_delete_external_lineage_relationship
      summary: >-
        Deletes an external lineage relationship between a Databricks or
        external metadata object and another
      tags:
        - catalog
        - external_lineage
      description: >-
        Deletes an external lineage relationship between a Databricks or
        external metadata object and another

        external metadata object.


        :param external_lineage_relationship:
        :class:`DeleteRequestExternalLineage`
      parameters:
        - name: external_lineage_relationship
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/DeleteRequestExternalLineage'
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLineageAPI
    get:
      operationId: external_lineage_list_external_lineage_relationships
      summary: >-
        Lists external lineage relationships of a Databricks object or external
        metadata given a supplied
      tags:
        - catalog
        - external_lineage
      description: >-
        Lists external lineage relationships of a Databricks object or external
        metadata given a supplied

        direction.


        :param object_info: :class:`ExternalLineageObject`
          The object to query external lineage relationships for. Since this field is a query parameter,
          please flatten the nested fields. For example, if the object is a table, the query parameter should
          look like: ``object_info.table.name=main.sales.customers``
        :param lineage_direction: :class:`LineageDirection`
          The lineage direction to filter on.
        :param page_size: int (optional)
          Specifies the maximum number of external lineage relationships to return in a single response. The
          value must be less than or equal to 1000.
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`ExternalLineageInfo`
      parameters:
        - name: object_info
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/ExternalLineageObject'
          description: >-
            The object to query external lineage relationships for. Since this
            field is a query parameter, please flatten the nested fields. For
            example, if the object is a table, the query parameter should look
            like: ``object_info.table.name=main.sales.customers``
        - name: lineage_direction
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/LineageDirection'
          description: The lineage direction to filter on.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Specifies the maximum number of external lineage relationships to
            return in a single response. The value must be less than or equal to
            1000.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListExternalLineageRelationshipsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLineageAPI
    patch:
      operationId: external_lineage_update_external_lineage_relationship
      summary: >-
        Updates an external lineage relationship between a Databricks or
        external metadata object and another
      tags:
        - catalog
        - external_lineage
      description: >-
        Updates an external lineage relationship between a Databricks or
        external metadata object and another

        external metadata object.


        :param external_lineage_relationship:
        :class:`UpdateRequestExternalLineage`

        :param update_mask: str
          The field mask must be a single string, with multiple fields separated by commas (no spaces). The
          field path is relative to the resource object, using a dot (``.``) to navigate sub-fields (e.g.,
          ``author.given_name``). Specification of elements in sequence or map fields is not allowed, as only
          the entire collection field can be specified. Field names must exactly match the resource field
          names.

          A field mask of ``*`` indicates full replacement. It’s recommended to always explicitly list the
          fields being updated and avoid using ``*`` wildcards, as it can lead to unintended results if the
          API changes in the future.

        :returns: :class:`ExternalLineageRelationship`
      parameters:
        - name: update_mask
          in: query
          required: true
          schema:
            type: string
          description: >-
            The field mask must be a single string, with multiple fields
            separated by commas (no spaces). The field path is relative to the
            resource object, using a dot (``.``) to navigate sub-fields (e.g.,
            ``author.given_name``). Specification of elements in sequence or map
            fields is not allowed, as only the entire collection field can be
            specified. Field names must exactly match the resource field names.
            A field mask of ``*`` indicates full replacement. It’s recommended
            to always explicitly list the fields being updated and avoid using
            ``*`` wildcards, as it can lead to unintended results if the API
            changes in the future.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                external_lineage_relationship:
                  type: object
                  properties:
                    source:
                      type: object
                      properties:
                        external_metadata:
                          type: object
                          properties:
                            name:
                              type: string
                        model_version:
                          type: object
                          properties:
                            name:
                              type: string
                            version:
                              type: string
                        path:
                          type: object
                          properties:
                            url:
                              type: string
                        table:
                          type: object
                          properties:
                            name:
                              type: string
                        volume:
                          type: object
                          properties:
                            name:
                              type: string
                    target:
                      type: object
                      properties:
                        external_metadata:
                          type: object
                          properties:
                            name:
                              type: string
                        model_version:
                          type: object
                          properties:
                            name:
                              type: string
                            version:
                              type: string
                        path:
                          type: object
                          properties:
                            url:
                              type: string
                        table:
                          type: object
                          properties:
                            name:
                              type: string
                        volume:
                          type: object
                          properties:
                            name:
                              type: string
                      description: Target object of the external lineage relationship.
                    columns:
                      type: array
                      items:
                        type: object
                        properties:
                          source:
                            type: string
                          target:
                            type: string
                      description: >-
                        List of column relationships between source and target
                        objects.
                    id:
                      type: string
                      description: Unique identifier of the external lineage relationship.
                    properties:
                      type: object
                      description: >-
                        Key-value properties associated with the external
                        lineage relationship.
                  required:
                    - source
                    - target
              required:
                - external_lineage_relationship
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalLineageRelationship'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLineageAPI
  /api/2.1/unity-catalog/external-locations:
    post:
      operationId: external_locations_create
      summary: >-
        Creates a new external location entry in the metastore. The caller must
        be a metastore admin or have
      tags:
        - catalog
        - external_locations
      description: >-
        Creates a new external location entry in the metastore. The caller must
        be a metastore admin or have

        the **CREATE_EXTERNAL_LOCATION** privilege on both the metastore and the
        associated storage

        credential.


        :param name: str
          Name of the external location.
        :param url: str
          Path URL of the external location.
        :param credential_name: str
          Name of the storage credential used with this location.
        :param comment: str (optional)
          User-provided free-form text description.
        :param effective_enable_file_events: bool (optional)
          The effective value of ``enable_file_events`` after applying server-side defaults.
        :param effective_file_event_queue: :class:`FileEventQueue` (optional)
          The effective file event queue configuration after applying server-side defaults. Always populated
          when a queue is provisioned, regardless of whether the user explicitly set ``enable_file_events``.
          Use this field instead of ``file_event_queue`` for reading the actual queue state.
        :param enable_file_events: bool (optional)
          Whether to enable file events on this external location. Default to ``true``. Set to ``false`` to
          disable file events. The actual applied value may differ due to server-side defaults; check
          ``effective_enable_file_events`` for the effective state.
        :param encryption_details: :class:`EncryptionDetails` (optional)

        :param fallback: bool (optional)
          Indicates whether fallback mode is enabled for this external location. When fallback mode is
          enabled, the access to the location falls back to cluster credentials if UC credentials are not
          sufficient.
        :param file_event_queue: :class:`FileEventQueue` (optional)
          File event queue settings. If ``enable_file_events`` is not ``false``, must be defined and have
          exactly one of the documented properties.
        :param read_only: bool (optional)
          Indicates whether the external location is read-only.
        :param skip_validation: bool (optional)
          Skips validation of the storage credential associated with the external location.

        :returns: :class:`ExternalLocationInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of the external location.
                url:
                  type: string
                  description: Path URL of the external location.
                credential_name:
                  type: string
                  description: Name of the storage credential used with this location.
                comment:
                  type: string
                  description: User-provided free-form text description.
                effective_enable_file_events:
                  type: boolean
                  description: >-
                    The effective value of ``enable_file_events`` after applying
                    server-side defaults.
                effective_file_event_queue:
                  type: object
                  properties:
                    managed_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    managed_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    managed_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    managed_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                    provided_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    provided_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    provided_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    provided_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                  description: >-
                    The effective file event queue configuration after applying
                    server-side defaults. Always populated when a queue is
                    provisioned, regardless of whether the user explicitly set
                    ``enable_file_events``. Use this field instead of
                    ``file_event_queue`` for reading the actual queue state.
                enable_file_events:
                  type: boolean
                  description: >-
                    Whether to enable file events on this external location.
                    Default to ``true``. Set to ``false`` to disable file
                    events. The actual applied value may differ due to
                    server-side defaults; check ``effective_enable_file_events``
                    for the effective state.
                encryption_details:
                  type: object
                  properties:
                    sse_encryption_details:
                      type: object
                      properties:
                        algorithm:
                          type: string
                          x-enum:
                            - AWS_SSE_KMS
                            - AWS_SSE_S3
                          description: >-
                            Sets the value of the 'x-amz-server-side-encryption'
                            header in S3 request.
                        aws_kms_key_arn:
                          type: string
                          description: >-
                            Optional. The ARN of the SSE-KMS key used with the
                            S3 location, when algorithm = "SSE-KMS". Sets the
                            value of the
                            'x-amz-server-side-encryption-aws-kms-key-id'
                            header.
                      description: >-
                        Server-Side Encryption properties for clients
                        communicating with AWS s3.
                  description: >-
                    Encryption options that apply to clients connecting to cloud
                    storage.
                fallback:
                  type: boolean
                  description: >-
                    Indicates whether fallback mode is enabled for this external
                    location. When fallback mode is enabled, the access to the
                    location falls back to cluster credentials if UC credentials
                    are not sufficient.
                file_event_queue:
                  type: object
                  properties:
                    managed_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    managed_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    managed_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    managed_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                    provided_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    provided_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    provided_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    provided_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                  description: >-
                    File event queue settings. If ``enable_file_events`` is not
                    ``false``, must be defined and have exactly one of the
                    documented properties.
                read_only:
                  type: boolean
                  description: Indicates whether the external location is read-only.
                skip_validation:
                  type: boolean
                  description: >-
                    Skips validation of the storage credential associated with
                    the external location.
              required:
                - name
                - url
                - credential_name
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalLocationInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLocationsAPI
    get:
      operationId: external_locations_list
      summary: >-
        Gets an array of external locations (**ExternalLocationInfo** objects)
        from the metastore. The caller
      tags:
        - catalog
        - external_locations
      description: >-
        Gets an array of external locations (**ExternalLocationInfo** objects)
        from the metastore. The caller

        must be a metastore admin, the owner of the external location, or a user
        that has some privilege on

        the external location. There is no guarantee of a specific ordering of
        the elements in the array.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param include_browse: bool (optional)
          Whether to include external locations in the response for which the principal can only access
          selective metadata for
        :param include_unbound: bool (optional)
          Whether to include external locations not bound to the workspace. Effective only if the user has
          permission to update the location–workspace binding.
        :param max_results: int (optional)
          Maximum number of external locations to return. If not set, all the external locations are returned
          (not recommended).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`ExternalLocationInfo`
      parameters:
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include external locations in the response for which the
            principal can only access selective metadata for
        - name: include_unbound
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include external locations not bound to the workspace.
            Effective only if the user has permission to update the
            location–workspace binding.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of external locations to return. If not set, all the
            external locations are returned (not recommended). - when set to a
            value greater than 0, the page length is the minimum of this value
            and a server configured value; - when set to 0, the page length is
            set to a server configured value (recommended); - when set to a
            value less than 0, an invalid parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListExternalLocationsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLocationsAPI
  /api/2.1/unity-catalog/external-locations/{name}:
    delete:
      operationId: external_locations_delete
      summary: >-
        Deletes the specified external location from the metastore. The caller
        must be the owner of the
      tags:
        - catalog
        - external_locations
      description: >-
        Deletes the specified external location from the metastore. The caller
        must be the owner of the

        external location.


        :param name: str
          Name of the external location.
        :param force: bool (optional)
          Force deletion even if there are dependent external tables or mounts.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the external location.
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Force deletion even if there are dependent external tables or
            mounts.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLocationsAPI
    get:
      operationId: external_locations_get
      summary: >-
        Gets an external location from the metastore. The caller must be either
        a metastore admin, the owner
      tags:
        - catalog
        - external_locations
      description: >-
        Gets an external location from the metastore. The caller must be either
        a metastore admin, the owner

        of the external location, or a user that has some privilege on the
        external location.


        :param name: str
          Name of the external location.
        :param include_browse: bool (optional)
          Whether to include external locations in the response for which the principal can only access
          selective metadata for

        :returns: :class:`ExternalLocationInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the external location.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include external locations in the response for which the
            principal can only access selective metadata for
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalLocationInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLocationsAPI
    patch:
      operationId: external_locations_update
      summary: >-
        Updates an external location in the metastore. The caller must be the
        owner of the external location,
      tags:
        - catalog
        - external_locations
      description: >-
        Updates an external location in the metastore. The caller must be the
        owner of the external location,

        or be a metastore admin. In the second case, the admin can only update
        the name of the external

        location.


        :param name: str
          Name of the external location.
        :param comment: str (optional)
          User-provided free-form text description.
        :param credential_name: str (optional)
          Name of the storage credential used with this location.
        :param effective_enable_file_events: bool (optional)
          The effective value of ``enable_file_events`` after applying server-side defaults.
        :param effective_file_event_queue: :class:`FileEventQueue` (optional)
          The effective file event queue configuration after applying server-side defaults. Always populated
          when a queue is provisioned, regardless of whether the user explicitly set ``enable_file_events``.
          Use this field instead of ``file_event_queue`` for reading the actual queue state.
        :param enable_file_events: bool (optional)
          Whether to enable file events on this external location. Default to ``true``. Set to ``false`` to
          disable file events. The actual applied value may differ due to server-side defaults; check
          ``effective_enable_file_events`` for the effective state.
        :param encryption_details: :class:`EncryptionDetails` (optional)

        :param fallback: bool (optional)
          Indicates whether fallback mode is enabled for this external location. When fallback mode is
          enabled, the access to the location falls back to cluster credentials if UC credentials are not
          sufficient.
        :param file_event_queue: :class:`FileEventQueue` (optional)
          File event queue settings. If ``enable_file_events`` is not ``false``, must be defined and have
          exactly one of the documented properties.
        :param force: bool (optional)
          Force update even if changing url invalidates dependent external tables or mounts.
        :param isolation_mode: :class:`IsolationMode` (optional)

        :param new_name: str (optional)
          New name for the external location.
        :param owner: str (optional)
          The owner of the external location.
        :param read_only: bool (optional)
          Indicates whether the external location is read-only.
        :param skip_validation: bool (optional)
          Skips validation of the storage credential associated with the external location.
        :param url: str (optional)
          Path URL of the external location.

        :returns: :class:`ExternalLocationInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the external location.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                comment:
                  type: string
                  description: User-provided free-form text description.
                credential_name:
                  type: string
                  description: Name of the storage credential used with this location.
                effective_enable_file_events:
                  type: boolean
                  description: >-
                    The effective value of ``enable_file_events`` after applying
                    server-side defaults.
                effective_file_event_queue:
                  type: object
                  properties:
                    managed_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    managed_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    managed_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    managed_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                    provided_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    provided_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    provided_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    provided_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                  description: >-
                    The effective file event queue configuration after applying
                    server-side defaults. Always populated when a queue is
                    provisioned, regardless of whether the user explicitly set
                    ``enable_file_events``. Use this field instead of
                    ``file_event_queue`` for reading the actual queue state.
                enable_file_events:
                  type: boolean
                  description: >-
                    Whether to enable file events on this external location.
                    Default to ``true``. Set to ``false`` to disable file
                    events. The actual applied value may differ due to
                    server-side defaults; check ``effective_enable_file_events``
                    for the effective state.
                encryption_details:
                  type: object
                  properties:
                    sse_encryption_details:
                      type: object
                      properties:
                        algorithm:
                          type: string
                          x-enum:
                            - AWS_SSE_KMS
                            - AWS_SSE_S3
                          description: >-
                            Sets the value of the 'x-amz-server-side-encryption'
                            header in S3 request.
                        aws_kms_key_arn:
                          type: string
                          description: >-
                            Optional. The ARN of the SSE-KMS key used with the
                            S3 location, when algorithm = "SSE-KMS". Sets the
                            value of the
                            'x-amz-server-side-encryption-aws-kms-key-id'
                            header.
                      description: >-
                        Server-Side Encryption properties for clients
                        communicating with AWS s3.
                  description: >-
                    Encryption options that apply to clients connecting to cloud
                    storage.
                fallback:
                  type: boolean
                  description: >-
                    Indicates whether fallback mode is enabled for this external
                    location. When fallback mode is enabled, the access to the
                    location falls back to cluster credentials if UC credentials
                    are not sufficient.
                file_event_queue:
                  type: object
                  properties:
                    managed_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    managed_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    managed_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    managed_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                    provided_aqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format https://{storage
                            account}.queue.core.windows.net/{queue name} Only
                            required for provided_aqs.
                        resource_group:
                          type: string
                          description: >-
                            Optional resource group for the queue, event grid
                            subscription, and external location storage account.
                            Only required for locations with a service principal
                            storage credential
                        subscription_id:
                          type: string
                          description: >-
                            Optional subscription id for the queue, event grid
                            subscription, and external location storage account.
                            Required for locations with a service principal
                            storage credential
                    provided_onelake:
                      type: object
                      properties:
                        consumer_group:
                          type: string
                          description: >-
                            Event Hubs consumer group used to consume file
                            events. Defaults to "$Default" when unset.
                            Recommended for provided_onelake: create a dedicated
                            consumer group on the Event Hub for file events to
                            avoid contending with the customer's other
                            consumers.
                        event_hub_name:
                          type: string
                          description: >-
                            Event Hub entity name within the namespace. Only
                            required for provided_onelake.
                        event_hub_url:
                          type: string
                          description: >-
                            The Event Hub URL in the format
                            https://{namespace}.servicebus.windows.net/{event_hub_name}.
                            Deprecated: use fully_qualified_namespace +
                            event_hub_name instead.
                        fully_qualified_namespace:
                          type: string
                          description: >-
                            The fully qualified domain name of the Event Hubs
                            namespace, e.g.
                            {yournamespace}.servicebus.windows.net. Only
                            required for provided_onelake.
                        managed_resource_id:
                          type: string
                          description: >-
                            Unique identifier included in the name of the file
                            events managed resources.
                      description: >-
                        File event queue for OneLake (Microsoft Fabric)
                        locations. Events flow through Fabric
                            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
                            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
                    provided_pubsub:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        subscription_name:
                          type: string
                          description: >-
                            The Pub/Sub subscription name in the format
                            projects/{project}/subscriptions/{subscription
                            name}. Only required for provided_pubsub.
                    provided_sqs:
                      type: object
                      properties:
                        managed_resource_id:
                          type: string
                        queue_url:
                          type: string
                          description: >-
                            The AQS queue url in the format
                            https://sqs.{region}.amazonaws.com/{account
                            id}/{queue name}. Only required for provided_sqs.
                  description: >-
                    File event queue settings. If ``enable_file_events`` is not
                    ``false``, must be defined and have exactly one of the
                    documented properties.
                force:
                  type: boolean
                  description: >-
                    Force update even if changing url invalidates dependent
                    external tables or mounts.
                isolation_mode:
                  type: string
                  x-enum:
                    - ISOLATION_MODE_ISOLATED
                    - ISOLATION_MODE_OPEN
                    - ISOLATION_MODE_OPEN_IN_ACCOUNT
                  description: >-
                    Create a collection of name/value pairs.


                    Example enumeration:


                    >>> class Color(Enum):

                    ...     RED = 1

                    ...     BLUE = 2

                    ...     GREEN = 3


                    Access them by:


                    - attribute access:

                      >>> Color.RED
                      <Color.RED: 1>

                    - value lookup:

                      >>> Color(1)
                      <Color.RED: 1>

                    - name lookup:

                      >>> Color['RED']
                      <Color.RED: 1>

                    Enumerations can be iterated over, and know how many members
                    they have:


                    >>> len(Color)

                    3


                    >>> list(Color)

                    [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                    Methods can be added to enumerations, and members can have
                    their own

                    attributes -- see the documentation for details.
                new_name:
                  type: string
                  description: New name for the external location.
                owner:
                  type: string
                  description: The owner of the external location.
                read_only:
                  type: boolean
                  description: Indicates whether the external location is read-only.
                skip_validation:
                  type: boolean
                  description: >-
                    Skips validation of the storage credential associated with
                    the external location.
                url:
                  type: string
                  description: Path URL of the external location.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalLocationInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalLocationsAPI
  /api/2.0/lineage-tracking/external-metadata:
    post:
      operationId: external_metadata_create_external_metadata
      summary: >-
        Creates a new external metadata object in the parent metastore if the
        caller is a metastore admin or
      tags:
        - catalog
        - external_metadata
      description: >-
        Creates a new external metadata object in the parent metastore if the
        caller is a metastore admin or

        has the **CREATE_EXTERNAL_METADATA** privilege. Grants **BROWSE** to all
        account users upon creation

        by default.


        :param external_metadata: :class:`ExternalMetadata`


        :returns: :class:`ExternalMetadata`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                external_metadata:
                  type: object
                  properties:
                    name:
                      type: string
                    system_type:
                      type: string
                      x-enum:
                        - AMAZON_REDSHIFT
                        - AZURE_SYNAPSE
                        - CONFLUENT
                        - DATABRICKS
                        - GOOGLE_BIGQUERY
                        - KAFKA
                        - LOOKER
                        - MICROSOFT_FABRIC
                        - MICROSOFT_SQL_SERVER
                        - MONGODB
                        - MYSQL
                        - ORACLE
                        - OTHER
                        - POSTGRESQL
                        - POWER_BI
                        - SALESFORCE
                        - SAP
                        - SERVICENOW
                        - SNOWFLAKE
                        - STREAM_NATIVE
                        - TABLEAU
                        - TERADATA
                        - WORKDAY
                      description: Type of external system.
                    entity_type:
                      type: string
                      description: Type of entity within the external system.
                    columns:
                      type: array
                      items:
                        type: string
                      description: >-
                        List of columns associated with the external metadata
                        object.
                    create_time:
                      type: string
                      description: Time at which this external metadata object was created.
                    created_by:
                      type: string
                      description: Username of external metadata object creator.
                    description:
                      type: string
                      description: User-provided free-form text description.
                    id:
                      type: string
                      description: Unique identifier of the external metadata object.
                    metastore_id:
                      type: string
                      description: Unique identifier of parent metastore.
                    owner:
                      type: string
                      description: Owner of the external metadata object.
                    properties:
                      type: object
                      description: >-
                        A map of key-value properties attached to the external
                        metadata object.
                    update_time:
                      type: string
                      description: >-
                        Time at which this external metadata object was last
                        modified.
                    updated_by:
                      type: string
                      description: >-
                        Username of user who last modified external metadata
                        object.
                    url:
                      type: string
                      description: URL associated with the external metadata object.
                  required:
                    - name
                    - system_type
                    - entity_type
              required:
                - external_metadata
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalMetadata'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalMetadataAPI
    get:
      operationId: external_metadata_list_external_metadata
      summary: >-
        Gets an array of external metadata objects in the metastore. If the
        caller is the metastore admin, all
      tags:
        - catalog
        - external_metadata
      description: >-
        Gets an array of external metadata objects in the metastore. If the
        caller is the metastore admin, all

        external metadata objects will be retrieved. Otherwise, only external
        metadata objects that the caller

        has **BROWSE** on will be retrieved. There is no guarantee of a specific
        ordering of the elements in

        the array.


        :param page_size: int (optional)
          Specifies the maximum number of external metadata objects to return in a single response. The value
          must be less than or equal to 1000.
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`ExternalMetadata`
      parameters:
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Specifies the maximum number of external metadata objects to return
            in a single response. The value must be less than or equal to 1000.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListExternalMetadataResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalMetadataAPI
  /api/2.0/lineage-tracking/external-metadata/{name}:
    delete:
      operationId: external_metadata_delete_external_metadata
      summary: >-
        Deletes the external metadata object that matches the supplied name. The
        caller must be a metastore
      tags:
        - catalog
        - external_metadata
      description: >-
        Deletes the external metadata object that matches the supplied name. The
        caller must be a metastore

        admin, the owner of the external metadata object, or a user that has the
        **MANAGE** privilege.


        :param name: str
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalMetadataAPI
    get:
      operationId: external_metadata_get_external_metadata
      summary: >-
        Gets the specified external metadata object in a metastore. The caller
        must be a metastore admin, the
      tags:
        - catalog
        - external_metadata
      description: >-
        Gets the specified external metadata object in a metastore. The caller
        must be a metastore admin, the

        owner of the external metadata object, or a user that has the **BROWSE**
        privilege.


        :param name: str


        :returns: :class:`ExternalMetadata`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalMetadata'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalMetadataAPI
    patch:
      operationId: external_metadata_update_external_metadata
      summary: >-
        Updates the external metadata object that matches the supplied name. The
        caller can only update either
      tags:
        - catalog
        - external_metadata
      description: >-
        Updates the external metadata object that matches the supplied name. The
        caller can only update either

        the owner or other metadata fields in one request. The caller must be a
        metastore admin, the owner of

        the external metadata object, or a user that has the **MODIFY**
        privilege. If the caller is updating

        the owner, they must also have the **MANAGE** privilege.


        :param name: str
          Name of the external metadata object.
        :param external_metadata: :class:`ExternalMetadata`

        :param update_mask: str
          The field mask must be a single string, with multiple fields separated by commas (no spaces). The
          field path is relative to the resource object, using a dot (``.``) to navigate sub-fields (e.g.,
          ``author.given_name``). Specification of elements in sequence or map fields is not allowed, as only
          the entire collection field can be specified. Field names must exactly match the resource field
          names.

          A field mask of ``*`` indicates full replacement. It’s recommended to always explicitly list the
          fields being updated and avoid using ``*`` wildcards, as it can lead to unintended results if the
          API changes in the future.

        :returns: :class:`ExternalMetadata`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the external metadata object.
        - name: update_mask
          in: query
          required: true
          schema:
            type: string
          description: >-
            The field mask must be a single string, with multiple fields
            separated by commas (no spaces). The field path is relative to the
            resource object, using a dot (``.``) to navigate sub-fields (e.g.,
            ``author.given_name``). Specification of elements in sequence or map
            fields is not allowed, as only the entire collection field can be
            specified. Field names must exactly match the resource field names.
            A field mask of ``*`` indicates full replacement. It’s recommended
            to always explicitly list the fields being updated and avoid using
            ``*`` wildcards, as it can lead to unintended results if the API
            changes in the future.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                external_metadata:
                  type: object
                  properties:
                    name:
                      type: string
                    system_type:
                      type: string
                      x-enum:
                        - AMAZON_REDSHIFT
                        - AZURE_SYNAPSE
                        - CONFLUENT
                        - DATABRICKS
                        - GOOGLE_BIGQUERY
                        - KAFKA
                        - LOOKER
                        - MICROSOFT_FABRIC
                        - MICROSOFT_SQL_SERVER
                        - MONGODB
                        - MYSQL
                        - ORACLE
                        - OTHER
                        - POSTGRESQL
                        - POWER_BI
                        - SALESFORCE
                        - SAP
                        - SERVICENOW
                        - SNOWFLAKE
                        - STREAM_NATIVE
                        - TABLEAU
                        - TERADATA
                        - WORKDAY
                      description: Type of external system.
                    entity_type:
                      type: string
                      description: Type of entity within the external system.
                    columns:
                      type: array
                      items:
                        type: string
                      description: >-
                        List of columns associated with the external metadata
                        object.
                    create_time:
                      type: string
                      description: Time at which this external metadata object was created.
                    created_by:
                      type: string
                      description: Username of external metadata object creator.
                    description:
                      type: string
                      description: User-provided free-form text description.
                    id:
                      type: string
                      description: Unique identifier of the external metadata object.
                    metastore_id:
                      type: string
                      description: Unique identifier of parent metastore.
                    owner:
                      type: string
                      description: Owner of the external metadata object.
                    properties:
                      type: object
                      description: >-
                        A map of key-value properties attached to the external
                        metadata object.
                    update_time:
                      type: string
                      description: >-
                        Time at which this external metadata object was last
                        modified.
                    updated_by:
                      type: string
                      description: >-
                        Username of user who last modified external metadata
                        object.
                    url:
                      type: string
                      description: URL associated with the external metadata object.
                  required:
                    - name
                    - system_type
                    - entity_type
              required:
                - external_metadata
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalMetadata'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ExternalMetadataAPI
  /api/2.1/unity-catalog/functions:
    post:
      operationId: functions_create
      summary: '**WARNING: This API is experimental and will change in future versions**'
      tags:
        - catalog
        - functions
      description: >-
        **WARNING: This API is experimental and will change in future versions**


        Creates a new function


        The user must have the following permissions in order for the function
        to be created:


        - **USE_CATALOG** on the function's parent catalog

        - **USE_SCHEMA** and **CREATE_FUNCTION** on the function's parent schema


        :param function_info: :class:`CreateFunction`
          Partial **FunctionInfo** specifying the function to be created.

        :returns: :class:`FunctionInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                function_info:
                  type: object
                  properties:
                    name:
                      type: string
                    catalog_name:
                      type: string
                      description: Name of parent Catalog.
                    schema_name:
                      type: string
                      description: Name of parent Schema relative to its parent Catalog.
                    input_params:
                      type: object
                      properties:
                        parameters:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                              type_text:
                                type: string
                                description: Full data type spec, SQL/catalogString text.
                              type_name:
                                type: string
                                x-enum:
                                  - ARRAY
                                  - BINARY
                                  - BOOLEAN
                                  - BYTE
                                  - CHAR
                                  - DATE
                                  - DECIMAL
                                  - DOUBLE
                                  - FILE
                                  - FLOAT
                                  - GEOGRAPHY
                                  - GEOMETRY
                                  - INT
                                  - INTERVAL
                                  - LONG
                                  - MAP
                                  - 'NULL'
                                  - SHORT
                                  - STRING
                                  - STRUCT
                                  - TABLEREF_TYPE
                                  - TABLE_TYPE
                                  - TIME
                                  - TIMESTAMP
                                  - TIMESTAMP_NTZ
                                  - USER_DEFINED_TYPE
                                  - VARIANT
                                description: Name of type (INT, STRUCT, MAP, etc.)
                              position:
                                type: integer
                                description: >-
                                  Ordinal position of column (starting at
                                  position 0).
                              comment:
                                type: string
                                description: User-provided free-form text description.
                              parameter_default:
                                type: string
                                description: Default value of the parameter.
                              parameter_mode:
                                type: string
                                x-enum:
                                  - IN
                                description: Function parameter mode.
                              parameter_type:
                                type: string
                                x-enum:
                                  - COLUMN
                                  - PARAM
                                description: Function parameter type.
                              type_interval_type:
                                type: string
                                description: Format of IntervalType.
                              type_json:
                                type: string
                                description: Full data type spec, JSON-serialized.
                              type_precision:
                                type: integer
                                description: >-
                                  Digits of precision; required on Create for
                                  DecimalTypes.
                              type_scale:
                                type: integer
                                description: >-
                                  Digits to right of decimal; Required on Create
                                  for DecimalTypes.
                            required:
                              - name
                              - type_text
                              - type_name
                              - position
                      description: Function input parameters.
                    data_type:
                      type: string
                      x-enum:
                        - ARRAY
                        - BINARY
                        - BOOLEAN
                        - BYTE
                        - CHAR
                        - DATE
                        - DECIMAL
                        - DOUBLE
                        - FILE
                        - FLOAT
                        - GEOGRAPHY
                        - GEOMETRY
                        - INT
                        - INTERVAL
                        - LONG
                        - MAP
                        - 'NULL'
                        - SHORT
                        - STRING
                        - STRUCT
                        - TABLEREF_TYPE
                        - TABLE_TYPE
                        - TIME
                        - TIMESTAMP
                        - TIMESTAMP_NTZ
                        - USER_DEFINED_TYPE
                        - VARIANT
                      description: Scalar function return data type.
                    full_data_type:
                      type: string
                      description: Pretty printed function data type.
                    routine_body:
                      type: string
                      x-enum:
                        - EXTERNAL
                        - SQL
                      description: >-
                        Function language. When **EXTERNAL** is used, the
                        language of the routine function should be specified in
                        the **external_language** field, and the
                        **return_params** of the function cannot be used (as
                        **TABLE** return type is not supported), and the
                        **sql_data_access** field must be **NO_SQL**.
                    routine_definition:
                      type: string
                      description: Function body.
                    parameter_style:
                      type: string
                      x-enum:
                        - S
                      description: Function parameter style. **S** is the value for SQL.
                    is_deterministic:
                      type: boolean
                      description: Whether the function is deterministic.
                    sql_data_access:
                      type: string
                      x-enum:
                        - CONTAINS_SQL
                        - NO_SQL
                        - READS_SQL_DATA
                      description: Function SQL data access.
                    is_null_call:
                      type: boolean
                      description: Function null call.
                    security_type:
                      type: string
                      x-enum:
                        - DEFINER
                      description: Function security type.
                    specific_name:
                      type: string
                      description: Specific name of the function; Reserved for future use.
                    comment:
                      type: string
                      description: User-provided free-form text description.
                    external_language:
                      type: string
                      description: External function language.
                    external_name:
                      type: string
                      description: External function name.
                    properties:
                      type: string
                      description: >-
                        JSON-serialized key-value pair map, encoded (escaped) as
                        a string.
                    return_params:
                      type: object
                      properties:
                        parameters:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                              type_text:
                                type: string
                                description: Full data type spec, SQL/catalogString text.
                              type_name:
                                type: string
                                x-enum:
                                  - ARRAY
                                  - BINARY
                                  - BOOLEAN
                                  - BYTE
                                  - CHAR
                                  - DATE
                                  - DECIMAL
                                  - DOUBLE
                                  - FILE
                                  - FLOAT
                                  - GEOGRAPHY
                                  - GEOMETRY
                                  - INT
                                  - INTERVAL
                                  - LONG
                                  - MAP
                                  - 'NULL'
                                  - SHORT
                                  - STRING
                                  - STRUCT
                                  - TABLEREF_TYPE
                                  - TABLE_TYPE
                                  - TIME
                                  - TIMESTAMP
                                  - TIMESTAMP_NTZ
                                  - USER_DEFINED_TYPE
                                  - VARIANT
                                description: Name of type (INT, STRUCT, MAP, etc.)
                              position:
                                type: integer
                                description: >-
                                  Ordinal position of column (starting at
                                  position 0).
                              comment:
                                type: string
                                description: User-provided free-form text description.
                              parameter_default:
                                type: string
                                description: Default value of the parameter.
                              parameter_mode:
                                type: string
                                x-enum:
                                  - IN
                                description: Function parameter mode.
                              parameter_type:
                                type: string
                                x-enum:
                                  - COLUMN
                                  - PARAM
                                description: Function parameter type.
                              type_interval_type:
                                type: string
                                description: Format of IntervalType.
                              type_json:
                                type: string
                                description: Full data type spec, JSON-serialized.
                              type_precision:
                                type: integer
                                description: >-
                                  Digits of precision; required on Create for
                                  DecimalTypes.
                              type_scale:
                                type: integer
                                description: >-
                                  Digits to right of decimal; Required on Create
                                  for DecimalTypes.
                            required:
                              - name
                              - type_text
                              - type_name
                              - position
                      description: Table function return parameters.
                    routine_dependencies:
                      type: object
                      properties:
                        dependencies:
                          type: array
                          items:
                            type: object
                            properties:
                              connection:
                                type: object
                                properties:
                                  connection_name:
                                    type: string
                                    description: >-
                                      Full name of the dependent connection, in
                                      the form of **connection_name**.
                                description: >-
                                  A connection that is dependent on a SQL
                                  object.
                              credential:
                                type: object
                                properties:
                                  credential_name:
                                    type: string
                                    description: >-
                                      Full name of the dependent credential, in
                                      the form of **credential_name**.
                                description: >-
                                  A credential that is dependent on a SQL
                                  object.
                              function:
                                type: object
                                properties:
                                  function_full_name:
                                    type: string
                                    description: >-
                                      Full name of the dependent function, in
                                      the form of
                                      **catalog_name**.**schema_name**.**function_name**.
                                required:
                                  - function_full_name
                                description: A function that is dependent on a SQL object.
                              metastore_name:
                                type: string
                                description: >-
                                  Metastore context for the dependency. Global
                                  UC server responses currently populate this
                                  with the metastore UUID string so clients can
                                  disambiguate dependencies with identical names
                                  across metastores.
                              secret:
                                type: object
                                properties:
                                  secret_full_name:
                                    type: string
                                    description: >-
                                      Full name of the dependent secret, in the
                                      form of
                                      **catalog_name**.**schema_name**.**secret_name**.
                                required:
                                  - secret_full_name
                                description: A dependency on a Unity Catalog secret.
                              table:
                                type: object
                                properties:
                                  table_full_name:
                                    type: string
                                    description: >-
                                      Full name of the dependent table, in the
                                      form of
                                      **catalog_name**.**schema_name**.**table_name**.
                                required:
                                  - table_full_name
                                description: A table that is dependent on a SQL object.
                              volume:
                                type: object
                                properties:
                                  volume_full_name:
                                    type: string
                                    description: >-
                                      Full name of the dependent volume, in the
                                      form of
                                      **catalog_name**.**schema_name**.**volume_name**.
                                required:
                                  - volume_full_name
                                description: A dependency on a Unity Catalog volume.
                            description: >-
                              A dependency of a SQL object. One of the following
                              fields must be defined: **table**,
                                  **function**, **connection**, **credential**, **volume**, or **secret**.
                          description: Array of dependencies.
                      description: function dependencies.
                    sql_path:
                      type: string
                      description: >-
                        List of schemes whose objects can be referenced without
                        qualification.
                  required:
                    - name
                    - catalog_name
                    - schema_name
                    - input_params
                    - data_type
                    - full_data_type
                    - routine_body
                    - routine_definition
                    - parameter_style
                    - is_deterministic
                    - sql_data_access
                    - is_null_call
                    - security_type
                    - specific_name
                  description: >-
                    Partial **FunctionInfo** specifying the function to be
                    created.
              required:
                - function_info
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FunctionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: FunctionsAPI
    get:
      operationId: functions_list
      summary: >-
        List functions within the specified parent catalog and schema. If the
        user is a metastore admin, all
      tags:
        - catalog
        - functions
      description: >-
        List functions within the specified parent catalog and schema. If the
        user is a metastore admin, all

        functions are returned in the output list. Otherwise, the user must have
        the **USE_CATALOG** privilege

        on the catalog and the **USE_SCHEMA** privilege on the schema, and the
        output list contains only

        functions for which either the user has the **EXECUTE** privilege or the
        user is the owner. There is

        no guarantee of a specific ordering of the elements in the array.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param catalog_name: str
          Name of parent catalog for functions of interest.
        :param schema_name: str
          Parent schema of functions.
        :param include_browse: bool (optional)
          Whether to include functions in the response for which the principal can only access selective
          metadata for
        :param max_results: int (optional)
          Maximum number of functions to return. If not set, all the functions are returned (not recommended).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`FunctionInfo`
      parameters:
        - name: catalog_name
          in: query
          required: true
          schema:
            type: string
          description: Name of parent catalog for functions of interest.
        - name: schema_name
          in: query
          required: true
          schema:
            type: string
          description: Parent schema of functions.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include functions in the response for which the principal
            can only access selective metadata for
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of functions to return. If not set, all the functions
            are returned (not recommended). - when set to a value greater than
            0, the page length is the minimum of this value and a server
            configured value; - when set to 0, the page length is set to a
            server configured value (recommended); - when set to a value less
            than 0, an invalid parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListFunctionsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: FunctionsAPI
  /api/2.1/unity-catalog/functions/{name}:
    delete:
      operationId: functions_delete
      summary: >-
        Deletes the function that matches the supplied name. For the deletion to
        succeed, the user must
      tags:
        - catalog
        - functions
      description: >-
        Deletes the function that matches the supplied name. For the deletion to
        succeed, the user must

        satisfy one of the following conditions:


        - Is the owner of the function's parent catalog

        - Is the owner of the function's parent schema and have the
        **USE_CATALOG** privilege on its parent
          catalog
        - Is the owner of the function itself and have both the **USE_CATALOG**
        privilege on its parent
          catalog and the **USE_SCHEMA** privilege on its parent schema

        :param name: str
          The fully-qualified name of the function (of the form
          **catalog_name**.**schema_name**.**function__name**) .
        :param force: bool (optional)
          Force deletion even if the function is notempty.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: >-
            The fully-qualified name of the function (of the form
            **catalog_name**.**schema_name**.**function__name**) .
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Force deletion even if the function is notempty.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: FunctionsAPI
    get:
      operationId: functions_get
      summary: >-
        Gets a function from within a parent catalog and schema. For the fetch
        to succeed, the user must
      tags:
        - catalog
        - functions
      description: >-
        Gets a function from within a parent catalog and schema. For the fetch
        to succeed, the user must

        satisfy one of the following requirements:


        - Is a metastore admin

        - Is an owner of the function's parent catalog

        - Have the **USE_CATALOG** privilege on the function's parent catalog
        and be the owner of the function

        - Have the **USE_CATALOG** privilege on the function's parent catalog,
        the **USE_SCHEMA** privilege on
          the function's parent schema, and the **EXECUTE** privilege on the function itself

        :param name: str
          The fully-qualified name of the function (of the form
          **catalog_name**.**schema_name**.**function__name**).
        :param include_browse: bool (optional)
          Whether to include functions in the response for which the principal can only access selective
          metadata for

        :returns: :class:`FunctionInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: >-
            The fully-qualified name of the function (of the form
            **catalog_name**.**schema_name**.**function__name**).
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include functions in the response for which the principal
            can only access selective metadata for
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FunctionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: FunctionsAPI
    patch:
      operationId: functions_update
      summary: >-
        Updates the function that matches the supplied name. Only the owner of
        the function can be updated. If
      tags:
        - catalog
        - functions
      description: >-
        Updates the function that matches the supplied name. Only the owner of
        the function can be updated. If

        the user is not a metastore admin, the user must be a member of the
        group that is the new function

        owner.


        - Is a metastore admin

        - Is the owner of the function's parent catalog

        - Is the owner of the function's parent schema and has the
        **USE_CATALOG** privilege on its parent
          catalog
        - Is the owner of the function itself and has the **USE_CATALOG**
        privilege on its parent catalog as
          well as the **USE_SCHEMA** privilege on the function's parent schema.

        :param name: str
          The fully-qualified name of the function (of the form
          **catalog_name**.**schema_name**.**function__name**).
        :param owner: str (optional)
          Username of current owner of the function.

        :returns: :class:`FunctionInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: >-
            The fully-qualified name of the function (of the form
            **catalog_name**.**schema_name**.**function__name**).
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  description: Username of current owner of the function.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FunctionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: FunctionsAPI
  /api/2.1/unity-catalog/permissions/{securable_type}/{full_name}:
    get:
      operationId: grants_get
      summary: >-
        Gets the permissions for a securable. Does not include inherited
        permissions.
      tags:
        - catalog
        - grants
      description: >-
        Gets the permissions for a securable. Does not include inherited
        permissions.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param securable_type: str
          Type of securable.
        :param full_name: str
          Full name of securable.
        :param include_deleted_principals: bool (optional)
          Optional. If true, also return privilege assignments whose principals have been deleted.
        :param max_results: int (optional)
          Specifies the maximum number of privileges to return (page length). Every PrivilegeAssignment
          present in a single page response is guaranteed to contain all the privileges granted on the
          requested Securable for the respective principal.

          If not set, all the permissions are returned. If set to

          - lesser than 0: invalid parameter error
          - 0: page length is set to a server configured value
          - lesser than 150 but greater than 0: invalid parameter error (this is to ensure that server is able
            to return at least one complete PrivilegeAssignment in a single page response)
          - greater than (or equal to) 150: page length is the minimum of this value and a server configured
            value
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.
        :param principal: str (optional)
          If provided, only the permissions for the specified principal (user or group) are returned.

        :returns: :class:`GetPermissionsResponse`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: Type of securable.
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of securable.
        - name: include_deleted_principals
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Optional. If true, also return privilege assignments whose
            principals have been deleted.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Specifies the maximum number of privileges to return (page length).
            Every PrivilegeAssignment present in a single page response is
            guaranteed to contain all the privileges granted on the requested
            Securable for the respective principal. If not set, all the
            permissions are returned. If set to - lesser than 0: invalid
            parameter error - 0: page length is set to a server configured value
            - lesser than 150 but greater than 0: invalid parameter error (this
            is to ensure that server is able to return at least one complete
            PrivilegeAssignment in a single page response) - greater than (or
            equal to) 150: page length is the minimum of this value and a server
            configured value
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
        - name: principal
          in: query
          required: false
          schema:
            type: string
          description: >-
            If provided, only the permissions for the specified principal (user
            or group) are returned.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPermissionsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GrantsAPI
    patch:
      operationId: grants_update
      summary: Updates the permissions for a securable.
      tags:
        - catalog
        - grants
      description: |-
        Updates the permissions for a securable.

        :param securable_type: str
          Type of securable.
        :param full_name: str
          Full name of securable.
        :param changes: List[:class:`PermissionsChange`] (optional)
          Array of permissions change objects.
        :param omit_permissions_in_response: bool (optional)
          Optional, default false. Specifies whether all the permissions should be returned in the response.

        :returns: :class:`UpdatePermissionsResponse`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: Type of securable.
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of securable.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                changes:
                  type: array
                  items:
                    type: object
                    properties:
                      add:
                        type: array
                        items:
                          type: string
                          x-enum:
                            - ACCESS
                            - ALL_PRIVILEGES
                            - APPLY_TAG
                            - BROWSE
                            - CREATE
                            - CREATE_CASE_COLLECTION
                            - CREATE_CATALOG
                            - CREATE_CLEAN_ROOM
                            - CREATE_CONNECTION
                            - CREATE_DATASOURCE
                            - CREATE_EXTERNAL_LOCATION
                            - CREATE_EXTERNAL_TABLE
                            - CREATE_EXTERNAL_VOLUME
                            - CREATE_FEATURE
                            - CREATE_FLOW
                            - CREATE_FOREIGN_CATALOG
                            - CREATE_FOREIGN_SECURABLE
                            - CREATE_FUNCTION
                            - CREATE_MANAGED_STORAGE
                            - CREATE_MATERIALIZED_VIEW
                            - CREATE_MEMORY_STORE
                            - CREATE_MODEL
                            - CREATE_PROVIDER
                            - CREATE_RECIPIENT
                            - CREATE_RULE
                            - CREATE_SCHEMA
                            - CREATE_SEMANTIC_GRAPH
                            - CREATE_SERVICE
                            - CREATE_SERVICE_CREDENTIAL
                            - CREATE_SHARE
                            - CREATE_SKILL
                            - CREATE_STORAGE_CREDENTIAL
                            - CREATE_STREAM
                            - CREATE_TABLE
                            - CREATE_VIEW
                            - CREATE_VOLUME
                            - DELETE
                            - DELETE_EVENTS
                            - DELETE_SECURITY_DATA
                            - EXECUTE
                            - EXECUTE_CLEAN_ROOM_TASK
                            - EXTERNAL_USE_SCHEMA
                            - INSERT
                            - INSERT_SECURITY_DATA
                            - MANAGE
                            - MANAGE_ACCESS
                            - MANAGE_ACCESS_CONTROL
                            - MANAGE_ALLOWLIST
                            - MANAGE_GRANTS
                            - MODIFY
                            - MODIFY_CLEAN_ROOM
                            - READ_EVENTS
                            - READ_FEATURE
                            - READ_FILES
                            - READ_FLOW
                            - READ_MEMORY_STORE
                            - READ_METADATA
                            - READ_PRIVATE_FILES
                            - READ_SECURITY_DATA
                            - READ_SEMANTIC_GRAPH
                            - READ_SKILL
                            - READ_STREAM
                            - READ_VOLUME
                            - REFRESH
                            - SELECT
                            - SET_SHARE_PERMISSION
                            - UPDATE
                            - UPDATE_EVENTS
                            - UPDATE_SECURITY_DATA
                            - USAGE
                            - USE_CATALOG
                            - USE_CONNECTION
                            - USE_MARKETPLACE_ASSETS
                            - USE_PROVIDER
                            - USE_RECIPIENT
                            - USE_SCHEMA
                            - USE_SHARE
                            - USE_VOLUME
                            - VIEW_ADMIN_METADATA
                            - VIEW_METADATA
                            - VIEW_OBJECT
                            - WRITE_FILES
                            - WRITE_FLOW
                            - WRITE_MEMORY_STORE
                            - WRITE_PRIVATE_FILES
                            - WRITE_SEMANTIC_GRAPH
                            - WRITE_SKILL
                            - WRITE_VOLUME
                          description: >-
                            Create a collection of name/value pairs.


                            Example enumeration:


                            >>> class Color(Enum):

                            ...     RED = 1

                            ...     BLUE = 2

                            ...     GREEN = 3


                            Access them by:


                            - attribute access:

                              >>> Color.RED
                              <Color.RED: 1>

                            - value lookup:

                              >>> Color(1)
                              <Color.RED: 1>

                            - name lookup:

                              >>> Color['RED']
                              <Color.RED: 1>

                            Enumerations can be iterated over, and know how many
                            members they have:


                            >>> len(Color)

                            3


                            >>> list(Color)

                            [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                            Methods can be added to enumerations, and members
                            can have their own

                            attributes -- see the documentation for details.
                      principal:
                        type: string
                        description: >-
                          The principal whose privileges we are changing. Only
                          one of principal or principal_id should be specified,
                          never both at the same time.
                      principal_id:
                        type: integer
                        description: >-
                          An opaque internal ID that identifies the principal
                          whose privileges should be removed. This field is
                          intended for removing privileges associated with a
                          deleted user. When set, only the entries specified in
                          the remove field are processed; any entries in the add
                          field will be rejected. Only one of principal or
                          principal_id should be specified, never both at the
                          same time.
                      remove:
                        type: array
                        items:
                          type: string
                          x-enum:
                            - ACCESS
                            - ALL_PRIVILEGES
                            - APPLY_TAG
                            - BROWSE
                            - CREATE
                            - CREATE_CASE_COLLECTION
                            - CREATE_CATALOG
                            - CREATE_CLEAN_ROOM
                            - CREATE_CONNECTION
                            - CREATE_DATASOURCE
                            - CREATE_EXTERNAL_LOCATION
                            - CREATE_EXTERNAL_TABLE
                            - CREATE_EXTERNAL_VOLUME
                            - CREATE_FEATURE
                            - CREATE_FLOW
                            - CREATE_FOREIGN_CATALOG
                            - CREATE_FOREIGN_SECURABLE
                            - CREATE_FUNCTION
                            - CREATE_MANAGED_STORAGE
                            - CREATE_MATERIALIZED_VIEW
                            - CREATE_MEMORY_STORE
                            - CREATE_MODEL
                            - CREATE_PROVIDER
                            - CREATE_RECIPIENT
                            - CREATE_RULE
                            - CREATE_SCHEMA
                            - CREATE_SEMANTIC_GRAPH
                            - CREATE_SERVICE
                            - CREATE_SERVICE_CREDENTIAL
                            - CREATE_SHARE
                            - CREATE_SKILL
                            - CREATE_STORAGE_CREDENTIAL
                            - CREATE_STREAM
                            - CREATE_TABLE
                            - CREATE_VIEW
                            - CREATE_VOLUME
                            - DELETE
                            - DELETE_EVENTS
                            - DELETE_SECURITY_DATA
                            - EXECUTE
                            - EXECUTE_CLEAN_ROOM_TASK
                            - EXTERNAL_USE_SCHEMA
                            - INSERT
                            - INSERT_SECURITY_DATA
                            - MANAGE
                            - MANAGE_ACCESS
                            - MANAGE_ACCESS_CONTROL
                            - MANAGE_ALLOWLIST
                            - MANAGE_GRANTS
                            - MODIFY
                            - MODIFY_CLEAN_ROOM
                            - READ_EVENTS
                            - READ_FEATURE
                            - READ_FILES
                            - READ_FLOW
                            - READ_MEMORY_STORE
                            - READ_METADATA
                            - READ_PRIVATE_FILES
                            - READ_SECURITY_DATA
                            - READ_SEMANTIC_GRAPH
                            - READ_SKILL
                            - READ_STREAM
                            - READ_VOLUME
                            - REFRESH
                            - SELECT
                            - SET_SHARE_PERMISSION
                            - UPDATE
                            - UPDATE_EVENTS
                            - UPDATE_SECURITY_DATA
                            - USAGE
                            - USE_CATALOG
                            - USE_CONNECTION
                            - USE_MARKETPLACE_ASSETS
                            - USE_PROVIDER
                            - USE_RECIPIENT
                            - USE_SCHEMA
                            - USE_SHARE
                            - USE_VOLUME
                            - VIEW_ADMIN_METADATA
                            - VIEW_METADATA
                            - VIEW_OBJECT
                            - WRITE_FILES
                            - WRITE_FLOW
                            - WRITE_MEMORY_STORE
                            - WRITE_PRIVATE_FILES
                            - WRITE_SEMANTIC_GRAPH
                            - WRITE_SKILL
                            - WRITE_VOLUME
                          description: >-
                            Create a collection of name/value pairs.


                            Example enumeration:


                            >>> class Color(Enum):

                            ...     RED = 1

                            ...     BLUE = 2

                            ...     GREEN = 3


                            Access them by:


                            - attribute access:

                              >>> Color.RED
                              <Color.RED: 1>

                            - value lookup:

                              >>> Color(1)
                              <Color.RED: 1>

                            - name lookup:

                              >>> Color['RED']
                              <Color.RED: 1>

                            Enumerations can be iterated over, and know how many
                            members they have:


                            >>> len(Color)

                            3


                            >>> list(Color)

                            [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                            Methods can be added to enumerations, and members
                            can have their own

                            attributes -- see the documentation for details.
                        description: The set of privileges to remove.
                  description: Array of permissions change objects.
                omit_permissions_in_response:
                  type: boolean
                  description: >-
                    Optional, default false. Specifies whether all the
                    permissions should be returned in the response.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdatePermissionsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GrantsAPI
  /api/2.1/unity-catalog/effective-permissions/{securable_type}/{full_name}:
    get:
      operationId: grants_get_effective
      summary: >-
        Gets the effective permissions for a securable. Includes inherited
        permissions from any parent
      tags:
        - catalog
        - grants
      description: >-
        Gets the effective permissions for a securable. Includes inherited
        permissions from any parent

        securables.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param securable_type: str
          Type of securable.
        :param full_name: str
          Full name of securable.
        :param max_results: int (optional)
          Specifies the maximum number of privileges to return (page length). Every
          EffectivePrivilegeAssignment present in a single page response is guaranteed to contain all the
          effective privileges granted on (or inherited by) the requested Securable for the respective
          principal.

          If not set, all the effective permissions are returned. If set to

          - lesser than 0: invalid parameter error
          - 0: page length is set to a server configured value
          - lesser than 150 but greater than 0: invalid parameter error (this is to ensure that server is able
            to return at least one complete EffectivePrivilegeAssignment in a single page response)
          - greater than (or equal to) 150: page length is the minimum of this value and a server configured
            value
        :param page_token: str (optional)
          Opaque token for the next page of results (pagination).
        :param principal: str (optional)
          If provided, only the effective permissions for the specified principal (user or group) are
          returned.

        :returns: :class:`EffectivePermissionsList`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: Type of securable.
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of securable.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Specifies the maximum number of privileges to return (page length).
            Every EffectivePrivilegeAssignment present in a single page response
            is guaranteed to contain all the effective privileges granted on (or
            inherited by) the requested Securable for the respective principal.
            If not set, all the effective permissions are returned. If set to -
            lesser than 0: invalid parameter error - 0: page length is set to a
            server configured value - lesser than 150 but greater than 0:
            invalid parameter error (this is to ensure that server is able to
            return at least one complete EffectivePrivilegeAssignment in a
            single page response) - greater than (or equal to) 150: page length
            is the minimum of this value and a server configured value
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque token for the next page of results (pagination).
        - name: principal
          in: query
          required: false
          schema:
            type: string
          description: >-
            If provided, only the effective permissions for the specified
            principal (user or group) are returned.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EffectivePermissionsList'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GrantsAPI
  /api/2.1/unity-catalog/privilege-assignments/{securable_type}/{full_name}:
    get:
      operationId: grants_list
      summary: >-
        Lists the privilege assignments for a securable. Does not include
        inherited privileges. Paginated
      tags:
        - catalog
        - grants
      description: >-
        Lists the privilege assignments for a securable. Does not include
        inherited privileges. Paginated

        version of Get Permissions API.


        :param securable_type: str
          Type of securable.
        :param full_name: str
          Full name of securable.
        :param include_deleted_principals: bool (optional)
          Optional. If true, also return privilege assignments whose principals have been deleted.
        :param page_size: int (optional)
          Specifies the maximum number of privilege assignments to return (page length). Every
          PrivilegeAssignment present in a single page response is guaranteed to contain all the privileges
          granted on the requested Securable for the respective principal.

          If not set, page length is the server configured value. If set to

          - lesser than 0: invalid parameter error
          - 0: page length is set to a server configured value
          - lesser than 150 but greater than 0: invalid parameter error (this is to ensure that server is able
            to return at least one complete PrivilegeAssignment in a single page response)
          - greater than (or equal to) 150: page length is the minimum of this value and a server configured
            value
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.
        :param principal: str (optional)
          If provided, only the permissions for the specified principal (user or group) are returned.

        :returns: Iterator over :class:`PrivilegeAssignment`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: Type of securable.
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of securable.
        - name: include_deleted_principals
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Optional. If true, also return privilege assignments whose
            principals have been deleted.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Specifies the maximum number of privilege assignments to return
            (page length). Every PrivilegeAssignment present in a single page
            response is guaranteed to contain all the privileges granted on the
            requested Securable for the respective principal. If not set, page
            length is the server configured value. If set to - lesser than 0:
            invalid parameter error - 0: page length is set to a server
            configured value - lesser than 150 but greater than 0: invalid
            parameter error (this is to ensure that server is able to return at
            least one complete PrivilegeAssignment in a single page response) -
            greater than (or equal to) 150: page length is the minimum of this
            value and a server configured value
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
        - name: principal
          in: query
          required: false
          schema:
            type: string
          description: >-
            If provided, only the permissions for the specified principal (user
            or group) are returned.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPermissionsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GrantsAPI
  /api/2.1/unity-catalog/effective-privilege-assignments/{securable_type}/{full_name}:
    get:
      operationId: grants_list_effective
      summary: >-
        Lists the effective privilege assignments for a securable. Includes
        inherited privileges. Paginated
      tags:
        - catalog
        - grants
      description: >-
        Lists the effective privilege assignments for a securable. Includes
        inherited privileges. Paginated

        version of Get Effective Permissions API.


        :param securable_type: str
          Type of securable.
        :param full_name: str
          Full name of securable.
        :param page_size: int (optional)
          Specifies the maximum number of privilege assignments to return (page length). Every
          EffectivePrivilegeAssignment present in a single page response is guaranteed to contain all the
          effective privileges granted on (or inherited by) the requested Securable for the respective
          principal.

          If not set, a server-configured default is used. If set to

          - lesser than 0: invalid parameter error
          - 0: page length is set to a server configured value
          - lesser than 150 but greater than 0: invalid parameter error (this is to ensure that server is able
            to return at least one complete EffectivePrivilegeAssignment in a single page response)
          - greater than (or equal to) 150: page length is the minimum of this value and a server configured
            value
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.
        :param principal: str (optional)
          If provided, only the effective permissions for the specified principal (user or group) are
          returned.

        :returns: Iterator over :class:`EffectivePrivilegeAssignment`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: Type of securable.
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of securable.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Specifies the maximum number of privilege assignments to return
            (page length). Every EffectivePrivilegeAssignment present in a
            single page response is guaranteed to contain all the effective
            privileges granted on (or inherited by) the requested Securable for
            the respective principal. If not set, a server-configured default is
            used. If set to - lesser than 0: invalid parameter error - 0: page
            length is set to a server configured value - lesser than 150 but
            greater than 0: invalid parameter error (this is to ensure that
            server is able to return at least one complete
            EffectivePrivilegeAssignment in a single page response) - greater
            than (or equal to) 150: page length is the minimum of this value and
            a server configured value
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
        - name: principal
          in: query
          required: false
          schema:
            type: string
          description: >-
            If provided, only the effective permissions for the specified
            principal (user or group) are returned.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EffectivePermissionsList'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GrantsAPI
  /api/2.1/unity-catalog/workspaces/{workspace_id}/metastore:
    put:
      operationId: metastores_assign
      summary: >-
        Creates a new metastore assignment. If an assignment for the same
        **workspace_id** exists, it will be
      tags:
        - catalog
        - metastores
      description: >-
        Creates a new metastore assignment. If an assignment for the same
        **workspace_id** exists, it will be

        overwritten by the new **metastore_id** and **default_catalog_name**.
        The caller must be an account

        admin.


        :param workspace_id: int
          A workspace ID.
        :param metastore_id: str
          The unique ID of the metastore.
        :param default_catalog_name: str
          The name of the default catalog in the metastore. This field is deprecated. Please use "Default
          Namespace API" to configure the default catalog for a Databricks workspace.
      parameters:
        - name: workspace_id
          in: path
          required: true
          schema:
            type: integer
          description: A workspace ID.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                metastore_id:
                  type: string
                  description: The unique ID of the metastore.
                default_catalog_name:
                  type: string
                  description: >-
                    The name of the default catalog in the metastore. This field
                    is deprecated. Please use "Default Namespace API" to
                    configure the default catalog for a Databricks workspace.
              required:
                - metastore_id
                - default_catalog_name
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
    delete:
      operationId: metastores_unassign
      summary: >-
        Deletes a metastore assignment. The caller must be an account
        administrator.
      tags:
        - catalog
        - metastores
      description: >-
        Deletes a metastore assignment. The caller must be an account
        administrator.


        :param workspace_id: int
          A workspace ID.
        :param metastore_id: str
          Query for the ID of the metastore to delete.
      parameters:
        - name: workspace_id
          in: path
          required: true
          schema:
            type: integer
          description: A workspace ID.
        - name: metastore_id
          in: query
          required: true
          schema:
            type: string
          description: Query for the ID of the metastore to delete.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
    patch:
      operationId: metastores_update_assignment
      summary: >-
        Updates a metastore assignment. This operation can be used to update
        **metastore_id** or
      tags:
        - catalog
        - metastores
      description: >-
        Updates a metastore assignment. This operation can be used to update
        **metastore_id** or

        **default_catalog_name** for a specified Workspace, if the Workspace is
        already assigned a metastore.

        The caller must be an account admin to update **metastore_id**;
        otherwise, the caller can be a

        Workspace admin.


        :param workspace_id: int
          A workspace ID.
        :param default_catalog_name: str (optional)
          The name of the default catalog in the metastore. This field is deprecated. Please use "Default
          Namespace API" to configure the default catalog for a Databricks workspace.
        :param metastore_id: str (optional)
          The unique ID of the metastore.
      parameters:
        - name: workspace_id
          in: path
          required: true
          schema:
            type: integer
          description: A workspace ID.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                default_catalog_name:
                  type: string
                  description: >-
                    The name of the default catalog in the metastore. This field
                    is deprecated. Please use "Default Namespace API" to
                    configure the default catalog for a Databricks workspace.
                metastore_id:
                  type: string
                  description: The unique ID of the metastore.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
  /api/2.1/unity-catalog/metastores:
    post:
      operationId: metastores_create
      summary: >-
        Creates a new metastore based on a provided name and optional storage
        root path. By default (if the
      tags:
        - catalog
        - metastores
      description: >-
        Creates a new metastore based on a provided name and optional storage
        root path. By default (if the

        **owner** field is not set), the owner of the new metastore is the user
        calling the

        **createMetastore** API. If the **owner** field is set to the empty
        string (**""**), the ownership is

        assigned to the System User instead.


        :param name: str
          The user-specified name of the metastore.
        :param external_access_enabled: bool (optional)
          Whether to allow non-DBR clients to directly access entities under the metastore.
        :param region: str (optional)
          Cloud region which the metastore serves (e.g., ``us-west-2``, ``westus``).
        :param storage_root: str (optional)
          The storage root URL for metastore

        :returns: :class:`MetastoreInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The user-specified name of the metastore.
                external_access_enabled:
                  type: boolean
                  description: >-
                    Whether to allow non-DBR clients to directly access entities
                    under the metastore.
                region:
                  type: string
                  description: >-
                    Cloud region which the metastore serves (e.g.,
                    ``us-west-2``, ``westus``).
                storage_root:
                  type: string
                  description: The storage root URL for metastore
              required:
                - name
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetastoreInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
    get:
      operationId: metastores_list
      summary: >-
        Gets an array of the available metastores (as **MetastoreInfo**
        objects). The caller must be an admin
      tags:
        - catalog
        - metastores
      description: >-
        Gets an array of the available metastores (as **MetastoreInfo**
        objects). The caller must be an admin

        to retrieve this info. There is no guarantee of a specific ordering of
        the elements in the array.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param max_results: int (optional)
          Maximum number of metastores to return.

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
          - If not set, all the metastores are returned (not recommended).
          - Note: The number of returned metastores might be less than the specified max_results size, even
            zero. The only definitive indication that no further metastores can be fetched is when the
            next_page_token is unset from the response.
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`MetastoreInfo`
      parameters:
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of metastores to return. - when set to a value
            greater than 0, the page length is the minimum of this value and a
            server configured value; - when set to 0, the page length is set to
            a server configured value (recommended); - when set to a value less
            than 0, an invalid parameter error is returned; - If not set, all
            the metastores are returned (not recommended). - Note: The number of
            returned metastores might be less than the specified max_results
            size, even zero. The only definitive indication that no further
            metastores can be fetched is when the next_page_token is unset from
            the response.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountsListMetastoresResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
  /api/2.1/unity-catalog/current-metastore-assignment:
    get:
      operationId: metastores_current
      summary: Gets the metastore assignment for the workspace being accessed.
      tags:
        - catalog
        - metastores
      description: |-
        Gets the metastore assignment for the workspace being accessed.


        :returns: :class:`MetastoreAssignment`
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetastoreAssignment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
  /api/2.1/unity-catalog/metastores/{id}:
    delete:
      operationId: metastores_delete
      summary: Deletes a metastore. The caller must be a metastore admin.
      tags:
        - catalog
        - metastores
      description: |-
        Deletes a metastore. The caller must be a metastore admin.

        :param id: str
          Unique ID of the metastore.
        :param force: bool (optional)
          Force deletion even if the metastore is not empty. Default is false.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID of the metastore.
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Force deletion even if the metastore is not empty. Default is false.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
    get:
      operationId: metastores_get
      summary: >-
        Gets a metastore that matches the supplied ID. The caller must be a
        metastore admin to retrieve this
      tags:
        - catalog
        - metastores
      description: >-
        Gets a metastore that matches the supplied ID. The caller must be a
        metastore admin to retrieve this

        info.


        :param id: str
          Unique ID of the metastore.

        :returns: :class:`MetastoreInfo`
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID of the metastore.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetastoreInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
    patch:
      operationId: metastores_update
      summary: >-
        Updates information for a specific metastore. The caller must be a
        metastore admin. If the **owner**
      tags:
        - catalog
        - metastores
      description: >-
        Updates information for a specific metastore. The caller must be a
        metastore admin. If the **owner**

        field is set to the empty string (**""**), the ownership is updated to
        the System User.


        :param id: str
          Unique ID of the metastore.
        :param delta_sharing_organization_name: str (optional)
          The organization name of a Delta Sharing entity, to be used in Databricks-to-Databricks Delta
          Sharing as the official name.
        :param delta_sharing_recipient_token_lifetime_in_seconds: int (optional)
          The lifetime of delta sharing recipient token in seconds.
        :param delta_sharing_scope: :class:`DeltaSharingScopeEnum` (optional)
          The scope of Delta Sharing enabled for the metastore.
        :param external_access_enabled: bool (optional)
          Whether to allow non-DBR clients to directly access entities under the metastore.
        :param new_name: str (optional)
          New name for the metastore.
        :param owner: str (optional)
          The owner of the metastore.
        :param privilege_model_version: str (optional)
          Privilege model version of the metastore, of the form ``major.minor`` (e.g., ``1.0``).
        :param storage_root_credential_id: str (optional)
          UUID of storage credential to access the metastore storage_root.

        :returns: :class:`MetastoreInfo`
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID of the metastore.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                delta_sharing_organization_name:
                  type: string
                  description: >-
                    The organization name of a Delta Sharing entity, to be used
                    in Databricks-to-Databricks Delta Sharing as the official
                    name.
                delta_sharing_recipient_token_lifetime_in_seconds:
                  type: integer
                  description: The lifetime of delta sharing recipient token in seconds.
                delta_sharing_scope:
                  type: string
                  x-enum:
                    - INTERNAL
                    - INTERNAL_AND_EXTERNAL
                  description: The scope of Delta Sharing enabled for the metastore.
                external_access_enabled:
                  type: boolean
                  description: >-
                    Whether to allow non-DBR clients to directly access entities
                    under the metastore.
                new_name:
                  type: string
                  description: New name for the metastore.
                owner:
                  type: string
                  description: The owner of the metastore.
                privilege_model_version:
                  type: string
                  description: >-
                    Privilege model version of the metastore, of the form
                    ``major.minor`` (e.g., ``1.0``).
                storage_root_credential_id:
                  type: string
                  description: >-
                    UUID of storage credential to access the metastore
                    storage_root.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetastoreInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
  /api/2.1/unity-catalog/metastore_summary:
    get:
      operationId: metastores_summary
      summary: >-
        Gets information about a metastore. This summary includes the storage
        credential, the cloud vendor,
      tags:
        - catalog
        - metastores
      description: >-
        Gets information about a metastore. This summary includes the storage
        credential, the cloud vendor,

        the cloud region, and the global metastore ID.



        :returns: :class:`GetMetastoreSummaryResponse`
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetMetastoreSummaryResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: MetastoresAPI
  /api/2.1/unity-catalog/models/{full_name}/versions/{version}:
    delete:
      operationId: model_versions_delete
      summary: >-
        Deletes a model version from the specified registered model. Any aliases
        assigned to the model version
      tags:
        - catalog
        - model_versions
      description: >-
        Deletes a model version from the specified registered model. Any aliases
        assigned to the model version

        will also be deleted.


        The caller must be a metastore admin or an owner of the parent
        registered model. For the latter case,

        the caller must also be the owner or have the **USE_CATALOG** privilege
        on the parent catalog and the

        **USE_SCHEMA** privilege on the parent schema.


        :param full_name: str
          The three-level (fully qualified) name of the model version
        :param version: int
          The integer version number of the model version
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the model version
        - name: version
          in: path
          required: true
          schema:
            type: integer
          description: The integer version number of the model version
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ModelVersionsAPI
    get:
      operationId: model_versions_get
      summary: Get a model version.
      tags:
        - catalog
        - model_versions
      description: >-
        Get a model version.


        The caller must be a metastore admin or an owner of (or have the
        **EXECUTE** privilege on) the parent

        registered model. For the latter case, the caller must also be the owner
        or have the **USE_CATALOG**

        privilege on the parent catalog and the **USE_SCHEMA** privilege on the
        parent schema.


        :param full_name: str
          The three-level (fully qualified) name of the model version
        :param version: int
          The integer version number of the model version
        :param include_aliases: bool (optional)
          Whether to include aliases associated with the model version in the response
        :param include_browse: bool (optional)
          Whether to include model versions in the response for which the principal can only access selective
          metadata for

        :returns: :class:`ModelVersionInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the model version
        - name: version
          in: path
          required: true
          schema:
            type: integer
          description: The integer version number of the model version
        - name: include_aliases
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include aliases associated with the model version in the
            response
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include model versions in the response for which the
            principal can only access selective metadata for
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelVersionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ModelVersionsAPI
    patch:
      operationId: model_versions_update
      summary: Updates the specified model version.
      tags:
        - catalog
        - model_versions
      description: >-
        Updates the specified model version.


        The caller must be a metastore admin or an owner of the parent
        registered model. For the latter case,

        the caller must also be the owner or have the **USE_CATALOG** privilege
        on the parent catalog and the

        **USE_SCHEMA** privilege on the parent schema.


        Currently only the comment of the model version can be updated.


        :param full_name: str
          The three-level (fully qualified) name of the model version
        :param version: int
          The integer version number of the model version
        :param aliases: List[:class:`RegisteredModelAlias`] (optional)
          List of aliases associated with the model version
        :param catalog_name: str (optional)
          The name of the catalog containing the model version
        :param comment: str (optional)
          The comment attached to the model version
        :param created_at: int (optional)

        :param created_by: str (optional)
          The identifier of the user who created the model version
        :param id: str (optional)
          The unique identifier of the model version
        :param metastore_id: str (optional)
          The unique identifier of the metastore containing the model version
        :param model_name: str (optional)
          The name of the parent registered model of the model version, relative to parent schema
        :param model_version_dependencies: :class:`DependencyList` (optional)
          Model version dependencies, for feature-store packaged models
        :param run_id: str (optional)
          MLflow run ID used when creating the model version, if ``source`` was generated by an experiment run
          stored in an MLflow tracking server
        :param run_workspace_id: int (optional)
          ID of the Databricks workspace containing the MLflow run that generated this model version, if
          applicable
        :param schema_name: str (optional)
          The name of the schema containing the model version, relative to parent catalog
        :param source: str (optional)
          URI indicating the location of the source artifacts (files) for the model version
        :param status: :class:`ModelVersionInfoStatus` (optional)
          Current status of the model version. Newly created model versions start in PENDING_REGISTRATION
          status, then move to READY status once the model version files are uploaded and the model version is
          finalized. Only model versions in READY status can be loaded for inference or served.
        :param storage_location: str (optional)
          The storage location on the cloud under which model version data files are stored
        :param updated_at: int (optional)

        :param updated_by: str (optional)
          The identifier of the user who updated the model version last time

        :returns: :class:`ModelVersionInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the model version
        - name: version
          in: path
          required: true
          schema:
            type: integer
          description: The integer version number of the model version
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                aliases:
                  type: array
                  items:
                    type: object
                    properties:
                      alias_name:
                        type: string
                      catalog_name:
                        type: string
                        description: The name of the catalog containing the model version
                      id:
                        type: string
                        description: The unique identifier of the alias
                      model_name:
                        type: string
                        description: >-
                          The name of the parent registered model of the model
                          version, relative to parent schema
                      schema_name:
                        type: string
                        description: >-
                          The name of the schema containing the model version,
                          relative to parent catalog
                      version_num:
                        type: integer
                        description: >-
                          Integer version number of the model version to which
                          this alias points.
                  description: List of aliases associated with the model version
                catalog_name:
                  type: string
                  description: The name of the catalog containing the model version
                comment:
                  type: string
                  description: The comment attached to the model version
                created_at:
                  type: integer
                created_by:
                  type: string
                  description: The identifier of the user who created the model version
                id:
                  type: string
                  description: The unique identifier of the model version
                metastore_id:
                  type: string
                  description: >-
                    The unique identifier of the metastore containing the model
                    version
                model_name:
                  type: string
                  description: >-
                    The name of the parent registered model of the model
                    version, relative to parent schema
                model_version_dependencies:
                  type: object
                  properties:
                    dependencies:
                      type: array
                      items:
                        type: object
                        properties:
                          connection:
                            type: object
                            properties:
                              connection_name:
                                type: string
                                description: >-
                                  Full name of the dependent connection, in the
                                  form of **connection_name**.
                            description: A connection that is dependent on a SQL object.
                          credential:
                            type: object
                            properties:
                              credential_name:
                                type: string
                                description: >-
                                  Full name of the dependent credential, in the
                                  form of **credential_name**.
                            description: A credential that is dependent on a SQL object.
                          function:
                            type: object
                            properties:
                              function_full_name:
                                type: string
                                description: >-
                                  Full name of the dependent function, in the
                                  form of
                                  **catalog_name**.**schema_name**.**function_name**.
                            required:
                              - function_full_name
                            description: A function that is dependent on a SQL object.
                          metastore_name:
                            type: string
                            description: >-
                              Metastore context for the dependency. Global UC
                              server responses currently populate this with the
                              metastore UUID string so clients can disambiguate
                              dependencies with identical names across
                              metastores.
                          secret:
                            type: object
                            properties:
                              secret_full_name:
                                type: string
                                description: >-
                                  Full name of the dependent secret, in the form
                                  of
                                  **catalog_name**.**schema_name**.**secret_name**.
                            required:
                              - secret_full_name
                            description: A dependency on a Unity Catalog secret.
                          table:
                            type: object
                            properties:
                              table_full_name:
                                type: string
                                description: >-
                                  Full name of the dependent table, in the form
                                  of
                                  **catalog_name**.**schema_name**.**table_name**.
                            required:
                              - table_full_name
                            description: A table that is dependent on a SQL object.
                          volume:
                            type: object
                            properties:
                              volume_full_name:
                                type: string
                                description: >-
                                  Full name of the dependent volume, in the form
                                  of
                                  **catalog_name**.**schema_name**.**volume_name**.
                            required:
                              - volume_full_name
                            description: A dependency on a Unity Catalog volume.
                        description: >-
                          A dependency of a SQL object. One of the following
                          fields must be defined: **table**,
                              **function**, **connection**, **credential**, **volume**, or **secret**.
                      description: Array of dependencies.
                  description: >-
                    Model version dependencies, for feature-store packaged
                    models
                run_id:
                  type: string
                  description: >-
                    MLflow run ID used when creating the model version, if
                    ``source`` was generated by an experiment run stored in an
                    MLflow tracking server
                run_workspace_id:
                  type: integer
                  description: >-
                    ID of the Databricks workspace containing the MLflow run
                    that generated this model version, if applicable
                schema_name:
                  type: string
                  description: >-
                    The name of the schema containing the model version,
                    relative to parent catalog
                source:
                  type: string
                  description: >-
                    URI indicating the location of the source artifacts (files)
                    for the model version
                status:
                  type: string
                  x-enum:
                    - FAILED_REGISTRATION
                    - MODEL_VERSION_STATUS_UNKNOWN
                    - PENDING_REGISTRATION
                    - READY
                  description: >-
                    Current status of the model version. Newly created model
                    versions start in PENDING_REGISTRATION status, then move to
                    READY status once the model version files are uploaded and
                    the model version is finalized. Only model versions in READY
                    status can be loaded for inference or served.
                storage_location:
                  type: string
                  description: >-
                    The storage location on the cloud under which model version
                    data files are stored
                updated_at:
                  type: integer
                updated_by:
                  type: string
                  description: >-
                    The identifier of the user who updated the model version
                    last time
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelVersionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ModelVersionsAPI
  /api/2.1/unity-catalog/models/{full_name}/aliases/{alias}:
    get:
      operationId: model_versions_get_by_alias
      summary: Get a model version by alias.
      tags:
        - catalog
        - model_versions
      description: >-
        Get a model version by alias.


        The caller must be a metastore admin or an owner of (or have the
        **EXECUTE** privilege on) the

        registered model. For the latter case, the caller must also be the owner
        or have the **USE_CATALOG**

        privilege on the parent catalog and the **USE_SCHEMA** privilege on the
        parent schema.


        :param full_name: str
          The three-level (fully qualified) name of the registered model
        :param alias: str
          The name of the alias
        :param include_aliases: bool (optional)
          Whether to include aliases associated with the model version in the response

        :returns: :class:`ModelVersionInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the registered model
        - name: alias
          in: path
          required: true
          schema:
            type: string
          description: The name of the alias
        - name: include_aliases
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include aliases associated with the model version in the
            response
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelVersionInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ModelVersionsAPI
    delete:
      operationId: registered_models_delete_alias
      summary: Deletes a registered model alias.
      tags:
        - catalog
        - registered_models
      description: >-
        Deletes a registered model alias.


        The caller must be a metastore admin or an owner of the registered
        model. For the latter case, the

        caller must also be the owner or have the **USE_CATALOG** privilege on
        the parent catalog and the

        **USE_SCHEMA** privilege on the parent schema.


        :param full_name: str
          The three-level (fully qualified) name of the registered model
        :param alias: str
          The name of the alias
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the registered model
        - name: alias
          in: path
          required: true
          schema:
            type: string
          description: The name of the alias
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RegisteredModelsAPI
    put:
      operationId: registered_models_set_alias
      summary: Set an alias on the specified registered model.
      tags:
        - catalog
        - registered_models
      description: >-
        Set an alias on the specified registered model.


        The caller must be a metastore admin or an owner of the registered
        model. For the latter case, the

        caller must also be the owner or have the **USE_CATALOG** privilege on
        the parent catalog and the

        **USE_SCHEMA** privilege on the parent schema.


        :param full_name: str
          The three-level (fully qualified) name of the registered model
        :param alias: str
          The name of the alias
        :param version_num: int
          The version number of the model version to which the alias points

        :returns: :class:`RegisteredModelAlias`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the registered model
        - name: alias
          in: path
          required: true
          schema:
            type: string
          description: The name of the alias
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                version_num:
                  type: integer
                  description: >-
                    The version number of the model version to which the alias
                    points
              required:
                - version_num
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisteredModelAlias'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RegisteredModelsAPI
  /api/2.1/unity-catalog/models/{full_name}/versions:
    get:
      operationId: model_versions_list
      summary: >-
        List model versions. You can list model versions under a particular
        schema, or list all model versions
      tags:
        - catalog
        - model_versions
      description: >-
        List model versions. You can list model versions under a particular
        schema, or list all model versions

        in the current metastore.


        The returned models are filtered based on the privileges of the calling
        user. For example, the

        metastore admin is able to list all the model versions. A regular user
        needs to be the owner or have

        the **EXECUTE** privilege on the parent registered model to receive the
        model versions in the

        response. For the latter case, the caller must also be the owner or have
        the **USE_CATALOG** privilege

        on the parent catalog and the **USE_SCHEMA** privilege on the parent
        schema.


        There is no guarantee of a specific ordering of the elements in the
        response. The elements in the

        response will not contain any aliases or tags.


        PAGINATION BEHAVIOR: The API is by default paginated, a page may contain
        zero results while still

        providing a next_page_token. Clients must continue reading pages until
        next_page_token is absent,

        which is the only indication that the end of results has been reached.


        :param full_name: str
          The full three-level name of the registered model under which to list model versions
        :param include_browse: bool (optional)
          Whether to include model versions in the response for which the principal can only access selective
          metadata for
        :param max_results: int (optional)
          Maximum number of model versions to return. If not set, the page length is set to a server
          configured value (100, as of 1/3/2024).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value(1000, as of 1/3/2024);
          - when set to 0, the page length is set to a server configured value (100, as of 1/3/2024)
            (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`ModelVersionInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            The full three-level name of the registered model under which to
            list model versions
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include model versions in the response for which the
            principal can only access selective metadata for
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of model versions to return. If not set, the page
            length is set to a server configured value (100, as of 1/3/2024). -
            when set to a value greater than 0, the page length is the minimum
            of this value and a server configured value(1000, as of 1/3/2024); -
            when set to 0, the page length is set to a server configured value
            (100, as of 1/3/2024) (recommended); - when set to a value less than
            0, an invalid parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListModelVersionsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ModelVersionsAPI
  /api/2.0/online-tables:
    post:
      operationId: online_tables_create
      summary: Create a new Online Table.
      tags:
        - catalog
        - online_tables
      description: |-
        Create a new Online Table.

        :param table: :class:`OnlineTable`
          Specification of the online table to be created.

        :returns:
          Long-running operation waiter for :class:`OnlineTable`.
          See :method:wait_get_online_table_active for more details.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                table:
                  type: object
                  properties:
                    name:
                      type: string
                      description: >-
                        Full three-part (catalog, schema, table) name of the
                        table.
                    spec:
                      type: object
                      properties:
                        perform_full_copy:
                          type: boolean
                          description: >-
                            Whether to create a full-copy pipeline -- a pipeline
                            that stops after creates a full copy of the source
                            table upon initialization and does not process any
                            change data feeds (CDFs) afterwards. The pipeline
                            can still be manually triggered afterwards, but it
                            always perform a full copy of the source table and
                            there are no incremental updates. This mode is
                            useful for syncing views or tables without CDFs to
                            online tables. Note that the full-copy pipeline only
                            supports "triggered" scheduling policy.
                        pipeline_id:
                          type: string
                          description: >-
                            ID of the associated pipeline. Generated by the
                            server - cannot be set by the caller.
                        primary_key_columns:
                          type: array
                          items:
                            type: string
                          description: >-
                            Primary Key columns to be used for data
                            insert/update in the destination.
                        run_continuously:
                          type: object
                          properties: {}
                          description: >-
                            Pipeline runs continuously after generating the
                            initial data.
                        run_triggered:
                          type: object
                          properties: {}
                          description: >-
                            Pipeline stops after generating the initial data and
                            can be triggered later (manually, through a cron job
                            or through data triggers)
                        source_table_full_name:
                          type: string
                          description: >-
                            Three-part (catalog, schema, table) name of the
                            source Delta table.
                        timeseries_key:
                          type: string
                          description: >-
                            Time series key to deduplicate (tie-break) rows with
                            the same primary key.
                      description: Specification of the online table.
                    status:
                      type: object
                      properties:
                        continuous_update_status:
                          type: object
                          properties:
                            initial_pipeline_sync_progress:
                              type: object
                              properties:
                                estimated_completion_time_seconds:
                                  type: number
                                  description: >-
                                    The estimated time remaining to complete
                                    this update in seconds.
                                latest_version_currently_processing:
                                  type: integer
                                  description: >-
                                    The source table Delta version that was last
                                    processed by the pipeline. The pipeline may
                                    not have completely processed this version
                                    yet.
                                sync_progress_completion:
                                  type: number
                                  description: >-
                                    The completion ratio of this update. This is
                                    a number between 0 and 1.
                                synced_row_count:
                                  type: integer
                                  description: >-
                                    The number of rows that have been synced in
                                    this update.
                                total_row_count:
                                  type: integer
                                  description: >-
                                    The total number of rows that need to be
                                    synced in this update. This number may be an
                                    estimate.
                              description: Progress of the initial data synchronization.
                            last_processed_commit_version:
                              type: integer
                              description: >-
                                The last source table Delta version that was
                                synced to the online table. Note that this Delta
                                version may not be completely synced to the
                                online table yet.
                            timestamp:
                              type: string
                              description: >-
                                The timestamp of the last time any data was
                                synchronized from the source table to the online
                                table.
                          description: >-
                            Detailed status of an online table. Shown if the
                            online table is in the ONLINE_CONTINUOUS_UPDATE
                                or the ONLINE_UPDATING_PIPELINE_RESOURCES state.
                        detailed_state:
                          type: string
                          x-enum:
                            - OFFLINE
                            - OFFLINE_FAILED
                            - ONLINE
                            - ONLINE_CONTINUOUS_UPDATE
                            - ONLINE_NO_PENDING_UPDATE
                            - ONLINE_PIPELINE_FAILED
                            - ONLINE_TRIGGERED_UPDATE
                            - ONLINE_UPDATING_PIPELINE_RESOURCES
                            - PROVISIONING
                            - PROVISIONING_INITIAL_SNAPSHOT
                            - PROVISIONING_PIPELINE_RESOURCES
                          description: The state of the online table.
                        failed_status:
                          type: object
                          properties:
                            last_processed_commit_version:
                              type: integer
                              description: >-
                                The last source table Delta version that was
                                synced to the online table. Note that this Delta
                                version may only be partially synced to the
                                online table. Only populated if the table is
                                still online and available for serving.
                            timestamp:
                              type: string
                              description: >-
                                The timestamp of the last time any data was
                                synchronized from the source table to the online
                                table. Only populated if the table is still
                                online and available for serving.
                          description: >-
                            Detailed status of an online table. Shown if the
                            online table is in the OFFLINE_FAILED or the
                                ONLINE_PIPELINE_FAILED state.
                        message:
                          type: string
                          description: >-
                            A text description of the current state of the
                            online table.
                        provisioning_status:
                          type: object
                          properties:
                            initial_pipeline_sync_progress:
                              type: object
                              properties:
                                estimated_completion_time_seconds:
                                  type: number
                                  description: >-
                                    The estimated time remaining to complete
                                    this update in seconds.
                                latest_version_currently_processing:
                                  type: integer
                                  description: >-
                                    The source table Delta version that was last
                                    processed by the pipeline. The pipeline may
                                    not have completely processed this version
                                    yet.
                                sync_progress_completion:
                                  type: number
                                  description: >-
                                    The completion ratio of this update. This is
                                    a number between 0 and 1.
                                synced_row_count:
                                  type: integer
                                  description: >-
                                    The number of rows that have been synced in
                                    this update.
                                total_row_count:
                                  type: integer
                                  description: >-
                                    The total number of rows that need to be
                                    synced in this update. This number may be an
                                    estimate.
                              description: >-
                                Details about initial data synchronization. Only
                                populated when in the
                                PROVISIONING_INITIAL_SNAPSHOT state.
                          description: >-
                            Detailed status of an online table. Shown if the
                            online table is in the
                                PROVISIONING_PIPELINE_RESOURCES or the PROVISIONING_INITIAL_SNAPSHOT state.
                        triggered_update_status:
                          type: object
                          properties:
                            last_processed_commit_version:
                              type: integer
                              description: >-
                                The last source table Delta version that was
                                synced to the online table. Note that this Delta
                                version may not be completely synced to the
                                online table yet.
                            timestamp:
                              type: string
                              description: >-
                                The timestamp of the last time any data was
                                synchronized from the source table to the online
                                table.
                            triggered_update_progress:
                              type: object
                              properties:
                                estimated_completion_time_seconds:
                                  type: number
                                  description: >-
                                    The estimated time remaining to complete
                                    this update in seconds.
                                latest_version_currently_processing:
                                  type: integer
                                  description: >-
                                    The source table Delta version that was last
                                    processed by the pipeline. The pipeline may
                                    not have completely processed this version
                                    yet.
                                sync_progress_completion:
                                  type: number
                                  description: >-
                                    The completion ratio of this update. This is
                                    a number between 0 and 1.
                                synced_row_count:
                                  type: integer
                                  description: >-
                                    The number of rows that have been synced in
                                    this update.
                                total_row_count:
                                  type: integer
                                  description: >-
                                    The total number of rows that need to be
                                    synced in this update. This number may be an
                                    estimate.
                              description: >-
                                Progress of the active data synchronization
                                pipeline.
                          description: >-
                            Detailed status of an online table. Shown if the
                            online table is in the ONLINE_TRIGGERED_UPDATE
                                or the ONLINE_NO_PENDING_UPDATE state.
                      description: Online Table data synchronization status
                    table_serving_url:
                      type: string
                      description: Data serving REST API URL for this table
                    unity_catalog_provisioning_state:
                      type: string
                      x-enum:
                        - ACTIVE
                        - DEGRADED
                        - DELETING
                        - FAILED
                        - PROVISIONING
                        - UPDATING
                      description: >-
                        The provisioning state of the online table entity in
                        Unity Catalog. This is distinct from the state of the
                        data synchronization pipeline (i.e. the table may be in
                        "ACTIVE" but the pipeline may be in "PROVISIONING" as it
                        runs asynchronously).
                  description: Specification of the online table to be created.
              required:
                - table
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OnlineTable'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: OnlineTablesAPI
  /api/2.0/online-tables/{name}:
    delete:
      operationId: online_tables_delete
      summary: >-
        Delete an online table. Warning: This will delete all the data in the
        online table. If the source
      tags:
        - catalog
        - online_tables
      description: >-
        Delete an online table. Warning: This will delete all the data in the
        online table. If the source

        Delta table was deleted or modified since this Online Table was created,
        this will lose the data

        forever!


        :param name: str
          Full three-part (catalog, schema, table) name of the table.
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Full three-part (catalog, schema, table) name of the table.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: OnlineTablesAPI
    get:
      operationId: online_tables_get
      summary: Get information about an existing online table and its status.
      tags:
        - catalog
        - online_tables
      description: |-
        Get information about an existing online table and its status.

        :param name: str
          Full three-part (catalog, schema, table) name of the table.

        :returns: :class:`OnlineTable`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Full three-part (catalog, schema, table) name of the table.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OnlineTable'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: OnlineTablesAPI
  /api/2.1/unity-catalog/policies:
    post:
      operationId: policies_create_policy
      summary: >-
        Creates a new policy on a securable. The new policy applies to the
        securable and all its descendants.
      tags:
        - catalog
        - policies
      description: >-
        Creates a new policy on a securable. The new policy applies to the
        securable and all its descendants.


        :param policy_info: :class:`PolicyInfo`
          Required. The policy to create.

        :returns: :class:`PolicyInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                policy_info:
                  type: object
                  properties:
                    to_principals:
                      type: array
                      items:
                        type: string
                    for_securable_type:
                      type: string
                      x-enum:
                        - CATALOG
                        - CLEAN_ROOM
                        - CONNECTION
                        - CREDENTIAL
                        - EXTERNAL_LOCATION
                        - EXTERNAL_METADATA
                        - FUNCTION
                        - METASTORE
                        - PIPELINE
                        - PROVIDER
                        - RECIPIENT
                        - SCHEMA
                        - SHARE
                        - STAGING_TABLE
                        - STORAGE_CREDENTIAL
                        - TABLE
                        - VOLUME
                      description: >-
                        Type of securables that the policy should take effect
                        on. Only ``TABLE`` is supported at this moment. Required
                        on create and optional on update.
                    policy_type:
                      type: string
                      x-enum:
                        - POLICY_TYPE_COLUMN_MASK
                        - POLICY_TYPE_DENY
                        - POLICY_TYPE_GRANT
                        - POLICY_TYPE_ROW_FILTER
                      description: Type of the policy. Required on create.
                    column_mask:
                      type: object
                      properties:
                        function_name:
                          type: string
                        on_column:
                          type: string
                          description: >-
                            The alias of the column to be masked. The alias must
                            refer to one of matched columns. The values of the
                            column is passed to the column mask function as the
                            first argument. Required on create and update.
                        using:
                          type: array
                          items:
                            type: object
                            properties:
                              alias:
                                type: string
                              constant:
                                type: string
                                description: A constant literal.
                              function_arg_expression:
                                type: object
                                properties:
                                  tag_introspection:
                                    type: object
                                    properties:
                                      column_tag_value:
                                        type: object
                                        properties:
                                          column_alias:
                                            type: string
                                            description: >-
                                              The alias from MATCH COLUMNS that
                                              identifies the column.
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - column_alias
                                          - tag_key
                                        description: >-
                                          Extracts the value of a column-level
                                          tag.
                                      tag_value:
                                        type: object
                                        properties:
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - tag_key
                                        description: >-
                                          Extracts the value of a securable-level
                                          tag.
                                    description: >-
                                      An expression that introspects tags at
                                      query time.
                                description: >-
                                  An expression evaluated at query time. Wraps
                                  per-request expression variants (e.g., tag
                                  introspection) so new variants can be added
                                  without extending the FunctionArgument oneof.
                          description: >-
                            Optional list of column aliases or constant literals
                            to be passed as additional arguments to the column
                            mask function. The type of each column should match
                            the positional argument of the column mask function.
                      required:
                        - function_name
                        - on_column
                      description: >-
                        Options for column mask policies. Valid only if
                        ``policy_type`` is ``POLICY_TYPE_COLUMN_MASK``. Required
                        on create and optional on update. When specified on
                        update, the new options will replace the existing
                        options as a whole.
                    comment:
                      type: string
                      description: Optional description of the policy.
                    created_at:
                      type: integer
                      description: >-
                        Time at which the policy was created, in epoch
                        milliseconds. Output only.
                    created_by:
                      type: string
                      description: >-
                        Username of the user who created the policy. Output
                        only.
                    deny:
                      type: object
                      properties:
                        privileges:
                          type: array
                          items:
                            type: string
                      required:
                        - privileges
                      description: >-
                        Options for deny policies. Valid only if ``policy_type``
                        is ``POLICY_TYPE_DENY``. Required on create and optional
                        on update. When specified on update, the new options
                        will replace the existing options as a whole.
                    except_principals:
                      type: array
                      items:
                        type: string
                      description: >-
                        Optional list of user or group names that should be
                        excluded from the policy.
                    grant:
                      type: object
                      properties:
                        privileges:
                          type: array
                          items:
                            type: string
                      required:
                        - privileges
                      description: >-
                        Options for grant policies. Valid only if
                        ``policy_type`` is ``POLICY_TYPE_GRANT``. Required on
                        create and optional on update. When specified on update,
                        the new options will replace the existing options as a
                        whole.
                    id:
                      type: string
                      description: >-
                        Unique identifier of the policy. This field is output
                        only and is generated by the system.
                    match_columns:
                      type: array
                      items:
                        type: object
                        properties:
                          alias:
                            type: string
                          condition:
                            type: string
                            description: >-
                              The condition expression used to match a table
                              column.
                      description: >-
                        Optional list of condition expressions used to match
                        table columns. Only valid when ``for_securable_type`` is
                        ``TABLE``. When specified, the policy only applies to
                        tables whose columns satisfy all match conditions.
                    name:
                      type: string
                      description: >-
                        Name of the policy. Required on create and optional on
                        update. To rename the policy, set ``name`` to a
                        different value on update.
                    on_securable_fullname:
                      type: string
                      description: >-
                        Full name of the securable on which the policy is
                        defined. Required on create.
                    on_securable_type:
                      type: string
                      x-enum:
                        - CATALOG
                        - CLEAN_ROOM
                        - CONNECTION
                        - CREDENTIAL
                        - EXTERNAL_LOCATION
                        - EXTERNAL_METADATA
                        - FUNCTION
                        - METASTORE
                        - PIPELINE
                        - PROVIDER
                        - RECIPIENT
                        - SCHEMA
                        - SHARE
                        - STAGING_TABLE
                        - STORAGE_CREDENTIAL
                        - TABLE
                        - VOLUME
                      description: >-
                        Type of the securable on which the policy is defined.
                        Only ``CATALOG``, ``SCHEMA`` and ``TABLE`` are supported
                        at this moment. Required on create.
                    row_filter:
                      type: object
                      properties:
                        function_name:
                          type: string
                        using:
                          type: array
                          items:
                            type: object
                            properties:
                              alias:
                                type: string
                              constant:
                                type: string
                                description: A constant literal.
                              function_arg_expression:
                                type: object
                                properties:
                                  tag_introspection:
                                    type: object
                                    properties:
                                      column_tag_value:
                                        type: object
                                        properties:
                                          column_alias:
                                            type: string
                                            description: >-
                                              The alias from MATCH COLUMNS that
                                              identifies the column.
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - column_alias
                                          - tag_key
                                        description: >-
                                          Extracts the value of a column-level
                                          tag.
                                      tag_value:
                                        type: object
                                        properties:
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - tag_key
                                        description: >-
                                          Extracts the value of a securable-level
                                          tag.
                                    description: >-
                                      An expression that introspects tags at
                                      query time.
                                description: >-
                                  An expression evaluated at query time. Wraps
                                  per-request expression variants (e.g., tag
                                  introspection) so new variants can be added
                                  without extending the FunctionArgument oneof.
                          description: >-
                            Optional list of column aliases or constant literals
                            to be passed as arguments to the row filter
                            function. The type of each column should match the
                            positional argument of the row filter function.
                      required:
                        - function_name
                      description: >-
                        Options for row filter policies. Valid only if
                        ``policy_type`` is ``POLICY_TYPE_ROW_FILTER``. Required
                        on create and optional on update. When specified on
                        update, the new options will replace the existing
                        options as a whole.
                    updated_at:
                      type: integer
                      description: >-
                        Time at which the policy was last modified, in epoch
                        milliseconds. Output only.
                    updated_by:
                      type: string
                      description: >-
                        Username of the user who last modified the policy.
                        Output only.
                    use_session_identity:
                      type: boolean
                      description: >-
                        Temporary for migrating customers to session identity.
                        Customers not currently using ABAC will not be able to
                        set this field to false and all new policies will have
                        this field default to true. Existing customers will have
                        this field default to false, but can set it to true to
                        opt in to session identity. after a grace period, this
                        field will be removed and all policies will use session
                        identity. Only for row filter and column mask policies.
                        Not applicable to deny policies.
                    when_condition:
                      type: string
                      description: Optional condition when the policy should take effect.
                  required:
                    - to_principals
                    - for_securable_type
                    - policy_type
                  description: Required. The policy to create.
              required:
                - policy_info
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PoliciesAPI
  /api/2.1/unity-catalog/policies/{on_securable_type}/{on_securable_fullname}/{name}:
    delete:
      operationId: policies_delete_policy
      summary: Delete an ABAC policy defined on a securable.
      tags:
        - catalog
        - policies
      description: |-
        Delete an ABAC policy defined on a securable.

        :param on_securable_type: str
          Required. The type of the securable to delete the policy from.
        :param on_securable_fullname: str
          Required. The fully qualified name of the securable to delete the policy from.
        :param name: str
          Required. The name of the policy to delete

        :returns: :class:`DeletePolicyResponse`
      parameters:
        - name: on_securable_type
          in: path
          required: true
          schema:
            type: string
          description: Required. The type of the securable to delete the policy from.
        - name: on_securable_fullname
          in: path
          required: true
          schema:
            type: string
          description: >-
            Required. The fully qualified name of the securable to delete the
            policy from.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Required. The name of the policy to delete
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeletePolicyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PoliciesAPI
    get:
      operationId: policies_get_policy
      summary: Get the policy definition on a securable
      tags:
        - catalog
        - policies
      description: |-
        Get the policy definition on a securable

        :param on_securable_type: str
          Required. The type of the securable to retrieve the policy for.
        :param on_securable_fullname: str
          Required. The fully qualified name of securable to retrieve policy for.
        :param name: str
          Required. The name of the policy to retrieve.

        :returns: :class:`PolicyInfo`
      parameters:
        - name: on_securable_type
          in: path
          required: true
          schema:
            type: string
          description: Required. The type of the securable to retrieve the policy for.
        - name: on_securable_fullname
          in: path
          required: true
          schema:
            type: string
          description: >-
            Required. The fully qualified name of securable to retrieve policy
            for.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Required. The name of the policy to retrieve.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PoliciesAPI
    patch:
      operationId: policies_update_policy
      summary: Update an ABAC policy on a securable.
      tags:
        - catalog
        - policies
      description: |-
        Update an ABAC policy on a securable.

        :param on_securable_type: str
          Required. The type of the securable to update the policy for.
        :param on_securable_fullname: str
          Required. The fully qualified name of the securable to update the policy for.
        :param name: str
          Required. The name of the policy to update.
        :param policy_info: :class:`PolicyInfo`
          Optional fields to update. This is the request body for updating a policy. Use ``update_mask`` field
          to specify which fields in the request is to be updated.

          - If ``update_mask`` is empty or "*", all specified fields will be updated.
          - If ``update_mask`` is specified, only the fields specified in the ``update_mask`` will be updated.
            If a field is specified in ``update_mask`` and not set in the request, the field will be cleared.
            Users can use the update mask to explicitly unset optional fields such as ``exception_principals``
            and ``when_condition``.
        :param update_mask: str (optional)
          Optional. The update mask field for specifying user intentions on which fields to update in the
          request.

        :returns: :class:`PolicyInfo`
      parameters:
        - name: on_securable_type
          in: path
          required: true
          schema:
            type: string
          description: Required. The type of the securable to update the policy for.
        - name: on_securable_fullname
          in: path
          required: true
          schema:
            type: string
          description: >-
            Required. The fully qualified name of the securable to update the
            policy for.
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Required. The name of the policy to update.
        - name: update_mask
          in: query
          required: false
          schema:
            type: string
          description: >-
            Optional. The update mask field for specifying user intentions on
            which fields to update in the request.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                policy_info:
                  type: object
                  properties:
                    to_principals:
                      type: array
                      items:
                        type: string
                    for_securable_type:
                      type: string
                      x-enum:
                        - CATALOG
                        - CLEAN_ROOM
                        - CONNECTION
                        - CREDENTIAL
                        - EXTERNAL_LOCATION
                        - EXTERNAL_METADATA
                        - FUNCTION
                        - METASTORE
                        - PIPELINE
                        - PROVIDER
                        - RECIPIENT
                        - SCHEMA
                        - SHARE
                        - STAGING_TABLE
                        - STORAGE_CREDENTIAL
                        - TABLE
                        - VOLUME
                      description: >-
                        Type of securables that the policy should take effect
                        on. Only ``TABLE`` is supported at this moment. Required
                        on create and optional on update.
                    policy_type:
                      type: string
                      x-enum:
                        - POLICY_TYPE_COLUMN_MASK
                        - POLICY_TYPE_DENY
                        - POLICY_TYPE_GRANT
                        - POLICY_TYPE_ROW_FILTER
                      description: Type of the policy. Required on create.
                    column_mask:
                      type: object
                      properties:
                        function_name:
                          type: string
                        on_column:
                          type: string
                          description: >-
                            The alias of the column to be masked. The alias must
                            refer to one of matched columns. The values of the
                            column is passed to the column mask function as the
                            first argument. Required on create and update.
                        using:
                          type: array
                          items:
                            type: object
                            properties:
                              alias:
                                type: string
                              constant:
                                type: string
                                description: A constant literal.
                              function_arg_expression:
                                type: object
                                properties:
                                  tag_introspection:
                                    type: object
                                    properties:
                                      column_tag_value:
                                        type: object
                                        properties:
                                          column_alias:
                                            type: string
                                            description: >-
                                              The alias from MATCH COLUMNS that
                                              identifies the column.
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - column_alias
                                          - tag_key
                                        description: >-
                                          Extracts the value of a column-level
                                          tag.
                                      tag_value:
                                        type: object
                                        properties:
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - tag_key
                                        description: >-
                                          Extracts the value of a securable-level
                                          tag.
                                    description: >-
                                      An expression that introspects tags at
                                      query time.
                                description: >-
                                  An expression evaluated at query time. Wraps
                                  per-request expression variants (e.g., tag
                                  introspection) so new variants can be added
                                  without extending the FunctionArgument oneof.
                          description: >-
                            Optional list of column aliases or constant literals
                            to be passed as additional arguments to the column
                            mask function. The type of each column should match
                            the positional argument of the column mask function.
                      required:
                        - function_name
                        - on_column
                      description: >-
                        Options for column mask policies. Valid only if
                        ``policy_type`` is ``POLICY_TYPE_COLUMN_MASK``. Required
                        on create and optional on update. When specified on
                        update, the new options will replace the existing
                        options as a whole.
                    comment:
                      type: string
                      description: Optional description of the policy.
                    created_at:
                      type: integer
                      description: >-
                        Time at which the policy was created, in epoch
                        milliseconds. Output only.
                    created_by:
                      type: string
                      description: >-
                        Username of the user who created the policy. Output
                        only.
                    deny:
                      type: object
                      properties:
                        privileges:
                          type: array
                          items:
                            type: string
                      required:
                        - privileges
                      description: >-
                        Options for deny policies. Valid only if ``policy_type``
                        is ``POLICY_TYPE_DENY``. Required on create and optional
                        on update. When specified on update, the new options
                        will replace the existing options as a whole.
                    except_principals:
                      type: array
                      items:
                        type: string
                      description: >-
                        Optional list of user or group names that should be
                        excluded from the policy.
                    grant:
                      type: object
                      properties:
                        privileges:
                          type: array
                          items:
                            type: string
                      required:
                        - privileges
                      description: >-
                        Options for grant policies. Valid only if
                        ``policy_type`` is ``POLICY_TYPE_GRANT``. Required on
                        create and optional on update. When specified on update,
                        the new options will replace the existing options as a
                        whole.
                    id:
                      type: string
                      description: >-
                        Unique identifier of the policy. This field is output
                        only and is generated by the system.
                    match_columns:
                      type: array
                      items:
                        type: object
                        properties:
                          alias:
                            type: string
                          condition:
                            type: string
                            description: >-
                              The condition expression used to match a table
                              column.
                      description: >-
                        Optional list of condition expressions used to match
                        table columns. Only valid when ``for_securable_type`` is
                        ``TABLE``. When specified, the policy only applies to
                        tables whose columns satisfy all match conditions.
                    name:
                      type: string
                      description: >-
                        Name of the policy. Required on create and optional on
                        update. To rename the policy, set ``name`` to a
                        different value on update.
                    on_securable_fullname:
                      type: string
                      description: >-
                        Full name of the securable on which the policy is
                        defined. Required on create.
                    on_securable_type:
                      type: string
                      x-enum:
                        - CATALOG
                        - CLEAN_ROOM
                        - CONNECTION
                        - CREDENTIAL
                        - EXTERNAL_LOCATION
                        - EXTERNAL_METADATA
                        - FUNCTION
                        - METASTORE
                        - PIPELINE
                        - PROVIDER
                        - RECIPIENT
                        - SCHEMA
                        - SHARE
                        - STAGING_TABLE
                        - STORAGE_CREDENTIAL
                        - TABLE
                        - VOLUME
                      description: >-
                        Type of the securable on which the policy is defined.
                        Only ``CATALOG``, ``SCHEMA`` and ``TABLE`` are supported
                        at this moment. Required on create.
                    row_filter:
                      type: object
                      properties:
                        function_name:
                          type: string
                        using:
                          type: array
                          items:
                            type: object
                            properties:
                              alias:
                                type: string
                              constant:
                                type: string
                                description: A constant literal.
                              function_arg_expression:
                                type: object
                                properties:
                                  tag_introspection:
                                    type: object
                                    properties:
                                      column_tag_value:
                                        type: object
                                        properties:
                                          column_alias:
                                            type: string
                                            description: >-
                                              The alias from MATCH COLUMNS that
                                              identifies the column.
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - column_alias
                                          - tag_key
                                        description: >-
                                          Extracts the value of a column-level
                                          tag.
                                      tag_value:
                                        type: object
                                        properties:
                                          tag_key:
                                            type: string
                                            description: >-
                                              1024 matches the max_length on
                                              FunctionArgument.constant above.
                                        required:
                                          - tag_key
                                        description: >-
                                          Extracts the value of a securable-level
                                          tag.
                                    description: >-
                                      An expression that introspects tags at
                                      query time.
                                description: >-
                                  An expression evaluated at query time. Wraps
                                  per-request expression variants (e.g., tag
                                  introspection) so new variants can be added
                                  without extending the FunctionArgument oneof.
                          description: >-
                            Optional list of column aliases or constant literals
                            to be passed as arguments to the row filter
                            function. The type of each column should match the
                            positional argument of the row filter function.
                      required:
                        - function_name
                      description: >-
                        Options for row filter policies. Valid only if
                        ``policy_type`` is ``POLICY_TYPE_ROW_FILTER``. Required
                        on create and optional on update. When specified on
                        update, the new options will replace the existing
                        options as a whole.
                    updated_at:
                      type: integer
                      description: >-
                        Time at which the policy was last modified, in epoch
                        milliseconds. Output only.
                    updated_by:
                      type: string
                      description: >-
                        Username of the user who last modified the policy.
                        Output only.
                    use_session_identity:
                      type: boolean
                      description: >-
                        Temporary for migrating customers to session identity.
                        Customers not currently using ABAC will not be able to
                        set this field to false and all new policies will have
                        this field default to true. Existing customers will have
                        this field default to false, but can set it to true to
                        opt in to session identity. after a grace period, this
                        field will be removed and all policies will use session
                        identity. Only for row filter and column mask policies.
                        Not applicable to deny policies.
                    when_condition:
                      type: string
                      description: Optional condition when the policy should take effect.
                  required:
                    - to_principals
                    - for_securable_type
                    - policy_type
                  description: >-
                    Optional fields to update. This is the request body for
                    updating a policy. Use ``update_mask`` field to specify
                    which fields in the request is to be updated. - If
                    ``update_mask`` is empty or "*", all specified fields will
                    be updated. - If ``update_mask`` is specified, only the
                    fields specified in the ``update_mask`` will be updated. If
                    a field is specified in ``update_mask`` and not set in the
                    request, the field will be cleared. Users can use the update
                    mask to explicitly unset optional fields such as
                    ``exception_principals`` and ``when_condition``.
              required:
                - policy_info
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PoliciesAPI
  /api/2.1/unity-catalog/policies/{on_securable_type}/{on_securable_fullname}:
    get:
      operationId: policies_list_policies
      summary: >-
        List all policies defined on a securable. Optionally, the list can
        include inherited policies defined
      tags:
        - catalog
        - policies
      description: >-
        List all policies defined on a securable. Optionally, the list can
        include inherited policies defined

        on the securable's parent schema or catalog.


        PAGINATION BEHAVIOR: The API is by default paginated, a page may contain
        zero results while still

        providing a next_page_token. Clients must continue reading pages until
        next_page_token is absent,

        which is the only indication that the end of results has been reached.


        :param on_securable_type: str
          Required. The type of the securable to list policies for.
        :param on_securable_fullname: str
          Required. The fully qualified name of securable to list policies for.
        :param include_inherited: bool (optional)
          Optional. Whether to include policies defined on parent securables. By default, the inherited
          policies are not included.
        :param max_results: int (optional)
          Optional. Maximum number of policies to return on a single page (page length).

          - When not set or set to 0, the page length is set to a server configured value (recommended);
          - When set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
        :param page_token: str (optional)
          Optional. Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`PolicyInfo`
      parameters:
        - name: on_securable_type
          in: path
          required: true
          schema:
            type: string
          description: Required. The type of the securable to list policies for.
        - name: on_securable_fullname
          in: path
          required: true
          schema:
            type: string
          description: >-
            Required. The fully qualified name of securable to list policies
            for.
        - name: include_inherited
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Optional. Whether to include policies defined on parent securables.
            By default, the inherited policies are not included.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Optional. Maximum number of policies to return on a single page
            (page length). - When not set or set to 0, the page length is set to
            a server configured value (recommended); - When set to a value
            greater than 0, the page length is the minimum of this value and a
            server configured value;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: >-
            Optional. Opaque pagination token to go to next page based on
            previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListPoliciesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PoliciesAPI
  /api/2.1/unity-catalog/tables/{table_name}/monitor/refreshes/{refresh_id}/cancel:
    post:
      operationId: quality_monitors_cancel_refresh
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Cancels an
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Cancels an

        already-initiated refresh job.


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. table_name is case insensitive and spaces are
          disallowed.
        :param refresh_id: int
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. table_name is
            case insensitive and spaces are disallowed.
        - name: refresh_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
  /api/2.1/unity-catalog/tables/{table_name}/monitor:
    post:
      operationId: quality_monitors_create
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Creates a new
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Creates a new

        monitor for the specified table.


        The caller must either:


        1. be an owner of the table's parent catalog, have **USE_SCHEMA** on the
        table's parent schema, and
           have **SELECT** access on the table
        2. have **USE_CATALOG** on the table's parent catalog, be an owner of
        the table's parent schema, and
           have **SELECT** access on the table.
        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - be an owner of the table.


        Workspace assets, such as the dashboard, will be created in the
        workspace where this call was made.


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. This field corresponds to the
          {full_table_name_arg} arg in the endpoint path.
        :param output_schema_name: str
          [Create:REQ Update:REQ] Schema where output tables are created. Needs to be in 2-level format
          {catalog}.{schema}
        :param assets_dir: str
          [Create:REQ Update:IGN] Field for specifying the absolute path to a custom directory to store
          data-monitoring assets. Normally prepopulated to a default user location via UI and Python APIs.
        :param baseline_table_name: str (optional)
          [Create:OPT Update:OPT] Baseline table name. Baseline data is used to compute drift from the data in
          the monitored ``table_name``. The baseline table and the monitored table shall have the same schema.
        :param custom_metrics: List[:class:`MonitorMetric`] (optional)
          [Create:OPT Update:OPT] Custom metrics.
        :param data_classification_config:
        :class:`MonitorDataClassificationConfig` (optional)
          [Create:OPT Update:OPT] Data classification related config.
        :param inference_log: :class:`MonitorInferenceLog` (optional)

        :param latest_monitor_failure_msg: str (optional)
          [Create:ERR Update:IGN] The latest error message for a monitor failure.
        :param notifications: :class:`MonitorNotifications` (optional)
          [Create:OPT Update:OPT] Field for specifying notification settings.
        :param schedule: :class:`MonitorCronSchedule` (optional)
          [Create:OPT Update:OPT] The monitor schedule.
        :param skip_builtin_dashboard: bool (optional)
          Whether to skip creating a default dashboard summarizing data quality metrics.
        :param slicing_exprs: List[str] (optional)
          [Create:OPT Update:OPT] List of column expressions to slice data with for targeted analysis. The
          data is grouped by each expression independently, resulting in a separate slice for each predicate
          and its complements. For example ``slicing_exprs=[“col_1”, “col_2 > 10”]`` will generate the
          following slices: two slices for ``col_2 > 10`` (True and False), and one slice per unique value in
          ``col1``. For high-cardinality columns, only the top 100 unique values by frequency will generate
          slices.
        :param snapshot: :class:`MonitorSnapshot` (optional)
          Configuration for monitoring snapshot tables.
        :param time_series: :class:`MonitorTimeSeries` (optional)
          Configuration for monitoring time series tables.
        :param warehouse_id: str (optional)
          Optional argument to specify the warehouse for dashboard creation. If not specified, the first
          running warehouse will be used.

        :returns: :class:`MonitorInfo`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. This field
            corresponds to the {full_table_name_arg} arg in the endpoint path.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                output_schema_name:
                  type: string
                  description: >-
                    [Create:REQ Update:REQ] Schema where output tables are
                    created. Needs to be in 2-level format {catalog}.{schema}
                assets_dir:
                  type: string
                  description: >-
                    [Create:REQ Update:IGN] Field for specifying the absolute
                    path to a custom directory to store data-monitoring assets.
                    Normally prepopulated to a default user location via UI and
                    Python APIs.
                baseline_table_name:
                  type: string
                  description: >-
                    [Create:OPT Update:OPT] Baseline table name. Baseline data
                    is used to compute drift from the data in the monitored
                    ``table_name``. The baseline table and the monitored table
                    shall have the same schema.
                custom_metrics:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: Name of the metric in the output tables.
                      definition:
                        type: string
                        description: >-
                          Jinja template for a SQL expression that specifies how
                          to compute the metric. See `create metric definition
                          <https://docs.databricks.com/en/lakehouse-monitoring/custom-metrics.html#create-definition>`__.
                      input_columns:
                        type: array
                        items:
                          type: string
                        description: >-
                          A list of column names in the input table the metric
                          should be computed for. Can use ``":table"`` to
                          indicate that the metric needs information from
                          multiple columns.
                      output_data_type:
                        type: string
                        description: The output type of the custom metric.
                      type:
                        type: string
                        x-enum:
                          - CUSTOM_METRIC_TYPE_AGGREGATE
                          - CUSTOM_METRIC_TYPE_DERIVED
                          - CUSTOM_METRIC_TYPE_DRIFT
                        description: >-
                          Can only be one of ``"CUSTOM_METRIC_TYPE_AGGREGATE"``,
                          ``"CUSTOM_METRIC_TYPE_DERIVED"``, or
                          ``"CUSTOM_METRIC_TYPE_DRIFT"``. The
                          ``"CUSTOM_METRIC_TYPE_AGGREGATE"`` and
                          ``"CUSTOM_METRIC_TYPE_DERIVED"`` metrics are computed
                          on a single table, whereas the
                          ``"CUSTOM_METRIC_TYPE_DRIFT"`` compare metrics across
                          baseline and input table, or across the two
                          consecutive time windows. -
                          CUSTOM_METRIC_TYPE_AGGREGATE: only depend on the
                          existing columns in your table -
                          CUSTOM_METRIC_TYPE_DERIVED: depend on previously
                          computed aggregate metrics - CUSTOM_METRIC_TYPE_DRIFT:
                          depend on previously computed aggregate or derived
                          metrics
                    required:
                      - name
                      - definition
                      - input_columns
                      - output_data_type
                      - type
                    description: Custom metric definition.
                  description: '[Create:OPT Update:OPT] Custom metrics.'
                data_classification_config:
                  type: object
                  properties:
                    enabled:
                      type: boolean
                      description: Whether to enable data classification.
                  description: '[Create:OPT Update:OPT] Data classification related config.'
                inference_log:
                  type: object
                  properties:
                    problem_type:
                      type: string
                      x-enum:
                        - PROBLEM_TYPE_CLASSIFICATION
                        - PROBLEM_TYPE_REGRESSION
                      description: >-
                        Create a collection of name/value pairs.


                        Example enumeration:


                        >>> class Color(Enum):

                        ...     RED = 1

                        ...     BLUE = 2

                        ...     GREEN = 3


                        Access them by:


                        - attribute access:

                          >>> Color.RED
                          <Color.RED: 1>

                        - value lookup:

                          >>> Color(1)
                          <Color.RED: 1>

                        - name lookup:

                          >>> Color['RED']
                          <Color.RED: 1>

                        Enumerations can be iterated over, and know how many
                        members they have:


                        >>> len(Color)

                        3


                        >>> list(Color)

                        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                        Methods can be added to enumerations, and members can
                        have their own

                        attributes -- see the documentation for details.
                    timestamp_col:
                      type: string
                      description: Column for the timestamp.
                    granularities:
                      type: array
                      items:
                        type: string
                      description: >-
                        List of granularities to use when aggregating data into
                        time windows based on their timestamp.
                    prediction_col:
                      type: string
                      description: Column for the prediction.
                    model_id_col:
                      type: string
                      description: Column for the model identifier.
                    label_col:
                      type: string
                      description: Column for the label.
                    prediction_proba_col:
                      type: string
                      description: Column for prediction probabilities
                  required:
                    - problem_type
                    - timestamp_col
                    - granularities
                    - prediction_col
                    - model_id_col
                latest_monitor_failure_msg:
                  type: string
                  description: >-
                    [Create:ERR Update:IGN] The latest error message for a
                    monitor failure.
                notifications:
                  type: object
                  properties:
                    on_failure:
                      type: object
                      properties:
                        email_addresses:
                          type: array
                          items:
                            type: string
                    on_new_classification_tag_detected:
                      type: object
                      properties:
                        email_addresses:
                          type: array
                          items:
                            type: string
                      description: >-
                        Destinations to send notifications on new classification
                        tag detected.
                  description: >-
                    [Create:OPT Update:OPT] Field for specifying notification
                    settings.
                schedule:
                  type: object
                  properties:
                    quartz_cron_expression:
                      type: string
                    timezone_id:
                      type: string
                      description: >-
                        The timezone id (e.g., ``PST``) in which to evaluate the
                        quartz expression.
                    pause_status:
                      type: string
                      x-enum:
                        - PAUSED
                        - UNPAUSED
                        - UNSPECIFIED
                      description: >-
                        Read only field that indicates whether a schedule is
                        paused or not.
                  required:
                    - quartz_cron_expression
                    - timezone_id
                  description: '[Create:OPT Update:OPT] The monitor schedule.'
                skip_builtin_dashboard:
                  type: boolean
                  description: >-
                    Whether to skip creating a default dashboard summarizing
                    data quality metrics.
                slicing_exprs:
                  type: array
                  items:
                    type: string
                  description: >-
                    [Create:OPT Update:OPT] List of column expressions to slice
                    data with for targeted analysis. The data is grouped by each
                    expression independently, resulting in a separate slice for
                    each predicate and its complements. For example
                    ``slicing_exprs=[“col_1”, “col_2 > 10”]`` will generate the
                    following slices: two slices for ``col_2 > 10`` (True and
                    False), and one slice per unique value in ``col1``. For
                    high-cardinality columns, only the top 100 unique values by
                    frequency will generate slices.
                snapshot:
                  type: object
                  properties: {}
                  description: Configuration for monitoring snapshot tables.
                time_series:
                  type: object
                  properties:
                    timestamp_col:
                      type: string
                      description: Column for the timestamp.
                    granularities:
                      type: array
                      items:
                        type: string
                      description: >-
                        Granularities for aggregating data into time windows
                        based on their timestamp. Currently the following static
                        granularities are supported: {``\"5 minutes\"``, ``\"30
                        minutes\"``, ``\"1 hour\"``, ``\"1 day\"``,
                        ``\"\u003cn\u003e week(s)\"``, ``\"1 month\"``, ``\"1
                        year\"``}.
                  required:
                    - timestamp_col
                    - granularities
                  description: Configuration for monitoring time series tables.
                warehouse_id:
                  type: string
                  description: >-
                    Optional argument to specify the warehouse for dashboard
                    creation. If not specified, the first running warehouse will
                    be used.
              required:
                - output_schema_name
                - assets_dir
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitorInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
    delete:
      operationId: quality_monitors_delete
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Deletes a monitor
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Deletes a monitor

        for the specified table.


        The caller must either:


        1. be an owner of the table's parent catalog

        2. have **USE_CATALOG** on the table's parent catalog and be an owner of
        the table's parent schema

        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - be an owner of the table.


        Additionally, the call must be made from the workspace where the monitor
        was created.


        Note that the metric tables and dashboard will not be deleted as part of
        this call; those assets must

        be manually cleaned up (if desired).


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. This field corresponds to the
          {full_table_name_arg} arg in the endpoint path.

        :returns: :class:`DeleteMonitorResponse`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. This field
            corresponds to the {full_table_name_arg} arg in the endpoint path.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteMonitorResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
    get:
      operationId: quality_monitors_get
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Gets a monitor for
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Gets a monitor for

        the specified table.


        The caller must either:


        1. be an owner of the table's parent catalog

        2. have **USE_CATALOG** on the table's parent catalog and be an owner of
        the table's parent schema.

        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - **SELECT** privilege on the table.


        The returned information includes configuration values, as well as
        information on assets created by

        the monitor. Some information (e.g., dashboard) may be filtered out if
        the caller is in a different

        workspace than where the monitor was created.


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. This field corresponds to the
          {full_table_name_arg} arg in the endpoint path.

        :returns: :class:`MonitorInfo`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. This field
            corresponds to the {full_table_name_arg} arg in the endpoint path.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitorInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
    put:
      operationId: quality_monitors_update
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Updates a monitor
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Updates a monitor

        for the specified table.


        The caller must either:


        1. be an owner of the table's parent catalog

        2. have **USE_CATALOG** on the table's parent catalog and be an owner of
        the table's parent schema

        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - be an owner of the table.


        Additionally, the call must be made from the workspace where the monitor
        was created, and the caller

        must be the original creator of the monitor.


        Certain configuration fields, such as output asset identifiers, cannot
        be updated.


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. This field corresponds to the
          {full_table_name_arg} arg in the endpoint path.
        :param output_schema_name: str
          [Create:REQ Update:REQ] Schema where output tables are created. Needs to be in 2-level format
          {catalog}.{schema}
        :param baseline_table_name: str (optional)
          [Create:OPT Update:OPT] Baseline table name. Baseline data is used to compute drift from the data in
          the monitored ``table_name``. The baseline table and the monitored table shall have the same schema.
        :param custom_metrics: List[:class:`MonitorMetric`] (optional)
          [Create:OPT Update:OPT] Custom metrics.
        :param dashboard_id: str (optional)
          [Create:ERR Update:OPT] Id of dashboard that visualizes the computed metrics. This can be empty if
          the monitor is in PENDING state.
        :param data_classification_config:
        :class:`MonitorDataClassificationConfig` (optional)
          [Create:OPT Update:OPT] Data classification related config.
        :param inference_log: :class:`MonitorInferenceLog` (optional)

        :param latest_monitor_failure_msg: str (optional)
          [Create:ERR Update:IGN] The latest error message for a monitor failure.
        :param notifications: :class:`MonitorNotifications` (optional)
          [Create:OPT Update:OPT] Field for specifying notification settings.
        :param schedule: :class:`MonitorCronSchedule` (optional)
          [Create:OPT Update:OPT] The monitor schedule.
        :param slicing_exprs: List[str] (optional)
          [Create:OPT Update:OPT] List of column expressions to slice data with for targeted analysis. The
          data is grouped by each expression independently, resulting in a separate slice for each predicate
          and its complements. For example ``slicing_exprs=[“col_1”, “col_2 > 10”]`` will generate the
          following slices: two slices for ``col_2 > 10`` (True and False), and one slice per unique value in
          ``col1``. For high-cardinality columns, only the top 100 unique values by frequency will generate
          slices.
        :param snapshot: :class:`MonitorSnapshot` (optional)
          Configuration for monitoring snapshot tables.
        :param time_series: :class:`MonitorTimeSeries` (optional)
          Configuration for monitoring time series tables.

        :returns: :class:`MonitorInfo`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. This field
            corresponds to the {full_table_name_arg} arg in the endpoint path.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                output_schema_name:
                  type: string
                  description: >-
                    [Create:REQ Update:REQ] Schema where output tables are
                    created. Needs to be in 2-level format {catalog}.{schema}
                baseline_table_name:
                  type: string
                  description: >-
                    [Create:OPT Update:OPT] Baseline table name. Baseline data
                    is used to compute drift from the data in the monitored
                    ``table_name``. The baseline table and the monitored table
                    shall have the same schema.
                custom_metrics:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: Name of the metric in the output tables.
                      definition:
                        type: string
                        description: >-
                          Jinja template for a SQL expression that specifies how
                          to compute the metric. See `create metric definition
                          <https://docs.databricks.com/en/lakehouse-monitoring/custom-metrics.html#create-definition>`__.
                      input_columns:
                        type: array
                        items:
                          type: string
                        description: >-
                          A list of column names in the input table the metric
                          should be computed for. Can use ``":table"`` to
                          indicate that the metric needs information from
                          multiple columns.
                      output_data_type:
                        type: string
                        description: The output type of the custom metric.
                      type:
                        type: string
                        x-enum:
                          - CUSTOM_METRIC_TYPE_AGGREGATE
                          - CUSTOM_METRIC_TYPE_DERIVED
                          - CUSTOM_METRIC_TYPE_DRIFT
                        description: >-
                          Can only be one of ``"CUSTOM_METRIC_TYPE_AGGREGATE"``,
                          ``"CUSTOM_METRIC_TYPE_DERIVED"``, or
                          ``"CUSTOM_METRIC_TYPE_DRIFT"``. The
                          ``"CUSTOM_METRIC_TYPE_AGGREGATE"`` and
                          ``"CUSTOM_METRIC_TYPE_DERIVED"`` metrics are computed
                          on a single table, whereas the
                          ``"CUSTOM_METRIC_TYPE_DRIFT"`` compare metrics across
                          baseline and input table, or across the two
                          consecutive time windows. -
                          CUSTOM_METRIC_TYPE_AGGREGATE: only depend on the
                          existing columns in your table -
                          CUSTOM_METRIC_TYPE_DERIVED: depend on previously
                          computed aggregate metrics - CUSTOM_METRIC_TYPE_DRIFT:
                          depend on previously computed aggregate or derived
                          metrics
                    required:
                      - name
                      - definition
                      - input_columns
                      - output_data_type
                      - type
                    description: Custom metric definition.
                  description: '[Create:OPT Update:OPT] Custom metrics.'
                dashboard_id:
                  type: string
                  description: >-
                    [Create:ERR Update:OPT] Id of dashboard that visualizes the
                    computed metrics. This can be empty if the monitor is in
                    PENDING state.
                data_classification_config:
                  type: object
                  properties:
                    enabled:
                      type: boolean
                      description: Whether to enable data classification.
                  description: '[Create:OPT Update:OPT] Data classification related config.'
                inference_log:
                  type: object
                  properties:
                    problem_type:
                      type: string
                      x-enum:
                        - PROBLEM_TYPE_CLASSIFICATION
                        - PROBLEM_TYPE_REGRESSION
                      description: >-
                        Create a collection of name/value pairs.


                        Example enumeration:


                        >>> class Color(Enum):

                        ...     RED = 1

                        ...     BLUE = 2

                        ...     GREEN = 3


                        Access them by:


                        - attribute access:

                          >>> Color.RED
                          <Color.RED: 1>

                        - value lookup:

                          >>> Color(1)
                          <Color.RED: 1>

                        - name lookup:

                          >>> Color['RED']
                          <Color.RED: 1>

                        Enumerations can be iterated over, and know how many
                        members they have:


                        >>> len(Color)

                        3


                        >>> list(Color)

                        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                        Methods can be added to enumerations, and members can
                        have their own

                        attributes -- see the documentation for details.
                    timestamp_col:
                      type: string
                      description: Column for the timestamp.
                    granularities:
                      type: array
                      items:
                        type: string
                      description: >-
                        List of granularities to use when aggregating data into
                        time windows based on their timestamp.
                    prediction_col:
                      type: string
                      description: Column for the prediction.
                    model_id_col:
                      type: string
                      description: Column for the model identifier.
                    label_col:
                      type: string
                      description: Column for the label.
                    prediction_proba_col:
                      type: string
                      description: Column for prediction probabilities
                  required:
                    - problem_type
                    - timestamp_col
                    - granularities
                    - prediction_col
                    - model_id_col
                latest_monitor_failure_msg:
                  type: string
                  description: >-
                    [Create:ERR Update:IGN] The latest error message for a
                    monitor failure.
                notifications:
                  type: object
                  properties:
                    on_failure:
                      type: object
                      properties:
                        email_addresses:
                          type: array
                          items:
                            type: string
                    on_new_classification_tag_detected:
                      type: object
                      properties:
                        email_addresses:
                          type: array
                          items:
                            type: string
                      description: >-
                        Destinations to send notifications on new classification
                        tag detected.
                  description: >-
                    [Create:OPT Update:OPT] Field for specifying notification
                    settings.
                schedule:
                  type: object
                  properties:
                    quartz_cron_expression:
                      type: string
                    timezone_id:
                      type: string
                      description: >-
                        The timezone id (e.g., ``PST``) in which to evaluate the
                        quartz expression.
                    pause_status:
                      type: string
                      x-enum:
                        - PAUSED
                        - UNPAUSED
                        - UNSPECIFIED
                      description: >-
                        Read only field that indicates whether a schedule is
                        paused or not.
                  required:
                    - quartz_cron_expression
                    - timezone_id
                  description: '[Create:OPT Update:OPT] The monitor schedule.'
                slicing_exprs:
                  type: array
                  items:
                    type: string
                  description: >-
                    [Create:OPT Update:OPT] List of column expressions to slice
                    data with for targeted analysis. The data is grouped by each
                    expression independently, resulting in a separate slice for
                    each predicate and its complements. For example
                    ``slicing_exprs=[“col_1”, “col_2 > 10”]`` will generate the
                    following slices: two slices for ``col_2 > 10`` (True and
                    False), and one slice per unique value in ``col1``. For
                    high-cardinality columns, only the top 100 unique values by
                    frequency will generate slices.
                snapshot:
                  type: object
                  properties: {}
                  description: Configuration for monitoring snapshot tables.
                time_series:
                  type: object
                  properties:
                    timestamp_col:
                      type: string
                      description: Column for the timestamp.
                    granularities:
                      type: array
                      items:
                        type: string
                      description: >-
                        Granularities for aggregating data into time windows
                        based on their timestamp. Currently the following static
                        granularities are supported: {``\"5 minutes\"``, ``\"30
                        minutes\"``, ``\"1 hour\"``, ``\"1 day\"``,
                        ``\"\u003cn\u003e week(s)\"``, ``\"1 month\"``, ``\"1
                        year\"``}.
                  required:
                    - timestamp_col
                    - granularities
                  description: Configuration for monitoring time series tables.
              required:
                - output_schema_name
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitorInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
  /api/2.1/unity-catalog/tables/{table_name}/monitor/refreshes/{refresh_id}:
    get:
      operationId: quality_monitors_get_refresh
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Gets info about a
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Gets info about a

        specific monitor refresh using the given refresh ID.


        The caller must either:


        1. be an owner of the table's parent catalog

        2. have **USE_CATALOG** on the table's parent catalog and be an owner of
        the table's parent schema

        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - **SELECT** privilege on the table.


        Additionally, the call must be made from the workspace where the monitor
        was created.


        :param table_name: str
          Full name of the table.
        :param refresh_id: int
          ID of the refresh.

        :returns: :class:`MonitorRefreshInfo`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the table.
        - name: refresh_id
          in: path
          required: true
          schema:
            type: integer
          description: ID of the refresh.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitorRefreshInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
  /api/2.1/unity-catalog/tables/{table_name}/monitor/refreshes:
    get:
      operationId: quality_monitors_list_refreshes
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Gets an array
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Gets an array

        containing the history of the most recent refreshes (up to 25) for this
        table.


        The caller must either:


        1. be an owner of the table's parent catalog

        2. have **USE_CATALOG** on the table's parent catalog and be an owner of
        the table's parent schema

        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - **SELECT** privilege on the table.


        Additionally, the call must be made from the workspace where the monitor
        was created.


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. table_name is case insensitive and spaces are
          disallowed.

        :returns: :class:`MonitorRefreshListResponse`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. table_name is
            case insensitive and spaces are disallowed.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitorRefreshListResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
    post:
      operationId: quality_monitors_run_refresh
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Queues a metric
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Queues a metric

        refresh on the monitor for the specified table. The refresh will execute
        in the background.


        The caller must either:


        1. be an owner of the table's parent catalog

        2. have **USE_CATALOG** on the table's parent catalog and be an owner of
        the table's parent schema

        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - be an owner of the table


        Additionally, the call must be made from the workspace where the monitor
        was created.


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. table_name is case insensitive and spaces are
          disallowed.

        :returns: :class:`MonitorRefreshInfo`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. table_name is
            case insensitive and spaces are disallowed.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitorRefreshInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
  /api/2.1/quality-monitoring/tables/{table_name}/monitor/dashboard:
    post:
      operationId: quality_monitors_regenerate_dashboard
      summary: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Regenerates the
      tags:
        - catalog
        - quality_monitors
      description: >-
        Deprecated: Use Data Quality Monitors API instead
        (/api/data-quality/v1/monitors). Regenerates the

        monitoring dashboard for the specified table.


        The caller must either:


        1. be an owner of the table's parent catalog

        2. have **USE_CATALOG** on the table's parent catalog and be an owner of
        the table's parent schema

        3. have the following permissions:


        - **USE_CATALOG** on the table's parent catalog

        - **USE_SCHEMA** on the table's parent schema

        - be an owner of the table


        The call must be made from the workspace where the monitor was created.
        The dashboard will be

        regenerated in the assets directory that was specified when the monitor
        was created.


        :param table_name: str
          UC table name in format ``catalog.schema.table_name``. This field corresponds to the
          {full_table_name_arg} arg in the endpoint path.
        :param warehouse_id: str (optional)
          Optional argument to specify the warehouse for dashboard regeneration. If not specified, the first
          running warehouse will be used.

        :returns: :class:`RegenerateDashboardResponse`
      parameters:
        - name: table_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            UC table name in format ``catalog.schema.table_name``. This field
            corresponds to the {full_table_name_arg} arg in the endpoint path.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                warehouse_id:
                  type: string
                  description: >-
                    Optional argument to specify the warehouse for dashboard
                    regeneration. If not specified, the first running warehouse
                    will be used.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegenerateDashboardResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: QualityMonitorsAPI
  /api/2.1/unity-catalog/models:
    post:
      operationId: registered_models_create
      summary: Creates a new registered model in Unity Catalog.
      tags:
        - catalog
        - registered_models
      description: >-
        Creates a new registered model in Unity Catalog.


        File storage for model versions in the registered model will be located
        in the default location which

        is specified by the parent schema, or the parent catalog, or the
        Metastore.


        For registered model creation to succeed, the user must satisfy the
        following conditions:


        - The caller must be a metastore admin, or be the owner of the parent
        catalog and schema, or have the
          **USE_CATALOG** privilege on the parent catalog and the **USE_SCHEMA** privilege on the parent
          schema.
        - The caller must have the **CREATE MODEL** or **CREATE FUNCTION**
        privilege on the parent schema.


        :param aliases: List[:class:`RegisteredModelAlias`] (optional)
          List of aliases associated with the registered model
        :param browse_only: bool (optional)
          Indicates whether the principal is limited to retrieving metadata for the associated object through
          the BROWSE privilege when include_browse is enabled in the request.
        :param catalog_name: str (optional)
          The name of the catalog where the schema and the registered model reside
        :param comment: str (optional)
          The comment attached to the registered model
        :param created_at: int (optional)
          Creation timestamp of the registered model in milliseconds since the Unix epoch
        :param created_by: str (optional)
          The identifier of the user who created the registered model
        :param full_name: str (optional)
          The three-level (fully qualified) name of the registered model
        :param metastore_id: str (optional)
          The unique identifier of the metastore
        :param name: str (optional)
          The name of the registered model
        :param owner: str (optional)
          The identifier of the user who owns the registered model
        :param schema_name: str (optional)
          The name of the schema where the registered model resides
        :param storage_location: str (optional)
          The storage location on the cloud under which model version data files are stored
        :param updated_at: int (optional)
          Last-update timestamp of the registered model in milliseconds since the Unix epoch
        :param updated_by: str (optional)
          The identifier of the user who updated the registered model last time

        :returns: :class:`RegisteredModelInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                aliases:
                  type: array
                  items:
                    type: object
                    properties:
                      alias_name:
                        type: string
                      catalog_name:
                        type: string
                        description: The name of the catalog containing the model version
                      id:
                        type: string
                        description: The unique identifier of the alias
                      model_name:
                        type: string
                        description: >-
                          The name of the parent registered model of the model
                          version, relative to parent schema
                      schema_name:
                        type: string
                        description: >-
                          The name of the schema containing the model version,
                          relative to parent catalog
                      version_num:
                        type: integer
                        description: >-
                          Integer version number of the model version to which
                          this alias points.
                  description: List of aliases associated with the registered model
                browse_only:
                  type: boolean
                  description: >-
                    Indicates whether the principal is limited to retrieving
                    metadata for the associated object through the BROWSE
                    privilege when include_browse is enabled in the request.
                catalog_name:
                  type: string
                  description: >-
                    The name of the catalog where the schema and the registered
                    model reside
                comment:
                  type: string
                  description: The comment attached to the registered model
                created_at:
                  type: integer
                  description: >-
                    Creation timestamp of the registered model in milliseconds
                    since the Unix epoch
                created_by:
                  type: string
                  description: The identifier of the user who created the registered model
                full_name:
                  type: string
                  description: >-
                    The three-level (fully qualified) name of the registered
                    model
                metastore_id:
                  type: string
                  description: The unique identifier of the metastore
                name:
                  type: string
                  description: The name of the registered model
                owner:
                  type: string
                  description: The identifier of the user who owns the registered model
                schema_name:
                  type: string
                  description: The name of the schema where the registered model resides
                storage_location:
                  type: string
                  description: >-
                    The storage location on the cloud under which model version
                    data files are stored
                updated_at:
                  type: integer
                  description: >-
                    Last-update timestamp of the registered model in
                    milliseconds since the Unix epoch
                updated_by:
                  type: string
                  description: >-
                    The identifier of the user who updated the registered model
                    last time
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisteredModelInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RegisteredModelsAPI
    get:
      operationId: registered_models_list
      summary: >-
        List registered models. You can list registered models under a
        particular schema, or list all
      tags:
        - catalog
        - registered_models
      description: >-
        List registered models. You can list registered models under a
        particular schema, or list all

        registered models in the current metastore.


        The returned models are filtered based on the privileges of the calling
        user. For example, the

        metastore admin is able to list all the registered models. A regular
        user needs to be the owner or

        have the **EXECUTE** privilege on the registered model to receive the
        registered models in the

        response. For the latter case, the caller must also be the owner or have
        the **USE_CATALOG** privilege

        on the parent catalog and the **USE_SCHEMA** privilege on the parent
        schema.


        There is no guarantee of a specific ordering of the elements in the
        response.


        PAGINATION BEHAVIOR: The API is by default paginated, a page may contain
        zero results while still

        providing a next_page_token. Clients must continue reading pages until
        next_page_token is absent,

        which is the only indication that the end of results has been reached.


        :param catalog_name: str (optional)
          The identifier of the catalog under which to list registered models. If specified, schema_name must
          be specified.
        :param include_browse: bool (optional)
          Whether to include registered models in the response for which the principal can only access
          selective metadata for
        :param max_results: int (optional)
          Max number of registered models to return.

          If both catalog and schema are specified:

          - when max_results is not specified, the page length is set to a server configured value (10000, as
            of 4/2/2024).
          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value (10000, as of 4/2/2024);
          - when set to 0, the page length is set to a server configured value (10000, as of 4/2/2024);
          - when set to a value less than 0, an invalid parameter error is returned;

          If neither schema nor catalog is specified:

          - when max_results is not specified, the page length is set to a server configured value (100, as of
            4/2/2024).
          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value (1000, as of 4/2/2024);
          - when set to 0, the page length is set to a server configured value (100, as of 4/2/2024);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque token to send for the next page of results (pagination).
        :param schema_name: str (optional)
          The identifier of the schema under which to list registered models. If specified, catalog_name must
          be specified.

        :returns: Iterator over :class:`RegisteredModelInfo`
      parameters:
        - name: catalog_name
          in: query
          required: false
          schema:
            type: string
          description: >-
            The identifier of the catalog under which to list registered models.
            If specified, schema_name must be specified.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include registered models in the response for which the
            principal can only access selective metadata for
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Max number of registered models to return. If both catalog and
            schema are specified: - when max_results is not specified, the page
            length is set to a server configured value (10000, as of 4/2/2024).
            - when set to a value greater than 0, the page length is the minimum
            of this value and a server configured value (10000, as of 4/2/2024);
            - when set to 0, the page length is set to a server configured value
            (10000, as of 4/2/2024); - when set to a value less than 0, an
            invalid parameter error is returned; If neither schema nor catalog
            is specified: - when max_results is not specified, the page length
            is set to a server configured value (100, as of 4/2/2024). - when
            set to a value greater than 0, the page length is the minimum of
            this value and a server configured value (1000, as of 4/2/2024); -
            when set to 0, the page length is set to a server configured value
            (100, as of 4/2/2024); - when set to a value less than 0, an invalid
            parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque token to send for the next page of results (pagination).
        - name: schema_name
          in: query
          required: false
          schema:
            type: string
          description: >-
            The identifier of the schema under which to list registered models.
            If specified, catalog_name must be specified.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListRegisteredModelsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RegisteredModelsAPI
  /api/2.1/unity-catalog/models/{full_name}:
    delete:
      operationId: registered_models_delete
      summary: >-
        Deletes a registered model and all its model versions from the specified
        parent catalog and schema.
      tags:
        - catalog
        - registered_models
      description: >-
        Deletes a registered model and all its model versions from the specified
        parent catalog and schema.


        The caller must be a metastore admin or an owner of the registered
        model. For the latter case, the

        caller must also be the owner or have the **USE_CATALOG** privilege on
        the parent catalog and the

        **USE_SCHEMA** privilege on the parent schema.


        :param full_name: str
          The three-level (fully qualified) name of the registered model
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the registered model
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RegisteredModelsAPI
    get:
      operationId: registered_models_get
      summary: Get a registered model.
      tags:
        - catalog
        - registered_models
      description: >-
        Get a registered model.


        The caller must be a metastore admin or an owner of (or have the
        **EXECUTE** privilege on) the

        registered model. For the latter case, the caller must also be the owner
        or have the **USE_CATALOG**

        privilege on the parent catalog and the **USE_SCHEMA** privilege on the
        parent schema.


        :param full_name: str
          The three-level (fully qualified) name of the registered model
        :param include_aliases: bool (optional)
          Whether to include registered model aliases in the response
        :param include_browse: bool (optional)
          Whether to include registered models in the response for which the principal can only access
          selective metadata for

        :returns: :class:`RegisteredModelInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the registered model
        - name: include_aliases
          in: query
          required: false
          schema:
            type: boolean
          description: Whether to include registered model aliases in the response
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include registered models in the response for which the
            principal can only access selective metadata for
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisteredModelInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RegisteredModelsAPI
    patch:
      operationId: registered_models_update
      summary: Updates the specified registered model.
      tags:
        - catalog
        - registered_models
      description: >-
        Updates the specified registered model.


        The caller must be a metastore admin or an owner of the registered
        model. For the latter case, the

        caller must also be the owner or have the **USE_CATALOG** privilege on
        the parent catalog and the

        **USE_SCHEMA** privilege on the parent schema.


        Currently only the name, the owner or the comment of the registered
        model can be updated.


        :param full_name: str
          The three-level (fully qualified) name of the registered model
        :param aliases: List[:class:`RegisteredModelAlias`] (optional)
          List of aliases associated with the registered model
        :param browse_only: bool (optional)
          Indicates whether the principal is limited to retrieving metadata for the associated object through
          the BROWSE privilege when include_browse is enabled in the request.
        :param catalog_name: str (optional)
          The name of the catalog where the schema and the registered model reside
        :param comment: str (optional)
          The comment attached to the registered model
        :param created_at: int (optional)
          Creation timestamp of the registered model in milliseconds since the Unix epoch
        :param created_by: str (optional)
          The identifier of the user who created the registered model
        :param metastore_id: str (optional)
          The unique identifier of the metastore
        :param name: str (optional)
          The name of the registered model
        :param new_name: str (optional)
          New name for the registered model.
        :param owner: str (optional)
          The identifier of the user who owns the registered model
        :param schema_name: str (optional)
          The name of the schema where the registered model resides
        :param storage_location: str (optional)
          The storage location on the cloud under which model version data files are stored
        :param updated_at: int (optional)
          Last-update timestamp of the registered model in milliseconds since the Unix epoch
        :param updated_by: str (optional)
          The identifier of the user who updated the registered model last time

        :returns: :class:`RegisteredModelInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the registered model
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                aliases:
                  type: array
                  items:
                    type: object
                    properties:
                      alias_name:
                        type: string
                      catalog_name:
                        type: string
                        description: The name of the catalog containing the model version
                      id:
                        type: string
                        description: The unique identifier of the alias
                      model_name:
                        type: string
                        description: >-
                          The name of the parent registered model of the model
                          version, relative to parent schema
                      schema_name:
                        type: string
                        description: >-
                          The name of the schema containing the model version,
                          relative to parent catalog
                      version_num:
                        type: integer
                        description: >-
                          Integer version number of the model version to which
                          this alias points.
                  description: List of aliases associated with the registered model
                browse_only:
                  type: boolean
                  description: >-
                    Indicates whether the principal is limited to retrieving
                    metadata for the associated object through the BROWSE
                    privilege when include_browse is enabled in the request.
                catalog_name:
                  type: string
                  description: >-
                    The name of the catalog where the schema and the registered
                    model reside
                comment:
                  type: string
                  description: The comment attached to the registered model
                created_at:
                  type: integer
                  description: >-
                    Creation timestamp of the registered model in milliseconds
                    since the Unix epoch
                created_by:
                  type: string
                  description: The identifier of the user who created the registered model
                metastore_id:
                  type: string
                  description: The unique identifier of the metastore
                name:
                  type: string
                  description: The name of the registered model
                new_name:
                  type: string
                  description: New name for the registered model.
                owner:
                  type: string
                  description: The identifier of the user who owns the registered model
                schema_name:
                  type: string
                  description: The name of the schema where the registered model resides
                storage_location:
                  type: string
                  description: >-
                    The storage location on the cloud under which model version
                    data files are stored
                updated_at:
                  type: integer
                  description: >-
                    Last-update timestamp of the registered model in
                    milliseconds since the Unix epoch
                updated_by:
                  type: string
                  description: >-
                    The identifier of the user who updated the registered model
                    last time
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisteredModelInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RegisteredModelsAPI
  /api/2.1/unity-catalog/resource-quotas/{parent_securable_type}/{parent_full_name}/{quota_name}:
    get:
      operationId: resource_quotas_get_quota
      summary: >-
        The GetQuota API returns usage information for a single resource quota,
        defined as a child-parent
      tags:
        - catalog
        - resource_quotas
      description: >-
        The GetQuota API returns usage information for a single resource quota,
        defined as a child-parent

        pair. This API also refreshes the quota count if it is out of date.
        Refreshes are triggered

        asynchronously. The updated count might not be returned in the first
        call.


        :param parent_securable_type: str
          Securable type of the quota parent.
        :param parent_full_name: str
          Full name of the parent resource. Provide the metastore ID if the parent is a metastore.
        :param quota_name: str
          Name of the quota. Follows the pattern of the quota type, with "-quota" added as a suffix.

        :returns: :class:`GetQuotaResponse`
      parameters:
        - name: parent_securable_type
          in: path
          required: true
          schema:
            type: string
          description: Securable type of the quota parent.
        - name: parent_full_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            Full name of the parent resource. Provide the metastore ID if the
            parent is a metastore.
        - name: quota_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            Name of the quota. Follows the pattern of the quota type, with
            "-quota" added as a suffix.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetQuotaResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ResourceQuotasAPI
  /api/2.1/unity-catalog/resource-quotas/all-resource-quotas:
    get:
      operationId: resource_quotas_list_quotas
      summary: >-
        ListQuotas returns all quota values under the metastore. There are no
        SLAs on the freshness of the
      tags:
        - catalog
        - resource_quotas
      description: >-
        ListQuotas returns all quota values under the metastore. There are no
        SLAs on the freshness of the

        counts returned. This API does not trigger a refresh of quota counts.


        PAGINATION BEHAVIOR: The API is by default paginated, a page may contain
        zero results while still

        providing a next_page_token. Clients must continue reading pages until
        next_page_token is absent,

        which is the only indication that the end of results has been reached.


        :param max_results: int (optional)
          The number of quotas to return.
        :param page_token: str (optional)
          Opaque token for the next page of results.

        :returns: Iterator over :class:`QuotaInfo`
      parameters:
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: The number of quotas to return.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque token for the next page of results.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListQuotasResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ResourceQuotasAPI
  /api/3.0/rfa/requests:
    post:
      operationId: rfa_batch_create_access_requests
      summary: >-
        Creates access requests for Unity Catalog permissions for a specified
        principal on a securable object.
      tags:
        - catalog
        - rfa
      description: >-
        Creates access requests for Unity Catalog permissions for a specified
        principal on a securable object.

        This Batch API can take in multiple principals, securable objects, and
        permissions as the input and

        returns the access request destinations for each. Principals must be
        unique across the API call.


        The supported securable types are: "metastore", "catalog", "schema",
        "table", "external_location",

        "connection", "credential", "function", "registered_model", and
        "volume".


        :param requests: List[:class:`CreateAccessRequest`] (optional)
          A list of individual access requests, where each request corresponds to a set of permissions being
          requested on a list of securables for a specified principal.

          At most 30 requests per API call.

        :returns: :class:`BatchCreateAccessRequestsResponse`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                requests:
                  type: array
                  items:
                    type: object
                    properties:
                      behalf_of:
                        type: object
                        properties:
                          id:
                            type: string
                          principal_type:
                            type: string
                            x-enum:
                              - GROUP_PRINCIPAL
                              - SERVICE_PRINCIPAL
                              - USER_PRINCIPAL
                            description: >-
                              Create a collection of name/value pairs.


                              Example enumeration:


                              >>> class Color(Enum):

                              ...     RED = 1

                              ...     BLUE = 2

                              ...     GREEN = 3


                              Access them by:


                              - attribute access:

                                >>> Color.RED
                                <Color.RED: 1>

                              - value lookup:

                                >>> Color(1)
                                <Color.RED: 1>

                              - name lookup:

                                >>> Color['RED']
                                <Color.RED: 1>

                              Enumerations can be iterated over, and know how
                              many members they have:


                              >>> len(Color)

                              3


                              >>> list(Color)

                              [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN:
                              3>]


                              Methods can be added to enumerations, and members
                              can have their own

                              attributes -- see the documentation for details.
                      comment:
                        type: string
                        description: >-
                          Optional. Comment associated with the request. At most
                          200 characters, can only contain lowercase/uppercase
                          letters (a-z, A-Z), numbers (0-9), punctuation, and
                          spaces.
                      securable_permissions:
                        type: array
                        items:
                          type: object
                          properties:
                            permissions:
                              type: array
                              items:
                                type: string
                            securable:
                              type: object
                              properties:
                                full_name:
                                  type: string
                                  description: >-
                                    Required. The full name of the
                                    catalog/schema/table. Optional if
                                    resource_name is present.
                                provider_share:
                                  type: string
                                  description: >-
                                    Optional. The name of the Share object that
                                    contains the securable when the securable is
                                    getting shared in D2D Delta Sharing.
                                type:
                                  type: string
                                  x-enum:
                                    - CATALOG
                                    - CLEAN_ROOM
                                    - CONNECTION
                                    - CREDENTIAL
                                    - EXTERNAL_LOCATION
                                    - EXTERNAL_METADATA
                                    - FUNCTION
                                    - METASTORE
                                    - PIPELINE
                                    - PROVIDER
                                    - RECIPIENT
                                    - SCHEMA
                                    - SHARE
                                    - STAGING_TABLE
                                    - STORAGE_CREDENTIAL
                                    - TABLE
                                    - VOLUME
                                  description: >-
                                    Required. The type of securable
                                    (catalog/schema/table). Optional if
                                    resource_name is present.
                              description: >-
                                The securable for which the access request
                                destinations are being requested.
                        description: >-
                          List of securables and their corresponding requested
                          UC privileges. At most 30 securables can be requested
                          for a principal per batched call. Each securable can
                          only be requested once per principal.
                  description: >-
                    A list of individual access requests, where each request
                    corresponds to a set of permissions being requested on a
                    list of securables for a specified principal. At most 30
                    requests per API call.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchCreateAccessRequestsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RfaAPI
  /api/3.0/rfa/destinations/{securable_type}/{full_name}:
    get:
      operationId: rfa_get_access_request_destinations
      summary: >-
        Gets an array of access request destinations for the specified
        securable. Any caller can see URL
      tags:
        - catalog
        - rfa
      description: >-
        Gets an array of access request destinations for the specified
        securable. Any caller can see URL

        destinations or the destinations on the metastore. Otherwise, only those
        with **BROWSE** permissions

        on the securable can see destinations.


        The supported securable types are: "metastore", "catalog", "schema",
        "table", "external_location",

        "connection", "credential", "function", "registered_model", and
        "volume".


        :param securable_type: str
          The type of the securable.
        :param full_name: str
          The full name of the securable.

        :returns: :class:`AccessRequestDestinations`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: The type of the securable.
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: The full name of the securable.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestDestinations'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RfaAPI
  /api/3.0/rfa/destinations:
    patch:
      operationId: rfa_update_access_request_destinations
      summary: >-
        Updates the access request destinations for the given securable. The
        caller must be a metastore admin,
      tags:
        - catalog
        - rfa
      description: >-
        Updates the access request destinations for the given securable. The
        caller must be a metastore admin,

        the owner of the securable, or a user that has the **MANAGE** privilege
        on the securable in order to

        assign destinations. A maximum of 5 emails and 5 external notification
        destinations (Slack, Microsoft

        Teams, and Generic Webhook destinations) can be assigned to a securable.
        If a URL destination is

        assigned, no other destinations can be set.


        The supported securable types are: "metastore", "catalog", "schema",
        "table", "external_location",

        "connection", "credential", "function", "registered_model", and
        "volume".


        :param access_request_destinations: :class:`AccessRequestDestinations`
          The access request destinations to assign to the securable. For each destination, a
          **destination_id** and **destination_type** must be defined.
        :param update_mask: str
          The field mask must be a single string, with multiple fields separated by commas (no spaces). The
          field path is relative to the resource object, using a dot (``.``) to navigate sub-fields (e.g.,
          ``author.given_name``). Specification of elements in sequence or map fields is not allowed, as only
          the entire collection field can be specified. Field names must exactly match the resource field
          names.

          A field mask of ``*`` indicates full replacement. It’s recommended to always explicitly list the
          fields being updated and avoid using ``*`` wildcards, as it can lead to unintended results if the
          API changes in the future.

        :returns: :class:`AccessRequestDestinations`
      parameters:
        - name: update_mask
          in: query
          required: true
          schema:
            type: string
          description: >-
            The field mask must be a single string, with multiple fields
            separated by commas (no spaces). The field path is relative to the
            resource object, using a dot (``.``) to navigate sub-fields (e.g.,
            ``author.given_name``). Specification of elements in sequence or map
            fields is not allowed, as only the entire collection field can be
            specified. Field names must exactly match the resource field names.
            A field mask of ``*`` indicates full replacement. It’s recommended
            to always explicitly list the fields being updated and avoid using
            ``*`` wildcards, as it can lead to unintended results if the API
            changes in the future.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                access_request_destinations:
                  type: object
                  properties:
                    securable:
                      type: object
                      properties:
                        full_name:
                          type: string
                          description: >-
                            Required. The full name of the catalog/schema/table.
                            Optional if resource_name is present.
                        provider_share:
                          type: string
                          description: >-
                            Optional. The name of the Share object that contains
                            the securable when the securable is getting shared
                            in D2D Delta Sharing.
                        type:
                          type: string
                          x-enum:
                            - CATALOG
                            - CLEAN_ROOM
                            - CONNECTION
                            - CREDENTIAL
                            - EXTERNAL_LOCATION
                            - EXTERNAL_METADATA
                            - FUNCTION
                            - METASTORE
                            - PIPELINE
                            - PROVIDER
                            - RECIPIENT
                            - SCHEMA
                            - SHARE
                            - STAGING_TABLE
                            - STORAGE_CREDENTIAL
                            - TABLE
                            - VOLUME
                          description: >-
                            Required. The type of securable
                            (catalog/schema/table). Optional if resource_name is
                            present.
                      description: >-
                        Generic definition of a securable, which is uniquely
                        defined in a metastore by its type and full
                            name.
                    are_any_destinations_hidden:
                      type: boolean
                      description: >-
                        Indicates whether any destinations are hidden from the
                        caller due to a lack of permissions. This value is true
                        if the caller does not have permission to see all
                        destinations.
                    destination_source_securable:
                      type: object
                      properties:
                        full_name:
                          type: string
                          description: >-
                            Required. The full name of the catalog/schema/table.
                            Optional if resource_name is present.
                        provider_share:
                          type: string
                          description: >-
                            Optional. The name of the Share object that contains
                            the securable when the securable is getting shared
                            in D2D Delta Sharing.
                        type:
                          type: string
                          x-enum:
                            - CATALOG
                            - CLEAN_ROOM
                            - CONNECTION
                            - CREDENTIAL
                            - EXTERNAL_LOCATION
                            - EXTERNAL_METADATA
                            - FUNCTION
                            - METASTORE
                            - PIPELINE
                            - PROVIDER
                            - RECIPIENT
                            - SCHEMA
                            - SHARE
                            - STAGING_TABLE
                            - STORAGE_CREDENTIAL
                            - TABLE
                            - VOLUME
                          description: >-
                            Required. The type of securable
                            (catalog/schema/table). Optional if resource_name is
                            present.
                      description: >-
                        The source securable from which the destinations are
                        inherited. Either the same value as securable (if
                        destination is set directly on the securable) or the
                        nearest parent securable with destinations set.
                    destinations:
                      type: array
                      items:
                        type: object
                        properties:
                          destination_id:
                            type: string
                          destination_type:
                            type: string
                            x-enum:
                              - EMAIL
                              - GENERIC_WEBHOOK
                              - MICROSOFT_TEAMS
                              - SLACK
                              - URL
                            description: The type of the destination.
                          special_destination:
                            type: string
                            x-enum:
                              - SPECIAL_DESTINATION_CATALOG_OWNER
                              - SPECIAL_DESTINATION_CONNECTION_OWNER
                              - SPECIAL_DESTINATION_CREDENTIAL_OWNER
                              - SPECIAL_DESTINATION_EXTERNAL_LOCATION_OWNER
                              - SPECIAL_DESTINATION_FUNCTION_OWNER
                              - SPECIAL_DESTINATION_METASTORE_OWNER
                              - SPECIAL_DESTINATION_REGISTERED_MODEL_OWNER
                              - SPECIAL_DESTINATION_SCHEMA_OWNER
                              - SPECIAL_DESTINATION_TABLE_OWNER
                              - SPECIAL_DESTINATION_VOLUME_OWNER
                            description: >-
                              This field is used to denote whether the
                              destination is the email of the owner of the
                              securable object. The special destination cannot
                              be assigned to a securable and only represents the
                              default destination of the securable. The
                              securable types that support default special
                              destinations are: "catalog", "external_location",
                              "connection", "credential", and "metastore". The
                              **destination_type** of a **special_destination**
                              is always EMAIL.
                      description: The access request destinations for the securable.
                    full_name:
                      type: string
                      description: >-
                        The full name of the securable. Redundant with the name
                        in the securable object, but necessary for Terraform
                        integration
                    securable_type:
                      type: string
                      description: >-
                        The type of the securable. Redundant with the type in
                        the securable object, but necessary for Terraform
                        integration
                  required:
                    - securable
                  description: >-
                    The access request destinations to assign to the securable.
                    For each destination, a **destination_id** and
                    **destination_type** must be defined.
              required:
                - access_request_destinations
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestDestinations'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: RfaAPI
  /api/2.1/unity-catalog/schemas:
    post:
      operationId: schemas_create
      summary: >-
        Creates a new schema for catalog in the Metastore. The caller must be a
        metastore admin, or have the
      tags:
        - catalog
        - schemas
      description: >-
        Creates a new schema for catalog in the Metastore. The caller must be a
        metastore admin, or have the

        **CREATE_SCHEMA** privilege in the parent catalog.


        :param name: str
          Name of schema, relative to parent catalog.
        :param catalog_name: str
          Name of parent catalog.
        :param comment: str (optional)
          User-provided free-form text description.
        :param custom_max_retention_hours: int (optional)
          Custom maximum retention period in hours for the schema.
        :param properties: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.
        :param storage_root: str (optional)
          Storage root URL for managed tables within schema.

        :returns: :class:`SchemaInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of schema, relative to parent catalog.
                catalog_name:
                  type: string
                  description: Name of parent catalog.
                comment:
                  type: string
                  description: User-provided free-form text description.
                custom_max_retention_hours:
                  type: integer
                  description: Custom maximum retention period in hours for the schema.
                properties:
                  type: object
                  description: A map of key-value properties attached to the securable.
                storage_root:
                  type: string
                  description: Storage root URL for managed tables within schema.
              required:
                - name
                - catalog_name
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchemaInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SchemasAPI
    get:
      operationId: schemas_list
      summary: >-
        Gets an array of schemas for a catalog in the metastore. If the caller
        is the metastore admin or the
      tags:
        - catalog
        - schemas
      description: >-
        Gets an array of schemas for a catalog in the metastore. If the caller
        is the metastore admin or the

        owner of the parent catalog, all schemas for the catalog will be
        retrieved. Otherwise, only schemas

        owned by the caller (or for which the caller has the **USE_SCHEMA**
        privilege) will be retrieved.

        There is no guarantee of a specific ordering of the elements in the
        array.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param catalog_name: str
          Parent catalog for schemas of interest.
        :param include_browse: bool (optional)
          Whether to include schemas in the response for which the principal can only access selective
          metadata for
        :param max_results: int (optional)
          Maximum number of schemas to return. If not set, all the schemas are returned (not recommended).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`SchemaInfo`
      parameters:
        - name: catalog_name
          in: query
          required: true
          schema:
            type: string
          description: Parent catalog for schemas of interest.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include schemas in the response for which the principal
            can only access selective metadata for
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of schemas to return. If not set, all the schemas are
            returned (not recommended). - when set to a value greater than 0,
            the page length is the minimum of this value and a server configured
            value; - when set to 0, the page length is set to a server
            configured value (recommended); - when set to a value less than 0,
            an invalid parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSchemasResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SchemasAPI
  /api/2.1/unity-catalog/schemas/{full_name}:
    delete:
      operationId: schemas_delete
      summary: >-
        Deletes the specified schema from the parent catalog. The caller must be
        the owner of the schema or an
      tags:
        - catalog
        - schemas
      description: >-
        Deletes the specified schema from the parent catalog. The caller must be
        the owner of the schema or an

        owner of the parent catalog.


        :param full_name: str
          Full name of the schema.
        :param force: bool (optional)
          Force deletion even if the schema is not empty.
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the schema.
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: Force deletion even if the schema is not empty.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SchemasAPI
    get:
      operationId: schemas_get
      summary: >-
        Gets the specified schema within the metastore. The caller must be a
        metastore admin, the owner of the
      tags:
        - catalog
        - schemas
      description: >-
        Gets the specified schema within the metastore. The caller must be a
        metastore admin, the owner of the

        schema, or a user that has the **USE_SCHEMA** privilege on the schema.


        :param full_name: str
          Full name of the schema.
        :param include_browse: bool (optional)
          Whether to include schemas in the response for which the principal can only access selective
          metadata for

        :returns: :class:`SchemaInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the schema.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include schemas in the response for which the principal
            can only access selective metadata for
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchemaInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SchemasAPI
    patch:
      operationId: schemas_update
      summary: >-
        Updates a schema for a catalog. The caller must be the owner of the
        schema or a metastore admin. If
      tags:
        - catalog
        - schemas
      description: >-
        Updates a schema for a catalog. The caller must be the owner of the
        schema or a metastore admin. If

        the caller is a metastore admin, only the **owner** field can be changed
        in the update. If the

        **name** field must be updated, the caller must be a metastore admin or
        have the **CREATE_SCHEMA**

        privilege on the parent catalog.


        :param full_name: str
          Full name of the schema.
        :param comment: str (optional)
          User-provided free-form text description.
        :param custom_max_retention_hours: int (optional)
          Custom maximum retention period in hours for the schema.
        :param enable_predictive_optimization:
        :class:`EnablePredictiveOptimization` (optional)
          Whether predictive optimization should be enabled for this object and objects under it.
        :param new_name: str (optional)
          New name for the schema.
        :param owner: str (optional)
          Username of current owner of schema.
        :param properties: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.

        :returns: :class:`SchemaInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the schema.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                comment:
                  type: string
                  description: User-provided free-form text description.
                custom_max_retention_hours:
                  type: integer
                  description: Custom maximum retention period in hours for the schema.
                enable_predictive_optimization:
                  type: string
                  x-enum:
                    - DISABLE
                    - ENABLE
                    - INHERIT
                  description: >-
                    Whether predictive optimization should be enabled for this
                    object and objects under it.
                new_name:
                  type: string
                  description: New name for the schema.
                owner:
                  type: string
                  description: Username of current owner of schema.
                properties:
                  type: object
                  description: A map of key-value properties attached to the securable.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchemaInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SchemasAPI
  /api/2.1/unity-catalog/secrets:
    post:
      operationId: secrets_uc_create_secret
      summary: Creates a new secret in Unity Catalog.
      tags:
        - catalog
        - secrets_uc
      description: >-
        Creates a new secret in Unity Catalog.


        You must be the owner of the parent schema or have the **CREATE_SECRET**
        and **USE SCHEMA** privileges

        on the parent schema and **USE CATALOG** on the parent catalog.


        The secret is stored in the specified catalog and schema, and the
        **value** field contains the

        sensitive data to be securely stored.


        :param secret: :class:`Secret`
          The secret object to create. The **name**, **catalog_name**, **schema_name**, and **value** fields
          are required.

        :returns: :class:`Secret`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                secret:
                  type: object
                  properties:
                    name:
                      type: string
                      description: The name of the secret, relative to its parent schema.
                    catalog_name:
                      type: string
                      description: >-
                        The name of the catalog where the schema and the secret
                        reside.
                    schema_name:
                      type: string
                      description: The name of the schema where the secret resides.
                    value:
                      type: string
                      description: >-
                        The secret value to store. This field is input-only and
                        is not returned in responses — use the
                        **effective_value** field (via GetSecret with
                        **include_value** set to true) to read the secret value.
                        The maximum size is 60 KiB (pre-encryption). Accepted
                        content includes passwords, tokens, keys, and other
                        sensitive credential data.
                    comment:
                      type: string
                      description: User-provided free-form text description of the secret.
                    create_time:
                      type: string
                      format: date-time
                      description: The time at which this secret was created.
                    created_by:
                      type: string
                      description: The principal that created the secret.
                    effective_owner:
                      type: string
                      description: >-
                        The effective owner of the secret, which may differ from
                        the directly-set **owner** due to inheritance.
                    effective_value:
                      type: string
                      description: >-
                        The secret value. Only populated in responses when you
                        have the **READ_SECRET** privilege and **include_value**
                        is set to true in the request. The maximum size is 60
                        KiB.
                    expire_time:
                      type: string
                      format: date-time
                      description: >-
                        User-provided expiration time of the secret. This field
                        indicates when the secret should no longer be used and
                        may be displayed as a warning in the UI. It is purely
                        informational and does not trigger any automatic actions
                        or affect the secret's lifecycle.
                    full_name:
                      type: string
                      description: >-
                        The three-level (fully qualified) name of the secret, in
                        the form of **catalog_name.schema_name.secret_name**.
                    metastore_id:
                      type: string
                      description: Unique identifier of the metastore hosting the secret.
                    owner:
                      type: string
                      description: >-
                        The owner of the secret. Defaults to the creating
                        principal on creation. Can be updated to transfer
                        ownership of the secret to another principal.
                    update_time:
                      type: string
                      format: date-time
                      description: The time at which this secret was last updated.
                    updated_by:
                      type: string
                      description: The principal that last updated the secret.
                  required:
                    - name
                    - catalog_name
                    - schema_name
                    - value
                  description: >-
                    The secret object to create. The **name**, **catalog_name**,
                    **schema_name**, and **value** fields are required.
              required:
                - secret
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Secret'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SecretsUcAPI
    get:
      operationId: secrets_uc_list_secrets
      summary: Lists secrets in Unity Catalog.
      tags:
        - catalog
        - secrets_uc
      description: >-
        Lists secrets in Unity Catalog.


        You must be a metastore admin, the owner of the secret, or have the
        **MANAGE** privilege on the

        secret.


        Both **catalog_name** and **schema_name** must be specified together to
        filter secrets within a

        specific schema. Results are paginated; use the **page_token** field
        from the response to retrieve

        subsequent pages.


        :param catalog_name: str (optional)
          The name of the catalog under which to list secrets. Both **catalog_name** and **schema_name** must
          be specified together.
        :param page_size: int (optional)
          Maximum number of secrets to return.

          - If not specified, at most 1000 secrets are returned.
          - If set to a value greater than 0, the page length is the minimum of this value and 1000.
          - If set to 0, the page length is set to 1000.
          - If set to a value less than 0, an invalid parameter error is returned.
        :param page_token: str (optional)
          Opaque pagination token to go to the next page based on previous query. The maximum page length is
          determined by a server configured value.
        :param schema_name: str (optional)
          The name of the schema under which to list secrets. Both **catalog_name** and **schema_name** must
          be specified together.

        :returns: Iterator over :class:`Secret`
      parameters:
        - name: catalog_name
          in: query
          required: false
          schema:
            type: string
          description: >-
            The name of the catalog under which to list secrets. Both
            **catalog_name** and **schema_name** must be specified together.
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of secrets to return. - If not specified, at most
            1000 secrets are returned. - If set to a value greater than 0, the
            page length is the minimum of this value and 1000. - If set to 0,
            the page length is set to 1000. - If set to a value less than 0, an
            invalid parameter error is returned.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: >-
            Opaque pagination token to go to the next page based on previous
            query. The maximum page length is determined by a server configured
            value.
        - name: schema_name
          in: query
          required: false
          schema:
            type: string
          description: >-
            The name of the schema under which to list secrets. Both
            **catalog_name** and **schema_name** must be specified together.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSecretsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SecretsUcAPI
  /api/2.1/unity-catalog/secrets/{full_name}:
    delete:
      operationId: secrets_uc_delete_secret
      summary: Deletes a secret by its three-level (fully qualified) name.
      tags:
        - catalog
        - secrets_uc
      description: |-
        Deletes a secret by its three-level (fully qualified) name.

        You must be the owner of the secret or a metastore admin.

        :param full_name: str
          The three-level (fully qualified) name of the secret (for example,
          **catalog_name.schema_name.secret_name**).
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            The three-level (fully qualified) name of the secret (for example,
            **catalog_name.schema_name.secret_name**).
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SecretsUcAPI
    get:
      operationId: secrets_uc_get_secret
      summary: Gets a secret by its three-level (fully qualified) name.
      tags:
        - catalog
        - secrets_uc
      description: >-
        Gets a secret by its three-level (fully qualified) name.


        You must be a metastore admin, the owner of the secret, or have the
        **MANAGE** privilege on the

        secret.


        The secret value isn't returned by default. To retrieve it, you must
        also have the **READ_SECRET**

        privilege and set **include_value** to true in the request.


        :param full_name: str
          The three-level (fully qualified) name of the secret (for example,
          **catalog_name.schema_name.secret_name**).

        :returns: :class:`Secret`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            The three-level (fully qualified) name of the secret (for example,
            **catalog_name.schema_name.secret_name**).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Secret'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SecretsUcAPI
    patch:
      operationId: secrets_uc_update_secret
      summary: Updates an existing secret in Unity Catalog.
      tags:
        - catalog
        - secrets_uc
      description: >-
        Updates an existing secret in Unity Catalog.


        You must be the owner of the secret or a metastore admin. If you are a
        metastore admin, only the

        **owner** field can be changed.


        Use the **update_mask** field to specify which fields to update.
        Supported updatable fields include

        **value**, **comment**, **owner**, and **expire_time**.


        :param full_name: str
          The three-level (fully qualified) name of the secret (for example,
          **catalog_name.schema_name.secret_name**).
        :param secret: :class:`Secret`
          The secret object containing the fields to update. Only fields specified in **update_mask** will be
          updated.
        :param update_mask: FieldMask
          The field mask specifying which fields of the secret to update.

          - If **update_mask** is **"*"**, all fields specified in **secret** are updated.
          - If **update_mask** specifies one or more fields, only those fields are updated. Each specified
            field must be set in **secret**. Supported fields: **value**, **comment**, **owner**,
            **expire_time**. To change the secret name, delete and recreate the secret.

        :returns: :class:`Secret`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: >-
            The three-level (fully qualified) name of the secret (for example,
            **catalog_name.schema_name.secret_name**).
        - name: update_mask
          in: query
          required: true
          schema:
            type: object
          description: >-
            The field mask specifying which fields of the secret to update. - If
            **update_mask** is **"*"**, all fields specified in **secret** are
            updated. - If **update_mask** specifies one or more fields, only
            those fields are updated. Each specified field must be set in
            **secret**. Supported fields: **value**, **comment**, **owner**,
            **expire_time**. To change the secret name, delete and recreate the
            secret.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                secret:
                  type: object
                  properties:
                    name:
                      type: string
                      description: The name of the secret, relative to its parent schema.
                    catalog_name:
                      type: string
                      description: >-
                        The name of the catalog where the schema and the secret
                        reside.
                    schema_name:
                      type: string
                      description: The name of the schema where the secret resides.
                    value:
                      type: string
                      description: >-
                        The secret value to store. This field is input-only and
                        is not returned in responses — use the
                        **effective_value** field (via GetSecret with
                        **include_value** set to true) to read the secret value.
                        The maximum size is 60 KiB (pre-encryption). Accepted
                        content includes passwords, tokens, keys, and other
                        sensitive credential data.
                    comment:
                      type: string
                      description: User-provided free-form text description of the secret.
                    create_time:
                      type: string
                      format: date-time
                      description: The time at which this secret was created.
                    created_by:
                      type: string
                      description: The principal that created the secret.
                    effective_owner:
                      type: string
                      description: >-
                        The effective owner of the secret, which may differ from
                        the directly-set **owner** due to inheritance.
                    effective_value:
                      type: string
                      description: >-
                        The secret value. Only populated in responses when you
                        have the **READ_SECRET** privilege and **include_value**
                        is set to true in the request. The maximum size is 60
                        KiB.
                    expire_time:
                      type: string
                      format: date-time
                      description: >-
                        User-provided expiration time of the secret. This field
                        indicates when the secret should no longer be used and
                        may be displayed as a warning in the UI. It is purely
                        informational and does not trigger any automatic actions
                        or affect the secret's lifecycle.
                    full_name:
                      type: string
                      description: >-
                        The three-level (fully qualified) name of the secret, in
                        the form of **catalog_name.schema_name.secret_name**.
                    metastore_id:
                      type: string
                      description: Unique identifier of the metastore hosting the secret.
                    owner:
                      type: string
                      description: >-
                        The owner of the secret. Defaults to the creating
                        principal on creation. Can be updated to transfer
                        ownership of the secret to another principal.
                    update_time:
                      type: string
                      format: date-time
                      description: The time at which this secret was last updated.
                    updated_by:
                      type: string
                      description: The principal that last updated the secret.
                  required:
                    - name
                    - catalog_name
                    - schema_name
                    - value
                  description: >-
                    The secret object containing the fields to update. Only
                    fields specified in **update_mask** will be updated.
              required:
                - secret
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Secret'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SecretsUcAPI
  /api/2.1/unity-catalog/storage-credentials:
    post:
      operationId: storage_credentials_create
      summary: Creates a new storage credential.
      tags:
        - catalog
        - storage_credentials
      description: >-
        Creates a new storage credential.


        The caller must be a metastore admin or have the
        **CREATE_STORAGE_CREDENTIAL** privilege on the

        metastore.


        :param name: str
          The credential name. The name must be unique among storage and service credentials within the
          metastore.
        :param aws_iam_role: :class:`AwsIamRoleRequest` (optional)
          The AWS IAM role configuration.
        :param azure_managed_identity: :class:`AzureManagedIdentityRequest`
        (optional)
          The Azure managed identity configuration.
        :param azure_service_principal: :class:`AzureServicePrincipal`
        (optional)
          The Azure service principal configuration.
        :param cloudflare_api_token: :class:`CloudflareApiToken` (optional)
          The Cloudflare API token configuration.
        :param comment: str (optional)
          Comment associated with the credential.
        :param databricks_gcp_service_account:
        :class:`DatabricksGcpServiceAccountRequest` (optional)
          The Databricks managed GCP service account configuration.
        :param read_only: bool (optional)
          Whether the credential is usable only for read operations. Only applicable when purpose is
          **STORAGE**.
        :param skip_validation: bool (optional)
          Supplying true to this argument skips validation of the created credential.

        :returns: :class:`StorageCredentialInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: >-
                    The credential name. The name must be unique among storage
                    and service credentials within the metastore.
                aws_iam_role:
                  type: object
                  properties:
                    role_arn:
                      type: string
                      description: >-
                        The Amazon Resource Name (ARN) of the AWS IAM role used
                        to vend temporary credentials.
                  required:
                    - role_arn
                  description: The AWS IAM role configuration.
                azure_managed_identity:
                  type: object
                  properties:
                    access_connector_id:
                      type: string
                      description: >-
                        The Azure resource ID of the Azure Databricks Access
                        Connector. Use the format
                        ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.Databricks/accessConnectors/{connector-name}``.
                    managed_identity_id:
                      type: string
                      description: >-
                        The Azure resource ID of the managed identity. Use the
                        format,
                        ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identity-name}``
                        This is only available for user-assgined identities. For
                        system-assigned identities, the access_connector_id is
                        used to identify the identity. If this field is not
                        provided, then we assume the AzureManagedIdentity is
                        using the system-assigned identity.
                  required:
                    - access_connector_id
                  description: The Azure managed identity configuration.
                azure_service_principal:
                  type: object
                  properties:
                    directory_id:
                      type: string
                      description: >-
                        The directory ID corresponding to the Azure Active
                        Directory (AAD) tenant of the application.
                    application_id:
                      type: string
                      description: >-
                        The application ID of the application registration
                        within the referenced AAD tenant.
                    client_secret:
                      type: string
                      description: The client secret generated for the above app ID in AAD.
                  required:
                    - directory_id
                    - application_id
                    - client_secret
                  description: The Azure service principal configuration.
                cloudflare_api_token:
                  type: object
                  properties:
                    access_key_id:
                      type: string
                      description: The access key ID associated with the API token.
                    secret_access_key:
                      type: string
                      description: >-
                        The secret access token generated for the above access
                        key ID.
                    account_id:
                      type: string
                      description: The ID of the account associated with the API token.
                  required:
                    - access_key_id
                    - secret_access_key
                  description: The Cloudflare API token configuration.
                comment:
                  type: string
                  description: Comment associated with the credential.
                databricks_gcp_service_account:
                  type: object
                  properties: {}
                  description: The Databricks managed GCP service account configuration.
                read_only:
                  type: boolean
                  description: >-
                    Whether the credential is usable only for read operations.
                    Only applicable when purpose is **STORAGE**.
                skip_validation:
                  type: boolean
                  description: >-
                    Supplying true to this argument skips validation of the
                    created credential.
              required:
                - name
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageCredentialInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: StorageCredentialsAPI
    get:
      operationId: storage_credentials_list
      summary: >-
        Gets an array of storage credentials (as **StorageCredentialInfo**
        objects). The array is limited to
      tags:
        - catalog
        - storage_credentials
      description: >-
        Gets an array of storage credentials (as **StorageCredentialInfo**
        objects). The array is limited to

        only those storage credentials the caller has permission to access. If
        the caller is a metastore

        admin, retrieval of credentials is unrestricted. There is no guarantee
        of a specific ordering of the

        elements in the array.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param include_unbound: bool (optional)
          Whether to include credentials not bound to the workspace. Effective only if the user has permission
          to update the credential–workspace binding.
        :param max_results: int (optional)
          Maximum number of storage credentials to return. If not set, all the storage credentials are
          returned (not recommended).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`StorageCredentialInfo`
      parameters:
        - name: include_unbound
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include credentials not bound to the workspace. Effective
            only if the user has permission to update the credential–workspace
            binding.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of storage credentials to return. If not set, all the
            storage credentials are returned (not recommended). - when set to a
            value greater than 0, the page length is the minimum of this value
            and a server configured value; - when set to 0, the page length is
            set to a server configured value (recommended); - when set to a
            value less than 0, an invalid parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAccountStorageCredentialsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: StorageCredentialsAPI
  /api/2.1/unity-catalog/storage-credentials/{name}:
    delete:
      operationId: storage_credentials_delete
      summary: >-
        Deletes a storage credential from the metastore. The caller must be an
        owner of the storage
      tags:
        - catalog
        - storage_credentials
      description: >-
        Deletes a storage credential from the metastore. The caller must be an
        owner of the storage

        credential.


        :param name: str
          Name of the storage credential.
        :param force: bool (optional)
          Force an update even if there are dependent external locations or external tables (when purpose is
          **STORAGE**) or dependent services (when purpose is **SERVICE**).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the storage credential.
        - name: force
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Force an update even if there are dependent external locations or
            external tables (when purpose is **STORAGE**) or dependent services
            (when purpose is **SERVICE**).
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: StorageCredentialsAPI
    get:
      operationId: storage_credentials_get
      summary: >-
        Gets a storage credential from the metastore. The caller must be a
        metastore admin, the owner of the
      tags:
        - catalog
        - storage_credentials
      description: >-
        Gets a storage credential from the metastore. The caller must be a
        metastore admin, the owner of the

        storage credential, or have some permission on the storage credential.


        :param name: str
          Name of the storage credential.

        :returns: :class:`StorageCredentialInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the storage credential.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageCredentialInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: StorageCredentialsAPI
    patch:
      operationId: storage_credentials_update
      summary: Updates a storage credential on the metastore.
      tags:
        - catalog
        - storage_credentials
      description: >-
        Updates a storage credential on the metastore.


        The caller must be the owner of the storage credential or a metastore
        admin. If the caller is a

        metastore admin, only the **owner** field can be changed.


        :param name: str
          Name of the storage credential.
        :param aws_iam_role: :class:`AwsIamRoleRequest` (optional)
          The AWS IAM role configuration.
        :param azure_managed_identity: :class:`AzureManagedIdentityResponse`
        (optional)
          The Azure managed identity configuration.
        :param azure_service_principal: :class:`AzureServicePrincipal`
        (optional)
          The Azure service principal configuration.
        :param cloudflare_api_token: :class:`CloudflareApiToken` (optional)
          The Cloudflare API token configuration.
        :param comment: str (optional)
          Comment associated with the credential.
        :param databricks_gcp_service_account:
        :class:`DatabricksGcpServiceAccountRequest` (optional)
          The Databricks managed GCP service account configuration.
        :param force: bool (optional)
          Force update even if there are dependent external locations or external tables.
        :param isolation_mode: :class:`IsolationMode` (optional)
          Whether the current securable is accessible from all workspaces or a specific set of workspaces.
        :param new_name: str (optional)
          New name for the storage credential.
        :param owner: str (optional)
          Username of current owner of credential.
        :param read_only: bool (optional)
          Whether the credential is usable only for read operations. Only applicable when purpose is
          **STORAGE**.
        :param skip_validation: bool (optional)
          Supplying true to this argument skips validation of the updated credential.

        :returns: :class:`StorageCredentialInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Name of the storage credential.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                aws_iam_role:
                  type: object
                  properties:
                    role_arn:
                      type: string
                      description: >-
                        The Amazon Resource Name (ARN) of the AWS IAM role used
                        to vend temporary credentials.
                  required:
                    - role_arn
                  description: The AWS IAM role configuration.
                azure_managed_identity:
                  type: object
                  properties:
                    access_connector_id:
                      type: string
                      description: >-
                        The Azure resource ID of the Azure Databricks Access
                        Connector. Use the format
                        ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.Databricks/accessConnectors/{connector-name}``.
                    credential_id:
                      type: string
                      description: >-
                        The Databricks internal ID that represents this managed
                        identity.
                    managed_identity_id:
                      type: string
                      description: >-
                        The Azure resource ID of the managed identity. Use the
                        format,
                        ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identity-name}``
                        This is only available for user-assgined identities. For
                        system-assigned identities, the access_connector_id is
                        used to identify the identity. If this field is not
                        provided, then we assume the AzureManagedIdentity is
                        using the system-assigned identity.
                  required:
                    - access_connector_id
                  description: The Azure managed identity configuration.
                azure_service_principal:
                  type: object
                  properties:
                    directory_id:
                      type: string
                      description: >-
                        The directory ID corresponding to the Azure Active
                        Directory (AAD) tenant of the application.
                    application_id:
                      type: string
                      description: >-
                        The application ID of the application registration
                        within the referenced AAD tenant.
                    client_secret:
                      type: string
                      description: The client secret generated for the above app ID in AAD.
                  required:
                    - directory_id
                    - application_id
                    - client_secret
                  description: The Azure service principal configuration.
                cloudflare_api_token:
                  type: object
                  properties:
                    access_key_id:
                      type: string
                      description: The access key ID associated with the API token.
                    secret_access_key:
                      type: string
                      description: >-
                        The secret access token generated for the above access
                        key ID.
                    account_id:
                      type: string
                      description: The ID of the account associated with the API token.
                  required:
                    - access_key_id
                    - secret_access_key
                  description: The Cloudflare API token configuration.
                comment:
                  type: string
                  description: Comment associated with the credential.
                databricks_gcp_service_account:
                  type: object
                  properties: {}
                  description: The Databricks managed GCP service account configuration.
                force:
                  type: boolean
                  description: >-
                    Force update even if there are dependent external locations
                    or external tables.
                isolation_mode:
                  type: string
                  x-enum:
                    - ISOLATION_MODE_ISOLATED
                    - ISOLATION_MODE_OPEN
                    - ISOLATION_MODE_OPEN_IN_ACCOUNT
                  description: >-
                    Whether the current securable is accessible from all
                    workspaces or a specific set of workspaces.
                new_name:
                  type: string
                  description: New name for the storage credential.
                owner:
                  type: string
                  description: Username of current owner of credential.
                read_only:
                  type: boolean
                  description: >-
                    Whether the credential is usable only for read operations.
                    Only applicable when purpose is **STORAGE**.
                skip_validation:
                  type: boolean
                  description: >-
                    Supplying true to this argument skips validation of the
                    updated credential.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageCredentialInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: StorageCredentialsAPI
  /api/2.1/unity-catalog/validate-storage-credentials:
    post:
      operationId: storage_credentials_validate
      summary: >-
        Validates a storage credential. At least one of
        **external_location_name** and **url** need to be
      tags:
        - catalog
        - storage_credentials
      description: >-
        Validates a storage credential. At least one of
        **external_location_name** and **url** need to be

        provided. If only one of them is provided, it will be used for
        validation. And if both are provided,

        the **url** will be used for validation, and **external_location_name**
        will be ignored when checking

        overlapping urls.


        Either the **storage_credential_name** or the cloud-specific credential
        must be provided.


        The caller must be a metastore admin or the storage credential owner or
        have the

        **CREATE_EXTERNAL_LOCATION** privilege on the metastore and the storage
        credential.


        :param aws_iam_role: :class:`AwsIamRoleRequest` (optional)
          The AWS IAM role configuration.
        :param azure_managed_identity: :class:`AzureManagedIdentityRequest`
        (optional)
          The Azure managed identity configuration.
        :param azure_service_principal: :class:`AzureServicePrincipal`
        (optional)
          The Azure service principal configuration.
        :param cloudflare_api_token: :class:`CloudflareApiToken` (optional)
          The Cloudflare API token configuration.
        :param databricks_gcp_service_account:
        :class:`DatabricksGcpServiceAccountRequest` (optional)
          The Databricks created GCP service account configuration.
        :param external_location_name: str (optional)
          The name of an existing external location to validate.
        :param read_only: bool (optional)
          Whether the storage credential is only usable for read operations.
        :param storage_credential_name: str (optional)
          Required. The name of an existing credential or long-lived cloud credential to validate.
        :param url: str (optional)
          The external location url to validate.

        :returns: :class:`ValidateStorageCredentialResponse`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                aws_iam_role:
                  type: object
                  properties:
                    role_arn:
                      type: string
                      description: >-
                        The Amazon Resource Name (ARN) of the AWS IAM role used
                        to vend temporary credentials.
                  required:
                    - role_arn
                  description: The AWS IAM role configuration.
                azure_managed_identity:
                  type: object
                  properties:
                    access_connector_id:
                      type: string
                      description: >-
                        The Azure resource ID of the Azure Databricks Access
                        Connector. Use the format
                        ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.Databricks/accessConnectors/{connector-name}``.
                    managed_identity_id:
                      type: string
                      description: >-
                        The Azure resource ID of the managed identity. Use the
                        format,
                        ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identity-name}``
                        This is only available for user-assgined identities. For
                        system-assigned identities, the access_connector_id is
                        used to identify the identity. If this field is not
                        provided, then we assume the AzureManagedIdentity is
                        using the system-assigned identity.
                  required:
                    - access_connector_id
                  description: The Azure managed identity configuration.
                azure_service_principal:
                  type: object
                  properties:
                    directory_id:
                      type: string
                      description: >-
                        The directory ID corresponding to the Azure Active
                        Directory (AAD) tenant of the application.
                    application_id:
                      type: string
                      description: >-
                        The application ID of the application registration
                        within the referenced AAD tenant.
                    client_secret:
                      type: string
                      description: The client secret generated for the above app ID in AAD.
                  required:
                    - directory_id
                    - application_id
                    - client_secret
                  description: The Azure service principal configuration.
                cloudflare_api_token:
                  type: object
                  properties:
                    access_key_id:
                      type: string
                      description: The access key ID associated with the API token.
                    secret_access_key:
                      type: string
                      description: >-
                        The secret access token generated for the above access
                        key ID.
                    account_id:
                      type: string
                      description: The ID of the account associated with the API token.
                  required:
                    - access_key_id
                    - secret_access_key
                  description: The Cloudflare API token configuration.
                databricks_gcp_service_account:
                  type: object
                  properties: {}
                  description: The Databricks created GCP service account configuration.
                external_location_name:
                  type: string
                  description: The name of an existing external location to validate.
                read_only:
                  type: boolean
                  description: >-
                    Whether the storage credential is only usable for read
                    operations.
                storage_credential_name:
                  type: string
                  description: >-
                    Required. The name of an existing credential or long-lived
                    cloud credential to validate.
                url:
                  type: string
                  description: The external location url to validate.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidateStorageCredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: StorageCredentialsAPI
  /api/2.1/unity-catalog/metastores/{metastore_id}/systemschemas/{schema_name}:
    delete:
      operationId: system_schemas_disable
      summary: >-
        Disables the system schema and removes it from the system catalog. The
        caller must be an account admin
      tags:
        - catalog
        - system_schemas
      description: >-
        Disables the system schema and removes it from the system catalog. The
        caller must be an account admin

        or a metastore admin.


        :param metastore_id: str
          The metastore ID under which the system schema lives.
        :param schema_name: str
          Full name of the system schema.
      parameters:
        - name: metastore_id
          in: path
          required: true
          schema:
            type: string
          description: The metastore ID under which the system schema lives.
        - name: schema_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the system schema.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SystemSchemasAPI
    put:
      operationId: system_schemas_enable
      summary: >-
        Enables the system schema and adds it to the system catalog. The caller
        must be an account admin or a
      tags:
        - catalog
        - system_schemas
      description: >-
        Enables the system schema and adds it to the system catalog. The caller
        must be an account admin or a

        metastore admin.


        :param metastore_id: str
          The metastore ID under which the system schema lives.
        :param schema_name: str
          Full name of the system schema.
        :param catalog_name: str (optional)
          the catalog for which the system schema is to enabled in
      parameters:
        - name: metastore_id
          in: path
          required: true
          schema:
            type: string
          description: The metastore ID under which the system schema lives.
        - name: schema_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the system schema.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                catalog_name:
                  type: string
                  description: the catalog for which the system schema is to enabled in
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SystemSchemasAPI
  /api/2.1/unity-catalog/metastores/{metastore_id}/systemschemas:
    get:
      operationId: system_schemas_list
      summary: >-
        Gets an array of system schemas for a metastore. The caller must be an
        account admin or a metastore
      tags:
        - catalog
        - system_schemas
      description: >-
        Gets an array of system schemas for a metastore. The caller must be an
        account admin or a metastore

        admin.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param metastore_id: str
          The ID for the metastore in which the system schema resides.
        :param max_results: int (optional)
          Maximum number of schemas to return.

          - When set to 0, the page length is set to a server configured value (recommended);
          - When set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - When set to a value less than 0, an invalid parameter error is returned;
          - If not set, all the schemas are returned (not recommended).
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`SystemSchemaInfo`
      parameters:
        - name: metastore_id
          in: path
          required: true
          schema:
            type: string
          description: The ID for the metastore in which the system schema resides.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of schemas to return. - When set to 0, the page
            length is set to a server configured value (recommended); - When set
            to a value greater than 0, the page length is the minimum of this
            value and a server configured value; - When set to a value less than
            0, an invalid parameter error is returned; - If not set, all the
            schemas are returned (not recommended).
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSystemSchemasResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: SystemSchemasAPI
  /api/2.1/unity-catalog/constraints:
    post:
      operationId: table_constraints_create
      summary: Creates a new table constraint.
      tags:
        - catalog
        - table_constraints
      description: >-
        Creates a new table constraint.


        For the table constraint creation to succeed, the user must satisfy both
        of these conditions:


        - the user must have the **USE_CATALOG** privilege on the table's parent
        catalog, the **USE_SCHEMA**
          privilege on the table's parent schema, and be the owner of the table.
        - if the new constraint is a **ForeignKeyConstraint**, the user must
        have the **USE_CATALOG**
          privilege on the referenced parent table's catalog, the **USE_SCHEMA** privilege on the referenced
          parent table's schema, and be the owner of the referenced parent table.

        :param full_name_arg: str
          The full name of the table referenced by the constraint.
        :param constraint: :class:`TableConstraint`


        :returns: :class:`TableConstraint`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                full_name_arg:
                  type: string
                  description: The full name of the table referenced by the constraint.
                constraint:
                  type: object
                  properties:
                    foreign_key_constraint:
                      type: object
                      properties:
                        name:
                          type: string
                        child_columns:
                          type: array
                          items:
                            type: string
                          description: Column names for this constraint.
                        parent_table:
                          type: string
                          description: The full name of the parent constraint.
                        parent_columns:
                          type: array
                          items:
                            type: string
                          description: Column names for this constraint.
                        rely:
                          type: boolean
                          description: >-
                            True if the constraint is RELY, false or unset if
                            NORELY.
                      required:
                        - name
                        - child_columns
                        - parent_table
                        - parent_columns
                    named_table_constraint:
                      type: object
                      properties:
                        name:
                          type: string
                      required:
                        - name
                    primary_key_constraint:
                      type: object
                      properties:
                        name:
                          type: string
                        child_columns:
                          type: array
                          items:
                            type: string
                          description: Column names for this constraint.
                        rely:
                          type: boolean
                          description: >-
                            True if the constraint is RELY, false or unset if
                            NORELY.
                        timeseries_columns:
                          type: array
                          items:
                            type: string
                          description: Column names that represent a timeseries.
                      required:
                        - name
                        - child_columns
                  description: >-
                    A table constraint, as defined by *one* of the following
                    fields being set:
                        **primary_key_constraint**, **foreign_key_constraint**, **named_table_constraint**.
              required:
                - full_name_arg
                - constraint
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TableConstraint'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TableConstraintsAPI
  /api/2.1/unity-catalog/constraints/{full_name}:
    delete:
      operationId: table_constraints_delete
      summary: Deletes a table constraint.
      tags:
        - catalog
        - table_constraints
      description: >-
        Deletes a table constraint.


        For the table constraint deletion to succeed, the user must satisfy both
        of these conditions:


        - the user must have the **USE_CATALOG** privilege on the table's parent
        catalog, the **USE_SCHEMA**
          privilege on the table's parent schema, and be the owner of the table.
        - if **cascade** argument is **true**, the user must have the following
        permissions on all of the
          child tables: the **USE_CATALOG** privilege on the table's catalog, the **USE_SCHEMA** privilege on
          the table's schema, and be the owner of the table.

        :param full_name: str
          Full name of the table referenced by the constraint.
        :param constraint_name: str
          The name of the constraint to delete.
        :param cascade: bool
          If true, try deleting all child constraints of the current constraint. If false, reject this
          operation if the current constraint has any child constraints.
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the table referenced by the constraint.
        - name: constraint_name
          in: query
          required: true
          schema:
            type: string
          description: The name of the constraint to delete.
        - name: cascade
          in: query
          required: true
          schema:
            type: boolean
          description: >-
            If true, try deleting all child constraints of the current
            constraint. If false, reject this operation if the current
            constraint has any child constraints.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TableConstraintsAPI
  /api/2.1/unity-catalog/tables:
    post:
      operationId: tables_create
      summary: Creates a new table in the specified catalog and schema.
      tags:
        - catalog
        - tables
      description: >-
        Creates a new table in the specified catalog and schema.


        To create an external delta table, the caller must have the
        **EXTERNAL_USE_SCHEMA** privilege on the

        parent schema and the **EXTERNAL_USE_LOCATION** privilege on the
        external location. These privileges

        must always be granted explicitly, and cannot be inherited through
        ownership or **ALL_PRIVILEGES**.


        Standard UC permissions needed to create tables still apply:
        **USE_CATALOG** on the parent catalog (or

        ownership of the parent catalog), **CREATE_TABLE** and **USE_SCHEMA** on
        the parent schema (or

        ownership of the parent schema), and **CREATE_EXTERNAL_TABLE** on
        external location.


        The **columns** field needs to be in a Spark compatible format, so we
        recommend you use Spark to

        create these tables. The API itself does not validate the correctness of
        the column spec. If the spec

        is not Spark compatible, the tables may not be readable by Databricks
        Runtime.


        NOTE: The Create Table API for external clients only supports creating
        **external delta tables**. The

        values shown in the respective enums are all values supported by
        Databricks, however for this specific

        Create Table API, only **table_type** **EXTERNAL** and
        **data_source_format** **DELTA** are supported.

        Additionally, column masks are not supported when creating tables
        through this API.


        :param name: str
          Name of table, relative to parent schema.
        :param catalog_name: str
          Name of parent catalog.
        :param schema_name: str
          Name of parent schema relative to its parent catalog.
        :param table_type: :class:`TableType`

        :param data_source_format: :class:`DataSourceFormat`

        :param storage_location: str
          Storage root URL for table (for **MANAGED**, **EXTERNAL** tables).
        :param columns: List[:class:`ColumnInfo`] (optional)
          The array of **ColumnInfo** definitions of the table's columns.
        :param properties: Dict[str,str] (optional)
          A map of key-value properties attached to the securable.

        :returns: :class:`TableInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of table, relative to parent schema.
                catalog_name:
                  type: string
                  description: Name of parent catalog.
                schema_name:
                  type: string
                  description: Name of parent schema relative to its parent catalog.
                table_type:
                  type: string
                  x-enum:
                    - EXTERNAL
                    - EXTERNAL_SHALLOW_CLONE
                    - FOREIGN
                    - MANAGED
                    - MANAGED_SHALLOW_CLONE
                    - MATERIALIZED_VIEW
                    - METRIC_VIEW
                    - STREAMING_TABLE
                    - VIEW
                  description: >-
                    Create a collection of name/value pairs.


                    Example enumeration:


                    >>> class Color(Enum):

                    ...     RED = 1

                    ...     BLUE = 2

                    ...     GREEN = 3


                    Access them by:


                    - attribute access:

                      >>> Color.RED
                      <Color.RED: 1>

                    - value lookup:

                      >>> Color(1)
                      <Color.RED: 1>

                    - name lookup:

                      >>> Color['RED']
                      <Color.RED: 1>

                    Enumerations can be iterated over, and know how many members
                    they have:


                    >>> len(Color)

                    3


                    >>> list(Color)

                    [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                    Methods can be added to enumerations, and members can have
                    their own

                    attributes -- see the documentation for details.
                data_source_format:
                  type: string
                  x-enum:
                    - AVRO
                    - BIGQUERY_FORMAT
                    - CSV
                    - DATABRICKS_FORMAT
                    - DATABRICKS_ROW_STORE_FORMAT
                    - DELTA
                    - DELTASHARING
                    - DELTA_UNIFORM_HUDI
                    - DELTA_UNIFORM_ICEBERG
                    - HIVE
                    - ICEBERG
                    - JSON
                    - MONGODB_FORMAT
                    - MYSQL_FORMAT
                    - NETSUITE_FORMAT
                    - ORACLE_FORMAT
                    - ORC
                    - PARQUET
                    - POSTGRESQL_FORMAT
                    - REDSHIFT_FORMAT
                    - SALESFORCE_DATA_CLOUD_FORMAT
                    - SALESFORCE_FORMAT
                    - SNOWFLAKE_FORMAT
                    - SQLDW_FORMAT
                    - SQLSERVER_FORMAT
                    - TERADATA_FORMAT
                    - TEXT
                    - UNITY_CATALOG
                    - VECTOR_INDEX_FORMAT
                    - WORKDAY_RAAS_FORMAT
                  description: Data source format
                storage_location:
                  type: string
                  description: >-
                    Storage root URL for table (for **MANAGED**, **EXTERNAL**
                    tables).
                columns:
                  type: array
                  items:
                    type: object
                    properties:
                      comment:
                        type: string
                      mask:
                        type: object
                        properties:
                          function_name:
                            type: string
                          using_arguments:
                            type: array
                            items:
                              type: object
                              properties:
                                column:
                                  type: string
                                  description: A column reference.
                                constant:
                                  type: string
                                  description: A constant literal.
                              description: >-
                                A positional argument passed to a row filter or
                                column mask function. Distinguishes between
                                    column references and literals.
                            description: >-
                              The list of additional table columns or literals
                              to be passed as additional arguments to a column
                              mask function. This is the replacement of the
                              deprecated using_column_names field and carries
                              information about the types (alias or constant) of
                              the arguments to the mask function.
                          using_column_names:
                            type: array
                            items:
                              type: string
                            description: >-
                              The list of additional table columns to be passed
                              as input to the column mask function. The first
                              arg of the mask function should be of the type of
                              the column being masked and the types of the rest
                              of the args should match the types of columns in
                              'using_column_names'.
                      name:
                        type: string
                        description: Name of Column.
                      nullable:
                        type: boolean
                        description: 'Whether field may be Null (default: true).'
                      partition_index:
                        type: integer
                        description: Partition index for column.
                      position:
                        type: integer
                        description: Ordinal position of column (starting at position 0).
                      type_interval_type:
                        type: string
                        description: Format of IntervalType.
                      type_json:
                        type: string
                        description: Full data type specification, JSON-serialized.
                      type_name:
                        type: string
                        x-enum:
                          - ARRAY
                          - BINARY
                          - BOOLEAN
                          - BYTE
                          - CHAR
                          - DATE
                          - DECIMAL
                          - DOUBLE
                          - FILE
                          - FLOAT
                          - GEOGRAPHY
                          - GEOMETRY
                          - INT
                          - INTERVAL
                          - LONG
                          - MAP
                          - 'NULL'
                          - SHORT
                          - STRING
                          - STRUCT
                          - TABLEREF_TYPE
                          - TABLE_TYPE
                          - TIME
                          - TIMESTAMP
                          - TIMESTAMP_NTZ
                          - USER_DEFINED_TYPE
                          - VARIANT
                        description: >-
                          Create a collection of name/value pairs.


                          Example enumeration:


                          >>> class Color(Enum):

                          ...     RED = 1

                          ...     BLUE = 2

                          ...     GREEN = 3


                          Access them by:


                          - attribute access:

                            >>> Color.RED
                            <Color.RED: 1>

                          - value lookup:

                            >>> Color(1)
                            <Color.RED: 1>

                          - name lookup:

                            >>> Color['RED']
                            <Color.RED: 1>

                          Enumerations can be iterated over, and know how many
                          members they have:


                          >>> len(Color)

                          3


                          >>> list(Color)

                          [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]


                          Methods can be added to enumerations, and members can
                          have their own

                          attributes -- see the documentation for details.
                      type_precision:
                        type: integer
                        description: Digits of precision; required for DecimalTypes.
                      type_scale:
                        type: integer
                        description: Digits to right of decimal; Required for DecimalTypes.
                      type_text:
                        type: string
                        description: >-
                          Full data type specification as SQL/catalogString
                          text.
                  description: >-
                    The array of **ColumnInfo** definitions of the table's
                    columns.
                properties:
                  type: object
                  description: A map of key-value properties attached to the securable.
              required:
                - name
                - catalog_name
                - schema_name
                - table_type
                - data_source_format
                - storage_location
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TableInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TablesAPI
    get:
      operationId: tables_list
      summary: >-
        Gets an array of all tables for the current metastore under the parent
        catalog and schema. The caller
      tags:
        - catalog
        - tables
      description: >-
        Gets an array of all tables for the current metastore under the parent
        catalog and schema. The caller

        must be a metastore admin or an owner of (or have the **SELECT**
        privilege on) the table. For the

        latter case, the caller must also be the owner or have the
        **USE_CATALOG** privilege on the parent

        catalog and the **USE_SCHEMA** privilege on the parent schema. There is
        no guarantee of a specific

        ordering of the elements in the array.


        NOTE: **view_dependencies** and **table_constraints** are not returned
        by ListTables queries.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param catalog_name: str
          Name of parent catalog for tables of interest.
        :param schema_name: str
          Parent schema of tables.
        :param include_browse: bool (optional)
          Whether to include tables in the response for which the principal can only access selective metadata
          for.
        :param include_manifest_capabilities: bool (optional)
          Whether to include a manifest containing table capabilities in the response.
        :param max_results: int (optional)
          Maximum number of tables to return. If not set, all the tables are returned (not recommended).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - when set to 0, the page length is set to a server configured value (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param omit_columns: bool (optional)
          Whether to omit the columns of the table from the response or not.
        :param omit_properties: bool (optional)
          Whether to omit the properties of the table from the response or not.
        :param omit_username: bool (optional)
          Whether to omit the username of the table (e.g. owner, updated_by, created_by) from the response or
          not.
        :param page_token: str (optional)
          Opaque token to send for the next page of results (pagination).

        :returns: Iterator over :class:`TableInfo`
      parameters:
        - name: catalog_name
          in: query
          required: true
          schema:
            type: string
          description: Name of parent catalog for tables of interest.
        - name: schema_name
          in: query
          required: true
          schema:
            type: string
          description: Parent schema of tables.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include tables in the response for which the principal
            can only access selective metadata for.
        - name: include_manifest_capabilities
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include a manifest containing table capabilities in the
            response.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of tables to return. If not set, all the tables are
            returned (not recommended). - when set to a value greater than 0,
            the page length is the minimum of this value and a server configured
            value; - when set to 0, the page length is set to a server
            configured value (recommended); - when set to a value less than 0,
            an invalid parameter error is returned;
        - name: omit_columns
          in: query
          required: false
          schema:
            type: boolean
          description: Whether to omit the columns of the table from the response or not.
        - name: omit_properties
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to omit the properties of the table from the response or
            not.
        - name: omit_username
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to omit the username of the table (e.g. owner, updated_by,
            created_by) from the response or not.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque token to send for the next page of results (pagination).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListTablesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TablesAPI
  /api/2.1/unity-catalog/tables/{full_name}:
    delete:
      operationId: tables_delete
      summary: >-
        Deletes a table from the specified parent catalog and schema. The caller
        must be the owner of the
      tags:
        - catalog
        - tables
      description: >-
        Deletes a table from the specified parent catalog and schema. The caller
        must be the owner of the

        parent catalog, have the **USE_CATALOG** privilege on the parent catalog
        and be the owner of the

        parent schema, or be the owner of the table and have the **USE_CATALOG**
        privilege on the parent

        catalog and the **USE_SCHEMA** privilege on the parent schema.


        :param full_name: str
          Full name of the table.
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the table.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TablesAPI
    get:
      operationId: tables_get
      summary: >-
        Gets a table from the metastore for a specific catalog and schema. The
        caller must satisfy one of the
      tags:
        - catalog
        - tables
      description: >-
        Gets a table from the metastore for a specific catalog and schema. The
        caller must satisfy one of the

        following requirements:


        - Be a metastore admin

        - Be the owner of the parent catalog

        - Be the owner of the parent schema and have the **USE_CATALOG**
        privilege on the parent catalog

        - Have the **USE_CATALOG** privilege on the parent catalog and the
        **USE_SCHEMA** privilege on the
          parent schema, and either be the table owner or have the **SELECT** privilege on the table.

        :param full_name: str
          Full name of the table.
        :param include_browse: bool (optional)
          Whether to include tables in the response for which the principal can only access selective metadata
          for.
        :param include_delta_metadata: bool (optional)
          Whether delta metadata should be included in the response.
        :param include_manifest_capabilities: bool (optional)
          Whether to include a manifest containing table capabilities in the response.

        :returns: :class:`TableInfo`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the table.
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include tables in the response for which the principal
            can only access selective metadata for.
        - name: include_delta_metadata
          in: query
          required: false
          schema:
            type: boolean
          description: Whether delta metadata should be included in the response.
        - name: include_manifest_capabilities
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include a manifest containing table capabilities in the
            response.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TableInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TablesAPI
    patch:
      operationId: tables_update
      summary: >-
        Change the owner of the table. The caller must be the owner of the
        parent catalog, have the
      tags:
        - catalog
        - tables
      description: >-
        Change the owner of the table. The caller must be the owner of the
        parent catalog, have the

        **USE_CATALOG** privilege on the parent catalog and be the owner of the
        parent schema, or be the owner

        of the table and have the **USE_CATALOG** privilege on the parent
        catalog and the **USE_SCHEMA**

        privilege on the parent schema.


        :param full_name: str
          Full name of the table.
        :param owner: str (optional)
          Username of current owner of table.
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the table.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  description: Username of current owner of table.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TablesAPI
  /api/2.1/unity-catalog/tables/{full_name}/exists:
    get:
      operationId: tables_exists
      summary: >-
        Gets if a table exists in the metastore for a specific catalog and
        schema. The caller must satisfy one
      tags:
        - catalog
        - tables
      description: >-
        Gets if a table exists in the metastore for a specific catalog and
        schema. The caller must satisfy one

        of the following requirements:


        - Be a metastore admin

        - Be the owner of the parent catalog

        - Be the owner of the parent schema and have the **USE_CATALOG**
        privilege on the parent catalog

        - Have the **USE_CATALOG** privilege on the parent catalog and the
        **USE_SCHEMA** privilege on the
          parent schema, and either be the table owner or have the **SELECT** privilege on the table.
        - Have **BROWSE** privilege on the parent catalog

        - Have **BROWSE** privilege on the parent schema


        :param full_name: str
          Full name of the table.

        :returns: :class:`TableExistsResponse`
      parameters:
        - name: full_name
          in: path
          required: true
          schema:
            type: string
          description: Full name of the table.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TableExistsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TablesAPI
  /api/2.1/unity-catalog/table-summaries:
    get:
      operationId: tables_list_summaries
      summary: >-
        Gets an array of summaries for tables for a schema and catalog within
        the metastore. The table
      tags:
        - catalog
        - tables
      description: >-
        Gets an array of summaries for tables for a schema and catalog within
        the metastore. The table

        summaries returned are either:


        - summaries for tables (within the current metastore and parent catalog
        and schema), when the user is
          a metastore admin, or:
        - summaries for tables and schemas (within the current metastore and
        parent catalog) for which the
          user has ownership or the **SELECT** privilege on the table and ownership or **USE_SCHEMA**
          privilege on the schema, provided that the user also has ownership or the **USE_CATALOG** privilege
          on the parent catalog.

        There is no guarantee of a specific ordering of the elements in the
        array.


        PAGINATION BEHAVIOR: The API is by default paginated, a page may contain
        zero results while still

        providing a next_page_token. Clients must continue reading pages until
        next_page_token is absent,

        which is the only indication that the end of results has been reached.


        :param catalog_name: str
          Name of parent catalog for tables of interest.
        :param include_manifest_capabilities: bool (optional)
          Whether to include a manifest containing table capabilities in the response.
        :param max_results: int (optional)
          Maximum number of summaries for tables to return. If not set, the page length is set to a server
          configured value (10000, as of 1/5/2024).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value (10000, as of 1/5/2024);
          - when set to 0, the page length is set to a server configured value (10000, as of 1/5/2024)
            (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.
        :param schema_name_pattern: str (optional)
          A sql LIKE pattern (% and _) for schema names. All schemas will be returned if not set or empty.
        :param table_name_pattern: str (optional)
          A sql LIKE pattern (% and _) for table names. All tables will be returned if not set or empty.

        :returns: Iterator over :class:`TableSummary`
      parameters:
        - name: catalog_name
          in: query
          required: true
          schema:
            type: string
          description: Name of parent catalog for tables of interest.
        - name: include_manifest_capabilities
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include a manifest containing table capabilities in the
            response.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of summaries for tables to return. If not set, the
            page length is set to a server configured value (10000, as of
            1/5/2024). - when set to a value greater than 0, the page length is
            the minimum of this value and a server configured value (10000, as
            of 1/5/2024); - when set to 0, the page length is set to a server
            configured value (10000, as of 1/5/2024) (recommended); - when set
            to a value less than 0, an invalid parameter error is returned;
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
        - name: schema_name_pattern
          in: query
          required: false
          schema:
            type: string
          description: >-
            A sql LIKE pattern (% and _) for schema names. All schemas will be
            returned if not set or empty.
        - name: table_name_pattern
          in: query
          required: false
          schema:
            type: string
          description: >-
            A sql LIKE pattern (% and _) for table names. All tables will be
            returned if not set or empty.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListTableSummariesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TablesAPI
  /api/2.0/unity-catalog/temporary-path-credentials:
    post:
      operationId: temporary_path_credentials_generate_temporary_path_credentials
      summary: >-
        Get a short-lived credential for directly accessing cloud storage
        locations registered in Databricks.
      tags:
        - catalog
        - temporary_path_credentials
      description: >-
        Get a short-lived credential for directly accessing cloud storage
        locations registered in Databricks.

        The Generate Temporary Path Credentials API is only supported for
        external storage paths, specifically

        external locations and external tables. Managed tables are not supported
        by this API. The metastore

        must have **external_access_enabled** flag set to true (default false).
        The caller must have the

        **EXTERNAL_USE_LOCATION** privilege on the external location; this
        privilege can only be granted by

        external location owners. For requests on existing external tables, the
        caller must also have the

        **EXTERNAL_USE_SCHEMA** privilege on the parent schema; this privilege
        can only be granted by catalog

        owners.


        :param url: str
          URL for path-based access.
        :param operation: :class:`PathOperation`
          The operation being performed on the path.
        :param dry_run: bool (optional)
          Optional. When set to true, the service will not validate that the generated credentials can perform
          write operations, therefore no new paths will be created and the response will not contain valid
          credentials. Defaults to false.

        :returns: :class:`GenerateTemporaryPathCredentialResponse`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                url:
                  type: string
                  description: URL for path-based access.
                operation:
                  type: string
                  x-enum:
                    - PATH_CREATE_TABLE
                    - PATH_READ
                    - PATH_READ_WRITE
                  description: The operation being performed on the path.
                dry_run:
                  type: boolean
                  description: >-
                    Optional. When set to true, the service will not validate
                    that the generated credentials can perform write operations,
                    therefore no new paths will be created and the response will
                    not contain valid credentials. Defaults to false.
              required:
                - url
                - operation
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateTemporaryPathCredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TemporaryPathCredentialsAPI
  /api/2.0/unity-catalog/temporary-table-credentials:
    post:
      operationId: temporary_table_credentials_generate_temporary_table_credentials
      summary: >-
        Get a short-lived credential for directly accessing the table data on
        cloud storage. The metastore
      tags:
        - catalog
        - temporary_table_credentials
      description: >-
        Get a short-lived credential for directly accessing the table data on
        cloud storage. The metastore

        must have **external_access_enabled** flag set to true (default false).
        The caller must have the

        **EXTERNAL_USE_SCHEMA** privilege on the parent schema and this
        privilege can only be granted by

        catalog owners.


        :param operation: :class:`TableOperation` (optional)
          The operation performed against the table data, either READ or READ_WRITE. If READ_WRITE is
          specified, the credentials returned will have write permissions, otherwise, it will be read only.
        :param table_id: str (optional)
          UUID of the table to read or write.

        :returns: :class:`GenerateTemporaryTableCredentialResponse`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                operation:
                  type: string
                  x-enum:
                    - READ
                    - READ_WRITE
                  description: >-
                    The operation performed against the table data, either READ
                    or READ_WRITE. If READ_WRITE is specified, the credentials
                    returned will have write permissions, otherwise, it will be
                    read only.
                table_id:
                  type: string
                  description: UUID of the table to read or write.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateTemporaryTableCredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TemporaryTableCredentialsAPI
  /api/2.0/unity-catalog/temporary-volume-credentials:
    post:
      operationId: temporary_volume_credentials_generate_temporary_volume_credentials
      summary: >-
        Get a short-lived credential for directly accessing the volume data on
        cloud storage. The metastore
      tags:
        - catalog
        - temporary_volume_credentials
      description: >-
        Get a short-lived credential for directly accessing the volume data on
        cloud storage. The metastore

        must have **external_access_enabled** flag set to true (default false).
        The caller must have the

        **EXTERNAL_USE_SCHEMA** privilege on the parent schema and this
        privilege can only be granted by

        catalog owners.


        :param operation: :class:`VolumeOperation` (optional)
          The operation performed against the volume data, either READ_VOLUME or WRITE_VOLUME. If WRITE_VOLUME
          is specified, the credentials returned will have write permissions, otherwise, it will be read only.
        :param volume_id: str (optional)
          Id of the volume to read or write.

        :returns: :class:`GenerateTemporaryVolumeCredentialResponse`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                operation:
                  type: string
                  x-enum:
                    - READ_VOLUME
                    - WRITE_VOLUME
                  description: >-
                    The operation performed against the volume data, either
                    READ_VOLUME or WRITE_VOLUME. If WRITE_VOLUME is specified,
                    the credentials returned will have write permissions,
                    otherwise, it will be read only.
                volume_id:
                  type: string
                  description: Id of the volume to read or write.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateTemporaryVolumeCredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: TemporaryVolumeCredentialsAPI
  /api/2.1/unity-catalog/volumes:
    post:
      operationId: volumes_create
      summary: Creates a new volume.
      tags:
        - catalog
        - volumes
      description: >-
        Creates a new volume.


        The user could create either an external volume or a managed volume. An
        external volume will be

        created in the specified external location, while a managed volume will
        be located in the default

        location which is specified by the parent schema, or the parent catalog,
        or the Metastore.


        For the volume creation to succeed, the user must satisfy following
        conditions:


        - The caller must be a metastore admin, or be the owner of the parent
        catalog and schema, or have the
          **USE_CATALOG** privilege on the parent catalog and the **USE_SCHEMA** privilege on the parent
          schema.
        - The caller must have **CREATE VOLUME** privilege on the parent schema.


        For an external volume, following conditions also need to satisfy


        - The caller must have **CREATE EXTERNAL VOLUME** privilege on the
        external location.

        - There are no other tables, nor volumes existing in the specified
        storage location.

        - The specified storage location is not under the location of other
        tables, nor volumes, or catalogs
          or schemas.

        :param catalog_name: str
          The name of the catalog where the schema and the volume are
        :param schema_name: str
          The name of the schema where the volume is
        :param name: str
          The name of the volume
        :param volume_type: :class:`VolumeType`
          The type of the volume. An external volume is located in the specified external location. A managed
          volume is located in the default location which is specified by the parent schema, or the parent
          catalog, or the Metastore. `Learn more
          <https://docs.databricks.com/aws/en/volumes/managed-vs-external>`__
        :param comment: str (optional)
          The comment attached to the volume
        :param storage_location: str (optional)
          The storage location on the cloud

        :returns: :class:`VolumeInfo`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                catalog_name:
                  type: string
                  description: The name of the catalog where the schema and the volume are
                schema_name:
                  type: string
                  description: The name of the schema where the volume is
                name:
                  type: string
                  description: The name of the volume
                volume_type:
                  type: string
                  x-enum:
                    - EXTERNAL
                    - MANAGED
                  description: >-
                    The type of the volume. An external volume is located in the
                    specified external location. A managed volume is located in
                    the default location which is specified by the parent
                    schema, or the parent catalog, or the Metastore. `Learn more
                    <https://docs.databricks.com/aws/en/volumes/managed-vs-external>`__
                comment:
                  type: string
                  description: The comment attached to the volume
                storage_location:
                  type: string
                  description: The storage location on the cloud
              required:
                - catalog_name
                - schema_name
                - name
                - volume_type
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VolumeInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: VolumesAPI
    get:
      operationId: volumes_list
      summary: >-
        Gets an array of volumes for the current metastore under the parent
        catalog and schema.
      tags:
        - catalog
        - volumes
      description: >-
        Gets an array of volumes for the current metastore under the parent
        catalog and schema.


        The returned volumes are filtered based on the privileges of the calling
        user. For example, the

        metastore admin is able to list all the volumes. A regular user needs to
        be the owner or have the

        **READ VOLUME** privilege on the volume to receive the volumes in the
        response. For the latter case,

        the caller must also be the owner or have the **USE_CATALOG** privilege
        on the parent catalog and the

        **USE_SCHEMA** privilege on the parent schema.


        There is no guarantee of a specific ordering of the elements in the
        array.


        PAGINATION BEHAVIOR: The API is by default paginated, a page may contain
        zero results while still

        providing a next_page_token. Clients must continue reading pages until
        next_page_token is absent,

        which is the only indication that the end of results has been reached.


        :param catalog_name: str
          The identifier of the catalog
        :param schema_name: str
          The identifier of the schema
        :param include_browse: bool (optional)
          Whether to include volumes in the response for which the principal can only access selective
          metadata for
        :param max_results: int (optional)
          Maximum number of volumes to return (page length).

          If not set, the page length is set to a server configured value (10000, as of 1/29/2024).

          - when set to a value greater than 0, the page length is the minimum of this value and a server
            configured value (10000, as of 1/29/2024);
          - when set to 0, the page length is set to a server configured value (10000, as of 1/29/2024)
            (recommended);
          - when set to a value less than 0, an invalid parameter error is returned;

          Note: this parameter controls only the maximum number of volumes to return. The actual number of
          volumes returned in a page may be smaller than this value, including 0, even if there are more
          pages.
        :param page_token: str (optional)
          Opaque token returned by a previous request. It must be included in the request to retrieve the next
          page of results (pagination).

        :returns: Iterator over :class:`VolumeInfo`
      parameters:
        - name: catalog_name
          in: query
          required: true
          schema:
            type: string
          description: The identifier of the catalog
        - name: schema_name
          in: query
          required: true
          schema:
            type: string
          description: The identifier of the schema
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include volumes in the response for which the principal
            can only access selective metadata for
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of volumes to return (page length). If not set, the
            page length is set to a server configured value (10000, as of
            1/29/2024). - when set to a value greater than 0, the page length is
            the minimum of this value and a server configured value (10000, as
            of 1/29/2024); - when set to 0, the page length is set to a server
            configured value (10000, as of 1/29/2024) (recommended); - when set
            to a value less than 0, an invalid parameter error is returned;
            Note: this parameter controls only the maximum number of volumes to
            return. The actual number of volumes returned in a page may be
            smaller than this value, including 0, even if there are more pages.
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: >-
            Opaque token returned by a previous request. It must be included in
            the request to retrieve the next page of results (pagination).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListVolumesResponseContent'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: VolumesAPI
  /api/2.1/unity-catalog/volumes/{name}:
    delete:
      operationId: volumes_delete
      summary: Deletes a volume from the specified parent catalog and schema.
      tags:
        - catalog
        - volumes
      description: >-
        Deletes a volume from the specified parent catalog and schema.


        The caller must be a metastore admin or an owner of the volume. For the
        latter case, the caller must

        also be the owner or have the **USE_CATALOG** privilege on the parent
        catalog and the **USE_SCHEMA**

        privilege on the parent schema.


        :param name: str
          The three-level (fully qualified) name of the volume
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the volume
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: VolumesAPI
    get:
      operationId: volumes_read
      summary: Gets a volume from the metastore for a specific catalog and schema.
      tags:
        - catalog
        - volumes
      description: >-
        Gets a volume from the metastore for a specific catalog and schema.


        The caller must be a metastore admin or an owner of (or have the **READ
        VOLUME** privilege on) the

        volume. For the latter case, the caller must also be the owner or have
        the **USE_CATALOG** privilege

        on the parent catalog and the **USE_SCHEMA** privilege on the parent
        schema.


        :param name: str
          The three-level (fully qualified) name of the volume
        :param include_browse: bool (optional)
          Whether to include volumes in the response for which the principal can only access selective
          metadata for

        :returns: :class:`VolumeInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the volume
        - name: include_browse
          in: query
          required: false
          schema:
            type: boolean
          description: >-
            Whether to include volumes in the response for which the principal
            can only access selective metadata for
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VolumeInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: VolumesAPI
    patch:
      operationId: volumes_update
      summary: >-
        Updates the specified volume under the specified parent catalog and
        schema.
      tags:
        - catalog
        - volumes
      description: >-
        Updates the specified volume under the specified parent catalog and
        schema.


        The caller must be a metastore admin or an owner of the volume. For the
        latter case, the caller must

        also be the owner or have the **USE_CATALOG** privilege on the parent
        catalog and the **USE_SCHEMA**

        privilege on the parent schema.


        Currently only the name, the owner or the comment of the volume could be
        updated.


        :param name: str
          The three-level (fully qualified) name of the volume
        :param comment: str (optional)
          The comment attached to the volume
        :param new_name: str (optional)
          New name for the volume.
        :param owner: str (optional)
          The identifier of the user who owns the volume

        :returns: :class:`VolumeInfo`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The three-level (fully qualified) name of the volume
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                comment:
                  type: string
                  description: The comment attached to the volume
                new_name:
                  type: string
                  description: New name for the volume.
                owner:
                  type: string
                  description: The identifier of the user who owns the volume
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VolumeInfo'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: VolumesAPI
  /api/2.1/unity-catalog/workspace-bindings/catalogs/{name}:
    get:
      operationId: workspace_bindings_get
      summary: >-
        Gets workspace bindings of the catalog. The caller must be a metastore
        admin or an owner of the
      tags:
        - catalog
        - workspace_bindings
      description: >-
        Gets workspace bindings of the catalog. The caller must be a metastore
        admin or an owner of the

        catalog.


        :param name: str
          The name of the catalog.

        :returns: :class:`GetCatalogWorkspaceBindingsResponse`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCatalogWorkspaceBindingsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: WorkspaceBindingsAPI
    patch:
      operationId: workspace_bindings_update
      summary: >-
        Updates workspace bindings of the catalog. The caller must be a
        metastore admin or an owner of the
      tags:
        - catalog
        - workspace_bindings
      description: >-
        Updates workspace bindings of the catalog. The caller must be a
        metastore admin or an owner of the

        catalog.


        :param name: str
          The name of the catalog.
        :param assign_workspaces: List[int] (optional)
          A list of workspace IDs.
        :param unassign_workspaces: List[int] (optional)
          A list of workspace IDs.

        :returns: :class:`UpdateCatalogWorkspaceBindingsResponse`
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the catalog.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                assign_workspaces:
                  type: array
                  items:
                    type: integer
                  description: A list of workspace IDs.
                unassign_workspaces:
                  type: array
                  items:
                    type: integer
                  description: A list of workspace IDs.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateCatalogWorkspaceBindingsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: WorkspaceBindingsAPI
  /api/2.1/unity-catalog/bindings/{securable_type}/{securable_name}:
    get:
      operationId: workspace_bindings_get_bindings
      summary: >-
        Gets workspace bindings of the securable. The caller must be a metastore
        admin or an owner of the
      tags:
        - catalog
        - workspace_bindings
      description: >-
        Gets workspace bindings of the securable. The caller must be a metastore
        admin or an owner of the

        securable.


        NOTE: we recommend using max_results=0 to use the paginated version of
        this API. Unpaginated calls

        will be deprecated soon.


        PAGINATION BEHAVIOR: When using pagination (max_results >= 0), a page
        may contain zero results while

        still providing a next_page_token. Clients must continue reading pages
        until next_page_token is

        absent, which is the only indication that the end of results has been
        reached.


        :param securable_type: str
          The type of the securable to bind to a workspace (catalog, storage_credential, credential, or
          external_location).
        :param securable_name: str
          The name of the securable.
        :param max_results: int (optional)
          Maximum number of workspace bindings to return.

          - When set to 0, the page length is set to a server configured value (recommended);
          - When set to a value greater than 0, the page length is the minimum of this value and a server
            configured value;
          - When set to a value less than 0, an invalid parameter error is returned;
          - If not set, all the workspace bindings are returned (not recommended).
        :param page_token: str (optional)
          Opaque pagination token to go to next page based on previous query.

        :returns: Iterator over :class:`WorkspaceBinding`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: >-
            The type of the securable to bind to a workspace (catalog,
            storage_credential, credential, or external_location).
        - name: securable_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the securable.
        - name: max_results
          in: query
          required: false
          schema:
            type: integer
          description: >-
            Maximum number of workspace bindings to return. - When set to 0, the
            page length is set to a server configured value (recommended); -
            When set to a value greater than 0, the page length is the minimum
            of this value and a server configured value; - When set to a value
            less than 0, an invalid parameter error is returned; - If not set,
            all the workspace bindings are returned (not recommended).
        - name: page_token
          in: query
          required: false
          schema:
            type: string
          description: Opaque pagination token to go to next page based on previous query.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetWorkspaceBindingsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: WorkspaceBindingsAPI
    patch:
      operationId: workspace_bindings_update_bindings
      summary: >-
        Updates workspace bindings of the securable. The caller must be a
        metastore admin or an owner of the
      tags:
        - catalog
        - workspace_bindings
      description: >-
        Updates workspace bindings of the securable. The caller must be a
        metastore admin or an owner of the

        securable.


        :param securable_type: str
          The type of the securable to bind to a workspace (catalog, storage_credential, credential, or
          external_location).
        :param securable_name: str
          The name of the securable.
        :param add: List[:class:`WorkspaceBinding`] (optional)
          List of workspace bindings to add. If a binding for the workspace already exists with a different
          binding_type, adding it again with a new binding_type will update the existing binding (e.g., from
          READ_WRITE to READ_ONLY).
        :param remove: List[:class:`WorkspaceBinding`] (optional)
          List of workspace bindings to remove.

        :returns: :class:`UpdateWorkspaceBindingsResponse`
      parameters:
        - name: securable_type
          in: path
          required: true
          schema:
            type: string
          description: >-
            The type of the securable to bind to a workspace (catalog,
            storage_credential, credential, or external_location).
        - name: securable_name
          in: path
          required: true
          schema:
            type: string
          description: The name of the securable.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                add:
                  type: array
                  items:
                    type: object
                    properties:
                      workspace_id:
                        type: integer
                      binding_type:
                        type: string
                        x-enum:
                          - BINDING_TYPE_READ_ONLY
                          - BINDING_TYPE_READ_WRITE
                        description: One of READ_WRITE/READ_ONLY. Default is READ_WRITE.
                    required:
                      - workspace_id
                  description: >-
                    List of workspace bindings to add. If a binding for the
                    workspace already exists with a different binding_type,
                    adding it again with a new binding_type will update the
                    existing binding (e.g., from READ_WRITE to READ_ONLY).
                remove:
                  type: array
                  items:
                    type: object
                    properties:
                      workspace_id:
                        type: integer
                      binding_type:
                        type: string
                        x-enum:
                          - BINDING_TYPE_READ_ONLY
                          - BINDING_TYPE_READ_WRITE
                        description: One of READ_WRITE/READ_ONLY. Default is READ_WRITE.
                    required:
                      - workspace_id
                  description: List of workspace bindings to remove.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateWorkspaceBindingsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: WorkspaceBindingsAPI
components:
  schemas:
    AccessRequestDestinations:
      type: object
      properties:
        securable:
          $ref: '#/components/schemas/Securable'
        are_any_destinations_hidden:
          type: boolean
          description: >-
            Indicates whether any destinations are hidden from the caller due to
            a lack of permissions. This value is true if the caller does not
            have permission to see all destinations.
        destination_source_securable:
          $ref: '#/components/schemas/Securable'
          description: >-
            The source securable from which the destinations are inherited.
            Either the same value as securable (if destination is set directly
            on the securable) or the nearest parent securable with destinations
            set.
        destinations:
          type: array
          items:
            $ref: '#/components/schemas/NotificationDestination'
          description: The access request destinations for the securable.
        full_name:
          type: string
          description: >-
            The full name of the securable. Redundant with the name in the
            securable object, but necessary for Terraform integration
        securable_type:
          type: string
          description: >-
            The type of the securable. Redundant with the type in the securable
            object, but necessary for Terraform integration
      required:
        - securable
    AccountsCreateMetastoreAssignmentResponse:
      type: object
      properties: {}
      description: The metastore assignment was successfully created.
    AccountsCreateMetastoreResponse:
      type: object
      properties:
        metastore_info:
          $ref: '#/components/schemas/MetastoreInfo'
    AccountsCreateStorageCredentialInfo:
      type: object
      properties:
        credential_info:
          $ref: '#/components/schemas/StorageCredentialInfo'
    AccountsDeleteMetastoreAssignmentResponse:
      type: object
      properties: {}
      description: The metastore assignment was successfully deleted.
    AccountsDeleteMetastoreResponse:
      type: object
      properties: {}
      description: The metastore was successfully deleted.
    AccountsDeleteStorageCredentialResponse:
      type: object
      properties: {}
      description: The storage credential was successfully deleted.
    AccountsGetMetastoreResponse:
      type: object
      properties:
        metastore_info:
          $ref: '#/components/schemas/MetastoreInfo'
      description: The metastore was successfully returned.
    AccountsListMetastoresResponse:
      type: object
      properties:
        metastores:
          type: array
          items:
            $ref: '#/components/schemas/MetastoreInfo'
          description: An array of metastore information objects.
      description: Metastores were returned successfully.
    AccountsMetastoreAssignment:
      type: object
      properties:
        metastore_assignment:
          $ref: '#/components/schemas/MetastoreAssignment'
      description: The workspace metastore assignment was successfully returned.
    AccountsStorageCredentialInfo:
      type: object
      properties:
        credential_info:
          $ref: '#/components/schemas/StorageCredentialInfo'
      description: The storage credential was successfully retrieved.
    AccountsUpdateMetastoreAssignmentResponse:
      type: object
      properties: {}
      description: The metastore assignment was successfully updated.
    AccountsUpdateMetastoreResponse:
      type: object
      properties:
        metastore_info:
          $ref: '#/components/schemas/MetastoreInfo'
      description: The metastore update request succeeded.
    AccountsUpdateStorageCredentialResponse:
      type: object
      properties:
        credential_info:
          $ref: '#/components/schemas/StorageCredentialInfo'
      description: The storage credential was successfully updated.
    AgentService:
      type: object
      properties:
        agent_service_type:
          $ref: '#/components/schemas/AgentServiceAgentServiceType'
          description: >-
            Type of agent service. Required on create. Immutable after creation.
            For future internal / genie / app kinds, determines how the backing
            agent surface is provisioned.
        browse_only:
          type: boolean
          description: >-
            Whether the caller sees only metadata available through the BROWSE
            privilege.
        comment:
          type: string
          description: User-provided description.
        config:
          $ref: '#/components/schemas/AgentServiceConfig'
          description: >-
            Operational configuration: connection, base_path, system_prompt,
            model_service, optional inference table, optional rate limits.
            Required on CreateAgentService; on UpdateAgentService it is required
            only when ``config`` (or a ``config.*`` subpath) appears in
            ``update_mask``.
        create_time:
          type: string
          format: date-time
          description: When the agent service was created.
        created_by:
          type: string
          description: Creator identity.
        effective_owner:
          type: string
          description: >-
            The resolved owner of the agent service. Falls back to the caller's
            identity when ``owner`` is not explicitly set on creation.
        etag:
          type: string
          description: >-
            Optimistic concurrency control token. Server-generated from the
            entity's state and returned on every read. To use it as an if-match
            precondition on a mutation, echo the last-read value back via the
            dedicated ``etag`` field on the Update / Delete request; the server
            rejects the mutation if the stored etag differs.
        metastore_id:
          type: string
          description: Metastore hosting the agent service.
        name:
          type: string
          description: >-
            Resource name of the agent service. Format:
            ``agent-services/{catalog}.{schema}.{agent_service}``. Each
            ``{...}`` component is capped at 255 characters individually.
            Server-derived on Create from ``parent`` + ``agent_service_id``;
            required and immutable on Update/Get/Delete.
        owner:
          type: string
          description: >-
            The owner of the agent service. Write-only; read owner via
            effective_owner.
        update_time:
          type: string
          format: date-time
          description: When the agent service was last modified.
        updated_by:
          type: string
          description: Identity of the last updater.
      description: >-
        An agent service represents a governed registration of an external AI
        agent (Bedrock Agents,
            Snowflake Cortex Agents, Foundry, etc) in Unity Catalog. It acts as a schema-scoped securable
            that references a UC Connection holding the agent platform's auth + endpoint, and exposes the
            agent for discovery, authorization, and audit logging. Initial kind: AGENT_SERVICE_STANDARD.
            Future kinds (INTERNAL, GENIE, APP) can be added as separate enum values.
    AgentServiceConfig:
      type: object
      properties:
        base_path:
          type: string
          description: >-
            Vendor base path appended to the connection url at invocation time.
            Bedrock example: ``/agents/AGENT123/agentAliases/ALIAS1/sessions``.
            Empty when the agent platform does not require a per-agent suffix.
        inference_table:
          $ref: '#/components/schemas/InferenceTableConfig'
          description: >-
            Inference table config for invocation payload logging. Reuses the
            shared InferenceTableConfig (same as
            ModelServiceConfig.inference_table).
        model_service:
          type: string
          description: >-
            Resource name of the model service backing this agent. Empty when
            the agent platform manages its own model selection (e.g. Bedrock
            Agents). Format:
            ``model-services/{catalog}.{schema}.{model_service}``.
        rate_limits:
          type: array
          items:
            $ref: '#/components/schemas/RateLimit'
          description: >-
            Per-principal rate limits applied to invocations routed through this
            agent service. Repeated to support per-USER / USER_GROUP /
            SERVICE_PRINCIPAL / SERVICE / USER_DEFAULT scopes simultaneously,
            mirroring McpServiceConfig.rate_limits. Empty when no rate limit is
            configured.
        source_connection:
          $ref: '#/components/schemas/AgentServiceConfigSourceConnection'
          description: External agent referenced via a UC Connection.
        system_prompt:
          type: string
          description: System prompt prepended to invocations. Max 8 KB.
      description: >-
        Operational configuration for an agent service. Groups the source
        connection, base path, system
            prompt, model service reference, optional inference table, and optional rate limits. The
            ``source_connection.name`` is supplied on create.
    AgentServiceConfigSourceConnection:
      type: object
      properties:
        name:
          type: string
        is_deleted:
          type: boolean
      required:
        - name
      description: >-
        UC Connection for the agent platform, such as Bedrock Agents, Snowflake
        Cortex Agents, or
            Foundry. On create, provide ``name`` in the schema-scoped form
            ``connections/{catalog}.{schema}.{connection}``. On read, the service returns ``name``.
    ArtifactAllowlistInfo:
      type: object
      properties:
        artifact_matchers:
          type: array
          items:
            $ref: '#/components/schemas/ArtifactMatcher'
        created_at:
          type: integer
          description: >-
            Time at which this artifact allowlist was set, in epoch
            milliseconds.
        created_by:
          type: string
          description: Username of the user who set the artifact allowlist.
        metastore_id:
          type: string
          description: Unique identifier of parent metastore.
    ArtifactMatcher:
      type: object
      properties:
        artifact:
          type: string
        match_type:
          $ref: '#/components/schemas/MatchType'
          description: The pattern matching type of the artifact
      required:
        - artifact
        - match_type
    AssignResponse:
      type: object
      properties: {}
    AwsCredentials:
      type: object
      properties:
        access_key_id:
          type: string
          description: The access key ID that identifies the temporary credentials.
        access_point:
          type: string
          description: >-
            The Amazon Resource Name (ARN) of the S3 access point for temporary
            credentials related the external location.
        secret_access_key:
          type: string
          description: The secret access key that can be used to sign AWS API requests.
        session_token:
          type: string
          description: >-
            The token that users must pass to AWS API to use the temporary
            credentials.
      description: |-
        AWS temporary credentials for API authentication. Read more at
            https://docs.aws.amazon.com/STS/latest/APIReference/API_Credentials.html.
    AwsIamRole:
      type: object
      properties:
        external_id:
          type: string
          description: >-
            The external ID used in role assumption to prevent the confused
            deputy problem.
        role_arn:
          type: string
          description: >-
            The Amazon Resource Name (ARN) of the AWS IAM role used to vend
            temporary credentials.
        unity_catalog_iam_arn:
          type: string
          description: >-
            The Amazon Resource Name (ARN) of the AWS IAM user managed by
            Databricks. This is the identity that is going to assume the AWS IAM
            role.
      description: The AWS IAM role configuration
    AwsIamRoleRequest:
      type: object
      properties:
        role_arn:
          type: string
          description: >-
            The Amazon Resource Name (ARN) of the AWS IAM role used to vend
            temporary credentials.
      required:
        - role_arn
      description: The AWS IAM role configuration
    AwsIamRoleResponse:
      type: object
      properties:
        role_arn:
          type: string
          description: >-
            The Amazon Resource Name (ARN) of the AWS IAM role used to vend
            temporary credentials.
        external_id:
          type: string
          description: >-
            The external ID used in role assumption to prevent the confused
            deputy problem.
        unity_catalog_iam_arn:
          type: string
          description: >-
            The Amazon Resource Name (ARN) of the AWS IAM user managed by
            Databricks. This is the identity that is going to assume the AWS IAM
            role.
      required:
        - role_arn
      description: The AWS IAM role configuration
    AwsSqsQueue:
      type: object
      properties:
        managed_resource_id:
          type: string
        queue_url:
          type: string
          description: >-
            The AQS queue url in the format
            https://sqs.{region}.amazonaws.com/{account id}/{queue name}. Only
            required for provided_sqs.
    AzureActiveDirectoryToken:
      type: object
      properties:
        aad_token:
          type: string
          description: >-
            Opaque token that contains claims that you can use in Azure Active
            Directory to access cloud services.
      description: >-
        Azure Active Directory token, essentially the Oauth token for Azure
        Service Principal or Managed
            Identity. Read more at
            https://learn.microsoft.com/en-us/azure/databricks/dev-tools/api/latest/aad/service-prin-aad-token
    AzureEncryptionSettings:
      type: object
      properties:
        azure_tenant_id:
          type: string
        azure_cmk_access_connector_id:
          type: string
        azure_cmk_managed_identity_id:
          type: string
      required:
        - azure_tenant_id
    AzureManagedIdentity:
      type: object
      properties:
        access_connector_id:
          type: string
          description: >-
            The Azure resource ID of the Azure Databricks Access Connector. Use
            the format
            ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.Databricks/accessConnectors/{connector-name}``.
        credential_id:
          type: string
          description: The Databricks internal ID that represents this managed identity.
        managed_identity_id:
          type: string
          description: >-
            The Azure resource ID of the managed identity. Use the format,
            ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identity-name}``
            This is only available for user-assgined identities. For
            system-assigned identities, the access_connector_id is used to
            identify the identity. If this field is not provided, then we assume
            the AzureManagedIdentity is using the system-assigned identity.
      required:
        - access_connector_id
      description: The Azure managed identity configuration.
    AzureManagedIdentityRequest:
      type: object
      properties:
        access_connector_id:
          type: string
          description: >-
            The Azure resource ID of the Azure Databricks Access Connector. Use
            the format
            ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.Databricks/accessConnectors/{connector-name}``.
        managed_identity_id:
          type: string
          description: >-
            The Azure resource ID of the managed identity. Use the format,
            ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identity-name}``
            This is only available for user-assgined identities. For
            system-assigned identities, the access_connector_id is used to
            identify the identity. If this field is not provided, then we assume
            the AzureManagedIdentity is using the system-assigned identity.
      required:
        - access_connector_id
      description: The Azure managed identity configuration.
    AzureManagedIdentityResponse:
      type: object
      properties:
        access_connector_id:
          type: string
          description: >-
            The Azure resource ID of the Azure Databricks Access Connector. Use
            the format
            ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.Databricks/accessConnectors/{connector-name}``.
        credential_id:
          type: string
          description: The Databricks internal ID that represents this managed identity.
        managed_identity_id:
          type: string
          description: >-
            The Azure resource ID of the managed identity. Use the format,
            ``/subscriptions/{guid}/resourceGroups/{rg-name}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identity-name}``
            This is only available for user-assgined identities. For
            system-assigned identities, the access_connector_id is used to
            identify the identity. If this field is not provided, then we assume
            the AzureManagedIdentity is using the system-assigned identity.
      required:
        - access_connector_id
      description: The Azure managed identity configuration.
    AzureQueueStorage:
      type: object
      properties:
        managed_resource_id:
          type: string
        queue_url:
          type: string
          description: >-
            The AQS queue url in the format https://{storage
            account}.queue.core.windows.net/{queue name} Only required for
            provided_aqs.
        resource_group:
          type: string
          description: >-
            Optional resource group for the queue, event grid subscription, and
            external location storage account. Only required for locations with
            a service principal storage credential
        subscription_id:
          type: string
          description: >-
            Optional subscription id for the queue, event grid subscription, and
            external location storage account. Required for locations with a
            service principal storage credential
    AzureServicePrincipal:
      type: object
      properties:
        directory_id:
          type: string
          description: >-
            The directory ID corresponding to the Azure Active Directory (AAD)
            tenant of the application.
        application_id:
          type: string
          description: >-
            The application ID of the application registration within the
            referenced AAD tenant.
        client_secret:
          type: string
          description: The client secret generated for the above app ID in AAD.
      required:
        - directory_id
        - application_id
        - client_secret
      description: >-
        The Azure service principal configuration. Only applicable when purpose
        is **STORAGE**.
    AzureUserDelegationSas:
      type: object
      properties:
        sas_token:
          type: string
          description: >-
            The signed URI (SAS Token) used to access blob services for a given
            path
      description: |-
        Azure temporary credentials for API authentication. Read more at
            https://docs.microsoft.com/en-us/rest/api/storageservices/create-user-delegation-sas
    BatchCreateAccessRequestsResponse:
      type: object
      properties:
        responses:
          type: array
          items:
            $ref: '#/components/schemas/CreateAccessRequestResponse'
    CancelRefreshResponse:
      type: object
      properties: {}
    CatalogFederationConfig:
      type: object
      properties:
        schema_filter_type:
          $ref: '#/components/schemas/SchemaFilterType'
          description: Controls how the catalog's schema filter items are treated.
      description: Federation-only configuration for a FOREIGN catalog.
    CatalogInfo:
      type: object
      properties:
        browse_only:
          type: boolean
        catalog_federation_config:
          $ref: '#/components/schemas/CatalogFederationConfig'
          description: Federation-only configuration, present only for FOREIGN catalogs.
        catalog_type:
          $ref: '#/components/schemas/CatalogType'
        comment:
          type: string
          description: User-provided free-form text description.
        connection_name:
          type: string
          description: The name of the connection to an external data source.
        conversion_info:
          $ref: '#/components/schemas/ConversionInfo'
          description: Status of conversion of FOREIGN catalog to UC Native catalog.
        created_at:
          type: integer
          description: Time at which this catalog was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of catalog creator.
        custom_max_retention_hours:
          type: integer
          description: Custom maximum retention period in hours for the catalog
        dr_replication_info:
          $ref: '#/components/schemas/DrReplicationInfo'
          description: Disaster Recovery replication state snapshot.
        effective_predictive_optimization_flag:
          $ref: '#/components/schemas/EffectivePredictiveOptimizationFlag'
        enable_predictive_optimization:
          $ref: '#/components/schemas/EnablePredictiveOptimization'
          description: >-
            Whether predictive optimization should be enabled for this object
            and objects under it.
        full_name:
          type: string
          description: The full name of the catalog. Corresponds with the name field.
        isolation_mode:
          $ref: '#/components/schemas/CatalogIsolationMode'
          description: >-
            Whether the current securable is accessible from all workspaces or a
            specific set of workspaces.
        managed_encryption_settings:
          $ref: '#/components/schemas/EncryptionSettings'
          description: Control CMK encryption for managed catalog data
        metastore_id:
          type: string
          description: Unique identifier of parent metastore.
        name:
          type: string
          description: Name of catalog.
        options:
          type: object
          description: A map of key-value properties attached to the securable.
        owner:
          type: string
          description: Username of current owner of catalog.
        properties:
          type: object
          description: A map of key-value properties attached to the securable.
        provider_name:
          type: string
          description: >-
            The name of delta sharing provider. A Delta Sharing catalog is a
            catalog that is based on a Delta share on a remote sharing server.
        provisioning_info:
          $ref: '#/components/schemas/ProvisioningInfo'
        securable_type:
          $ref: '#/components/schemas/SecurableType'
        share_name:
          type: string
          description: The name of the share under the share provider.
        storage_location:
          type: string
          description: Storage Location URL (full path) for managed tables within catalog.
        storage_root:
          type: string
          description: Storage root URL for managed tables within catalog.
        updated_at:
          type: integer
          description: Time at which this catalog was last modified, in epoch milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified catalog.
    CloudflareApiToken:
      type: object
      properties:
        access_key_id:
          type: string
          description: The access key ID associated with the API token.
        secret_access_key:
          type: string
          description: The secret access token generated for the above access key ID.
        account_id:
          type: string
          description: The ID of the account associated with the API token.
      required:
        - access_key_id
        - secret_access_key
      description: |-
        The Cloudflare API token configuration. Read more at
            https://developers.cloudflare.com/r2/api/s3/tokens/
    ColumnInfo:
      type: object
      properties:
        comment:
          type: string
        mask:
          $ref: '#/components/schemas/ColumnMask'
        name:
          type: string
          description: Name of Column.
        nullable:
          type: boolean
          description: 'Whether field may be Null (default: true).'
        partition_index:
          type: integer
          description: Partition index for column.
        position:
          type: integer
          description: Ordinal position of column (starting at position 0).
        type_interval_type:
          type: string
          description: Format of IntervalType.
        type_json:
          type: string
          description: Full data type specification, JSON-serialized.
        type_name:
          $ref: '#/components/schemas/ColumnTypeName'
        type_precision:
          type: integer
          description: Digits of precision; required for DecimalTypes.
        type_scale:
          type: integer
          description: Digits to right of decimal; Required for DecimalTypes.
        type_text:
          type: string
          description: Full data type specification as SQL/catalogString text.
    ColumnMask:
      type: object
      properties:
        function_name:
          type: string
        using_arguments:
          type: array
          items:
            $ref: '#/components/schemas/PolicyFunctionArgument'
          description: >-
            The list of additional table columns or literals to be passed as
            additional arguments to a column mask function. This is the
            replacement of the deprecated using_column_names field and carries
            information about the types (alias or constant) of the arguments to
            the mask function.
        using_column_names:
          type: array
          items:
            type: string
          description: >-
            The list of additional table columns to be passed as input to the
            column mask function. The first arg of the mask function should be
            of the type of the column being masked and the types of the rest of
            the args should match the types of columns in 'using_column_names'.
    ColumnMaskOptions:
      type: object
      properties:
        function_name:
          type: string
        on_column:
          type: string
          description: >-
            The alias of the column to be masked. The alias must refer to one of
            matched columns. The values of the column is passed to the column
            mask function as the first argument. Required on create and update.
        using:
          type: array
          items:
            $ref: '#/components/schemas/FunctionArgument'
          description: >-
            Optional list of column aliases or constant literals to be passed as
            additional arguments to the column mask function. The type of each
            column should match the positional argument of the column mask
            function.
      required:
        - function_name
        - on_column
    ColumnRelationship:
      type: object
      properties:
        source:
          type: string
        target:
          type: string
    ColumnTagValueExtraction:
      type: object
      properties:
        column_alias:
          type: string
          description: The alias from MATCH COLUMNS that identifies the column.
        tag_key:
          type: string
          description: 1024 matches the max_length on FunctionArgument.constant above.
      required:
        - column_alias
        - tag_key
      description: >-
        Extracts the value of a column-level tag: get_column_tag_value(col,
        "tagKey").
    ConditionalDisplay:
      type: object
      properties:
        depends_on_option:
          type: string
          description: >-
            The name of the option whose value determines visibility of this
            option.
        hidden_when_values:
          type: array
          items:
            type: string
          description: >-
            The values of the depends_on_option that will hide this option. If
            empty or not set, this option follows default visibility (shown
            unless is_hidden is true). If depends_on_option has any of these
            values, this option is hidden.
      description: >-
        Defines when an option should be hidden based on another option's value.
        For example, for
            pre-created OAuth connections, some options are conditionally hidden. This field works in
            conjunction with OptionSpec.is_hidden:

            - If OptionSpec.is_hidden is true, the option is always hidden regardless of ConditionalDisplay.
            - If OptionSpec.is_hidden is false (or unset), ConditionalDisplay determines visibility:
            - If depends_on_option matches any value in hidden_when_values, hide this option.
            - Otherwise, show this option.
    ConnectionDependency:
      type: object
      properties:
        connection_name:
          type: string
          description: >-
            Full name of the dependent connection, in the form of
            **connection_name**.
      description: A connection that is dependent on a SQL object.
    ConnectionInfo:
      type: object
      properties:
        comment:
          type: string
        connection_id:
          type: string
          description: Unique identifier of the Connection.
        connection_type:
          $ref: '#/components/schemas/ConnectionType'
          description: The type of connection.
        created_at:
          type: integer
          description: Time at which this connection was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of connection creator.
        credential_type:
          $ref: '#/components/schemas/CredentialType'
          description: The type of credential.
        environment_settings:
          $ref: '#/components/schemas/EnvironmentSettings'
          description: >-
            [Create,Update:OPT] Connection environment settings as
            EnvironmentSettings object.
        full_name:
          type: string
          description: Full name of connection.
        metastore_id:
          type: string
          description: Unique identifier of parent metastore.
        name:
          type: string
          description: Name of the connection.
        options:
          type: object
          description: A map of key-value properties attached to the securable.
        owner:
          type: string
          description: Username of current owner of the connection.
        properties:
          type: object
          description: A map of key-value properties attached to the securable.
        provisioning_info:
          $ref: '#/components/schemas/ProvisioningInfo'
        read_only:
          type: boolean
          description: If the connection is read only.
        secrets:
          type: object
          description: >-
            A map of option names to UC Secret references. Keys are connection
            option names (same as in OptionsKVPairs) and values are UC Secret
            fully qualified names.
        securable_type:
          $ref: '#/components/schemas/SecurableType'
        updated_at:
          type: integer
          description: Time at which this connection was updated, in epoch milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified connection.
        url:
          type: string
          description: URL of the remote data source, extracted from options.
    ContinuousUpdateStatus:
      type: object
      properties:
        initial_pipeline_sync_progress:
          $ref: '#/components/schemas/PipelineProgress'
          description: Progress of the initial data synchronization.
        last_processed_commit_version:
          type: integer
          description: >-
            The last source table Delta version that was synced to the online
            table. Note that this Delta version may not be completely synced to
            the online table yet.
        timestamp:
          type: string
          description: >-
            The timestamp of the last time any data was synchronized from the
            source table to the online table.
      description: >-
        Detailed status of an online table. Shown if the online table is in the
        ONLINE_CONTINUOUS_UPDATE
            or the ONLINE_UPDATING_PIPELINE_RESOURCES state.
    ConversionInfo:
      type: object
      properties:
        state:
          $ref: '#/components/schemas/ConversionInfoState'
          description: The conversion state of the resource.
      description: Status of conversion of FOREIGN entity into UC Native entity.
    CreateAccessRequest:
      type: object
      properties:
        behalf_of:
          $ref: '#/components/schemas/Principal'
        comment:
          type: string
          description: >-
            Optional. Comment associated with the request. At most 200
            characters, can only contain lowercase/uppercase letters (a-z, A-Z),
            numbers (0-9), punctuation, and spaces.
        securable_permissions:
          type: array
          items:
            $ref: '#/components/schemas/SecurablePermissions'
          description: >-
            List of securables and their corresponding requested UC privileges.
            At most 30 securables can be requested for a principal per batched
            call. Each securable can only be requested once per principal.
    CreateAccessRequestResponse:
      type: object
      properties:
        behalf_of:
          $ref: '#/components/schemas/Principal'
        request_destinations:
          type: array
          items:
            $ref: '#/components/schemas/AccessRequestDestinations'
          description: >-
            The access request destinations for all the securables the principal
            requested.
    CreateAccountsMetastore:
      type: object
      properties:
        name:
          type: string
        external_access_enabled:
          type: boolean
          description: >-
            Whether to allow non-DBR clients to directly access entities under
            the metastore.
        region:
          type: string
          description: >-
            Cloud region which the metastore serves (e.g., ``us-west-2``,
            ``westus``).
        storage_root:
          type: string
          description: The storage root URL for metastore
      required:
        - name
    CreateAccountsStorageCredential:
      type: object
      properties:
        name:
          type: string
        aws_iam_role:
          $ref: '#/components/schemas/AwsIamRoleRequest'
          description: The AWS IAM role configuration.
        azure_managed_identity:
          $ref: '#/components/schemas/AzureManagedIdentityRequest'
          description: The Azure managed identity configuration.
        azure_service_principal:
          $ref: '#/components/schemas/AzureServicePrincipal'
          description: The Azure service principal configuration.
        cloudflare_api_token:
          $ref: '#/components/schemas/CloudflareApiToken'
          description: The Cloudflare API token configuration.
        comment:
          type: string
          description: Comment associated with the credential.
        databricks_gcp_service_account:
          $ref: '#/components/schemas/DatabricksGcpServiceAccountRequest'
          description: The Databricks managed GCP service account configuration.
        read_only:
          type: boolean
          description: >-
            Whether the credential is usable only for read operations. Only
            applicable when purpose is **STORAGE**.
      required:
        - name
    CreateFunction:
      type: object
      properties:
        name:
          type: string
        catalog_name:
          type: string
          description: Name of parent Catalog.
        schema_name:
          type: string
          description: Name of parent Schema relative to its parent Catalog.
        input_params:
          $ref: '#/components/schemas/FunctionParameterInfos'
          description: Function input parameters.
        data_type:
          $ref: '#/components/schemas/ColumnTypeName'
          description: Scalar function return data type.
        full_data_type:
          type: string
          description: Pretty printed function data type.
        routine_body:
          $ref: '#/components/schemas/CreateFunctionRoutineBody'
          description: >-
            Function language. When **EXTERNAL** is used, the language of the
            routine function should be specified in the **external_language**
            field, and the **return_params** of the function cannot be used (as
            **TABLE** return type is not supported), and the **sql_data_access**
            field must be **NO_SQL**.
        routine_definition:
          type: string
          description: Function body.
        parameter_style:
          $ref: '#/components/schemas/CreateFunctionParameterStyle'
          description: Function parameter style. **S** is the value for SQL.
        is_deterministic:
          type: boolean
          description: Whether the function is deterministic.
        sql_data_access:
          $ref: '#/components/schemas/CreateFunctionSqlDataAccess'
          description: Function SQL data access.
        is_null_call:
          type: boolean
          description: Function null call.
        security_type:
          $ref: '#/components/schemas/CreateFunctionSecurityType'
          description: Function security type.
        specific_name:
          type: string
          description: Specific name of the function; Reserved for future use.
        comment:
          type: string
          description: User-provided free-form text description.
        external_language:
          type: string
          description: External function language.
        external_name:
          type: string
          description: External function name.
        properties:
          type: string
          description: JSON-serialized key-value pair map, encoded (escaped) as a string.
        return_params:
          $ref: '#/components/schemas/FunctionParameterInfos'
          description: Table function return parameters.
        routine_dependencies:
          $ref: '#/components/schemas/DependencyList'
          description: function dependencies.
        sql_path:
          type: string
          description: >-
            List of schemes whose objects can be referenced without
            qualification.
      required:
        - name
        - catalog_name
        - schema_name
        - input_params
        - data_type
        - full_data_type
        - routine_body
        - routine_definition
        - parameter_style
        - is_deterministic
        - sql_data_access
        - is_null_call
        - security_type
        - specific_name
    CreateMetastoreAssignment:
      type: object
      properties:
        workspace_id:
          type: integer
        metastore_id:
          type: string
          description: The unique ID of the metastore.
        default_catalog_name:
          type: string
          description: >-
            The name of the default catalog in the metastore. This field is
            deprecated. Please use "Default Namespace API" to configure the
            default catalog for a Databricks workspace.
      required:
        - workspace_id
        - metastore_id
        - default_catalog_name
    CreateRequestExternalLineage:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/ExternalLineageObject'
        target:
          $ref: '#/components/schemas/ExternalLineageObject'
          description: Target object of the external lineage relationship.
        columns:
          type: array
          items:
            $ref: '#/components/schemas/ColumnRelationship'
          description: List of column relationships between source and target objects.
        id:
          type: string
          description: Unique identifier of the external lineage relationship.
        properties:
          type: object
          description: >-
            Key-value properties associated with the external lineage
            relationship.
      required:
        - source
        - target
    CredentialDependency:
      type: object
      properties:
        credential_name:
          type: string
          description: >-
            Full name of the dependent credential, in the form of
            **credential_name**.
      description: A credential that is dependent on a SQL object.
    CredentialInfo:
      type: object
      properties:
        aws_iam_role:
          $ref: '#/components/schemas/AwsIamRole'
        azure_managed_identity:
          $ref: '#/components/schemas/AzureManagedIdentity'
          description: The Azure managed identity configuration.
        azure_service_principal:
          $ref: '#/components/schemas/AzureServicePrincipal'
          description: The Azure service principal configuration.
        comment:
          type: string
          description: Comment associated with the credential.
        created_at:
          type: integer
          description: Time at which this credential was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of credential creator.
        databricks_gcp_service_account:
          $ref: '#/components/schemas/DatabricksGcpServiceAccount'
          description: The Databricks managed GCP service account configuration.
        full_name:
          type: string
          description: The full name of the credential.
        id:
          type: string
          description: The unique identifier of the credential.
        isolation_mode:
          $ref: '#/components/schemas/IsolationMode'
          description: >-
            Whether the current securable is accessible from all workspaces or a
            specific set of workspaces.
        metastore_id:
          type: string
          description: Unique identifier of the parent metastore.
        name:
          type: string
          description: >-
            The credential name. The name must be unique among storage and
            service credentials within the metastore.
        owner:
          type: string
          description: Username of current owner of credential.
        purpose:
          $ref: '#/components/schemas/CredentialPurpose'
          description: Indicates the purpose of the credential.
        read_only:
          type: boolean
          description: >-
            Whether the credential is usable only for read operations. Only
            applicable when purpose is **STORAGE**.
        updated_at:
          type: integer
          description: >-
            Time at which this credential was last modified, in epoch
            milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified the credential.
        used_for_managed_storage:
          type: boolean
          description: >-
            Whether this credential is the current metastore's root storage
            credential. Only applicable when purpose is **STORAGE**.
    CredentialValidationResult:
      type: object
      properties:
        message:
          type: string
        result:
          $ref: '#/components/schemas/ValidateCredentialResult'
          description: The results of the tested operation.
    DatabricksGcpServiceAccount:
      type: object
      properties:
        credential_id:
          type: string
          description: The Databricks internal ID that represents this managed identity.
        email:
          type: string
          description: The email of the service account.
        private_key_id:
          type: string
          description: The ID that represents the private key for this Service Account
      description: >-
        GCP long-lived credential. Databricks-created Google Cloud Storage
        service account.
    DatabricksGcpServiceAccountRequest:
      type: object
      properties: {}
      description: >-
        GCP long-lived credential. Databricks-created Google Cloud Storage
        service account.
    DatabricksGcpServiceAccountResponse:
      type: object
      properties:
        credential_id:
          type: string
          description: The Databricks internal ID that represents this managed identity.
        email:
          type: string
          description: The email of the service account.
      description: >-
        GCP long-lived credential. Databricks-created Google Cloud Storage
        service account.
    DeleteCredentialResponse:
      type: object
      properties: {}
    DeleteMonitorResponse:
      type: object
      properties: {}
    DeletePolicyResponse:
      type: object
      properties: {}
    DeleteRequestExternalLineage:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/ExternalLineageObject'
        target:
          $ref: '#/components/schemas/ExternalLineageObject'
          description: Target object of the external lineage relationship.
        id:
          type: string
          description: Unique identifier of the external lineage relationship.
      required:
        - source
        - target
    DeleteResponse:
      type: object
      properties: {}
    DeleteTableConstraintResponse:
      type: object
      properties: {}
    DeltaRuntimePropertiesKvPairs:
      type: object
      properties:
        delta_runtime_properties:
          type: object
          description: A map of key-value properties attached to the securable.
      required:
        - delta_runtime_properties
      description: >-
        Properties pertaining to the current state of the delta table as given
        by the commit server.
            This does not contain **delta.*** (input) properties in **TableInfo.properties**.
    DenyOptions:
      type: object
      properties:
        privileges:
          type: array
          items:
            type: string
      required:
        - privileges
    Dependency:
      type: object
      properties:
        connection:
          $ref: '#/components/schemas/ConnectionDependency'
        credential:
          $ref: '#/components/schemas/CredentialDependency'
        function:
          $ref: '#/components/schemas/FunctionDependency'
        metastore_name:
          type: string
          description: >-
            Metastore context for the dependency. Global UC server responses
            currently populate this with the metastore UUID string so clients
            can disambiguate dependencies with identical names across
            metastores.
        secret:
          $ref: '#/components/schemas/SecretDependency'
          description: A dependency on a Unity Catalog secret.
        table:
          $ref: '#/components/schemas/TableDependency'
        volume:
          $ref: '#/components/schemas/VolumeDependency'
          description: A dependency on a Unity Catalog volume.
      description: >-
        A dependency of a SQL object. One of the following fields must be
        defined: **table**,
            **function**, **connection**, **credential**, **volume**, or **secret**.
    DependencyList:
      type: object
      properties:
        dependencies:
          type: array
          items:
            $ref: '#/components/schemas/Dependency'
          description: Array of dependencies.
      description: A list of dependencies.
    DisableResponse:
      type: object
      properties: {}
    DrReplicationInfo:
      type: object
      properties:
        last_failover_time_ms:
          type: integer
          description: >-
            Wall-clock epoch milliseconds when this catalog was last promoted to
            primary via failover or failback. Set by DR Manager. Used by
            Predictive Optimization to suppress operations until sufficient
            workload history accumulates.
        replicated_entities:
          type: string
          description: >-
            See
            https://docs.google.com/document/d/1X0A_3hMhzuS2V1E3zB0x5wxPsFx70bVYK5rHep2AjW8.
        status:
          $ref: '#/components/schemas/DrReplicationStatus'
      description: Metadata related to Disaster Recovery.
    EffectivePermissionsList:
      type: object
      properties:
        next_page_token:
          type: string
        privilege_assignments:
          type: array
          items:
            $ref: '#/components/schemas/EffectivePrivilegeAssignment'
          description: >-
            The privileges conveyed to each principal (either directly or via
            inheritance)
    EffectivePredictiveOptimizationFlag:
      type: object
      properties:
        value:
          $ref: '#/components/schemas/EnablePredictiveOptimization'
        inherited_from_name:
          type: string
          description: >-
            The name of the object from which the flag was inherited. If there
            was no inheritance, this field is left blank.
        inherited_from_type:
          $ref: >-
            #/components/schemas/EffectivePredictiveOptimizationFlagInheritedFromType
          description: >-
            The type of the object from which the flag was inherited. If there
            was no inheritance, this field is left blank.
      required:
        - value
    EffectivePrivilege:
      type: object
      properties:
        inherited_from_name:
          type: string
        inherited_from_type:
          $ref: '#/components/schemas/SecurableType'
          description: >-
            The type of the object that conveys this privilege via inheritance.
            This field is omitted when privilege is not inherited (it's assigned
            to the securable itself).
        privilege:
          $ref: '#/components/schemas/Privilege'
          description: The privilege assigned to the principal.
    EffectivePrivilegeAssignment:
      type: object
      properties:
        principal:
          type: string
        privileges:
          type: array
          items:
            $ref: '#/components/schemas/EffectivePrivilege'
          description: >-
            The privileges conveyed to the principal (either directly or via
            inheritance).
    EnableResponse:
      type: object
      properties: {}
    EncryptionDetails:
      type: object
      properties:
        sse_encryption_details:
          $ref: '#/components/schemas/SseEncryptionDetails'
          description: >-
            Server-Side Encryption properties for clients communicating with AWS
            s3.
      description: Encryption options that apply to clients connecting to cloud storage.
    EncryptionSettings:
      type: object
      properties:
        azure_encryption_settings:
          $ref: '#/components/schemas/AzureEncryptionSettings'
          description: optional Azure settings - only required if an Azure CMK is used.
        azure_key_vault_key_id:
          type: string
          description: the AKV URL in Azure, null otherwise.
        customer_managed_key_id:
          type: string
          description: the CMK uuid in AWS and GCP, null otherwise.
      description: >-
        Encryption Settings are used to carry metadata for securable encryption
        at rest. Currently used
            for catalogs, we can use the information supplied here to interact with a CMK.
    EntityTagAssignment:
      type: object
      properties:
        entity_name:
          type: string
          description: The fully qualified name of the entity to which the tag is assigned
        tag_key:
          type: string
          description: The key of the tag
        entity_type:
          type: string
          description: The type of the entity to which the tag is assigned.
        inherited:
          type: boolean
          description: Boolean which indicates whether this tag is inherited.
        source_type:
          $ref: '#/components/schemas/TagAssignmentSourceType'
          description: >-
            The source type of the tag assignment, e.g., user-assigned or
            system-assigned
        tag_value:
          type: string
          description: The value of the tag
        update_time:
          type: string
          format: date-time
          description: The timestamp when the tag assignment was last updated
        updated_by:
          type: string
          description: The user or principal who updated the tag assignment
      required:
        - entity_name
        - tag_key
        - entity_type
      description: Represents a tag assignment to an entity
    EnvironmentSettings:
      type: object
      properties:
        environment_version:
          type: string
        java_dependencies:
          type: array
          items:
            type: string
    ExternalLineageExternalMetadata:
      type: object
      properties:
        name:
          type: string
    ExternalLineageExternalMetadataInfo:
      type: object
      properties:
        entity_type:
          type: string
          description: Type of entity represented by the external metadata object.
        event_time:
          type: string
          description: Timestamp of the lineage event.
        name:
          type: string
          description: Name of the external metadata object.
        system_type:
          $ref: '#/components/schemas/SystemType'
          description: Type of external system.
      description: Represents the external metadata object in the lineage event.
    ExternalLineageFileInfo:
      type: object
      properties:
        event_time:
          type: string
          description: Timestamp of the lineage event.
        path:
          type: string
          description: URL of the path.
        securable_name:
          type: string
          description: The full name of the securable on the path.
        securable_type:
          type: string
          description: The securable type of the securable on the path.
        storage_location:
          type: string
          description: The storage location associated with securable on the path.
      description: Represents the path information in the lineage event.
    ExternalLineageInfo:
      type: object
      properties:
        external_lineage_info:
          $ref: '#/components/schemas/ExternalLineageRelationshipInfo'
          description: >-
            Information about the edge metadata of the external lineage
            relationship.
        external_metadata_info:
          $ref: '#/components/schemas/ExternalLineageExternalMetadataInfo'
          description: >-
            Information about external metadata involved in the lineage
            relationship.
        file_info:
          $ref: '#/components/schemas/ExternalLineageFileInfo'
          description: Information about the file involved in the lineage relationship.
        model_info:
          $ref: '#/components/schemas/ExternalLineageModelVersionInfo'
          description: >-
            Information about the model version involved in the lineage
            relationship.
        table_info:
          $ref: '#/components/schemas/ExternalLineageTableInfo'
          description: Information about the table involved in the lineage relationship.
        volume_info:
          $ref: '#/components/schemas/ExternalLineageVolumeInfo'
          description: Information about the volume involved in the lineage relationship.
      description: Lineage response containing lineage information of a data asset.
    ExternalLineageModelVersion:
      type: object
      properties:
        name:
          type: string
        version:
          type: string
    ExternalLineageModelVersionInfo:
      type: object
      properties:
        event_time:
          type: string
          description: Timestamp of the lineage event.
        model_name:
          type: string
          description: Name of the model.
        version:
          type: integer
          description: Version number of the model.
      description: Represents the model version information in the lineage event.
    ExternalLineageObject:
      type: object
      properties:
        external_metadata:
          $ref: '#/components/schemas/ExternalLineageExternalMetadata'
        model_version:
          $ref: '#/components/schemas/ExternalLineageModelVersion'
        path:
          $ref: '#/components/schemas/ExternalLineagePath'
        table:
          $ref: '#/components/schemas/ExternalLineageTable'
        volume:
          $ref: '#/components/schemas/ExternalLineageVolume'
    ExternalLineagePath:
      type: object
      properties:
        url:
          type: string
    ExternalLineageRelationship:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/ExternalLineageObject'
        target:
          $ref: '#/components/schemas/ExternalLineageObject'
          description: Target object of the external lineage relationship.
        columns:
          type: array
          items:
            $ref: '#/components/schemas/ColumnRelationship'
          description: List of column relationships between source and target objects.
        id:
          type: string
          description: Unique identifier of the external lineage relationship.
        properties:
          type: object
          description: >-
            Key-value properties associated with the external lineage
            relationship.
      required:
        - source
        - target
    ExternalLineageRelationshipInfo:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/ExternalLineageObject'
        target:
          $ref: '#/components/schemas/ExternalLineageObject'
          description: Target object of the external lineage relationship.
        columns:
          type: array
          items:
            $ref: '#/components/schemas/ColumnRelationship'
          description: List of column relationships between source and target objects.
        id:
          type: string
          description: Unique identifier of the external lineage relationship.
        properties:
          type: object
          description: >-
            Key-value properties associated with the external lineage
            relationship.
      required:
        - source
        - target
    ExternalLineageTable:
      type: object
      properties:
        name:
          type: string
    ExternalLineageTableInfo:
      type: object
      properties:
        catalog_name:
          type: string
          description: Name of Catalog.
        event_time:
          type: string
          description: Timestamp of the lineage event.
        name:
          type: string
          description: Name of Table.
        schema_name:
          type: string
          description: Name of Schema.
      description: Represents the table information in the lineage event.
    ExternalLineageVolume:
      type: object
      properties:
        name:
          type: string
    ExternalLineageVolumeInfo:
      type: object
      properties:
        catalog_name:
          type: string
        event_time:
          type: string
          format: date-time
          description: Timestamp of the lineage event.
        name:
          type: string
          description: Name of the volume.
        schema_name:
          type: string
          description: Name of the schema.
    ExternalLocationInfo:
      type: object
      properties:
        browse_only:
          type: boolean
        comment:
          type: string
          description: User-provided free-form text description.
        created_at:
          type: integer
          description: >-
            Time at which this external location was created, in epoch
            milliseconds.
        created_by:
          type: string
          description: Username of external location creator.
        credential_id:
          type: string
          description: Unique ID of the location's storage credential.
        credential_name:
          type: string
          description: Name of the storage credential used with this location.
        effective_enable_file_events:
          type: boolean
          description: >-
            The effective value of ``enable_file_events`` after applying
            server-side defaults.
        effective_file_event_queue:
          $ref: '#/components/schemas/FileEventQueue'
          description: >-
            The effective file event queue configuration after applying
            server-side defaults. Always populated when a queue is provisioned,
            regardless of whether the user explicitly set
            ``enable_file_events``. Use this field instead of
            ``file_event_queue`` for reading the actual queue state.
        enable_file_events:
          type: boolean
          description: >-
            Whether to enable file events on this external location. Default to
            ``true``. Set to ``false`` to disable file events. The actual
            applied value may differ due to server-side defaults; check
            ``effective_enable_file_events`` for the effective state.
        encryption_details:
          $ref: '#/components/schemas/EncryptionDetails'
        fallback:
          type: boolean
          description: >-
            Indicates whether fallback mode is enabled for this external
            location. When fallback mode is enabled, the access to the location
            falls back to cluster credentials if UC credentials are not
            sufficient.
        file_event_queue:
          $ref: '#/components/schemas/FileEventQueue'
          description: >-
            File event queue settings. If ``enable_file_events`` is not
            ``false``, must be defined and have exactly one of the documented
            properties.
        isolation_mode:
          $ref: '#/components/schemas/IsolationMode'
        metastore_id:
          type: string
          description: Unique identifier of metastore hosting the external location.
        name:
          type: string
          description: Name of the external location.
        owner:
          type: string
          description: The owner of the external location.
        read_only:
          type: boolean
          description: Indicates whether the external location is read-only.
        updated_at:
          type: integer
          description: >-
            Time at which external location this was last modified, in epoch
            milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified the external location.
        url:
          type: string
          description: Path URL of the external location.
    ExternalMetadata:
      type: object
      properties:
        name:
          type: string
        system_type:
          $ref: '#/components/schemas/SystemType'
          description: Type of external system.
        entity_type:
          type: string
          description: Type of entity within the external system.
        columns:
          type: array
          items:
            type: string
          description: List of columns associated with the external metadata object.
        create_time:
          type: string
          description: Time at which this external metadata object was created.
        created_by:
          type: string
          description: Username of external metadata object creator.
        description:
          type: string
          description: User-provided free-form text description.
        id:
          type: string
          description: Unique identifier of the external metadata object.
        metastore_id:
          type: string
          description: Unique identifier of parent metastore.
        owner:
          type: string
          description: Owner of the external metadata object.
        properties:
          type: object
          description: >-
            A map of key-value properties attached to the external metadata
            object.
        update_time:
          type: string
          description: Time at which this external metadata object was last modified.
        updated_by:
          type: string
          description: Username of user who last modified external metadata object.
        url:
          type: string
          description: URL associated with the external metadata object.
      required:
        - name
        - system_type
        - entity_type
    FailedStatus:
      type: object
      properties:
        last_processed_commit_version:
          type: integer
          description: >-
            The last source table Delta version that was synced to the online
            table. Note that this Delta version may only be partially synced to
            the online table. Only populated if the table is still online and
            available for serving.
        timestamp:
          type: string
          description: >-
            The timestamp of the last time any data was synchronized from the
            source table to the online table. Only populated if the table is
            still online and available for serving.
      description: >-
        Detailed status of an online table. Shown if the online table is in the
        OFFLINE_FAILED or the
            ONLINE_PIPELINE_FAILED state.
    FileEventQueue:
      type: object
      properties:
        managed_aqs:
          $ref: '#/components/schemas/AzureQueueStorage'
        managed_onelake:
          $ref: '#/components/schemas/OneLakeEventQueue'
        managed_pubsub:
          $ref: '#/components/schemas/GcpPubsub'
        managed_sqs:
          $ref: '#/components/schemas/AwsSqsQueue'
        provided_aqs:
          $ref: '#/components/schemas/AzureQueueStorage'
        provided_onelake:
          $ref: '#/components/schemas/OneLakeEventQueue'
        provided_pubsub:
          $ref: '#/components/schemas/GcpPubsub'
        provided_sqs:
          $ref: '#/components/schemas/AwsSqsQueue'
    ForeignKeyConstraint:
      type: object
      properties:
        name:
          type: string
        child_columns:
          type: array
          items:
            type: string
          description: Column names for this constraint.
        parent_table:
          type: string
          description: The full name of the parent constraint.
        parent_columns:
          type: array
          items:
            type: string
          description: Column names for this constraint.
        rely:
          type: boolean
          description: True if the constraint is RELY, false or unset if NORELY.
      required:
        - name
        - child_columns
        - parent_table
        - parent_columns
    FunctionArgExpression:
      type: object
      properties:
        tag_introspection:
          $ref: '#/components/schemas/TagIntrospectionExpression'
          description: An expression that introspects tags at query time.
      description: >-
        An expression that is evaluated at query time against per-request
        context. New variants (e.g.,
            identity attributes) are added as additional oneof cases.
    FunctionArgument:
      type: object
      properties:
        alias:
          type: string
        constant:
          type: string
          description: A constant literal.
        function_arg_expression:
          $ref: '#/components/schemas/FunctionArgExpression'
          description: >-
            An expression evaluated at query time. Wraps per-request expression
            variants (e.g., tag introspection) so new variants can be added
            without extending the FunctionArgument oneof.
    FunctionDependency:
      type: object
      properties:
        function_full_name:
          type: string
          description: >-
            Full name of the dependent function, in the form of
            **catalog_name**.**schema_name**.**function_name**.
      required:
        - function_full_name
      description: A function that is dependent on a SQL object.
    FunctionInfo:
      type: object
      properties:
        browse_only:
          type: boolean
        catalog_name:
          type: string
          description: Name of parent Catalog.
        comment:
          type: string
          description: User-provided free-form text description.
        created_at:
          type: integer
          description: Time at which this function was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of function creator.
        data_type:
          $ref: '#/components/schemas/ColumnTypeName'
          description: Scalar function return data type.
        external_language:
          type: string
          description: External function language.
        external_name:
          type: string
          description: External function name.
        full_data_type:
          type: string
          description: Pretty printed function data type.
        full_name:
          type: string
          description: >-
            Full name of Function, in form of
            **catalog_name**.**schema_name**.**function_name**
        function_id:
          type: string
          description: Id of Function, relative to parent schema.
        input_params:
          $ref: '#/components/schemas/FunctionParameterInfos'
          description: Function input parameters.
        is_deterministic:
          type: boolean
          description: Whether the function is deterministic.
        is_null_call:
          type: boolean
          description: Function null call.
        metastore_id:
          type: string
          description: Unique identifier of parent metastore.
        name:
          type: string
          description: Name of function, relative to parent schema.
        owner:
          type: string
          description: Username of current owner of the function.
        parameter_style:
          $ref: '#/components/schemas/FunctionInfoParameterStyle'
          description: Function parameter style. **S** is the value for SQL.
        properties:
          type: string
          description: JSON-serialized key-value pair map, encoded (escaped) as a string.
        return_params:
          $ref: '#/components/schemas/FunctionParameterInfos'
          description: Table function return parameters.
        routine_body:
          $ref: '#/components/schemas/FunctionInfoRoutineBody'
          description: >-
            Function language. When **EXTERNAL** is used, the language of the
            routine function should be specified in the **external_language**
            field, and the **return_params** of the function cannot be used (as
            **TABLE** return type is not supported), and the **sql_data_access**
            field must be **NO_SQL**.
        routine_definition:
          type: string
          description: Function body.
        routine_dependencies:
          $ref: '#/components/schemas/DependencyList'
          description: function dependencies.
        schema_name:
          type: string
          description: Name of parent Schema relative to its parent Catalog.
        security_type:
          $ref: '#/components/schemas/FunctionInfoSecurityType'
          description: Function security type.
        specific_name:
          type: string
          description: Specific name of the function; Reserved for future use.
        sql_data_access:
          $ref: '#/components/schemas/FunctionInfoSqlDataAccess'
          description: Function SQL data access.
        sql_path:
          type: string
          description: >-
            List of schemes whose objects can be referenced without
            qualification.
        updated_at:
          type: integer
          description: >-
            Time at which this function was last modified, in epoch
            milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified the function.
    FunctionParameterInfo:
      type: object
      properties:
        name:
          type: string
        type_text:
          type: string
          description: Full data type spec, SQL/catalogString text.
        type_name:
          $ref: '#/components/schemas/ColumnTypeName'
          description: Name of type (INT, STRUCT, MAP, etc.)
        position:
          type: integer
          description: Ordinal position of column (starting at position 0).
        comment:
          type: string
          description: User-provided free-form text description.
        parameter_default:
          type: string
          description: Default value of the parameter.
        parameter_mode:
          $ref: '#/components/schemas/FunctionParameterMode'
          description: Function parameter mode.
        parameter_type:
          $ref: '#/components/schemas/FunctionParameterType'
          description: Function parameter type.
        type_interval_type:
          type: string
          description: Format of IntervalType.
        type_json:
          type: string
          description: Full data type spec, JSON-serialized.
        type_precision:
          type: integer
          description: Digits of precision; required on Create for DecimalTypes.
        type_scale:
          type: integer
          description: Digits to right of decimal; Required on Create for DecimalTypes.
      required:
        - name
        - type_text
        - type_name
        - position
    FunctionParameterInfos:
      type: object
      properties:
        parameters:
          type: array
          items:
            $ref: '#/components/schemas/FunctionParameterInfo'
    GcpOauthToken:
      type: object
      properties:
        oauth_token:
          type: string
      description: |-
        GCP temporary credentials for API authentication. Read more at
            https://developers.google.com/identity/protocols/oauth2/service-account
    GcpPubsub:
      type: object
      properties:
        managed_resource_id:
          type: string
        subscription_name:
          type: string
          description: >-
            The Pub/Sub subscription name in the format
            projects/{project}/subscriptions/{subscription name}. Only required
            for provided_pubsub.
    GenerateTemporaryPathCredentialResponse:
      type: object
      properties:
        aws_temp_credentials:
          $ref: '#/components/schemas/AwsCredentials'
        azure_aad:
          $ref: '#/components/schemas/AzureActiveDirectoryToken'
        azure_user_delegation_sas:
          $ref: '#/components/schemas/AzureUserDelegationSas'
        expiration_time:
          type: integer
          description: >-
            Server time when the credential will expire, in epoch milliseconds.
            The API client is advised to cache the credential given this
            expiration time.
        gcp_oauth_token:
          $ref: '#/components/schemas/GcpOauthToken'
        r2_temp_credentials:
          $ref: '#/components/schemas/R2Credentials'
        uc_encrypted_token:
          $ref: '#/components/schemas/UcEncryptedToken'
        url:
          type: string
          description: The URL of the storage path accessible by the temporary credential.
    GenerateTemporaryServiceCredentialAzureOptions:
      type: object
      properties:
        resources:
          type: array
          items:
            type: string
          description: >-
            The resources to which the temporary Azure credential should apply.
            These resources are the scopes that are passed to the token provider
            (see
            https://learn.microsoft.com/python/api/azure-core/azure.core.credentials.tokencredential?view=azure-python)
      description: The Azure cloud options to customize the requested temporary credential
    GenerateTemporaryServiceCredentialGcpOptions:
      type: object
      properties:
        scopes:
          type: array
          items:
            type: string
          description: >-
            The scopes to which the temporary GCP credential should apply. These
            resources are the scopes that are passed to the token provider (see
            https://google-auth.readthedocs.io/en/latest/reference/google.auth.html#google.auth.credentials.Credentials)
      description: The GCP cloud options to customize the requested temporary credential
    GenerateTemporaryTableCredentialResponse:
      type: object
      properties:
        aws_temp_credentials:
          $ref: '#/components/schemas/AwsCredentials'
        azure_aad:
          $ref: '#/components/schemas/AzureActiveDirectoryToken'
        azure_user_delegation_sas:
          $ref: '#/components/schemas/AzureUserDelegationSas'
        expiration_time:
          type: integer
          description: >-
            Server time when the credential will expire, in epoch milliseconds.
            The API client is advised to cache the credential given this
            expiration time.
        gcp_oauth_token:
          $ref: '#/components/schemas/GcpOauthToken'
        r2_temp_credentials:
          $ref: '#/components/schemas/R2Credentials'
        uc_encrypted_token:
          $ref: '#/components/schemas/UcEncryptedToken'
        url:
          type: string
          description: The URL of the storage path accessible by the temporary credential.
    GenerateTemporaryVolumeCredentialResponse:
      type: object
      properties:
        aws_temp_credentials:
          $ref: '#/components/schemas/AwsCredentials'
        azure_aad:
          $ref: '#/components/schemas/AzureActiveDirectoryToken'
        azure_user_delegation_sas:
          $ref: '#/components/schemas/AzureUserDelegationSas'
        expiration_time:
          type: integer
          description: >-
            Server time when the credential will expire, in epoch milliseconds.
            The API client is advised to cache the credential given this
            expiration time.
        gcp_oauth_token:
          $ref: '#/components/schemas/GcpOauthToken'
        r2_temp_credentials:
          $ref: '#/components/schemas/R2Credentials'
        uc_encrypted_token:
          $ref: '#/components/schemas/UcEncryptedToken'
        url:
          type: string
          description: The URL of the storage path accessible by the temporary credential.
    GetCatalogWorkspaceBindingsResponse:
      type: object
      properties:
        workspaces:
          type: array
          items:
            type: integer
    GetMetastoreSummaryResponse:
      type: object
      properties:
        cloud:
          type: string
        created_at:
          type: integer
          description: Time at which this metastore was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of metastore creator.
        default_data_access_config_id:
          type: string
          description: >-
            Unique identifier of the metastore's (Default) Data Access
            Configuration.
        delta_sharing_organization_name:
          type: string
          description: >-
            The organization name of a Delta Sharing entity, to be used in
            Databricks-to-Databricks Delta Sharing as the official name.
        delta_sharing_recipient_token_lifetime_in_seconds:
          type: integer
          description: The lifetime of delta sharing recipient token in seconds.
        delta_sharing_scope:
          $ref: '#/components/schemas/DeltaSharingScopeEnum'
          description: The scope of Delta Sharing enabled for the metastore.
        external_access_enabled:
          type: boolean
          description: >-
            Whether to allow non-DBR clients to directly access entities under
            the metastore.
        global_metastore_id:
          type: string
          description: >-
            Globally unique metastore ID across clouds and regions, of the form
            ``cloud:region:metastore_id``.
        metastore_id:
          type: string
          description: Unique identifier of metastore.
        name:
          type: string
          description: The user-specified name of the metastore.
        owner:
          type: string
          description: The owner of the metastore.
        privilege_model_version:
          type: string
          description: >-
            Privilege model version of the metastore, of the form
            ``major.minor`` (e.g., ``1.0``).
        region:
          type: string
          description: >-
            Cloud region which the metastore serves (e.g., ``us-west-2``,
            ``westus``).
        storage_root:
          type: string
          description: The storage root URL for metastore
        storage_root_credential_id:
          type: string
          description: UUID of storage credential to access the metastore storage_root.
        storage_root_credential_name:
          type: string
          description: Name of the storage credential to access the metastore storage_root.
        updated_at:
          type: integer
          description: >-
            Time at which the metastore was last modified, in epoch
            milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified the metastore.
    GetPermissionsResponse:
      type: object
      properties:
        next_page_token:
          type: string
        privilege_assignments:
          type: array
          items:
            $ref: '#/components/schemas/PrivilegeAssignment'
          description: The privileges assigned to each principal
    GetQuotaResponse:
      type: object
      properties:
        quota_info:
          $ref: '#/components/schemas/QuotaInfo'
    GetWorkspaceBindingsResponse:
      type: object
      properties:
        bindings:
          type: array
          items:
            $ref: '#/components/schemas/WorkspaceBinding'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    GrantOptions:
      type: object
      properties:
        privileges:
          type: array
          items:
            type: string
      required:
        - privileges
    InferenceTableConfig:
      type: object
      properties:
        parent:
          type: string
          description: >-
            Parent UC schema where the inference table is created. Format:
            ``schemas/{catalog}.{schema}``. Set at create time and immutable
            thereafter; changing it on an existing service is rejected.
        disabled:
          type: boolean
          description: >-
            Indicates whether payload logging is disabled (opt-out). Unset means
            that payload logging is active (the on-by-default state coincides
            with the proto zero-value, so the server never fills this field for
            a client that leaves it unset). Set ``disabled = true`` to pause
            runtime logging while keeping the sub-message attached (preserving
            ``parent`` and ``table_name_prefix`` for a later flip back to
            active). ``parent`` remains required either way.
        is_deleted:
          type: boolean
          description: >-
            True when the bound inference TABLE has been deleted but the parent
            service still references it. The dangling reference is surfaced (not
            silently dropped) so callers can see the broken dependency. AI
            Gateway payload logging fails closed in this state.
        table:
          type: string
          description: >-
            Resolved UC table for payload logs. Format:
            ``tables/{catalog}.{schema}.{table}``.
        table_name_prefix:
          type: string
          description: >-
            Prefix for the inference-table's UC-registered name. The actual leaf
            name UC stores is ``<table_name_prefix>_payload``; the ``_payload``
            suffix is appended automatically. To find the actual UC table after
            Create, read the ``table`` field on the response. Defaults to
            ``<model_service_name>_payload`` when unset. Set at create time and
            immutable thereafter; changing it on an existing service is
            rejected.
      required:
        - parent
      description: |-
        Inference table configuration for payload logging on a model service.

            ``parent`` is always REQUIRED when the sub-message is set; the destination UC schema is needed
            to construct or rebind the payload TABLE regardless of whether payload logging is currently
            active. Payload logging is active by default; set ``disabled = true`` to pause runtime logging
            without dropping the table or the binding.
    ListAccountMetastoreAssignmentsResponse:
      type: object
      properties:
        workspace_ids:
          type: array
          items:
            type: integer
      description: The metastore assignments were successfully returned.
    ListAccountStorageCredentialsResponse:
      type: object
      properties:
        storage_credentials:
          type: array
          items:
            $ref: '#/components/schemas/StorageCredentialInfo'
          description: An array of metastore storage credentials.
      description: The metastore storage credentials were successfully returned.
    ListAgentServicesResponse:
      type: object
      properties:
        agent_services:
          type: array
          items:
            $ref: '#/components/schemas/AgentService'
          description: The list of agent services.
        next_page_token:
          type: string
          description: Pagination token for retrieving the next page of results.
      description: Response for listing agent services.
    ListCatalogsResponse:
      type: object
      properties:
        catalogs:
          type: array
          items:
            $ref: '#/components/schemas/CatalogInfo'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListConnectionsResponse:
      type: object
      properties:
        connections:
          type: array
          items:
            $ref: '#/components/schemas/ConnectionInfo'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListCredentialsResponse:
      type: object
      properties:
        credentials:
          type: array
          items:
            $ref: '#/components/schemas/CredentialInfo'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListEffectivePrivilegeAssignmentsResponse:
      type: object
      properties:
        effective_privilege_assignments:
          type: array
          items:
            $ref: '#/components/schemas/EffectivePrivilegeAssignment'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListEntityTagAssignmentsResponse:
      type: object
      properties:
        next_page_token:
          type: string
        tag_assignments:
          type: array
          items:
            $ref: '#/components/schemas/EntityTagAssignment'
          description: The list of tag assignments
    ListExternalLineageRelationshipsResponse:
      type: object
      properties:
        external_lineage_relationships:
          type: array
          items:
            $ref: '#/components/schemas/ExternalLineageInfo'
        next_page_token:
          type: string
    ListExternalLocationsResponse:
      type: object
      properties:
        external_locations:
          type: array
          items:
            $ref: '#/components/schemas/ExternalLocationInfo'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListExternalMetadataResponse:
      type: object
      properties:
        external_metadata:
          type: array
          items:
            $ref: '#/components/schemas/ExternalMetadata'
        next_page_token:
          type: string
    ListFunctionsResponse:
      type: object
      properties:
        functions:
          type: array
          items:
            $ref: '#/components/schemas/FunctionInfo'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListMcpServicesResponse:
      type: object
      properties:
        mcp_services:
          type: array
          items:
            $ref: '#/components/schemas/McpService'
          description: The list of MCP services.
        next_page_token:
          type: string
          description: Pagination token for retrieving the next page of results.
      description: Response for listing MCP services.
    ListMetastoresResponse:
      type: object
      properties:
        metastores:
          type: array
          items:
            $ref: '#/components/schemas/MetastoreInfo'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListModelProviderServicesResponse:
      type: object
      properties:
        model_provider_services:
          type: array
          items:
            $ref: '#/components/schemas/ModelProviderService'
          description: The list of model provider services.
        next_page_token:
          type: string
          description: Pagination token for retrieving the next page of results.
      description: Response for listing model provider services.
    ListModelServicesResponse:
      type: object
      properties:
        model_services:
          type: array
          items:
            $ref: '#/components/schemas/ModelService'
          description: The list of model services.
        next_page_token:
          type: string
          description: Pagination token for retrieving the next page of results.
      description: Response for listing model services.
    ListModelVersionsResponse:
      type: object
      properties:
        model_versions:
          type: array
          items:
            $ref: '#/components/schemas/ModelVersionInfo'
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request (for the next page of results).
    ListPoliciesResponse:
      type: object
      properties:
        next_page_token:
          type: string
        policies:
          type: array
          items:
            $ref: '#/components/schemas/PolicyInfo'
          description: The list of retrieved policies.
    ListPrivilegeAssignmentsResponse:
      type: object
      properties:
        next_page_token:
          type: string
        privilege_assignments:
          type: array
          items:
            $ref: '#/components/schemas/PrivilegeAssignment'
    ListQuotasResponse:
      type: object
      properties:
        next_page_token:
          type: string
        quotas:
          type: array
          items:
            $ref: '#/components/schemas/QuotaInfo'
          description: An array of returned QuotaInfos.
    ListRegisteredModelsResponse:
      type: object
      properties:
        next_page_token:
          type: string
        registered_models:
          type: array
          items:
            $ref: '#/components/schemas/RegisteredModelInfo'
    ListSchemasResponse:
      type: object
      properties:
        next_page_token:
          type: string
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/SchemaInfo'
          description: An array of schema information objects.
    ListSecretsResponse:
      type: object
      properties:
        next_page_token:
          type: string
          description: >-
            Opaque token to retrieve the next page of results. Absent if there
            are no more pages. **page_token** should be set to this value for
            the next request.
        secrets:
          type: array
          items:
            $ref: '#/components/schemas/Secret'
          description: An array of secret objects.
      description: Response message for ListSecrets.
    ListStorageCredentialsResponse:
      type: object
      properties:
        next_page_token:
          type: string
        storage_credentials:
          type: array
          items:
            $ref: '#/components/schemas/StorageCredentialInfo'
    ListSystemSchemasResponse:
      type: object
      properties:
        next_page_token:
          type: string
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/SystemSchemaInfo'
          description: An array of system schema information objects.
    ListTableSummariesResponse:
      type: object
      properties:
        next_page_token:
          type: string
        tables:
          type: array
          items:
            $ref: '#/components/schemas/TableSummary'
          description: List of table summaries.
    ListTablesResponse:
      type: object
      properties:
        next_page_token:
          type: string
        tables:
          type: array
          items:
            $ref: '#/components/schemas/TableInfo'
          description: An array of table information objects.
    ListVolumesResponseContent:
      type: object
      properties:
        next_page_token:
          type: string
        volumes:
          type: array
          items:
            $ref: '#/components/schemas/VolumeInfo'
    MatchColumn:
      type: object
      properties:
        alias:
          type: string
        condition:
          type: string
          description: The condition expression used to match a table column.
    McpService:
      type: object
      properties:
        browse_only:
          type: boolean
          description: >-
            Whether the caller sees only metadata available through the BROWSE
            privilege.
        comment:
          type: string
          description: User-provided description.
        config:
          $ref: '#/components/schemas/McpServiceConfig'
          description: >-
            Operational configuration: connection, tool selectors, rate limit.
            Required on CreateMcpService; on UpdateMcpService it is required
            only when ``config`` (or a ``config.*`` subpath) appears in
            ``update_mask``.
        create_time:
          type: string
          format: date-time
          description: When the MCP service was created.
        created_by:
          type: string
          description: Creator identity.
        effective_owner:
          type: string
          description: >-
            The resolved owner of the MCP service. Falls back to the caller's
            identity when ``owner`` is not explicitly set on creation.
        etag:
          type: string
          description: >-
            Optimistic concurrency control token. Server-generated from the
            entity's state and returned on every read. To use it as an if-match
            precondition on a mutation, echo the last-read value back via the
            dedicated ``etag`` field on the Update / Delete request; the server
            rejects the mutation if the stored etag differs.
        metastore_id:
          type: string
          description: Metastore hosting the MCP service.
        name:
          type: string
          description: >-
            Resource name of the MCP service. Format:
            ``mcp-services/{catalog}.{schema}.{mcp_service}``. Each ``{...}``
            component is capped at 255 characters individually. Server-derived
            on Create from ``parent`` + ``mcp_service_id``; required and
            immutable on Update/Get/Delete.
        owner:
          type: string
          description: >-
            The owner of the MCP service. Write-only; read owner via
            effective_owner.
        update_time:
          type: string
          format: date-time
          description: When the MCP service was last modified.
        updated_by:
          type: string
          description: Identity of the last updater.
      description: >-
        A governed MCP server registration in Unity Catalog. Acts as a container
        securable that
            references an MCP server -- customer-external via a UC Connection, or Databricks-hosted via an
            internal server -- and exposes its tools for discovery, authorization, and invocation.
    McpServiceConfig:
      type: object
      properties:
        include_tool_selectors:
          type: array
          items:
            type: string
          description: >-
            Glob or exact-match patterns selecting which tools from the MCP
            server to expose. Prefix match for patterns with ``*``, exact match
            otherwise. An empty list means all tools are included. Per-element
            max 256 chars.
        rate_limits:
          type: array
          items:
            $ref: '#/components/schemas/RateLimit'
          description: >-
            Per-principal rate limits applied to tool invocations routed through
            this MCP service. Repeated to support per-USER / USER_GROUP /
            SERVICE_PRINCIPAL / SERVICE / USER_DEFAULT scopes simultaneously,
            mirroring the ``ModelServiceConfig.rate_limits`` shape. Empty when
            no rate limit is configured.
        source_connection:
          $ref: '#/components/schemas/McpServiceConfigSourceConnection'
          description: UC Connection referencing the MCP server.
      description: >-
        Operational configuration for an MCP service. Groups the source
        reference, tool selectors, and
            rate limit -- the fields that configure how the MCP service behaves at invocation time.
    McpServiceConfigSourceConnection:
      type: object
      properties:
        name:
          type: string
        is_deleted:
          type: boolean
      required:
        - name
      description: >-
        UC Connection that hosts the MCP server. On create, provide ``name`` in
        the schema-scoped form
            ``connections/{catalog}.{schema}.{connection}``. On read, the service populates the resolved
            connection metadata and preserves a dangling source so callers can diagnose a deleted backing
            connection.
    MetastoreAssignment:
      type: object
      properties:
        workspace_id:
          type: integer
        metastore_id:
          type: string
          description: The unique ID of the metastore.
        default_catalog_name:
          type: string
          description: >-
            The name of the default catalog in the metastore. This field is
            deprecated. Please use "Default Namespace API" to configure the
            default catalog for a Databricks workspace.
      required:
        - workspace_id
        - metastore_id
    MetastoreInfo:
      type: object
      properties:
        cloud:
          type: string
        created_at:
          type: integer
          description: Time at which this metastore was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of metastore creator.
        default_data_access_config_id:
          type: string
          description: >-
            Unique identifier of the metastore's (Default) Data Access
            Configuration.
        delta_sharing_organization_name:
          type: string
          description: >-
            The organization name of a Delta Sharing entity, to be used in
            Databricks-to-Databricks Delta Sharing as the official name.
        delta_sharing_recipient_token_lifetime_in_seconds:
          type: integer
          description: The lifetime of delta sharing recipient token in seconds.
        delta_sharing_scope:
          $ref: '#/components/schemas/DeltaSharingScopeEnum'
          description: The scope of Delta Sharing enabled for the metastore.
        external_access_enabled:
          type: boolean
          description: >-
            Whether to allow non-DBR clients to directly access entities under
            the metastore.
        global_metastore_id:
          type: string
          description: >-
            Globally unique metastore ID across clouds and regions, of the form
            ``cloud:region:metastore_id``.
        metastore_id:
          type: string
          description: Unique identifier of metastore.
        name:
          type: string
          description: The user-specified name of the metastore.
        owner:
          type: string
          description: The owner of the metastore.
        privilege_model_version:
          type: string
          description: >-
            Privilege model version of the metastore, of the form
            ``major.minor`` (e.g., ``1.0``).
        region:
          type: string
          description: >-
            Cloud region which the metastore serves (e.g., ``us-west-2``,
            ``westus``).
        storage_root:
          type: string
          description: The storage root URL for metastore
        storage_root_credential_id:
          type: string
          description: UUID of storage credential to access the metastore storage_root.
        storage_root_credential_name:
          type: string
          description: Name of the storage credential to access the metastore storage_root.
        updated_at:
          type: integer
          description: >-
            Time at which the metastore was last modified, in epoch
            milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified the metastore.
    ModelProviderService:
      type: object
      properties:
        browse_only:
          type: boolean
          description: >-
            Whether the caller sees only metadata available through the BROWSE
            privilege.
        comment:
          type: string
          description: User-provided description.
        config:
          $ref: '#/components/schemas/ModelProviderServiceConfig'
          description: >-
            Behavioral configuration: provider connection, model catalog, and
            passthrough policy. See ``ModelProviderServiceConfig`` for the
            per-field contract. Required on CreateModelProviderService; on
            Update it is required only when ``config`` (or a ``config.*``
            subpath) appears in ``update_mask``.
        create_time:
          type: string
          format: date-time
          description: When the provider service was created.
        created_by:
          type: string
          description: Creator identity.
        effective_owner:
          type: string
          description: >-
            The resolved owner of the model provider service. Falls back to the
            caller's identity when ``owner`` is not explicitly set on creation.
        etag:
          type: string
          description: >-
            Optimistic concurrency control token. Server-generated from the
            entity's state and returned on every read. To use it as an if-match
            precondition on a mutation, echo the last-read value back via the
            dedicated ``etag`` field on the Update / Delete request; the server
            rejects the mutation if the stored etag differs.
        metastore_id:
          type: string
          description: Metastore hosting the provider service.
        name:
          type: string
          description: >-
            Resource name of the provider service. Format:
            ``model-provider-services/{catalog}.{schema}.{model_provider_service}``.
            Each ``{...}`` component is capped at 255 characters individually.
            Server-derived on Create from ``parent`` +
            ``model_provider_service_id``; required and immutable on
            Update/Get/Delete.
        owner:
          type: string
          description: >-
            The owner of the model provider service. Write-only; read owner via
            effective_owner.
        update_time:
          type: string
          format: date-time
          description: When the provider service was last modified.
        updated_by:
          type: string
          description: Identity of the last updater.
      description: >-
        A governed external model-provider connection stored in Unity Catalog
        (e.g. an OpenAI API
            account, an Azure OpenAI deployment, an Amazon Bedrock account). Owns the provider type and the
            auth/configuration the platform needs to invoke that provider, and is referenced from
            ``ExternalModelConfig.model_provider_service`` on a ModelService.

            One ModelProviderService can back many ModelServices (e.g. an ``openai_prod`` provider serving
            multiple models); a single ModelService can fan out across multiple ModelProviderServices for
            traffic split or failover.
    ModelProviderServiceConfig:
      type: object
      properties:
        allow_all_targets:
          type: boolean
          description: >-
            When true, accepts any model exposed by the upstream provider;
            ``targets`` is not required and does not restrict routability. When
            false, only models listed in ``targets`` are routable.
        amazon_bedrock:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAmazonBedrockProviderConfig
        anthropic:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAnthropicProviderConfig
        azure_openai:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAzureOpenAiProviderConfig
        custom:
          $ref: '#/components/schemas/ModelProviderServiceConfigCustomProviderConfig'
        forward_headers:
          type: boolean
          description: >-
            Whether to forward incoming request headers to the upstream
            provider. Applies to managed (multi-model) requests as well as
            passthrough requests served by this provider service.
            Governance-level decision by the provider service owner; not
            selectable per inference call.
        forward_query_parameters:
          type: boolean
          description: >-
            Whether to forward incoming request query parameters to the upstream
            provider. Same trust-boundary semantics as ``forward_headers``.
        forward_unmanaged_paths:
          type: boolean
          description: >-
            Whether to forward request paths that fall outside this service's
            managed API set to the upstream provider as opaque passthrough. When
            true, requests addressed to subpaths not recognized by the managed
            API surface are proxied to the upstream provider over the same
            provider connection. When false, only managed-API paths are served.
            Governance-level decision by the provider service owner; expanding
            this expands the trust boundary that the ModelProviderService
            exposes.
        gemini_enterprise:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigGeminiEnterpriseProviderConfig
        inference_table:
          $ref: '#/components/schemas/InferenceTableConfig'
          description: >-
            Inference table configuration for payload logging when this provider
            service is invoked directly. When it is invoked through a model
            service, the model service's own inference table captures the
            invocation instead. Mirrors ``ModelServiceConfig.inference_table`` /
            ``AgentServiceConfig.inference_table``.
        microsoft_foundry:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigMicrosoftFoundryProviderConfig
        openai:
          $ref: '#/components/schemas/ModelProviderServiceConfigOpenAiProviderConfig'
        provider_type:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigExternalModelProviderType
          description: >-
            Provider type discriminator. Required at create time; immutable
            after. Determines which variant of the ``provider`` oneof must be
            set. May not be changed via Update; attempts to include
            ``config.provider_type`` in
            ``UpdateModelProviderServiceRequest.update_mask`` are rejected.
            Required on CreateModelProviderService and immutable thereafter.
        rate_limits:
          type: array
          items:
            $ref: '#/components/schemas/RateLimit'
          description: >-
            Rate limits applied when this provider service is invoked directly.
            When it is invoked through a model service, the model service's own
            ``rate_limits`` apply instead. Mirrors
            ``ModelServiceConfig.rate_limits`` /
            ``McpServiceConfig.rate_limits``.
        targets:
          type: array
          items:
            $ref: '#/components/schemas/ModelProviderServiceConfigModelTargetConfig'
          description: >-
            Routing targets this provider service exposes (provider-side model
            identifier + unified API types per entry). Required (>=1) when
            ``allow_all_targets = false``; optional and additive when
            ``allow_all_targets = true``. References from
            ``ExternalModelConfig.target`` must match an entry here unless
            ``allow_all_targets = true``.
      description: >-
        Behavioral configuration for a ModelProviderService: provider connection
        (auth +
            provider-specific fields), the catalog of models this provider service can route to, and the
            passthrough policy that governs how request headers, query parameters, and unmanaged subpaths
            cross the trust boundary to the upstream provider.
    ModelProviderServiceConfigAmazonBedrockProviderConfig:
      type: object
      properties:
        direct:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAmazonBedrockProviderDirectConfig
      description: Amazon Bedrock provider configuration.
    ModelProviderServiceConfigAmazonBedrockProviderDirectConfig:
      type: object
      properties:
        aws_access_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigAwsAccessKey'
          description: >-
            AWS access-key-pair auth. Mutually exclusive with
            ``service_credential``. Supersedes the flat ``aws_access_key_id`` /
            ``aws_secret_access_key`` fields.
        aws_access_key_id:
          type: string
          description: >-
            Deprecated flat AWS access key ID. Superseded by
            ``aws_access_key.access_key_id``. Kept for one migration cycle; the
            handler mirrors it to/from ``aws_access_key``. Treated as
            username-equivalent (not a secret value): round-trips on reads and
            is scrubbed from audit logs.
        aws_secret_access_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Deprecated flat AWS secret access key. Superseded by
            ``aws_access_key.secret_access_key``. Kept for one migration cycle;
            the handler mirrors it to/from ``aws_access_key``. Supplied as
            inline plaintext via ``ProviderSecret.plaintext``.
        region:
          type: string
          description: >-
            AWS region where the Bedrock endpoint is hosted (e.g.,
            ``us-east-1``). Required on Create.
        service_credential:
          $ref: '#/components/schemas/ModelProviderServiceConfigServiceCredential'
          description: >-
            Reference to a UC service credential authorizing Bedrock requests.
            On Create the caller supplies ``service_credential.name`` in the
            AIP-122 resource-name form ``credentials/{name}``. Required on
            Create when using UC-service-credential auth; mutually exclusive
            with ``aws_access_key``. The credential is referenced by name; its
            value is not carried here. On read the resolved ``id`` and
            ``is_deleted`` are also populated. Only supported on AWS-hosted
            workspaces; Create requests from other clouds are rejected with
            INVALID_PARAMETER_VALUE.
      description: |-
        Direct form of Amazon Bedrock provider config.

            Authentication is one of two mutually exclusive modes, exactly one of which must be supplied on
            Create:

            - Access keys: set ``aws_access_key``, leave ``service_credential`` unset.
            - UC service credential: set ``service_credential.name`` to the AIP-122 resource-name form
              ``credentials/{name}``, leave ``aws_access_key`` unset. The credential value lives in UC and
              is referenced by name, not held on this message. Setting more than one mode is rejected.
    ModelProviderServiceConfigAnthropicProviderConfig:
      type: object
      properties:
        direct:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAnthropicProviderDirectConfig
          description: >-
            Direct (inline-credentials) form: caller supplies the API key in the
            request body. Required on Create unless ``relayed`` is set.
        relayed:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAnthropicProviderRelayedConfig
          description: >-
            Relayed (credential-less) form: no Anthropic credential is stored.
            Each inference request instead carries the caller's own OAuth token,
            which the platform forwards to Anthropic on outbound requests.
            Mutually exclusive with ``direct``; no ``api_key`` is required or
            persisted.
      description: >-
        Anthropic provider configuration. Exactly one of ``direct`` or
        ``relayed`` must be set on
            Create; the two are mutually exclusive.
    ModelProviderServiceConfigAnthropicProviderDirectConfig:
      type: object
      properties:
        api_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Anthropic API key. Required on Create. Sent as the ``x-api-key``
            header on outbound requests. Supplied as inline plaintext via
            ``ProviderSecret.plaintext``.
      description: Direct form of Anthropic provider config.
    ModelProviderServiceConfigAnthropicProviderRelayedConfig:
      type: object
      properties:
        plan_type:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAnthropicProviderRelayedConfigAnthropicRelayedPlanType
          description: >-
            Which Anthropic subscription tier the relayed token belongs to.
            Optional; when unset the MPS gets the full governance surface (see
            TEAM_ENTERPRISE). Immutable after Create, so the tier cannot be
            flipped in place.
      description: >-
        Relayed form of Anthropic provider config: no credential is stored.
        Authentication is the
            caller's own OAuth token, forwarded to Anthropic on outbound requests, so there is no persisted
            secret. Presence of this variant is the signal that the provider service uses relayed auth;
            ``plan_type`` further distinguishes which Anthropic subscription tier the token belongs to.
    ModelProviderServiceConfigAwsAccessKey:
      type: object
      properties:
        access_key_id:
          type: string
          description: >-
            AWS access key ID. Required on Create when using access-key auth.
            Treated as username-equivalent (not a secret value): round-trips on
            reads and is scrubbed from audit logs.
        secret_access_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            AWS secret access key paired with ``access_key_id``. Required on
            Create when using access-key auth. Supplied as inline plaintext via
            ``ProviderSecret.plaintext``.
      description: 'AWS access-key-pair auth for Amazon Bedrock: a SigV4-signing key pair.'
    ModelProviderServiceConfigAzureOpenAiProviderConfig:
      type: object
      properties:
        direct:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigAzureOpenAiProviderDirectConfig
      description: Azure OpenAI provider configuration.
    ModelProviderServiceConfigAzureOpenAiProviderDirectConfig:
      type: object
      properties:
        api_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Azure OpenAI API key. Mutually exclusive with the Entra and
            service-credential modes. Supplied as inline plaintext via
            ``ProviderSecret.plaintext``.
        base_url:
          type: string
          description: >-
            Full Azure OpenAI endpoint base URL, e.g.
            ``https://myresource.openai.azure.com``. Required on Create.
        client_id:
          type: string
          description: >-
            Deprecated flat Entra client ID. Superseded by
            ``entra_service_principal.client_id``. Kept for one migration cycle;
            the handler mirrors it to/from ``entra_service_principal``.
        client_secret:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Deprecated flat Entra client secret. Superseded by
            ``entra_service_principal.client_secret``. Kept for one migration
            cycle; the handler mirrors it to/from ``entra_service_principal``.
            Supplied as inline plaintext via ``ProviderSecret.plaintext``.
        entra_service_principal:
          $ref: '#/components/schemas/ModelProviderServiceConfigEntraServicePrincipal'
          description: >-
            Entra ID (service principal) auth. Mutually exclusive with
            ``api_key`` and ``service_credential``. Supersedes the flat
            ``tenant_id`` / ``client_id`` / ``client_secret`` fields.
        service_credential:
          $ref: '#/components/schemas/ModelProviderServiceConfigServiceCredential'
          description: >-
            Reference to a UC service credential authorizing Azure OpenAI
            requests. On Create the caller supplies ``service_credential.name``
            in the AIP-122 resource-name form ``credentials/{name}``. Required
            on Create when using UC-service-credential auth; mutually exclusive
            with ``api_key`` and ``entra_service_principal``. The credential is
            referenced by name; its value is not carried here. On read the
            resolved ``id`` and ``is_deleted`` are also populated. Only
            supported on Azure-hosted workspaces; Create requests from other
            clouds are rejected with INVALID_PARAMETER_VALUE.
        tenant_id:
          type: string
          description: >-
            Deprecated flat Entra tenant ID. Superseded by
            ``entra_service_principal.tenant_id``. Kept for one migration cycle;
            the handler mirrors it to/from ``entra_service_principal``.
      description: >-
        Direct form of Azure OpenAI provider config. Exactly one of three
        mutually-exclusive auth modes
            must be supplied on Create:

            - API key: set ``api_key``, leave ``entra_service_principal`` and ``service_credential`` unset.
            - Entra ID (service principal): set ``entra_service_principal``, leave ``api_key`` and
              ``service_credential`` unset.
            - UC service credential: set ``service_credential.name`` to the AIP-122 resource-name form
              ``credentials/{name}``, leave ``api_key`` and ``entra_service_principal`` unset. The
              credential value lives in UC and is referenced by name, not held on this message. Only
              supported on Azure-hosted workspaces. Setting more than one mode is rejected.
    ModelProviderServiceConfigCustomProviderConfig:
      type: object
      properties:
        direct:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigCustomProviderDirectConfig
      description: >-
        Custom provider configuration: arbitrary HTTP endpoint with bearer-token
        auth.
    ModelProviderServiceConfigCustomProviderDirectConfig:
      type: object
      properties:
        api_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Bearer token forwarded as the ``Authorization: Bearer ...`` header
            on outbound requests. Supplied as inline plaintext via
            ``ProviderSecret.plaintext``. Set this for bearer-token auth.
        base_url:
          type: string
          description: >-
            Endpoint URL of the OpenAI-compatible service (e.g.,
            ``https://api.example.com/v1``). Required on Create.
      description: |-
        Direct form of custom provider config.

            Authentication is one of two mutually exclusive modes, exactly one of which must be supplied on
            Create:

            - Bearer: set ``api_key``, leave ``header_auth`` unset. The secret is forwarded as
              ``Authorization: Bearer <secret>``.
            - Header: set ``header_auth``, leave ``api_key`` unset. The secret is forwarded as
              ``<api_key_name>: <api_key_value>``. Setting both modes or neither mode is rejected.
    ModelProviderServiceConfigEntraServicePrincipal:
      type: object
      properties:
        client_id:
          type: string
          description: Entra ID client (application) ID. Required on Create.
        client_secret:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Entra ID client secret. Supplied as inline plaintext via
            ``ProviderSecret.plaintext``.
        tenant_id:
          type: string
          description: Entra ID (Azure AD) tenant ID. Required on Create.
      description: >-
        Entra ID (Azure AD) service-principal auth: AI Gateway exchanges the
        ``tenant_id`` +
            ``client_id`` identify the service principal, and the ``credential`` oneof proves that identity,
            exchanged for an Entra bearer token on outbound requests via the OAuth2 client-credentials
            grant. Shared by the Azure OpenAI and Microsoft Foundry provider configs.
    ModelProviderServiceConfigGeminiEnterpriseProviderConfig:
      type: object
      properties:
        direct:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigGeminiEnterpriseProviderDirectConfig
      description: Gemini Enterprise provider configuration.
    ModelProviderServiceConfigGeminiEnterpriseProviderDirectConfig:
      type: object
      properties:
        api_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Google Gemini Enterprise API key. Required on Create. Supplied as
            inline plaintext via ``ProviderSecret.plaintext``.
        project_id:
          type: string
          description: >-
            GCP project ID hosting the Gemini Enterprise endpoint. Required on
            Create.
        region:
          type: string
          description: >-
            GCP region of the Gemini Enterprise endpoint (e.g.,
            ``us-central1``). Required on Create.
      description: Direct form of Gemini Enterprise provider config.
    ModelProviderServiceConfigMicrosoftFoundryProviderConfig:
      type: object
      properties:
        direct:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigMicrosoftFoundryProviderDirectConfig
      description: Microsoft Foundry provider configuration.
    ModelProviderServiceConfigMicrosoftFoundryProviderDirectConfig:
      type: object
      properties:
        api_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Microsoft AI Foundry API key. Mutually exclusive with the Entra and
            service-credential modes. Supplied as inline plaintext via
            ``ProviderSecret.plaintext``.
        base_url:
          type: string
          description: Microsoft AI Foundry endpoint URL. Required on Create.
        client_id:
          type: string
          description: >-
            Deprecated flat Entra client ID. Superseded by
            ``entra_service_principal.client_id``. Kept for one migration cycle;
            the handler mirrors it to/from ``entra_service_principal``.
        client_secret:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            Deprecated flat Entra client secret. Superseded by
            ``entra_service_principal.client_secret``. Kept for one migration
            cycle; the handler mirrors it to/from ``entra_service_principal``.
            Supplied as inline plaintext via ``ProviderSecret.plaintext``.
        entra_service_principal:
          $ref: '#/components/schemas/ModelProviderServiceConfigEntraServicePrincipal'
          description: >-
            Entra ID (service principal) auth. Mutually exclusive with
            ``api_key`` and ``service_credential``. Supersedes the flat
            ``tenant_id`` / ``client_id`` / ``client_secret`` fields.
        service_credential:
          $ref: '#/components/schemas/ModelProviderServiceConfigServiceCredential'
          description: >-
            Reference to a UC service credential authorizing Microsoft Foundry
            requests. On Create the caller supplies ``service_credential.name``
            in the AIP-122 resource-name form ``credentials/{name}``. Required
            on Create when using UC-service-credential auth; mutually exclusive
            with ``api_key`` and ``entra_service_principal``. The credential is
            referenced by name; its value is not carried here. On read the
            resolved ``id`` and ``is_deleted`` are also populated. Only
            supported on Azure-hosted workspaces; Create requests from other
            clouds are rejected with INVALID_PARAMETER_VALUE.
        tenant_id:
          type: string
          description: >-
            Deprecated flat Entra tenant ID. Superseded by
            ``entra_service_principal.tenant_id``. Kept for one migration cycle;
            the handler mirrors it to/from ``entra_service_principal``.
      description: |-
        Direct form of Microsoft Foundry provider config.

            Authentication is one of three mutually exclusive modes, exactly one of which must be supplied
            on Create:

            - API key: set ``api_key``, leave ``entra_service_principal`` and ``service_credential`` unset.
            - Entra ID (service principal): set ``entra_service_principal``, leave ``api_key`` and
              ``service_credential`` unset. AI Gateway exchanges these for an Entra bearer token on outbound
              requests via the OAuth2 client-credentials grant.
            - UC service credential: set ``service_credential.name`` to the AIP-122 resource-name form
              ``credentials/{name}``, leave ``api_key`` and ``entra_service_principal`` unset. The
              credential value lives in UC and is referenced by name, not held on this message. Only
              supported on Azure-hosted workspaces. Setting more than one mode is rejected.
    ModelProviderServiceConfigModelTargetConfig:
      type: object
      properties:
        model:
          type: string
          description: >-
            Provider-side model identifier (e.g. "gpt-5", "claude-opus-4-7").
            This is a string on the LLM provider's side, not a UC entity. The UC
            governance hook for external destinations is the
            ModelProviderService referenced by
            ``ExternalModelConfig.model_provider_service``, not the model
            itself.
        native_api_types:
          type: array
          items:
            type: string
          description: >-
            Provider-native API types the model supports (e.g.
            "openai/v1/chat/completions"). Used by the platform for
            request/response translation from the unified API type. At most 64
            entries of at most 256 characters each; the list is persisted into
            the destination binding's bounded storage envelope.
      required:
        - model
      description: Model target configuration for an external model destination.
    ModelProviderServiceConfigOpenAiProviderConfig:
      type: object
      properties:
        direct:
          $ref: >-
            #/components/schemas/ModelProviderServiceConfigOpenAiProviderDirectConfig
      description: OpenAI provider configuration.
    ModelProviderServiceConfigOpenAiProviderDirectConfig:
      type: object
      properties:
        api_key:
          $ref: '#/components/schemas/ModelProviderServiceConfigProviderSecret'
          description: >-
            OpenAI API key. Required on Create. Supplied as inline plaintext via
            ``ProviderSecret.plaintext``.
        base_url:
          type: string
          description: >-
            Optional custom base URL. Defaults to ``https://api.openai.com/v1``.
            Use for OpenAI-API-compatible third-party endpoints or in-network
            proxies.
        organization:
          type: string
          description: >-
            Optional OpenAI organization ID. When set, the platform forwards it
            as the ``OpenAI-Organization`` header.
      description: Direct (inline-credentials) form of the OpenAI provider config.
    ModelProviderServiceConfigProviderSecret:
      type: object
      properties:
        plaintext:
          type: string
          description: >-
            Inline plaintext credential. INPUT_ONLY: the value never round-trips
            on reads. Get and List responses omit ``plaintext``; the field's
            presence in the read shape only indicates that a secret is
            configured.
      description: >-
        A secret value supplied as part of an inline provider config. The caller
        supplies the value as
            inline ``plaintext`` on writes; the platform stores it encrypted. The ``plaintext`` field is
            ``INPUT_ONLY`` and never round-trips on reads.
    ModelProviderServiceConfigServiceCredential:
      type: object
      properties:
        name:
          type: string
          description: >-
            Resource name of the bound UC service credential, in the AIP-122
            form ``credentials/{name}`` (a metastore-level single-part
            credential name). On create the caller supplies the name here. On
            read it reflects the credential's current name at read time.
      required:
        - name
      description: >-
        ---- Provider configuration (nested; see the ``provider`` oneof below)
        ---- The customer-owned
            UC service credential a ModelProviderService uses to authenticate to its provider, referenced by
            name.
    ModelService:
      type: object
      properties:
        browse_only:
          type: boolean
          description: >-
            Whether the caller sees only metadata available through the BROWSE
            privilege.
        comment:
          type: string
          description: User-provided description.
        config:
          $ref: '#/components/schemas/ModelServiceConfig'
          description: >-
            Operational configuration: destinations, routing, rate limits,
            inference table. Required on CreateModelService; on
            UpdateModelService it is required only when ``config`` (or a
            ``config.*`` subpath) appears in ``update_mask``.
        create_time:
          type: string
          format: date-time
          description: When the model service was created.
        created_by:
          type: string
          description: Creator identity.
        effective_owner:
          type: string
          description: >-
            The resolved owner of the ModelService. Falls back to the caller's
            identity when ``owner`` is not explicitly set on creation.
        etag:
          type: string
          description: >-
            Optimistic concurrency control token. Server-generated from the
            entity's state and returned on every read. To use it as an if-match
            precondition on a mutation, echo the last-read value back via the
            dedicated ``etag`` field on the Update / Delete request; the server
            rejects the mutation if the stored etag differs.
        metastore_id:
          type: string
          description: Metastore hosting the model service.
        name:
          type: string
          description: >-
            Resource name of the model service. Format:
            ``model-services/{catalog}.{schema}.{model_service}``. Each
            ``{...}`` component is capped at 255 characters individually.
            Server-derived on Create from ``parent`` + ``model_service_id``;
            required and immutable on Update/Get/Delete.
        owner:
          type: string
          description: >-
            The owner of the model service. Write-only; read owner via
            effective_owner.
        supported_api_types:
          type: array
          items:
            type: string
          description: >-
            Unified API types this endpoint supports (e.g. "chat", "embeddings",
            "completions"). Derived from the destinations' backing models /
            providers at read time.
        update_time:
          type: string
          format: date-time
          description: When the model service was last modified.
        updated_by:
          type: string
          description: Identity of the last updater.
      description: >-
        A governed AI Gateway endpoint in Unity Catalog that routes inference
        requests to one or more
            model destinations (for example a foundation model or an external LLM reached through a
            ModelProviderService). Applies centralized access control, rate limits, guardrails, and auditing
            to the traffic it serves.
    ModelServiceConfig:
      type: object
      properties:
        inference_table:
          $ref: '#/components/schemas/InferenceTableConfig'
          description: Inference table config for payload logging.
        rate_limits:
          type: array
          items:
            $ref: '#/components/schemas/RateLimit'
          description: Rate limits applied to requests routed through this model service.
        routing:
          $ref: '#/components/schemas/ModelServiceConfigRoutingConfig'
          description: 'Routing configuration: destinations, routing strategy, and fallback.'
      description: Operational configuration wrapped around the ModelService resource.
    ModelServiceConfigDestinationConfig:
      type: object
      properties:
        name:
          type: string
          description: User-facing label for this destination, used in routing references.
        destination_type:
          $ref: >-
            #/components/schemas/ModelServiceConfigDestinationConfigDestinationType
          description: Backing-model category. Determines which oneof variant is populated.
        external_model_config:
          $ref: '#/components/schemas/ModelServiceConfigExternalModelConfig'
        is_deleted:
          type: boolean
          description: >-
            True when the destination's backing UC entity (MODEL for
            foundation-model destinations, MODEL_PROVIDER_SERVICE for external
            destinations) has been deleted but the destination row still
            references it. The dangling destination is surfaced (not silently
            dropped) so callers can see the broken routing. Inference traffic
            through this destination fails closed (BAD_REQUEST /
            FAILED_PRECONDITION).
        pay_per_token_config:
          $ref: '#/components/schemas/ModelServiceConfigPayPerTokenConfig'
        provisioned_throughput_config:
          $ref: '#/components/schemas/ModelServiceConfigProvisionedThroughputConfig'
        traffic_percentage:
          type: integer
          description: >-
            Share of traffic sent to this destination, 0-100. Optional on
            fallback destinations; see FallbackConfig.
      required:
        - name
        - destination_type
      description: >-
        A destination the model service can route traffic to. Exactly one of the
        per-type configs inside
            ``type_config`` must be set, and it must match ``destination_type``.
    ModelServiceConfigExternalModelConfig:
      type: object
      properties:
        model_provider_service:
          type: string
          description: >-
            Resource name of the governed ModelProviderService that owns
            provider auth and provider-specific configuration. The referenced
            ModelProviderService also carries the provider type, so this message
            does not surface it directly. Format:
            ``model-provider-services/{catalog}.{schema}.{model_provider_service}``.
            Each ``{...}`` component is capped at 255 characters individually.
        target:
          $ref: '#/components/schemas/ModelProviderServiceConfigModelTargetConfig'
          description: >-
            Routing target for the destination: the provider-side model selected
            from the referenced ModelProviderService's ``targets`` catalog, plus
            the unified API types the platform should translate to/from at
            request time.
      required:
        - model_provider_service
        - target
      description: >-
        Configuration for an external-foundation-model destination. Provider
        auth and provider-specific
            cloud configuration are owned by a separate, governed ModelProviderService entity referenced via
            ``model_provider_service``; the platform resolves the provider at invocation time.
    ModelServiceConfigFallbackConfig:
      type: object
      properties:
        destinations:
          type: array
          items:
            $ref: '#/components/schemas/ModelServiceConfigDestinationConfig'
          description: >-
            Ordered list of fallback destinations. Traversal is in list order;
            the attempt count is the length of the list. At most 5 are allowed.
      description: >-
        Fallback routing, applied after the primary destination returns a
        retryable error. Traversal is
            in list order; the attempt count is the length of the list.
    ModelServiceConfigPayPerTokenConfig:
      type: object
      properties:
        model:
          type: string
          description: >-
            Resource name of the UC model. Format:
            ``models/{catalog}.{schema}.{model}``.
      required:
        - model
      description: >-
        Configuration for a pay-per-token foundation-model destination.
        Identifies the foundation model
            by its UC resource name; the platform resolves it to a Model Serving endpoint at request time.
    ModelServiceConfigProvisionedThroughputConfig:
      type: object
      properties:
        model_serving_endpoint:
          type: string
          description: >-
            Name of the backing Model Serving endpoint serving the provisioned-
            throughput foundation model, as the AIP-122 typed resource name
            ``serving-endpoints/{name}``. The same UC model can be served on
            multiple Model Serving endpoints (different throughput / region /
            config); the caller picks which one this destination routes to. The
            endpoint must exist at create time.
        model:
          type: string
          description: >-
            UC model FQN of the model served by the backing endpoint (e.g.,
            ``system.ai.databricks-claude-opus-4-6``). Resolved from Model
            Serving at Create/Update time.
      required:
        - model_serving_endpoint
      description: >-
        Configuration for a provisioned-throughput foundation-model destination.
        References a
            pre-existing Model Serving endpoint that serves the model; sizing (provisioned throughput, burst
            scaling, model version) is owned by the Model Serving endpoint itself, not by this message.
    ModelServiceConfigRoutingConfig:
      type: object
      properties:
        destinations:
          type: array
          items:
            $ref: '#/components/schemas/ModelServiceConfigDestinationConfig'
          description: >-
            Primary routing destinations. At most 10 are allowed. At least one
            is required on CreateModelService; on UpdateModelService it is
            required only when ``config.routing`` (or a ``config.routing.*``
            subpath) appears in ``update_mask``.
        fallback:
          $ref: '#/components/schemas/ModelServiceConfigFallbackConfig'
          description: Fallback routing config, applied after primary destinations fail.
        first_token_timeout:
          type: string
          description: >-
            Timeout for the first token of a streaming response. If a
            destination does not return its first token within this duration, AI
            Gateway aborts the attempt and fails over to the next destination.
            Applies to streaming requests only. Leave unset for no first-token
            timeout.
        traffic_splitting:
          $ref: '#/components/schemas/ModelServiceConfigRoutingConfigTrafficSplitting'
          description: >-
            Marker message selecting request-based traffic splitting. Traffic is
            distributed according to each destination's traffic_percentage
            value; no configuration lives on this message itself.
      description: >-
        Routing configuration for a model service, nesting destinations, routing
        strategy, and fallback
            under a single sub-message.
    ModelServiceConfigRoutingConfigTrafficSplitting:
      type: object
      properties: {}
      description: >-
        Marker message selecting request-based traffic splitting across primary
        destinations. Split
            weights are read from each DestinationConfig.traffic_percentage.
    ModelVersionInfo:
      type: object
      properties:
        aliases:
          type: array
          items:
            $ref: '#/components/schemas/RegisteredModelAlias'
        catalog_name:
          type: string
          description: The name of the catalog containing the model version
        comment:
          type: string
          description: The comment attached to the model version
        created_at:
          type: integer
        created_by:
          type: string
          description: The identifier of the user who created the model version
        id:
          type: string
          description: The unique identifier of the model version
        metastore_id:
          type: string
          description: The unique identifier of the metastore containing the model version
        model_name:
          type: string
          description: >-
            The name of the parent registered model of the model version,
            relative to parent schema
        model_version_dependencies:
          $ref: '#/components/schemas/DependencyList'
          description: Model version dependencies, for feature-store packaged models
        run_id:
          type: string
          description: >-
            MLflow run ID used when creating the model version, if ``source``
            was generated by an experiment run stored in an MLflow tracking
            server
        run_workspace_id:
          type: integer
          description: >-
            ID of the Databricks workspace containing the MLflow run that
            generated this model version, if applicable
        schema_name:
          type: string
          description: >-
            The name of the schema containing the model version, relative to
            parent catalog
        source:
          type: string
          description: >-
            URI indicating the location of the source artifacts (files) for the
            model version
        status:
          $ref: '#/components/schemas/ModelVersionInfoStatus'
          description: >-
            Current status of the model version. Newly created model versions
            start in PENDING_REGISTRATION status, then move to READY status once
            the model version files are uploaded and the model version is
            finalized. Only model versions in READY status can be loaded for
            inference or served.
        storage_location:
          type: string
          description: >-
            The storage location on the cloud under which model version data
            files are stored
        updated_at:
          type: integer
        updated_by:
          type: string
          description: The identifier of the user who updated the model version last time
        version:
          type: integer
          description: >-
            Integer model version number, used to reference the model version in
            API requests.
    MonitorCronSchedule:
      type: object
      properties:
        quartz_cron_expression:
          type: string
        timezone_id:
          type: string
          description: >-
            The timezone id (e.g., ``PST``) in which to evaluate the quartz
            expression.
        pause_status:
          $ref: '#/components/schemas/MonitorCronSchedulePauseStatus'
          description: Read only field that indicates whether a schedule is paused or not.
      required:
        - quartz_cron_expression
        - timezone_id
    MonitorDataClassificationConfig:
      type: object
      properties:
        enabled:
          type: boolean
          description: Whether to enable data classification.
      description: Data classification related configuration.
    MonitorDestination:
      type: object
      properties:
        email_addresses:
          type: array
          items:
            type: string
    MonitorInferenceLog:
      type: object
      properties:
        problem_type:
          $ref: '#/components/schemas/MonitorInferenceLogProblemType'
        timestamp_col:
          type: string
          description: Column for the timestamp.
        granularities:
          type: array
          items:
            type: string
          description: >-
            List of granularities to use when aggregating data into time windows
            based on their timestamp.
        prediction_col:
          type: string
          description: Column for the prediction.
        model_id_col:
          type: string
          description: Column for the model identifier.
        label_col:
          type: string
          description: Column for the label.
        prediction_proba_col:
          type: string
          description: Column for prediction probabilities
      required:
        - problem_type
        - timestamp_col
        - granularities
        - prediction_col
        - model_id_col
    MonitorInfo:
      type: object
      properties:
        output_schema_name:
          type: string
        table_name:
          type: string
          description: >-
            [Create:ERR Update:IGN] UC table to monitor. Format:
            ``catalog.schema.table_name``
        status:
          $ref: '#/components/schemas/MonitorInfoStatus'
          description: '[Create:ERR Update:IGN] The monitor status.'
        profile_metrics_table_name:
          type: string
          description: >-
            [Create:ERR Update:IGN] Table that stores profile metrics data.
            Format: ``catalog.schema.table_name``.
        drift_metrics_table_name:
          type: string
          description: >-
            [Create:ERR Update:IGN] Table that stores drift metrics data.
            Format: ``catalog.schema.table_name``.
        monitor_version:
          type: integer
          description: >-
            [Create:ERR Update:IGN] Represents the current monitor configuration
            version in use. The version will be represented in a numeric fashion
            (1,2,3...). The field has flexibility to take on negative values,
            which can indicate corrupted monitor_version numbers.
        assets_dir:
          type: string
          description: >-
            [Create:REQ Update:IGN] Field for specifying the absolute path to a
            custom directory to store data-monitoring assets. Normally
            prepopulated to a default user location via UI and Python APIs.
        baseline_table_name:
          type: string
          description: >-
            [Create:OPT Update:OPT] Baseline table name. Baseline data is used
            to compute drift from the data in the monitored ``table_name``. The
            baseline table and the monitored table shall have the same schema.
        custom_metrics:
          type: array
          items:
            $ref: '#/components/schemas/MonitorMetric'
          description: '[Create:OPT Update:OPT] Custom metrics.'
        dashboard_id:
          type: string
          description: >-
            [Create:ERR Update:OPT] Id of dashboard that visualizes the computed
            metrics. This can be empty if the monitor is in PENDING state.
        data_classification_config:
          $ref: '#/components/schemas/MonitorDataClassificationConfig'
          description: '[Create:OPT Update:OPT] Data classification related config.'
        inference_log:
          $ref: '#/components/schemas/MonitorInferenceLog'
        latest_monitor_failure_msg:
          type: string
          description: >-
            [Create:ERR Update:IGN] The latest error message for a monitor
            failure.
        notifications:
          $ref: '#/components/schemas/MonitorNotifications'
          description: '[Create:OPT Update:OPT] Field for specifying notification settings.'
        schedule:
          $ref: '#/components/schemas/MonitorCronSchedule'
          description: '[Create:OPT Update:OPT] The monitor schedule.'
        slicing_exprs:
          type: array
          items:
            type: string
          description: >-
            [Create:OPT Update:OPT] List of column expressions to slice data
            with for targeted analysis. The data is grouped by each expression
            independently, resulting in a separate slice for each predicate and
            its complements. For example ``slicing_exprs=[“col_1”, “col_2 >
            10”]`` will generate the following slices: two slices for ``col_2 >
            10`` (True and False), and one slice per unique value in ``col1``.
            For high-cardinality columns, only the top 100 unique values by
            frequency will generate slices.
        snapshot:
          $ref: '#/components/schemas/MonitorSnapshot'
          description: Configuration for monitoring snapshot tables.
        time_series:
          $ref: '#/components/schemas/MonitorTimeSeries'
          description: Configuration for monitoring time series tables.
      required:
        - output_schema_name
        - table_name
        - status
        - profile_metrics_table_name
        - drift_metrics_table_name
        - monitor_version
    MonitorMetric:
      type: object
      properties:
        name:
          type: string
          description: Name of the metric in the output tables.
        definition:
          type: string
          description: >-
            Jinja template for a SQL expression that specifies how to compute
            the metric. See `create metric definition
            <https://docs.databricks.com/en/lakehouse-monitoring/custom-metrics.html#create-definition>`__.
        input_columns:
          type: array
          items:
            type: string
          description: >-
            A list of column names in the input table the metric should be
            computed for. Can use ``":table"`` to indicate that the metric needs
            information from multiple columns.
        output_data_type:
          type: string
          description: The output type of the custom metric.
        type:
          $ref: '#/components/schemas/MonitorMetricType'
          description: >-
            Can only be one of ``"CUSTOM_METRIC_TYPE_AGGREGATE"``,
            ``"CUSTOM_METRIC_TYPE_DERIVED"``, or ``"CUSTOM_METRIC_TYPE_DRIFT"``.
            The ``"CUSTOM_METRIC_TYPE_AGGREGATE"`` and
            ``"CUSTOM_METRIC_TYPE_DERIVED"`` metrics are computed on a single
            table, whereas the ``"CUSTOM_METRIC_TYPE_DRIFT"`` compare metrics
            across baseline and input table, or across the two consecutive time
            windows. - CUSTOM_METRIC_TYPE_AGGREGATE: only depend on the existing
            columns in your table - CUSTOM_METRIC_TYPE_DERIVED: depend on
            previously computed aggregate metrics - CUSTOM_METRIC_TYPE_DRIFT:
            depend on previously computed aggregate or derived metrics
      required:
        - name
        - definition
        - input_columns
        - output_data_type
        - type
      description: Custom metric definition.
    MonitorNotifications:
      type: object
      properties:
        on_failure:
          $ref: '#/components/schemas/MonitorDestination'
        on_new_classification_tag_detected:
          $ref: '#/components/schemas/MonitorDestination'
          description: >-
            Destinations to send notifications on new classification tag
            detected.
    MonitorRefreshInfo:
      type: object
      properties:
        refresh_id:
          type: integer
        state:
          $ref: '#/components/schemas/MonitorRefreshInfoState'
          description: The current state of the refresh.
        start_time_ms:
          type: integer
          description: >-
            Time at which refresh operation was initiated (milliseconds since
            1/1/1970 UTC).
        end_time_ms:
          type: integer
          description: >-
            Time at which refresh operation completed (milliseconds since
            1/1/1970 UTC).
        message:
          type: string
          description: >-
            An optional message to give insight into the current state of the
            job (e.g. FAILURE messages).
        trigger:
          $ref: '#/components/schemas/MonitorRefreshInfoTrigger'
          description: The method by which the refresh was triggered.
      required:
        - refresh_id
        - state
        - start_time_ms
    MonitorRefreshListResponse:
      type: object
      properties:
        refreshes:
          type: array
          items:
            $ref: '#/components/schemas/MonitorRefreshInfo'
    MonitorSnapshot:
      type: object
      properties: {}
      description: Snapshot analysis configuration
    MonitorTimeSeries:
      type: object
      properties:
        timestamp_col:
          type: string
          description: Column for the timestamp.
        granularities:
          type: array
          items:
            type: string
          description: >-
            Granularities for aggregating data into time windows based on their
            timestamp. Currently the following static granularities are
            supported: {``\"5 minutes\"``, ``\"30 minutes\"``, ``\"1 hour\"``,
            ``\"1 day\"``, ``\"\u003cn\u003e week(s)\"``, ``\"1 month\"``, ``\"1
            year\"``}.
      required:
        - timestamp_col
        - granularities
      description: Time series analysis configuration.
    NamedTableConstraint:
      type: object
      properties:
        name:
          type: string
      required:
        - name
    NotificationDestination:
      type: object
      properties:
        destination_id:
          type: string
        destination_type:
          $ref: '#/components/schemas/DestinationType'
          description: The type of the destination.
        special_destination:
          $ref: '#/components/schemas/SpecialDestination'
          description: >-
            This field is used to denote whether the destination is the email of
            the owner of the securable object. The special destination cannot be
            assigned to a securable and only represents the default destination
            of the securable. The securable types that support default special
            destinations are: "catalog", "external_location", "connection",
            "credential", and "metastore". The **destination_type** of a
            **special_destination** is always EMAIL.
    OneLakeEventQueue:
      type: object
      properties:
        consumer_group:
          type: string
          description: >-
            Event Hubs consumer group used to consume file events. Defaults to
            "$Default" when unset. Recommended for provided_onelake: create a
            dedicated consumer group on the Event Hub for file events to avoid
            contending with the customer's other consumers.
        event_hub_name:
          type: string
          description: >-
            Event Hub entity name within the namespace. Only required for
            provided_onelake.
        event_hub_url:
          type: string
          description: >-
            The Event Hub URL in the format
            https://{namespace}.servicebus.windows.net/{event_hub_name}.
            Deprecated: use fully_qualified_namespace + event_hub_name instead.
        fully_qualified_namespace:
          type: string
          description: >-
            The fully qualified domain name of the Event Hubs namespace, e.g.
            {yournamespace}.servicebus.windows.net. Only required for
            provided_onelake.
        managed_resource_id:
          type: string
          description: >-
            Unique identifier included in the name of the file events managed
            resources.
      description: >-
        File event queue for OneLake (Microsoft Fabric) locations. Events flow
        through Fabric
            Eventstream in both arms; CSMS consumes from a user-provided Azure Event Hub (provided_onelake)
            or from a Fabric Eventstream that CSMS provisions in the user's workspace (managed_onelake).
    OnlineTable:
      type: object
      properties:
        name:
          type: string
          description: Full three-part (catalog, schema, table) name of the table.
        spec:
          $ref: '#/components/schemas/OnlineTableSpec'
          description: Specification of the online table.
        status:
          $ref: '#/components/schemas/OnlineTableStatus'
          description: Online Table data synchronization status
        table_serving_url:
          type: string
          description: Data serving REST API URL for this table
        unity_catalog_provisioning_state:
          $ref: '#/components/schemas/ProvisioningInfoState'
          description: >-
            The provisioning state of the online table entity in Unity Catalog.
            This is distinct from the state of the data synchronization pipeline
            (i.e. the table may be in "ACTIVE" but the pipeline may be in
            "PROVISIONING" as it runs asynchronously).
      description: Online Table information.
    OnlineTableSpec:
      type: object
      properties:
        perform_full_copy:
          type: boolean
          description: >-
            Whether to create a full-copy pipeline -- a pipeline that stops
            after creates a full copy of the source table upon initialization
            and does not process any change data feeds (CDFs) afterwards. The
            pipeline can still be manually triggered afterwards, but it always
            perform a full copy of the source table and there are no incremental
            updates. This mode is useful for syncing views or tables without
            CDFs to online tables. Note that the full-copy pipeline only
            supports "triggered" scheduling policy.
        pipeline_id:
          type: string
          description: >-
            ID of the associated pipeline. Generated by the server - cannot be
            set by the caller.
        primary_key_columns:
          type: array
          items:
            type: string
          description: >-
            Primary Key columns to be used for data insert/update in the
            destination.
        run_continuously:
          $ref: '#/components/schemas/OnlineTableSpecContinuousSchedulingPolicy'
          description: Pipeline runs continuously after generating the initial data.
        run_triggered:
          $ref: '#/components/schemas/OnlineTableSpecTriggeredSchedulingPolicy'
          description: >-
            Pipeline stops after generating the initial data and can be
            triggered later (manually, through a cron job or through data
            triggers)
        source_table_full_name:
          type: string
          description: Three-part (catalog, schema, table) name of the source Delta table.
        timeseries_key:
          type: string
          description: >-
            Time series key to deduplicate (tie-break) rows with the same
            primary key.
      description: Specification of an online table.
    OnlineTableSpecContinuousSchedulingPolicy:
      type: object
      properties: {}
    OnlineTableSpecTriggeredSchedulingPolicy:
      type: object
      properties: {}
    OnlineTableStatus:
      type: object
      properties:
        continuous_update_status:
          $ref: '#/components/schemas/ContinuousUpdateStatus'
        detailed_state:
          $ref: '#/components/schemas/OnlineTableState'
          description: The state of the online table.
        failed_status:
          $ref: '#/components/schemas/FailedStatus'
        message:
          type: string
          description: A text description of the current state of the online table.
        provisioning_status:
          $ref: '#/components/schemas/ProvisioningStatus'
        triggered_update_status:
          $ref: '#/components/schemas/TriggeredUpdateStatus'
      description: Status of an online table.
    OptionSpec:
      type: object
      properties:
        allowed_values:
          type: array
          items:
            type: string
          description: >-
            For drop down / radio button selections, UI will want to know the
            possible input values, it can also be used by other option types to
            limit input selections.
        conditional_display:
          $ref: '#/components/schemas/ConditionalDisplay'
          description: >-
            Conditional display configuration. Specifies when this option should
            be hidden based on another option's value.
        default_value:
          type: string
          description: >-
            The default value of the option, for example, value '443' for 'port'
            option.
        description:
          type: string
          description: >-
            A concise user facing description of what the input value of this
            option should look like.
        hint:
          type: string
          description: >-
            The hint is used on the UI to suggest what the input value can
            possibly be like, for example: example.com for 'host' option. Unlike
            default value, it will not be applied automatically without user
            input.
        is_copiable:
          type: boolean
          description: >-
            Indicates whether an option should be displayed with copy button on
            the UI.
        is_creatable:
          type: boolean
          description: >-
            Indicates whether an option can be provided by users in the
            create/update path of an entity.
        is_hidden:
          type: boolean
          description: >-
            Is the option value not user settable and is thus not shown on the
            UI.
        is_loggable:
          type: boolean
          description: >-
            Specifies whether this option is safe to log, i.e. no sensitive
            information.
        is_required:
          type: boolean
          description: Is the option required.
        is_secret:
          type: boolean
          description: Is the option value considered secret and thus redacted on the UI.
        is_updatable:
          type: boolean
          description: Is the option updatable by users.
        name:
          type: string
          description: The unique name of the option.
        oauth_stage:
          $ref: '#/components/schemas/OptionSpecOauthStage'
          description: >-
            Specifies when the option value is displayed on the UI within the
            OAuth flow.
        type:
          $ref: '#/components/schemas/OptionSpecOptionType'
          description: The type of the option.
      description: >-
        Spec of an allowed option on a securable kind and its attributes. This
        is mostly used by UI to
            provide user friendly hints and descriptions in order to facilitate the securable creation
            process.
    PermissionsChange:
      type: object
      properties:
        add:
          type: array
          items:
            $ref: '#/components/schemas/Privilege'
        principal:
          type: string
          description: >-
            The principal whose privileges we are changing. Only one of
            principal or principal_id should be specified, never both at the
            same time.
        principal_id:
          type: integer
          description: >-
            An opaque internal ID that identifies the principal whose privileges
            should be removed. This field is intended for removing privileges
            associated with a deleted user. When set, only the entries specified
            in the remove field are processed; any entries in the add field will
            be rejected. Only one of principal or principal_id should be
            specified, never both at the same time.
        remove:
          type: array
          items:
            $ref: '#/components/schemas/Privilege'
          description: The set of privileges to remove.
    PipelineProgress:
      type: object
      properties:
        estimated_completion_time_seconds:
          type: number
          description: The estimated time remaining to complete this update in seconds.
        latest_version_currently_processing:
          type: integer
          description: >-
            The source table Delta version that was last processed by the
            pipeline. The pipeline may not have completely processed this
            version yet.
        sync_progress_completion:
          type: number
          description: >-
            The completion ratio of this update. This is a number between 0 and
            1.
        synced_row_count:
          type: integer
          description: The number of rows that have been synced in this update.
        total_row_count:
          type: integer
          description: >-
            The total number of rows that need to be synced in this update. This
            number may be an estimate.
      description: Progress information of the Online Table data synchronization pipeline.
    PolicyFunctionArgument:
      type: object
      properties:
        column:
          type: string
          description: A column reference.
        constant:
          type: string
          description: A constant literal.
      description: >-
        A positional argument passed to a row filter or column mask function.
        Distinguishes between
            column references and literals.
    PolicyInfo:
      type: object
      properties:
        to_principals:
          type: array
          items:
            type: string
        for_securable_type:
          $ref: '#/components/schemas/SecurableType'
          description: >-
            Type of securables that the policy should take effect on. Only
            ``TABLE`` is supported at this moment. Required on create and
            optional on update.
        policy_type:
          $ref: '#/components/schemas/PolicyType'
          description: Type of the policy. Required on create.
        column_mask:
          $ref: '#/components/schemas/ColumnMaskOptions'
          description: >-
            Options for column mask policies. Valid only if ``policy_type`` is
            ``POLICY_TYPE_COLUMN_MASK``. Required on create and optional on
            update. When specified on update, the new options will replace the
            existing options as a whole.
        comment:
          type: string
          description: Optional description of the policy.
        created_at:
          type: integer
          description: >-
            Time at which the policy was created, in epoch milliseconds. Output
            only.
        created_by:
          type: string
          description: Username of the user who created the policy. Output only.
        deny:
          $ref: '#/components/schemas/DenyOptions'
          description: >-
            Options for deny policies. Valid only if ``policy_type`` is
            ``POLICY_TYPE_DENY``. Required on create and optional on update.
            When specified on update, the new options will replace the existing
            options as a whole.
        except_principals:
          type: array
          items:
            type: string
          description: >-
            Optional list of user or group names that should be excluded from
            the policy.
        grant:
          $ref: '#/components/schemas/GrantOptions'
          description: >-
            Options for grant policies. Valid only if ``policy_type`` is
            ``POLICY_TYPE_GRANT``. Required on create and optional on update.
            When specified on update, the new options will replace the existing
            options as a whole.
        id:
          type: string
          description: >-
            Unique identifier of the policy. This field is output only and is
            generated by the system.
        match_columns:
          type: array
          items:
            $ref: '#/components/schemas/MatchColumn'
          description: >-
            Optional list of condition expressions used to match table columns.
            Only valid when ``for_securable_type`` is ``TABLE``. When specified,
            the policy only applies to tables whose columns satisfy all match
            conditions.
        name:
          type: string
          description: >-
            Name of the policy. Required on create and optional on update. To
            rename the policy, set ``name`` to a different value on update.
        on_securable_fullname:
          type: string
          description: >-
            Full name of the securable on which the policy is defined. Required
            on create.
        on_securable_type:
          $ref: '#/components/schemas/SecurableType'
          description: >-
            Type of the securable on which the policy is defined. Only
            ``CATALOG``, ``SCHEMA`` and ``TABLE`` are supported at this moment.
            Required on create.
        row_filter:
          $ref: '#/components/schemas/RowFilterOptions'
          description: >-
            Options for row filter policies. Valid only if ``policy_type`` is
            ``POLICY_TYPE_ROW_FILTER``. Required on create and optional on
            update. When specified on update, the new options will replace the
            existing options as a whole.
        updated_at:
          type: integer
          description: >-
            Time at which the policy was last modified, in epoch milliseconds.
            Output only.
        updated_by:
          type: string
          description: Username of the user who last modified the policy. Output only.
        use_session_identity:
          type: boolean
          description: >-
            Temporary for migrating customers to session identity. Customers not
            currently using ABAC will not be able to set this field to false and
            all new policies will have this field default to true. Existing
            customers will have this field default to false, but can set it to
            true to opt in to session identity. after a grace period, this field
            will be removed and all policies will use session identity. Only for
            row filter and column mask policies. Not applicable to deny
            policies.
        when_condition:
          type: string
          description: Optional condition when the policy should take effect.
      required:
        - to_principals
        - for_securable_type
        - policy_type
    PrimaryKeyConstraint:
      type: object
      properties:
        name:
          type: string
        child_columns:
          type: array
          items:
            type: string
          description: Column names for this constraint.
        rely:
          type: boolean
          description: True if the constraint is RELY, false or unset if NORELY.
        timeseries_columns:
          type: array
          items:
            type: string
          description: Column names that represent a timeseries.
      required:
        - name
        - child_columns
    Principal:
      type: object
      properties:
        id:
          type: string
        principal_type:
          $ref: '#/components/schemas/PrincipalType'
    PrivilegeAssignment:
      type: object
      properties:
        principal:
          type: string
        principal_id:
          type: integer
          description: >-
            Unique identifier of the principal. For active principals, both
            ``principal`` and ``principal_id`` are present.
        privileges:
          type: array
          items:
            $ref: '#/components/schemas/Privilege'
          description: The privileges assigned to the principal.
    ProvisioningInfo:
      type: object
      properties:
        state:
          $ref: '#/components/schemas/ProvisioningInfoState'
          description: The provisioning state of the resource.
      description: Status of an asynchronously provisioned resource.
    ProvisioningStatus:
      type: object
      properties:
        initial_pipeline_sync_progress:
          $ref: '#/components/schemas/PipelineProgress'
          description: >-
            Details about initial data synchronization. Only populated when in
            the PROVISIONING_INITIAL_SNAPSHOT state.
      description: |-
        Detailed status of an online table. Shown if the online table is in the
            PROVISIONING_PIPELINE_RESOURCES or the PROVISIONING_INITIAL_SNAPSHOT state.
    QuotaInfo:
      type: object
      properties:
        last_refreshed_at:
          type: integer
        parent_full_name:
          type: string
          description: >-
            Name of the parent resource. Returns metastore ID if the parent is a
            metastore.
        parent_securable_type:
          $ref: '#/components/schemas/SecurableType'
          description: The quota parent securable type.
        quota_count:
          type: integer
          description: The current usage of the resource quota.
        quota_limit:
          type: integer
          description: The current limit of the resource quota.
        quota_name:
          type: string
          description: The name of the quota.
    R2Credentials:
      type: object
      properties:
        access_key_id:
          type: string
          description: The access key ID that identifies the temporary credentials.
        secret_access_key:
          type: string
          description: The secret access key associated with the access key.
        session_token:
          type: string
          description: >-
            The generated JWT that users must pass to use the temporary
            credentials.
      description: |-
        R2 temporary credentials for API authentication. Read more at
            https://developers.cloudflare.com/r2/api/s3/tokens/.
    RateLimit:
      type: object
      properties:
        key:
          $ref: '#/components/schemas/RateLimitRateLimitKey'
          description: Scope key. Determines whether ``principal`` is required.
        renewal_period:
          $ref: '#/components/schemas/RateLimitRateLimitRenewalPeriod'
          description: Renewal period.
        principal:
          type: string
          description: >-
            Principal this limit applies to: user email, group name, or service
            principal application ID. Required unless ``key`` is
            ``RATE_LIMIT_KEY_SERVICE``, ``RATE_LIMIT_KEY_USER_DEFAULT``, or
            ``RATE_LIMIT_KEY_REQUEST_TAG`` (which must not set a principal).
        request_tag_key:
          type: string
          description: >-
            Request tag key this limit applies to. Required when ``key`` is
            ``RATE_LIMIT_KEY_REQUEST_TAG``, forbidden otherwise.
        request_tag_value:
          type: string
          description: >-
            Request tag value this limit applies to. Only valid when ``key`` is
            ``RATE_LIMIT_KEY_REQUEST_TAG``. Leave unset to apply the limit to
            every value of ``request_tag_key`` (an any-value default); a set
            value is a specific override for that value.
        requests:
          type: integer
          description: >-
            Max requests allowed within a renewal period. Leave unset for no
            request limit.
        tokens:
          type: integer
          description: >-
            Max tokens allowed within a renewal period. Leave unset for no token
            limit.
      required:
        - key
        - renewal_period
      description: >-
        A rate limit applied to service requests. Leave ``requests`` or
        ``tokens`` unset to impose no
            limit on that dimension; set a value to cap that dimension within the renewal period.
    RegenerateDashboardResponse:
      type: object
      properties:
        dashboard_id:
          type: string
        parent_folder:
          type: string
          description: Parent folder is equivalent to {assets_dir}/{tableName}
    RegisteredModelAlias:
      type: object
      properties:
        alias_name:
          type: string
        catalog_name:
          type: string
          description: The name of the catalog containing the model version
        id:
          type: string
          description: The unique identifier of the alias
        model_name:
          type: string
          description: >-
            The name of the parent registered model of the model version,
            relative to parent schema
        schema_name:
          type: string
          description: >-
            The name of the schema containing the model version, relative to
            parent catalog
        version_num:
          type: integer
          description: >-
            Integer version number of the model version to which this alias
            points.
    RegisteredModelInfo:
      type: object
      properties:
        aliases:
          type: array
          items:
            $ref: '#/components/schemas/RegisteredModelAlias'
        browse_only:
          type: boolean
          description: >-
            Indicates whether the principal is limited to retrieving metadata
            for the associated object through the BROWSE privilege when
            include_browse is enabled in the request.
        catalog_name:
          type: string
          description: >-
            The name of the catalog where the schema and the registered model
            reside
        comment:
          type: string
          description: The comment attached to the registered model
        created_at:
          type: integer
          description: >-
            Creation timestamp of the registered model in milliseconds since the
            Unix epoch
        created_by:
          type: string
          description: The identifier of the user who created the registered model
        full_name:
          type: string
          description: The three-level (fully qualified) name of the registered model
        metastore_id:
          type: string
          description: The unique identifier of the metastore
        name:
          type: string
          description: The name of the registered model
        owner:
          type: string
          description: The identifier of the user who owns the registered model
        schema_name:
          type: string
          description: The name of the schema where the registered model resides
        storage_location:
          type: string
          description: >-
            The storage location on the cloud under which model version data
            files are stored
        updated_at:
          type: integer
          description: >-
            Last-update timestamp of the registered model in milliseconds since
            the Unix epoch
        updated_by:
          type: string
          description: >-
            The identifier of the user who updated the registered model last
            time
    RowFilterOptions:
      type: object
      properties:
        function_name:
          type: string
        using:
          type: array
          items:
            $ref: '#/components/schemas/FunctionArgument'
          description: >-
            Optional list of column aliases or constant literals to be passed as
            arguments to the row filter function. The type of each column should
            match the positional argument of the row filter function.
      required:
        - function_name
    SchemaInfo:
      type: object
      properties:
        browse_only:
          type: boolean
        catalog_name:
          type: string
          description: Name of parent catalog.
        catalog_type:
          $ref: '#/components/schemas/CatalogType'
          description: The type of the parent catalog.
        comment:
          type: string
          description: User-provided free-form text description.
        created_at:
          type: integer
          description: Time at which this schema was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of schema creator.
        custom_max_retention_hours:
          type: integer
          description: Custom maximum retention period in hours for the schema.
        effective_predictive_optimization_flag:
          $ref: '#/components/schemas/EffectivePredictiveOptimizationFlag'
        enable_predictive_optimization:
          $ref: '#/components/schemas/EnablePredictiveOptimization'
          description: >-
            Whether predictive optimization should be enabled for this object
            and objects under it.
        full_name:
          type: string
          description: Full name of schema, in form of **catalog_name**.**schema_name**.
        metastore_id:
          type: string
          description: Unique identifier of parent metastore.
        name:
          type: string
          description: Name of schema, relative to parent catalog.
        owner:
          type: string
          description: Username of current owner of schema.
        properties:
          type: object
          description: A map of key-value properties attached to the securable.
        schema_id:
          type: string
          description: The unique identifier of the schema.
        storage_location:
          type: string
          description: Storage location for managed tables within schema.
        storage_root:
          type: string
          description: Storage root URL for managed tables within schema.
        updated_at:
          type: integer
          description: Time at which this schema was created, in epoch milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified schema.
    Secret:
      type: object
      properties:
        name:
          type: string
          description: The name of the secret, relative to its parent schema.
        catalog_name:
          type: string
          description: The name of the catalog where the schema and the secret reside.
        schema_name:
          type: string
          description: The name of the schema where the secret resides.
        value:
          type: string
          description: >-
            The secret value to store. This field is input-only and is not
            returned in responses — use the **effective_value** field (via
            GetSecret with **include_value** set to true) to read the secret
            value. The maximum size is 60 KiB (pre-encryption). Accepted content
            includes passwords, tokens, keys, and other sensitive credential
            data.
        comment:
          type: string
          description: User-provided free-form text description of the secret.
        create_time:
          type: string
          format: date-time
          description: The time at which this secret was created.
        created_by:
          type: string
          description: The principal that created the secret.
        effective_owner:
          type: string
          description: >-
            The effective owner of the secret, which may differ from the
            directly-set **owner** due to inheritance.
        effective_value:
          type: string
          description: >-
            The secret value. Only populated in responses when you have the
            **READ_SECRET** privilege and **include_value** is set to true in
            the request. The maximum size is 60 KiB.
        expire_time:
          type: string
          format: date-time
          description: >-
            User-provided expiration time of the secret. This field indicates
            when the secret should no longer be used and may be displayed as a
            warning in the UI. It is purely informational and does not trigger
            any automatic actions or affect the secret's lifecycle.
        full_name:
          type: string
          description: >-
            The three-level (fully qualified) name of the secret, in the form of
            **catalog_name.schema_name.secret_name**.
        metastore_id:
          type: string
          description: Unique identifier of the metastore hosting the secret.
        owner:
          type: string
          description: >-
            The owner of the secret. Defaults to the creating principal on
            creation. Can be updated to transfer ownership of the secret to
            another principal.
        update_time:
          type: string
          format: date-time
          description: The time at which this secret was last updated.
        updated_by:
          type: string
          description: The principal that last updated the secret.
      required:
        - name
        - catalog_name
        - schema_name
        - value
      description: >-
        A secret stored in Unity Catalog. Secrets are three-level namespace
        objects
            (catalog.schema.secret) that securely store sensitive credential data such as passwords, tokens,
            and keys.
    SecretDependency:
      type: object
      properties:
        secret_full_name:
          type: string
          description: >-
            Full name of the dependent secret, in the form of
            **catalog_name**.**schema_name**.**secret_name**.
      required:
        - secret_full_name
      description: A secret that is dependent on a SQL object.
    Securable:
      type: object
      properties:
        full_name:
          type: string
          description: >-
            Required. The full name of the catalog/schema/table. Optional if
            resource_name is present.
        provider_share:
          type: string
          description: >-
            Optional. The name of the Share object that contains the securable
            when the securable is getting shared in D2D Delta Sharing.
        type:
          $ref: '#/components/schemas/SecurableType'
          description: >-
            Required. The type of securable (catalog/schema/table). Optional if
            resource_name is present.
      description: >-
        Generic definition of a securable, which is uniquely defined in a
        metastore by its type and full
            name.
    SecurableKindManifest:
      type: object
      properties:
        assignable_privileges:
          type: array
          items:
            type: string
          description: Privileges that can be assigned to the securable.
        capabilities:
          type: array
          items:
            type: string
          description: A list of capabilities in the securable kind.
        options:
          type: array
          items:
            $ref: '#/components/schemas/OptionSpec'
          description: Detailed specs of allowed options.
        securable_kind:
          $ref: '#/components/schemas/SecurableKind'
          description: Securable kind to get manifest of.
        securable_type:
          $ref: '#/components/schemas/SecurableType'
          description: Securable Type of the kind.
      description: Manifest of a specific securable kind.
    SecurablePermissions:
      type: object
      properties:
        permissions:
          type: array
          items:
            type: string
        securable:
          $ref: '#/components/schemas/Securable'
          description: >-
            The securable for which the access request destinations are being
            requested.
    SseEncryptionDetails:
      type: object
      properties:
        algorithm:
          $ref: '#/components/schemas/SseEncryptionDetailsAlgorithm'
          description: >-
            Sets the value of the 'x-amz-server-side-encryption' header in S3
            request.
        aws_kms_key_arn:
          type: string
          description: >-
            Optional. The ARN of the SSE-KMS key used with the S3 location, when
            algorithm = "SSE-KMS". Sets the value of the
            'x-amz-server-side-encryption-aws-kms-key-id' header.
      description: Server-Side Encryption properties for clients communicating with AWS s3.
    StorageCredentialInfo:
      type: object
      properties:
        aws_iam_role:
          $ref: '#/components/schemas/AwsIamRoleResponse'
        azure_managed_identity:
          $ref: '#/components/schemas/AzureManagedIdentityResponse'
          description: The Azure managed identity configuration.
        azure_service_principal:
          $ref: '#/components/schemas/AzureServicePrincipal'
          description: The Azure service principal configuration.
        cloudflare_api_token:
          $ref: '#/components/schemas/CloudflareApiToken'
          description: The Cloudflare API token configuration.
        comment:
          type: string
          description: Comment associated with the credential.
        created_at:
          type: integer
          description: Time at which this credential was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of credential creator.
        databricks_gcp_service_account:
          $ref: '#/components/schemas/DatabricksGcpServiceAccountResponse'
          description: The Databricks managed GCP service account configuration.
        full_name:
          type: string
          description: The full name of the credential.
        id:
          type: string
          description: The unique identifier of the credential.
        isolation_mode:
          $ref: '#/components/schemas/IsolationMode'
          description: >-
            Whether the current securable is accessible from all workspaces or a
            specific set of workspaces.
        metastore_id:
          type: string
          description: Unique identifier of the parent metastore.
        name:
          type: string
          description: >-
            The credential name. The name must be unique among storage and
            service credentials within the metastore.
        owner:
          type: string
          description: Username of current owner of credential.
        read_only:
          type: boolean
          description: >-
            Whether the credential is usable only for read operations. Only
            applicable when purpose is **STORAGE**.
        updated_at:
          type: integer
          description: >-
            Time at which this credential was last modified, in epoch
            milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified the credential.
        used_for_managed_storage:
          type: boolean
          description: >-
            Whether this credential is the current metastore's root storage
            credential. Only applicable when purpose is **STORAGE**.
    SystemSchemaInfo:
      type: object
      properties:
        schema:
          type: string
        state:
          type: string
          description: >-
            The current state of enablement for the system schema. An empty
            string means the system schema is available and ready for opt-in.
            Possible values: AVAILABLE | ENABLE_INITIALIZED | ENABLE_COMPLETED |
            DISABLE_INITIALIZED | UNAVAILABLE | MANAGED
      required:
        - schema
        - state
    TableConstraint:
      type: object
      properties:
        foreign_key_constraint:
          $ref: '#/components/schemas/ForeignKeyConstraint'
        named_table_constraint:
          $ref: '#/components/schemas/NamedTableConstraint'
        primary_key_constraint:
          $ref: '#/components/schemas/PrimaryKeyConstraint'
      description: >-
        A table constraint, as defined by *one* of the following fields being
        set:
            **primary_key_constraint**, **foreign_key_constraint**, **named_table_constraint**.
    TableDependency:
      type: object
      properties:
        table_full_name:
          type: string
          description: >-
            Full name of the dependent table, in the form of
            **catalog_name**.**schema_name**.**table_name**.
      required:
        - table_full_name
      description: A table that is dependent on a SQL object.
    TableExistsResponse:
      type: object
      properties:
        table_exists:
          type: boolean
    TableInfo:
      type: object
      properties:
        access_point:
          type: string
        browse_only:
          type: boolean
          description: >-
            Indicates whether the principal is limited to retrieving metadata
            for the associated object through the BROWSE privilege when
            include_browse is enabled in the request.
        catalog_name:
          type: string
          description: Name of parent catalog.
        columns:
          type: array
          items:
            $ref: '#/components/schemas/ColumnInfo'
          description: The array of **ColumnInfo** definitions of the table's columns.
        comment:
          type: string
          description: User-provided free-form text description.
        created_at:
          type: integer
          description: Time at which this table was created, in epoch milliseconds.
        created_by:
          type: string
          description: Username of table creator.
        data_access_configuration_id:
          type: string
          description: >-
            Unique ID of the Data Access Configuration to use with the table
            data.
        data_source_format:
          $ref: '#/components/schemas/DataSourceFormat'
        deleted_at:
          type: integer
          description: >-
            Time at which this table was deleted, in epoch milliseconds. Field
            is omitted if table is not deleted.
        delta_runtime_properties_kvpairs:
          $ref: '#/components/schemas/DeltaRuntimePropertiesKvPairs'
          description: Information pertaining to current state of the delta table.
        effective_predictive_optimization_flag:
          $ref: '#/components/schemas/EffectivePredictiveOptimizationFlag'
        enable_predictive_optimization:
          $ref: '#/components/schemas/EnablePredictiveOptimization'
        encryption_details:
          $ref: '#/components/schemas/EncryptionDetails'
        full_name:
          type: string
          description: >-
            Full name of table, in form of
            **catalog_name**.**schema_name**.**table_name**
        metastore_id:
          type: string
          description: Unique identifier of parent metastore.
        name:
          type: string
          description: Name of table, relative to parent schema.
        owner:
          type: string
          description: Username of current owner of table.
        pipeline_id:
          type: string
          description: >-
            The pipeline ID of the table. Applicable for tables created by
            pipelines (Materialized View, Streaming Table, etc.).
        properties:
          type: object
          description: A map of key-value properties attached to the securable.
        row_filter:
          $ref: '#/components/schemas/TableRowFilter'
        schema_name:
          type: string
          description: Name of parent schema relative to its parent catalog.
        securable_kind_manifest:
          $ref: '#/components/schemas/SecurableKindManifest'
          description: >-
            SecurableKindManifest of table, including capabilities the table
            has.
        sql_path:
          type: string
          description: >-
            List of schemes whose objects can be referenced without
            qualification.
        storage_credential_name:
          type: string
          description: >-
            Name of the storage credential, when a storage credential is
            configured for use with this table.
        storage_location:
          type: string
          description: Storage root URL for table (for **MANAGED**, **EXTERNAL** tables).
        table_constraints:
          type: array
          items:
            $ref: '#/components/schemas/TableConstraint'
          description: >-
            List of table constraints. Note: this field is not set in the output
            of the **listTables** API.
        table_id:
          type: string
          description: The unique identifier of the table.
        table_type:
          $ref: '#/components/schemas/TableType'
        updated_at:
          type: integer
          description: Time at which this table was last modified, in epoch milliseconds.
        updated_by:
          type: string
          description: Username of user who last modified the table.
        view_definition:
          type: string
          description: >-
            View definition SQL (when **table_type** is **VIEW**,
            **MATERIALIZED_VIEW**, or **STREAMING_TABLE**)
        view_dependencies:
          $ref: '#/components/schemas/DependencyList'
          description: >-
            View dependencies (when table_type == **VIEW** or
            **MATERIALIZED_VIEW**, **STREAMING_TABLE**) - when DependencyList is
            None, the dependency is not provided; - when DependencyList is an
            empty list, the dependency is provided but is empty; - when
            DependencyList is not an empty list, dependencies are provided and
            recorded. Note: this field is not set in the output of the
            **listTables** API.
    TableRowFilter:
      type: object
      properties:
        function_name:
          type: string
        input_column_names:
          type: array
          items:
            type: string
          description: >-
            The list of table columns to be passed as input to the row filter
            function. The column types should match the types of the filter
            function arguments.
        input_arguments:
          type: array
          items:
            $ref: '#/components/schemas/PolicyFunctionArgument'
          description: >-
            The list of additional table columns or literals to be passed as
            additional arguments to a row filter function. This is the
            replacement of the deprecated input_column_names field and carries
            information about the types (alias or constant) of the arguments to
            the filter function.
      required:
        - function_name
        - input_column_names
    TableSummary:
      type: object
      properties:
        full_name:
          type: string
        securable_kind_manifest:
          $ref: '#/components/schemas/SecurableKindManifest'
          description: >-
            SecurableKindManifest of table, including capabilities the table
            has.
        table_type:
          $ref: '#/components/schemas/TableType'
    TagIntrospectionExpression:
      type: object
      properties:
        column_tag_value:
          $ref: '#/components/schemas/ColumnTagValueExtraction'
          description: Extracts the value of a column-level tag.
        tag_value:
          $ref: '#/components/schemas/TagValueExtraction'
          description: Extracts the value of a securable-level tag.
      description: An expression that introspects tags at query time.
    TagKeyValue:
      type: object
      properties:
        key:
          type: string
        value:
          type: string
          description: value of the tag associated with the key, could be optional
    TagValueExtraction:
      type: object
      properties:
        tag_key:
          type: string
          description: 1024 matches the max_length on FunctionArgument.constant above.
      required:
        - tag_key
      description: 'Extracts the value of a securable-level tag: get_tag_value("tagKey").'
    TemporaryCredentials:
      type: object
      properties:
        aws_temp_credentials:
          $ref: '#/components/schemas/AwsCredentials'
        azure_aad:
          $ref: '#/components/schemas/AzureActiveDirectoryToken'
        expiration_time:
          type: integer
          description: >-
            Server time when the credential will expire, in epoch milliseconds.
            The API client is advised to cache the credential given this
            expiration time.
        gcp_oauth_token:
          $ref: '#/components/schemas/GcpOauthToken'
        r2_temp_credentials:
          $ref: '#/components/schemas/R2Credentials'
    TriggeredUpdateStatus:
      type: object
      properties:
        last_processed_commit_version:
          type: integer
          description: >-
            The last source table Delta version that was synced to the online
            table. Note that this Delta version may not be completely synced to
            the online table yet.
        timestamp:
          type: string
          description: >-
            The timestamp of the last time any data was synchronized from the
            source table to the online table.
        triggered_update_progress:
          $ref: '#/components/schemas/PipelineProgress'
          description: Progress of the active data synchronization pipeline.
      description: >-
        Detailed status of an online table. Shown if the online table is in the
        ONLINE_TRIGGERED_UPDATE
            or the ONLINE_NO_PENDING_UPDATE state.
    UcEncryptedToken:
      type: object
      properties:
        encrypted_payload:
          type: string
          description: Stores encrypted ScopedCloudToken as a base64-encoded string
      description: >-
        Encrypted token used when we cannot downscope the cloud provider token
        appropriately See:
            https://docs.google.com/document/d/1hEKDnSckuU5PIS798CtfqBElrMR6OJuR2wgz_BjhMSY
    UnassignResponse:
      type: object
      properties: {}
    UpdateAccountsMetastore:
      type: object
      properties:
        delta_sharing_organization_name:
          type: string
        delta_sharing_recipient_token_lifetime_in_seconds:
          type: integer
          description: The lifetime of delta sharing recipient token in seconds.
        delta_sharing_scope:
          $ref: '#/components/schemas/DeltaSharingScopeEnum'
          description: The scope of Delta Sharing enabled for the metastore.
        external_access_enabled:
          type: boolean
          description: >-
            Whether to allow non-DBR clients to directly access entities under
            the metastore.
        owner:
          type: string
          description: The owner of the metastore.
        privilege_model_version:
          type: string
          description: >-
            Privilege model version of the metastore, of the form
            ``major.minor`` (e.g., ``1.0``).
        storage_root_credential_id:
          type: string
          description: UUID of storage credential to access the metastore storage_root.
    UpdateAccountsStorageCredential:
      type: object
      properties:
        aws_iam_role:
          $ref: '#/components/schemas/AwsIamRoleRequest'
        azure_managed_identity:
          $ref: '#/components/schemas/AzureManagedIdentityResponse'
          description: The Azure managed identity configuration.
        azure_service_principal:
          $ref: '#/components/schemas/AzureServicePrincipal'
          description: The Azure service principal configuration.
        cloudflare_api_token:
          $ref: '#/components/schemas/CloudflareApiToken'
          description: The Cloudflare API token configuration.
        comment:
          type: string
          description: Comment associated with the credential.
        databricks_gcp_service_account:
          $ref: '#/components/schemas/DatabricksGcpServiceAccountRequest'
          description: The Databricks managed GCP service account configuration.
        isolation_mode:
          $ref: '#/components/schemas/IsolationMode'
          description: >-
            Whether the current securable is accessible from all workspaces or a
            specific set of workspaces.
        owner:
          type: string
          description: Username of current owner of credential.
        read_only:
          type: boolean
          description: >-
            Whether the credential is usable only for read operations. Only
            applicable when purpose is **STORAGE**.
    UpdateAssignmentResponse:
      type: object
      properties: {}
    UpdateCatalogWorkspaceBindingsResponse:
      type: object
      properties:
        workspaces:
          type: array
          items:
            type: integer
    UpdateMetastoreAssignment:
      type: object
      properties:
        workspace_id:
          type: integer
        default_catalog_name:
          type: string
          description: >-
            The name of the default catalog in the metastore. This field is
            deprecated. Please use "Default Namespace API" to configure the
            default catalog for a Databricks workspace.
        metastore_id:
          type: string
          description: The unique ID of the metastore.
      required:
        - workspace_id
    UpdatePermissionsResponse:
      type: object
      properties:
        privilege_assignments:
          type: array
          items:
            $ref: '#/components/schemas/PrivilegeAssignment'
    UpdateRequestExternalLineage:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/ExternalLineageObject'
        target:
          $ref: '#/components/schemas/ExternalLineageObject'
          description: Target object of the external lineage relationship.
        columns:
          type: array
          items:
            $ref: '#/components/schemas/ColumnRelationship'
          description: List of column relationships between source and target objects.
        id:
          type: string
          description: Unique identifier of the external lineage relationship.
        properties:
          type: object
          description: >-
            Key-value properties associated with the external lineage
            relationship.
      required:
        - source
        - target
    UpdateResponse:
      type: object
      properties: {}
    UpdateWorkspaceBindingsResponse:
      type: object
      properties:
        bindings:
          type: array
          items:
            $ref: '#/components/schemas/WorkspaceBinding'
          description: List of workspace bindings.
      description: A list of workspace IDs that are bound to the securable
    ValidateCredentialResponse:
      type: object
      properties:
        isDir:
          type: boolean
        results:
          type: array
          items:
            $ref: '#/components/schemas/CredentialValidationResult'
          description: The results of the validation check.
    ValidateStorageCredentialResponse:
      type: object
      properties:
        isDir:
          type: boolean
        results:
          type: array
          items:
            $ref: '#/components/schemas/ValidationResult'
          description: The results of the validation check.
    ValidationResult:
      type: object
      properties:
        message:
          type: string
        operation:
          $ref: '#/components/schemas/ValidationResultOperation'
          description: The operation tested.
        result:
          $ref: '#/components/schemas/ValidationResultResult'
          description: The results of the tested operation.
    VolumeDependency:
      type: object
      properties:
        volume_full_name:
          type: string
          description: >-
            Full name of the dependent volume, in the form of
            **catalog_name**.**schema_name**.**volume_name**.
      required:
        - volume_full_name
      description: A volume that is dependent on a SQL object.
    VolumeInfo:
      type: object
      properties:
        access_point:
          type: string
        browse_only:
          type: boolean
          description: >-
            Indicates whether the principal is limited to retrieving metadata
            for the associated object through the BROWSE privilege when
            include_browse is enabled in the request.
        catalog_name:
          type: string
          description: The name of the catalog where the schema and the volume are
        comment:
          type: string
          description: The comment attached to the volume
        created_at:
          type: integer
        created_by:
          type: string
          description: The identifier of the user who created the volume
        encryption_details:
          $ref: '#/components/schemas/EncryptionDetails'
        full_name:
          type: string
          description: The three-level (fully qualified) name of the volume
        metastore_id:
          type: string
          description: The unique identifier of the metastore
        name:
          type: string
          description: The name of the volume
        owner:
          type: string
          description: The identifier of the user who owns the volume
        schema_name:
          type: string
          description: The name of the schema where the volume is
        storage_location:
          type: string
          description: The storage location on the cloud
        updated_at:
          type: integer
        updated_by:
          type: string
          description: The identifier of the user who updated the volume last time
        volume_id:
          type: string
          description: The unique identifier of the volume
        volume_type:
          $ref: '#/components/schemas/VolumeType'
          description: >-
            The type of the volume. An external volume is located in the
            specified external location. A managed volume is located in the
            default location which is specified by the parent schema, or the
            parent catalog, or the Metastore. `Learn more
            <https://docs.databricks.com/aws/en/volumes/managed-vs-external>`__
    WorkspaceBinding:
      type: object
      properties:
        workspace_id:
          type: integer
        binding_type:
          $ref: '#/components/schemas/WorkspaceBindingBindingType'
          description: One of READ_WRITE/READ_ONLY. Default is READ_WRITE.
      required:
        - workspace_id
    AgentServiceAgentServiceType:
      type: string
      x-enum:
        - AGENT_SERVICE_TYPE_EXTERNAL
      description: >-
        Type of agent service. Determines how the backing agent surface is
        provisioned and how

        invocation routes to the runtime. Nested to scope enum values
        (guardrail:

        avoid-generic-enum-names).
    ArtifactType:
      type: string
      x-enum:
        - INIT_SCRIPT
        - LIBRARY_JAR
        - LIBRARY_MAVEN
      description: The artifact type
    CatalogIsolationMode:
      type: string
      x-enum:
        - ISOLATED
        - OPEN
        - OPEN_IN_ACCOUNT
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    CatalogType:
      type: string
      x-enum:
        - DELTASHARING_CATALOG
        - FOREIGN_CATALOG
        - INTERNAL_CATALOG
        - MANAGED_CATALOG
        - MANAGED_ONLINE_CATALOG
        - SYSTEM_CATALOG
      description: The type of the catalog.
    ColumnTypeName:
      type: string
      x-enum:
        - ARRAY
        - BINARY
        - BOOLEAN
        - BYTE
        - CHAR
        - DATE
        - DECIMAL
        - DOUBLE
        - FILE
        - FLOAT
        - GEOGRAPHY
        - GEOMETRY
        - INT
        - INTERVAL
        - LONG
        - MAP
        - 'NULL'
        - SHORT
        - STRING
        - STRUCT
        - TABLEREF_TYPE
        - TABLE_TYPE
        - TIME
        - TIMESTAMP
        - TIMESTAMP_NTZ
        - USER_DEFINED_TYPE
        - VARIANT
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    ConnectionType:
      type: string
      x-enum:
        - ADOBE_COMMERCE
        - ADP_WORKFORCE_NOW
        - AHA
        - AIRTABLE
        - AMPLITUDE
        - APPFIGURES
        - APPLE_APP_STORE
        - APPLE_SEARCH_ADS
        - ATLASSIAN_ORGANIZATION
        - AWIN
        - AZURE_MONITOR_LOGS
        - BIGLAKE
        - BIGQUERY
        - CERIDIAN_DAYFORCE
        - CONFLUENCE
        - DATABRICKS
        - DELIGHTED
        - DYNAMICS365
        - EPIC_CLARITY
        - FRESHSERVICE
        - FRONT
        - GA4_RAW_DATA
        - GENESYS
        - GITHUB
        - GITLAB
        - GLUE
        - GMAIL
        - GONG
        - GOOGLE_ANALYTICS
        - GOOGLE_CALENDAR
        - GOOGLE_CLOUD_LAKEHOUSE
        - GOOGLE_WORKSPACE
        - GURU
        - HIBOB
        - HIVE_METASTORE
        - HTTP
        - HUBSPOT
        - ICEBERG_REST
        - IRONCLAD
        - JDBC
        - KINESIS
        - LINEAR
        - MARKETO
        - META_MARKETING
        - MICROSOFT_ENTRA_ID
        - MONDAY_COM
        - MYSQL
        - NETSKOPE_LOGS
        - NOTION
        - ORACLE
        - ORACLE_ELOQUA
        - ORACLE_FUSION_CLOUD
        - OUTLOOK
        - PAGERDUTY
        - PALANTIR
        - PARTNERSTACK
        - PENDO
        - POSTGRESQL
        - POWER_BI
        - PUBSUB
        - QUICKBOOKS
        - REDSHIFT
        - SALESFORCE
        - SALESFORCE_DATA_CLOUD
        - SALESLOFT
        - SAP_SUCCESSFACTORS
        - SAS
        - SENDGRID
        - SERVICENOW
        - SHOPIFY
        - SLACK_ACCESS_AND_INTEGRATION_LOGS
        - SMARTSHEET
        - SNAPCHAT_ADS
        - SNOWFLAKE
        - SPLUNK
        - SQLDW
        - SQLSERVER
        - SQUARE
        - TERADATA
        - UNKNOWN_CONNECTION_TYPE
        - VERKADA
        - WORKDAY_RAAS
        - YOUTUBE_ANALYTICS
        - ZENDESK
        - ZIP
        - ZOHO_BOOKS
        - ZOOM
        - ZOOM_LOGS
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    ConversionInfoState:
      type: string
      x-enum:
        - COMPLETED
        - IN_PROGRESS
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    CreateFunctionParameterStyle:
      type: string
      x-enum:
        - S
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    CreateFunctionRoutineBody:
      type: string
      x-enum:
        - EXTERNAL
        - SQL
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    CreateFunctionSecurityType:
      type: string
      x-enum:
        - DEFINER
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    CreateFunctionSqlDataAccess:
      type: string
      x-enum:
        - CONTAINS_SQL
        - NO_SQL
        - READS_SQL_DATA
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    CredentialPurpose:
      type: string
      x-enum:
        - SERVICE
        - STORAGE
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    CredentialType:
      type: string
      x-enum:
        - ANY_STATIC_CREDENTIAL
        - BEARER_TOKEN
        - EDGEGRID_AKAMAI
        - GENERIC_TOKEN_EXCHANGE
        - INLINE_YAML
        - OAUTH_ACCESS_TOKEN
        - OAUTH_DCR
        - OAUTH_GOOGLE_SERVICE_ACCOUNT
        - OAUTH_M2M
        - OAUTH_MTLS
        - OAUTH_REFRESH_TOKEN
        - OAUTH_RESOURCE_OWNER_PASSWORD
        - OAUTH_U2M
        - OAUTH_U2M_MAPPING
        - OIDC_TOKEN
        - PEM_PRIVATE_KEY
        - SERVICE_CREDENTIAL
        - SSWS_TOKEN
        - UNKNOWN_CREDENTIAL_TYPE
        - USERNAME_PASSWORD
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    DataSourceFormat:
      type: string
      x-enum:
        - AVRO
        - BIGQUERY_FORMAT
        - CSV
        - DATABRICKS_FORMAT
        - DATABRICKS_ROW_STORE_FORMAT
        - DELTA
        - DELTASHARING
        - DELTA_UNIFORM_HUDI
        - DELTA_UNIFORM_ICEBERG
        - HIVE
        - ICEBERG
        - JSON
        - MONGODB_FORMAT
        - MYSQL_FORMAT
        - NETSUITE_FORMAT
        - ORACLE_FORMAT
        - ORC
        - PARQUET
        - POSTGRESQL_FORMAT
        - REDSHIFT_FORMAT
        - SALESFORCE_DATA_CLOUD_FORMAT
        - SALESFORCE_FORMAT
        - SNOWFLAKE_FORMAT
        - SQLDW_FORMAT
        - SQLSERVER_FORMAT
        - TERADATA_FORMAT
        - TEXT
        - UNITY_CATALOG
        - VECTOR_INDEX_FORMAT
        - WORKDAY_RAAS_FORMAT
      description: Data source format
    DeltaSharingScopeEnum:
      type: string
      x-enum:
        - INTERNAL
        - INTERNAL_AND_EXTERNAL
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    DestinationType:
      type: string
      x-enum:
        - EMAIL
        - GENERIC_WEBHOOK
        - MICROSOFT_TEAMS
        - SLACK
        - URL
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    DrReplicationStatus:
      type: string
      x-enum:
        - DR_REPLICATION_STATUS_PRIMARY
        - DR_REPLICATION_STATUS_SECONDARY
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    EffectivePredictiveOptimizationFlagInheritedFromType:
      type: string
      x-enum:
        - CATALOG
        - SCHEMA
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    EnablePredictiveOptimization:
      type: string
      x-enum:
        - DISABLE
        - ENABLE
        - INHERIT
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    FunctionInfoParameterStyle:
      type: string
      x-enum:
        - S
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    FunctionInfoRoutineBody:
      type: string
      x-enum:
        - EXTERNAL
        - SQL
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    FunctionInfoSecurityType:
      type: string
      x-enum:
        - DEFINER
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    FunctionInfoSqlDataAccess:
      type: string
      x-enum:
        - CONTAINS_SQL
        - NO_SQL
        - READS_SQL_DATA
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    FunctionParameterMode:
      type: string
      x-enum:
        - IN
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    FunctionParameterType:
      type: string
      x-enum:
        - COLUMN
        - PARAM
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    IsolationMode:
      type: string
      x-enum:
        - ISOLATION_MODE_ISOLATED
        - ISOLATION_MODE_OPEN
        - ISOLATION_MODE_OPEN_IN_ACCOUNT
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    LineageDirection:
      type: string
      x-enum:
        - DOWNSTREAM
        - UPSTREAM
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    ListMcpServicesRequestView:
      type: string
      x-enum:
        - BASIC
        - FULL
      description: >-
        Controls which fields are populated on each McpService in the response.
        The server treats unset

        / VIEW_UNSPECIFIED as BASIC. Callers needing the full configuration must
        request it explicitly

        with ``view = FULL``.
    ListModelProviderServicesRequestView:
      type: string
      x-enum:
        - BASIC
        - FULL
      description: >-
        Controls which fields are populated on each ModelProviderService in the
        response. The server

        treats unset / VIEW_UNSPECIFIED as BASIC. Callers needing the full
        configuration must request it

        explicitly with ``view = FULL``.
    ListModelServicesRequestView:
      type: string
      x-enum:
        - BASIC
        - FULL
      description: >-
        Controls which fields are populated on each ModelService in the
        response. The server treats

        unset / VIEW_UNSPECIFIED as BASIC. Callers needing the full
        configuration must request it

        explicitly with ``view = FULL``.
    MatchType:
      type: string
      x-enum:
        - PREFIX_MATCH
      description: The artifact pattern matching type
    ModelProviderServiceConfigAnthropicProviderRelayedConfigAnthropicRelayedPlanType:
      type: string
      x-enum:
        - ANTHROPIC_RELAYED_PLAN_TYPE_MAX
        - ANTHROPIC_RELAYED_PLAN_TYPE_TEAM_ENTERPRISE
      description: >-
        Which Anthropic subscription tier the relayed OAuth token belongs to.
        Immutable after Create

        (switching tiers changes which governance controls the platform
        enforces). Only MAX and

        TEAM_ENTERPRISE differ in the governance surface the gateway can
        enforce, not in how the token

        is relayed.
    ModelProviderServiceConfigExternalModelProviderType:
      type: string
      x-enum:
        - EXTERNAL_MODEL_PROVIDER_TYPE_AMAZON_BEDROCK
        - EXTERNAL_MODEL_PROVIDER_TYPE_ANTHROPIC
        - EXTERNAL_MODEL_PROVIDER_TYPE_AZURE_OPENAI
        - EXTERNAL_MODEL_PROVIDER_TYPE_CUSTOM
        - EXTERNAL_MODEL_PROVIDER_TYPE_GEMINI_ENTERPRISE
        - EXTERNAL_MODEL_PROVIDER_TYPE_MICROSOFT_FOUNDRY
        - EXTERNAL_MODEL_PROVIDER_TYPE_OPENAI
      description: External LLM provider for an EXTERNAL_FOUNDATION_MODEL destination.
    ModelServiceConfigDestinationConfigDestinationType:
      type: string
      x-enum:
        - DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL
        - DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL
        - DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
      description: Backing-model category for a model service destination.
    ModelVersionInfoStatus:
      type: string
      x-enum:
        - FAILED_REGISTRATION
        - MODEL_VERSION_STATUS_UNKNOWN
        - PENDING_REGISTRATION
        - READY
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    MonitorCronSchedulePauseStatus:
      type: string
      x-enum:
        - PAUSED
        - UNPAUSED
        - UNSPECIFIED
      description: >-
        Source link:

        https://src.dev.databricks.com/databricks/universe/-/blob/elastic-spark-common/api/messages/schedule.proto

        Monitoring workflow schedule pause status.
    MonitorInferenceLogProblemType:
      type: string
      x-enum:
        - PROBLEM_TYPE_CLASSIFICATION
        - PROBLEM_TYPE_REGRESSION
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    MonitorInfoStatus:
      type: string
      x-enum:
        - MONITOR_STATUS_ACTIVE
        - MONITOR_STATUS_DELETE_PENDING
        - MONITOR_STATUS_ERROR
        - MONITOR_STATUS_FAILED
        - MONITOR_STATUS_PENDING
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    MonitorMetricType:
      type: string
      x-enum:
        - CUSTOM_METRIC_TYPE_AGGREGATE
        - CUSTOM_METRIC_TYPE_DERIVED
        - CUSTOM_METRIC_TYPE_DRIFT
      description: >-
        Can only be one of ``"CUSTOM_METRIC_TYPE_AGGREGATE"``,
        ``"CUSTOM_METRIC_TYPE_DERIVED"``, or

        ``"CUSTOM_METRIC_TYPE_DRIFT"``. The ``"CUSTOM_METRIC_TYPE_AGGREGATE"``
        and

        ``"CUSTOM_METRIC_TYPE_DERIVED"`` metrics are computed on a single table,
        whereas the

        ``"CUSTOM_METRIC_TYPE_DRIFT"`` compare metrics across baseline and input
        table, or across the

        two consecutive time windows.


        - CUSTOM_METRIC_TYPE_AGGREGATE: only depend on the existing columns in
        your table

        - CUSTOM_METRIC_TYPE_DERIVED: depend on previously computed aggregate
        metrics

        - CUSTOM_METRIC_TYPE_DRIFT: depend on previously computed aggregate or
        derived metrics
    MonitorRefreshInfoState:
      type: string
      x-enum:
        - CANCELED
        - FAILED
        - PENDING
        - RUNNING
        - SUCCESS
        - UNKNOWN
      description: The current state of the refresh.
    MonitorRefreshInfoTrigger:
      type: string
      x-enum:
        - MANUAL
        - SCHEDULE
        - UNKNOWN_TRIGGER
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    OnlineTableState:
      type: string
      x-enum:
        - OFFLINE
        - OFFLINE_FAILED
        - ONLINE
        - ONLINE_CONTINUOUS_UPDATE
        - ONLINE_NO_PENDING_UPDATE
        - ONLINE_PIPELINE_FAILED
        - ONLINE_TRIGGERED_UPDATE
        - ONLINE_UPDATING_PIPELINE_RESOURCES
        - PROVISIONING
        - PROVISIONING_INITIAL_SNAPSHOT
        - PROVISIONING_PIPELINE_RESOURCES
      description: The state of an online table.
    OptionSpecOauthStage:
      type: string
      x-enum:
        - BEFORE_ACCESS_TOKEN
        - BEFORE_AUTHORIZATION_CODE
      description: >-
        During the OAuth flow, specifies which stage the option should be
        displayed in the UI.

        OAUTH_STAGE_UNSPECIFIED is the default value for options unrelated to
        the OAuth flow.

        BEFORE_AUTHORIZATION_CODE corresponds to options necessary to initiate
        the OAuth process.

        BEFORE_ACCESS_TOKEN corresponds to options that are necessary to create
        a foreign connection,

        but that should be displayed after the authorization code has already
        been received.
    OptionSpecOptionType:
      type: string
      x-enum:
        - OPTION_BIGINT
        - OPTION_BOOLEAN
        - OPTION_ENUM
        - OPTION_MULTILINE_STRING
        - OPTION_NUMBER
        - OPTION_SERVICE_CREDENTIAL
        - OPTION_STORAGE_CREDENTIAL
        - OPTION_STRING
      description: >-
        Type of the option, we purposely follow JavaScript types so that the UI
        can map the options to

        JS types. https://www.w3schools.com/js/js_datatypes.asp Enum is a
        special case that it's just

        string with selections.
    PathOperation:
      type: string
      x-enum:
        - PATH_CREATE_TABLE
        - PATH_READ
        - PATH_READ_WRITE
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    PolicyType:
      type: string
      x-enum:
        - POLICY_TYPE_COLUMN_MASK
        - POLICY_TYPE_DENY
        - POLICY_TYPE_GRANT
        - POLICY_TYPE_ROW_FILTER
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    PrincipalType:
      type: string
      x-enum:
        - GROUP_PRINCIPAL
        - SERVICE_PRINCIPAL
        - USER_PRINCIPAL
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    Privilege:
      type: string
      x-enum:
        - ACCESS
        - ALL_PRIVILEGES
        - APPLY_TAG
        - BROWSE
        - CREATE
        - CREATE_CASE_COLLECTION
        - CREATE_CATALOG
        - CREATE_CLEAN_ROOM
        - CREATE_CONNECTION
        - CREATE_DATASOURCE
        - CREATE_EXTERNAL_LOCATION
        - CREATE_EXTERNAL_TABLE
        - CREATE_EXTERNAL_VOLUME
        - CREATE_FEATURE
        - CREATE_FLOW
        - CREATE_FOREIGN_CATALOG
        - CREATE_FOREIGN_SECURABLE
        - CREATE_FUNCTION
        - CREATE_MANAGED_STORAGE
        - CREATE_MATERIALIZED_VIEW
        - CREATE_MEMORY_STORE
        - CREATE_MODEL
        - CREATE_PROVIDER
        - CREATE_RECIPIENT
        - CREATE_RULE
        - CREATE_SCHEMA
        - CREATE_SEMANTIC_GRAPH
        - CREATE_SERVICE
        - CREATE_SERVICE_CREDENTIAL
        - CREATE_SHARE
        - CREATE_SKILL
        - CREATE_STORAGE_CREDENTIAL
        - CREATE_STREAM
        - CREATE_TABLE
        - CREATE_VIEW
        - CREATE_VOLUME
        - DELETE
        - DELETE_EVENTS
        - DELETE_SECURITY_DATA
        - EXECUTE
        - EXECUTE_CLEAN_ROOM_TASK
        - EXTERNAL_USE_SCHEMA
        - INSERT
        - INSERT_SECURITY_DATA
        - MANAGE
        - MANAGE_ACCESS
        - MANAGE_ACCESS_CONTROL
        - MANAGE_ALLOWLIST
        - MANAGE_GRANTS
        - MODIFY
        - MODIFY_CLEAN_ROOM
        - READ_EVENTS
        - READ_FEATURE
        - READ_FILES
        - READ_FLOW
        - READ_MEMORY_STORE
        - READ_METADATA
        - READ_PRIVATE_FILES
        - READ_SECURITY_DATA
        - READ_SEMANTIC_GRAPH
        - READ_SKILL
        - READ_STREAM
        - READ_VOLUME
        - REFRESH
        - SELECT
        - SET_SHARE_PERMISSION
        - UPDATE
        - UPDATE_EVENTS
        - UPDATE_SECURITY_DATA
        - USAGE
        - USE_CATALOG
        - USE_CONNECTION
        - USE_MARKETPLACE_ASSETS
        - USE_PROVIDER
        - USE_RECIPIENT
        - USE_SCHEMA
        - USE_SHARE
        - USE_VOLUME
        - VIEW_ADMIN_METADATA
        - VIEW_METADATA
        - VIEW_OBJECT
        - WRITE_FILES
        - WRITE_FLOW
        - WRITE_MEMORY_STORE
        - WRITE_PRIVATE_FILES
        - WRITE_SEMANTIC_GRAPH
        - WRITE_SKILL
        - WRITE_VOLUME
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    ProvisioningInfoState:
      type: string
      x-enum:
        - ACTIVE
        - DEGRADED
        - DELETING
        - FAILED
        - PROVISIONING
        - UPDATING
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    RateLimitRateLimitKey:
      type: string
      x-enum:
        - RATE_LIMIT_KEY_REQUEST_TAG
        - RATE_LIMIT_KEY_SERVICE
        - RATE_LIMIT_KEY_SERVICE_PRINCIPAL
        - RATE_LIMIT_KEY_USER
        - RATE_LIMIT_KEY_USER_DEFAULT
        - RATE_LIMIT_KEY_USER_GROUP
      description: Scope key for a rate limit.
    RateLimitRateLimitRenewalPeriod:
      type: string
      x-enum:
        - RATE_LIMIT_RENEWAL_PERIOD_HOUR
        - RATE_LIMIT_RENEWAL_PERIOD_MINUTE
      description: Renewal period for a rate limit.
    SchemaFilterType:
      type: string
      x-enum:
        - SCHEMA_FILTER_TYPE_EXCLUDE
        - SCHEMA_FILTER_TYPE_INCLUDE
        - SCHEMA_FILTER_TYPE_INCLUDE_ALL
      description: Determines how a FOREIGN catalog's schema filter items are interpreted.
    SecurableKind:
      type: string
      x-enum:
        - CATALOG_FOREIGN_BIGLAKE
        - CATALOG_FOREIGN_GOOGLE_CLOUD_LAKEHOUSE
        - CATALOG_FOREIGN_ICEBERG_REST
        - CONNECTION_BIGLAKE_SERVICE_ACCOUNT
        - CONNECTION_COMMUNITY_OAUTH_M2M
        - CONNECTION_COMMUNITY_OAUTH_U2M
        - CONNECTION_COMMUNITY_OAUTH_U2M_MAPPING
        - CONNECTION_GOOGLE_CLOUD_LAKEHOUSE_OAUTH_U2M
        - CONNECTION_GOOGLE_CLOUD_LAKEHOUSE_SERVICE_ACCOUNT
        - CONNECTION_ICEBERG_REST_BEARER_TOKEN
        - CONNECTION_ICEBERG_REST_OAUTH_M2M
        - RECIPIENT_EMAIL
        - RECIPIENT_EMAIL_DATABRICKS
        - RECIPIENT_EMAIL_TOKEN
        - SCHEMA_FOREIGN_BIGLAKE
        - SCHEMA_FOREIGN_GOOGLE_CLOUD_LAKEHOUSE
        - SCHEMA_FOREIGN_ICEBERG_REST
        - TABLE_DB_STORAGE
        - TABLE_DELTA
        - TABLE_DELTASHARING
        - TABLE_DELTASHARING_MUTABLE
        - TABLE_DELTASHARING_OPEN_DIR_BASED
        - TABLE_DELTA_EXTERNAL
        - TABLE_DELTA_ICEBERG_DELTASHARING
        - TABLE_DELTA_ICEBERG_MANAGED
        - TABLE_DELTA_UNIFORM_HUDI_EXTERNAL
        - TABLE_DELTA_UNIFORM_ICEBERG_EXTERNAL
        - TABLE_DELTA_UNIFORM_ICEBERG_EXTERNAL_DELTASHARING
        - TABLE_DELTA_UNIFORM_ICEBERG_FOREIGN_DELTASHARING
        - TABLE_DELTA_UNIFORM_ICEBERG_FOREIGN_HIVE_METASTORE_EXTERNAL
        - TABLE_DELTA_UNIFORM_ICEBERG_FOREIGN_HIVE_METASTORE_MANAGED
        - TABLE_DELTA_UNIFORM_ICEBERG_FOREIGN_SNOWFLAKE
        - TABLE_EXTERNAL
        - TABLE_FEATURE_STORE
        - TABLE_FEATURE_STORE_EXTERNAL
        - TABLE_FOREIGN_BIGLAKE
        - TABLE_FOREIGN_BIGQUERY
        - TABLE_FOREIGN_DATABRICKS
        - TABLE_FOREIGN_DELTASHARING
        - TABLE_FOREIGN_DELTA_DELTASHARING
        - TABLE_FOREIGN_GOOGLE_CLOUD_LAKEHOUSE
        - TABLE_FOREIGN_HIVE_METASTORE
        - TABLE_FOREIGN_HIVE_METASTORE_DBFS_EXTERNAL
        - TABLE_FOREIGN_HIVE_METASTORE_DBFS_MANAGED
        - TABLE_FOREIGN_HIVE_METASTORE_DBFS_SHALLOW_CLONE_EXTERNAL
        - TABLE_FOREIGN_HIVE_METASTORE_DBFS_SHALLOW_CLONE_MANAGED
        - TABLE_FOREIGN_HIVE_METASTORE_DBFS_VIEW
        - TABLE_FOREIGN_HIVE_METASTORE_EXTERNAL
        - TABLE_FOREIGN_HIVE_METASTORE_MANAGED
        - TABLE_FOREIGN_HIVE_METASTORE_SHALLOW_CLONE_EXTERNAL
        - TABLE_FOREIGN_HIVE_METASTORE_SHALLOW_CLONE_MANAGED
        - TABLE_FOREIGN_HIVE_METASTORE_VIEW
        - TABLE_FOREIGN_ICEBERG_REST
        - TABLE_FOREIGN_MONGODB
        - TABLE_FOREIGN_MYSQL
        - TABLE_FOREIGN_NETSUITE
        - TABLE_FOREIGN_ORACLE
        - TABLE_FOREIGN_PALANTIR
        - TABLE_FOREIGN_POSTGRESQL
        - TABLE_FOREIGN_REDSHIFT
        - TABLE_FOREIGN_SALESFORCE
        - TABLE_FOREIGN_SALESFORCE_DATA_CLOUD
        - TABLE_FOREIGN_SALESFORCE_DATA_CLOUD_FILE_SHARING
        - TABLE_FOREIGN_SALESFORCE_DATA_CLOUD_FILE_SHARING_VIEW
        - TABLE_FOREIGN_SNOWFLAKE
        - TABLE_FOREIGN_SQLDW
        - TABLE_FOREIGN_SQLSERVER
        - TABLE_FOREIGN_TERADATA
        - TABLE_FOREIGN_WORKDAY_RAAS
        - TABLE_ICEBERG_UNIFORM_MANAGED
        - TABLE_INTERNAL
        - TABLE_MANAGED_POSTGRESQL
        - TABLE_MATERIALIZED_VIEW
        - TABLE_MATERIALIZED_VIEW_DELTASHARING
        - TABLE_METRIC_VIEW
        - TABLE_METRIC_VIEW_DELTASHARING
        - TABLE_ONLINE_VECTOR_INDEX_DIRECT
        - TABLE_ONLINE_VECTOR_INDEX_REPLICA
        - TABLE_ONLINE_VIEW
        - TABLE_STANDARD
        - TABLE_STREAMING_LIVE_TABLE
        - TABLE_STREAMING_LIVE_TABLE_DELTASHARING
        - TABLE_SYSTEM
        - TABLE_SYSTEM_DELTASHARING
        - TABLE_VIEW
        - TABLE_VIEW_DELTASHARING
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    SecurableType:
      type: string
      x-enum:
        - CATALOG
        - CLEAN_ROOM
        - CONNECTION
        - CREDENTIAL
        - EXTERNAL_LOCATION
        - EXTERNAL_METADATA
        - FUNCTION
        - METASTORE
        - PIPELINE
        - PROVIDER
        - RECIPIENT
        - SCHEMA
        - SHARE
        - STAGING_TABLE
        - STORAGE_CREDENTIAL
        - TABLE
        - VOLUME
      description: The type of Unity Catalog securable.
    SpecialDestination:
      type: string
      x-enum:
        - SPECIAL_DESTINATION_CATALOG_OWNER
        - SPECIAL_DESTINATION_CONNECTION_OWNER
        - SPECIAL_DESTINATION_CREDENTIAL_OWNER
        - SPECIAL_DESTINATION_EXTERNAL_LOCATION_OWNER
        - SPECIAL_DESTINATION_FUNCTION_OWNER
        - SPECIAL_DESTINATION_METASTORE_OWNER
        - SPECIAL_DESTINATION_REGISTERED_MODEL_OWNER
        - SPECIAL_DESTINATION_SCHEMA_OWNER
        - SPECIAL_DESTINATION_TABLE_OWNER
        - SPECIAL_DESTINATION_VOLUME_OWNER
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    SseEncryptionDetailsAlgorithm:
      type: string
      x-enum:
        - AWS_SSE_KMS
        - AWS_SSE_S3
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    SystemType:
      type: string
      x-enum:
        - AMAZON_REDSHIFT
        - AZURE_SYNAPSE
        - CONFLUENT
        - DATABRICKS
        - GOOGLE_BIGQUERY
        - KAFKA
        - LOOKER
        - MICROSOFT_FABRIC
        - MICROSOFT_SQL_SERVER
        - MONGODB
        - MYSQL
        - ORACLE
        - OTHER
        - POSTGRESQL
        - POWER_BI
        - SALESFORCE
        - SAP
        - SERVICENOW
        - SNOWFLAKE
        - STREAM_NATIVE
        - TABLEAU
        - TERADATA
        - WORKDAY
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    TableOperation:
      type: string
      x-enum:
        - READ
        - READ_WRITE
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    TableType:
      type: string
      x-enum:
        - EXTERNAL
        - EXTERNAL_SHALLOW_CLONE
        - FOREIGN
        - MANAGED
        - MANAGED_SHALLOW_CLONE
        - MATERIALIZED_VIEW
        - METRIC_VIEW
        - STREAMING_TABLE
        - VIEW
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    TagAssignmentSourceType:
      type: string
      x-enum:
        - TAG_ASSIGNMENT_SOURCE_TYPE_SYSTEM_DATA_CLASSIFICATION
      description: Enum representing the source type of a tag assignment
    ValidateCredentialResult:
      type: string
      x-enum:
        - FAIL
        - PASS
        - SKIP
      description: A enum represents the result of the file operation
    ValidationResultOperation:
      type: string
      x-enum:
        - DELETE
        - LIST
        - PATH_EXISTS
        - READ
        - WRITE
      description: >-
        A enum represents the file operation performed on the external location
        with the storage

        credential
    ValidationResultResult:
      type: string
      x-enum:
        - FAIL
        - PASS
        - SKIP
      description: A enum represents the result of the file operation
    VolumeOperation:
      type: string
      x-enum:
        - READ_VOLUME
        - WRITE_VOLUME
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    VolumeType:
      type: string
      x-enum:
        - EXTERNAL
        - MANAGED
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    WorkspaceBindingBindingType:
      type: string
      x-enum:
        - BINDING_TYPE_READ_ONLY
        - BINDING_TYPE_READ_WRITE
      description: >-
        Using ``BINDING_TYPE_`` prefix here to avoid conflict with
        ``TableOperation`` enum in

        ``credentials_common.proto``.
  x-stackQL-resources:
    ai_gateway_agent_services:
      id: databricks_workspace.catalog.ai_gateway_agent_services
      name: ai_gateway_agent_services
      title: Ai Gateway Agent Services
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1agent-services/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1agent-services/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.agent_services
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_agent_services/methods/list
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_agent_services/methods/create
        update: []
        delete: []
        replace: []
    ai_gateway_mcp_services:
      id: databricks_workspace.catalog.ai_gateway_mcp_services
      name: ai_gateway_mcp_services
      title: Ai Gateway Mcp Services
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1mcp-services/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1mcp-services/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.mcp_services
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_mcp_services/methods/list
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_mcp_services/methods/create
        update: []
        delete: []
        replace: []
    ai_gateway_model_provider_services:
      id: databricks_workspace.catalog.ai_gateway_model_provider_services
      name: ai_gateway_model_provider_services
      title: Ai Gateway Model Provider Services
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1model-provider-services/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1model-provider-services/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.model_provider_services
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_model_provider_services/methods/list
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_model_provider_services/methods/create
        update: []
        delete: []
        replace: []
    ai_gateway_model_services:
      id: databricks_workspace.catalog.ai_gateway_model_services
      name: ai_gateway_model_services
      title: Ai Gateway Model Services
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1model-services/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1model-services/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.model_services
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1{name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1{name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_model_services/methods/get
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_model_services/methods/list
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_model_services/methods/create
        update:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_model_services/methods/update
        delete:
          - $ref: >-
              #/components/x-stackQL-resources/ai_gateway_model_services/methods/delete
        replace: []
    artifact_allowlists:
      id: databricks_workspace.catalog.artifact_allowlists
      name: artifact_allowlists
      title: Artifact Allowlists
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1artifact-allowlists~1{artifact_type.value}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        replace:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1artifact-allowlists~1{artifact_type.value}/put
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/artifact_allowlists/methods/get'
        insert: []
        update: []
        delete: []
        replace:
          - $ref: >-
              #/components/x-stackQL-resources/artifact_allowlists/methods/replace
    catalogs:
      id: databricks_workspace.catalog.catalogs
      name: catalogs
      title: Catalogs
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1catalogs/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1catalogs/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.catalogs
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1catalogs~1{name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1catalogs~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1catalogs~1{name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/catalogs/methods/get'
          - $ref: '#/components/x-stackQL-resources/catalogs/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/catalogs/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/catalogs/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/catalogs/methods/delete'
        replace: []
    connections:
      id: databricks_workspace.catalog.connections
      name: connections
      title: Connections
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1connections/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1connections/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.connections
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1connections~1{name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1connections~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1connections~1{name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/connections/methods/get'
          - $ref: '#/components/x-stackQL-resources/connections/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/connections/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/connections/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/connections/methods/delete'
        replace: []
    entity_tag_assignments:
      id: databricks_workspace.catalog.entity_tag_assignments
      name: entity_tag_assignments
      title: Entity Tag Assignments
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1entity-tag-assignments/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1entity-tag-assignments~1{entity_type}~1{entity_name}~1tags~1{tag_key}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1entity-tag-assignments~1{entity_type}~1{entity_name}~1tags~1{tag_key}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1entity-tag-assignments~1{entity_type}~1{entity_name}~1tags~1{tag_key}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1entity-tag-assignments~1{entity_type}~1{entity_name}~1tags/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.tag_assignments
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/entity_tag_assignments/methods/get
          - $ref: >-
              #/components/x-stackQL-resources/entity_tag_assignments/methods/list
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/entity_tag_assignments/methods/create
        update:
          - $ref: >-
              #/components/x-stackQL-resources/entity_tag_assignments/methods/update
        delete:
          - $ref: >-
              #/components/x-stackQL-resources/entity_tag_assignments/methods/delete
        replace: []
    external_lineage:
      id: databricks_workspace.catalog.external_lineage
      name: external_lineage
      title: External Lineage
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.0~1lineage-tracking~1external-lineage/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: '#/paths/~1api~12.0~1lineage-tracking~1external-lineage/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.0~1lineage-tracking~1external-lineage/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.external_lineage_relationships
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.0~1lineage-tracking~1external-lineage/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/external_lineage/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/external_lineage/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/external_lineage/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/external_lineage/methods/delete'
        replace: []
    external_locations:
      id: databricks_workspace.catalog.external_locations
      name: external_locations
      title: External Locations
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1external-locations/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1external-locations/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.external_locations
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1external-locations~1{name}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1external-locations~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1external-locations~1{name}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/external_locations/methods/get'
          - $ref: '#/components/x-stackQL-resources/external_locations/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/external_locations/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/external_locations/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/external_locations/methods/delete'
        replace: []
    external_metadata:
      id: databricks_workspace.catalog.external_metadata
      name: external_metadata
      title: External Metadata
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.0~1lineage-tracking~1external-metadata/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.0~1lineage-tracking~1external-metadata/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.external_metadata
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1lineage-tracking~1external-metadata~1{name}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1lineage-tracking~1external-metadata~1{name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.0~1lineage-tracking~1external-metadata~1{name}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/external_metadata/methods/get'
          - $ref: '#/components/x-stackQL-resources/external_metadata/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/external_metadata/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/external_metadata/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/external_metadata/methods/delete'
        replace: []
    functions:
      id: databricks_workspace.catalog.functions
      name: functions
      title: Functions
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1functions/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1functions/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.functions
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1functions~1{name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1functions~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1functions~1{name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/functions/methods/get'
          - $ref: '#/components/x-stackQL-resources/functions/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/functions/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/functions/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/functions/methods/delete'
        replace: []
    grants:
      id: databricks_workspace.catalog.grants
      name: grants
      title: Grants
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1permissions~1{securable_type}~1{full_name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1permissions~1{securable_type}~1{full_name}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/grants/methods/get'
        insert: []
        update:
          - $ref: '#/components/x-stackQL-resources/grants/methods/update'
        delete: []
        replace: []
    effective_grants:
      id: databricks_workspace.catalog.effective_grants
      name: effective_grants
      title: Effective Grants
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1effective-permissions~1{securable_type}~1{full_name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/effective_grants/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    privilege_assignments:
      id: databricks_workspace.catalog.privilege_assignments
      name: privilege_assignments
      title: Privilege Assignments
      methods:
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1privilege-assignments~1{securable_type}~1{full_name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.privilege_assignments
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/privilege_assignments/methods/list
        insert: []
        update: []
        delete: []
        replace: []
    effective_privilege_assignments:
      id: databricks_workspace.catalog.effective_privilege_assignments
      name: effective_privilege_assignments
      title: Effective Privilege Assignments
      methods:
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1effective-privilege-assignments~1{securable_type}~1{full_name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.privilege_assignments
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/effective_privilege_assignments/methods/list
        insert: []
        update: []
        delete: []
        replace: []
    metastores:
      id: databricks_workspace.catalog.metastores
      name: metastores
      title: Metastores
      methods:
        assign:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1workspaces~1{workspace_id}~1metastore/put
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        unassign:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1workspaces~1{workspace_id}~1metastore/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update_assignment:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1workspaces~1{workspace_id}~1metastore/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1metastores/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1metastores/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.metastores
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1metastores~1{id}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1metastores~1{id}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1metastores~1{id}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/metastores/methods/get'
          - $ref: '#/components/x-stackQL-resources/metastores/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/metastores/methods/create'
        update:
          - $ref: >-
              #/components/x-stackQL-resources/metastores/methods/update_assignment
          - $ref: '#/components/x-stackQL-resources/metastores/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/metastores/methods/unassign'
          - $ref: '#/components/x-stackQL-resources/metastores/methods/delete'
        replace:
          - $ref: '#/components/x-stackQL-resources/metastores/methods/assign'
    current_metastore_assignment:
      id: databricks_workspace.catalog.current_metastore_assignment
      name: current_metastore_assignment
      title: Current Metastore Assignment
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1current-metastore-assignment/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/current_metastore_assignment/methods/get
        insert: []
        update: []
        delete: []
        replace: []
    metastore_summary:
      id: databricks_workspace.catalog.metastore_summary
      name: metastore_summary
      title: Metastore Summary
      methods:
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1metastore_summary/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/metastore_summary/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    model_versions:
      id: databricks_workspace.catalog.model_versions
      name: model_versions
      title: Model Versions
      methods:
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1models~1{full_name}~1versions~1{version}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1models~1{full_name}~1versions~1{version}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1models~1{full_name}~1versions~1{version}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get_by_alias:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1models~1{full_name}~1aliases~1{alias}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1models~1{full_name}~1versions/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.model_versions
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/model_versions/methods/get'
          - $ref: >-
              #/components/x-stackQL-resources/model_versions/methods/get_by_alias
          - $ref: '#/components/x-stackQL-resources/model_versions/methods/list'
        insert: []
        update:
          - $ref: '#/components/x-stackQL-resources/model_versions/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/model_versions/methods/delete'
        replace: []
    registered_models:
      id: databricks_workspace.catalog.registered_models
      name: registered_models
      title: Registered Models
      methods:
        delete_alias:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1models~1{full_name}~1aliases~1{alias}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        set_alias:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1models~1{full_name}~1aliases~1{alias}/put
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1models/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1models/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.registered_models
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1models~1{full_name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1models~1{full_name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1models~1{full_name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/registered_models/methods/get'
          - $ref: '#/components/x-stackQL-resources/registered_models/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/registered_models/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/registered_models/methods/update'
        delete:
          - $ref: >-
              #/components/x-stackQL-resources/registered_models/methods/delete_alias
          - $ref: '#/components/x-stackQL-resources/registered_models/methods/delete'
        replace:
          - $ref: >-
              #/components/x-stackQL-resources/registered_models/methods/set_alias
    online_tables:
      id: databricks_workspace.catalog.online_tables
      name: online_tables
      title: Online Tables
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.0~1online-tables/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: '#/paths/~1api~12.0~1online-tables~1{name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.0~1online-tables~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/online_tables/methods/get'
        insert:
          - $ref: '#/components/x-stackQL-resources/online_tables/methods/create'
        update: []
        delete:
          - $ref: '#/components/x-stackQL-resources/online_tables/methods/delete'
        replace: []
    policies:
      id: databricks_workspace.catalog.policies
      name: policies
      title: Policies
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1policies/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1policies~1{on_securable_type}~1{on_securable_fullname}~1{name}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1policies~1{on_securable_type}~1{on_securable_fullname}~1{name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1policies~1{on_securable_type}~1{on_securable_fullname}~1{name}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1policies~1{on_securable_type}~1{on_securable_fullname}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.policies
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/policies/methods/get'
          - $ref: '#/components/x-stackQL-resources/policies/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/policies/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/policies/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/policies/methods/delete'
        replace: []
    quality_monitors:
      id: databricks_workspace.catalog.quality_monitors
      name: quality_monitors
      title: Quality Monitors
      methods:
        cancel_refresh:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor~1refreshes~1{refresh_id}~1cancel/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor/put
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        regenerate_dashboard:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1quality-monitoring~1tables~1{table_name}~1monitor~1dashboard/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/quality_monitors/methods/get'
        insert:
          - $ref: '#/components/x-stackQL-resources/quality_monitors/methods/create'
        update: []
        delete:
          - $ref: '#/components/x-stackQL-resources/quality_monitors/methods/delete'
        replace:
          - $ref: '#/components/x-stackQL-resources/quality_monitors/methods/update'
    quality_monitor_refreshes:
      id: databricks_workspace.catalog.quality_monitor_refreshes
      name: quality_monitor_refreshes
      title: Quality Monitor Refreshes
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor~1refreshes~1{refresh_id}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor~1refreshes/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        run:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1tables~1{table_name}~1monitor~1refreshes/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/quality_monitor_refreshes/methods/get
          - $ref: >-
              #/components/x-stackQL-resources/quality_monitor_refreshes/methods/list
        insert: []
        update: []
        delete: []
        replace: []
    resource_quotas:
      id: databricks_workspace.catalog.resource_quotas
      name: resource_quotas
      title: Resource Quotas
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1resource-quotas~1{parent_securable_type}~1{parent_full_name}~1{quota_name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1resource-quotas~1all-resource-quotas/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.quotas
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/resource_quotas/methods/get'
          - $ref: '#/components/x-stackQL-resources/resource_quotas/methods/list'
        insert: []
        update: []
        delete: []
        replace: []
    rfa:
      id: databricks_workspace.catalog.rfa
      name: rfa
      title: Rfa
      methods:
        batch_create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~13.0~1rfa~1requests/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get_destinations:
          operation:
            $ref: >-
              #/paths/~1api~13.0~1rfa~1destinations~1{securable_type}~1{full_name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update_destinations:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~13.0~1rfa~1destinations/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/rfa/methods/get_destinations'
        insert:
          - $ref: '#/components/x-stackQL-resources/rfa/methods/batch_create'
        update:
          - $ref: '#/components/x-stackQL-resources/rfa/methods/update_destinations'
        delete: []
        replace: []
    schemas:
      id: databricks_workspace.catalog.schemas
      name: schemas
      title: Schemas
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1schemas/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1schemas/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.schemas
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1schemas~1{full_name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1schemas~1{full_name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1schemas~1{full_name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/schemas/methods/get'
          - $ref: '#/components/x-stackQL-resources/schemas/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/schemas/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/schemas/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/schemas/methods/delete'
        replace: []
    secrets_uc:
      id: databricks_workspace.catalog.secrets_uc
      name: secrets_uc
      title: Secrets Uc
      methods:
        secrets_uc_create_secret:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1secrets/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        secrets_uc_list_secrets:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1secrets/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.secrets
        secrets_uc_delete_secret:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1secrets~1{full_name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        secrets_uc_get_secret:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1secrets~1{full_name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        secrets_uc_update_secret:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1secrets~1{full_name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/secrets_uc/methods/secrets_uc_get_secret
          - $ref: >-
              #/components/x-stackQL-resources/secrets_uc/methods/secrets_uc_list_secrets
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/secrets_uc/methods/secrets_uc_create_secret
        update:
          - $ref: >-
              #/components/x-stackQL-resources/secrets_uc/methods/secrets_uc_update_secret
        delete:
          - $ref: >-
              #/components/x-stackQL-resources/secrets_uc/methods/secrets_uc_delete_secret
        replace: []
    storage_credentials:
      id: databricks_workspace.catalog.storage_credentials
      name: storage_credentials
      title: Storage Credentials
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1storage-credentials/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1storage-credentials/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.storage_credentials
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1storage-credentials~1{name}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1storage-credentials~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1storage-credentials~1{name}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        validate:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1validate-storage-credentials/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/storage_credentials/methods/get'
          - $ref: '#/components/x-stackQL-resources/storage_credentials/methods/list'
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/storage_credentials/methods/create
        update:
          - $ref: >-
              #/components/x-stackQL-resources/storage_credentials/methods/update
        delete:
          - $ref: >-
              #/components/x-stackQL-resources/storage_credentials/methods/delete
        replace: []
    system_schemas:
      id: databricks_workspace.catalog.system_schemas
      name: system_schemas
      title: System Schemas
      methods:
        disable:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1metastores~1{metastore_id}~1systemschemas~1{schema_name}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        enable:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1metastores~1{metastore_id}~1systemschemas~1{schema_name}/put
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1metastores~1{metastore_id}~1systemschemas/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.schemas
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/system_schemas/methods/list'
        insert: []
        update: []
        delete:
          - $ref: '#/components/x-stackQL-resources/system_schemas/methods/disable'
        replace:
          - $ref: '#/components/x-stackQL-resources/system_schemas/methods/enable'
    table_constraints:
      id: databricks_workspace.catalog.table_constraints
      name: table_constraints
      title: Table Constraints
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1constraints/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1constraints~1{full_name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select: []
        insert:
          - $ref: '#/components/x-stackQL-resources/table_constraints/methods/create'
        update: []
        delete:
          - $ref: '#/components/x-stackQL-resources/table_constraints/methods/delete'
        replace: []
    tables:
      id: databricks_workspace.catalog.tables
      name: tables
      title: Tables
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1tables/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1tables/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.tables
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1tables~1{full_name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1tables~1{full_name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1tables~1{full_name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        exists:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1tables~1{full_name}~1exists/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/tables/methods/get'
          - $ref: '#/components/x-stackQL-resources/tables/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/tables/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/tables/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/tables/methods/delete'
        replace: []
    table_summaries:
      id: databricks_workspace.catalog.table_summaries
      name: table_summaries
      title: Table Summaries
      methods:
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1table-summaries/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.tables
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/table_summaries/methods/list'
        insert: []
        update: []
        delete: []
        replace: []
    temporary_path_credentials:
      id: databricks_workspace.catalog.temporary_path_credentials
      name: temporary_path_credentials
      title: Temporary Path Credentials
      methods:
        generate:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.0~1unity-catalog~1temporary-path-credentials/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select: []
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/temporary_path_credentials/methods/generate
        update: []
        delete: []
        replace: []
    temporary_table_credentials:
      id: databricks_workspace.catalog.temporary_table_credentials
      name: temporary_table_credentials
      title: Temporary Table Credentials
      methods:
        generate:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.0~1unity-catalog~1temporary-table-credentials/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select: []
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/temporary_table_credentials/methods/generate
        update: []
        delete: []
        replace: []
    temporary_volume_credentials:
      id: databricks_workspace.catalog.temporary_volume_credentials
      name: temporary_volume_credentials
      title: Temporary Volume Credentials
      methods:
        temporary_volume_credentials_generate_temporary_volume_credentials:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.0~1unity-catalog~1temporary-volume-credentials/post
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select: []
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/temporary_volume_credentials/methods/temporary_volume_credentials_generate_temporary_volume_credentials
        update: []
        delete: []
        replace: []
    volumes:
      id: databricks_workspace.catalog.volumes
      name: volumes
      title: Volumes
      methods:
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1volumes/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1volumes/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.volumes
        delete:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1volumes~1{name}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        read:
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1volumes~1{name}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1api~12.1~1unity-catalog~1volumes~1{name}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/volumes/methods/read'
          - $ref: '#/components/x-stackQL-resources/volumes/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/volumes/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/volumes/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/volumes/methods/delete'
        replace: []
    workspace_bindings:
      id: databricks_workspace.catalog.workspace_bindings
      name: workspace_bindings
      title: Workspace Bindings
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1workspace-bindings~1catalogs~1{name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1workspace-bindings~1catalogs~1{name}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get_bindings:
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1bindings~1{securable_type}~1{securable_name}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.bindings
        update_bindings:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: >-
              #/paths/~1api~12.1~1unity-catalog~1bindings~1{securable_type}~1{securable_name}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/workspace_bindings/methods/get_bindings
          - $ref: '#/components/x-stackQL-resources/workspace_bindings/methods/get'
        insert: []
        update:
          - $ref: >-
              #/components/x-stackQL-resources/workspace_bindings/methods/update_bindings
          - $ref: '#/components/x-stackQL-resources/workspace_bindings/methods/update'
        delete: []
        replace: []
x-stackQL-config:
  pagination:
    requestToken:
      key: page_token
      location: query
    responseToken:
      key: next_page_token
      location: body
